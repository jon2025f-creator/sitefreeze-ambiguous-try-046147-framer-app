import {
  Ac as mn,
  Ba as dl,
  Ca as Bn,
  Cf as Yr,
  Ea as ul,
  Ec as vl,
  Fa as Tt,
  Ga as Uo,
  Gf as Zr,
  Ha as fn,
  Hf as Nl,
  Ia as fl,
  Ic as $o,
  If as xl,
  Ja as $t,
  Jf as Dl,
  Kf as Rl,
  Nf as ei,
  Of as An,
  Pc as Sl,
  Qc as Cl,
  Qf as wl,
  Tf as El,
  Ub as pl,
  Vc as Il,
  Zf as hn,
  cg as Bl,
  dg as Fl,
  eg as Go,
  ha as Kr,
  hh as Ol,
  ig as Al,
  ih as $l,
  ja as ol,
  jg as Ul,
  jh as Ho,
  ka as Bo,
  kh as Jl,
  la as rl,
  lg as jo,
  lh as oi,
  ma as Fo,
  oc as hl,
  pa as il,
  qc as Oo,
  ra as Ce,
  sa as Ao,
  ta as al,
  ua as sl,
  uf as Ll,
  va as ll,
  vf as _o,
  vh as Kl,
  wa as cl,
  wh as Ql,
  ya as En,
} from "chunk-TT2V4UQ5.mjs";
import { m as Tl, o as ql } from "chunk-F7UZWEGK.mjs";
import { a as _l, b as Wl, c as Gl, d as jl } from "chunk-EKTMEAV7.mjs";
import { A as gl, B as yl, D as pn, K as Wo, O as _t, R as Hl } from "chunk-GRRUNQYU.mjs";
import {
  Ad as Ys,
  Gb as Js,
  H as Ot,
  Hb as Ks,
  K as $s,
  Nb as Mo,
  P as Xe,
  Qd as el,
  R as kn,
  S as wo,
  T as Vo,
  Vb as Qs,
  aa as un,
  ba as zo,
  ea as _s,
  je as Eo,
  nb as Ws,
  ne as tl,
  ob as Gs,
  pe as nl,
  qb as Hs,
  sb as qs,
  zd as Xs,
} from "chunk-IJTPKJSM.mjs";
import {
  Da as Ar,
  Md as Zs,
  Nd as ko,
  Pa as Bs,
  W as ze,
  X as ss,
  Y as ls,
  ea as kr,
  hb as Jr,
  jb as js,
  kb as $e,
} from "chunk-J3JRWALL.mjs";
import { d as Vl, m as zl, n as Pl, o as Ml, r as kl, s as ti } from "chunk-HCIRIA4F.mjs";
import { b as Po } from "chunk-SPYOA764.mjs";
import {
  $a as Ra,
  $g as It,
  $q as us,
  Aa as ba,
  As as Ss,
  Bo as Qe,
  Ew as ct,
  Fa as La,
  Fe as $a,
  Ha as Na,
  Js as Cs,
  Ke as vo,
  Kf as an,
  Lf as Ga,
  Ni as Xa,
  Of as Co,
  Pf as zr,
  Pv as Ms,
  Qa as $,
  Qf as sn,
  Qw as Fs,
  Ra as T,
  Rf as ja,
  Sa as xa,
  Sf as Ue,
  Si as Ya,
  Ta as B,
  Va as F,
  Vf as qa,
  Vn as as,
  Wa as p,
  Xa as Re,
  Xf as Ja,
  Ya as Ke,
  Yf as Ka,
  Zi as Za,
  _v as pe,
  _w as As,
  ab as wa,
  ag as Pr,
  aq as I,
  br as fs,
  bx as Us,
  cq as To,
  cr as Ft,
  dq as cs,
  dr as ps,
  e as na,
  fj as es,
  fr as ee,
  gr as Er,
  id as Ma,
  ig as Mr,
  ij as ts,
  jb as Va,
  jg as zn,
  jr as ms,
  kg as Qa,
  lq as ds,
  ls as ln,
  mr as Lo,
  ms as hs,
  nj as Oe,
  ns as gs,
  nt as Ur,
  nv as Pn,
  ov as ws,
  pc as za,
  pr as No,
  pu as At,
  py as Os,
  qr as xo,
  qv as Mn,
  rv as Vs,
  rw as qr,
  sc as Pa,
  ss as Fr,
  tv as jr,
  uj as ns,
  us as ys,
  ut as Is,
  uv as zs,
  va as Ia,
  vt as Or,
  vv as Hr,
  wt as $r,
  wv as Ps,
  xk as Bt,
  xt as bs,
  ys as vs,
  yt as _r,
  zf as St,
  zx as Ie,
} from "chunk-IFNNE4C3.mjs";
import { a as Br } from "chunk-PU3NKHRX.mjs";
import {
  Ba as He,
  Bp as Rs,
  Ca as J,
  Cd as Ve,
  Dd as pa,
  Df as ma,
  Dp as dn,
  E as Nr,
  Ef as ha,
  Em as Se,
  Fi as Sa,
  Kk as go,
  Km as Io,
  Ma as he,
  Mj as ka,
  Mm as is,
  Mo as Ut,
  Nd as po,
  Ni as Ca,
  Nj as kt,
  Nm as bo,
  Od as st,
  Pj as Vn,
  Qd as mo,
  Qj as Ea,
  Qk as yo,
  Qo as Ro,
  Rb as sa,
  Rj as rn,
  Tb as la,
  Tc as Mt,
  To as Ts,
  Uj as Ba,
  Uo as bt,
  Vb as nn,
  Vc as fa,
  Vd as De,
  Vk as Et,
  Vo as Te,
  Wb as ca,
  We as wr,
  Wk as Wa,
  Xj as Fa,
  Yk as So,
  _a as ge,
  aj as Ta,
  ak as Aa,
  al as lt,
  ap as Wr,
  bb as tn,
  ec as Je,
  fc as Rr,
  fk as Ua,
  hb as oa,
  hj as Da,
  ho as Do,
  ib as ae,
  jb as K,
  kb as ye,
  kk as Oa,
  lb as x,
  ll as Ha,
  m as ro,
  mb as Ae,
  mc as uo,
  nb as xe,
  np as Ls,
  ob as wn,
  oi as Vr,
  qb as ra,
  qi as ya,
  qp as cn,
  ra as ao,
  rb as ve,
  ri as va,
  sa as Xi,
  sb as co,
  sh as on,
  si as ho,
  ta as vt,
  tp as Ns,
  ua as it,
  ub as ia,
  ug as ga,
  va as Yi,
  vb as W,
  vc as Pt,
  vd as fo,
  vm as os,
  wa as Zi,
  wb as qe,
  wc as da,
  wp as xs,
  xp as Ds,
  ya as ea,
  za as ie,
  zb as aa,
  zm as rs,
  zp as Gr,
} from "chunk-7VNTNHPA.mjs";
import { c as Fn } from "chunk-2UKNUJIZ.mjs";
import { a as ml } from "chunk-K6L5GVTR.mjs";
import { a as Jo } from "chunk-4HRYW54D.mjs";
import { b as Xl } from "chunk-CL6NHK4I.mjs";
import { a as qo } from "chunk-7WJ42D6X.mjs";
import { a as Es } from "chunk-GBD3AMAR.mjs";
import { a as ks } from "chunk-MYL546Y2.mjs";
import { f as Pe } from "chunk-OTBEGHUQ.mjs";
import { a as ni } from "chunk-24G7FGVN.mjs";
import { W as Qi, Z as ua, ka as Ct } from "chunk-HFUJNNAN.mjs";
import { b as bl, d as Qr, e as Xr } from "chunk-GBWZWM2Q.mjs";
import { b as _a } from "chunk-I27XAJRL.mjs";
import { a as je } from "chunk-7BWPFUCH.mjs";
import { a as io } from "chunk-2FCXHKEL.mjs";
import { i as ta, m as R, q as O } from "chunk-ZB6LWF4P.mjs";
import {
  a as Rn,
  b as L,
  d as xr,
  e as A,
  f as Ne,
  h as Dr,
  k as so,
  m as _,
  p as at,
  s as lo,
} from "chunk-LA34HORX.mjs";
import { a as Ki } from "chunk-SWYZG2NI.mjs";
import { b, c as Z } from "chunk-4JY5UMT2.mjs";
import { k as Lr, l as yt } from "chunk-MQDP3TYU.mjs";
import { c as Ji } from "chunk-3HBIYPN4.mjs";
import { b as qi, i as oe } from "chunk-VJ7UYMJI.mjs";
import { a as Tr } from "chunk-YRQ7G4QH.mjs";
import { e as en, p as xn, q as Dn } from "chunk-WLHSDIGQ.mjs";
function ri({ identifier: t, file: e, error: n, custom: o }) {
  let r = ou(e);
  return {
    depth: t.startsWith(".") ? 0 : 1,
    error: n,
    file: e,
    identifier: t,
    name: r,
    packageIdentifier: "<unknown>",
    properties: {},
    type: "component",
    update: 0,
    ...o,
  };
}
function ou(t) {
  return t.startsWith("./") ? t.slice(2) : t;
}
var Yl = oe("componentloader");
function Zl(t) {
  return R(
    ee({
      type: t.type,
      name: t.name,
      title: "title" in t ? t.title : void 0,
      annotations: t.annotations,
      properties: t.properties,
    })
  );
}
var ec = class {
  environment = "editor";
  activeBundleHash = void 0;
  modulesRevision = -1;
  entities = new Map();
  definitionHashes = new Map();
  readTrackers = new Set();
  constructor(e) {
    e &&
      ((this.entities = new Map(e.entities)),
      (this.definitionHashes = new Map(e.definitionHashes)),
      (this.activeBundleHash = e.activeBundleHash),
      (this.modulesRevision = e.modulesRevision));
  }
  localPackageIdentifier() {
    return "|local|";
  }
  getAllEntities() {
    return this.entities.values();
  }
  *getAllLocalModules() {
    for (let e of this.entities.values()) e.packageIdentifier === "|local|" && (yield e);
  }
  setModuleRevision(e, n) {
    ((this.activeBundleHash = `${e}_${n}`), (this.modulesRevision = e));
  }
  updateEntity(e, n) {
    (Yl.trace("updateEntity:", e, n),
      b(n.properties, () => `Entities must have properties: ${JSON.stringify(n)}`),
      this.entities.set(e, n),
      this.definitionHashes.set(e, Zl(n)));
  }
  deleteEntity(e) {
    (Yl.trace("deleteEntity:", e), this.entities.delete(e), this.definitionHashes.delete(e));
  }
  componentForIdentifier(e) {
    for (let n of this.readTrackers) n.add(e);
    return this.entities.get(e) ?? null;
  }
  getDefinitionHash(e) {
    return this.definitionHashes.get(e) ?? 0;
  }
  trackEntityReads(e, n) {
    this.readTrackers.add(e);
    try {
      return n();
    } finally {
      this.readTrackers.delete(e);
    }
  }
  reactComponentForIdentifier(e) {
    let n = this.componentForIdentifier(e);
    return !n || !Dl(n) ? null : n;
  }
  shaderForIdentifier(e) {
    let n = this.componentForIdentifier(e);
    return !n || !xl(n) ? null : n;
  }
  getPropertyControlsForIdentifier(e) {
    return this.componentForIdentifier(e)?.properties ?? null;
  }
  actionForIdentifier(e) {
    let n = this.componentForIdentifier(e);
    return !n || !Rl(n) ? null : n;
  }
  dataForIdentifier(e) {
    if (!e) return null;
    let n = this.componentForIdentifier(e);
    return !n || !ei(n) ? null : n;
  }
  getData() {
    let e = [];
    for (let n of this.entities.values()) ei(n) && e.push(n);
    return e;
  }
  errorForIdentifier(e) {
    let n = this.entities.get(e);
    if (n) return An(n) ? n : null;
    let o = J(e);
    if (!o)
      return ri({ identifier: e, file: e, error: "Components that are not modules do not exist." });
    let r = ao(o),
      i = o.value === r,
      a = this.entities.get(r);
    if (An(a)) return a;
    if (vt(o) || i) {
      let s = "Component does not exist.";
      return (
        vt(o) && o.type === "codeFile" && (s = "Component file does not exist."),
        ri({ identifier: e, file: o.value, error: s })
      );
    }
    return null;
  }
  testing = {
    setEntity: (e) => {
      (this.entities.set(e.identifier, e),
        this.definitionHashes.set(e.identifier, Zl(e)),
        (this.activeBundleHash = Math.random().toString(36).substring(2)));
    },
    clearEntities: () => {
      (this.entities.clear(), this.definitionHashes.clear(), (this.activeBundleHash = void 0));
    },
  };
};
function tc(t) {
  return new Promise((e) => {
    setTimeout(e, t);
  });
}
var ru = crypto.randomUUID();
function nc() {
  return ru;
}
var Un = new Set();
function iu(t) {
  let e = au();
  e.length !== 0 &&
    (Pe("before_unload_prompt", {
      blockers: e.join(","),
      hadUserActivation: navigator.userActivation?.hasBeenActive,
      tabSessionId: nc(),
    }),
    t.preventDefault(),
    (t.returnValue = "You have unsaved changes. They might be lost if you leave the page."),
    rc());
}
window.addEventListener("beforeunload", iu);
function oc(t, e) {
  (Un.add(t), e?.addEventListener("abort", () => Un.delete(t)));
}
function rc() {
  let t = [];
  for (let e of Un)
    if (e.check())
      try {
        e.effect?.();
      } catch (n) {
        t.push(n);
      }
  if (t.length === 1) throw t[0];
  if (t.length > 0) throw new AggregateError(t, "Unhandled errors in beforeunload handlers");
}
function au() {
  let t = [];
  for (let e of Un) e.check() && t.push(e.name);
  return t.sort();
}
function su() {
  for (let t of Un) if (t.check()) return !0;
  return !1;
}
async function Vh({ timeout: t = 3e4, polling: e = 50 } = {}) {
  rc();
  let n = Date.now() + t;
  for (; Date.now() < n;) {
    if (!su()) return !0;
    await tc(e);
  }
  return !1;
}
function ic(t) {
  return Number.isFinite(t);
}
function ac() {}
var gn = class {
  #e;
  #t = 0;
  #n;
  #u = !0;
  #a;
  #l = ac;
  constructor({ delay: e, maxDelay: n, task: o, abortSignal: r }) {
    ((this.#t = e),
      (this.#n = n),
      (this.#l = o),
      r?.addEventListener(
        "abort",
        () => {
          (this.#e && (clearTimeout(this.#e), (this.#e = void 0)),
            (this.#a = void 0),
            (this.#l = ac),
            (this.#u = !1));
        },
        { once: !0 }
      ));
  }
  set enabled(e) {
    this.#u = e;
  }
  set delay(e) {
    this.#t = e;
  }
  cancel() {
    (this.#e && (clearTimeout(this.#e), (this.#e = void 0)), (this.#a = void 0));
  }
  debounce(e = this.#t) {
    if (!this.#u) return;
    (this.#e && clearTimeout(this.#e),
      ic(this.#n) && ic(this.#a) && performance.now() - this.#a > this.#n
        ? ((this.#e = void 0), (this.#a = void 0), this.#l())
        : ((this.#e = window.setTimeout(() => {
            ((this.#e = void 0), (this.#a = void 0), this.#l());
          }, e)),
          (this.#a = this.#a ?? performance.now())));
  }
};
var lu = oe("CodeGenerationStore"),
  cu = "transform",
  Ko = class {
    constructor(e) {
      this.sourceNode = e;
    }
    sourceNode;
    #e = [];
    #t = void 0;
    start(e) {
      let n = performance.now();
      return {
        end: (o) => {
          (this.#e.push([e, performance.now() - n]),
            (this.#t = this.#t ?? o),
            e === cu && this.completeSession());
        },
      };
    }
    restartSession() {
      ((this.#e = []), (this.#t = void 0));
    }
    completeSession() {
      (this.#t?.forEach(({ type: e, source: n, artifacts: o }) => {
        let r = du(e, this.sourceNode);
        if (!r) return;
        let i = {
          codeType: r,
          durationMs: 0,
          updateCount: 0,
          updateDurationMs: 0,
          serializationDurationMs: 0,
          compilationDurationMs: 0,
          synchronizationDurationMs: 0,
          size: n.length,
          id: this.sourceNode.id,
          nodes: o?.metrics?.nodes ?? 1,
          variants: o?.metrics?.variants ?? 0,
          svgBytes: o?.metrics?.svgBytes,
          textBytes: o?.metrics?.textBytes,
        };
        for (let [s, l] of this.#e)
          switch (((i.durationMs += l), s)) {
            case "update":
              (i.updateCount++, (i.updateDurationMs += l));
              break;
            case "serialize":
              i.serializationDurationMs += l;
              break;
            case "transform":
              i.compilationDurationMs += l;
              break;
            case "synchronize":
              i.synchronizationDurationMs += l;
              break;
            default:
              Z(s);
          }
        let a;
        for (a in i) {
          let s = i[a];
          A(s) && (i[a] = Math.round(s));
        }
        (lu.trace("Code Generation Metrics", i), Pe("code_generation", i));
      }),
        this.restartSession());
    }
  };
function du(t, e) {
  switch (t) {
    case "canvasComponent":
      return "smart_component";
    case "css":
      return "styles_preset";
    case "collection":
      return "collection";
    case "draftCollection":
      return "draft_collection";
    case "componentPresets":
      return "component_presets";
    case "screen":
      return x(e) ? "web_page" : "prototype_screen";
    case "prototype":
      return "prototype";
    case "vector":
      return "vector";
    case "vectorSet":
      return "vector_set";
    case "snippets":
      return "snippets";
    case "codeFile":
    case "config":
    case "siteMetadata":
    case "webPageMetadata":
    case "layoutTemplate":
    case "localization":
    case "design":
    case "kit":
    case "shader":
      return;
    default:
      Z(t);
  }
}
var uu = oe("jsdoc");
function Ye(t, e = {}) {
  let n = "/**";
  t &&
    (n += `
 * ${t}`);
  for (let o in e) {
    let r = e[o];
    if (!_(r) && fu(o, r))
      if (
        ((n += `
 * @${o}`),
        L(r))
      ) {
        let i = r.trim();
        i && (n += ` ${i}`);
      } else (Rn(r) || A(r)) && (n += ` ${r}`);
  }
  return new T(
    n +
      `
 */`
  );
}
function fu(t, e) {
  return Rn(e) || A(e) || (L(e) && e.trim() !== "")
    ? !0
    : (uu.reportError(
        new Error(
          `Annotation ${t} must be a boolean, number, or non-empty string, but found "${e}" it will be omitted from the generated code. Use undefined if intentional.`
        )
      ),
      !1);
}
function pu(t, e) {
  let n = {};
  if (!e || cn(t)) return n;
  for (let o of t.cases)
    for (let r of e) {
      let i = o.nameLocalized;
      if (!i) continue;
      let a = i[r.id]?.value;
      if (!L(a)) continue;
      let s = n[o.id] ?? {};
      ((n[o.id] = s), (s[r.id] = a));
    }
  return n;
}
function sc(t, e, n) {
  if (t.cases.length === 0) return p`(value) => value`;
  let o = pu(t, e),
    r = p`value`,
    i = p`locale`,
    a = p`fallbackLocale`;
  return new T(
    F.lines(
      p`(${r}, ${i}) => {`,
      p`const ${a} = ${i}?.fallback;`,
      p`switch (${r}) {`,
      ...t.cases.map(({ id: s, name: l }) => {
        let c = o[s];
        return c
          ? new T(
              F.lines(
                p`case ${s}:`,
                p`switch (${i}?.id) {`,
                ...Object.entries(c).map(([f, d]) => p`case ${f}: return ${d};`),
                p`default:`,
                p`if (${a}) return ${T.fn(n, r, a)};`,
                p`return ${l};`,
                p`}`
              )
            )
          : p`case ${s}: return ${l};`;
      }),
      p`default: return "";`,
      p`}`,
      p`}`
    )
  );
}
var ii = "framercms";
function lc(t, e, n) {
  return `${t}-${e}.${n}`;
}
var Qo = class {
  constructor(e) {
    this.componentSourceNodeId = e;
  }
  componentSourceNodeId;
  assets = {};
  create(e, n, o) {
    let r = lc(this.componentSourceNodeId, e, n);
    return ((this.assets[r] = o), p`new URL(${`./${r}`}, import.meta.url).href`);
  }
};
function ai(t) {
  return `on${t}Change`;
}
function cc(t, e) {
  let n =
      t.itemVariables.length === 1 && t.itemVariables[0]?.type === "image"
        ? t.itemVariables[0]
        : void 0,
    o = p`item`,
    r = { id: p`typeof ${o}?.[${"id"}] === "string" ? ${o}[${"id"}] : undefined` };
  for (let a of t.itemVariables) {
    let s = To(a.name);
    s ? (r[a.id] = p`${o}?.[${s}] ?? ${o}?.[${a.id}]`) : (r[a.id] = p`${o}?.[${a.id}]`);
  }
  let i = e.bindings.create("arrayFromHumanReadable" + t.id);
  return (
    e.declarations.file.create(
      p.joinLines(
        p`const ${i} = (value) => {${p.joinLines(p`if (!Array.isArray(value)) return`, p`return value.map(${o} => {${p.joinLines(n ? p`if (typeof ${o} === "string") return { [${n.id}]: ${o} }` : void 0, p`return ${r}`)}})`)}}`
      )
    ),
    i
  );
}
function hu(t) {
  return [t[1], t[0]];
}
var gu = new Map(Object.entries(he).map(hu));
function Ze(t, e) {
  let n = e.addImport("framer", { exportSpecifier: "ControlType" }),
    o = gu.get(t),
    r = new T(o);
  return p`${n}.${r}`;
}
var yu = {
  boolean: "boolean",
  number: "number",
  dimension: "string",
  string: "string",
  richtext: "string",
  fusednumber: "any",
  enum: "string",
  segmentedenum: "string",
  color: "string",
  image: "string",
  responsiveimage: "{src: string; srcSet?: string; alt?: string}",
  file: "string",
  componentinstance: "any",
  slot: "any",
  array: "any[]",
  eventhandler: "any",
  changehandler: "(value: any) => void",
  transition: "Record<string, any>",
  link: "string",
  linkrelvalues: "string[] | undefined",
  pagescope: "string",
  date: "string",
  object: "Record<string, any>",
  font: "Record<string, any> | undefined",
  scrollsectionref: "React.MutableRefObject<HTMLElement>",
  customcursor: "string",
  border: "Record<string, any>",
  cursor: "string",
  boxshadow: "string",
  gap: "string",
  padding: "string",
  borderradius: "string",
  collectionreference: "string | undefined",
  multicollectionreference: "string[] | undefined",
  trackingid: "string | undefined",
  vectorsetitem: "React.ComponentType<any> | undefined",
  location: "Record<string, any>",
};
function vu(t) {
  return t ? new T(yu[t]) : p`any`;
}
var fc = "variant";
function Su(t) {
  return !_(t?.defaultValue);
}
function Cu(t, e, n) {
  if (!ae(t)) return {};
  b(t.isLoaded(), "Node must be loaded to generate controls");
  let o = t.getTopLevelVariants();
  if (o?.length < 2) return {};
  let r = [],
    i = [];
  for (let a of o) {
    let s = a.resolveValue("name") ?? $e(e, a);
    (r.push(a.id), i.push(s));
  }
  return { [fc]: { type: Ze("enum", n), title: "Variant", options: r, optionTitles: i } };
}
function Iu(t, e, n, o) {
  if (t.type === "responsiveimage" && _r(t) && $r(t.__vekterDefault)) {
    let { assetReference: r, alt: i } = t.__vekterDefault,
      a = n.assets?.resolveResponsiveImage(r, { focalPoint: t.__vekterDefault });
    return (a && L(i) && (a.alt = i), a);
  } else if (Or(t))
    switch (t.type) {
      case "responsiveimage": {
        let r = n.assets?.resolveResponsiveImage(t.__defaultAssetReference);
        return r || void 0;
      }
      case "file": {
        let r = n.assets?.resolve(t.__defaultAssetReference);
        return r ? r.src : void 0;
      }
    }
  else {
    if (e?.type === "array")
      return e.initialValue
        ? e.initialValue.map((r) => {
            let i = {};
            if (!r || r.type !== "object" || !Ne(r.value)) return i;
            for (let a of e.itemVariables) {
              if (!Te(a)) continue;
              let s = r.value[a.id],
                l = dc(s, n, o);
              (_(l) && !bu(s) && (l = dc(Ns(e, a, o), n, o)), _(l) || (i[a.id] = l));
            }
            return i;
          })
        : [];
    if (Su(t)) {
      if (e && bt(e, o)) return;
      let r = t.defaultValue;
      if (t.type === "vectorsetitem")
        return (
          b(gl(r), "Vector set item must be a module export identifier"),
          n.imports.addModuleImport(r.identifier)
        );
      if (t.type === "richtext" && L(r))
        return ti(r, { collectors: n, useFramerMotion: o === "SmartComponent" });
      if (t.type === "boxshadow" && vs(r))
        return Ss(r, "emptyStringWhenEmpty", (i) => (yo(i) ? go(i) : Nr.toFormatString(i)));
      if (Er(r)) return yo(r) ? go(r) : r;
    }
  }
}
function dc(t, e, n) {
  if (t && fa(t.type))
    switch (t.type) {
      case "image": {
        if (!L(t.value)) return;
        let o = e.assets?.resolveResponsiveImage(t.value, { focalPoint: t });
        return o ? ((o.alt = t.alt ?? ""), o) : void 0;
      }
      case "boolean":
        return Rn(t.value) ? t.value : void 0;
      case "number":
        return A(t.value) ? t.value : void 0;
      case "string":
      case "date":
        return L(t.value) ? t.value : void 0;
      case "file":
        return L(t.value) ? e.assets?.resolve(t.value)?.src : void 0;
      case "richtext":
        return !Se(t.value) || _t(t.value)
          ? void 0
          : ti(t.value, { collectors: e, useFramerMotion: n === "SmartComponent" });
      case "link":
        return e.links?.create(So(t.value));
      case "color":
        return L(t.value) ? (yo(t.value) ? go(t.value) : Nr.toFormatString(t.value)) : void 0;
      case "vectorsetitem":
        return !L(t.value) || !it(t.value) ? void 0 : e.imports.addModuleImport(t.value);
      default:
        Z(t.type);
    }
}
function bu(t) {
  return t?.type !== "richtext" ? !1 : Se(t.value) && _t(t.value);
}
function Xo(t, e) {
  return p`${t}?: ${e};`;
}
var pc = ["id", "width", "height"],
  Tu = Object.fromEntries(
    pc.map((t) => {
      let e = new T(t);
      return [e, e];
    })
  );
function Lu(t, e) {
  let n = new B(t);
  return (B.addSpreadAtEnd(n, e), n);
}
var mc = [
  { name: I.style, type: p`Record<string, unknown>` },
  { name: I.className, type: p`string` },
  { name: I.layoutId, type: p`string | number` },
];
function Nu(t, e) {
  if (!ae(t) && !x(t) && !K(t) && !ye(t)) return [];
  let n = mc.map(({ name: r, type: i }) => ({ key: $(r), type: Xo(r, i) })),
    o = $(I.variant);
  return e && Oe(t)
    ? n.concat([
        {
          key: o,
          type: Xo(I.variant, p`keyof typeof ${e}`),
          value: p`${e}[props.${I.variant}] ?? props.${I.variant} ?? ${t.baseVariantId}`,
        },
      ])
    : n.concat([{ key: o }]);
}
function li(t, e, n) {
  let o = p`props.${new T(t)}`;
  return (e && (o = p`${e} ?? ${o}`), _(n) || (o = p`${o} ?? ${n}`), o);
}
function hc(t, e, n) {
  if (!x(t) || !Xe(n, t)) return;
  let r = t.getProvidedVariableMap(n, void 0, e, e.activeBundleHash);
  if (!r) return;
  let i = new Map();
  for (let [a, s] of r) (x(t) && t.hasVariable(a)) || i.set(a, s);
  if (i.size !== 0) return i;
}
var xu = [...fs, fc, ...pc, ...mc.map(({ name: t }) => t.toString()), "name"];
function Du(t, e, n, o, r) {
  let i = new Set(xu),
    a = Object.entries(t).map(([C, { control: S, variable: w, binding: N }]) => {
      if (!S) return { key: C, binding: N };
      let D = ps(To(S.title) ?? C, i),
        V = new T(D),
        z = Iu(S, w, n, o),
        P = Xo(V, vu(S.type)),
        M;
      if (S.type === "enum" && S.options && S.optionTitles && V) {
        let ne = new B();
        S.options.forEach((j, Q) => {
          if (!L(j) || !S.optionTitles) return;
          let me = S.optionTitles[Q];
          L(me) && (ne[me] = j);
        });
        let re = n.declarations.file.dedupe("humanReadableEnumMap", ne);
        ((P = Xo(V, p`keyof typeof ${re}`)), (M = li(C, V, z)), (M = p`${re}[${V}] ?? ${M}`));
      } else if (w?.type === "array") {
        let ne = cc(w, n),
          re = T.fn(ne, V);
        M = li(C, re, z);
      } else M = li(C, V, z);
      return { key: C, binding: N, name: D, type: P, value: M };
    }),
    s = "",
    l = [],
    c = [],
    f = new B(),
    d = hc(e, n.componentLoader, n.tree),
    u = {},
    m = { ...Tu },
    h = Nu(e, r);
  for (let { key: C, binding: S, type: w, value: N, name: D } of [...h, ...a]) {
    let V = d?.get(C),
      z = S ?? new T(C);
    if (V) {
      let P = p`${z} = ${I.currentRouteData}[${V.id}]`;
      if (ka(V.type)) {
        let M = Ro(V.type);
        Er(M) && (P = p`${P} ?? ${M}`);
      }
      l.push(P);
    } else {
      let P = n.variables.getSetterBinding(C);
      if (P) {
        let M = ai(C),
          ne = $(z),
          re = n.bindings.create(`external${ne}`),
          j = n.bindings.create(`setExternal${ne}`);
        (l.push(p`${C}: ${re}`), l.push(p`${M}: ${j}`));
        let Q = n.declarations.file.dedupe(
          "useStateVariable",
          p.joinLines(
            p`(externalState, setExternalState) => {`,
            p`    const [internalState, setInternalState] = React.useState(externalState)`,
            p`    const [previousExternalState, setPreviousExternalState] = React.useState(externalState)`,
            p`    if (setExternalState) {`,
            p`        return [externalState, setExternalState]`,
            p`    }`,
            p`    if (externalState !== previousExternalState) {`,
            p`        setInternalState(externalState)`,
            p`        setPreviousExternalState(externalState)`,
            p`    }`,
            p`    return [internalState, setInternalState]`,
            p`}`
          )
        );
        c.push(p`const [${z}, ${P}] = ${T.fn(Q, re, j)}`);
      } else $(z) !== C ? l.push(p`${C}: ${z}`) : l.push(z);
    }
    (w && (s += w), N && (f[C] = N), D && ((u[C] = D), (m[D] = new T(D))));
  }
  (o === "LayoutTemplate" && l.push(I.children),
    o === "DesignPage" && l.push(I.style),
    B.addSpreadAtStart(f, I.props));
  let g = n.declarations.file.dedupe("getProps", p`(${Lu(m, I.props)}) => { return ${f} }`),
    y = l.join(", "),
    v = y.length > 0 ? new T(`{ ${y}, ...${I.restProps} }`) : I.restProps;
  return {
    variableMap: St(u) ? void 0 : u,
    types: new T(s),
    props: p.joinSections(p`const ${v} = ${T.fn(g, I.props)}`, ...c),
  };
}
function Ru(t, e) {
  let n = [],
    o = ye(t),
    r = gc(t),
    i = e.variables.getBindings(t.id);
  for (let s of r) {
    if (!Te(s)) continue;
    let l = i.get(s.id),
      c = e.variables.getSetterBinding(s.id);
    (!o && !l && !c) || n.push({ id: s.id, binding: l, definition: s });
  }
  let a = hc(t, e.componentLoader, e.tree);
  if (a)
    for (let [s] of a) {
      let l = i.get(s);
      l && n.push({ id: s, binding: l });
    }
  return n;
}
function wu(t, e) {
  let { hidden: n, ...o } = t;
  if (!e) return o;
  let { description: r, ...i } = o;
  return i;
}
function Vu(t, e, n, o, r, i, a, s, l) {
  let { id: c, type: f } = e;
  switch (f) {
    case "controlReference": {
      let d = J(xs(o, e.entityIdentifier));
      if (!d || !e.entityIdentifier || !it(d)) return;
      let u = r.addModuleImport(d, {
        composeWithImports: [r.addImport("framer", { exportSpecifier: "getPropertyControls" })],
      });
      if (!u) return;
      let m = new B({ hidden: p`undefined`, description: p`undefined` });
      return (
        B.addSpreadAtStart(m, p`${u}[${e.controlKey}]`),
        L(n.title) && (m.title = n.title),
        L(n.description) && (m.description = n.description),
        Ts(e, l) && (m.optional = bt(e, l) ? !0 : p`undefined`),
        n.type === "responsiveimage" &&
          _r(n) &&
          $r(n.__vekterDefault) &&
          (m[bs] = T.fromValue(n.__vekterDefault)),
        bt(e, l)
          ? (m.defaultValue = p`undefined`)
          : Or(n)
            ? (m[Is] = T.fromValue(n.__defaultAssetReference))
            : lo(e.initialValue) || (m.defaultValue = T.fromValue(e.initialValue)),
        p`${u}?.[${e.controlKey}] && ${m}`
      );
    }
    case "nodePropertyControlReference":
    case "array":
    case "vectorsetitem":
    case "boolean":
    case "color":
    case "date":
    case "enum":
    case "eventhandler":
    case "file":
    case "image":
    case "link":
    case "linkrelvalues":
    case "number":
    case "richtext":
    case "scrollsectionref":
    case "customcursor":
    case "cursor":
    case "transition":
    case "boxshadow":
    case "string":
    case "border":
    case "borderradius":
    case "gap":
    case "padding":
    case "collectionreference":
    case "multicollectionreference":
    case "trackingid":
    case "location":
    case "slug": {
      let d = { ...wu(n, a), type: Ze(n.type, r) };
      return (s === "vector" && i && (d.hidden = !i.getBinding(t.id, c)), d);
    }
    default:
      Z(f);
  }
}
function gc(t) {
  return dn(t) ? t.variables : [];
}
function ci(t, e, n, o, r, i, a, s = gc(t)) {
  let l = Cu(t, e, o),
    c = i === "canvasComponent",
    f = Da(t),
    d = [...s];
  if (W(t)) {
    let u = zs();
    d.push(u);
    let m = Ps();
    d.push(m);
    let h = ws(t);
    d.push(h);
    let g = Vs(t);
    d.push(g);
  }
  for (let u of d) {
    if (!Te(u)) continue;
    let m = Gr(n, e, u, a);
    if (at(m)) continue;
    b(m.type !== "image", "Image variables should become responsive images");
    let h = Vu(t, u, m, n, o, r, f, i, a);
    if (_(h) || ((l[u.id] = h), !c) || !wl(u)) continue;
    let g = { type: "changehandler", changes: u.id };
    l[ai(u.id)] = { ...g, type: Ze("changehandler", o) };
  }
  if (!St(l)) return new B(l, 1);
}
function Yo(t, e, n, o, r) {
  let i = {},
    a = [];
  for (let { id: s, binding: l, definition: c } of Ru(t, e)) {
    if (c && x(t) && t.hasVariable(c.id)) continue;
    let d = c ? Gr(e.tree, e.componentLoader, c, n) : void 0;
    ((i[s] = { control: d ?? void 0, variable: c, binding: l }), c && a.push(c));
  }
  return {
    propertyControls: ci(t, e.componentLoader, e.tree, e.imports, e.variables, o, n, a),
    ...Du(i, t, e, n, r),
  };
}
var yc = en(Ki(), 1);
function X(t, ...e) {
  if (!t) throw Error("Assertion Error" + (e.length > 0 ? ": " + e.join(" ") : ""));
}
function On(t) {
  throw new Error(`Unexpected value: ${t}`);
}
var zu = en(io(), 1);
var vc = `// src/code-generation/components/cms/bundled/getRichTextJsonResolver.tsx?bundle
import { AutoBreakpointVariant, ComponentPresetsConsumer, Link, motion } from "framer";

// ../../library/src/router/lazy.tsx
import { forwardRef, isValidElement, useEffect } from "react";

// ../../library/src/utils/utils.ts
var isWindow = typeof window !== "undefined";
var supportsRequestIdleCallback = isWindow && typeof window.requestIdleCallback === "function";

// ../../library/src/router/lazy.tsx
var preloadKey = "preload";
function isLazyComponentType(componentType) {
  return typeof componentType === "object" && componentType !== null && !isValidElement(componentType) && preloadKey in componentType;
}

// src/code-generation/components/cms/bundled/getRichTextJsonResolver.tsx?bundle
import { Fragment, createElement } from "react";

// src/code-generation/components/cms/bundled/assert.ts
function assert(condition, ...msg) {
  if (condition) return;
  throw Error("Assertion Error" + (msg.length > 0 ? ": " + msg.join(" ") : ""));
}

// src/code-generation/components/cms/bundled/getRichTextJsonResolver.tsx?bundle
var RichTextJsonType = /* @__PURE__ */ ((RichTextJsonType2) => {
  RichTextJsonType2[RichTextJsonType2["Fragment"] = 1] = "Fragment";
  RichTextJsonType2[RichTextJsonType2["Link"] = 2] = "Link";
  RichTextJsonType2[RichTextJsonType2["Module"] = 3] = "Module";
  RichTextJsonType2[RichTextJsonType2["Tag"] = 4] = "Tag";
  RichTextJsonType2[RichTextJsonType2["Text"] = 5] = "Text";
  return RichTextJsonType2;
})(RichTextJsonType || {});
function getRichTextJsonResolver(components) {
  const cache = /* @__PURE__ */ new Map();
  function deserializeChildren(children) {
    return children.map(deserializeNode);
  }
  function deserializeNode(node) {
    switch (node[0]) {
      case 1 /* Fragment */: {
        const [, ...children] = node;
        const childNodes = deserializeChildren(children);
        return createElement(Fragment, void 0, ...childNodes);
      }
      case 2 /* Link */: {
        const [, props, ...children] = node;
        const childNodes = deserializeChildren(children);
        return createElement(Link, props, ...childNodes);
      }
      case 3 /* Module */: {
        const [, identifier, props, richTextKeys, vectorSetItemKeys] = node;
        for (const key of richTextKeys) {
          const value = props[key];
          if (!value) continue;
          props[key] = deserializeNode(value);
        }
        for (const key of vectorSetItemKeys) {
          const value = props[key];
          if (typeof value !== "string") continue;
          const VectorSetItem = components[value];
          if (!VectorSetItem) continue;
          if (isLazyComponentType(VectorSetItem)) {
            void VectorSetItem.preload();
          }
          props[key] = VectorSetItem;
        }
        const Component = components[identifier];
        assert(Component, "Module not found");
        if (isLazyComponentType(Component)) {
          void Component.preload();
        }
        return <ComponentPresetsConsumer componentIdentifier={identifier}>
						{(presetProps) => <AutoBreakpointVariant component={Component} props={{ ...presetProps, ...props }} />}
					</ComponentPresetsConsumer>;
      }
      case 4 /* Tag */: {
        const [, tag, props, ...children] = node;
        const childNodes = deserializeChildren(children);
        if (tag === "a") {
          return createElement(motion.a, props, ...childNodes);
        }
        return createElement(tag, props, ...childNodes);
      }
      case 5 /* Text */: {
        const [, text] = node;
        return text;
      }
    }
  }
  return (pointer) => {
    const cached = cache.get(pointer);
    if (cached) return cached;
    const json = JSON.parse(pointer);
    const result = deserializeNode(json);
    cache.set(pointer, result);
    return result;
  };
}
export {
  RichTextJsonType,
  getRichTextJsonResolver
};
`;
var Zo = class {
  constructor(e, n, o, r, i, a, s, l, c) {
    this.componentLoader = n;
    this.tree = o;
    this.imports = r;
    this.submodules = i;
    this.bindings = a;
    this.declarations = s;
    this.assets = l;
    this.links = c;
    this.resolveRichTextBinding = this.bindings.create("resolveRichText");
    let f = this.tree.getScopeNodeFor(e);
    ((this.options = { nodeId: e.id, scopeId: f?.id }),
      (this.resolvers = kl(this.assets, this.links, void 0, void 0)));
  }
  componentLoader;
  tree;
  imports;
  submodules;
  bindings;
  declarations;
  assets;
  links;
  cache = new Map();
  components = {};
  options;
  resolvers;
  serializers = {
    fragment: (e) => [1, ...e],
    link: (e, n) => [2, e, ...n],
    module: (e, n, o, r) => {
      if (!this.addModuleImport(e)) return;
      let a = this.componentLoader.reactComponentForIdentifier(e);
      b(a, "Component must exist for module");
      let s = zl(
          this.tree,
          this.componentLoader,
          e,
          n,
          this.resolvers,
          this.serializers,
          this.options
        ),
        l = [],
        c = [];
      for (let m in a.properties) {
        let h = a.properties[m];
        if (h) {
          if (h.type === "richtext") l.push(m);
          else if (h.type === "vectorsetitem") {
            let g = s[m];
            if (!L(g) || !it(g) || !this.addModuleImport(g)) continue;
            c.push(m);
          }
        }
      }
      let f = Pl(e, n),
        d = ga(o, r, f),
        u = [3, e, s, l, c];
      return [4, "div", d, u];
    },
    tag: (e, n, o) => [4, e, n, ...o],
    text: (e) => [5, e],
  };
  resolveRichTextBinding;
  addModuleImport(e) {
    if (e in this.components) return !0;
    let n = J(e),
      o = this.imports.addModuleImport(n, { lazy: !0 });
    return o ? ((this.components[e] = o), !0) : !1;
  }
  add(e) {
    let n = Pu(e),
      o = this.cache.get(n);
    if (o) return o;
    let r = Ml(e, this.resolvers, this.serializers, this.options),
      i = JSON.stringify(r);
    return (this.cache.set(n, i), i);
  }
  serialize() {
    let e = this.imports.addImport(this.submodules.create(vc).submoduleImport, {
        exportSpecifier: "getRichTextJsonResolver",
      }),
      n = this.declarations.dedupe("richTextComponents", this.components);
    this.declarations.create(p`const ${this.resolveRichTextBinding} = ${T.fn(e, n)}`);
  }
};
function Pu(t) {
  return typeof t == "string" ? `html:${t}` : `document:${JSON.stringify(t)}`;
}
var er = class {
  values = new Map();
  add(e) {
    let n = this.values.get(e);
    if (A(n)) return n;
    let o = this.values.size;
    return (this.values.set(e, o), o);
  }
  serialize(e, n, o) {
    let r = [];
    for (let [a, s] of this.values) {
      let l = J(a),
        { importSpecifier: c } = hs(l, "collection", e, n);
      (b(c, "Import specifier must be defined"), (r[s] = p`() => import(${c})`));
    }
    let i = o.dedupe("vectors", r);
    return o.dedupe(
      "resolveVectorSetItem",
      p`(pointer: number) => {${p.joinLines(p`const vector = ${i}[pointer]`, p`if (vector) return vector().then(v => v.default)`)}}`
    );
  }
};
var te = {
    Uint8: 1,
    Uint16: 2,
    Uint32: 4,
    BigUint64: 8,
    Int8: 1,
    Int16: 2,
    Int32: 4,
    BigInt64: 8,
    Float32: 4,
    Float64: 8,
  },
  tr = class t {
    constructor(e) {
      this.bytes = e;
      this.view = vn(this.bytes);
    }
    bytes;
    static textDecoder = new TextDecoder();
    offset = 0;
    getOffset() {
      return this.offset;
    }
    view;
    ensureLength(e) {
      let n = this.bytes.length;
      if (!(this.offset + e <= n)) throw new Error("Reading out of bounds");
    }
    readUint8() {
      let e = te.Uint8;
      this.ensureLength(e);
      let n = this.view.getUint8(this.offset);
      return ((this.offset += e), n);
    }
    readUint16() {
      let e = te.Uint16;
      this.ensureLength(e);
      let n = this.view.getUint16(this.offset);
      return ((this.offset += e), n);
    }
    readUint32() {
      let e = te.Uint32;
      this.ensureLength(e);
      let n = this.view.getUint32(this.offset);
      return ((this.offset += e), n);
    }
    readUint64() {
      let e = this.readBigUint64();
      return Number(e);
    }
    readBigUint64() {
      let e = te.BigUint64;
      this.ensureLength(e);
      let n = this.view.getBigUint64(this.offset);
      return ((this.offset += e), n);
    }
    readInt8() {
      let e = te.Int8;
      this.ensureLength(e);
      let n = this.view.getInt8(this.offset);
      return ((this.offset += e), n);
    }
    readInt16() {
      let e = te.Int16;
      this.ensureLength(e);
      let n = this.view.getInt16(this.offset);
      return ((this.offset += e), n);
    }
    readInt32() {
      let e = te.Int32;
      this.ensureLength(e);
      let n = this.view.getInt32(this.offset);
      return ((this.offset += e), n);
    }
    readInt64() {
      let e = this.readBigInt64();
      return Number(e);
    }
    readBigInt64() {
      let e = te.BigInt64;
      this.ensureLength(e);
      let n = this.view.getBigInt64(this.offset);
      return ((this.offset += e), n);
    }
    readFloat32() {
      let e = te.Float32;
      this.ensureLength(e);
      let n = this.view.getFloat32(this.offset);
      return ((this.offset += e), n);
    }
    readFloat64() {
      let e = te.Float64;
      this.ensureLength(e);
      let n = this.view.getFloat64(this.offset);
      return ((this.offset += e), n);
    }
    readBytes(e) {
      let n = this.offset,
        o = n + e,
        r = this.bytes.subarray(n, o);
      return ((this.offset = o), r);
    }
    readString() {
      let e = this.readUint32(),
        n = this.readBytes(e);
      return t.textDecoder.decode(n);
    }
    readJson() {
      let e = this.readString();
      return JSON.parse(e);
    }
  };
function vn(t) {
  return new DataView(t.buffer, t.byteOffset, t.byteLength);
}
var Mu = 1024,
  ku = 1.5,
  di = (t) => 2 ** t - 1,
  ui = (t) => -(2 ** (t - 1)),
  fi = (t) => 2 ** (t - 1) - 1,
  et = {
    Uint8: 0,
    Uint16: 0,
    Uint32: 0,
    Uint64: 0,
    BigUint64: 0,
    Int8: ui(8),
    Int16: ui(16),
    Int32: ui(32),
    Int64: Number.MIN_SAFE_INTEGER,
    BigInt64: -(BigInt(2) ** BigInt(63)),
  },
  tt = {
    Uint8: di(8),
    Uint16: di(16),
    Uint32: di(32),
    Uint64: Number.MAX_SAFE_INTEGER,
    BigUint64: BigInt(2) ** BigInt(64) - BigInt(1),
    Int8: fi(8),
    Int16: fi(16),
    Int32: fi(32),
    Int64: Number.MAX_SAFE_INTEGER,
    BigInt64: BigInt(2) ** BigInt(63) - BigInt(1),
  };
function nt(t, e, n, o) {
  (X(t >= e, t, "outside lower bound for", o), X(t <= n, t, "outside upper bound for", o));
}
var Wt = class {
  offset = 0;
  getOffset() {
    return this.offset;
  }
  bytes = new Uint8Array(Mu);
  view = vn(this.bytes);
  slice(e = 0, n = this.offset) {
    return this.bytes.slice(e, n);
  }
  subarray(e = 0, n = this.offset) {
    return this.bytes.subarray(e, n);
  }
  encoder = new TextEncoder();
  ensureLength(e) {
    let n = this.bytes.length;
    if (this.offset + e <= n) return;
    let o = Math.ceil(n * ku) + e,
      r = new Uint8Array(o);
    (r.set(this.bytes), (this.bytes = r), (this.view = vn(r)));
  }
  writeUint8(e) {
    nt(e, et.Uint8, tt.Uint8, "Uint8");
    let n = te.Uint8;
    (this.ensureLength(n), this.view.setUint8(this.offset, e), (this.offset += n));
  }
  writeUint16(e) {
    nt(e, et.Uint16, tt.Uint16, "Uint16");
    let n = te.Uint16;
    (this.ensureLength(n), this.view.setUint16(this.offset, e), (this.offset += n));
  }
  writeUint32(e) {
    nt(e, et.Uint32, tt.Uint32, "Uint32");
    let n = te.Uint32;
    (this.ensureLength(n), this.view.setUint32(this.offset, e), (this.offset += n));
  }
  writeUint64(e) {
    nt(e, et.Uint64, tt.Uint64, "Uint64");
    let n = BigInt(e);
    this.writeBigUint64(n);
  }
  writeBigUint64(e) {
    nt(e, et.BigUint64, tt.BigUint64, "BigUint64");
    let n = te.BigUint64;
    (this.ensureLength(n), this.view.setBigUint64(this.offset, e), (this.offset += n));
  }
  writeInt8(e) {
    nt(e, et.Int8, tt.Int8, "Int8");
    let n = te.Int8;
    (this.ensureLength(n), this.view.setInt8(this.offset, e), (this.offset += n));
  }
  writeInt16(e) {
    nt(e, et.Int16, tt.Int16, "Int16");
    let n = te.Int16;
    (this.ensureLength(n), this.view.setInt16(this.offset, e), (this.offset += n));
  }
  writeInt32(e) {
    nt(e, et.Int32, tt.Int32, "Int32");
    let n = te.Int32;
    (this.ensureLength(n), this.view.setInt32(this.offset, e), (this.offset += n));
  }
  writeInt64(e) {
    nt(e, et.Int64, tt.Int64, "Int64");
    let n = BigInt(e);
    this.writeBigInt64(n);
  }
  writeBigInt64(e) {
    nt(e, et.BigInt64, tt.BigInt64, "BigInt64");
    let n = te.BigInt64;
    (this.ensureLength(n), this.view.setBigInt64(this.offset, e), (this.offset += n));
  }
  writeFloat32(e) {
    let n = te.Float32;
    (this.ensureLength(n), this.view.setFloat32(this.offset, e), (this.offset += n));
  }
  writeFloat64(e) {
    let n = te.Float64;
    (this.ensureLength(n), this.view.setFloat64(this.offset, e), (this.offset += n));
  }
  writeBytes(e) {
    let n = e.length;
    (this.ensureLength(n), this.bytes.set(e, this.offset), (this.offset += n));
  }
  encodedStrings = new Map();
  encodeString(e) {
    let n = this.encodedStrings.get(e);
    if (n) return n;
    let o = this.encoder.encode(e);
    return (this.encodedStrings.set(e, o), o);
  }
  writeString(e) {
    let n = this.encodeString(e),
      o = n.length;
    (this.writeUint32(o), this.writeBytes(n));
  }
  writeJson(e) {
    let n = JSON.stringify(e);
    this.writeString(n);
  }
};
var Sc = `var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJS = (cb, mod) => function __require() {
  try {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  } catch (e) {
    throw mod = 0, e;
  }
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// ../../../node_modules/dataloader/index.js
var require_dataloader = __commonJS({
  "../../../node_modules/dataloader/index.js"(exports, module) {
    "use strict";
    var DataLoader2 = /* @__PURE__ */ (function() {
      function DataLoader3(batchLoadFn, options) {
        if (typeof batchLoadFn !== "function") {
          throw new TypeError("DataLoader must be constructed with a function which accepts " + ("Array<key> and returns Promise<Array<value>>, but got: " + batchLoadFn + "."));
        }
        this._batchLoadFn = batchLoadFn;
        this._maxBatchSize = getValidMaxBatchSize(options);
        this._batchScheduleFn = getValidBatchScheduleFn(options);
        this._cacheKeyFn = getValidCacheKeyFn(options);
        this._cacheMap = getValidCacheMap(options);
        this._batch = null;
        this.name = getValidName(options);
      }
      var _proto = DataLoader3.prototype;
      _proto.load = function load(key) {
        if (key === null || key === void 0) {
          throw new TypeError("The loader.load() function must be called with a value, " + ("but got: " + String(key) + "."));
        }
        var batch = getCurrentBatch(this);
        var cacheMap = this._cacheMap;
        var cacheKey = this._cacheKeyFn(key);
        if (cacheMap) {
          var cachedPromise = cacheMap.get(cacheKey);
          if (cachedPromise) {
            var cacheHits = batch.cacheHits || (batch.cacheHits = []);
            return new Promise(function(resolve) {
              cacheHits.push(function() {
                resolve(cachedPromise);
              });
            });
          }
        }
        batch.keys.push(key);
        var promise = new Promise(function(resolve, reject) {
          batch.callbacks.push({
            resolve,
            reject
          });
        });
        if (cacheMap) {
          cacheMap.set(cacheKey, promise);
        }
        return promise;
      };
      _proto.loadMany = function loadMany(keys) {
        if (!isArrayLike(keys)) {
          throw new TypeError("The loader.loadMany() function must be called with Array<key> " + ("but got: " + keys + "."));
        }
        var loadPromises = [];
        for (var i = 0; i < keys.length; i++) {
          loadPromises.push(this.load(keys[i])["catch"](function(error) {
            return error;
          }));
        }
        return Promise.all(loadPromises);
      };
      _proto.clear = function clear(key) {
        var cacheMap = this._cacheMap;
        if (cacheMap) {
          var cacheKey = this._cacheKeyFn(key);
          cacheMap["delete"](cacheKey);
        }
        return this;
      };
      _proto.clearAll = function clearAll() {
        var cacheMap = this._cacheMap;
        if (cacheMap) {
          cacheMap.clear();
        }
        return this;
      };
      _proto.prime = function prime(key, value) {
        var cacheMap = this._cacheMap;
        if (cacheMap) {
          var cacheKey = this._cacheKeyFn(key);
          if (cacheMap.get(cacheKey) === void 0) {
            var promise;
            if (value instanceof Error) {
              promise = Promise.reject(value);
              promise["catch"](function() {
              });
            } else {
              promise = Promise.resolve(value);
            }
            cacheMap.set(cacheKey, promise);
          }
        }
        return this;
      };
      return DataLoader3;
    })();
    var enqueuePostPromiseJob = typeof process === "object" && typeof process.nextTick === "function" ? function(fn) {
      if (!resolvedPromise) {
        resolvedPromise = Promise.resolve();
      }
      resolvedPromise.then(function() {
        process.nextTick(fn);
      });
    } : typeof setImmediate === "function" ? function(fn) {
      setImmediate(fn);
    } : function(fn) {
      setTimeout(fn);
    };
    var resolvedPromise;
    function getCurrentBatch(loader) {
      var existingBatch = loader._batch;
      if (existingBatch !== null && !existingBatch.hasDispatched && existingBatch.keys.length < loader._maxBatchSize) {
        return existingBatch;
      }
      var newBatch = {
        hasDispatched: false,
        keys: [],
        callbacks: []
      };
      loader._batch = newBatch;
      loader._batchScheduleFn(function() {
        dispatchBatch(loader, newBatch);
      });
      return newBatch;
    }
    function dispatchBatch(loader, batch) {
      batch.hasDispatched = true;
      if (batch.keys.length === 0) {
        resolveCacheHits(batch);
        return;
      }
      var batchPromise;
      try {
        batchPromise = loader._batchLoadFn(batch.keys);
      } catch (e) {
        return failedDispatch(loader, batch, new TypeError("DataLoader must be constructed with a function which accepts Array<key> and returns Promise<Array<value>>, but the function " + ("errored synchronously: " + String(e) + ".")));
      }
      if (!batchPromise || typeof batchPromise.then !== "function") {
        return failedDispatch(loader, batch, new TypeError("DataLoader must be constructed with a function which accepts Array<key> and returns Promise<Array<value>>, but the function did " + ("not return a Promise: " + String(batchPromise) + ".")));
      }
      batchPromise.then(function(values) {
        if (!isArrayLike(values)) {
          throw new TypeError("DataLoader must be constructed with a function which accepts Array<key> and returns Promise<Array<value>>, but the function did " + ("not return a Promise of an Array: " + String(values) + "."));
        }
        if (values.length !== batch.keys.length) {
          throw new TypeError("DataLoader must be constructed with a function which accepts Array<key> and returns Promise<Array<value>>, but the function did not return a Promise of an Array of the same length as the Array of keys." + ("\\n\\nKeys:\\n" + String(batch.keys)) + ("\\n\\nValues:\\n" + String(values)));
        }
        resolveCacheHits(batch);
        for (var i = 0; i < batch.callbacks.length; i++) {
          var value = values[i];
          if (value instanceof Error) {
            batch.callbacks[i].reject(value);
          } else {
            batch.callbacks[i].resolve(value);
          }
        }
      })["catch"](function(error) {
        failedDispatch(loader, batch, error);
      });
    }
    function failedDispatch(loader, batch, error) {
      resolveCacheHits(batch);
      for (var i = 0; i < batch.keys.length; i++) {
        loader.clear(batch.keys[i]);
        batch.callbacks[i].reject(error);
      }
    }
    function resolveCacheHits(batch) {
      if (batch.cacheHits) {
        for (var i = 0; i < batch.cacheHits.length; i++) {
          batch.cacheHits[i]();
        }
      }
    }
    function getValidMaxBatchSize(options) {
      var shouldBatch = !options || options.batch !== false;
      if (!shouldBatch) {
        return 1;
      }
      var maxBatchSize = options && options.maxBatchSize;
      if (maxBatchSize === void 0) {
        return Infinity;
      }
      if (typeof maxBatchSize !== "number" || maxBatchSize < 1) {
        throw new TypeError("maxBatchSize must be a positive number: " + maxBatchSize);
      }
      return maxBatchSize;
    }
    function getValidBatchScheduleFn(options) {
      var batchScheduleFn = options && options.batchScheduleFn;
      if (batchScheduleFn === void 0) {
        return enqueuePostPromiseJob;
      }
      if (typeof batchScheduleFn !== "function") {
        throw new TypeError("batchScheduleFn must be a function: " + batchScheduleFn);
      }
      return batchScheduleFn;
    }
    function getValidCacheKeyFn(options) {
      var cacheKeyFn = options && options.cacheKeyFn;
      if (cacheKeyFn === void 0) {
        return function(key) {
          return key;
        };
      }
      if (typeof cacheKeyFn !== "function") {
        throw new TypeError("cacheKeyFn must be a function: " + cacheKeyFn);
      }
      return cacheKeyFn;
    }
    function getValidCacheMap(options) {
      var shouldCache = !options || options.cache !== false;
      if (!shouldCache) {
        return null;
      }
      var cacheMap = options && options.cacheMap;
      if (cacheMap === void 0) {
        return /* @__PURE__ */ new Map();
      }
      if (cacheMap !== null) {
        var cacheFunctions = ["get", "set", "delete", "clear"];
        var missingFunctions = cacheFunctions.filter(function(fnName) {
          return cacheMap && typeof cacheMap[fnName] !== "function";
        });
        if (missingFunctions.length !== 0) {
          throw new TypeError("Custom cacheMap missing methods: " + missingFunctions.join(", "));
        }
      }
      return cacheMap;
    }
    function getValidName(options) {
      if (options && options.name) {
        return options.name;
      }
      return null;
    }
    function isArrayLike(x) {
      return typeof x === "object" && x !== null && typeof x.length === "number" && (x.length === 0 || x.length > 0 && Object.prototype.hasOwnProperty.call(x, x.length - 1));
    }
    module.exports = DataLoader2;
  }
});

// src/code-generation/components/cms/bundled/DatabaseCollection.legacy.ts?bundle
var import_dataloader = __toESM(require_dataloader(), 1);

// src/code-generation/components/cms/bundled/BufferReader.ts
var BYTE_LENGTH = {
  Uint8: 1,
  Uint16: 2,
  Uint32: 4,
  BigUint64: 8,
  Int8: 1,
  Int16: 2,
  Int32: 4,
  BigInt64: 8,
  Float32: 4,
  Float64: 8
};
var BufferReader = class _BufferReader {
  constructor(bytes) {
    this.bytes = bytes;
    this.view = getDataView(this.bytes);
  }
  bytes;
  // TextDecoder has no per-decode state unless streaming is enabled. Reusing one class-owned decoder
  // avoids constructing one for every CMS item, where each item gets its own BufferReader.
  static textDecoder = new TextDecoder();
  offset = 0;
  getOffset() {
    return this.offset;
  }
  view;
  ensureLength(requiredBytes) {
    const oldLength = this.bytes.length;
    if (this.offset + requiredBytes <= oldLength) return;
    throw new Error("Reading out of bounds");
  }
  readUint8() {
    const size = BYTE_LENGTH.Uint8;
    this.ensureLength(size);
    const value = this.view.getUint8(this.offset);
    this.offset += size;
    return value;
  }
  readUint16() {
    const size = BYTE_LENGTH.Uint16;
    this.ensureLength(size);
    const value = this.view.getUint16(this.offset);
    this.offset += size;
    return value;
  }
  readUint32() {
    const size = BYTE_LENGTH.Uint32;
    this.ensureLength(size);
    const value = this.view.getUint32(this.offset);
    this.offset += size;
    return value;
  }
  readUint64() {
    const bigint = this.readBigUint64();
    return Number(bigint);
  }
  readBigUint64() {
    const size = BYTE_LENGTH.BigUint64;
    this.ensureLength(size);
    const value = this.view.getBigUint64(this.offset);
    this.offset += size;
    return value;
  }
  readInt8() {
    const size = BYTE_LENGTH.Int8;
    this.ensureLength(size);
    const value = this.view.getInt8(this.offset);
    this.offset += size;
    return value;
  }
  readInt16() {
    const size = BYTE_LENGTH.Int16;
    this.ensureLength(size);
    const value = this.view.getInt16(this.offset);
    this.offset += size;
    return value;
  }
  readInt32() {
    const size = BYTE_LENGTH.Int32;
    this.ensureLength(size);
    const value = this.view.getInt32(this.offset);
    this.offset += size;
    return value;
  }
  readInt64() {
    const bigint = this.readBigInt64();
    return Number(bigint);
  }
  readBigInt64() {
    const size = BYTE_LENGTH.BigInt64;
    this.ensureLength(size);
    const value = this.view.getBigInt64(this.offset);
    this.offset += size;
    return value;
  }
  readFloat32() {
    const size = BYTE_LENGTH.Float32;
    this.ensureLength(size);
    const value = this.view.getFloat32(this.offset);
    this.offset += size;
    return value;
  }
  readFloat64() {
    const size = BYTE_LENGTH.Float64;
    this.ensureLength(size);
    const value = this.view.getFloat64(this.offset);
    this.offset += size;
    return value;
  }
  readBytes(length) {
    const start = this.offset;
    const end = start + length;
    const value = this.bytes.subarray(start, end);
    this.offset = end;
    return value;
  }
  readString() {
    const length = this.readUint32();
    const bytes = this.readBytes(length);
    return _BufferReader.textDecoder.decode(bytes);
  }
  readJson() {
    const string = this.readString();
    return JSON.parse(string);
  }
};
function getDataView(bytes) {
  return new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength);
}

// src/code-generation/components/cms/bundled/DatabaseDictionaryIndex.legacy.ts
import { ControlType as ControlType2 } from "framer";

// ../../library/src/utils/utils.ts
var isWindow = typeof window !== "undefined";
var supportsRequestIdleCallback = isWindow && typeof window.requestIdleCallback === "function";

// src/code-generation/components/cms/bundled/assert.ts
function assert(condition, ...msg) {
  if (condition) return;
  throw Error("Assertion Error" + (msg.length > 0 ? ": " + msg.join(" ") : ""));
}
function assertNever(condition) {
  throw new Error(\`Unexpected value: \${condition}\`);
}

// src/code-generation/components/cms/bundled/BufferWriter.ts
var INITIAL_BUFFER_SIZE = 1024;
var GROWTH_FACTOR = 1.5;
var maxUnsignedNumber = (bits) => 2 ** bits - 1;
var minSignedNumber = (bits) => -(2 ** (bits - 1));
var maxSignedNumber = (bits) => 2 ** (bits - 1) - 1;
var MIN_VALUE = {
  Uint8: 0,
  Uint16: 0,
  Uint32: 0,
  Uint64: 0,
  BigUint64: 0,
  Int8: minSignedNumber(8),
  Int16: minSignedNumber(16),
  Int32: minSignedNumber(32),
  Int64: Number.MIN_SAFE_INTEGER,
  BigInt64: -(BigInt(2) ** BigInt(63))
};
var MAX_VALUE = {
  Uint8: maxUnsignedNumber(8),
  Uint16: maxUnsignedNumber(16),
  Uint32: maxUnsignedNumber(32),
  Uint64: Number.MAX_SAFE_INTEGER,
  BigUint64: BigInt(2) ** BigInt(64) - BigInt(1),
  Int8: maxSignedNumber(8),
  Int16: maxSignedNumber(16),
  Int32: maxSignedNumber(32),
  Int64: Number.MAX_SAFE_INTEGER,
  BigInt64: BigInt(2) ** BigInt(63) - BigInt(1)
};
function assertRange(value, min, max, dataType) {
  assert(value >= min, value, "outside lower bound for", dataType);
  assert(value <= max, value, "outside upper bound for", dataType);
}
var BufferWriter = class {
  offset = 0;
  getOffset() {
    return this.offset;
  }
  bytes = new Uint8Array(INITIAL_BUFFER_SIZE);
  view = getDataView(this.bytes);
  slice(start = 0, end = this.offset) {
    return this.bytes.slice(start, end);
  }
  subarray(start = 0, end = this.offset) {
    return this.bytes.subarray(start, end);
  }
  encoder = new TextEncoder();
  ensureLength(requiredBytes) {
    const oldLength = this.bytes.length;
    if (this.offset + requiredBytes <= oldLength) return;
    const newLength = Math.ceil(oldLength * GROWTH_FACTOR) + requiredBytes;
    const newBytes = new Uint8Array(newLength);
    newBytes.set(this.bytes);
    this.bytes = newBytes;
    this.view = getDataView(newBytes);
  }
  writeUint8(value) {
    assertRange(value, MIN_VALUE.Uint8, MAX_VALUE.Uint8, "Uint8");
    const size = BYTE_LENGTH.Uint8;
    this.ensureLength(size);
    this.view.setUint8(this.offset, value);
    this.offset += size;
  }
  writeUint16(value) {
    assertRange(value, MIN_VALUE.Uint16, MAX_VALUE.Uint16, "Uint16");
    const size = BYTE_LENGTH.Uint16;
    this.ensureLength(size);
    this.view.setUint16(this.offset, value);
    this.offset += size;
  }
  writeUint32(value) {
    assertRange(value, MIN_VALUE.Uint32, MAX_VALUE.Uint32, "Uint32");
    const size = BYTE_LENGTH.Uint32;
    this.ensureLength(size);
    this.view.setUint32(this.offset, value);
    this.offset += size;
  }
  writeUint64(value) {
    assertRange(value, MIN_VALUE.Uint64, MAX_VALUE.Uint64, "Uint64");
    const bigint = BigInt(value);
    this.writeBigUint64(bigint);
  }
  writeBigUint64(value) {
    assertRange(value, MIN_VALUE.BigUint64, MAX_VALUE.BigUint64, "BigUint64");
    const size = BYTE_LENGTH.BigUint64;
    this.ensureLength(size);
    this.view.setBigUint64(this.offset, value);
    this.offset += size;
  }
  writeInt8(value) {
    assertRange(value, MIN_VALUE.Int8, MAX_VALUE.Int8, "Int8");
    const size = BYTE_LENGTH.Int8;
    this.ensureLength(size);
    this.view.setInt8(this.offset, value);
    this.offset += size;
  }
  writeInt16(value) {
    assertRange(value, MIN_VALUE.Int16, MAX_VALUE.Int16, "Int16");
    const size = BYTE_LENGTH.Int16;
    this.ensureLength(size);
    this.view.setInt16(this.offset, value);
    this.offset += size;
  }
  writeInt32(value) {
    assertRange(value, MIN_VALUE.Int32, MAX_VALUE.Int32, "Int32");
    const size = BYTE_LENGTH.Int32;
    this.ensureLength(size);
    this.view.setInt32(this.offset, value);
    this.offset += size;
  }
  writeInt64(value) {
    assertRange(value, MIN_VALUE.Int64, MAX_VALUE.Int64, "Int64");
    const bigint = BigInt(value);
    this.writeBigInt64(bigint);
  }
  writeBigInt64(value) {
    assertRange(value, MIN_VALUE.BigInt64, MAX_VALUE.BigInt64, "BigInt64");
    const size = BYTE_LENGTH.BigInt64;
    this.ensureLength(size);
    this.view.setBigInt64(this.offset, value);
    this.offset += size;
  }
  writeFloat32(value) {
    const size = BYTE_LENGTH.Float32;
    this.ensureLength(size);
    this.view.setFloat32(this.offset, value);
    this.offset += size;
  }
  writeFloat64(value) {
    const size = BYTE_LENGTH.Float64;
    this.ensureLength(size);
    this.view.setFloat64(this.offset, value);
    this.offset += size;
  }
  writeBytes(value) {
    const size = value.length;
    this.ensureLength(size);
    this.bytes.set(value, this.offset);
    this.offset += size;
  }
  encodedStrings = /* @__PURE__ */ new Map();
  encodeString(value) {
    const cached = this.encodedStrings.get(value);
    if (cached) return cached;
    const bytes = this.encoder.encode(value);
    this.encodedStrings.set(value, bytes);
    return bytes;
  }
  writeString(value) {
    const bytes = this.encodeString(value);
    const size = bytes.length;
    this.writeUint32(size);
    this.writeBytes(bytes);
  }
  writeJson(value) {
    const json = JSON.stringify(value);
    this.writeString(json);
  }
};

// src/utils/typeChecks.ts
function isString(value) {
  return typeof value === "string";
}
function isNumber(value) {
  return Number.isFinite(value);
}
function isNull(value) {
  return value === null;
}

// src/code-generation/components/cms/bundled/models/DatabaseItemPointerModel.ts
var DatabaseItemPointerModel = class _DatabaseItemPointerModel {
  constructor(chunkId, offset, length) {
    this.chunkId = chunkId;
    this.offset = offset;
    this.length = length;
  }
  chunkId;
  offset;
  length;
  static fromString(value) {
    const [chunkId, offset, length] = value.split("/").map(Number);
    assert(isNumber(chunkId), "Invalid chunkId");
    assert(isNumber(offset), "Invalid offset");
    assert(isNumber(length), "Invalid length");
    return new _DatabaseItemPointerModel(chunkId, offset, length);
  }
  toString() {
    return \`\${this.chunkId}/\${this.offset}/\${this.length}\`;
  }
  static read(reader) {
    const chunkId = reader.readUint16();
    const offset = reader.readUint32();
    const length = reader.readUint32();
    return new _DatabaseItemPointerModel(chunkId, offset, length);
  }
  write(writer) {
    writer.writeUint16(this.chunkId);
    writer.writeUint32(this.offset);
    writer.writeUint32(this.length);
  }
  compare(other) {
    if (this.chunkId < other.chunkId) return -1;
    if (this.chunkId > other.chunkId) return 1;
    if (this.offset < other.offset) return -1;
    if (this.offset > other.offset) return 1;
    assert(this.length === other.length);
    return 0;
  }
};

// src/code-generation/components/cms/bundled/models/DatabaseValueModel.ts
import { ControlType } from "framer";
function getValueType(value) {
  if (isNull(value)) {
    return 0 /* Null */;
  }
  switch (value.type) {
    case ControlType.Array:
      return 1 /* Array */;
    case ControlType.Boolean:
      return 2 /* Boolean */;
    case ControlType.Color:
      return 3 /* Color */;
    case ControlType.Date:
      return 4 /* Date */;
    case ControlType.Enum:
      return 5 /* Enum */;
    case ControlType.File:
      return 6 /* File */;
    case ControlType.ResponsiveImage:
      return 10 /* ResponsiveImage */;
    case ControlType.Link:
      return 7 /* Link */;
    case ControlType.Number:
      return 8 /* Number */;
    case ControlType.Object:
      return 9 /* Object */;
    case ControlType.RichText:
      return 11 /* RichText */;
    case ControlType.String:
      return 12 /* String */;
    case ControlType.VectorSetItem:
      return 13 /* VectorSetItem */;
    default:
      assertNever(value);
  }
}
var DatabaseValueModel;
((DatabaseValueModel2) => {
  function read(reader) {
    const type = reader.readUint8();
    switch (type) {
      case 0 /* Null */:
        return null;
      case 1 /* Array */:
        return readArray(reader);
      case 2 /* Boolean */:
        return readBoolean(reader);
      case 3 /* Color */:
        return readColor(reader);
      case 4 /* Date */:
        return readDate(reader);
      case 5 /* Enum */:
        return readEnum(reader);
      case 6 /* File */:
        return readFile(reader);
      case 7 /* Link */:
        return readLink(reader);
      case 8 /* Number */:
        return readNumber(reader);
      case 9 /* Object */:
        return readObject(reader);
      case 10 /* ResponsiveImage */:
        return readResponsiveImage(reader);
      case 11 /* RichText */:
        return readRichText(reader);
      case 12 /* String */:
        return readString(reader);
      case 13 /* VectorSetItem */:
        return readVectorSetItem(reader);
      default:
        assertNever(type);
    }
  }
  DatabaseValueModel2.read = read;
  function write(writer, value) {
    const type = getValueType(value);
    writer.writeUint8(type);
    if (isNull(value)) return;
    switch (value.type) {
      case ControlType.Array:
        return writeArray(writer, value);
      case ControlType.Boolean:
        return writeBoolean(writer, value);
      case ControlType.Color:
        return writeColor(writer, value);
      case ControlType.Date:
        return writeDate(writer, value);
      case ControlType.Enum:
        return writeEnum(writer, value);
      case ControlType.File:
        return writeFile(writer, value);
      case ControlType.Link:
        return writeLink(writer, value);
      case ControlType.Number:
        return writeNumber(writer, value);
      case ControlType.Object:
        return writeObject(writer, value);
      case ControlType.ResponsiveImage:
        return writeResponsiveImage(writer, value);
      case ControlType.RichText:
        return writeRichText(writer, value);
      case ControlType.VectorSetItem:
        return writeVectorSetItem(writer, value);
      case ControlType.String:
        return writeString(writer, value);
      default:
        assertNever(value);
    }
  }
  DatabaseValueModel2.write = write;
  function compare(left, right, collation) {
    const leftType = getValueType(left);
    const rightType = getValueType(right);
    if (leftType < rightType) return -1;
    if (leftType > rightType) return 1;
    if (isNull(left) || isNull(right)) return 0;
    switch (left.type) {
      case ControlType.Array:
        assert(right.type === ControlType.Array);
        return compareArray(left, right, collation);
      case ControlType.Boolean:
        assert(right.type === ControlType.Boolean);
        return compareBoolean(left, right);
      case ControlType.Color:
        assert(right.type === ControlType.Color);
        return compareColor(left, right);
      case ControlType.Date:
        assert(right.type === ControlType.Date);
        return compareDate(left, right);
      case ControlType.Enum:
        assert(right.type === ControlType.Enum);
        return compareEnum(left, right);
      case ControlType.File:
        assert(right.type === ControlType.File);
        return compareFile(left, right);
      case ControlType.Link:
        assert(right.type === ControlType.Link);
        return compareLink(left, right);
      case ControlType.Number:
        assert(right.type === ControlType.Number);
        return compareNumber(left, right);
      case ControlType.Object:
        assert(right.type === ControlType.Object);
        return compareObject(left, right, collation);
      case ControlType.ResponsiveImage:
        assert(right.type === ControlType.ResponsiveImage);
        return compareResponsiveImage(left, right);
      case ControlType.RichText:
        assert(right.type === ControlType.RichText);
        return compareRichText(left, right);
      case ControlType.VectorSetItem:
        assert(right.type === ControlType.VectorSetItem);
        return compareVectorSetItem(left, right);
      case ControlType.String:
        assert(right.type === ControlType.String);
        return compareString(left, right, collation);
      default:
        assertNever(left);
    }
  }
  DatabaseValueModel2.compare = compare;
})(DatabaseValueModel || (DatabaseValueModel = {}));
function readArray(reader) {
  const length = reader.readUint16();
  const value = [];
  for (let i = 0; i < length; i++) {
    const item = DatabaseValueModel.read(reader);
    value.push(item);
  }
  return {
    type: ControlType.Array,
    value
  };
}
function writeArray(writer, value) {
  writer.writeUint16(value.value.length);
  for (const item of value.value) {
    DatabaseValueModel.write(writer, item);
  }
}
function compareArray(left, right, collation) {
  const leftLength = left.value.length;
  const rightLength = right.value.length;
  if (leftLength < rightLength) return -1;
  if (leftLength > rightLength) return 1;
  for (let i = 0; i < leftLength; i++) {
    const leftItem = left.value[i];
    const rightItem = right.value[i];
    const result = DatabaseValueModel.compare(leftItem, rightItem, collation);
    if (result !== 0) return result;
  }
  return 0;
}
function readBoolean(reader) {
  return {
    type: ControlType.Boolean,
    value: reader.readUint8() !== 0
  };
}
function writeBoolean(writer, value) {
  writer.writeUint8(value.value ? 1 : 0);
}
function compareBoolean(left, right) {
  if (left.value < right.value) return -1;
  if (left.value > right.value) return 1;
  return 0;
}
function readColor(reader) {
  return {
    type: ControlType.Color,
    value: reader.readString()
  };
}
function writeColor(writer, value) {
  writer.writeString(value.value);
}
function compareColor(left, right) {
  if (left.value < right.value) return -1;
  if (left.value > right.value) return 1;
  return 0;
}
function readDate(reader) {
  const timestamp = reader.readInt64();
  const date = new Date(timestamp);
  return {
    type: ControlType.Date,
    value: date.toISOString()
  };
}
function writeDate(writer, value) {
  const date = new Date(value.value);
  const timestamp = date.getTime();
  writer.writeInt64(timestamp);
}
function compareDate(left, right) {
  const leftDate = new Date(left.value);
  const rightDate = new Date(right.value);
  if (leftDate < rightDate) return -1;
  if (leftDate > rightDate) return 1;
  return 0;
}
function readEnum(reader) {
  return {
    type: ControlType.Enum,
    value: reader.readString()
  };
}
function writeEnum(writer, value) {
  writer.writeString(value.value);
}
function compareEnum(left, right) {
  if (left.value < right.value) return -1;
  if (left.value > right.value) return 1;
  return 0;
}
function readFile(reader) {
  return {
    type: ControlType.File,
    value: reader.readString()
  };
}
function writeFile(writer, value) {
  writer.writeString(value.value);
}
function compareFile(left, right) {
  if (left.value < right.value) return -1;
  if (left.value > right.value) return 1;
  return 0;
}
function readLink(reader) {
  return {
    type: ControlType.Link,
    value: reader.readJson()
  };
}
function writeLink(writer, value) {
  writer.writeJson(value.value);
}
function compareLink(left, right) {
  const leftEncoded = JSON.stringify(left.value);
  const rightEncoded = JSON.stringify(right.value);
  if (leftEncoded < rightEncoded) return -1;
  if (leftEncoded > rightEncoded) return 1;
  return 0;
}
function readNumber(reader) {
  return {
    type: ControlType.Number,
    value: reader.readFloat64()
  };
}
function writeNumber(writer, value) {
  writer.writeFloat64(value.value);
}
function compareNumber(left, right) {
  if (left.value < right.value) return -1;
  if (left.value > right.value) return 1;
  return 0;
}
function readObject(reader) {
  const length = reader.readUint16();
  const result = {};
  for (let i = 0; i < length; i++) {
    const key = reader.readString();
    result[key] = DatabaseValueModel.read(reader);
  }
  return {
    type: ControlType.Object,
    value: result
  };
}
function writeObject(writer, value) {
  const entries = Object.entries(value.value);
  writer.writeUint16(entries.length);
  for (const [key, item] of entries) {
    writer.writeString(key);
    DatabaseValueModel.write(writer, item);
  }
}
function compareObject(left, right, collation) {
  const leftKeys = Object.keys(left.value).sort();
  const rightKeys = Object.keys(right.value).sort();
  if (leftKeys.length < rightKeys.length) return -1;
  if (leftKeys.length > rightKeys.length) return 1;
  for (let i = 0; i < leftKeys.length; i++) {
    const leftKey = leftKeys[i];
    const rightKey = rightKeys[i];
    if (leftKey < rightKey) return -1;
    if (leftKey > rightKey) return 1;
    const leftValue = left.value[leftKey] ?? null;
    const rightValue = right.value[rightKey] ?? null;
    const result = DatabaseValueModel.compare(leftValue, rightValue, collation);
    if (result !== 0) return result;
  }
  return 0;
}
function readResponsiveImage(reader) {
  return {
    type: ControlType.ResponsiveImage,
    value: reader.readJson()
  };
}
function writeResponsiveImage(writer, value) {
  writer.writeJson(value.value);
}
function compareResponsiveImage(left, right) {
  const leftEncoded = JSON.stringify(left.value);
  const rightEncoded = JSON.stringify(right.value);
  if (leftEncoded < rightEncoded) return -1;
  if (leftEncoded > rightEncoded) return 1;
  return 0;
}
function readRichText(reader) {
  const type = reader.readInt8();
  if (type === 0) {
    return {
      type: ControlType.RichText,
      value: reader.readUint32()
    };
  }
  if (type === 1) {
    return {
      type: ControlType.RichText,
      value: reader.readString()
    };
  }
  throw new Error(\`Invalid rich text pointer\`);
}
function writeRichText(writer, value) {
  if (isNumber(value.value)) {
    writer.writeInt8(0);
    writer.writeUint32(value.value);
    return;
  }
  if (isString(value.value)) {
    writer.writeInt8(1);
    writer.writeString(value.value);
    return;
  }
  throw new Error(\`Invalid rich text pointer\`);
}
function compareRichText(left, right) {
  const leftValue = left.value;
  const rightValue = right.value;
  if (isNumber(leftValue) && isNumber(rightValue)) {
    if (leftValue < rightValue) return -1;
    if (leftValue > rightValue) return 1;
    return 0;
  }
  if (isString(leftValue) && isString(rightValue)) {
    if (leftValue < rightValue) return -1;
    if (leftValue > rightValue) return 1;
    return 0;
  }
  throw new Error(\`Invalid rich text pointer\`);
}
function readString(reader) {
  return {
    type: ControlType.String,
    value: reader.readString()
  };
}
function writeString(writer, value) {
  writer.writeString(value.value);
}
function compareString(left, right, collation) {
  let leftValue = left.value;
  let rightValue = right.value;
  if (collation.type === 0 /* CaseInsensitive */) {
    leftValue = left.value.toLowerCase();
    rightValue = right.value.toLowerCase();
  }
  if (leftValue < rightValue) return -1;
  if (leftValue > rightValue) return 1;
  return 0;
}
function readVectorSetItem(reader) {
  return {
    type: ControlType.VectorSetItem,
    value: reader.readUint32()
  };
}
function writeVectorSetItem(writer, value) {
  writer.writeUint32(value.value);
}
function compareVectorSetItem(left, right) {
  const leftValue = left.value;
  const rightValue = right.value;
  if (leftValue < rightValue) return -1;
  if (leftValue > rightValue) return 1;
  return 0;
}

// src/code-generation/components/cms/bundled/models/DatabaseDictionaryIndexModel.ts
var DatabaseDictionaryIndexModel = class _DatabaseDictionaryIndexModel {
  constructor(fieldNames, options) {
    this.fieldNames = fieldNames;
    this.options = options;
  }
  fieldNames;
  options;
  entries = [];
  sortEntries() {
    this.entries.sort((left, right) => {
      for (let i = 0; i < this.fieldNames.length; i++) {
        const leftValue = left.values[i];
        const rightValue = right.values[i];
        const result = DatabaseValueModel.compare(leftValue, rightValue, this.options.collation);
        if (result !== 0) return result;
      }
      return left.pointer.compare(right.pointer);
    });
  }
  static async deserialize(bytes, yieldBeforeEntry) {
    const reader = new BufferReader(bytes);
    const collation = reader.readJson();
    const fieldCount = reader.readUint8();
    const fieldNames = [];
    for (let i = 0; i < fieldCount; i++) {
      const fieldName = reader.readString();
      fieldNames.push(fieldName);
    }
    const index = new _DatabaseDictionaryIndexModel(fieldNames, {
      collation
    });
    const entryCount = reader.readUint32();
    const readEntry = () => {
      const values = [];
      for (let j = 0; j < fieldCount; j++) {
        const fieldValue = DatabaseValueModel.read(reader);
        values.push(fieldValue);
      }
      const pointer = DatabaseItemPointerModel.read(reader);
      index.entries.push({ values, pointer });
    };
    for (let i = 0; i < entryCount; i++) {
      const yieldPromise = yieldBeforeEntry?.();
      if (yieldPromise) await yieldPromise;
      readEntry();
    }
    return index;
  }
  serialize() {
    const writer = new BufferWriter();
    writer.writeJson(this.options.collation);
    writer.writeUint8(this.fieldNames.length);
    for (const fieldName of this.fieldNames) {
      writer.writeString(fieldName);
    }
    this.sortEntries();
    writer.writeUint32(this.entries.length);
    for (const entry of this.entries) {
      const { values, pointer } = entry;
      for (const value of values) {
        DatabaseValueModel.write(writer, value);
      }
      pointer.write(writer);
    }
    return writer.subarray();
  }
  addItem(item, pointer) {
    const values = this.fieldNames.map((fieldName) => {
      return item.getField(fieldName) ?? null;
    });
    this.entries.push({ values, pointer });
  }
};

// src/code-generation/components/cms/bundled/fetchWithRetries.ts
var maxRetries = 3;
var baseDelay = 250;
var retriableHTTPCodes = [
  408,
  // Request Timeout
  429,
  // Too Many Requests
  500,
  // Internal Server Error
  502,
  // Bad Gateway
  503,
  // Service Unavailable
  504
  // Gateway Timeout
];
var fetchWithRetries = async (input, init) => {
  let retryCount = 0;
  while (true) {
    try {
      const res = await fetch(input, init);
      if (!retriableHTTPCodes.includes(res.status) || ++retryCount > maxRetries) {
        return res;
      }
    } catch (error) {
      if (init?.signal?.aborted || ++retryCount > maxRetries) {
        throw error;
      }
    }
    await retryDelay(retryCount);
  }
};
async function retryDelay(retryCount) {
  const backoffExponent = retryCount - 1;
  const jitter = Math.random() + 1;
  const timeout = Math.floor(baseDelay * jitter * 2 ** backoffExponent);
  await new Promise((resolve) => {
    setTimeout(resolve, timeout);
  });
}

// src/code-generation/components/cms/bundled/rangeRequest.ts
async function rangeRequest(url, ranges) {
  const collapsedRanges = collapseRanges(ranges);
  const rangeStrings = [];
  let expectedLength = 0;
  for (const range of collapsedRanges) {
    rangeStrings.push(\`\${range.from}-\${range.to - 1}\`);
    expectedLength += range.to - range.from;
  }
  const rangeUrl = new URL(url);
  const rangeQuery = rangeStrings.join(",");
  rangeUrl.searchParams.set("range", rangeQuery);
  const response = await fetchWithRetries(rangeUrl);
  if (response.status !== 200) {
    throw new Error(\`Request failed: \${response.status} \${response.statusText}\`);
  }
  const buffer = await response.arrayBuffer();
  const bytes = new Uint8Array(buffer);
  if (bytes.length !== expectedLength) {
    throw new Error("Request failed: Unexpected response length");
  }
  const file = new PartialFile();
  let start = 0;
  for (const range of collapsedRanges) {
    const length = range.to - range.from;
    const end = start + length;
    const part = bytes.subarray(start, end);
    file.write(range.from, part);
    start = end;
  }
  return ranges.map((range) => {
    return file.read(range.from, range.to - range.from);
  });
}
var PartialFile = class {
  chunks = [];
  read(position, length) {
    for (const chunk of this.chunks) {
      if (position < chunk.start) {
        throw new Error("Missing data");
      }
      if (position > chunk.end) {
        continue;
      }
      if (position + length > chunk.end) {
        throw new Error("Missing data");
      }
      const from = position - chunk.start;
      const to = from + length;
      return chunk.data.slice(from, to);
    }
    throw new Error("Missing data");
  }
  write(position, data) {
    let start = position;
    let end = start + data.length;
    let fromIndex = 0;
    let toIndex = this.chunks.length;
    for (; fromIndex < toIndex; fromIndex++) {
      const chunk2 = this.chunks[fromIndex];
      assert(chunk2, "Missing chunk");
      if (start > chunk2.end) {
        continue;
      }
      if (start > chunk2.start) {
        const offset = start - chunk2.start;
        const prefix = chunk2.data.subarray(0, offset);
        data = join(prefix, data);
        start = chunk2.start;
      }
      break;
    }
    for (; toIndex > fromIndex; toIndex--) {
      const chunk2 = this.chunks[toIndex - 1];
      assert(chunk2, "Missing chunk");
      if (end < chunk2.start) {
        continue;
      }
      if (end < chunk2.end) {
        const offset = end - chunk2.start;
        const suffix = chunk2.data.subarray(offset);
        data = join(data, suffix);
        end = chunk2.end;
      }
      break;
    }
    const chunk = {
      start,
      end,
      data
    };
    const deleteCount = toIndex - fromIndex;
    this.chunks.splice(fromIndex, deleteCount, chunk);
  }
};
function join(left, right) {
  const length = left.length + right.length;
  const result = new Uint8Array(length);
  result.set(left, 0);
  result.set(right, left.length);
  return result;
}
function collapseRanges(ranges) {
  assert(ranges.length > 0, "Must have at least one range");
  const sorted = [...ranges].sort((a, b) => a.from - b.from);
  const collapsed = [];
  for (const range of sorted) {
    const index = collapsed.length - 1;
    const previous = collapsed[index];
    if (previous && range.from <= previous.to) {
      collapsed[index] = {
        from: previous.from,
        to: Math.max(previous.to, range.to)
      };
    } else {
      collapsed.push(range);
    }
  }
  return collapsed;
}

// src/code-generation/components/cms/bundled/DatabaseDictionaryIndex.legacy.ts
var DatabaseDictionaryIndex = class {
  constructor(options) {
    this.options = options;
    const schema = {};
    const fields = [];
    for (const fieldName of this.options.fieldNames) {
      const definition = this.options.collectionSchema[fieldName];
      assert(definition, "Missing definition for field", fieldName);
      schema[fieldName] = definition;
      fields.push({ type: "Identifier", name: fieldName });
    }
    this.schema = schema;
    this.fields = fields;
    this.collation = this.options.collation;
  }
  options;
  schema;
  fields;
  supportedLookupTypes = [
    "All" /* All */,
    "Equals" /* Equals */,
    "NotEquals" /* NotEquals */,
    "LessThan" /* LessThan */,
    "GreaterThan" /* GreaterThan */,
    "Contains" /* Contains */,
    "StartsWith" /* StartsWith */,
    "EndsWith" /* EndsWith */
  ];
  modelPromise;
  model;
  collation;
  async loadModel() {
    const [bytes] = await rangeRequest(this.options.url, [this.options.range]);
    assert(bytes, "Failed to load model");
    return DatabaseDictionaryIndexModel.deserialize(bytes);
  }
  async getModel() {
    this.modelPromise ??= this.loadModel();
    this.model ??= await this.modelPromise;
    return this.model;
  }
  async lookupItems(queries) {
    assert(queries.length === this.fields.length, "Invalid query length");
    const model = await this.getModel();
    const chunks = queries.reduce(
      (memo, query, position) => memo.flatMap((entries) => {
        switch (query.type) {
          case "All" /* All */:
            return [entries];
          case "Equals" /* Equals */:
            return this.queryEquals(entries, query, position);
          case "NotEquals" /* NotEquals */:
            return this.queryNotEquals(entries, query, position);
          case "LessThan" /* LessThan */:
            return this.queryLessThan(entries, query, position);
          case "GreaterThan" /* GreaterThan */:
            return this.queryGreaterThan(entries, query, position);
          case "Contains" /* Contains */:
            return this.queryContains(entries, query, position);
          case "StartsWith" /* StartsWith */:
            return this.queryStartsWith(entries, query, position);
          case "EndsWith" /* EndsWith */:
            return this.queryEndsWith(entries, query, position);
          default:
            assertNever(query);
        }
      }),
      [model.entries]
    );
    const result = [];
    for (const entries of chunks) {
      for (const entry of entries) {
        const data = {};
        for (let i = 0; i < this.options.fieldNames.length; i++) {
          const fieldName = this.options.fieldNames[i];
          const fieldValue = entry.values[i];
          data[fieldName] = fieldValue;
        }
        result.push({ pointer: entry.pointer.toString(), data });
      }
    }
    return result;
  }
  queryEquals(entries, query, position) {
    const leftMost = this.getLeftMost(entries, position, query.value);
    const rightMost = this.getRightMost(entries, position, query.value);
    const slice = entries.slice(leftMost, rightMost + 1);
    return slice.length > 0 ? [slice] : [];
  }
  queryNotEquals(entries, query, position) {
    const leftMost = this.getLeftMost(entries, position, query.value);
    const rightMost = this.getRightMost(entries, position, query.value);
    const result = [];
    const before = entries.slice(0, leftMost);
    if (before.length > 0) result.push(before);
    const after = entries.slice(rightMost + 1);
    if (after.length > 0) result.push(after);
    return result;
  }
  queryLessThan(entries, query, position) {
    const nullBound = this.getRightMost(entries, position, null);
    entries = entries.slice(nullBound + 1);
    if (query.inclusive) {
      const rightMost = this.getRightMost(entries, position, query.value);
      const slice2 = entries.slice(0, rightMost + 1);
      return slice2.length > 0 ? [slice2] : [];
    }
    const leftMost = this.getLeftMost(entries, position, query.value);
    const slice = entries.slice(0, leftMost);
    return slice.length > 0 ? [slice] : [];
  }
  queryGreaterThan(entries, query, position) {
    const nullBound = this.getRightMost(entries, position, null);
    entries = entries.slice(nullBound + 1);
    if (query.inclusive) {
      const leftMost = this.getLeftMost(entries, position, query.value);
      const slice2 = entries.slice(leftMost);
      return slice2.length > 0 ? [slice2] : [];
    }
    const rightMost = this.getRightMost(entries, position, query.value);
    const slice = entries.slice(rightMost + 1);
    return slice.length > 0 ? [slice] : [];
  }
  queryContains(entries, query, position) {
    return this.findItems(entries, position, (current) => {
      if (current?.type !== ControlType2.String) return false;
      if (query.value?.type !== ControlType2.String) return false;
      let source = current.value;
      let target = query.value.value;
      if (this.collation.type === 0 /* CaseInsensitive */) {
        source = source.toLowerCase();
        target = target.toLowerCase();
      }
      return source.includes(target);
    });
  }
  queryStartsWith(entries, query, position) {
    return this.findItems(entries, position, (current) => {
      if (current?.type !== ControlType2.String) return false;
      if (query.value?.type !== ControlType2.String) return false;
      let source = current.value;
      let target = query.value.value;
      if (this.collation.type === 0 /* CaseInsensitive */) {
        source = source.toLowerCase();
        target = target.toLowerCase();
      }
      return source.startsWith(target);
    });
  }
  queryEndsWith(entries, query, position) {
    return this.findItems(entries, position, (current) => {
      if (current?.type !== ControlType2.String) return false;
      if (query.value?.type !== ControlType2.String) return false;
      let source = current.value;
      let target = query.value.value;
      if (this.collation.type === 0 /* CaseInsensitive */) {
        source = source.toLowerCase();
        target = target.toLowerCase();
      }
      return source.endsWith(target);
    });
  }
  /**
   * Returns the index of the left most entry that is equal to the target.
   *
   * \`\`\`text
   *   Left most
   *       \u2193
   * \u250C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u2510
   * \u2502 1 \u2502 2 \u2502 2 \u2502 2 \u2502 2 \u2502 3 \u2502
   * \u2514\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2518
   * \`\`\`
   *
   * @param entries The entries array to search in.
   * @param position The position of the value in the entry.
   * @param target The target value to search for.
   * @returns The index of the left most entry that is equal to the target.
   */
  getLeftMost(entries, position, target) {
    let left = 0;
    let right = entries.length;
    while (left < right) {
      const middle = left + right >> 1;
      const entry = entries[middle];
      const value = entry.values[position];
      if (DatabaseValueModel.compare(value, target, this.collation) < 0) {
        left = middle + 1;
      } else {
        right = middle;
      }
    }
    return left;
  }
  /**
   * Returns the index of the right most entry that is equal to the target.
   *
   * \`\`\`text
   *              Right most
   *                   \u2193
   * \u250C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u2510
   * \u2502 1 \u2502 2 \u2502 2 \u2502 2 \u2502 2 \u2502 3 \u2502
   * \u2514\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2518
   * \`\`\`
   *
   * @param entries The entries array to search in.
   * @param position The position of the value in the entry.
   * @param target The target value to search for.
   * @returns The index of the right most entry that is equal to the target.
   */
  getRightMost(entries, position, target) {
    let left = 0;
    let right = entries.length;
    while (left < right) {
      const middle = left + right >> 1;
      const entry = entries[middle];
      const value = entry.values[position];
      if (DatabaseValueModel.compare(value, target, this.collation) > 0) {
        right = middle;
      } else {
        left = middle + 1;
      }
    }
    return right - 1;
  }
  /**
   * Finds all items that are matching the predicate and groups adjacent items together.
   *
   * @param entries The entries array to search in.
   * @param position The position of the value in the entry.
   * @param predicate The predicate to match the values against.
   * @returns An array of chunks that match the predicate.
   */
  findItems(entries, position, predicate) {
    const result = [];
    let start = 0;
    for (let i = 0; i < entries.length; i++) {
      const entry = entries[i];
      const value = entry.values[position];
      const isMatching = predicate(value);
      if (isMatching) continue;
      if (start < i) {
        const slice = entries.slice(start, i);
        result.push(slice);
      }
      start = i + 1;
    }
    if (start < entries.length) {
      const slice = entries.slice(start);
      result.push(slice);
    }
    return result;
  }
};

// src/code-generation/components/cms/bundled/models/DatabaseItemModel.ts
function readDatabaseItemData(reader) {
  const data = {};
  const fieldCount = reader.readUint16();
  for (let i = 0; i < fieldCount; i++) {
    const key = reader.readString();
    data[key] = DatabaseValueModel.read(reader);
  }
  return data;
}

// src/code-generation/components/cms/bundled/DatabaseCollection.legacy.ts?bundle
var DatabaseChunk = class {
  constructor(id, url) {
    this.id = id;
    this.url = url;
  }
  id;
  url;
  itemsPromise;
  itemLoader = new import_dataloader.default(
    async (pointers) => {
      const ranges = pointers.map((pointer) => {
        const pointerModel = DatabaseItemPointerModel.fromString(pointer);
        return { from: pointerModel.offset, to: pointerModel.offset + pointerModel.length };
      });
      const rangeBytes = await rangeRequest(this.url, ranges);
      return rangeBytes.map((bytes, index) => {
        const reader = new BufferReader(bytes);
        const data = readDatabaseItemData(reader);
        const pointer = pointers[index];
        assert(pointer, "Missing pointer");
        return {
          pointer,
          data
        };
      });
    },
    { maxBatchSize: 250 }
  );
  scanItems() {
    this.itemsPromise ??= fetchWithRetries(this.url).then(async (response) => {
      if (!response.ok) {
        throw new Error(\`Request failed: \${response.status} \${response.statusText}\`);
      }
      const buffer = await response.arrayBuffer();
      const bytes = new Uint8Array(buffer);
      const reader = new BufferReader(bytes);
      const items = [];
      const itemCount = reader.readUint32();
      for (let i = 0; i < itemCount; i++) {
        const offset = reader.getOffset();
        const data = readDatabaseItemData(reader);
        const length = reader.getOffset() - offset;
        const pointerModel = new DatabaseItemPointerModel(this.id, offset, length);
        const pointer = pointerModel.toString();
        const item = {
          pointer,
          data
        };
        this.itemLoader.prime(pointer, item);
        items.push(item);
      }
      return items;
    });
    return this.itemsPromise;
  }
  resolveItem(pointer) {
    return this.itemLoader.load(pointer);
  }
};
var DatabaseCollection = class {
  constructor(options) {
    this.options = options;
    this.chunks = this.options.chunks.map((url, id) => {
      return new DatabaseChunk(id, url);
    });
    this.schema = options.schema;
    this.indexes = options.indexes;
    this.resolveRichText = options.resolveRichText;
    this.resolveVectorSetItem = options.resolveVectorSetItem;
    this.id = options.id;
  }
  options;
  id;
  schema;
  indexes;
  resolveRichText;
  resolveVectorSetItem;
  chunks;
  async scanItems() {
    const chunkedItems = await Promise.all(
      this.chunks.map(async (chunk) => {
        return chunk.scanItems();
      })
    );
    return chunkedItems.flat();
  }
  resolveItems(pointers) {
    return Promise.all(
      pointers.map((pointer) => {
        const pointerModel = DatabaseItemPointerModel.fromString(pointer);
        const chunk = this.chunks[pointerModel.chunkId];
        assert(chunk, "Missing chunk");
        return chunk.resolveItem(pointer);
      })
    );
  }
  compareItems(left, right) {
    const leftModel = DatabaseItemPointerModel.fromString(left.pointer);
    const rightModel = DatabaseItemPointerModel.fromString(right.pointer);
    return leftModel.compare(rightModel);
  }
  compareValues(left, right, collation) {
    return DatabaseValueModel.compare(left, right, collation);
  }
};
export {
  DatabaseCollection,
  DatabaseDictionaryIndex
};
`;
var Cc = `var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJS = (cb, mod) => function __require() {
  try {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  } catch (e) {
    throw mod = 0, e;
  }
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// ../../../node_modules/dataloader/index.js
var require_dataloader = __commonJS({
  "../../../node_modules/dataloader/index.js"(exports, module) {
    "use strict";
    var DataLoader2 = /* @__PURE__ */ (function() {
      function DataLoader3(batchLoadFn, options) {
        if (typeof batchLoadFn !== "function") {
          throw new TypeError("DataLoader must be constructed with a function which accepts " + ("Array<key> and returns Promise<Array<value>>, but got: " + batchLoadFn + "."));
        }
        this._batchLoadFn = batchLoadFn;
        this._maxBatchSize = getValidMaxBatchSize(options);
        this._batchScheduleFn = getValidBatchScheduleFn(options);
        this._cacheKeyFn = getValidCacheKeyFn(options);
        this._cacheMap = getValidCacheMap(options);
        this._batch = null;
        this.name = getValidName(options);
      }
      var _proto = DataLoader3.prototype;
      _proto.load = function load(key) {
        if (key === null || key === void 0) {
          throw new TypeError("The loader.load() function must be called with a value, " + ("but got: " + String(key) + "."));
        }
        var batch = getCurrentBatch(this);
        var cacheMap = this._cacheMap;
        var cacheKey = this._cacheKeyFn(key);
        if (cacheMap) {
          var cachedPromise = cacheMap.get(cacheKey);
          if (cachedPromise) {
            var cacheHits = batch.cacheHits || (batch.cacheHits = []);
            return new Promise(function(resolve) {
              cacheHits.push(function() {
                resolve(cachedPromise);
              });
            });
          }
        }
        batch.keys.push(key);
        var promise = new Promise(function(resolve, reject) {
          batch.callbacks.push({
            resolve,
            reject
          });
        });
        if (cacheMap) {
          cacheMap.set(cacheKey, promise);
        }
        return promise;
      };
      _proto.loadMany = function loadMany(keys) {
        if (!isArrayLike(keys)) {
          throw new TypeError("The loader.loadMany() function must be called with Array<key> " + ("but got: " + keys + "."));
        }
        var loadPromises = [];
        for (var i = 0; i < keys.length; i++) {
          loadPromises.push(this.load(keys[i])["catch"](function(error) {
            return error;
          }));
        }
        return Promise.all(loadPromises);
      };
      _proto.clear = function clear(key) {
        var cacheMap = this._cacheMap;
        if (cacheMap) {
          var cacheKey = this._cacheKeyFn(key);
          cacheMap["delete"](cacheKey);
        }
        return this;
      };
      _proto.clearAll = function clearAll() {
        var cacheMap = this._cacheMap;
        if (cacheMap) {
          cacheMap.clear();
        }
        return this;
      };
      _proto.prime = function prime(key, value) {
        var cacheMap = this._cacheMap;
        if (cacheMap) {
          var cacheKey = this._cacheKeyFn(key);
          if (cacheMap.get(cacheKey) === void 0) {
            var promise;
            if (value instanceof Error) {
              promise = Promise.reject(value);
              promise["catch"](function() {
              });
            } else {
              promise = Promise.resolve(value);
            }
            cacheMap.set(cacheKey, promise);
          }
        }
        return this;
      };
      return DataLoader3;
    })();
    var enqueuePostPromiseJob = typeof process === "object" && typeof process.nextTick === "function" ? function(fn) {
      if (!resolvedPromise) {
        resolvedPromise = Promise.resolve();
      }
      resolvedPromise.then(function() {
        process.nextTick(fn);
      });
    } : typeof setImmediate === "function" ? function(fn) {
      setImmediate(fn);
    } : function(fn) {
      setTimeout(fn);
    };
    var resolvedPromise;
    function getCurrentBatch(loader) {
      var existingBatch = loader._batch;
      if (existingBatch !== null && !existingBatch.hasDispatched && existingBatch.keys.length < loader._maxBatchSize) {
        return existingBatch;
      }
      var newBatch = {
        hasDispatched: false,
        keys: [],
        callbacks: []
      };
      loader._batch = newBatch;
      loader._batchScheduleFn(function() {
        dispatchBatch(loader, newBatch);
      });
      return newBatch;
    }
    function dispatchBatch(loader, batch) {
      batch.hasDispatched = true;
      if (batch.keys.length === 0) {
        resolveCacheHits(batch);
        return;
      }
      var batchPromise;
      try {
        batchPromise = loader._batchLoadFn(batch.keys);
      } catch (e) {
        return failedDispatch(loader, batch, new TypeError("DataLoader must be constructed with a function which accepts Array<key> and returns Promise<Array<value>>, but the function " + ("errored synchronously: " + String(e) + ".")));
      }
      if (!batchPromise || typeof batchPromise.then !== "function") {
        return failedDispatch(loader, batch, new TypeError("DataLoader must be constructed with a function which accepts Array<key> and returns Promise<Array<value>>, but the function did " + ("not return a Promise: " + String(batchPromise) + ".")));
      }
      batchPromise.then(function(values) {
        if (!isArrayLike(values)) {
          throw new TypeError("DataLoader must be constructed with a function which accepts Array<key> and returns Promise<Array<value>>, but the function did " + ("not return a Promise of an Array: " + String(values) + "."));
        }
        if (values.length !== batch.keys.length) {
          throw new TypeError("DataLoader must be constructed with a function which accepts Array<key> and returns Promise<Array<value>>, but the function did not return a Promise of an Array of the same length as the Array of keys." + ("\\n\\nKeys:\\n" + String(batch.keys)) + ("\\n\\nValues:\\n" + String(values)));
        }
        resolveCacheHits(batch);
        for (var i = 0; i < batch.callbacks.length; i++) {
          var value = values[i];
          if (value instanceof Error) {
            batch.callbacks[i].reject(value);
          } else {
            batch.callbacks[i].resolve(value);
          }
        }
      })["catch"](function(error) {
        failedDispatch(loader, batch, error);
      });
    }
    function failedDispatch(loader, batch, error) {
      resolveCacheHits(batch);
      for (var i = 0; i < batch.keys.length; i++) {
        loader.clear(batch.keys[i]);
        batch.callbacks[i].reject(error);
      }
    }
    function resolveCacheHits(batch) {
      if (batch.cacheHits) {
        for (var i = 0; i < batch.cacheHits.length; i++) {
          batch.cacheHits[i]();
        }
      }
    }
    function getValidMaxBatchSize(options) {
      var shouldBatch = !options || options.batch !== false;
      if (!shouldBatch) {
        return 1;
      }
      var maxBatchSize = options && options.maxBatchSize;
      if (maxBatchSize === void 0) {
        return Infinity;
      }
      if (typeof maxBatchSize !== "number" || maxBatchSize < 1) {
        throw new TypeError("maxBatchSize must be a positive number: " + maxBatchSize);
      }
      return maxBatchSize;
    }
    function getValidBatchScheduleFn(options) {
      var batchScheduleFn = options && options.batchScheduleFn;
      if (batchScheduleFn === void 0) {
        return enqueuePostPromiseJob;
      }
      if (typeof batchScheduleFn !== "function") {
        throw new TypeError("batchScheduleFn must be a function: " + batchScheduleFn);
      }
      return batchScheduleFn;
    }
    function getValidCacheKeyFn(options) {
      var cacheKeyFn = options && options.cacheKeyFn;
      if (cacheKeyFn === void 0) {
        return function(key) {
          return key;
        };
      }
      if (typeof cacheKeyFn !== "function") {
        throw new TypeError("cacheKeyFn must be a function: " + cacheKeyFn);
      }
      return cacheKeyFn;
    }
    function getValidCacheMap(options) {
      var shouldCache = !options || options.cache !== false;
      if (!shouldCache) {
        return null;
      }
      var cacheMap = options && options.cacheMap;
      if (cacheMap === void 0) {
        return /* @__PURE__ */ new Map();
      }
      if (cacheMap !== null) {
        var cacheFunctions = ["get", "set", "delete", "clear"];
        var missingFunctions = cacheFunctions.filter(function(fnName) {
          return cacheMap && typeof cacheMap[fnName] !== "function";
        });
        if (missingFunctions.length !== 0) {
          throw new TypeError("Custom cacheMap missing methods: " + missingFunctions.join(", "));
        }
      }
      return cacheMap;
    }
    function getValidName(options) {
      if (options && options.name) {
        return options.name;
      }
      return null;
    }
    function isArrayLike(x) {
      return typeof x === "object" && x !== null && typeof x.length === "number" && (x.length === 0 || x.length > 0 && Object.prototype.hasOwnProperty.call(x, x.length - 1));
    }
    module.exports = DataLoader2;
  }
});

// src/code-generation/components/cms/bundled/DatabaseCollection.ts?bundle
var import_dataloader = __toESM(require_dataloader(), 1);
import { yieldToMain as yieldToMain2 } from "framer";

// ../../shared/src/yield.ts
function resolveSchedulerPriority(priority) {
  return typeof priority === "function" ? priority() : priority;
}
var schedulerPriorityRank = /* @__PURE__ */ (() => ({
  background: 0,
  "user-visible": 1,
  "user-blocking": 2
}))();
function isHigherSchedulerPriority(requested, current) {
  return schedulerPriorityRank[requested] > schedulerPriorityRank[current];
}

// ../../library/src/utils/schedulerPriority.ts
function getHighestSchedulerPriority(prioritySources) {
  let highestPriority;
  for (const source of prioritySources) {
    const priority = resolveSchedulerPriority(source);
    if (highestPriority === void 0 || isHigherSchedulerPriority(priority, highestPriority)) {
      highestPriority = priority;
    }
    if (highestPriority === "user-blocking") return highestPriority;
  }
  return highestPriority;
}

// src/code-generation/components/cms/bundled/BufferReader.ts
var BYTE_LENGTH = {
  Uint8: 1,
  Uint16: 2,
  Uint32: 4,
  BigUint64: 8,
  Int8: 1,
  Int16: 2,
  Int32: 4,
  BigInt64: 8,
  Float32: 4,
  Float64: 8
};
var BufferReader = class _BufferReader {
  constructor(bytes) {
    this.bytes = bytes;
    this.view = getDataView(this.bytes);
  }
  bytes;
  // TextDecoder has no per-decode state unless streaming is enabled. Reusing one class-owned decoder
  // avoids constructing one for every CMS item, where each item gets its own BufferReader.
  static textDecoder = new TextDecoder();
  offset = 0;
  getOffset() {
    return this.offset;
  }
  view;
  ensureLength(requiredBytes) {
    const oldLength = this.bytes.length;
    if (this.offset + requiredBytes <= oldLength) return;
    throw new Error("Reading out of bounds");
  }
  readUint8() {
    const size = BYTE_LENGTH.Uint8;
    this.ensureLength(size);
    const value = this.view.getUint8(this.offset);
    this.offset += size;
    return value;
  }
  readUint16() {
    const size = BYTE_LENGTH.Uint16;
    this.ensureLength(size);
    const value = this.view.getUint16(this.offset);
    this.offset += size;
    return value;
  }
  readUint32() {
    const size = BYTE_LENGTH.Uint32;
    this.ensureLength(size);
    const value = this.view.getUint32(this.offset);
    this.offset += size;
    return value;
  }
  readUint64() {
    const bigint = this.readBigUint64();
    return Number(bigint);
  }
  readBigUint64() {
    const size = BYTE_LENGTH.BigUint64;
    this.ensureLength(size);
    const value = this.view.getBigUint64(this.offset);
    this.offset += size;
    return value;
  }
  readInt8() {
    const size = BYTE_LENGTH.Int8;
    this.ensureLength(size);
    const value = this.view.getInt8(this.offset);
    this.offset += size;
    return value;
  }
  readInt16() {
    const size = BYTE_LENGTH.Int16;
    this.ensureLength(size);
    const value = this.view.getInt16(this.offset);
    this.offset += size;
    return value;
  }
  readInt32() {
    const size = BYTE_LENGTH.Int32;
    this.ensureLength(size);
    const value = this.view.getInt32(this.offset);
    this.offset += size;
    return value;
  }
  readInt64() {
    const bigint = this.readBigInt64();
    return Number(bigint);
  }
  readBigInt64() {
    const size = BYTE_LENGTH.BigInt64;
    this.ensureLength(size);
    const value = this.view.getBigInt64(this.offset);
    this.offset += size;
    return value;
  }
  readFloat32() {
    const size = BYTE_LENGTH.Float32;
    this.ensureLength(size);
    const value = this.view.getFloat32(this.offset);
    this.offset += size;
    return value;
  }
  readFloat64() {
    const size = BYTE_LENGTH.Float64;
    this.ensureLength(size);
    const value = this.view.getFloat64(this.offset);
    this.offset += size;
    return value;
  }
  readBytes(length) {
    const start = this.offset;
    const end = start + length;
    const value = this.bytes.subarray(start, end);
    this.offset = end;
    return value;
  }
  readString() {
    const length = this.readUint32();
    const bytes = this.readBytes(length);
    return _BufferReader.textDecoder.decode(bytes);
  }
  readJson() {
    const string = this.readString();
    return JSON.parse(string);
  }
};
function getDataView(bytes) {
  return new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength);
}

// src/code-generation/components/cms/bundled/DatabaseDictionaryIndex.ts
import { ControlType as ControlType2, yieldToMain } from "framer";

// ../../library/src/utils/utils.ts
var isWindow = typeof window !== "undefined";
var supportsRequestIdleCallback = isWindow && typeof window.requestIdleCallback === "function";

// src/code-generation/components/cms/bundled/assert.ts
function assert(condition, ...msg) {
  if (condition) return;
  throw Error("Assertion Error" + (msg.length > 0 ? ": " + msg.join(" ") : ""));
}
function assertNever(condition) {
  throw new Error(\`Unexpected value: \${condition}\`);
}

// src/code-generation/components/cms/bundled/BufferWriter.ts
var INITIAL_BUFFER_SIZE = 1024;
var GROWTH_FACTOR = 1.5;
var maxUnsignedNumber = (bits) => 2 ** bits - 1;
var minSignedNumber = (bits) => -(2 ** (bits - 1));
var maxSignedNumber = (bits) => 2 ** (bits - 1) - 1;
var MIN_VALUE = {
  Uint8: 0,
  Uint16: 0,
  Uint32: 0,
  Uint64: 0,
  BigUint64: 0,
  Int8: minSignedNumber(8),
  Int16: minSignedNumber(16),
  Int32: minSignedNumber(32),
  Int64: Number.MIN_SAFE_INTEGER,
  BigInt64: -(BigInt(2) ** BigInt(63))
};
var MAX_VALUE = {
  Uint8: maxUnsignedNumber(8),
  Uint16: maxUnsignedNumber(16),
  Uint32: maxUnsignedNumber(32),
  Uint64: Number.MAX_SAFE_INTEGER,
  BigUint64: BigInt(2) ** BigInt(64) - BigInt(1),
  Int8: maxSignedNumber(8),
  Int16: maxSignedNumber(16),
  Int32: maxSignedNumber(32),
  Int64: Number.MAX_SAFE_INTEGER,
  BigInt64: BigInt(2) ** BigInt(63) - BigInt(1)
};
function assertRange(value, min, max, dataType) {
  assert(value >= min, value, "outside lower bound for", dataType);
  assert(value <= max, value, "outside upper bound for", dataType);
}
var BufferWriter = class {
  offset = 0;
  getOffset() {
    return this.offset;
  }
  bytes = new Uint8Array(INITIAL_BUFFER_SIZE);
  view = getDataView(this.bytes);
  slice(start = 0, end = this.offset) {
    return this.bytes.slice(start, end);
  }
  subarray(start = 0, end = this.offset) {
    return this.bytes.subarray(start, end);
  }
  encoder = new TextEncoder();
  ensureLength(requiredBytes) {
    const oldLength = this.bytes.length;
    if (this.offset + requiredBytes <= oldLength) return;
    const newLength = Math.ceil(oldLength * GROWTH_FACTOR) + requiredBytes;
    const newBytes = new Uint8Array(newLength);
    newBytes.set(this.bytes);
    this.bytes = newBytes;
    this.view = getDataView(newBytes);
  }
  writeUint8(value) {
    assertRange(value, MIN_VALUE.Uint8, MAX_VALUE.Uint8, "Uint8");
    const size = BYTE_LENGTH.Uint8;
    this.ensureLength(size);
    this.view.setUint8(this.offset, value);
    this.offset += size;
  }
  writeUint16(value) {
    assertRange(value, MIN_VALUE.Uint16, MAX_VALUE.Uint16, "Uint16");
    const size = BYTE_LENGTH.Uint16;
    this.ensureLength(size);
    this.view.setUint16(this.offset, value);
    this.offset += size;
  }
  writeUint32(value) {
    assertRange(value, MIN_VALUE.Uint32, MAX_VALUE.Uint32, "Uint32");
    const size = BYTE_LENGTH.Uint32;
    this.ensureLength(size);
    this.view.setUint32(this.offset, value);
    this.offset += size;
  }
  writeUint64(value) {
    assertRange(value, MIN_VALUE.Uint64, MAX_VALUE.Uint64, "Uint64");
    const bigint = BigInt(value);
    this.writeBigUint64(bigint);
  }
  writeBigUint64(value) {
    assertRange(value, MIN_VALUE.BigUint64, MAX_VALUE.BigUint64, "BigUint64");
    const size = BYTE_LENGTH.BigUint64;
    this.ensureLength(size);
    this.view.setBigUint64(this.offset, value);
    this.offset += size;
  }
  writeInt8(value) {
    assertRange(value, MIN_VALUE.Int8, MAX_VALUE.Int8, "Int8");
    const size = BYTE_LENGTH.Int8;
    this.ensureLength(size);
    this.view.setInt8(this.offset, value);
    this.offset += size;
  }
  writeInt16(value) {
    assertRange(value, MIN_VALUE.Int16, MAX_VALUE.Int16, "Int16");
    const size = BYTE_LENGTH.Int16;
    this.ensureLength(size);
    this.view.setInt16(this.offset, value);
    this.offset += size;
  }
  writeInt32(value) {
    assertRange(value, MIN_VALUE.Int32, MAX_VALUE.Int32, "Int32");
    const size = BYTE_LENGTH.Int32;
    this.ensureLength(size);
    this.view.setInt32(this.offset, value);
    this.offset += size;
  }
  writeInt64(value) {
    assertRange(value, MIN_VALUE.Int64, MAX_VALUE.Int64, "Int64");
    const bigint = BigInt(value);
    this.writeBigInt64(bigint);
  }
  writeBigInt64(value) {
    assertRange(value, MIN_VALUE.BigInt64, MAX_VALUE.BigInt64, "BigInt64");
    const size = BYTE_LENGTH.BigInt64;
    this.ensureLength(size);
    this.view.setBigInt64(this.offset, value);
    this.offset += size;
  }
  writeFloat32(value) {
    const size = BYTE_LENGTH.Float32;
    this.ensureLength(size);
    this.view.setFloat32(this.offset, value);
    this.offset += size;
  }
  writeFloat64(value) {
    const size = BYTE_LENGTH.Float64;
    this.ensureLength(size);
    this.view.setFloat64(this.offset, value);
    this.offset += size;
  }
  writeBytes(value) {
    const size = value.length;
    this.ensureLength(size);
    this.bytes.set(value, this.offset);
    this.offset += size;
  }
  encodedStrings = /* @__PURE__ */ new Map();
  encodeString(value) {
    const cached = this.encodedStrings.get(value);
    if (cached) return cached;
    const bytes = this.encoder.encode(value);
    this.encodedStrings.set(value, bytes);
    return bytes;
  }
  writeString(value) {
    const bytes = this.encodeString(value);
    const size = bytes.length;
    this.writeUint32(size);
    this.writeBytes(bytes);
  }
  writeJson(value) {
    const json = JSON.stringify(value);
    this.writeString(json);
  }
};

// src/utils/typeChecks.ts
function isString(value) {
  return typeof value === "string";
}
function isNumber(value) {
  return Number.isFinite(value);
}
function isNull(value) {
  return value === null;
}

// src/code-generation/components/cms/bundled/models/DatabaseItemPointerModel.ts
var DatabaseItemPointerModel = class _DatabaseItemPointerModel {
  constructor(chunkId, offset, length) {
    this.chunkId = chunkId;
    this.offset = offset;
    this.length = length;
  }
  chunkId;
  offset;
  length;
  static fromString(value) {
    const [chunkId, offset, length] = value.split("/").map(Number);
    assert(isNumber(chunkId), "Invalid chunkId");
    assert(isNumber(offset), "Invalid offset");
    assert(isNumber(length), "Invalid length");
    return new _DatabaseItemPointerModel(chunkId, offset, length);
  }
  toString() {
    return \`\${this.chunkId}/\${this.offset}/\${this.length}\`;
  }
  static read(reader) {
    const chunkId = reader.readUint16();
    const offset = reader.readUint32();
    const length = reader.readUint32();
    return new _DatabaseItemPointerModel(chunkId, offset, length);
  }
  write(writer) {
    writer.writeUint16(this.chunkId);
    writer.writeUint32(this.offset);
    writer.writeUint32(this.length);
  }
  compare(other) {
    if (this.chunkId < other.chunkId) return -1;
    if (this.chunkId > other.chunkId) return 1;
    if (this.offset < other.offset) return -1;
    if (this.offset > other.offset) return 1;
    assert(this.length === other.length);
    return 0;
  }
};

// src/code-generation/components/cms/bundled/models/DatabaseValueModel.ts
import { ControlType } from "framer";
function getValueType(value) {
  if (isNull(value)) {
    return 0 /* Null */;
  }
  switch (value.type) {
    case ControlType.Array:
      return 1 /* Array */;
    case ControlType.Boolean:
      return 2 /* Boolean */;
    case ControlType.Color:
      return 3 /* Color */;
    case ControlType.Date:
      return 4 /* Date */;
    case ControlType.Enum:
      return 5 /* Enum */;
    case ControlType.File:
      return 6 /* File */;
    case ControlType.ResponsiveImage:
      return 10 /* ResponsiveImage */;
    case ControlType.Link:
      return 7 /* Link */;
    case ControlType.Number:
      return 8 /* Number */;
    case ControlType.Object:
      return 9 /* Object */;
    case ControlType.RichText:
      return 11 /* RichText */;
    case ControlType.String:
      return 12 /* String */;
    case ControlType.VectorSetItem:
      return 13 /* VectorSetItem */;
    default:
      assertNever(value);
  }
}
var DatabaseValueModel;
((DatabaseValueModel2) => {
  function read(reader) {
    const type = reader.readUint8();
    switch (type) {
      case 0 /* Null */:
        return null;
      case 1 /* Array */:
        return readArray(reader);
      case 2 /* Boolean */:
        return readBoolean(reader);
      case 3 /* Color */:
        return readColor(reader);
      case 4 /* Date */:
        return readDate(reader);
      case 5 /* Enum */:
        return readEnum(reader);
      case 6 /* File */:
        return readFile(reader);
      case 7 /* Link */:
        return readLink(reader);
      case 8 /* Number */:
        return readNumber(reader);
      case 9 /* Object */:
        return readObject(reader);
      case 10 /* ResponsiveImage */:
        return readResponsiveImage(reader);
      case 11 /* RichText */:
        return readRichText(reader);
      case 12 /* String */:
        return readString(reader);
      case 13 /* VectorSetItem */:
        return readVectorSetItem(reader);
      default:
        assertNever(type);
    }
  }
  DatabaseValueModel2.read = read;
  function write(writer, value) {
    const type = getValueType(value);
    writer.writeUint8(type);
    if (isNull(value)) return;
    switch (value.type) {
      case ControlType.Array:
        return writeArray(writer, value);
      case ControlType.Boolean:
        return writeBoolean(writer, value);
      case ControlType.Color:
        return writeColor(writer, value);
      case ControlType.Date:
        return writeDate(writer, value);
      case ControlType.Enum:
        return writeEnum(writer, value);
      case ControlType.File:
        return writeFile(writer, value);
      case ControlType.Link:
        return writeLink(writer, value);
      case ControlType.Number:
        return writeNumber(writer, value);
      case ControlType.Object:
        return writeObject(writer, value);
      case ControlType.ResponsiveImage:
        return writeResponsiveImage(writer, value);
      case ControlType.RichText:
        return writeRichText(writer, value);
      case ControlType.VectorSetItem:
        return writeVectorSetItem(writer, value);
      case ControlType.String:
        return writeString(writer, value);
      default:
        assertNever(value);
    }
  }
  DatabaseValueModel2.write = write;
  function compare(left, right, collation) {
    const leftType = getValueType(left);
    const rightType = getValueType(right);
    if (leftType < rightType) return -1;
    if (leftType > rightType) return 1;
    if (isNull(left) || isNull(right)) return 0;
    switch (left.type) {
      case ControlType.Array:
        assert(right.type === ControlType.Array);
        return compareArray(left, right, collation);
      case ControlType.Boolean:
        assert(right.type === ControlType.Boolean);
        return compareBoolean(left, right);
      case ControlType.Color:
        assert(right.type === ControlType.Color);
        return compareColor(left, right);
      case ControlType.Date:
        assert(right.type === ControlType.Date);
        return compareDate(left, right);
      case ControlType.Enum:
        assert(right.type === ControlType.Enum);
        return compareEnum(left, right);
      case ControlType.File:
        assert(right.type === ControlType.File);
        return compareFile(left, right);
      case ControlType.Link:
        assert(right.type === ControlType.Link);
        return compareLink(left, right);
      case ControlType.Number:
        assert(right.type === ControlType.Number);
        return compareNumber(left, right);
      case ControlType.Object:
        assert(right.type === ControlType.Object);
        return compareObject(left, right, collation);
      case ControlType.ResponsiveImage:
        assert(right.type === ControlType.ResponsiveImage);
        return compareResponsiveImage(left, right);
      case ControlType.RichText:
        assert(right.type === ControlType.RichText);
        return compareRichText(left, right);
      case ControlType.VectorSetItem:
        assert(right.type === ControlType.VectorSetItem);
        return compareVectorSetItem(left, right);
      case ControlType.String:
        assert(right.type === ControlType.String);
        return compareString(left, right, collation);
      default:
        assertNever(left);
    }
  }
  DatabaseValueModel2.compare = compare;
})(DatabaseValueModel || (DatabaseValueModel = {}));
function readArray(reader) {
  const length = reader.readUint16();
  const value = [];
  for (let i = 0; i < length; i++) {
    const item = DatabaseValueModel.read(reader);
    value.push(item);
  }
  return {
    type: ControlType.Array,
    value
  };
}
function writeArray(writer, value) {
  writer.writeUint16(value.value.length);
  for (const item of value.value) {
    DatabaseValueModel.write(writer, item);
  }
}
function compareArray(left, right, collation) {
  const leftLength = left.value.length;
  const rightLength = right.value.length;
  if (leftLength < rightLength) return -1;
  if (leftLength > rightLength) return 1;
  for (let i = 0; i < leftLength; i++) {
    const leftItem = left.value[i];
    const rightItem = right.value[i];
    const result = DatabaseValueModel.compare(leftItem, rightItem, collation);
    if (result !== 0) return result;
  }
  return 0;
}
function readBoolean(reader) {
  return {
    type: ControlType.Boolean,
    value: reader.readUint8() !== 0
  };
}
function writeBoolean(writer, value) {
  writer.writeUint8(value.value ? 1 : 0);
}
function compareBoolean(left, right) {
  if (left.value < right.value) return -1;
  if (left.value > right.value) return 1;
  return 0;
}
function readColor(reader) {
  return {
    type: ControlType.Color,
    value: reader.readString()
  };
}
function writeColor(writer, value) {
  writer.writeString(value.value);
}
function compareColor(left, right) {
  if (left.value < right.value) return -1;
  if (left.value > right.value) return 1;
  return 0;
}
function readDate(reader) {
  const timestamp = reader.readInt64();
  const date = new Date(timestamp);
  return {
    type: ControlType.Date,
    value: date.toISOString()
  };
}
function writeDate(writer, value) {
  const date = new Date(value.value);
  const timestamp = date.getTime();
  writer.writeInt64(timestamp);
}
function compareDate(left, right) {
  const leftDate = new Date(left.value);
  const rightDate = new Date(right.value);
  if (leftDate < rightDate) return -1;
  if (leftDate > rightDate) return 1;
  return 0;
}
function readEnum(reader) {
  return {
    type: ControlType.Enum,
    value: reader.readString()
  };
}
function writeEnum(writer, value) {
  writer.writeString(value.value);
}
function compareEnum(left, right) {
  if (left.value < right.value) return -1;
  if (left.value > right.value) return 1;
  return 0;
}
function readFile(reader) {
  return {
    type: ControlType.File,
    value: reader.readString()
  };
}
function writeFile(writer, value) {
  writer.writeString(value.value);
}
function compareFile(left, right) {
  if (left.value < right.value) return -1;
  if (left.value > right.value) return 1;
  return 0;
}
function readLink(reader) {
  return {
    type: ControlType.Link,
    value: reader.readJson()
  };
}
function writeLink(writer, value) {
  writer.writeJson(value.value);
}
function compareLink(left, right) {
  const leftEncoded = JSON.stringify(left.value);
  const rightEncoded = JSON.stringify(right.value);
  if (leftEncoded < rightEncoded) return -1;
  if (leftEncoded > rightEncoded) return 1;
  return 0;
}
function readNumber(reader) {
  return {
    type: ControlType.Number,
    value: reader.readFloat64()
  };
}
function writeNumber(writer, value) {
  writer.writeFloat64(value.value);
}
function compareNumber(left, right) {
  if (left.value < right.value) return -1;
  if (left.value > right.value) return 1;
  return 0;
}
function readObject(reader) {
  const length = reader.readUint16();
  const result = {};
  for (let i = 0; i < length; i++) {
    const key = reader.readString();
    result[key] = DatabaseValueModel.read(reader);
  }
  return {
    type: ControlType.Object,
    value: result
  };
}
function writeObject(writer, value) {
  const entries = Object.entries(value.value);
  writer.writeUint16(entries.length);
  for (const [key, item] of entries) {
    writer.writeString(key);
    DatabaseValueModel.write(writer, item);
  }
}
function compareObject(left, right, collation) {
  const leftKeys = Object.keys(left.value).sort();
  const rightKeys = Object.keys(right.value).sort();
  if (leftKeys.length < rightKeys.length) return -1;
  if (leftKeys.length > rightKeys.length) return 1;
  for (let i = 0; i < leftKeys.length; i++) {
    const leftKey = leftKeys[i];
    const rightKey = rightKeys[i];
    if (leftKey < rightKey) return -1;
    if (leftKey > rightKey) return 1;
    const leftValue = left.value[leftKey] ?? null;
    const rightValue = right.value[rightKey] ?? null;
    const result = DatabaseValueModel.compare(leftValue, rightValue, collation);
    if (result !== 0) return result;
  }
  return 0;
}
function readResponsiveImage(reader) {
  return {
    type: ControlType.ResponsiveImage,
    value: reader.readJson()
  };
}
function writeResponsiveImage(writer, value) {
  writer.writeJson(value.value);
}
function compareResponsiveImage(left, right) {
  const leftEncoded = JSON.stringify(left.value);
  const rightEncoded = JSON.stringify(right.value);
  if (leftEncoded < rightEncoded) return -1;
  if (leftEncoded > rightEncoded) return 1;
  return 0;
}
function readRichText(reader) {
  const type = reader.readInt8();
  if (type === 0) {
    return {
      type: ControlType.RichText,
      value: reader.readUint32()
    };
  }
  if (type === 1) {
    return {
      type: ControlType.RichText,
      value: reader.readString()
    };
  }
  throw new Error(\`Invalid rich text pointer\`);
}
function writeRichText(writer, value) {
  if (isNumber(value.value)) {
    writer.writeInt8(0);
    writer.writeUint32(value.value);
    return;
  }
  if (isString(value.value)) {
    writer.writeInt8(1);
    writer.writeString(value.value);
    return;
  }
  throw new Error(\`Invalid rich text pointer\`);
}
function compareRichText(left, right) {
  const leftValue = left.value;
  const rightValue = right.value;
  if (isNumber(leftValue) && isNumber(rightValue)) {
    if (leftValue < rightValue) return -1;
    if (leftValue > rightValue) return 1;
    return 0;
  }
  if (isString(leftValue) && isString(rightValue)) {
    if (leftValue < rightValue) return -1;
    if (leftValue > rightValue) return 1;
    return 0;
  }
  throw new Error(\`Invalid rich text pointer\`);
}
function readString(reader) {
  return {
    type: ControlType.String,
    value: reader.readString()
  };
}
function writeString(writer, value) {
  writer.writeString(value.value);
}
function compareString(left, right, collation) {
  let leftValue = left.value;
  let rightValue = right.value;
  if (collation.type === 0 /* CaseInsensitive */) {
    leftValue = left.value.toLowerCase();
    rightValue = right.value.toLowerCase();
  }
  if (leftValue < rightValue) return -1;
  if (leftValue > rightValue) return 1;
  return 0;
}
function readVectorSetItem(reader) {
  return {
    type: ControlType.VectorSetItem,
    value: reader.readUint32()
  };
}
function writeVectorSetItem(writer, value) {
  writer.writeUint32(value.value);
}
function compareVectorSetItem(left, right) {
  const leftValue = left.value;
  const rightValue = right.value;
  if (leftValue < rightValue) return -1;
  if (leftValue > rightValue) return 1;
  return 0;
}

// src/code-generation/components/cms/bundled/models/DatabaseDictionaryIndexModel.ts
var DatabaseDictionaryIndexModel = class _DatabaseDictionaryIndexModel {
  constructor(fieldNames, options) {
    this.fieldNames = fieldNames;
    this.options = options;
  }
  fieldNames;
  options;
  entries = [];
  sortEntries() {
    this.entries.sort((left, right) => {
      for (let i = 0; i < this.fieldNames.length; i++) {
        const leftValue = left.values[i];
        const rightValue = right.values[i];
        const result = DatabaseValueModel.compare(leftValue, rightValue, this.options.collation);
        if (result !== 0) return result;
      }
      return left.pointer.compare(right.pointer);
    });
  }
  static async deserialize(bytes, yieldBeforeEntry) {
    const reader = new BufferReader(bytes);
    const collation = reader.readJson();
    const fieldCount = reader.readUint8();
    const fieldNames = [];
    for (let i = 0; i < fieldCount; i++) {
      const fieldName = reader.readString();
      fieldNames.push(fieldName);
    }
    const index = new _DatabaseDictionaryIndexModel(fieldNames, {
      collation
    });
    const entryCount = reader.readUint32();
    const readEntry = () => {
      const values = [];
      for (let j = 0; j < fieldCount; j++) {
        const fieldValue = DatabaseValueModel.read(reader);
        values.push(fieldValue);
      }
      const pointer = DatabaseItemPointerModel.read(reader);
      index.entries.push({ values, pointer });
    };
    for (let i = 0; i < entryCount; i++) {
      const yieldPromise = yieldBeforeEntry?.();
      if (yieldPromise) await yieldPromise;
      readEntry();
    }
    return index;
  }
  serialize() {
    const writer = new BufferWriter();
    writer.writeJson(this.options.collation);
    writer.writeUint8(this.fieldNames.length);
    for (const fieldName of this.fieldNames) {
      writer.writeString(fieldName);
    }
    this.sortEntries();
    writer.writeUint32(this.entries.length);
    for (const entry of this.entries) {
      const { values, pointer } = entry;
      for (const value of values) {
        DatabaseValueModel.write(writer, value);
      }
      pointer.write(writer);
    }
    return writer.subarray();
  }
  addItem(item, pointer) {
    const values = this.fieldNames.map((fieldName) => {
      return item.getField(fieldName) ?? null;
    });
    this.entries.push({ values, pointer });
  }
};

// src/code-generation/components/cms/bundled/fetchWithRetries.ts
var maxRetries = 3;
var baseDelay = 250;
var retriableHTTPCodes = [
  408,
  // Request Timeout
  429,
  // Too Many Requests
  500,
  // Internal Server Error
  502,
  // Bad Gateway
  503,
  // Service Unavailable
  504
  // Gateway Timeout
];
var fetchWithRetries = async (input, init) => {
  let retryCount = 0;
  while (true) {
    try {
      const res = await fetch(input, init);
      if (!retriableHTTPCodes.includes(res.status) || ++retryCount > maxRetries) {
        return res;
      }
    } catch (error) {
      if (init?.signal?.aborted || ++retryCount > maxRetries) {
        throw error;
      }
    }
    await retryDelay(retryCount);
  }
};
async function retryDelay(retryCount) {
  const backoffExponent = retryCount - 1;
  const jitter = Math.random() + 1;
  const timeout = Math.floor(baseDelay * jitter * 2 ** backoffExponent);
  await new Promise((resolve) => {
    setTimeout(resolve, timeout);
  });
}

// src/code-generation/components/cms/bundled/rangeRequest.ts
async function rangeRequest(url, ranges) {
  const collapsedRanges = collapseRanges(ranges);
  const rangeStrings = [];
  let expectedLength = 0;
  for (const range of collapsedRanges) {
    rangeStrings.push(\`\${range.from}-\${range.to - 1}\`);
    expectedLength += range.to - range.from;
  }
  const rangeUrl = new URL(url);
  const rangeQuery = rangeStrings.join(",");
  rangeUrl.searchParams.set("range", rangeQuery);
  const response = await fetchWithRetries(rangeUrl);
  if (response.status !== 200) {
    throw new Error(\`Request failed: \${response.status} \${response.statusText}\`);
  }
  const buffer = await response.arrayBuffer();
  const bytes = new Uint8Array(buffer);
  if (bytes.length !== expectedLength) {
    throw new Error("Request failed: Unexpected response length");
  }
  const file = new PartialFile();
  let start = 0;
  for (const range of collapsedRanges) {
    const length = range.to - range.from;
    const end = start + length;
    const part = bytes.subarray(start, end);
    file.write(range.from, part);
    start = end;
  }
  return ranges.map((range) => {
    return file.read(range.from, range.to - range.from);
  });
}
var PartialFile = class {
  chunks = [];
  read(position, length) {
    for (const chunk of this.chunks) {
      if (position < chunk.start) {
        throw new Error("Missing data");
      }
      if (position > chunk.end) {
        continue;
      }
      if (position + length > chunk.end) {
        throw new Error("Missing data");
      }
      const from = position - chunk.start;
      const to = from + length;
      return chunk.data.slice(from, to);
    }
    throw new Error("Missing data");
  }
  write(position, data) {
    let start = position;
    let end = start + data.length;
    let fromIndex = 0;
    let toIndex = this.chunks.length;
    for (; fromIndex < toIndex; fromIndex++) {
      const chunk2 = this.chunks[fromIndex];
      assert(chunk2, "Missing chunk");
      if (start > chunk2.end) {
        continue;
      }
      if (start > chunk2.start) {
        const offset = start - chunk2.start;
        const prefix = chunk2.data.subarray(0, offset);
        data = join(prefix, data);
        start = chunk2.start;
      }
      break;
    }
    for (; toIndex > fromIndex; toIndex--) {
      const chunk2 = this.chunks[toIndex - 1];
      assert(chunk2, "Missing chunk");
      if (end < chunk2.start) {
        continue;
      }
      if (end < chunk2.end) {
        const offset = end - chunk2.start;
        const suffix = chunk2.data.subarray(offset);
        data = join(data, suffix);
        end = chunk2.end;
      }
      break;
    }
    const chunk = {
      start,
      end,
      data
    };
    const deleteCount = toIndex - fromIndex;
    this.chunks.splice(fromIndex, deleteCount, chunk);
  }
};
function join(left, right) {
  const length = left.length + right.length;
  const result = new Uint8Array(length);
  result.set(left, 0);
  result.set(right, left.length);
  return result;
}
function collapseRanges(ranges) {
  assert(ranges.length > 0, "Must have at least one range");
  const sorted = [...ranges].sort((a, b) => a.from - b.from);
  const collapsed = [];
  for (const range of sorted) {
    const index = collapsed.length - 1;
    const previous = collapsed[index];
    if (previous && range.from <= previous.to) {
      collapsed[index] = {
        from: previous.from,
        to: Math.max(previous.to, range.to)
      };
    } else {
      collapsed.push(range);
    }
  }
  return collapsed;
}

// src/code-generation/components/cms/bundled/DatabaseDictionaryIndex.ts
var DatabaseDictionaryIndex = class {
  constructor(options) {
    this.options = options;
    const schema = {};
    const fields = [];
    for (const fieldName of this.options.fieldNames) {
      const definition = this.options.collectionSchema[fieldName];
      assert(definition, "Missing definition for field", fieldName);
      schema[fieldName] = definition;
      fields.push({ type: "Identifier", name: fieldName });
    }
    this.schema = schema;
    this.fields = fields;
    this.collation = this.options.collation;
  }
  options;
  schema;
  fields;
  supportedLookupTypes = [
    "All" /* All */,
    "Equals" /* Equals */,
    "NotEquals" /* NotEquals */,
    "LessThan" /* LessThan */,
    "GreaterThan" /* GreaterThan */,
    "Contains" /* Contains */,
    "StartsWith" /* StartsWith */,
    "EndsWith" /* EndsWith */
  ];
  modelPromise;
  model;
  modelPrioritySources = /* @__PURE__ */ new Set();
  collation;
  async loadModel() {
    const [bytes] = await rangeRequest(this.options.url, [this.options.range]);
    assert(bytes, "Failed to load model");
    return DatabaseDictionaryIndexModel.deserialize(bytes, () => {
      const priority = getHighestSchedulerPriority(this.modelPrioritySources);
      return priority ? yieldToMain({ batch: true, priority }) : void 0;
    });
  }
  async getModel(priority) {
    if (this.model) return this.model;
    if (priority) this.modelPrioritySources.add(priority);
    this.modelPromise ??= this.loadModel().finally(() => {
      this.modelPrioritySources.clear();
    });
    this.model ??= await this.modelPromise;
    return this.model;
  }
  async lookupItems(queries, priority) {
    assert(queries.length === this.fields.length, "Invalid query length");
    const model = await this.getModel(priority);
    let chunks = [model.entries];
    for (const [position, query] of queries.entries()) {
      const nextChunks = [];
      for (const entries of chunks) {
        const yieldPromise = priority ? yieldToMain({ batch: true, priority: resolveSchedulerPriority(priority) }) : void 0;
        if (yieldPromise) await yieldPromise;
        let matchingChunks;
        switch (query.type) {
          case "All" /* All */:
            matchingChunks = [entries];
            break;
          case "Equals" /* Equals */:
            matchingChunks = this.queryEquals(entries, query, position);
            break;
          case "NotEquals" /* NotEquals */:
            matchingChunks = this.queryNotEquals(entries, query, position);
            break;
          case "LessThan" /* LessThan */:
            matchingChunks = this.queryLessThan(entries, query, position);
            break;
          case "GreaterThan" /* GreaterThan */:
            matchingChunks = this.queryGreaterThan(entries, query, position);
            break;
          case "Contains" /* Contains */:
            matchingChunks = await this.queryContains(entries, query, position, priority);
            break;
          case "StartsWith" /* StartsWith */:
            matchingChunks = await this.queryStartsWith(entries, query, position, priority);
            break;
          case "EndsWith" /* EndsWith */:
            matchingChunks = await this.queryEndsWith(entries, query, position, priority);
            break;
          default:
            assertNever(query);
        }
        nextChunks.push(...matchingChunks);
      }
      chunks = nextChunks;
    }
    const result = [];
    for (const entries of chunks) {
      for (const entry of entries) {
        const yieldPromise = priority ? yieldToMain({ batch: true, priority: resolveSchedulerPriority(priority) }) : void 0;
        if (yieldPromise) await yieldPromise;
        const data = {};
        for (let i = 0; i < this.options.fieldNames.length; i++) {
          const fieldName = this.options.fieldNames[i];
          const fieldValue = entry.values[i];
          data[fieldName] = fieldValue;
        }
        result.push({ pointer: entry.pointer.toString(), data });
      }
    }
    return result;
  }
  queryEquals(entries, query, position) {
    const leftMost = this.getLeftMost(entries, position, query.value);
    const rightMost = this.getRightMost(entries, position, query.value);
    const slice = entries.slice(leftMost, rightMost + 1);
    return slice.length > 0 ? [slice] : [];
  }
  queryNotEquals(entries, query, position) {
    const leftMost = this.getLeftMost(entries, position, query.value);
    const rightMost = this.getRightMost(entries, position, query.value);
    const result = [];
    const before = entries.slice(0, leftMost);
    if (before.length > 0) result.push(before);
    const after = entries.slice(rightMost + 1);
    if (after.length > 0) result.push(after);
    return result;
  }
  queryLessThan(entries, query, position) {
    const nullBound = this.getRightMost(entries, position, null);
    entries = entries.slice(nullBound + 1);
    if (query.inclusive) {
      const rightMost = this.getRightMost(entries, position, query.value);
      const slice2 = entries.slice(0, rightMost + 1);
      return slice2.length > 0 ? [slice2] : [];
    }
    const leftMost = this.getLeftMost(entries, position, query.value);
    const slice = entries.slice(0, leftMost);
    return slice.length > 0 ? [slice] : [];
  }
  queryGreaterThan(entries, query, position) {
    const nullBound = this.getRightMost(entries, position, null);
    entries = entries.slice(nullBound + 1);
    if (query.inclusive) {
      const leftMost = this.getLeftMost(entries, position, query.value);
      const slice2 = entries.slice(leftMost);
      return slice2.length > 0 ? [slice2] : [];
    }
    const rightMost = this.getRightMost(entries, position, query.value);
    const slice = entries.slice(rightMost + 1);
    return slice.length > 0 ? [slice] : [];
  }
  queryContains(entries, query, position, priority) {
    return this.findItems(entries, position, priority, (current) => {
      if (current?.type !== ControlType2.String) return false;
      if (query.value?.type !== ControlType2.String) return false;
      let source = current.value;
      let target = query.value.value;
      if (this.collation.type === 0 /* CaseInsensitive */) {
        source = source.toLowerCase();
        target = target.toLowerCase();
      }
      return source.includes(target);
    });
  }
  queryStartsWith(entries, query, position, priority) {
    return this.findItems(entries, position, priority, (current) => {
      if (current?.type !== ControlType2.String) return false;
      if (query.value?.type !== ControlType2.String) return false;
      let source = current.value;
      let target = query.value.value;
      if (this.collation.type === 0 /* CaseInsensitive */) {
        source = source.toLowerCase();
        target = target.toLowerCase();
      }
      return source.startsWith(target);
    });
  }
  queryEndsWith(entries, query, position, priority) {
    return this.findItems(entries, position, priority, (current) => {
      if (current?.type !== ControlType2.String) return false;
      if (query.value?.type !== ControlType2.String) return false;
      let source = current.value;
      let target = query.value.value;
      if (this.collation.type === 0 /* CaseInsensitive */) {
        source = source.toLowerCase();
        target = target.toLowerCase();
      }
      return source.endsWith(target);
    });
  }
  /**
   * Returns the index of the left most entry that is equal to the target.
   *
   * \`\`\`text
   *   Left most
   *       \u2193
   * \u250C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u2510
   * \u2502 1 \u2502 2 \u2502 2 \u2502 2 \u2502 2 \u2502 3 \u2502
   * \u2514\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2518
   * \`\`\`
   *
   * @param entries The entries array to search in.
   * @param position The position of the value in the entry.
   * @param target The target value to search for.
   * @returns The index of the left most entry that is equal to the target.
   */
  getLeftMost(entries, position, target) {
    let left = 0;
    let right = entries.length;
    while (left < right) {
      const middle = left + right >> 1;
      const entry = entries[middle];
      const value = entry.values[position];
      if (DatabaseValueModel.compare(value, target, this.collation) < 0) {
        left = middle + 1;
      } else {
        right = middle;
      }
    }
    return left;
  }
  /**
   * Returns the index of the right most entry that is equal to the target.
   *
   * \`\`\`text
   *              Right most
   *                   \u2193
   * \u250C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u2510
   * \u2502 1 \u2502 2 \u2502 2 \u2502 2 \u2502 2 \u2502 3 \u2502
   * \u2514\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2518
   * \`\`\`
   *
   * @param entries The entries array to search in.
   * @param position The position of the value in the entry.
   * @param target The target value to search for.
   * @returns The index of the right most entry that is equal to the target.
   */
  getRightMost(entries, position, target) {
    let left = 0;
    let right = entries.length;
    while (left < right) {
      const middle = left + right >> 1;
      const entry = entries[middle];
      const value = entry.values[position];
      if (DatabaseValueModel.compare(value, target, this.collation) > 0) {
        right = middle;
      } else {
        left = middle + 1;
      }
    }
    return right - 1;
  }
  /**
   * Finds all items that are matching the predicate and groups adjacent items together.
   *
   * @param entries The entries array to search in.
   * @param position The position of the value in the entry.
   * @param predicate The predicate to match the values against.
   * @returns An array of chunks that match the predicate.
   */
  async findItems(entries, position, priority, predicate) {
    const result = [];
    let start = 0;
    for (let i = 0; i < entries.length; i++) {
      const yieldPromise = priority ? yieldToMain({ batch: true, priority: resolveSchedulerPriority(priority) }) : void 0;
      if (yieldPromise) await yieldPromise;
      const entry = entries[i];
      const value = entry.values[position];
      const isMatching = predicate(value);
      if (isMatching) continue;
      if (start < i) {
        const slice = entries.slice(start, i);
        result.push(slice);
      }
      start = i + 1;
    }
    if (start < entries.length) {
      const slice = entries.slice(start);
      result.push(slice);
    }
    return result;
  }
};

// src/code-generation/components/cms/bundled/models/DatabaseItemModel.ts
function readDatabaseItemData(reader) {
  const data = {};
  const fieldCount = reader.readUint16();
  for (let i = 0; i < fieldCount; i++) {
    const key = reader.readString();
    data[key] = DatabaseValueModel.read(reader);
  }
  return data;
}

// src/code-generation/components/cms/bundled/DatabaseCollection.ts?bundle
function* getRequestPrioritySources(requests) {
  for (const request of requests) {
    yield* request.prioritySources;
  }
}
var DatabaseChunk = class {
  constructor(id, url) {
    this.id = id;
    this.url = url;
  }
  id;
  url;
  itemsPromise;
  isScanning = false;
  scanPrioritySources = /* @__PURE__ */ new Set();
  itemPrioritySources = /* @__PURE__ */ new Map();
  itemLoader = new import_dataloader.default(
    async (requests) => {
      const ranges = requests.map(({ pointer }) => {
        const pointerModel = DatabaseItemPointerModel.fromString(pointer);
        return { from: pointerModel.offset, to: pointerModel.offset + pointerModel.length };
      });
      const rangeBytes = await rangeRequest(this.url, ranges);
      const items = [];
      for (let index = 0; index < rangeBytes.length; index++) {
        const priority = getHighestSchedulerPriority(getRequestPrioritySources(requests));
        const yieldPromise = priority ? yieldToMain2({ batch: true, priority }) : void 0;
        if (yieldPromise) await yieldPromise;
        const bytes = rangeBytes[index];
        assert(bytes, "Missing range bytes");
        const reader = new BufferReader(bytes);
        const data = readDatabaseItemData(reader);
        const pointer = requests[index]?.pointer;
        assert(pointer, "Missing pointer");
        items.push({
          pointer,
          data
        });
      }
      return items;
    },
    {
      // capping to 250 avoids overly long URLs that can cause requests to fail with 414
      maxBatchSize: 250,
      // Different route workflows can request the same item. Keep DataLoader's cache keyed by the
      // pointer while still carrying the strongest scheduling priority into the shared batch
      cacheKeyFn: (request) => request.pointer
    }
  );
  scanItems(priority) {
    if (this.itemsPromise) {
      if (this.isScanning && priority) this.scanPrioritySources.add(priority);
      return this.itemsPromise;
    }
    this.isScanning = true;
    if (priority) this.scanPrioritySources.add(priority);
    this.itemsPromise = fetchWithRetries(this.url).then(async (response) => {
      if (!response.ok) {
        throw new Error(\`Request failed: \${response.status} \${response.statusText}\`);
      }
      const buffer = await response.arrayBuffer();
      const bytes = new Uint8Array(buffer);
      const reader = new BufferReader(bytes);
      const items = [];
      const itemCount = reader.readUint32();
      for (let i = 0; i < itemCount; i++) {
        const priority2 = getHighestSchedulerPriority(this.scanPrioritySources);
        const yieldPromise = priority2 ? yieldToMain2({ batch: true, priority: priority2 }) : void 0;
        if (yieldPromise) await yieldPromise;
        const offset = reader.getOffset();
        const data = readDatabaseItemData(reader);
        const length = reader.getOffset() - offset;
        const pointerModel = new DatabaseItemPointerModel(this.id, offset, length);
        const pointer = pointerModel.toString();
        const item = {
          pointer,
          data
        };
        this.itemLoader.prime({ pointer, prioritySources: /* @__PURE__ */ new Set() }, item);
        items.push(item);
      }
      return items;
    }).finally(() => {
      this.isScanning = false;
      this.scanPrioritySources.clear();
    });
    return this.itemsPromise;
  }
  resolveItem(pointer, priority) {
    let prioritySources = this.itemPrioritySources.get(pointer);
    if (!prioritySources) {
      prioritySources = /* @__PURE__ */ new Set();
      this.itemPrioritySources.set(pointer, prioritySources);
    }
    if (priority) prioritySources.add(priority);
    const itemPromise = this.itemLoader.load({ pointer, prioritySources });
    const removePointer = () => this.itemPrioritySources.delete(pointer);
    void itemPromise.then(removePointer, removePointer);
    return itemPromise;
  }
};
var DatabaseCollection = class {
  constructor(options) {
    this.options = options;
    this.chunks = this.options.chunks.map((url, id) => {
      return new DatabaseChunk(id, url);
    });
    this.schema = options.schema;
    this.indexes = options.indexes;
    this.resolveRichText = options.resolveRichText;
    this.resolveVectorSetItem = options.resolveVectorSetItem;
    this.id = options.id;
  }
  options;
  id;
  schema;
  indexes;
  resolveRichText;
  resolveVectorSetItem;
  chunks;
  async scanItems(priority) {
    const chunkedItems = await Promise.all(
      this.chunks.map(async (chunk) => {
        return chunk.scanItems(priority);
      })
    );
    return chunkedItems.flat();
  }
  resolveItems(pointers, priority) {
    return Promise.all(
      pointers.map((pointer) => {
        const pointerModel = DatabaseItemPointerModel.fromString(pointer);
        const chunk = this.chunks[pointerModel.chunkId];
        assert(chunk, "Missing chunk");
        return chunk.resolveItem(pointer, priority);
      })
    );
  }
  compareItems(left, right) {
    const leftModel = DatabaseItemPointerModel.fromString(left.pointer);
    const rightModel = DatabaseItemPointerModel.fromString(right.pointer);
    return leftModel.compare(rightModel);
  }
  compareValues(left, right, collation) {
    return DatabaseValueModel.compare(left, right, collation);
  }
};
export {
  DatabaseCollection,
  DatabaseDictionaryIndex
};
`;
var Ic = `// src/code-generation/components/cms/bundled/DraftDatabaseCollection.legacy.ts?bundle
import { ControlType } from "framer";

// src/utils/typeChecks.ts
function isString(value) {
  return typeof value === "string";
}
function isNumber(value) {
  return Number.isFinite(value);
}

// src/code-generation/components/cms/bundled/assert.ts
function assert(condition, ...msg) {
  if (condition) return;
  throw Error("Assertion Error" + (msg.length > 0 ? ": " + msg.join(" ") : ""));
}

// src/code-generation/components/cms/bundled/DraftDatabaseCollection.legacy.ts?bundle
var DraftDatabaseCollection = class {
  constructor(draftCollection, originalCollection, previousItemId, nextItemId) {
    this.draftCollection = draftCollection;
    this.originalCollection = originalCollection;
    this.previousItemId = previousItemId;
    this.nextItemId = nextItemId;
    this.schema = originalCollection.schema;
    this.itemsPromise = this.getItems();
  }
  draftCollection;
  originalCollection;
  previousItemId;
  nextItemId;
  schema;
  // We cannot use the original indexes because the draft items
  // are not in the original collection.
  // Having no indexes means that we perform a full collection scan for every query.
  indexes = [];
  itemsPromise;
  async getItems() {
    const [[...draftItems], [...originalItems]] = await Promise.all([
      this.draftCollection.scanItems(),
      this.originalCollection.scanItems()
    ]);
    const lastDraftItem = draftItems.pop();
    const firstOriginalItem = originalItems.shift();
    if (lastDraftItem) {
      draftItems.push({
        ...lastDraftItem,
        data: {
          ...lastDraftItem.data,
          [this.nextItemId]: firstOriginalItem?.data.id ?? null
        }
      });
    }
    if (firstOriginalItem) {
      originalItems.unshift({
        ...firstOriginalItem,
        data: {
          ...firstOriginalItem.data,
          [this.previousItemId]: lastDraftItem?.data.id ?? null
        }
      });
    }
    return draftItems.map(draftItem).concat(...originalItems);
  }
  /**
   * Get all items, updating pointers for draft items.
   */
  scanItems() {
    return this.itemsPromise;
  }
  /**
   * Depending on the pointer, resolve draft rich text from the draft
   * collection, and original rich text from the original collection.
   */
  resolveRichText(pointer) {
    if (isDraftRichTextPointer(pointer)) {
      const originalPointer = undraftRichTextPointer(pointer);
      return this.draftCollection.resolveRichText(originalPointer);
    }
    return this.originalCollection.resolveRichText(pointer);
  }
  resolveVectorSetItem(pointer) {
    if (isDraftVectorSetItemPointer(pointer)) {
      const originalPointer = undraftVectorSetItemPointer(pointer);
      return this.draftCollection.resolveVectorSetItem?.(originalPointer);
    }
    return this.originalCollection.resolveVectorSetItem?.(pointer);
  }
  async resolveItems(pointers) {
    const items = await this.itemsPromise;
    return pointers.map((pointer) => {
      const item = items.find((current) => current.pointer === pointer);
      assert(item, "Item must exist");
      return item;
    });
  }
  /**
   * Compare two items, the draft items should come first.
   */
  compareItems(left, right) {
    const leftIsDraft = isDraftPointer(left.pointer);
    const rightIsDraft = isDraftPointer(right.pointer);
    if (leftIsDraft && rightIsDraft) {
      const originalLeft = undraftItem(left);
      const originalRight = undraftItem(right);
      return this.draftCollection.compareItems(originalLeft, originalRight);
    }
    if (leftIsDraft) return -1;
    if (rightIsDraft) return 1;
    return this.originalCollection.compareItems(left, right);
  }
};
var draftPrefix = "draft:";
function isDraftPointer(pointer) {
  return pointer.startsWith(draftPrefix);
}
function draftPointer(pointer) {
  assert(!isDraftPointer(pointer), "Should be a non-draft pointer");
  return draftPrefix + pointer;
}
function undraftPointer(pointer) {
  assert(isDraftPointer(pointer), "Should be a draft pointer");
  return pointer.slice(draftPrefix.length);
}
function isDraftRichTextPointer(pointer) {
  if (isNumber(pointer)) {
    return pointer < 0;
  }
  if (isString(pointer)) {
    return pointer.startsWith(draftPrefix);
  }
  throw new Error("Invalid rich text pointer");
}
function draftRichTextPointer(pointer) {
  assert(!isDraftRichTextPointer(pointer), "Should be a non-draft rich text pointer");
  if (isNumber(pointer)) {
    return -pointer - 1;
  }
  if (isString(pointer)) {
    return draftPrefix + pointer;
  }
  throw new Error("Invalid rich text pointer");
}
function undraftRichTextPointer(pointer) {
  assert(isDraftRichTextPointer(pointer), "Should be a draft rich text pointer");
  if (isNumber(pointer)) {
    return -pointer - 1;
  }
  if (isString(pointer)) {
    return pointer.slice(draftPrefix.length);
  }
  throw new Error("Invalid rich text pointer");
}
function isDraftVectorSetItemPointer(pointer) {
  assert(isNumber(pointer), "Invalid vector set item pointer");
  return pointer < 0;
}
function draftVectorSetItemPointer(pointer) {
  assert(isNumber(pointer), "Invalid vector set item pointer");
  assert(!isDraftVectorSetItemPointer(pointer), "Should be a non-draft vector set item pointer");
  return -pointer - 1;
}
function undraftVectorSetItemPointer(pointer) {
  assert(isNumber(pointer), "Invalid vector set item pointer");
  assert(isDraftVectorSetItemPointer(pointer), "Should be a draft vector set item pointer");
  return -pointer - 1;
}
function draftItem(item) {
  const data = {};
  for (const [key, value] of Object.entries(item.data)) {
    if (value?.type === ControlType.RichText) {
      data[key] = {
        type: ControlType.RichText,
        value: draftRichTextPointer(value.value)
      };
    } else if (value?.type === ControlType.VectorSetItem) {
      data[key] = {
        type: ControlType.VectorSetItem,
        value: draftVectorSetItemPointer(value.value)
      };
    } else {
      data[key] = value;
    }
  }
  return {
    pointer: draftPointer(item.pointer),
    data
  };
}
function undraftItem(item) {
  const data = {};
  for (const [key, value] of Object.entries(item.data)) {
    if (value?.type === ControlType.RichText) {
      data[key] = {
        type: ControlType.RichText,
        value: undraftRichTextPointer(value.value)
      };
    } else if (value?.type === ControlType.VectorSetItem) {
      data[key] = {
        type: ControlType.VectorSetItem,
        value: undraftVectorSetItemPointer(value.value)
      };
    } else {
      data[key] = value;
    }
  }
  return {
    pointer: undraftPointer(item.pointer),
    data
  };
}
export {
  DraftDatabaseCollection
};
`;
var bc = `// src/code-generation/components/cms/bundled/DraftDatabaseCollection.ts?bundle
import { ControlType, yieldToMain } from "framer";

// src/utils/typeChecks.ts
function isString(value) {
  return typeof value === "string";
}
function isNumber(value) {
  return Number.isFinite(value);
}

// src/code-generation/components/cms/bundled/assert.ts
function assert(condition, ...msg) {
  if (condition) return;
  throw Error("Assertion Error" + (msg.length > 0 ? ": " + msg.join(" ") : ""));
}

// src/code-generation/components/cms/bundled/DraftDatabaseCollection.ts?bundle
var DraftDatabaseCollection = class {
  constructor(draftCollection, originalCollection, previousItemId, nextItemId) {
    this.draftCollection = draftCollection;
    this.originalCollection = originalCollection;
    this.previousItemId = previousItemId;
    this.nextItemId = nextItemId;
    this.schema = originalCollection.schema;
    this.itemsPromise = this.getItems();
  }
  draftCollection;
  originalCollection;
  previousItemId;
  nextItemId;
  schema;
  // We cannot use the original indexes because the draft items
  // are not in the original collection.
  // Having no indexes means that we perform a full collection scan for every query.
  indexes = [];
  itemsPromise;
  async getItems() {
    const [[...draftItems], [...originalItems]] = await Promise.all([
      this.draftCollection.scanItems(),
      this.originalCollection.scanItems()
    ]);
    const lastDraftItem = draftItems.pop();
    const firstOriginalItem = originalItems.shift();
    if (lastDraftItem) {
      draftItems.push({
        ...lastDraftItem,
        data: {
          ...lastDraftItem.data,
          [this.nextItemId]: firstOriginalItem?.data.id ?? null
        }
      });
    }
    if (firstOriginalItem) {
      originalItems.unshift({
        ...firstOriginalItem,
        data: {
          ...firstOriginalItem.data,
          [this.previousItemId]: lastDraftItem?.data.id ?? null
        }
      });
    }
    return draftItems.map(draftItem).concat(...originalItems);
  }
  /**
   * Get all items, updating pointers for draft items.
   */
  scanItems() {
    return this.itemsPromise;
  }
  /**
   * Depending on the pointer, resolve draft rich text from the draft
   * collection, and original rich text from the original collection.
   */
  resolveRichText(pointer) {
    if (isDraftRichTextPointer(pointer)) {
      const originalPointer = undraftRichTextPointer(pointer);
      return this.draftCollection.resolveRichText(originalPointer);
    }
    return this.originalCollection.resolveRichText(pointer);
  }
  resolveVectorSetItem(pointer) {
    if (isDraftVectorSetItemPointer(pointer)) {
      const originalPointer = undraftVectorSetItemPointer(pointer);
      return this.draftCollection.resolveVectorSetItem?.(originalPointer);
    }
    return this.originalCollection.resolveVectorSetItem?.(pointer);
  }
  async resolveItems(pointers, priority) {
    const items = await this.itemsPromise;
    const resolvedItems = [];
    for (const pointer of pointers) {
      const resolvedPriority = typeof priority === "function" ? priority() : priority;
      const yieldPromise = resolvedPriority ? yieldToMain({ batch: true, priority: resolvedPriority }) : void 0;
      if (yieldPromise) await yieldPromise;
      const item = items.find((current) => current.pointer === pointer);
      assert(item, "Item must exist");
      resolvedItems.push(item);
    }
    return resolvedItems;
  }
  /**
   * Compare two items, the draft items should come first.
   */
  compareItems(left, right) {
    const leftIsDraft = isDraftPointer(left.pointer);
    const rightIsDraft = isDraftPointer(right.pointer);
    if (leftIsDraft && rightIsDraft) {
      const originalLeft = undraftItem(left);
      const originalRight = undraftItem(right);
      return this.draftCollection.compareItems(originalLeft, originalRight);
    }
    if (leftIsDraft) return -1;
    if (rightIsDraft) return 1;
    return this.originalCollection.compareItems(left, right);
  }
};
var draftPrefix = "draft:";
function isDraftPointer(pointer) {
  return pointer.startsWith(draftPrefix);
}
function draftPointer(pointer) {
  assert(!isDraftPointer(pointer), "Should be a non-draft pointer");
  return draftPrefix + pointer;
}
function undraftPointer(pointer) {
  assert(isDraftPointer(pointer), "Should be a draft pointer");
  return pointer.slice(draftPrefix.length);
}
function isDraftRichTextPointer(pointer) {
  if (isNumber(pointer)) {
    return pointer < 0;
  }
  if (isString(pointer)) {
    return pointer.startsWith(draftPrefix);
  }
  throw new Error("Invalid rich text pointer");
}
function draftRichTextPointer(pointer) {
  assert(!isDraftRichTextPointer(pointer), "Should be a non-draft rich text pointer");
  if (isNumber(pointer)) {
    return -pointer - 1;
  }
  if (isString(pointer)) {
    return draftPrefix + pointer;
  }
  throw new Error("Invalid rich text pointer");
}
function undraftRichTextPointer(pointer) {
  assert(isDraftRichTextPointer(pointer), "Should be a draft rich text pointer");
  if (isNumber(pointer)) {
    return -pointer - 1;
  }
  if (isString(pointer)) {
    return pointer.slice(draftPrefix.length);
  }
  throw new Error("Invalid rich text pointer");
}
function isDraftVectorSetItemPointer(pointer) {
  assert(isNumber(pointer), "Invalid vector set item pointer");
  return pointer < 0;
}
function draftVectorSetItemPointer(pointer) {
  assert(isNumber(pointer), "Invalid vector set item pointer");
  assert(!isDraftVectorSetItemPointer(pointer), "Should be a non-draft vector set item pointer");
  return -pointer - 1;
}
function undraftVectorSetItemPointer(pointer) {
  assert(isNumber(pointer), "Invalid vector set item pointer");
  assert(isDraftVectorSetItemPointer(pointer), "Should be a draft vector set item pointer");
  return -pointer - 1;
}
function draftItem(item) {
  const data = {};
  for (const [key, value] of Object.entries(item.data)) {
    if (value?.type === ControlType.RichText) {
      data[key] = {
        type: ControlType.RichText,
        value: draftRichTextPointer(value.value)
      };
    } else if (value?.type === ControlType.VectorSetItem) {
      data[key] = {
        type: ControlType.VectorSetItem,
        value: draftVectorSetItemPointer(value.value)
      };
    } else {
      data[key] = value;
    }
  }
  return {
    pointer: draftPointer(item.pointer),
    data
  };
}
function undraftItem(item) {
  const data = {};
  for (const [key, value] of Object.entries(item.data)) {
    if (value?.type === ControlType.RichText) {
      data[key] = {
        type: ControlType.RichText,
        value: undraftRichTextPointer(value.value)
      };
    } else if (value?.type === ControlType.VectorSetItem) {
      data[key] = {
        type: ControlType.VectorSetItem,
        value: undraftVectorSetItemPointer(value.value)
      };
    } else {
      data[key] = value;
    }
  }
  return {
    pointer: undraftPointer(item.pointer),
    data
  };
}
export {
  DraftDatabaseCollection
};
`;
var Sn = class t {
  constructor(e, n, o) {
    this.chunkId = e;
    this.offset = n;
    this.length = o;
  }
  chunkId;
  offset;
  length;
  static fromString(e) {
    let [n, o, r] = e.split("/").map(Number);
    return (
      X(A(n), "Invalid chunkId"),
      X(A(o), "Invalid offset"),
      X(A(r), "Invalid length"),
      new t(n, o, r)
    );
  }
  toString() {
    return `${this.chunkId}/${this.offset}/${this.length}`;
  }
  static read(e) {
    let n = e.readUint16(),
      o = e.readUint32(),
      r = e.readUint32();
    return new t(n, o, r);
  }
  write(e) {
    (e.writeUint16(this.chunkId), e.writeUint32(this.offset), e.writeUint32(this.length));
  }
  compare(e) {
    return this.chunkId < e.chunkId
      ? -1
      : this.chunkId > e.chunkId
        ? 1
        : this.offset < e.offset
          ? -1
          : this.offset > e.offset
            ? 1
            : (X(this.length === e.length), 0);
  }
};
function pi(t) {
  if (at(t)) return 0;
  switch (t.type) {
    case "array":
      return 1;
    case "boolean":
      return 2;
    case "color":
      return 3;
    case "date":
      return 4;
    case "enum":
      return 5;
    case "file":
      return 6;
    case "responsiveimage":
      return 10;
    case "link":
      return 7;
    case "number":
      return 8;
    case "object":
      return 9;
    case "richtext":
      return 11;
    case "string":
      return 12;
    case "vectorsetitem":
      return 13;
    default:
      On(t);
  }
}
var Le;
((o) => {
  function t(r) {
    let i = r.readUint8();
    switch (i) {
      case 0:
        return null;
      case 1:
        return Bu(r);
      case 2:
        return Uu(r);
      case 3:
        return _u(r);
      case 4:
        return ju(r);
      case 5:
        return Ju(r);
      case 6:
        return Xu(r);
      case 7:
        return ef(r);
      case 8:
        return of(r);
      case 9:
        return sf(r);
      case 10:
        return df(r);
      case 11:
        return pf(r);
      case 12:
        return gf(r);
      case 13:
        return Sf(r);
      default:
        On(i);
    }
  }
  o.read = t;
  function e(r, i) {
    let a = pi(i);
    if ((r.writeUint8(a), !at(i)))
      switch (i.type) {
        case "array":
          return Fu(r, i);
        case "boolean":
          return Ou(r, i);
        case "color":
          return Wu(r, i);
        case "date":
          return Hu(r, i);
        case "enum":
          return Ku(r, i);
        case "file":
          return Yu(r, i);
        case "link":
          return tf(r, i);
        case "number":
          return rf(r, i);
        case "object":
          return lf(r, i);
        case "responsiveimage":
          return uf(r, i);
        case "richtext":
          return mf(r, i);
        case "vectorsetitem":
          return Cf(r, i);
        case "string":
          return yf(r, i);
        default:
          On(i);
      }
  }
  o.write = e;
  function n(r, i, a) {
    let s = pi(r),
      l = pi(i);
    if (s < l) return -1;
    if (s > l) return 1;
    if (at(r) || at(i)) return 0;
    switch (r.type) {
      case "array":
        return (X(i.type === "array"), Au(r, i, a));
      case "boolean":
        return (X(i.type === "boolean"), $u(r, i));
      case "color":
        return (X(i.type === "color"), Gu(r, i));
      case "date":
        return (X(i.type === "date"), qu(r, i));
      case "enum":
        return (X(i.type === "enum"), Qu(r, i));
      case "file":
        return (X(i.type === "file"), Zu(r, i));
      case "link":
        return (X(i.type === "link"), nf(r, i));
      case "number":
        return (X(i.type === "number"), af(r, i));
      case "object":
        return (X(i.type === "object"), cf(r, i, a));
      case "responsiveimage":
        return (X(i.type === "responsiveimage"), ff(r, i));
      case "richtext":
        return (X(i.type === "richtext"), hf(r, i));
      case "vectorsetitem":
        return (X(i.type === "vectorsetitem"), If(r, i));
      case "string":
        return (X(i.type === "string"), vf(r, i, a));
      default:
        On(r);
    }
  }
  o.compare = n;
})((Le ||= {}));
function Bu(t) {
  let e = t.readUint16(),
    n = [];
  for (let o = 0; o < e; o++) {
    let r = Le.read(t);
    n.push(r);
  }
  return { type: "array", value: n };
}
function Fu(t, e) {
  t.writeUint16(e.value.length);
  for (let n of e.value) Le.write(t, n);
}
function Au(t, e, n) {
  let o = t.value.length,
    r = e.value.length;
  if (o < r) return -1;
  if (o > r) return 1;
  for (let i = 0; i < o; i++) {
    let a = t.value[i],
      s = e.value[i],
      l = Le.compare(a, s, n);
    if (l !== 0) return l;
  }
  return 0;
}
function Uu(t) {
  return { type: "boolean", value: t.readUint8() !== 0 };
}
function Ou(t, e) {
  t.writeUint8(e.value ? 1 : 0);
}
function $u(t, e) {
  return t.value < e.value ? -1 : t.value > e.value ? 1 : 0;
}
function _u(t) {
  return { type: "color", value: t.readString() };
}
function Wu(t, e) {
  t.writeString(e.value);
}
function Gu(t, e) {
  return t.value < e.value ? -1 : t.value > e.value ? 1 : 0;
}
function ju(t) {
  let e = t.readInt64(),
    n = new Date(e);
  return { type: "date", value: n.toISOString() };
}
function Hu(t, e) {
  let o = new Date(e.value).getTime();
  t.writeInt64(o);
}
function qu(t, e) {
  let n = new Date(t.value),
    o = new Date(e.value);
  return n < o ? -1 : n > o ? 1 : 0;
}
function Ju(t) {
  return { type: "enum", value: t.readString() };
}
function Ku(t, e) {
  t.writeString(e.value);
}
function Qu(t, e) {
  return t.value < e.value ? -1 : t.value > e.value ? 1 : 0;
}
function Xu(t) {
  return { type: "file", value: t.readString() };
}
function Yu(t, e) {
  t.writeString(e.value);
}
function Zu(t, e) {
  return t.value < e.value ? -1 : t.value > e.value ? 1 : 0;
}
function ef(t) {
  return { type: "link", value: t.readJson() };
}
function tf(t, e) {
  t.writeJson(e.value);
}
function nf(t, e) {
  let n = JSON.stringify(t.value),
    o = JSON.stringify(e.value);
  return n < o ? -1 : n > o ? 1 : 0;
}
function of(t) {
  return { type: "number", value: t.readFloat64() };
}
function rf(t, e) {
  t.writeFloat64(e.value);
}
function af(t, e) {
  return t.value < e.value ? -1 : t.value > e.value ? 1 : 0;
}
function sf(t) {
  let e = t.readUint16(),
    n = {};
  for (let o = 0; o < e; o++) {
    let r = t.readString();
    n[r] = Le.read(t);
  }
  return { type: "object", value: n };
}
function lf(t, e) {
  let n = Object.entries(e.value);
  t.writeUint16(n.length);
  for (let [o, r] of n) (t.writeString(o), Le.write(t, r));
}
function cf(t, e, n) {
  let o = Object.keys(t.value).sort(),
    r = Object.keys(e.value).sort();
  if (o.length < r.length) return -1;
  if (o.length > r.length) return 1;
  for (let i = 0; i < o.length; i++) {
    let a = o[i],
      s = r[i];
    if (a < s) return -1;
    if (a > s) return 1;
    let l = t.value[a] ?? null,
      c = e.value[s] ?? null,
      f = Le.compare(l, c, n);
    if (f !== 0) return f;
  }
  return 0;
}
function df(t) {
  return { type: "responsiveimage", value: t.readJson() };
}
function uf(t, e) {
  t.writeJson(e.value);
}
function ff(t, e) {
  let n = JSON.stringify(t.value),
    o = JSON.stringify(e.value);
  return n < o ? -1 : n > o ? 1 : 0;
}
function pf(t) {
  let e = t.readInt8();
  if (e === 0) return { type: "richtext", value: t.readUint32() };
  if (e === 1) return { type: "richtext", value: t.readString() };
  throw new Error("Invalid rich text pointer");
}
function mf(t, e) {
  if (A(e.value)) {
    (t.writeInt8(0), t.writeUint32(e.value));
    return;
  }
  if (L(e.value)) {
    (t.writeInt8(1), t.writeString(e.value));
    return;
  }
  throw new Error("Invalid rich text pointer");
}
function hf(t, e) {
  let n = t.value,
    o = e.value;
  if ((A(n) && A(o)) || (L(n) && L(o))) return n < o ? -1 : n > o ? 1 : 0;
  throw new Error("Invalid rich text pointer");
}
function gf(t) {
  return { type: "string", value: t.readString() };
}
function yf(t, e) {
  t.writeString(e.value);
}
function vf(t, e, n) {
  let o = t.value,
    r = e.value;
  return (
    n.type === 0 && ((o = t.value.toLowerCase()), (r = e.value.toLowerCase())),
    o < r ? -1 : o > r ? 1 : 0
  );
}
function Sf(t) {
  return { type: "vectorsetitem", value: t.readUint32() };
}
function Cf(t, e) {
  t.writeUint32(e.value);
}
function If(t, e) {
  let n = t.value,
    o = e.value;
  return n < o ? -1 : n > o ? 1 : 0;
}
var ot = class t {
  constructor(e, n) {
    this.fieldNames = e;
    this.options = n;
  }
  fieldNames;
  options;
  entries = [];
  sortEntries() {
    this.entries.sort((e, n) => {
      for (let o = 0; o < this.fieldNames.length; o++) {
        let r = e.values[o],
          i = n.values[o],
          a = Le.compare(r, i, this.options.collation);
        if (a !== 0) return a;
      }
      return e.pointer.compare(n.pointer);
    });
  }
  static async deserialize(e, n) {
    let o = new tr(e),
      r = o.readJson(),
      i = o.readUint8(),
      a = [];
    for (let f = 0; f < i; f++) {
      let d = o.readString();
      a.push(d);
    }
    let s = new t(a, { collation: r }),
      l = o.readUint32(),
      c = () => {
        let f = [];
        for (let u = 0; u < i; u++) {
          let m = Le.read(o);
          f.push(m);
        }
        let d = Sn.read(o);
        s.entries.push({ values: f, pointer: d });
      };
    for (let f = 0; f < l; f++) {
      let d = n?.();
      (d && (await d), c());
    }
    return s;
  }
  serialize() {
    let e = new Wt();
    (e.writeJson(this.options.collation), e.writeUint8(this.fieldNames.length));
    for (let n of this.fieldNames) e.writeString(n);
    (this.sortEntries(), e.writeUint32(this.entries.length));
    for (let n of this.entries) {
      let { values: o, pointer: r } = n;
      for (let i of o) Le.write(e, i);
      r.write(e);
    }
    return e.subarray();
  }
  addItem(e, n) {
    let o = this.fieldNames.map((r) => e.getField(r) ?? null);
    this.entries.push({ values: o, pointer: n });
  }
};
var nr = class {
  fields = new Map();
  write(e) {
    e.writeUint16(this.fields.size);
    for (let [n, o] of this.fields) (e.writeString(n), Le.write(e, o));
  }
  setField(e, n) {
    this.fields.set(e, n);
  }
  getField(e) {
    return this.fields.get(e);
  }
};
function _y(t) {
  let e = {},
    n = t.readUint16();
  for (let o = 0; o < n; o++) {
    let r = t.readString();
    e[r] = Le.read(t);
  }
  return e;
}
function mi(t) {
  return p`${t}.replace("/modules/", "/cms/")`;
}
var bf = oe("serializeDatabase"),
  hi = 4 * 1024 * 1024,
  Tf = 5e3;
async function Tc(t, e, n, o, r, i, a, s, l, c, f, d, u) {
  let m = new Zo(t, e, n, a, r, c, f, s, l),
    h = t.getPropertyControls(n, e),
    g = Object.entries(h),
    y = Ue(n, "includeDrafts"),
    v = {
      id: { type: Ze("string", a), isNullable: !1 },
      [jr]: { type: Ze("date", a), isNullable: !0 },
      [Hr]: { type: Ze("date", a), isNullable: !0 },
      [Pn]: { type: Ze("string", a), isNullable: !0 },
      [Mn]: { type: Ze("string", a), isNullable: !0 },
    };
  function C(H) {
    return Ze(H, a);
  }
  for (let [H, de] of g) {
    let ue = jl(de, C);
    ue && (v[H] = ue);
  }
  let S = f.dedupe("schema", new B(v, 0)),
    w = _l(t.variables),
    N = t.getSlugVariable(),
    D = O.isOn("preloadLoaderScheduling"),
    V = D ? Cc : Sc,
    z = r.create(V),
    P,
    M,
    ne = u ? !0 : void 0,
    re = O.isOn("cmsDatabase")
      ? []
      : t.getSortedChildren("public-only", "group-drafts").filter((H) => H.isDraft === ne),
    j = crypto.randomUUID();
  if (u) {
    let H = ie("collection", t.id);
    if (
      ((P = p`${a.addImport(`../${H}`, { exportSpecifier: "default", importBinding: "originalCollection" })}.collectionByLocaleId`),
      re.length === 0)
    ) {
      let ke = { id: j, displayName: t.resolveValue("name") ?? "Unknown", collectionByLocaleId: P };
      return p`${ke}`;
    }
    let de = D ? bc : Ic,
      ue = r.create(de);
    M = a.addImport(ue.submoduleImport, { exportSpecifier: "DraftDatabaseCollection" });
  }
  let Q = a.addImport(z.submoduleImport, { exportSpecifier: "DatabaseCollection" }),
    me = {},
    ce = new Map(),
    wt = new Set();
  for (let H of Ue(n, u ? "includeDrafts" : "excludeDrafts")) (ce.set(H.id, H), wt.add(H.id));
  for (let H of y) {
    let zt = function () {
        let E = new Wt();
        return (E.writeUint32(0), E);
      },
      Kt = function (E) {
        let U = Be.subarray(0, E);
        (vn(U).setUint32(0, Ee), rt.push(U));
      };
    var pt = zt,
      mt = Kt;
    let de = [
      new ot(["id"], { collation: { type: 1 } }),
      new ot([Pn], { collation: { type: 1 } }),
      new ot([Mn], { collation: { type: 1 } }),
    ];
    N &&
      de.push(
        new ot(["id", N.id], { collation: { type: 1 } }),
        new ot([N.id, "id"], { collation: { type: 0 } })
      );
    for (let [E] of g) de.push(new ot([E], { collation: { type: 0 } }));
    let ue = new er(),
      ke = re.filter((E) => wo(E, H.id) !== "exclude"),
      Vt = [];
    for (let E = 0; E < ke.length; E++) {
      let U = ke[E];
      b(U, "Node must exist");
      let we = Vo(U, H, lt, wt),
        Xt = ce.get(we) ?? ce.get(lt),
        Fe = new nr();
      Fe.setField("id", { type: "string", value: U.id });
      let no = U.createdAt;
      no && Fe.setField(jr, { type: "date", value: new Date(no).toISOString() });
      let oo = U.updatedAt;
      oo && Fe.setField(Hr, { type: "date", value: new Date(oo).toISOString() });
      let ht = ke[E - 1];
      ht && Fe.setField(Pn, { type: "string", value: ht.id });
      let Yt = ke[E + 1];
      Yt && Fe.setField(Mn, { type: "string", value: Yt.id });
      let Zt = {
        resolveColor(fe) {
          return d.addColor(fe);
        },
        resolveFile(fe) {
          return s.resolve(fe)?.src;
        },
        resolveImage(fe, gt) {
          return s.resolveResponsiveImage(fe, { focalPoint: gt });
        },
        resolveLink(fe) {
          let gt = l.create(fe);
          if (!_(gt) && !xa(gt)) return gt;
        },
        resolveRichTextPointer(fe) {
          return m.add(fe);
        },
        resolveVectorSetItemPointer(fe) {
          return ue.add(fe);
        },
      };
      for (let [fe, gt] of g) {
        let ji = Wl(fe, gt, w, h, U);
        if (_(ji)) continue;
        let nu = fe === N?.id ? H : Xt,
          Hi = Gl({ control: gt, controlProp: ji, resolvers: Zt, locale: nu });
        _(Hi) || Fe.setField(fe, Hi);
      }
      Vt.push(Fe);
    }
    let rt = [],
      Ee = 0,
      Be = zt();
    for (let E of Vt) {
      let U = Be.getOffset();
      (E.write(Be),
        Be.getOffset() > hi && (Kt(U), (Be = zt()), (U = Be.getOffset()), (Ee = 0), E.write(Be)),
        Ee++);
      let we = Be.getOffset() - U,
        Xt = new Sn(rt.length, U, we);
      for (let Fe of de) Fe.addItem(E, Xt);
    }
    Be.getOffset() > 0 && Kt();
    let Cr = a.addImport(z.submoduleImport, { exportSpecifier: "DatabaseDictionaryIndex" }),
      Ir = rt.map((E, U) => mi(i.create(`chunk-${H.id}-${U}`, ii, E)));
    rt.reduce((E, U) => E + U.length, 0) < Tf && (de.length = 0);
    let Qt = [],
      Nn = 1 / 0;
    for (let E of de) {
      let U = E.serialize();
      if (U.length >= hi) {
        bf.warn("Skipping index:", U.length);
        continue;
      }
      Nn + U.length > hi && (Qt.push([]), (Nn = 0));
      let we = Qt.at(-1);
      (b(we, "Chunk must exist"), we.push({ model: E, bytes: U }), (Nn += U.length));
    }
    let eo = [];
    for (let E = 0; E < Qt.length; E++) {
      let U = Qt[E];
      b(U, "Chunk must exist");
      let we = new Wt(),
        Xt = [];
      for (let { bytes: ht } of U) {
        let Yt = we.getOffset();
        we.writeBytes(ht);
        let Zt = we.getOffset();
        Xt.push({ from: Yt, to: Zt });
      }
      let Fe = we.subarray(),
        no = mi(i.create(`indexes-${H.id}-${E}`, ii, Fe)),
        oo = U.map(({ model: ht }, Yt) => {
          if (ht instanceof ot) {
            let Zt = Xt[Yt];
            b(Zt, "Can't find range for index");
            let fe = {
              url: no,
              range: Zt,
              fieldNames: f.dedupe("fieldNames", ht.fieldNames),
              collation: f.dedupe("collation", ht.options.collation),
              collectionSchema: S,
            };
            return p`new ${Cr}(${fe})`;
          }
          throw new Error("Unsupported index type");
        });
      eo.push(...oo);
    }
    let br = ue.serialize(e, o, f),
      to = {
        id: j + H.id,
        schema: S,
        chunks: Ir,
        indexes: eo,
        resolveRichText: m.resolveRichTextBinding,
        resolveVectorSetItem: br,
      };
    if (P && M) {
      let E = p`new ${Q}(${to})`,
        U = p`${P}[${H.id}]`;
      me[H.id] = p`new ${M}(${E}, ${U}, ${Pn}, ${Mn})`;
    } else me[H.id] = p`new ${Q}(${to})`;
  }
  m.serialize();
  let ft = { id: j, displayName: t.resolveValue("name") ?? "Unknown", collectionByLocaleId: me };
  return p`${ft}`;
}
var Nc = Ce(async (t, e, n) => Promise.all([Lc(t.id, e, !1), Lc(t.id, e, !0)]));
async function Lc(t, e, n) {
  let { componentLoader: o, modulesStore: r, tree: i, assetMap: a } = e,
    s = n ? "draftCollection" : "collection",
    l = new Bo(a),
    c = new Qo(t),
    f = new il(s, o, r, t),
    d = i.getNodeWithTrait(t, W);
  b(d, "Must have a source node to serialize");
  let u = d.moduleSourceRevision,
    m = Il(d);
  await Oo(m, r, o, "codegen-collection");
  let { module: h } = vl(o, r, d);
  Pe("collection_update", { moduleId: h?.id, recordCount: d.getItemCount(), source: "unknown" });
  let g = d.getSlugVariable()?.id,
    y = new Ft(),
    v = new ln(s, o, r, y),
    C = new No(y),
    S = new Fo(d, v, y, void 0, i),
    w = new Lo(),
    N = await Tc(d, o, i, r, f, c, v, l, S, y, C, w, n),
    D = Ye("", {
      framerRecordIdKey: "id",
      framerSlug: g,
      framerEnumToDisplayNameUtils: "2",
      framerCollectionUtils: "1",
      framerCollectionId: d.id,
      framerColorSyntax: w.hasColorsOfType("wideGamut"),
      framerAutoSizeImages: !0,
      framerData: !0,
    }),
    V = y.create("data"),
    z = wf(d, V, i, o, v, s),
    P = Nf(
      v,
      C,
      V,
      "id",
      g,
      d,
      O.isOn("localizationModelUpdate") ? Ue(i, n ? "includeDrafts" : "excludeDrafts") : void 0
    ),
    M = p.joinLines(
      ...v.statements,
      ...C.list(),
      D,
      p`const ${V} = ${N}`,
      p`export default ${V}`,
      z,
      ...Lf(d.variables, i.root.locales),
      P
    );
  return {
    type: s,
    revision: u,
    source: M,
    artifacts: {
      assets: new Set(l.keys),
      binaryAssets: c.assets,
      submodules: f.serialize(),
      metrics: { nodes: d.getItemCount() },
    },
  };
}
function Lf(t, e) {
  let n = [],
    o = new B();
  for (let r of t) {
    if (r.type !== "enum") continue;
    let i = new T(Zs(r.id));
    (n.push(p`export const ${i} = ${sc(r, e, i)};`), (o[r.id] = i));
  }
  return (n.push(p`export const ${new T(ko)} = ${o};`), n);
}
function Nf(t, e, n, o, r, i, a) {
  let s = a ? xf(i, a) : void 0,
    l = s ? e.dedupe("contentLocaleIdByRecordId", s) : void 0;
  return p.joinLines(
    p`export const utils = {`,
    p`async getSlugByRecordId(id, locale) {`,
    r ? Df(t, e, n, o, r) : void 0,
    p`},`,
    p`async getRecordIdBySlug(slug, locale) {`,
    r ? Rf(t, e, n, o, r) : void 0,
    p`},`,
    a
      ? p.joinLines(
          p`getContentLocaleIdByRecordId(id, locale) {`,
          l ? p`return ${l}[id]?.[locale?.id ?? ${lt}]` : void 0,
          p`},`
        )
      : void 0,
    p`}`
  );
}
function xf(t, e) {
  let n = {},
    o = e.find((i) => i.id === lt) ?? e[0];
  if (!o) return;
  let r = new Set(e.map((i) => i.id));
  for (let i of t.getSortedChildren("public-only", "group-drafts")) {
    let a = {};
    for (let s of e) {
      if (wo(i, s.id) !== "fallback") continue;
      let l = Vo(i, s, o.id, r);
      l !== s.id && (a[s.id] = l);
    }
    Object.keys(a).length > 0 && (n[i.id] = a);
  }
  if (Object.keys(n).length !== 0) return n;
}
function xc(t, e) {
  let n = t.addImport("framer", { exportSpecifier: "QueryEngine" });
  return e.dedupe("queryEngine", p`new ${n}()`);
}
function Df(t, e, n, o, r) {
  let i = xc(t, e),
    a = {
      from: { type: "Collection", data: n },
      select: [{ type: "Identifier", name: r }],
      where: {
        type: "BinaryOperation",
        operator: "==",
        left: { type: "Identifier", name: o },
        right: { type: "LiteralValue", value: p`id` },
      },
      limit: { type: "LiteralValue", value: 1 },
    };
  return p.joinLines(p`const [item] = await ${i}.query(${a}, locale)`, p`return item?.[${r}]`);
}
function Rf(t, e, n, o, r) {
  let i = xc(t, e),
    a = {
      from: { type: "Collection", data: n },
      select: [{ type: "Identifier", name: o }],
      where: {
        type: "BinaryOperation",
        operator: "==",
        left: { type: "Identifier", name: r },
        right: { type: "LiteralValue", value: p`slug` },
      },
      limit: { type: "LiteralValue", value: 1 },
    };
  return p.joinLines(p`const [item] = await ${i}.query(${a}, locale)`, p`return item?.[${o}]`);
}
function wf(t, e, n, o, r, i) {
  let a = ci(t, o, n, r, void 0, i, "ContentManagement");
  if (_(a)) return;
  let s = r.addImport("framer", { exportSpecifier: "addPropertyControls" });
  return T.fn(s, e, a);
}
function or({
  componentName: t,
  fontObjects: e,
  fontReferences: n,
  imports: o,
  declarations: r,
  sharedFontArrays: i = [],
}) {
  return p.joinLines(
    ...r.list(),
    T.fn(
      o.addImport("framer", { exportSpecifier: "addFonts" }),
      t,
      $n({ fontObjects: e, fontReferences: n, sharedFontArrays: i }),
      { supportsExplicitInterCodegen: !0 }
    )
  );
}
function $n({ fontObjects: t, fontReferences: e, sharedFontArrays: n = [] }) {
  let o = [],
    r = { explicitInter: !0, fonts: [] };
  for (let i of t) {
    try {
      new URL(i.url);
    } catch (a) {
      throw new Error(
        `Expected URL for font "${i.uiFamilyName}" to be a valid, absolute URL, but got: ${i.url}`,
        { cause: a }
      );
    }
    r.fonts.push(i);
  }
  o.push(r);
  for (let i of e.values()) o.push(p`...${i}`);
  for (let i of n) o.push(p`...${i}`);
  return o;
}
var Dc = Ce(async (t, { componentLoader: e, assetMap: n, tree: o, modulesStore: r }) => {
  let i = new Ft(),
    a = new Bo(n),
    s = new ln("componentPresets", e, r, i),
    l = new Fo(t, s, i, void 0, o),
    c = new No(i),
    f = new gs(t, i),
    d = new ol(t, o, f, c, i, s, a),
    u = new rl(i, f, d, a, l),
    m = t.moduleSourceRevision,
    h = {},
    g = {},
    y = t.getComponentPresets();
  await Oo(
    y.map((S) => S.componentIdentifier),
    r,
    e,
    "codegen-component-presets"
  );
  let v = new Ft(),
    C = new xo(v);
  for (let S of y) {
    let w = e.reactComponentForIdentifier(S.componentIdentifier);
    b(w, "Can't find react component for preset");
    let N = S.getFontsForCodeGeneration(e);
    await Qi.loadFonts(N);
    let D = new Go(C),
      V = At(Po(w, "onlyPresets"), S.getRawControlProps()),
      z = El(S, w.properties, V, {
        context: { assets: a, imports: s, links: l, fonts: D, fetches: u },
      });
    ((h[S.id] = new B(z)),
      (D.fontObjects.size > 0 || D.fontReferences.size > 0) &&
        (g[S.id] = $n({ fontObjects: D.fontObjects, fontReferences: D.fontReferences })));
  }
  return [
    {
      revision: m,
      type: "componentPresets",
      source: p.joinLines(
        ...s.statements,
        p`export const props = ${h}`,
        ...C.list(),
        p`export const fonts = ${g}`
      ),
      artifacts: { assets: a.keys, metrics: { nodes: Object.keys(h).length } },
    },
  ];
});
var Vf = oe("CodeGenerationStore");
function Rc(t, e, n) {
  return (
    n.add(t),
    e[t]?.forEach((o) => {
      n.has(o) || Rc(o, e, n);
    }),
    n
  );
}
function zf(t, e) {
  let n = {};
  for (let { fromNode: o, toNode: r } of e) {
    let i = n[o] ?? new Set();
    (i.add(r), (n[o] = i));
  }
  return Rc(t, n, new Set([t]));
}
var wc = Ce(async (t, { componentLoader: e, tree: n, treeStore: o, modulesStore: r }) => {
  let i = o.treeIndex,
    a = $(`Prototype/${t.name || "Generated Prototype"}`),
    s = n.get(t.homeNodeId);
  b(s, () => `Can't serialize ${a}: home node must exist.`);
  let l = n.getRect(s),
    c = new ln("prototype", e, r);
  (c.addImport("react", { exportSpecifier: "*", importBinding: "React" }),
    c.addImport("framer-motion", { exportSpecifier: "LayoutGroup" }),
    c.addImport("framer", { exportSpecifier: "PageRoot" }),
    c.addImport("framer", { exportSpecifier: "useInitialRouteComponent" }));
  let f = {};
  for (let g of zf(s.id, i.getPrototypeScreenLinks(n, t.id))) {
    let v = `../${ie("screen", g)}`;
    try {
      await hl(e, [He("screen", g, "default").value], r, "codegen-prototype");
      let C = c.addImport(v, {
        exportSpecifier: "default",
        importBinding: "Screen",
        lazy: g !== t.homeNodeId,
      });
      f[g] = { page: C, path: g };
    } catch {
      Vf.error("Missing module for prototype screen", { screenId: g });
    }
  }
  let d = $(t.homeNodeId),
    u = $(f),
    m = p`${c.addImport("react", { exportSpecifier: "*", importBinding: "React" })}.useId`,
    h = new T(
      F.sections(
        F.linesFrom(c.statements),
        `const routes = ${u}`,
        F.lines(
          "function Prototype({ layoutId }: { layoutId?: string }): JSX.Element {",
          `  const InitialRouteComponent = useInitialRouteComponent(routes, "${t.homeNodeId}")`,
          `  const defaultLayoutId = ${T.fn(m)};`,
          "  if (!InitialRouteComponent) return null",
          "  return (",
          "    <LayoutGroup id={layoutId ?? defaultLayoutId} inherit={false}>",
          `      <PageRoot RootComponent={InitialRouteComponent} isWebsite={false} routeId={${d}} routes={routes} />`,
          "    </LayoutGroup>",
          "  )",
          "}"
        ),
        F.lines(
          Ye("This is a generated Framer component.", {
            framerIntrinsicHeight: l?.height,
            framerIntrinsicWidth: l?.width,
            framerSupportedLayoutWidth: "fixed",
            framerSupportedLayoutHeight: "fixed",
            framerPrototype: !0,
          }),
          "const Component: React.ComponentType<{}> = React.memo(Prototype)",
          "export default Component;"
        ),
        `Component.displayName = ${a};`
      )
    );
  return [{ type: "prototype", source: h, revision: void 0 }];
});
async function rr(t, e, n, o, r, i) {
  return Ia(e) &&
    e.usesDOMRectCached() &&
    !e.getDOMRect() &&
    (await Pf(e, o, n, r), !e.cache.getRawDOMRect(e.id) && i)
    ? ro.atOrigin(i)
    : t.getRect(e);
}
async function Pf(t, e, n, o) {
  if (e) {
    let a = (await e([t.id]))?.[0];
    if (a) {
      let { layoutMetrics: s } = a,
        l = ro.from(s);
      t.cache.setRawDOMRect(t.id, l);
      return;
    }
  }
  if (!n) return;
  let r = Mf(n, o);
  if (r) {
    let i = ro.atOrigin(r);
    t.cache.setRawDOMRect(t.id, i);
  }
}
function Mf(t, e) {
  if (!vt(t)) return;
  let n = e.getPersistedModuleByLocalIdentifier(t);
  if (!n) return;
  let { intrinsicWidth: o, intrinsicHeight: r } = n.metadata;
  if (A(o) && A(r)) return { width: o, height: r };
}
function In(t, e) {
  for (let [n, o] of t.rules) {
    let r = e.get(o);
    r ? r.push(n) : e.set(o, [n]);
  }
}
function bn(t) {
  let e = [];
  for (let [n, o] of t) e.push(Re`${Re.join(o, ", ")} { ${n} }`);
  return e;
}
function ir(t, e, n, o, r, i) {
  return F.lines(
    Ye("This is a generated Framer component.", r),
    p`const ${t}: React.ComponentType<Props> = ${T.fn(i.addImport("framer", { exportSpecifier: "withCSS" }), n, o, e)} as typeof ${n};`,
    p`export default ${t};`
  );
}
var _e = {
    Screen: p`Screen`,
    css: I.css,
    ref: I.ref,
    serializationHash: I.serializationHash,
    sharedStyleClassNames: I.sharedStyleClassNames,
    restProps: I.restProps,
    refBinding: I.refBinding,
  },
  kf = new Set(Object.values(_e).map((t) => $(t))),
  Vc = Ce(async (t, e, { serializationId: n, initialIntrinsicSize: o }) => {
    let { modulesStore: r, tree: i } = e,
      a = i.getScopeNodeFor(t);
    if (!xe(a)) return [];
    let s = i.get(t.id);
    (b(s, "Must have a source node to serialize. This should be a primary variant."),
      b(xe(a), "Must have a scope node to serialize. This should be a CanvasPage Node."));
    let l = `FramerScreen${It(t.id)}`,
      c = He("screen", t.id, "default").value,
      { jsx: f, ctx: d } = await jo(s, a, e, new Set(kf).add(l), n);
    b(f, "There must be JSX to serialize.");
    let u = d.imports,
      m = await rr(i, s, c, e.measureNodes, r, o),
      h = d.events.navigationTargets.size > 0,
      g = d.events.variantOnAppearId.get(t.id),
      y = d.events.keyBindHooks.size > 0,
      v = [];
    for (let j of d.events.navigationTargets) j !== Bl && v.push(j);
    let C = d.fonts.fontObjects,
      S = new Map(d.fonts.fontReferences),
      w = [],
      N = new Set(d.css.sharedStyleIds),
      D = [];
    for (let j of N) {
      let Q = ie("css", j),
        me = u.addImport(`../${Q}`, { exportSpecifier: "*", importBinding: "sharedStyle" });
      (D.push(me),
        w.push(
          T.fn(
            u.addImport("framer", { exportSpecifier: "getFontsFromSharedStyle" }),
            p`${me}.fonts`
          )
        ));
    }
    let V;
    for (let j of d.componentPresetIds) {
      V ??= u.addImport(`../${_o}`, { exportSpecifier: "*", importBinding: "componentPresets" });
      let Q = p`${V}.fonts?.[${j}]`;
      w.push(
        p`(${Q} ? ${T.fn(d.imports.addImport("framer", { exportSpecifier: "getFontsFromComponentPreset" }), Q)} : [])`
      );
    }
    let z = new Map();
    In(d.css.get(), z);
    let P = new T(l),
      M = u.addImport("react", { exportSpecifier: "useRef" }),
      ne = a.resolveValue("name"),
      re = F.lines(
        h && `const preload = ${$(v)};`,
        ...d.variables.fallbackDeclarations(),
        ...d.declarations.file.list(),
        p`const ${_e.serializationHash} = ${n}`,
        p`const ${_e.Screen} = ${Ef({ props: p`style, className` }, p`const fallbackRef = ${T.fn(p`${M}<HTMLElement>`, p`null`)}`, p`const ${_e.refBinding} = ${_e.ref} ?? fallbackRef`, h ? p`const navigate = ${T.fn(u.addImport("framer", { exportSpecifier: "usePrototypeNavigate" }), { preload: p`preload` })}` : void 0, p`const ${_e.sharedStyleClassNames} = ${D.map((j) => p`${j}.className`)}`, ...d.declarations.component.list(), y && F.linesFrom(d.events.keyBindHooks), g && T.fn(u.addImport("framer", { exportSpecifier: "useOnAppear" }), g), p`return ${f}`)}`,
        p`const ${_e.css} = ${[...d.css.conditionalRules(n), ...bn(z), ...D.map((j) => p`...${j}.css`)]}`,
        ir(
          P,
          n,
          _e.Screen,
          _e.css,
          {
            framerIntrinsicHeight: m.height,
            framerIntrinsicWidth: m.width,
            framerSupportedLayoutWidth: "fixed",
            framerSupportedLayoutHeight: "fixed",
            framerScreen: void 0,
            framerDisplayContentsDiv: !1,
          },
          u
        ),
        p`${P}.displayName = ${`Screens/${ne ? ne.trim() : "Generated Component"}`};`,
        p`${P}.defaultProps = ${{ width: m.width, height: m.height }};`,
        or({
          componentName: P,
          fontObjects: C,
          fontReferences: S,
          imports: u,
          declarations: d.declarations.font,
          sharedFontArrays: w,
        })
      );
    return [
      {
        type: "screen",
        revision: void 0,
        source: new T(F.sections(F.linesFrom(u.statements), re)),
        artifacts: {
          assets: d.assets.keys,
          packageDependencies: d.packages.dependencies,
          metrics: d.metrics.get(),
        },
      },
    ];
  });
function Ef({ props: t }, ...e) {
  return new T(
    F.lines(p`React.forwardRef(function({ ${t}, ...${_e.restProps} }, ${_e.ref}) {`, ...e, p`});`)
  );
}
var Pc = "{serializationId}";
function zc(t) {
  return `.${Pc} ${t}`;
}
var Mc = Ce(async (t, { componentLoader: e, tree: n }, { serializationId: o }) => {
  let r = new Ft(),
    i = new xo(r),
    a = new Go(i),
    s = t.getFontsForCodeGeneration(),
    l = t.moduleSourceRevision,
    c = Ma(t);
  for (let C of s) a.addFontObjectBySelector(C, c);
  let f = [],
    d = new Lo(),
    u = { componentLoader: e, colors: d };
  for (let { selectors: C, declarations: S } of t.getCSS(u)) {
    let w = C.map(zc);
    f.push(`${w.join(", ")} { ${S} }`);
  }
  let m = n
    .getNodesWithTrait(t.cache.replicaInstances ?? [], js)
    .filter(Bs)
    .sort((C, S) => (S.breakpointWidth ?? 0) - (C.breakpointWidth ?? 0));
  for (let C = 0; C < m.length; C++) {
    let S = m[C];
    if (_(S)) continue;
    let w = m[C + 1],
      N = S.breakpointWidth - 1,
      D = w?.breakpointWidth ?? 0;
    for (let { selectors: V, declarations: z } of S.getCSS(u)) {
      let M = `${V.map(zc).join(", ")} { ${z} }`;
      f.push(`@media (max-width: ${N}px) and (min-width: ${D}px) { ${M} }`);
    }
  }
  let h = d.getWideGamutColorRules(`.${o}`);
  h.length && f.push(`@supports (color: color(display-p3 1 1 1)) { ${h.join(" ")} }`);
  let g = f.map((C) => C.replaceAll(Pc, o)),
    y = $n({ fontObjects: a.fontObjects, fontReferences: a.fontReferences }),
    v = p.joinLines(
      p`import { fontStore } from "framer";`,
      p`fontStore.loadFonts(${s});`,
      ...i.list(),
      p`export const fonts = ${y};`,
      p`export const css = ${g};`,
      p`export const className = ${o};`
    );
  return [{ revision: l, type: "css", source: v, artifacts: { metrics: { nodes: f.length } } }];
});
function gi(t) {
  if (ba(t))
    return {
      widthType: t.widthType,
      heightType: t.heightType,
      ...(Na(t)
        ? {
            minHeight: t.minHeight,
            maxHeight: t.maxHeight,
            minWidth: t.minWidth,
            maxWidth: t.maxWidth,
          }
        : La),
    };
}
function yi(t, e) {
  if (Ca(e)) return { min: t.getRect(e).width };
}
var Bf = new Set(Object.values(I).map((t) => $(t)));
function Ff(t, e) {
  if (tn(t)) {
    let n = e.get(t.parentid);
    return !Ae(n) || !n.isLoaded() ? null : n;
  }
  return e.current(t);
}
var vi = Ce(async (t, e, { serializationId: n, initialIntrinsicSize: o }) => {
  let { modulesStore: r, tree: i, measureNodes: a } = e,
    s = ze(t, i) ? t.moduleSourceRevision : void 0,
    l = Ff(t, i);
  b(l?.isLoaded(), "The scope node must be valid.");
  let c = Ul(l),
    f = Oe(l) ? l.baseVariantId : t.id,
    d = i.get(f);
  b(d, "Must have a source node to serialize. This should be a primary variant.");
  let u = `Framer${t.id}`,
    m = new T(u),
    { jsx: h, ctx: g } = await jo(d, l, e, new Set(Bf).add(u), n);
  b(h, "There must be JSX to serialize.");
  let y = g.imports,
    v = Ae(l),
    C = v ? void 0 : l.instanceIdentifier,
    S = await rr(i, d, C, a, r, o),
    w = Oe(l) ? l.getReplicaVariants() : [],
    { variants: N, hook: D } = g.preload.getHook(),
    { gestures: V, breakpoints: z, classNames: P, sizes: M, appearEventMap: ne } = ep(d, w, g, N),
    re = ae(l) ? [l.baseVariantId, ...l.getTopLevelReplicaVariants().map(qo)] : [],
    j = re.length > 1,
    Q = !St(V),
    me = !St(ne),
    ce = !St(z),
    wt = p`${y.addImport("react", { exportSpecifier: "*", importBinding: "React" })}.useId`,
    { mediaQueries: ft, sharedStyleImports: pt, sharedFontArrays: mt, css: H } = Qf(n, g, w, z, l),
    de = Zf(l, g.componentLoader, g.declarations),
    ue = Ke.applyTransforms(h, tp(l, g, Kf(l, d, w, ft, i, g), ce ? I.baseVariant : void 0, de)),
    ke = $f(l, g),
    Vt = Wf(l, g),
    zt = Vt ? g.cmsCodegen.createDetailPageDataCode(Vt) : void 0,
    rt = g.preload.getRouteLoaderCode(),
    { props: Ee, types: Be, propertyControls: Kt, variableMap: Cr } = Yo(l, g, hn(l), c, de),
    Ir =
      !ce &&
      p`const ${I.layoutDependency} = ${T.fn(g.declarations.file.dedupe("createLayoutDependency", p`(props, variants) => {${p.joinLines(p`if (props.layoutDependency) return variants.join('-') + props.layoutDependency`, p`return variants.join('-')`)}}`), I.props, I.variants)}`,
    { cursors: Gi } = g.customCursors,
    Qt = y.addImport("react", { exportSpecifier: "useRef" }),
    Nn = Jf(l),
    eo = F.sections(
      Q && p`const ${I.enabledGestures} = ${V};`,
      j && `const ${I.cycleOrder} = ${$(re)};`,
      ce && `const ${I.breakpoints} = ${$(ft)}`,
      ce && `const ${I.isBrowser} = () => typeof document !== "undefined";`,
      v && p`const ${I.variant} = undefined;`,
      v && p`const ${I.breakpoints} = undefined;`,
      v && p`const ${I.layoutId} = undefined;`,
      v && p`const ${I.className} = undefined;`,
      v && p`const ${I.style} = undefined;`,
      x(l) && p`export const ${I.queryParamNames} = ${Nn}`,
      p`const ${I.serializationHash} = ${n}`,
      `const ${I.variantClassNames} = ${$(P)};`,
      !ce && Gf,
      p.linesFrom(g.variables.fallbackDeclarations()),
      ...g.declarations.file.list(),
      `export interface Props extends React.HTMLAttributes<HTMLDivElement> { ${Be} }`,
      `const ${I.Component} = ${Uf(p`const fallbackRef = ${T.fn(p`${Qt}<HTMLElement>`, p`null`)}`, p`const ${I.refBinding} = ${I.ref} ?? fallbackRef`, p`const ${I.defaultLayoutId} = ${T.fn(wt)}`, O.isOn("localizationModelUpdate") ? p`const { ${I.activeLocale}, ${I.contentLocale}, ${I.setLocale} } = ${T.fn(y.addImport("framer", { exportSpecifier: "useLocaleInfo" }))}` : p`const { ${I.activeLocale}, ${I.setLocale} } = ${T.fn(y.addImport("framer", { exportSpecifier: "useLocaleInfo" }))}`, g.variables.shouldProvideLayoutDirection ? p`const ${I.layoutDirection} = ${T.fn(y.addImport("framer", { exportSpecifier: "useLayoutDirection" }))}` : void 0, ae(l) || x(l) ? p`const ${I.componentViewport} = ${T.fn(g.imports.addImport("framer", { exportSpecifier: "useComponentViewport" }))}` : void 0, zt, x(l) && qf(i, e.componentLoader, l, y, g.variables), Ee, x(l) && jf({ webPageNodeId: t.id, metadataDependencies: ke, imports: y, bindings: g.bindings }), ce ? p.joinLines(p`const [${I.baseVariant}, ${I.hydratedBaseVariant}] = ${T.fn(y.addImport("framer", { exportSpecifier: "useHydratedBreakpointVariants" }), I.variant, I.breakpoints, p`false`)};`, p`const ${I.gestureVariant} = undefined;`) : Af(d.id, Q, j, y), Ir, Yf(g), p`const ${I.sharedStyleClassNames} = ${pt.map((to) => p`${to}.className`)}`, ...g.declarations.component.list(), me && T.fn(y.addImport("framer", { exportSpecifier: "useOnVariantChange" }), ce ? I.hydratedBaseVariant : I.baseVariant, ne), Gi, D?.(Q ? I.enabledGestures : p`undefined`), p`return (${ue})`)}`,
      H,
      ir(m, n, I.Component, I.css, Of(l, g, S, M, Cr), y),
      `${u}.displayName = ${$(l.resolveValue("name")?.trim() || "Generated Component")};`,
      `${u}.defaultProps = ${$({ width: S.width, height: S.height })};`,
      Kt &&
        T.fn(
          y.addImport("framer", { exportSpecifier: "addPropertyControls" }),
          m,
          new T(`${$(Kt)} as any`)
        ),
      or({
        componentName: m,
        fontObjects: g.fonts.fontObjects,
        fontReferences: g.fonts.fontReferences,
        imports: y,
        declarations: g.declarations.font,
        sharedFontArrays: mt,
      }),
      rt && p`${m}.loader = ${rt}`
    ),
    [br] = x(l) ? await ll(l, e, { serializationId: n }) : [];
  return Ao([
    {
      type: c,
      revision: s,
      source: new T(F.sections(F.linesFrom(y.statements), eo)),
      artifacts: {
        packageDependencies: g.packages.dependencies,
        errors: g.errors.errors,
        assets: g.assets.keys,
        metrics: g.metrics.get(),
      },
    },
    br,
    g.localizedValues.module(s),
  ]);
});
function Af(t, e, n, o) {
  let r = B.fromEntries([
      [I.defaultVariant, t],
      [I.variant, I.variant],
      [I.variantClassNames, I.variantClassNames],
      [I.enabledGestures, e ? I.enabledGestures : void 0],
      [I.cycleOrder, n ? I.cycleOrder : void 0],
      [I.ref, I.refBinding],
    ]),
    i = T.fn(o.addImport("framer", { exportSpecifier: "useVariantState" }), r),
    a = I.variants,
    s = I.baseVariant,
    l = p`setVariant`,
    c = I.gestureVariant,
    f = I.classNames,
    d = I.setGestureState,
    u = I.gestureHandlers,
    m = I.clearLoadingGesture,
    h = I.isLoading;
  return F.lines(
    p`const ${{ variants: a, baseVariant: s, setVariant: l, gestureVariant: c, classNames: f, setGestureState: d, gestureHandlers: u, clearLoadingGesture: m, isLoading: h }} = ${i}`
  );
}
function Uf(...t) {
  return F.sections(
    p`React.forwardRef<HTMLDivElement, Props>(function(${I.props}, ${I.ref}) {`,
    ...t,
    p`});`
  );
}
function Of(t, e, n, o, r) {
  let { height: i, width: a } = n,
    s = {
      propertyName: "variant",
      data: Object.fromEntries(Object.entries(o).map(([c, f]) => [c, Fs(f)])),
    },
    l = {
      framerIntrinsicHeight: i,
      framerIntrinsicWidth: a,
      framerRootFontSize: $a(t) ? t.getPrimaryVariant()?.rootFontSize : void 0,
      framerCanvasComponentVariantDetails: JSON.stringify(s),
      framerVariables: r && JSON.stringify(r),
      framerImmutableVariables: !0,
      framerResolvesOwnDefaults: O.isOn("smartComponentResolvesOwnDefaults") ? !0 : void 0,
      framerDisplayContentsDiv: !1,
      framerAutoSizeImages: !0,
      framerComponentViewportWidth: !0,
      framerColorSyntax: !0,
      framerVectorSets: e.vectorSets.annotation(),
    };
  if (K(t)) {
    let c = {},
      f = t.getBreakpointValues();
    for (let d in f) {
      let u = e.tree.get(d);
      c[d] = {
        ...f[d],
        canvasClassName: Fc(d),
        minHeight: u?.height,
        viewportHeight: u?.viewportHeight,
        name: u?.resolveValue("name") ?? void 0,
        width: u?.widthType === 0 ? u?.width : void 0,
        rootFontSize: u?.rootFontSize ?? za,
      };
    }
    ((l.framerHitTargets = e.hitTargets.annotation()),
      (l.framerBreakpoints = JSON.stringify(c)),
      (l.framerDefaultVariant = t.baseVariantId),
      (l.framerTrackingIds = e.trackingIds.annotation()),
      (l.framerLayoutTemplate = !0));
  } else
    x(t)
      ? ((l.framerAcceptsLayoutTemplate = e.acceptsLayoutTemplate),
        (l.framerTrackingIds = e.trackingIds.annotation()),
        (l.framerScrollSections = e.elementIds.annotation() ?? !1),
        (l.framerResponsiveScreen = !0),
        (l.framerLayoutTemplateFlowEffect = !0))
      : ae(t)
        ? (l.framerTrackingIds = e.trackingIds.annotation())
        : Ae(t)
          ? (l.framerDesign = !0)
          : ae(t) || (l.framerScreen = !0);
  return l;
}
function $f(t, e) {
  if (!x(t)) return {};
  let n = _f(t, e),
    o = cl(t, n),
    r = {};
  for (let i of o) {
    let a = pa(i, t.id);
    r[i] = e.variables.create(a);
  }
  return r;
}
function _f(t, e) {
  if (zn(t) && Xe(e.tree, t))
    return (
      t.getProvidedControlMap(
        e.tree,
        void 0,
        e.componentLoader,
        e.componentLoader.activeBundleHash
      ) ?? Ur
    );
}
function Wf(t, e) {
  if (!x(t) || !zn(t) || !Xe(e.tree, t)) return;
  let o =
      t.getProvidedControlMap(
        e.tree,
        void 0,
        e.componentLoader,
        e.componentLoader.activeBundleHash
      ) ?? Ur,
    r = e.variables.getBindings(t.id),
    i = new Set();
  for (let [a] of o) r.has(a) && i.add(a);
  return { scopeNode: t, controlMap: o, usedVariableIds: i };
}
var Gf = F.lines(
  "function addPropertyOverrides(overrides, ...variants) {",
  "const nextOverrides = {}",
  "variants?.forEach(variant => variant && Object.assign(nextOverrides, overrides[variant]))",
  "return nextOverrides",
  "}"
);
function jf({ webPageNodeId: t, metadataDependencies: e, imports: n, bindings: o }) {
  let r = n.addImport(`../${al(t).localId}`, {
      exportSpecifier: "default",
      importBinding: "metadataProvider",
    }),
    i = o.create("metadata"),
    a = n.addImport("framer", { exportSpecifier: "useMetadata" }),
    s = O.isOn("localizationModelUpdate") ? I.contentLocale : I.activeLocale,
    l = T.fn(r, e, s),
    c = Object.values(e).concat(s);
  return F.lines(p`const ${i} = React.useMemo(() => ${l}, ${c})`, T.fn(a, i));
}
function Hf(t) {
  if (Sa(t)) return { type: t.gesture, topLevel: t.replicaInfo.inheritsFrom };
}
function qf(t, e, n, o, r) {
  let i = n.variables.filter(Te);
  if (i.length === 0) return;
  let a = r.getBindings(n.id),
    s = [];
  for (let l of i) {
    if (!Wr(l)) continue;
    let c = Kr(l),
      f;
    switch (l.type) {
      case "boolean": {
        let h = o.addImport("framer", { exportSpecifier: "useBooleanQueryParam" }),
          g = new B({
            initialValue: l.optional ? void 0 : l.initialValue,
            parameterName: c,
            optional: l.optional ? !0 : void 0,
          });
        f = T.fn(h, g);
        break;
      }
      case "string": {
        let h = o.addImport("framer", { exportSpecifier: "useStringQueryParam" }),
          g = new B({
            initialValue: l.optional ? void 0 : l.initialValue,
            parameterName: c,
            optional: l.optional === !0 ? !0 : void 0,
          });
        f = T.fn(h, g);
        break;
      }
      case "number": {
        let h = o.addImport("framer", { exportSpecifier: "useNumberQueryParam" }),
          g = new B({
            initialValue: l.optional ? void 0 : l.initialValue,
            parameterName: c,
            optional: l.optional === !0 ? !0 : void 0,
          });
        f = T.fn(h, g);
        break;
      }
      case "date": {
        let h = o.addImport("framer", { exportSpecifier: "useDateQueryParam" }),
          g = new B({
            initialValue: l.optional ? void 0 : l.initialValue,
            parameterName: c,
            displayTime: l.options?.displayTime === !0 ? !0 : void 0,
            optional: l.optional === !0 ? !0 : void 0,
          });
        f = T.fn(h, g);
        break;
      }
      case "collectionreference": {
        let h = o.addImport("framer", { exportSpecifier: "useCollectionReferenceQueryParam" }),
          g = new B({
            collectionId: Pr(l.dataIdentifier),
            initialValue: l.optional ? void 0 : l.initialValue,
            parameterName: c,
            optional: l.optional === !0 ? !0 : void 0,
          });
        f = T.fn(h, g);
        break;
      }
      case "multicollectionreference": {
        let h = o.addImport("framer", { exportSpecifier: "useMultiCollectionReferenceQueryParam" }),
          g = new B({
            collectionId: Pr(l.dataIdentifier),
            initialValue: l.optional ? void 0 : l.initialValue,
            parameterName: c,
            optional: l.optional === !0 ? !0 : void 0,
          });
        f = T.fn(h, g);
        break;
      }
      case "controlReference": {
        if (l.expectedType !== "enum") continue;
        let h = Ds(t, e, l);
        if (!h || h.type !== "enum") continue;
        let g = o.addImport("framer", { exportSpecifier: "useEnumQueryParam" }),
          y = l.entityIdentifier,
          v = J(y);
        if (!it(v)) continue;
        let C = l.controlKey,
          w = e.dataForIdentifier(y)?.annotations?.framerEnumToDisplayNameUtils;
        b(L(w) && Number.parseInt(w, 10) === 2, "Unexpected FramerEnumToDisplayNameUtils version");
        let N = o.addModuleImport(v, { exportSpecifier: ko });
        if (!N) continue;
        let D = o.addModuleImport(v, { importBinding: `${cs(C)}CollectionData` });
        if (!D) continue;
        let V = L(l.initialValue) ? l.initialValue : Ro("enum"),
          z;
        (l.optional === !0 || !V
          ? (z = new B({
              getOptionTitle: p`${N}?.[${C}]`,
              initialValue: void 0,
              optional: !0,
              options: p`${D}.propertyControls?.[${C}]?.options`,
              parameterName: c,
            }))
          : (z = new B({
              getOptionTitle: p`${N}?.[${C}]`,
              initialValue: V,
              options: p`${D}.propertyControls?.[${C}]?.options`,
              parameterName: c,
            })),
          (f = T.fn(g, z)));
        break;
      }
      default:
        Z(l);
    }
    let d = a.get(l.id);
    if (!d) continue;
    let u = r.getSetterBinding(l.id),
      m = [d];
    (u && m.push(u), s.push(p`const ${m} = ${f};`));
  }
  return p.joinLines(...s);
}
function Jf(t) {
  return x(t) ? t.variables.flatMap((e) => (Te(e) ? (Wr(e) ? [Kr(e)] : []) : [])) : [];
}
function Fc(t) {
  return `framer-${It(t)}-override`;
}
function kc(t, e) {
  if (!(!Ra(t) || (wa(t) && !t.fillEnabled)))
    return t.fillType === "color" && De(t.fillColor) ? e(t.fillColor, t, "fill") : Cs(t);
}
function Ec(t) {
  if (!Pa(t)) return;
  let e = (t.rootFontSize / 16) * 100;
  return Ta(e, "%");
}
function Bc(t, e) {
  return L(t) ? t : T.fn(e.imports.addImport("framer", { exportSpecifier: "safeCSSValue" }), t);
}
function Kf(t, e, n, o, r, i) {
  if (!K(t) && !x(t)) return;
  let a = Al(r, i.declarations, i.variables, i.valueTransforms),
    s = K(t) ? ":root" : "html",
    l = `${s} body`,
    c = Ec(e),
    f = kc(e, a) ?? "none",
    d = [Re`${l} { background: ${Bc(f, i)}; }`];
  L(c) && d.push(`${s} { font-size: ${c}; }`);
  for (let u of n) {
    let m = o[u.id];
    if (m) {
      let h = kc(u, a) ?? "none",
        g = Ec(u),
        y = [];
      if (
        (Zr(f, h) || y.push(Re`${l} { background: ${Bc(h, i)}; }`),
        Zr(c, g) || y.push(`${s} { font-size: ${g}; }`),
        y.length === 0)
      )
        continue;
      d.push(Re`@media ${m} { ${Re.join(y, " ")} }`);
    }
  }
  return Re.join(d, " ");
}
function Qf(t, e, n, o, r) {
  let i = Ks(Js(o)),
    a = [],
    s = new Map();
  if (K(r)) {
    let g = r.getPrimaryVariant(),
      y = { marginBottom: Ar(g, e) };
    e.css.createRule(`[${Yr}="true"] > #overlay`, Fr(y));
    for (let v of n) {
      let C = { marginBottom: Ar(v, e) },
        S = Nl(C, y);
      !S || St(S) || e.css.createRule(`[${Yr}="true"] > #overlay`, Fr(S), v.id);
    }
  }
  In(e.css.get(), s);
  let l = {},
    c = {};
  for (let g of n) {
    let y = i[g.id];
    if (y) {
      let v = new Map();
      if ((In(e.css.get(g.id), v), !v.size)) continue;
      K(r)
        ? ((l[g.id] = bn(v)), (c[g.id] = `.${Fc(g.id)}`))
        : a.push(Re`@media ${y} { ${Re.join(bn(v), " ")}}`);
    } else In(e.css.get(g.id), s);
  }
  let f = [],
    d = [];
  for (let g of e.css.sharedStyleIds) {
    let y = ie("css", g),
      v = e.imports.addImport(`../${y}`, { exportSpecifier: "*", importBinding: "sharedStyle" });
    (d.push(v),
      f.push(
        T.fn(
          e.imports.addImport("framer", { exportSpecifier: "getFontsFromSharedStyle" }),
          p`${v}.fonts`
        )
      ));
  }
  let u;
  for (let g of e.componentPresetIds) {
    u ??= e.imports.addImport(`../${_o}`, {
      exportSpecifier: "*",
      importBinding: "componentPresets",
    });
    let y = p`${u}.fonts?.[${g}]`;
    f.push(
      p`(${y} ? ${T.fn(e.imports.addImport("framer", { exportSpecifier: "getFontsFromComponentPreset" }), y)} : [])`
    );
  }
  let m = [...bn(s), ...d.map((g) => p`...${g}.css`), ...e.css.conditionalRules(t)],
    h = Xf(r, e, m, a, l, c, i);
  return {
    sharedStyleImports: d,
    sharedFontArrays: f,
    mediaQueries: i,
    css: p`const ${I.css} = ${h}`,
  };
}
function Xf(t, e, n, o, r, i, a) {
  if (ae(t) || Ae(t)) return n;
  if (x(t)) return [...n, ...o];
  if (K(t)) {
    let s = e.declarations.file.dedupe("breakpointRules", r),
      l = e.declarations.file.dedupe("breakpointKeys", p`Object.keys(${s})`),
      c = e.declarations.file.dedupe("selectors", i),
      f = e.declarations.file.dedupe("rules", n),
      d = e.declarations.file.dedupe("mediaQueries", a),
      u = e.bindings.create("target"),
      m = e.bindings.create("query"),
      h = e.bindings.create("selector"),
      g = e.bindings.create("rule"),
      y = e.imports.addImport("framer", { exportSpecifier: "RenderTarget" });
    return p`(target) => {${p.joinLines(p`const isStaticRenderer = ${u} === ${y}.canvas || ${u} === ${y}.export`, p`if (!isStaticRenderer) {${p.joinLines(p`return [...${f}, ...${l}.map(key => {${p.joinLines(p`const ${m} = ${d}[key]`, p`return ${Re`@media ${m} { ${p`${s}[key].join(" ")`} }`}`)}})]`)}}`, p`return [...${f}, ...${l}.flatMap(key => {${p.joinLines(p`const ${h} = ${c}[key]`, p`return ${s}[key].map(${g} => ${Re`${h} {${g}}`})`)}})]`)}}`;
  }
  Z(t);
}
function Yf(t) {
  if (t.events.needsPathVariables)
    return `const ${Fl} = React.useContext(${t.imports.addImport("framer", { exportSpecifier: "PathVariablesContext" })});`;
}
function Zf(t, e, n) {
  let o = Oe(t) ? t.getTopLevelVariants() : [];
  if (o.length < 2) return;
  let r = new B();
  for (let i of o) {
    let a = i.resolveValue("name") ?? $e(e, i);
    r[a] = i.id;
  }
  return n.file.dedupe("humanReadableVariantMap", r);
}
function ep(t, e, n, o) {
  let r = gi(t);
  b(r, "The primary variant should have a default size as it must be a FrameNode.");
  let i = { default: r },
    a = { [t.id]: `framer-v-${It(t.id)}` },
    s = {},
    l = {},
    c = {},
    f = yi(n.tree, t);
  f && (c[t.id] = f);
  let d = n.events.variantOnAppearId.get(t.id),
    u = {};
  !_(d) && !T.isUndefined(d) && (u.default = d);
  for (let m of e) {
    n.metrics.count("variants");
    let h = Hf(m);
    if (h) {
      let S = s[h.topLevel] ?? {};
      ((S[h.type] = h.type === "loading" ? o?.has(h.topLevel) : !0),
        (s[h.topLevel] = S),
        (l[h.topLevel] = [...(l[h.topLevel] ?? []), m.id]));
    } else a[m.id] = `framer-v-${It(m.id)}`;
    let g = gi(m);
    g && (i[m.id] = g);
    let y = yi(n.tree, m);
    y && (c[m.id] = y);
    let v = n.events.variantOnAppearId.get(m.id),
      C = $(u[m.replicaInfo.inheritsFrom ?? ""] ?? d);
    !_(v) && $(v) !== C && (u[m.id] = v);
  }
  return { gestures: s, breakpoints: c, classNames: a, sizes: i, appearEventMap: u };
}
function tp(t, e, n, o, r) {
  let i = [];
  if ((K(t) || x(t)) && n) {
    let s = e.declarations.file.dedupe(
      "HTMLStyle",
      p`({value}) => {${p.joinLines(p`const onCanvas = ${T.fn(e.imports.addImport("framer", { exportSpecifier: "useIsOnFramerCanvas" }))}`, p`if (onCanvas) return null`, p`return ${Ke.tag("style", new B({ "data-framer-html-style": "", dangerouslySetInnerHTML: { __html: p`value` } }))}`)}}`
    );
    i.push((l) => Ke`${p.joinLines(Ke.tag(s, new B({ value: n })), l)}`);
  }
  if (ae(t)) {
    let s = e.imports.addImport("react", { exportSpecifier: "*", importBinding: "React" }),
      l = e.imports.addImport("framer-motion", { exportSpecifier: "motion.create" }),
      c = e.declarations.file.dedupe("Variants", T.fn(l, p`${s}.Fragment`));
    i.push((f) => Ke.tag(c, new B({ initial: !1, animate: I.variants }), f));
  }
  if (x(t) || K(t) || Ae(t)) {
    let s = new B({ id: K(t) ? ha : ma });
    i.push((l) => Ke`${p.joinLines(l, Ke.tag("div", s))}`);
  }
  let a = e.imports.addImport("framer-motion", { exportSpecifier: "LayoutGroup" });
  if (
    (i.push((s) => Ke.tag(a, new B({ id: p`layoutId ?? ${I.defaultLayoutId}` }), s)), x(t) || K(t))
  ) {
    let s = e.imports.addImport("framer", { exportSpecifier: "GeneratedComponentContext" });
    i.push((l) =>
      Ke.tag(
        p`${s}.Provider`,
        new B({
          value: {
            primaryVariantId: t.baseVariantId,
            variantClassNames: I.variantClassNames,
            activeVariantId: o,
            humanReadableVariantMap: r,
            isLayoutTemplate: K(t) ? !0 : void 0,
          },
        }),
        l
      )
    );
  }
  return i;
}
function Ac() {
  let t = new T("9801af1");
  return p`// Generated by Framer (${t})`;
}
var Si = {
    ref: I.ref,
    restProps: I.restProps,
    style: I.style,
    props: I.props,
    className: I.className,
    layoutId: I.layoutId,
  },
  Ci = p`css`,
  _n = p`Icon`,
  ar = p`Component`,
  Wn = p`Props`,
  np = [...Object.values(Si), Ci, _n, ar, Wn].map((t) => $(t)),
  Uc = Ce(async (t, e, n) => {
    let { serializationId: o } = n,
      { modulesStore: r, tree: i, vectorsStore: a } = e,
      s = i.getScopeNodeFor(t);
    b(ye(s), "Vector set item must be in a VectorSetNode.");
    let l = i.get(t.id);
    b(l, "Must have a source node to serialize");
    let { ctx: c, jsx: f, svg: d, type: u } = ys(l, s, e, o, np),
      m = He("vectorSet", s.id, "default").localId,
      h = r.getPersistedModuleByLocalId(m);
    b(h, "VectorSet must be persisted.");
    let g = i.getRect(t),
      {
        propertyControls: y,
        variableMap: v,
        types: C,
        props: S,
      } = Yo(s, { ...e, ...c, cssVariableDefinitions: void 0 }, "VectorSet", "vector", void 0),
      w = op(c),
      N = {
        name: l.resolveValue("name") ?? "Vector",
        empty: rp(l) ? !0 : void 0,
        color: w,
        set: { localId: m, id: s.id, moduleId: h.id },
      },
      D = c.imports.addImport("react", { exportSpecifier: "*", importBinding: "React" }),
      V = c.imports.addImport("framer", { exportSpecifier: "withCSS" }),
      z = ip[u],
      P = F.lines(
        ...c.variables.fallbackDeclarations(),
        ...c.declarations.file.list(),
        p`interface ${Wn} extends ${D}.HTMLAttributes<${D}.${z}> { ${C ?? p``} }`,
        p`const ${ar}: ${D}.ComponentType<${Wn}> = ${D}.forwardRef<${D}.${z}, ${Wn}>(function(${Si.props}, ${Si.ref}) {${p.joinLines(S, ...c.declarations.component.list(), p`return (${f})`)}})`,
        p`const ${Ci} = ${c.css.rules}`,
        Ye("This is a generated Framer component.", {
          framerIntrinsicWidth: g.width,
          framerIntrinsicHeight: g.height,
          framerSupportedLayoutWidth: "any-prefer-fixed",
          framerSupportedLayoutHeight: "any-prefer-fixed",
          framerVariables: v && JSON.stringify(v),
          framerImmutableVariables: !0,
          framerVector: JSON.stringify(N),
          framerDisableUnlink: Tl.has(N.set.moduleId) ? !0 : void 0,
        }),
        p`const ${_n}: ${D}.ComponentType<${Wn}> = ${T.fn(V, ar, Ci, o)} as typeof ${ar};`,
        p`${_n}.displayName = ${l.resolveValue("name") ?? "Vector"};`,
        p`export default ${_n}`,
        y &&
          T.fn(
            c.imports.addImport("framer", { exportSpecifier: "addPropertyControls" }),
            _n,
            new T(`${$(y)} as any`)
          )
      );
    return (
      a.cache(t.id, { svg: $(d), color: w }),
      Ao([
        {
          revision: l.moduleSourceRevision,
          type: "vector",
          source: new T(F.sections(F.linesFrom(c.imports.statements), P)),
          artifacts: { files: [{ name: us, type: "svgIcon", content: $(d) }] },
        },
      ])
    );
  });
function op(t) {
  if (t.colors.uniqueColorCount !== 1) return;
  let e = t.colors.singleColor;
  if (e) return { type: "static", value: e };
  let n = t.colors.singleVariableColor;
  if (n) return { type: "variable", value: It(n) };
}
function rp(t) {
  return !t.children || t.children.length === 0 ? !0 : t.children.every((e) => e.visible === !1);
}
var ip = {
  0: p`HTMLImageElement`,
  1: p`HTMLDivElement`,
  3: p`HTMLSVGElement`,
  2: p`HTMLSVGElement`,
};
var Oc = Ce(async (t, { modulesStore: e }, n) => {
  let o = [];
  for (let a of t.getVectorSetItems()) {
    let s = He("vector", a.id, "default").localId,
      l = e.getPersistedModuleByLocalId(s);
    l &&
      o.push({
        id: a.id,
        name: a.resolveValue("name") ?? "Vector",
        moduleId: l.id,
        saveId: n.includeVectorSetSaveIds ? l.saveId : void 0,
      });
  }
  let r = { framerVectorSet: JSON.stringify({ name: t.resolveValue("name") ?? "Set", items: o }) },
    i = new T(
      F.lines(
        Ye("This is a generated Framer component.", r),
        p`const VectorSet: React.ComponentType<unknown> = function VectorSet() { return null; };`,
        p`export default VectorSet;`
      )
    );
  return [{ revision: t.moduleSourceRevision, type: "vectorSet", source: i }];
});
function Wc(t) {
  return Math.ceil(t / Math.ceil(t / 100));
}
var $c = class {
    status = null;
    results;
    reportProgress;
    onDone;
    constructor(e, n) {
      ((this.reportProgress = e), (this.onDone = n));
    }
    async import(e, n, o) {
      ((this.status = "importing"), (this.results = { scopeId: n, vectors: [], targetLayout: o }));
      let r = o ? { left: o.x + o.width * 2, top: o.y } : { left: 0, top: 0 };
      for (let i = 0; i < e.length; i++) {
        let a = e[i];
        if (!a) continue;
        let s = await sp(a, n, r);
        if (this.status !== "importing") return;
        (s && this.results.vectors.push(s),
          this.reportProgress?.({ status: this.status, finishedCount: i + 1 }));
      }
      await this.done();
    }
    cancel() {
      this.status = "canceled";
    }
    async done() {
      (this.results && (await this.onDone?.(this.results)), (this.status = "done"));
    }
  },
  _c = "run-svgo",
  ap = 1e6;
async function sp(t, e, n) {
  if (t.size > ap) {
    Fn({
      type: "add",
      variant: "error",
      key: _c,
      primaryText: "SVG is too large.",
      secondaryText: "You may need a PNG.",
      icon: "error",
      duration: 5e3,
    });
    return;
  }
  try {
    let o = await t.text(),
      r = ql(o),
      i = cp(t.name),
      a = pl(o, nl.createEmpty, r);
    return a
      ? (a.setProp("parentid", e),
        a.set({ name: i, isVectorSetItem: !0, ...n }),
        { node: a, size: r })
      : void 0;
  } catch (o) {
    (console.error("Failed to insert SVG:", o),
      Fn({
        type: "add",
        variant: "error",
        key: _c,
        primaryText: "Failed to insert",
        secondaryText: "your SVG.",
        icon: "error",
      }));
    return;
  }
}
var lp = /[_-]/gu;
function cp(t) {
  let [e] = Hl(t);
  return ua(e.replace(lp, " "));
}
var dp = {
  cache: !0,
  mutable: !0,
  update: !0,
  parentid: !0,
  originalid: !0,
  duplicatedFrom: !0,
  moduleSourceRevision: !0,
  moduleSourceRevisionHint: !0,
  moduleSourceRevisionCommittedHint: !0,
  kitSectionsStructure: !0,
  kitSectionHash: !0,
  kitSectionSource: !0,
  kitPageHTMLHash: !0,
  customizations: !0,
  customizationsDescription: !0,
  customizationsSectionId: !0,
  customizationsCategory: !0,
  customizationsRules: !0,
  isApplyingCustomizations: !0,
  layoutTemplateIdentifierOverride: !0,
  abTestingParentId: !0,
  abTestingDistributionWeightPpm: !0,
};
function dt(t, e) {
  if (!_(e)) {
    if (t === "children" && e instanceof Oa) return e.toArray();
    if (!dp[t] && !t.startsWith("export") && !ts(t)) return e;
  }
}
function up(t, e) {
  if (t !== "children") return dt(t, e);
}
function fp(t, e, n, o, r) {
  if (!Vn(t) || !kt(e)) return e;
  if (e.type === "slot")
    return !Dr(e.value) || (ve(n) && n.slotsAreChildNodes)
      ? e
      : e.value.map((i) => {
          let a = r.get(i.reference);
          return Eo(o, a) ? a : void 0;
        });
  if (e.type === "componentinstance") {
    if (!L(e.value)) return e;
    let i = r.get(e.value);
    return Eo(o, i) ? i : void 0;
  } else if (e.type === "array") {
    if (!Dr(e.value)) return e;
    let i = [],
      a = !1;
    for (let s of e.value) {
      if (!kt(s) || s.type !== "componentinstance" || ((a = !0), !L(s.value))) continue;
      let l = r.get(s.value);
      Eo(o, l) && i.push(l);
    }
    return a ? i : e;
  }
  return e;
}
function pp(t, e) {
  return (n, o, r) => dt(n, fp(n, o, r, e, t));
}
var Gn = oe("ModuleSourceRevision");
function Hb(t) {
  return R(ee({ path: t.path, targetUrl: t.targetUrl }));
}
function mp(t) {
  return R(ee(t, dt));
}
function qb(t, e) {
  return mp(t.cloneWithIds({ id: e }));
}
function hp(t, e) {
  switch (t.__class) {
    case "CollectionItemNode":
    case "ImageStylePresetNode":
    case "InlineCodeStylePresetNode":
    case "LayoutTemplateNode":
    case "LinkStylePresetNode":
    case "SmartComponentNode":
    case "TableStylePresetNode":
    case "WebPageNode":
      return R(ee(t, dt));
    case "FrameNode": {
      let n = e.get(t.parentid);
      return !n || !Ae(n) ? void 0 : R(ee(t, pp(e, n.id)));
    }
    case "CollectionNode": {
      b(W(t), "Expected collection node");
      let n = ee(e.root.locales),
        o = ee(e.root.webMetadata?.translatePagePaths),
        r = ee(t, up);
      if (O.isOn("cmsDatabase")) return R(n + o + r);
      let i = ee(t.getUnsortedChildren()?.map((a) => a.moduleSourceRevision));
      return R(n + o + r + i);
    }
    case "PresetsListNode": {
      let n = t.children?.filter((o) => !ss(o, e)) ?? [];
      return R(ee(n, dt));
    }
    case "TextStylePresetNode":
    case "BlockquoteStylePresetNode": {
      if (Vr(t)) return t.moduleSourceRevision;
      let n = ee(t, dt);
      return (
        t.cache.replicaInstances?.forEach((o) => {
          let r = e.getNodeWithTrait(o, Vr);
          r && (n += ee(r, dt));
        }),
        R(n)
      );
    }
    case "VectorSetNode": {
      b(ye(t), "Expected VectorSetNode");
      let n = t.getVectorSetItems().map((o) => {
        let r = He("vector", o.id, "default").localId,
          i = e.getNodeWithTrait(r, Os);
        return i ? i.save.moduleId : "";
      });
      return R(ee(n) + t.name);
    }
    case "ShapeContainerNode": {
      if (!Qe(t)) return;
      let n = e.getScopeNodeFor(t);
      if (!ye(n)) return;
      let o = ee(t, dt);
      return R(o + ee(n.variables));
    }
    case "CustomCodeScopeNode": {
      b(Pt(t), "Expected CustomCodeScopeNode");
      let n = t.loaded;
      b(n, "Expected CustomCodeScopeNode to be loaded");
      let o = {};
      for (let i of n.children)
        if (!(!da(i) || !i.pageIds))
          for (let a of i.pageIds) {
            let s = e.getNodeWithTrait(a, zn);
            s && (o[a] = s.dataIdentifier);
          }
      let r = ee(t, dt);
      return R(r + ee(o));
    }
    case "CanvasPageNode":
    case "RootNode":
      return;
    case "ComponentPresetNode":
      return;
    case "AbTestsEntityTypeRootNode":
    case "AgentPanelTool":
    case "AgentDragSelectTool":
    case "AgentSelectTool":
    case "AgentSkillNode":
    case "AgentSkillsListNode":
    case "AnalyticsScopeNode":
    case "BlockquoteEntityTypeRootNode":
    case "BooleanShapeNode":
    case "BoxShadow":
    case "BranchNode":
    case "BranchesNode":
    case "CanvasNode":
    case "CanvasScopeNode":
    case "CustomCodeNode":
    case "DesignPageNode":
    case "DesignPageEntityTypeRootNode":
    case "ChatTool":
    case "CMSEntityTypeRootNode":
    case "CodeComponentNode":
    case "CodeFileEntityTypeRootNode":
    case "CollectionReferenceVariableEditTool":
    case "Color":
    case "ColorEntityTypeRootNode":
    case "ColorStyleTokenListNode":
    case "ColorStyleTokenNode":
    case "ComponentEntityTypeRootNode":
    case "ConicGradient":
    case "ContentManagementNode":
    case "CreateCodeComponentBaseTool":
    case "CreateFrameBaseTool":
    case "CreateShapeBaseTool":
    case "CreateTextTool":
    case "EntityFolderNode":
    case "EntityReferenceNode":
    case "EntityRootNode":
    case "ContentManagementEntityTypeRootNode":
    case "ErrorListNode":
    case "ErrorNode":
    case "ExportOptions":
    case "ExternalModuleNode":
    case "ExternalModulesListNode":
    case "FeedbackTool":
    case "FormBooleanInputNode":
    case "FormPlainTextInputNode":
    case "FormSelectNode":
    case "FunnelNode":
    case "FunnelsEntityTypeRootNode":
    case "FunnelStepActionNode":
    case "FunnelStepNode":
    case "GapTool":
    case "GradientColorStop":
    case "GradientToolManagerTool":
    case "GuideMoveTool":
    case "HeaderRouteNode":
    case "FirewallRouteNode":
    case "HighlightStackItemTool":
    case "HighlightTool":
    case "InitialTool":
    case "InlineCodeEntityTypeRootNode":
    case "LayoutTemplateEntityTypeRootNode":
    case "LineAnchorMoveTool":
    case "LinearGradient":
    case "LinkEntityTypeRootNode":
    case "LinkTool":
    case "LoadingShieldTool":
    case "LocalizationGlossaryItemNode":
    case "LocalizationGlossaryNode":
    case "LocalModuleNode":
    case "LocalModulesListNode":
    case "MoveTool":
    case "MutableNode":
    case "OvalShapeNode":
    case "OverlayNode":
    case "PaddingTool":
    case "PageLinkTool":
    case "PanTool":
    case "PathCurveBendTool":
    case "PathDefaultTool":
    case "PathNode":
    case "PathSegment":
    case "PathSegmentAdditionTool":
    case "PathSegmentHandleMoveTool":
    case "PathSegmentMoveTool":
    case "PathSegmentSelectTool":
    case "PolygonShapeNode":
    case "PreviewSettings":
    case "ProxyRouteNode":
    case "RadialGradient":
    case "RadiusTool":
    case "RecoveryTool":
    case "RectangleShapeNode":
    case "RedirectRouteNode":
    case "ResizeTool":
    case "RewriteRouteNode":
    case "RichTextEditTool":
    case "RichTextNode":
    case "RichTextVariableEditTool":
    case "RotateTool":
    case "RouteNode":
    case "RouteLocaleNode":
    case "RouteSegmentNode":
    case "RouteSegmentRootNode":
    case "RoutesNode":
    case "SampleColorTool":
    case "ScaleTool":
    case "ScopeNode":
    case "SelectTool":
    case "ShaderNode":
    case "Shadow":
    case "ShapeGroupNode":
    case "SlotConnectTool":
    case "SlotNode":
    case "SlotPropertyNode":
    case "StarShapeNode":
    case "StringVariableEditTool":
    case "DateVariableEditTool":
    case "EnumVariableEditTool":
    case "StyledTextDraft":
    case "SVGNode":
    case "TestTool":
    case "TextEditTool":
    case "TextEntityTypeRootNode":
    case "TextNode":
    case "VectorSetEntityTypeRootNode":
    case "VekterGradientTool":
    case "VekterPathEngine":
    case "VekterTool":
    case "ViewportResizeTool":
    case "ZoomSelectTool":
    case "ZoomTool":
      return;
    default:
      Z(t.__class, "getModuleRevision: node.__class must be a valid value");
  }
}
function bi(t, e) {
  let n = t.get(e);
  return !n || !ze(n, t)
    ? !1
    : _(n.moduleSourceRevisionHint) ||
        n.moduleSourceRevisionCommittedHint === n.moduleSourceRevisionHint
      ? (Gn.debug(`Hint hasn't changed for ${n.id}.`), !1)
      : (Gn.debug(`Initial Revision: ${n.moduleSourceRevision}`), !0);
}
function Ii(t, e) {
  let n = t.get(e);
  if (!n || !ze(n, t)) return;
  let o = performance.now(),
    r = hp(n, t);
  Gn.debug(`Spent ${performance.now() - o}ms computing module revision for ${n.id}`);
  let i = A(r) && r !== n.moduleSourceRevision;
  Gn.debug(`Revisions: ${n.moduleSourceRevision}, ${r}`);
  let a = n.moduleSourceRevisionHint,
    s = i
      ? { moduleSourceRevision: r, moduleSourceRevisionCommittedHint: a }
      : { moduleSourceRevisionCommittedHint: a };
  return (
    Gn.debug(
      i
        ? `Updating moduleSourceRevision for ${n.id}.`
        : `Updating moduleSourceRevisionCommittedHint for ${n.id}.`
    ),
    n.setIgnoringReplica(s, t),
    i ? r : void 0
  );
}
function jc(t, e) {
  return e((n) => {
    if (n.isViewOnly) return;
    let o = n.get(t);
    return (
      W(o) &&
        !O.isOn("cmsDatabase") &&
        o.getUnsortedChildren()?.forEach((r) => {
          bi(n, r.id) && Ii(n, r.id);
        }),
      Ii(n, t)
    );
  });
}
function Hc(t, e) {
  return e((n) => {
    if (!n.isViewOnly) return t.reduce((o, r) => ((o[r.id] = Ii(n, r.id) ?? 1), o), {});
  });
}
var gp = 2;
function Ti(t) {
  if (x(t)) return gp;
}
function nT(t, e, n) {
  if (e.type === "siteMetadata" || e.type === "webPageMetadata") return !0;
  let o = Bn(t, n).includes(e.type);
  if ((b(t.id === e.name && o, "sourceNode and module must match"), Ho(e))) return !1;
  let r = Ti(t);
  if (_(r)) return !0;
  let i = e.metadata.codeGenVersion;
  return A(i) && i >= r;
}
var Li = 1;
function yp(t) {
  let e = t.metadata.compatibleTextStyleVersion;
  return A(e) ? e : 0;
}
function aT(t) {
  return yp(t) < Li;
}
var Nt = en(io(), 1);
function Jc(t, e) {
  if (ae(t)) return "Smart Component";
  if (x(t)) return "web page";
  if (xe(e.get(t.parentid))) return "prototype screen";
  if (xe(t)) return "prototype";
  if (W(t)) return "CMS collection";
}
function Kc(t, e, n) {
  return x(n) ? En(t, e, n) : (n.resolveValue("name") ?? void 0);
}
function Qc(t, e = "module", n) {
  let o = "is too large and will not update";
  Fn({
    type: "add",
    variant: "warning",
    text: n
      ? (0, Nt.jsxs)(Nt.Fragment, {
          children: [
            (0, Nt.jsxs)(Jo, { children: ["The ", e] }),
            " \u201C",
            n,
            "\u201D ",
            (0, Nt.jsx)(Jo, { children: o }),
          ],
        })
      : (0, Nt.jsxs)(Jo, { children: ["A ", e, " ", o] }),
    key: `module-too-large-${t}`,
    duration: 1 / 0,
    extraAction: {
      title: "Learn more",
      onClick() {
        (Pe("ui_interaction", { page: "module-too-large-toast", id: "learn_more" }),
          ml("https://www.framer.com/help/articles/how-to-fix-module-too-large-warning/"));
      },
    },
  });
  let i = `module-too-large-toast-${e.split(" ").join("-")}`;
  Pe("ui_impression", { page: i });
}
var Ip = 1e4,
  bp = 1e3,
  jn = oe("CodeGenerationStore");
function Tp(t) {
  return !(W(t) || xe(t) || ge(t) || tn(t) || nn(t) || ye(t));
}
function Lp(t, e) {
  if (!Qe(e)) return;
  let n = t.getScopeNodeFor(e);
  return (b(ye(n), "Vector set item must be in a VectorSetNode."), n.variables);
}
function Np(t, e) {
  let n = e.resolveValue("name")?.trim();
  return n || (ds(e) ? "Text" : $e(t, e, e.__class));
}
function Xc(t, e, n, o, r, i) {
  let a = {},
    s = Np(e, n);
  a.metadata || (a.metadata = {});
  let l = [...(i?.files ?? [])];
  Tp(n) &&
    l.push({
      name: "tree.json",
      type: "nodeTree",
      content: JSON.stringify(
        Object.assign(n.toJS(), {
          __treeSerializationVersion: t.serializationVersion,
          __vectorSetVariables: Lp(t, n),
        })
      ),
    });
  let c = Ti(n);
  c && (a.metadata.codeGenVersion = c);
  let f = i?.packageDependencies;
  return (
    f && (a.metadata.packageDependencies = Array.from(f)),
    $l(o) && (a.metadata.compatibleCmsVersion = Ol),
    ca(n) && (a.metadata.compatibleTextStyleVersion = Li),
    (a.assets = i?.assets ? Array.from(i.assets) : []),
    { ...a, title: s, files: l, errors: i?.errors ?? [], type: o, name: r }
  );
}
async function xp(t, e, n, o, r) {
  if (t && e instanceof Ji && e.code === 1001) {
    let i = Hs(t, (s) => o.treeIndex.getScopeIdFor(s)) ?? void 0,
      a = new qs({
        type: "CodeGeneration",
        nodeId: Oe(t) ? t.baseVariantId : t.id,
        reason: "FileTooLarge",
        sourceNodeId: t.id,
        scopeId: i,
      });
    (Qc(t.id, Jc(t, o.tree), Kc(o.tree, n, t)),
      await r((s) => {
        if (s.isViewOnly) return;
        s.lineage.setEditReason("codegeneration");
        let l = Gs.ensure(s);
        (jn.debug("Writing ClientFileTooLarge error to tree for", t.id), s.insertNode(a, l.id));
      }));
  } else jn.reportError(e);
}
var sr = class {
  constructor(e, n, o, r, i, a) {
    this.componentLoader = e;
    this.treeStore = n;
    this.modulesStore = o;
    this.runWhenIdle = r;
    this.scheduleDocumentUpdateIgnoringUndo = i;
    this.#t = new gn({
      delay: Ip,
      task: () =>
        this.runWhenIdle(() => {
          this.#e.synchronize();
        }),
      abortSignal: a,
    });
  }
  componentLoader;
  treeStore;
  modulesStore;
  runWhenIdle;
  scheduleDocumentUpdateIgnoringUndo;
  #e = new Jl();
  #t;
  #n = { coalescedTasks: 0, peakTasks: 0, tasksStarted: 0, tasksSettled: 0, taskDurationMs: 0 };
  getMetrics() {
    let e = 0,
      n = 0;
    for (let o of new Set([...this.#e.queue, ...this.#e.capturedItems]))
      (o.promise.state === "initial" && e++, o.promise.state === "pending" && n++);
    return { ...this.#n, pendingTasks: e, activeTasks: n };
  }
  prunePendingTasks(e) {
    let n = this.#e.capturedItems;
    this.#e.snapshot((o) =>
      o.promise.isResolved()
        ? !1
        : o.id === e && o.promise.state === "initial"
          ? (n.has(o) || this.#n.coalescedTasks++, !1)
          : !0
    );
  }
  enqueue(e, n, o) {
    this.prunePendingTasks(n);
    let r = e.get(n);
    if (!r || !fl(r, e)) return;
    let i = Tt(r),
      a = {};
    for (let { type: c, artifacts: f } of o) {
      let d = Xc(e, this.componentLoader, r, c, i, f);
      a[ie(c, i)] = d;
    }
    let { pendingTasks: s, activeTasks: l } = this.getMetrics();
    ((this.#n.peakTasks = Math.max(this.#n.peakTasks, s + l + 1)),
      this.#e.request(
        async () => {
          let c = performance.now();
          this.#n.tasksStarted++;
          try {
            (jn.debug("Persisting transient modules for", n),
              await this.modulesStore.upsertBatch(a));
          } catch (f) {
            await xp(
              r,
              f,
              this.componentLoader,
              this.treeStore,
              this.scheduleDocumentUpdateIgnoringUndo
            );
          } finally {
            (this.#n.tasksSettled++, (this.#n.taskDurationMs += performance.now() - c));
          }
        },
        { id: n, synchronize: !1 }
      ));
  }
  async persist(e, n, o) {
    let r = this.treeStore.getDataTreeOrPartialTree(),
      i = r.get(e);
    if (i)
      return (
        this.enqueue(r, e, n),
        o
          ? this.#t.debounce()
          : (await this.#e.synchronize(),
            Uo(i, r).map((a) => this.modulesStore.getPersistedModuleByLocalId(a)))
      );
  }
  async persistBatch(e) {
    let n = this.treeStore.getDataTreeOrPartialTree(),
      o = {},
      r = "batch";
    for (let s in e) {
      let l = n.get(s),
        c = e[s];
      if (!l || !c) continue;
      let f = Tt(l),
        d = new Set();
      for (let { type: u, artifacts: m } of c) {
        let h = ie(u, f);
        (b(!d.has(h), () => `Duplicate local module id: ${h}`),
          d.add(h),
          (o[h] = Xc(n, this.componentLoader, l, u, f, m)),
          (r += `-${h}`));
      }
    }
    this.prunePendingTasks(r);
    let { pendingTasks: i, activeTasks: a } = this.getMetrics();
    ((this.#n.peakTasks = Math.max(this.#n.peakTasks, i + a + 1)),
      await this.#e.request(
        async () => {
          let s = performance.now();
          (this.#n.tasksStarted++, jn.debug("Persisting transient modules for batch: ", r));
          try {
            await this.modulesStore.upsertBatch(o);
          } catch (l) {
            jn.reportError(l);
          } finally {
            (this.#n.tasksSettled++, (this.#n.taskDurationMs += performance.now() - s));
          }
        },
        { id: r, synchronize: !0 }
      ));
  }
  process = this.#e.synchronize;
  debugPendingTasks() {
    let e = this.#e.queue.filter((n) => !n.promise.isResolved());
    return {
      count: e.length,
      tasks: e.slice(0, 32).map((n) => ({ nodeId: n.id, state: n.promise.state })),
    };
  }
  processWithProgress = this.#e.synchronizeWithProgress;
  changeScope = () => this.#t.debounce(bp);
  isIdle = () => this.#e.size === 0;
  get size() {
    return this.#e.size;
  }
  isBlocking = () => this.#e.snapshot().some((e) => e.promise.state === "pending");
};
var Dp = () => ({
    stylePresets: [],
    componentPresets: [],
    collections: [],
    smartComponents: [],
    screens: [],
    prototypes: [],
    webpages: [],
    metadata: [],
    layoutTemplates: [],
    snippets: [],
  }),
  q = oe("CodeGenerationStore");
var Rp = 500,
  ed = 1500,
  wp = 5e3,
  Vp = 6e4,
  zp = 16,
  Yc = 64;
function Pp(t) {
  for (let e in t.to) if (e in Qs) return !0;
  return !1;
}
function Mp(t) {
  return ye(t) ? Rp : wn(t) || W(t) ? wp : ed;
}
var Zc = class {
  constructor(e, n, o, r, i, a, s, l, c, f, d, u, m = new Map()) {
    this.scheduler = e;
    this.componentLoader = n;
    this.treeStore = o;
    this.modulesStore = r;
    this.vectorsStore = i;
    this.assetMap = a;
    this.timeline = s;
    this.engineChanges = l;
    this.scheduleDocumentUpdateIgnoringUndo = c;
    this.updateTreeCache = f;
    this.measureNodes = d;
    ((this.#t = m),
      (this.#u = this.timeline.openNodeChangesReader()),
      (this.#m = new gn({
        delay: ed,
        maxDelay: Vp,
        task: () =>
          this.scheduler.runWhenIdle(() => {
            this.generateAndUpdateQueuedComponents(void 0, "scheduled");
          }),
        abortSignal: u,
      })),
      (this.persistence = new sr(
        this.componentLoader,
        this.treeStore,
        this.modulesStore,
        (h) => this.scheduler.runWhenIdle(h),
        (h) => this.scheduleDocumentUpdateIgnoringUndo(h),
        u
      )),
      oc(
        {
          name: "code-generation",
          check: () => {
            if (yt.isTest) return !1;
            let h = this.treeStore.getDataTreeOrPartialTree();
            return h.isViewOnly ? !1 : this.hasBlockingUpdates(h);
          },
          effect: () => {
            q.debug(
              "Showing Unsaved Changes dialog (generation queue #: ",
              this.#t.size,
              ", component ids to generate: ",
              [...this.#t.keys()],
              ")"
            );
            let h = this.getMetrics();
            (h.paused > 1
              ? q.reportError("Tab closed with paused code generations", h)
              : (h.persisting > 5 || h.serializing > 5 || h.queue > 5) &&
                q.reportError("Tab closed with multiple code generations in progress", h),
              this.#e.clear(),
              this.generateAndUpdateQueuedComponents(void 0, "unload")
                .then(() => {
                  this.persistence.process();
                })
                .catch((g) => q.reportError(g)));
          },
        },
        u
      ),
      window.addEventListener(
        "blur",
        async () => {
          try {
            await this.flushQueuedCodeGenerationOnWindowBlur();
          } catch (h) {
            q.reportError(h);
          }
        },
        { signal: u }
      ));
  }
  scheduler;
  componentLoader;
  treeStore;
  modulesStore;
  vectorsStore;
  assetMap;
  timeline;
  engineChanges;
  scheduleDocumentUpdateIgnoringUndo;
  updateTreeCache;
  measureNodes;
  #e = new Set();
  #t;
  #n = new Map();
  #u;
  #a = 0;
  #l = 0;
  #p = new Map();
  #i = {
    sourceRequests: 0,
    coalescedSourceRequests: 0,
    preparedQueuedSources: 0,
    queueWaitMs: 0,
    maxQueueWaitMs: 0,
    peakQueuedSources: 0,
    peakPreparingSources: 0,
    fullDrainRequests: 0,
  };
  #S = {
    scheduled: 0,
    bulkScopeCompletion: 0,
    scopeChange: 0,
    explicitDemand: 0,
    windowBlur: 0,
    unload: 0,
  };
  #s = { attempts: 0, active: 0, peakActive: 0, outputModules: 0, durationMs: 0, storageWaitMs: 0 };
  #f = { active: 0, peakActive: 0, durationMs: 0 };
  #I = new oi(zp);
  isIdle() {
    return (
      this.#d.size === 0 &&
      this.#t.size === 0 &&
      this.#l === 0 &&
      this.serialization.isIdle() &&
      this.persistence.isIdle()
    );
  }
  getMetrics() {
    let e = this.#p.values().next().value;
    return {
      queue: this.#t.size,
      paused: this.#e.size,
      serializing: this.#o.size,
      pendingGenerations: this.#d.size,
      persisting: this.persistence.size,
      updating: this.#l,
      completed: this.#a,
      scheduling: {
        ...this.#i,
        drainRequests: { ...this.#S },
        oldestPendingWaitMs: e === void 0 ? 0 : performance.now() - e,
      },
      serialization: { ...this.#s },
      compileOutput: { ...this.#f },
      persistence: this.persistence.getMetrics(),
    };
  }
  documentDidLoad() {
    (this.#t.clear(),
      this.#p.clear(),
      this.#n.clear(),
      this.#o.clear(),
      this.#r.clear(),
      this.#d.clear(),
      this.#h.clear());
  }
  #m;
  #h = new Map();
  telemetrySession(e, n) {
    let o = this.#h.get(n);
    if (o) return o;
    let r = e.get(n);
    b(r, "TelemetrySession: source node must exist");
    let i = new Ko(r);
    return (this.#h.set(n, i), i);
  }
  #g = void 0;
  getSerializationId() {
    return this.#g ? `framer-${this.#g}` : `framer-${Ll(5)}`;
  }
  testing = {
    setComponentGenerationQueue: (e) => {
      ((this.#t = e), this.#p.clear());
    },
    flushQueuedCallbacks: () => {
      this.#n.forEach((e) => e.forEach((n) => n()));
    },
    generateAndUpdateQueuedComponents: (e) => this.generateAndUpdateQueuedComponents(e),
    setSerializationId: (e) => {
      this.#g = e;
    },
    setModulesStore: (e) => {
      (Object.assign(this, { modulesStore: e }),
        Object.assign(this.persistence, { modulesStore: e }));
    },
    waitForProcessing: async (e) => {
      await this.serialization.promise(e);
    },
    compiledBarrier: (e) => this.#r.get(e),
    markSerializeStarted: (e) => {
      (this.#o.get(e)?.resolve(), this.#o.delete(e));
    },
  };
  persistence;
  async flushQueuedCodeGenerationOnWindowBlur() {
    document.visibilityState === "hidden" &&
      (await this.generateAndPersistAllQueuedComponents("windowBlur"));
  }
  async generateAndPersistAllQueuedComponents(e = "explicitDemand") {
    (this.#i.fullDrainRequests++,
      await this.ensureSiteMetadataModule(),
      await this.ensureSnippetsModule(),
      await this.ensureCollectionModulesExportUtils(),
      await this.generateAndUpdateQueuedComponents(void 0, e),
      await this.serialization.idle(),
      await this.persistence.process());
  }
  debugGetComponentSourceNodes() {
    let e = Dp();
    b(!1, "This method should only be used in tests.");
    let n = this.treeStore.getDataTreeOrPartialTree();
    for (let { node: o, skipChildren: r } of n.root.walkWithSkipChildren())
      x(o)
        ? (e.webpages.push(o), e.metadata.push(o), r())
        : ae(o)
          ? (e.smartComponents.push(o), r())
          : W(o)
            ? (e.collections.push(o), r())
            : Jr(o)
              ? (e.stylePresets.push(o), r())
              : nn(o)
                ? e.componentPresets.push(o)
                : ul(o, n)
                  ? (e.screens.push(o), r())
                  : xe(o)
                    ? e.prototypes.push(o)
                    : ge(o)
                      ? e.metadata.push(o)
                      : K(o)
                        ? e.layoutTemplates.push(o)
                        : Pt(o) && e.snippets.push(o);
    return e;
  }
  postProcess(e, n, o) {
    if (!e.isViewOnly) {
      if (o) {
        this.#u.clear();
        return;
      }
      (this.ingestChanges(e), this.#m.debounce(Mp(n)));
    }
  }
  ingestChanges(e) {
    for (let [n, o] of this.#u.read()) {
      if (o.length === 0 || o.every((i) => i.ignoreInCodeGeneration)) continue;
      let r = e.get(n);
      if (r) {
        if (x(r) && Qa(o)) {
          let i = e.get(Xs);
          i && this.addChangeToQueue(i);
        }
        if (ze(r, e)) {
          ls(r, o) && this.addChangeToQueue(r);
          continue;
        }
        if (ge(r)) {
          if (!o.some(Pp)) continue;
          this.addChangeToQueue(r);
        } else if (xe(r)) this.addChangeToQueue(r);
        else {
          let i = this.treeStore.treeIndex.getGroundNodeIdFor(n),
            a = e.get(i);
          if (!a || ve(a)) continue;
          let s = e.getScopeNodeFor(a);
          if (!xe(s)) continue;
          this.queueCanvasPageNodesFollowingLinks(e, a);
        }
      }
    }
  }
  queueCanvasPageNodesFollowingLinks(e, n) {
    if ((this.addChangeToQueue(n), !(!n.cache.links || n.cache.links.size === 0)))
      for (let o of n.cache.links) {
        let r = this.treeStore.treeIndex.getGroundNodeIdFor(o),
          i = e.get(r);
        !i || ve(i) || this.queueCanvasPageNodesFollowingLinks(e, i);
      }
  }
  changeScope() {
    this.treeStore.tree.isViewOnly ||
      yt.isTest ||
      this.isIdle() ||
      this.serialization
        .idle()
        .then(() => this.generateAndUpdateQueuedComponents(void 0, "scopeChange"))
        .then(() => {
          if (!O.isOn("prioritizedModuleEvaluation")) {
            this.persistence.process();
            return;
          }
          this.persistence.isIdle() || this.persistence.changeScope();
        })
        .catch((e) => q.reportError(e));
  }
  async createVectorSet(e, n) {
    let o = e[0];
    if (!o) return;
    let i = this.treeStore.getDataTreeOrPartialTree().getScopeNodeFor(o);
    b(ye(i), "VectorSet: VectorSetNode must exist.");
    let a = performance.now(),
      s = await this.batchSerializeVectors(e, n);
    if (!s) return;
    let l = {},
      c = 0,
      f = Object.keys(s).length,
      d = Wc(f),
      u = [];
    for (let m in s) {
      let h = s[m];
      h && ((l[m] = h), c++, !(c % d !== 0 && c !== f) && (u.push(l), (l = {})));
    }
    for (let m = 0; m < u.length; m++) {
      let h = u[m];
      h && (await this.persistence.persistBatch(h), n?.("persisting", m));
    }
    (await this.forceComponentUpdate(i.id),
      q.debug("createVectorSet: Spent ", performance.now() - a, "ms creating a new vector set."));
  }
  async createModuleFromComponentSource(e, n) {
    let o = this.treeStore.getDataTreeOrPartialTree();
    if (
      (b(
        !o.isViewOnly,
        "CodeGenerationStore: viewers cannot create smart components or web pages."
      ),
      b($t(e, o), "CodeGenerationStore: componentSourceNode must be a valid component source."),
      fn(e, o, this.modulesStore))
    )
      return;
    (q.debug("createModuleFromComponentSource", e.id), this.serialization.prepare(e.id));
    let i = this.#r.get(e.id);
    return o.withPersistedCaches(async () => {
      let a = await this.generateComponent(e.id, 1, void 0, { intrinsicSize: n });
      if (!a) throw new Error("Code not generated");
      return (
        await this.compileOutput(e, a, i),
        (o = this.treeStore.getDataTreeOrPartialTree()),
        Uo(e, o).map((s) => this.modulesStore.getPersistedModuleByLocalId(s))
      );
    });
  }
  revisionNodes(e) {
    let n = [];
    for (let o of e.root.children)
      (ze(o, e) && n.push(o),
        (nn(o) || wn(o)) &&
          o.children.forEach((r) => {
            (ze(r, e) && n.push(r),
              W(r) &&
                r.getUnsortedChildren().forEach((i) => {
                  ze(i, e) && n.push(i);
                }));
          }));
    return n;
  }
  nodesWithStaleModules(e, n) {
    let o = [],
      r = n?.ignoreHint ?? !1,
      i = this.treeStore.getDataTreeOrPartialTree();
    for (let a of this.revisionNodes(i))
      qe(a) ||
        (e && !e(a)) ||
        (((!r && a.moduleSourceRevisionHint !== a.moduleSourceRevisionCommittedHint) ||
          !this.allModulesAreAtRevision(i, a.id)) &&
          o.push(a));
    return o;
  }
  debugNodeIdsByCodeGenPhase() {
    return {
      stale: new Set(this.nodesWithStaleModules().map((e) => e.id)),
      queued: new Set(this.#t.keys()),
      generating: new Set(this.#o.keys()),
      paused: new Set(this.#e),
    };
  }
  canSerialize(e) {
    let n = this.treeStore.getDataTreeOrPartialTree();
    if (!n.isViewOnly) return !0;
    let o = n.get(e);
    if (!o || !n.isGroundNode(o)) return !1;
    let r = n.getScopeNodeFor(o);
    return Ae(r);
  }
  async updateComponent(e, n = 0) {
    b(
      this.canSerialize(e),
      "CodeGenerationStore: viewers cannot generate smart component or web page updates."
    );
    let o = this.treeStore.getDataTreeOrPartialTree(),
      r = o.get(e);
    if (!r || !$t(r, o)) return !1;
    if (this.serialization.preparing(e)) {
      let s = this.#r.get(e);
      return (await this.serialization.promise(e), await s, !0);
    }
    q.debug("Update component: ", e, ", Condition: ", n);
    let i = this.#t.get(e);
    this.serialization.prepare(e);
    let a = this.#r.get(e);
    this.#l++;
    try {
      return await o.withPersistedCaches(async () => {
        let s = await this.generateComponent(e, n, i);
        return s ? this.compileOutput(r, s, a) : !1;
      });
    } finally {
      this.#l--;
    }
  }
  async forceComponentUpdate(e) {
    return (
      b(
        this.canSerialize(e),
        "CodeGenerationStore: viewers cannot generate smart component or web page updates."
      ),
      (await this.updateComponent(e, 2)) ? (await this.persistence.process(), !0) : !1
    );
  }
  async updateComponentBatch(e, n, { concurrency: o, onUpdated: r, onError: i } = {}) {
    let a = o === void 0 ? this.#I : new oi(o),
      s = Array.from(e),
      l = async (c) => {
        try {
          let f = await a.run(() =>
            n === 2 ? this.forceComponentUpdate(c) : this.updateComponent(c, n)
          );
          r?.(c, f);
        } catch (f) {
          if (!i) throw f;
          i(f, c);
        }
      };
    for (let c = 0; c < s.length; c += Yc) await Promise.all(s.slice(c, c + Yc).map(l));
  }
  queueCallbackAfterComponentCompilation(e, n) {
    let o = this.#n.get(e);
    o ? o.push(n) : this.#n.set(e, [n]);
  }
  #y(e) {
    (this.#n.get(e)?.forEach((n) => n()), this.#n.delete(e));
  }
  untilCompiled(e) {
    return new Promise((n) => {
      this.queueCallbackAfterComponentCompilation(e, n);
    });
  }
  calculateNewModuleRevision(e) {
    let n = this.treeStore.getDataTreeOrPartialTree(),
      o = n.get(e);
    if (!o || !ze(o, n)) return 1;
    if (!((!Pt(o) && !bi(n, e)) || !n.has(e)))
      return jc(e, this.scheduleDocumentUpdateIgnoringUndo);
  }
  allModulesAreAtRevision(e, n, o) {
    let r = e.get(n);
    if (!r || !ze(r, e)) return !1;
    let i = o ?? r.moduleSourceRevision;
    return Bn(r, e).every(
      (a) => this.modulesStore.forType(a).getByStableName(Tt(r)).sourceRevision() === i
    );
  }
  async ensureDependencyModulesExist() {
    let e = this.treeStore.getDataTreeOrPartialTree();
    for (let n of this.revisionNodes(e))
      if ($t(n, e) && !fn(n, e, this.modulesStore))
        try {
          await this.updateComponent(n.id);
        } catch (o) {
          q.error("Error updating dependency module", n.id, o);
        }
  }
  async ensureSourceNode(e) {
    let n = this.treeStore.getDataTreeOrPartialTree().get(e),
      o = ct(n) && !n.isLoaded() ? await n.load() : n;
    if (o) {
      if (
        (x(o) &&
          (await this.ensureSiteMetadataModule(),
          b(o.loaded, "WebPageNode must be loaded"),
          await this.ensureReferencedCollectionsExportUpToDateEnumUtils(o.loaded)),
        W(o) && (await this.ensureDependentModulesAreCompatibleWithCollection(o)),
        Qe(o))
      ) {
        let r = this.treeStore.getDataTreeOrPartialTree().getScopeNodeFor(o);
        if (!ye(r)) return;
        let i = ie("vectorSet", r.id);
        if (this.modulesStore.getModuleEntryByLocalId(i)) return;
        await this.createModuleFromComponentSource(r);
      }
      await this.ensureAllNodesWithLinksHaveLinkedScopesLoaded(o);
    }
  }
  async ensureSiteMetadataModule() {
    let e = this.treeStore.getDataTreeOrPartialTree();
    if (e.isViewOnly) return;
    let n = e.root;
    if (!fn(n, e, this.modulesStore)) return this.updateComponent(n.id);
  }
  async ensureSnippetsModule() {
    let e = this.treeStore.getDataTreeOrPartialTree();
    if (e.isViewOnly) return;
    let n = Ys.get(e);
    if (!n) return;
    let o = await n.load();
    if (!(!o || fn(o, e, this.modulesStore))) return this.updateComponent(o.id);
  }
  async ensureCollectionModulesExportUtils() {
    let e = this.treeStore.getDataTreeOrPartialTree();
    if (e.isViewOnly) return;
    let n = Ms(e);
    if (n) {
      b(n.isLoaded(), "ContentManagementNode is never shallow.");
      for (let o of n.children) {
        let r = ie("collection", o.id),
          i = this.modulesStore.getPersistedModuleByLocalId(r);
        if (!(!i || i.exports.includes("utils"))) {
          Pe("ui_interaction", { page: "canvas", id: "regenerate-collection-for-utils" });
          try {
            await this.forceComponentUpdate(o.id);
          } catch (a) {
            q.error("Error regenerating collection for utils export", o.id, a);
          }
        }
      }
    }
  }
  hasBlockingUpdates(e) {
    if (this.#d.size > 0) return !0;
    if (this.#t.size > 0) {
      for (let [n, o] of this.#t) if (e.has(n) && o) return !0;
    }
    return !this.persistence.isIdle() || !this.serialization.isIdle();
  }
  async updateModules(e, n) {
    let o = this.treeStore.getDataTreeOrPartialTree(),
      r = o.get(e),
      i = this.timeline.remoteTreeVersion;
    if (!r || !$t(r, o)) return !1;
    let a = [];
    for (let { revision: l, source: c, type: f, artifacts: d } of n) {
      let u = {
          assets: d?.assets,
          submodules: d?.submodules,
          binaryAssets: d?.binaryAssets,
          preventCircularImports: f === "canvasComponent",
          telemetrySession: this.telemetrySession(o, e),
          treeVersion: i,
          sourceRevision: l,
          svgIcon: d?.files?.find((y) => y.type === "svgIcon")?.content,
          stableName: !0,
        },
        m = this.modulesStore.forType(f).getByStableName(Tt(r)),
        h = m.sourceRevision() !== l,
        g = F.sections(Ac(), c);
      (!h && m.currentSourceEquals(g, u)) || a.push({ localId: m.localId, source: g, options: u });
    }
    if (a.length === 0) return !1;
    let s = await this.modulesStore.updateSources(a);
    return (Qe(r) && this.vectorsStore.delete(e), s);
  }
  moduleTypeOrder = {
    localization: 0,
    webPageMetadata: 1,
    siteMetadata: 2,
    collection: 3,
    draftCollection: 4,
    css: 5,
    componentPresets: 6,
    vector: 7,
    vectorSet: 8,
    canvasComponent: 9,
    screen: 10,
    ["prototype"]: 11,
    codeFile: 12,
    config: 13,
    layoutTemplate: 14,
    design: 15,
    kit: 16,
    snippets: 17,
    shader: 18,
  };
  allModulesExistForSources(e, n) {
    let o = Tt(e);
    for (let r of n) if (!this.modulesStore.getPersistedModuleByLocalId(ie(r.type, o))) return !1;
    return !0;
  }
  async compileOutput(e, n, o) {
    if (!this.canSerialize(e.id)) return (this.#c(e.id, o), !1);
    let r = performance.now();
    (this.#f.active++, (this.#f.peakActive = Math.max(this.#f.peakActive, this.#f.active)));
    try {
      let i = n.slice().sort((s, l) => this.moduleTypeOrder[s.type] - this.moduleTypeOrder[l.type]);
      if (this.allModulesExistForSources(e, i)) await this.updateModules(e.id, i);
      else
        for (let s of i) {
          let l = await this.updateModules(e.id, [s]),
            c = Tt(e);
          !this.modulesStore.getPersistedModuleByLocalId(ie(s.type, c)) &&
            l &&
            (await this.persistence.persist(e.id, [s]));
        }
      W(e) && (await this.ensureDependentModulesCanRenderCollectionColors(e));
    } catch (i) {
      throw (this.#c(e.id, o, i), i);
    } finally {
      (this.#f.active--, (this.#f.durationMs += performance.now() - r));
    }
    return (this.#a++, this.#y(e.id), this.#c(e.id, o), this.persistence.persist(e.id, n, !0), !0);
  }
  pauseCodeGeneration(e) {
    return (
      q.debug("Pausing code generation for", e),
      this.#e.add(e),
      () => {
        (q.debug("Resuming code generation for", e), this.#e.delete(e), this.#m.debounce());
      }
    );
  }
  async generateAndUpdateQueuedComponents(e, n = "explicitDemand") {
    if ((this.#S[n]++, this.treeStore.getDataTreeOrPartialTree().isViewOnly)) return [];
    await this.ensureDependencyModulesExist();
    let o = [],
      r = new Map();
    for (let [s] of this.#t) {
      if (this.#e.has(s) || this.serialization.preparing(s)) continue;
      let l = this.treeStore.getDataTreeOrPartialTree(),
        c = l.get(s);
      if (!c || !$t(c, l) || (e && !Bn(c, l).includes(e))) {
        this.dequeueSource(s);
        continue;
      }
      if ((this.serialization.prepare(s), Qe(c))) {
        let f = l.getScopeNodeFor(c);
        if (!f) continue;
        let d = r.get(f.id) ?? [];
        (d.push(c), r.set(f.id, d));
      } else o.push(c);
    }
    let i = new Set();
    q.debug("Batch generating queued vector nodes");
    for (let s of r.values())
      try {
        let l = await this.batchSerializeVectors(s, void 0, i);
        for (let c in l) this.persistence.persist(c, l[c], !0);
      } catch {}
    return (
      q.debug("Generating queued components", { ids: o.map(qo) }),
      await this.treeStore.getDataTreeOrPartialTree().withPersistedCaches(async () => {
        for (let s of o)
          try {
            q.debug("Flushing queue of ", s.id);
            let l = this.#r.get(s.id),
              c = await this.generateComponent(s.id, 1);
            c && (await this.compileOutput(s, c, l), i.add(s.id));
          } catch {}
      }),
      Array.from(i)
    );
  }
  #o = new Map();
  #r = new Map();
  #d = new Set();
  async batchSerializeVectors(e, n, o) {
    let r = e[0];
    if (!r) return;
    let i = e.map((a) => {
      this.serialization.prepare(a.id);
      let s = this.#o.get(a.id),
        l = this.#r.get(a.id);
      return (b(s && l), { node: a, prepared: s, compiled: l });
    });
    try {
      (await this.ensureSourceNode(r.id), await this.ensureSourceCache(r.id));
      let a = await Hc(e, this.scheduleDocumentUpdateIgnoringUndo);
      b(a, "VectorSet: newModuleRevisions must exist");
      let s = {
          componentLoader: this.componentLoader,
          assetMap: this.assetMap,
          modulesStore: this.modulesStore,
          treeStore: this.treeStore,
          tree: this.treeStore.getDataTreeOrPartialTree(),
          vectorsStore: this.vectorsStore,
          engineChanges: this.engineChanges,
          measureNodes: this.measureNodes,
        },
        l = await Promise.allSettled(
          i.map(async ({ node: u, prepared: m, compiled: h }) => {
            try {
              if (!a[u.id]) {
                this.#c(u.id, h);
                return;
              }
              let C = (
                await this.serializerForSourceNode(this.treeStore.getDataTreeOrPartialTree(), u)(
                  u,
                  s,
                  { serializationId: this.getSerializationId() }
                )
              )?.[0];
              return (
                b(C, () => `VectorSet: source must exist. ${u.id}`),
                { id: u.id, source: C, compiled: h }
              );
            } finally {
              (m.resolve(), this.#o.get(u.id) === m && this.#o.delete(u.id));
            }
          })
        ),
        c = l.find((u) => u.status === "rejected");
      if (c?.status === "rejected") throw c.reason;
      let f = 0,
        d = {};
      for (let u of l) {
        if (u.status !== "fulfilled" || !u.value) continue;
        let { id: m, source: h, compiled: g } = u.value,
          y = await this.updateModules(m, [h]);
        (this.#c(m, g), y && ((d[m] = [h]), n?.("serializing", f), f++, o?.add(m)));
      }
      return d;
    } catch (a) {
      for (let { node: s, prepared: l, compiled: c } of i)
        (l.resolve(),
          this.#o.get(s.id) === l && this.#o.delete(s.id),
          c.isResolved() || this.#c(s.id, c, a));
      throw a;
    }
  }
  async createVersionedVectorSet(e) {
    return this.treeStore.getDataTreeOrPartialTree().withPersistedCaches(async () => {
      await this.generateAndPersistAllQueuedComponents();
      let o = this.#r.get(e.id),
        r = await this.generateComponent(e.id, 2, void 0, { includeVectorSetSaveIds: !0 });
      return !r || !(await this.compileOutput(e, r, o))
        ? !1
        : (await this.persistence.process(), !0);
    });
  }
  serialization = {
    idle: async () => {
      for (; this.#o.size > 0 || this.#d.size > 0;)
        await Promise.all([...this.#o.values(), ...Array.from(this.#d, (e) => e.catch(() => {}))]);
    },
    isIdle: () => this.#o.size === 0,
    prepare: (e) => {
      let n = this.#p.get(e);
      if (n !== void 0) {
        let i = performance.now() - n;
        (this.#i.preparedQueuedSources++,
          (this.#i.queueWaitMs += i),
          (this.#i.maxQueueWaitMs = Math.max(this.#i.maxQueueWaitMs, i)));
      }
      this.dequeueSource(e);
      let o = this.#o.get(e);
      (this.#o.set(e, o ?? new ni()),
        (this.#i.peakPreparingSources = Math.max(this.#i.peakPreparingSources, this.#o.size)));
      let r = (o && this.#r.get(e)) || new ni();
      (r.catch(() => {}), this.#r.set(e, r), this.#d.add(r));
    },
    cancel: (e, { prepared: n, compiled: o } = this.#C(e)) => {
      let r = this.#o.get(e) === n && this.#r.get(e) === o;
      (r && this.dequeueSource(e), this.#v(e, n), r && this.#y(e), this.#c(e, o));
    },
    preparing: (e) => this.#o.has(e),
    promise: (e) => this.#o.get(e),
    commit: async (e, n, o, r, i) => {
      let a = r ? r.prepared : this.#o.get(e.id),
        s = r?.compiled,
        l = () => this.#v(e.id, a, !0),
        c = performance.now();
      (this.#s.attempts++,
        this.#s.active++,
        (this.#s.peakActive = Math.max(this.#s.peakActive, this.#s.active)));
      try {
        let f = await this.#b(e, n, o, a, i);
        return (
          (this.#s.outputModules += f?.length ?? 0),
          f || (l(), this.#r.get(e.id) === s && this.#y(e.id), this.#c(e.id, s)),
          f
        );
      } catch (f) {
        throw (q.reportError(f), l(), this.#c(e.id, s, f), f);
      } finally {
        (this.#s.active--, (this.#s.durationMs += performance.now() - c));
      }
    },
  };
  #C(e) {
    return { prepared: this.#o.get(e), compiled: this.#r.get(e) };
  }
  #v(e, n, o = !1) {
    (o ? n?.reject() : n?.resolve(), this.#o.get(e) === n && this.#o.delete(e));
  }
  #c(e, n, o) {
    if (n) {
      if ((this.#d.delete(n), this.#r.get(e) === n && this.#r.delete(e), o !== void 0)) {
        n.reject(o);
        return;
      }
      n.resolve();
    }
  }
  async #b(e, n, o, r, i) {
    let a = this.treeStore.getDataTreeOrPartialTree();
    if (!ge(e) && !a.has(n.id)) return;
    let s = this.telemetrySession(a, e.id),
      l = s.start("synchronize"),
      c = performance.now();
    (q.debug("Waiting for ModulesStore to be idle", { id: e.id }),
      await this.modulesStore.whenIdle());
    let f = performance.now() - c;
    if (
      ((this.#s.storageWaitMs += f),
      q.debug("Waited", Math.round((f / 1e3) * 100) / 100, "seconds for ModulesStore to be idle", {
        id: e.id,
      }),
      l.end(),
      (a = this.treeStore.getDataTreeOrPartialTree()),
      !a.has(n.id))
    )
      return;
    let d = e.draftOrCurrent();
    if (o !== 2 && this.isSkippedEmptyScopeNode(d)) return;
    let u = s.start("serialize");
    W(e) && kp(e, this.componentLoader, a, this);
    let m = this.serializerForSourceNode(a, e);
    this.#v(e.id, r);
    let h = await m(
      e,
      {
        componentLoader: this.componentLoader,
        assetMap: this.assetMap,
        modulesStore: this.modulesStore,
        treeStore: this.treeStore,
        tree: this.treeStore.getDataTreeOrPartialTree(),
        vectorsStore: this.vectorsStore,
        engineChanges: this.engineChanges,
        measureNodes: this.measureNodes,
      },
      {
        serializationId: this.getSerializationId(),
        initialIntrinsicSize: i?.intrinsicSize,
        includeVectorSetSaveIds: i?.includeVectorSetSaveIds,
      }
    );
    return (
      h.forEach(({ source: g, artifacts: y }) => {
        if (
          (q.trace(
            "serializeComponent",
            `
>>>> >>>>
`,
            $(g),
            `
<<<< <<<<`
          ),
          y?.submodules)
        )
          for (let [v, C] of y.submodules)
            q.trace(
              "serializeComponent",
              `
>>>>`,
              v,
              `>>>>
`,
              C,
              `
<<<<`,
              v,
              "<<<<"
            );
      }),
      u.end(h),
      h
    );
  }
  isSkippedEmptyScopeNode(e) {
    return Pt(e) ? !1 : !!(ct(e) && (!e.isLoaded() || e.loaded.children?.length === 0));
  }
  async generateComponent(e, n, o, r) {
    let i = this.#C(e);
    try {
      (q.debug("Ensure source node", e),
        await this.ensureSourceNode(e),
        q.debug("Ensure source cache", e),
        await this.ensureSourceCache(e));
    } catch (c) {
      throw (this.serialization.cancel(e, i), q.error("Failed to ensure source cache", e), c);
    }
    let a = await this.getComponentSourceNodeAndRoot(e, n, o, i);
    if (!a) return;
    q.debug("Serialize", e);
    let { componentSourceNode: s, root: l } = a;
    return this.serialization.commit(s, l, n, i, r);
  }
  async getComponentSourceNodeAndRoot(e, n, o, r) {
    try {
      let i = await this.calculateNewModuleRevision(e),
        a = this.treeStore.getDataTreeOrPartialTree(),
        s = a.get(e);
      if (!s || !$t(s, a) || !this.shouldGenerateUnderCondition(a, n, s, i, o)) {
        (q.debug("Skipping component source node", e), this.serialization.cancel(e, r));
        return;
      }
      let l = Oe(s) ? s.getPrimaryVariant() : s;
      return { componentSourceNode: s, root: l };
    } catch (i) {
      throw (this.serialization.cancel(e, r), i);
    }
  }
  shouldGenerateUnderCondition(e, n, o, r, i) {
    let a = i ?? this.#t.get(o.id);
    if ((this.dequeueSource(o.id), !fn(o, e, this.modulesStore))) return !0;
    switch (n) {
      case 2:
        return !0;
      case 1:
        return !_(r) || !this.allModulesAreAtRevision(e, o.id, r);
      case 0:
        return !_(r) && !!a && !this.allModulesAreAtRevision(e, o.id, r);
      default:
        Z(n);
    }
  }
  addChangeToQueue(e) {
    if (!this.shouldSkipChange(e)) {
      if ((Qe(e) && this.vectorsStore.queue(e.id), qe(e))) {
        let n = e.parentid;
        (b(n, "Collection node has no parent"), this.queueSource(n));
        return;
      }
      this.queueSource(e.id);
    }
  }
  dequeueSource(e) {
    (this.#t.delete(e), this.#p.delete(e));
  }
  queueSource(e) {
    (this.#i.sourceRequests++,
      this.#t.has(e) ? this.#i.coalescedSourceRequests++ : this.#p.set(e, performance.now()),
      this.#t.set(e, !0),
      (this.#i.peakQueuedSources = Math.max(this.#i.peakQueuedSources, this.#t.size)));
  }
  serializerForSourceNode(e, n) {
    return W(n)
      ? Nc
      : Oe(n)
        ? vi
        : Jr(n)
          ? Mc
          : xe(n)
            ? wc
            : ge(n)
              ? sl
              : nn(n)
                ? Dc
                : ye(n)
                  ? Oc
                  : Qe(n)
                    ? Uc
                    : Pt(n)
                      ? dl
                      : Ae(e.get(n.parentid))
                        ? vi
                        : Vc;
  }
  shouldSkipChange(e) {
    if (ve(e)) {
      let n = J(e.codeComponentIdentifier);
      if (vt(n) && n.type === "screen") return !0;
    }
    return !1;
  }
  async ensureSourceCache(e) {
    let n = this.treeStore.getDataTreeOrPartialTree().get(e);
    !n ||
      n.update === n.cache.lastUpdate ||
      (await new Promise((o) => {
        this.scheduler.runBeforeNextFrame(() => {
          (ct(n) && n.update !== n.cache.lastUpdate && this.updateTreeCache(n.id), o());
        });
      }));
  }
  async ensureDependentModulesAreCompatibleWithCollection(e) {
    let n = ie("collection", e.id),
      o = this.modulesStore.getDependentsOfModule(n);
    !o ||
      o.size === 0 ||
      (await Promise.all(
        Array.from(o).map(async (r) => {
          let i = r,
            a = this.modulesStore.getPersistedModuleByLocalId(i);
          if (!(!a || !Ho(a))) return this.forceComponentUpdate(a.name);
        })
      ));
  }
  async ensureDependentModulesCanRenderCollectionColors(e) {
    let n = ie("collection", e.id),
      o = this.modulesStore.getDependentsOfModule(n);
    if (!o || o.size === 0) return;
    let r = this.modulesStore.forType("collection").getByStableName(e.id);
    r &&
      r.annotations(null, "default")?.framerColorSyntax &&
      (await Promise.all(
        Array.from(o).map(async (i) => {
          let [a, s] = ea(i);
          a === "draftCollection" ||
            this.modulesStore.forType(a).getByStableName(s)?.annotations(null, "default")
              ?.framerColorSyntax ||
            (await this.forceComponentUpdate(s));
        })
      ));
  }
  async ensureReferencedCollectionsExportUpToDateEnumUtils(e) {
    let n = new Set();
    for (let o of e.variables) {
      if (o.type !== "controlReference" || o.expectedType !== "enum") continue;
      let r = J(o.entityIdentifier);
      if (!vt(r) || r.type !== "collection") continue;
      let s = this.modulesStore
        .forType("collection")
        .getByStableName(r.localIdName)
        ?.annotations(null, "default")?.framerEnumToDisplayNameUtils;
      (A(s) && s >= 2) || n.add(r.localIdName);
    }
    for (let o of n)
      (Pe("ui_interaction", { page: "canvas", id: "regenerate-collection-for-enum-utils" }),
        await this.forceComponentUpdate(o));
  }
  async ensureAllNodesWithLinksHaveLinkedScopesLoaded(e) {
    let n = new Set(),
      o = this.treeStore.getDataTreeOrPartialTree();
    o.beginAllowPartialScopeAccess();
    for (let r of e.walkWithSkipChildren()) {
      if (!as(r.node)) continue;
      let i = r.node.getLink();
      if (!Wa(i)) continue;
      let a = o.get(i.webPageId);
      if (!a || a.loaded) continue;
      let s = this.modulesStore
        .forType("screen")
        .getByStableName(i.webPageId)
        ?.annotations(null, "default");
      _(s?.framerScrollSections) && (n.add(a), r.skipChildren());
    }
    (o.endAllowPartialScopeAccess(), await o.loadScopes(Array.from(n)));
  }
};
function kp(t, e, n, o) {
  let r = t.instanceIdentifier,
    i = e.dataForIdentifier(r);
  if (!i || !Object.values(i.properties).some((l) => l?.type === "image")) return;
  let s = new Set();
  for (let l of n.root.children) {
    if (!ct(l)) continue;
    if (x(l) && l.dataIdentifier === r) {
      s.add(l);
      continue;
    }
    l.loaded?.some((f) => oa(f) && f.dataIdentifier === r) && s.add(l);
  }
  s.forEach((l) => {
    o.forceComponentUpdate(l.id).catch(qi);
  });
}
function BL(t) {
  return typeof t.versionHostname == "string";
}
function FL(t) {
  return ["ssg-pipeline-failed", "ssg-user-code-failed"].includes(t);
}
function Ep(t) {
  return (
    t.status === "ssg-ready-with-warnings" ||
    t.searchIndexStatus === "failed" ||
    !!(t.status === "ssg-ready" && t.messages?.length)
  );
}
function AL(t) {
  let e = Ep(t);
  return { ...t, hasWarnings: e };
}
function le(t, e) {
  return `${t ?? ""}.${e}`;
}
function Me(t, e) {
  return `${t ?? ""}[${e}]`;
}
function Ni(t) {
  let e = [],
    n = t.split(".");
  for (let o of n) {
    let [r, i] = o.split("[");
    if ((r.length !== 0 && e.push({ type: "object", key: r }), i)) {
      let a = i.slice(0, -1);
      (b(a.length > 0), e.push({ type: "array", id: a }));
    }
  }
  return e;
}
function k(t, e = !1) {
  let n = (e ? Ga(t) : t).trim();
  return n === "" ? 0 : n.split(/\W+\b/).length;
}
function Hn(t) {
  return ae(t) || x(t) || K(t);
}
function qL(t) {
  return Hn(t) || W(t) || qe(t) || ge(t) || Je(t);
}
function qn(t) {
  return `${t}_html`;
}
function Jn(t) {
  return `${t}_fillImageLocalized`;
}
function xt(t, e, n) {
  return `${t}.${e}.${n ?? ""}`;
}
function lr(t) {
  if (Ua(t)) return Bp(t);
  let e = an(t);
  for (let n of e.querySelectorAll("*"))
    n.tagName !== "BR" &&
      (n.removeAttribute("class"),
      n.removeAttribute("style"),
      n.removeAttribute("data-styles-preset"));
  return e.innerHTML;
}
function Bp(t) {
  let e = t.variant;
  if (!Aa(t)) return t;
  let n = ms(t, {
    textBlock(o) {
      let r = { ...o };
      for (let i of Ba) delete r[i];
      return r;
    },
    text(o) {
      let r = { ...o };
      for (let i of Fa) delete r[i];
      return r;
    },
  });
  return (b(n.variant === e, "mapRichTextDocument must not change the document variant"), n);
}
var Dt = new Set();
function rN(t) {
  let e = {
    resolvedCount: 0,
    unresolvedCountsByModuleType: new Map(),
    unresolvedLocalizableCount: 0,
  };
  Dt.add(e);
  try {
    return { result: t(), lookups: e };
  } finally {
    Dt.delete(e);
  }
}
function Kn() {
  let t = {
    resolvedCount: 0,
    unresolvedCountsByModuleType: new Map(),
    unresolvedLocalizableCount: 0,
  };
  return (Dt.add(t), { lookups: t, [ta()]: () => Dt.delete(t) });
}
function td(t) {
  if (t !== 0) for (let e of Dt) e.unresolvedLocalizableCount += t;
}
function Tn(t, e, n) {
  if (Dt.size === 0) return;
  if (e) {
    for (let a of Dt) a.resolvedCount++;
    return;
  }
  let o = J(t),
    r = o ? ("type" in o ? o.type : o.kind) : "unparsed",
    i = Fp(n());
  for (let a of Dt) {
    let s = a.unresolvedCountsByModuleType;
    (s.set(r, (s.get(r) ?? 0) + 1), i && a.unresolvedLocalizableCount++);
  }
}
function Fp(t) {
  for (let e of t)
    switch (e) {
      case void 0:
        continue;
      case null:
      case "string":
      case "richtext":
      case "image":
      case "responsiveimage":
      case "link":
      case "array":
      case "object":
        return !0;
      default:
        continue;
    }
  return !1;
}
var Ri = "framer-module[data-module-identifier]",
  od = "framer-module-translation-text",
  dr = "framer-module-translation-richtext",
  rd = "data-module-control-key",
  id = "data-module-control-ancestor-path",
  ad = "data-module-control-storage-type",
  Ap = Xa(Ya),
  Gt = class extends Error {
    name = "RichTextModuleTranslationError";
  };
function TN(t) {
  return t instanceof Gt;
}
function fr(t, e) {
  let o = an(t).querySelectorAll(Ri);
  if (o.length === 0) return "";
  let r = [];
  for (let i of o) {
    let a = i.getAttribute("data-module-identifier");
    if (!L(a)) continue;
    let s = i.getAttribute("data-module-props");
    if (!L(s)) continue;
    let l;
    try {
      l = JSON.parse(s);
    } catch {
      continue;
    }
    if (!l) continue;
    let c = wi(a, e, l);
    if (!c) continue;
    let f = At(c, l),
      d = [];
    (sd(f, c, d, { suppressGeneratedIds: !0 }), d.length !== 0 && (d.sort(Up), r.push(d)));
  }
  return r.length === 0 ? "" : ee(r);
}
function Up(t, e) {
  return (
    cr(t.controlPropKey, e.controlPropKey) ||
    cr(t.ancestorControlKeyPath, e.ancestorControlKeyPath) ||
    cr(t.tagName, e.tagName) ||
    cr(t.storageType, e.storageType)
  );
}
function cr(t, e) {
  let n = t ?? "",
    o = e ?? "";
  return n < o ? -1 : n > o ? 1 : 0;
}
function LN(t, e) {
  let n = an(t),
    o = n.querySelectorAll(Ri);
  for (let r of o) {
    let i = r.getAttribute("data-module-identifier");
    if (!L(i)) continue;
    let a = r.getAttribute("data-module-props");
    if (!L(a)) continue;
    let s;
    try {
      s = JSON.parse(a);
    } catch {
      continue;
    }
    if (!s) continue;
    let l = wi(i, e, s);
    if (!l) continue;
    let c = At(l, s),
      f = [];
    sd(c, l, f);
    for (let d of f) {
      let u = n.ownerDocument.createElement(d.tagName);
      (u.setAttribute(rd, d.controlPropKey),
        d.ancestorControlKeyPath && u.setAttribute(id, d.ancestorControlKeyPath),
        d.storageType && u.setAttribute(ad, d.storageType),
        d.tagName === dr ? (u.innerHTML = d.value) : (u.textContent = d.value),
        r.append(u));
    }
  }
  return n.innerHTML;
}
function NN(t, e, { strict: n = !0 } = {}) {
  let o = an(t),
    r = o.querySelectorAll(Ri);
  for (let i of r) {
    let a = i.getAttribute("data-module-identifier");
    if (!L(a)) continue;
    let s = i.getAttribute("data-module-props");
    if (!L(s)) continue;
    let l = Array.from(i.querySelectorAll(`${od}, ${dr}`));
    if (l.length === 0) continue;
    let c;
    try {
      c = JSON.parse(s);
    } catch {
      if (!n) continue;
      throw new Gt("Translated module props are no longer valid JSON.");
    }
    if (!c || !wi(a, e, c)) continue;
    let d = c;
    for (let u of l) {
      if (u.parentElement !== i) {
        if (!n) continue;
        throw new Gt(
          "Translated module controls must remain direct children of the module element."
        );
      }
      let m = u.getAttribute(rd);
      if (!L(m) || m.length === 0) {
        if (!n) continue;
        throw new Gt("Translated module control key is missing or invalid.");
      }
      let h = u.getAttribute(id) ?? void 0;
      if (u.tagName.toLowerCase() === dr) {
        let v = $p(u.getAttribute(ad));
        if (!v) {
          if (!n) continue;
          throw new Gt("Translated rich text module control storage type is missing or invalid.");
        }
        d = nd(d, m, h, _p(u.innerHTML, v), "richtext");
      } else d = nd(d, m, h, u.textContent, "string");
      u.remove();
    }
    d !== c && i.setAttribute("data-module-props", JSON.stringify(d));
  }
  return o.innerHTML;
}
function sd(t, e, n, { suppressGeneratedIds: o = !1 } = {}) {
  for (let [r, i] of Object.entries(t)) {
    if (!i) continue;
    let a = e[r];
    a && xi(r, i, a, void 0, n, o);
  }
}
function xi(t, e, n, o, r, i) {
  switch (n.type) {
    case "array": {
      if (!is(e)) return;
      for (let a of e.value) Io(a) && xi(t, a, n.control, Op(o, a.id, i), r, i);
      return;
    }
    case "object": {
      if (e.type !== "object" || !Ne(e.value)) return;
      for (let [a, s] of Object.entries(e.value)) {
        let l = n.controls[a];
        l && xi(t, s, l, le(o, a), r, i);
      }
      return;
    }
    case "string": {
      if (n.preventLocalization || e.type !== "string" || !L(e.value) || e.value.length === 0)
        return;
      r.push({
        controlPropKey: t,
        ancestorControlKeyPath: o,
        tagName: od,
        storageType: void 0,
        value: e.value,
      });
      return;
    }
    case "richtext": {
      if (n.preventLocalization || e.type !== "richtext" || !Se(e.value)) return;
      let a = typeof e.value == "string" ? "html" : "json",
        s = typeof e.value == "string" ? e.value : Za(e.value, Ap);
      if (s.length === 0) return;
      r.push({
        controlPropKey: t,
        ancestorControlKeyPath: o,
        tagName: dr,
        storageType: a,
        value: s,
      });
      return;
    }
    default:
      return;
  }
}
function Op(t, e, n) {
  return n ? `${t ?? ""}[]` : Me(t, e);
}
function $p(t) {
  return t === "html" || t === "json" ? t : null;
}
function wi(t, e, n) {
  Tn(t, e.componentForIdentifier(t) !== null, () => Object.values(n).map((r) => r?.type));
  let o = e.reactComponentForIdentifier(t);
  return o ? Vl(Po(o, "onlyNonPresets")) : null;
}
function _p(t, e) {
  return e === "json" ? yl(t, "collectionControlPropLocalized") : t;
}
function nd(t, e, n, o, r) {
  let i = t[e];
  if (!i) {
    if (!n) return { ...t, [e]: { type: r, value: o } };
    let s = Ni(n);
    return { ...t, [e]: ur(s, r, o) };
  }
  let a = Wp(i, n, o, r);
  return a === i ? t : { ...t, [e]: a };
}
function ur(t, e, n) {
  if (t.length === 0) return { type: e, value: n };
  let [o, ...r] = t;
  b(o);
  let i = ur(r, e, n);
  return o.type === "array"
    ? { type: "array", value: [{ ...i, id: o.id }] }
    : { type: "object", value: { [o.key]: i } };
}
function Wp(t, e, n, o) {
  return e ? Di(t, Ni(e), n, o) : ld(t, n, o);
}
function Di(t, e, n, o) {
  let [r, ...i] = e;
  if (!r) return ld(t, n, o);
  if (r.type === "array") {
    if ((t.type !== "array" && t.type !== null) || !Array.isArray(t.value)) return t;
    let l = t.value,
      c = !1,
      f = !1,
      d = Cl(l, (u) => {
        if (!kt(u) || !Io(u) || u.id !== r.id) return u;
        c = !0;
        let m = Di(u, i, n, o);
        return ((f ||= m !== u), m);
      });
    if (!c && l.some((u) => kt(u) && !Io(u))) return t;
    if (!c) {
      let u = ur(i, o, n);
      return { ...t, value: [...l, { ...u, id: r.id }] };
    }
    return f ? { ...t, value: d } : t;
  }
  if ((t.type !== "object" && t.type !== null) || !Ne(t.value)) return t;
  let a = t.value[r.key];
  if (!kt(a)) {
    let l = ur(i, o, n);
    return { ...t, value: { ...t.value, [r.key]: l } };
  }
  let s = Di(a, i, n, o);
  return s === a ? t : { ...t, value: { ...t.value, [r.key]: s } };
}
function ld(t, e, n) {
  return t.type !== n && t.type !== null
    ? t
    : n === "string"
      ? typeof e == "string" && t.value !== e
        ? { ...t, type: n, value: e }
        : t
      : t.value !== e
        ? { ...t, type: n, value: e }
        : t;
}
var Ln = {};
Object.freeze(Ln);
function jt(t, e) {
  if (!t) return Ln;
  let n = t,
    o = {},
    r = !1;
  for (let i of e) {
    let a = n[i.id];
    a !== void 0 && ((o[i.id] = a), (r = !0));
  }
  return r ? o : Ln;
}
function Ht(t, e, ...n) {
  let o = {},
    r = !1;
  for (let i of e) {
    let a = t.getSubfield(...n, i.id);
    a !== void 0 && ((o[i.id] = a), (r = !0));
  }
  return r ? o : Ln;
}
function jp(t, e) {
  let n = mn(t);
  if (typeof t != "string" || !t.includes("data-module-identifier")) return n;
  let o = fr(t, e);
  return o.length === 0 ? n : R(`${n}:${o}`);
}
function be(t, e, n, o, r, i) {
  let a = {};
  for (let s of e) {
    if (r?.[s.id]) {
      a[s.id] = "warning";
      continue;
    }
    let l = i?.(s);
    if (l) {
      a[s.id] = l;
      continue;
    }
    let c = n[s.id];
    if (!c) {
      a[s.id] = _t(t) ? "done" : "new";
      continue;
    }
    a[s.id] = c.hash !== o || c.needsReview ? "needsReview" : "done";
  }
  return a;
}
function pr(t, e, n, o, r, i, a) {
  if (!(!st(o) || !po(o.outputType) || o.outputType !== "string")) {
    for (let s of o.transforms)
      if (fo(s)) {
        if (s.name === "convertFromEnum" && s.outputType === "string") {
          b(Array.isArray(s.cases), "expected enum transform cases to be an array");
          let l = mo(o) && o.startValue.id === Do;
          for (let c of l ? [] : s.cases) {
            if (Ve(c.to) || !L(c.to) || (c.to.length === 0 && !c.toLocalized)) continue;
            let f = R(c.to),
              d = jt(c.toLocalized, a);
            t.push({
              type: "convertFromEnum",
              transformId: s.id,
              keyPath: Me(le(r, "cases"), c.id),
              nodeId: e.id,
              scopeId: n,
              caseId: c.id,
              isDefaultCase: !1,
              value: c.to,
              valueWordCount: k(c.to),
              hash: f,
              localizedValues: d,
              localizedValueStatuses: be(c.to, a, d, f),
              managedByPluginButUserEditable: !1,
              controlPropKey: i,
              controlPropName: void 0,
              ancestorControlKeyPath: void 0,
            });
          }
          if (s.default && !Ve(s.default) && L(s.default)) {
            let c = R(s.default),
              f = jt(s.defaultLocalized, a);
            t.push({
              type: "convertFromEnum",
              transformId: s.id,
              keyPath: le(r, "default"),
              nodeId: e.id,
              scopeId: n,
              caseId: null,
              isDefaultCase: !0,
              value: s.default,
              valueWordCount: k(s.default),
              hash: c,
              localizedValues: f,
              localizedValueStatuses: be(s.default, a, f, c),
              managedByPluginButUserEditable: !1,
              controlPropKey: i,
              controlPropName: void 0,
              ancestorControlKeyPath: void 0,
            });
          }
        }
        if (s.name === "convertFromBoolean")
          for (let l of ["truthy", "falsy", "fallback"]) {
            let c = l === "fallback" ? s.fallback : void 0,
              f = c ? c.value : s[l],
              d = c
                ? c.valueLocalized
                : l === "truthy"
                  ? s.truthyLocalized
                  : l === "falsy"
                    ? s.falsyLocalized
                    : void 0;
            if (Ve(f) || !L(f) || (f.length === 0 && !d)) continue;
            let u = R(f),
              m = jt(d, a);
            t.push({
              type: s.name,
              transformId: s.id,
              keyPath: le(r, l),
              nodeId: e.id,
              scopeId: n,
              property: l,
              value: f,
              valueWordCount: k(f),
              hash: u,
              localizedValues: m,
              localizedValueStatuses: be(f, a, m, u),
              managedByPluginButUserEditable: !1,
              controlPropKey: i,
              controlPropName: void 0,
              ancestorControlKeyPath: void 0,
            });
          }
        if (s.name === "prefix" || s.name === "suffix") {
          if (
            Ve(s.value) ||
            (b(L(s.value), "expected prefix or suffix transform value to be a string"),
            s.value.length === 0 && !s.valueLocalized)
          )
            return;
          let l = R(s.value),
            c = jt(s.valueLocalized, a);
          t.push({
            type: s.name,
            transformId: s.id,
            keyPath: Me(r, s.id),
            nodeId: e.id,
            scopeId: n,
            value: s.value,
            valueWordCount: k(s.value),
            hash: l,
            localizedValues: c,
            localizedValueStatuses: be(s.value, a, c, l),
            managedByPluginButUserEditable: !1,
            controlPropKey: i,
            controlPropName: void 0,
            ancestorControlKeyPath: void 0,
          });
        }
      }
  }
}
function Hp(t, e, n, o, r, i, a) {
  let s = R(i);
  t.push({
    type: "FormPlainTextInputNode",
    keyPath: r,
    nodeId: e.id,
    scopeId: n,
    value: i,
    valueWordCount: k(i),
    hash: s,
    localizedValues: a,
    localizedValueStatuses: be(i, o, a, s),
    managedByPluginButUserEditable: !1,
  });
}
function qp(t, e, n, o) {
  let r = e.get("formInputPlaceholder");
  if (De(r)) {
    if (Ve(r) || !e.isKeySetAndNotInherited("formInputPlaceholder")) return;
    pr(t, e, n, r, `${e.id}_placeholder`, e.id, o);
    return;
  }
  !r ||
    !e.isKeySetAndNotInherited("formInputPlaceholder") ||
    Hp(t, e, n, o, `${e.id}_placeholder`, r, Ht(e, o, "formInputPlaceholderLocalized"));
}
function Jp(t, e, n, o, r, i) {
  let a = n.get("textContent");
  if (st(a)) {
    if (!n.isKeySetAndNotInherited("textContent")) return;
    pr(t, n, o, a, n.id, n.id, r);
    return;
  }
  if (De(n.get("htmlContent")) || De(a)) return;
  let s = n.isKeySetAndNotInherited("html"),
    l = n.isKeySetAndNotInherited("htmlLocalized");
  if (!s && !l) return;
  let c = n.get("html"),
    f = pn(c),
    d = Wo(c),
    u = i === "canvasHtmlAsTextContentIfPossible" ? d : "rich-text",
    m = lr(c),
    h = u === "rich-text" ? Ut(m) : f,
    g = n.get("originalid");
  if (u === "text" && !l && g && !n.get("replicaInfo")) {
    let w = e.getAccessor(g);
    if (w) {
      let N = w.get("html");
      if (pn(N) === f) return;
    }
  }
  let v = mn(d === u ? h : f),
    C = Ht(n, r, "htmlLocalized"),
    S = be(h, r, C, v);
  t.push({
    type: "RichTextNode",
    keyPath: qn(n.id),
    nodeId: n.id,
    scopeId: o,
    value: h,
    richTextValue: m,
    valueWordCount: k(h, !0),
    textType: u,
    hash: v,
    localizedValues: C,
    localizedValueStatuses: S,
    managedByPluginButUserEditable: !1,
    inherited: !l,
  });
}
function Kp(t, e, n, o) {
  let r = e.get("fillImage");
  if (e.get("fillType") !== "image" || !e.get("fillEnabled") || !L(r) || r.length === 0) return;
  let i = e.isKeySetAndNotInherited("fillImageLocalized"),
    a = e.isKeySetAndNotInherited("altAttributeLocalized");
  if (
    !e.isKeySetAndNotInherited("fillImage") &&
    !i &&
    !e.isKeySetAndNotInherited("altAttribute") &&
    !a
  )
    return;
  let s = e.get("altAttribute") ?? Bt,
    l = R(s),
    c = R(r),
    f = Ht(e, o, "altAttributeLocalized"),
    d = Ht(e, o, "fillImageLocalized"),
    u = e.get("fillImagePositionX"),
    m = e.get("fillImagePositionY"),
    h = on(u) ? u : void 0,
    g = on(m) ? m : void 0;
  t.push({
    type: "FrameNode",
    keyPath: Jn(e.id),
    nodeId: e.id,
    scopeId: n,
    value: s,
    valueWordCount: k(s),
    hash: l,
    localizedValues: f,
    localizedValueStatuses: be(s, o, f, l, void 0, (y) => {
      let v = d[y.id];
      return v ? (v.hash !== c || v.needsReview ? "needsReview" : "done") : null;
    }),
    managedByPluginButUserEditable: !1,
    inherited: !a,
    supportsAlt: !0,
    image: r,
    imageHash: c,
    imageLocalizedValues: d,
    imageInherited: !i,
    positionX: h,
    positionY: g,
    supportsFocalPoint: !0,
  });
}
function cd(t, e, n, o) {
  if (!O.isOn("localizedLinks")) return;
  let r = So(e.get("link"));
  if (!Et(r)) return;
  let i = r.url,
    a = R(i),
    s = Ht(e, o, "linkLocalized");
  t.push({
    type: "nodeLink",
    keyPath: `${e.id}_linkLocalized`,
    nodeId: e.id,
    scopeId: n,
    value: i,
    valueWordCount: i ? 1 : 0,
    hash: a,
    localizedValues: s,
    localizedValueStatuses: be(i, o, s, a, void 0, (l) => (s[l.id] ? null : "done")),
    managedByPluginButUserEditable: !1,
    aiTranslationDisabled: !0,
  });
}
function Qp(t, e, n, o) {
  let r = e.get("bindingType");
  if ((b(r === "variable" || r === "form", "unexpected form select binding type"), r === "form")) {
    let i = e.get("formSelectOptions");
    if (!i || i.length === 0) return;
    for (let a of i) {
      if (!a.title) continue;
      let s = jt(a.titleLocalized, o),
        l = R(a.title),
        c = be(a.title, o, s, l);
      t.push({
        type: "FormSelectNode",
        keyPath: `${e.id}_${a.id}`,
        nodeId: e.id,
        optionId: a.id,
        scopeId: n.id,
        value: a.title,
        valueWordCount: k(a.title),
        hash: l,
        localizedValues: s,
        localizedValueStatuses: c,
        managedByPluginButUserEditable: !1,
      });
    }
  } else if (r === "variable") {
    let i = e.getSubfield("variableBinding", "id");
    if (!i || !ct(n) || !dn(n)) return;
    let a = n.getVariable(i);
    if (!a) return;
    let s = (l) => {
      let f = e.get(l) || ns[l],
        d = Ht(e, o, `${l}Localized`),
        u = R(f);
      t.push({
        type: "formSelectGenericOption",
        keyPath: `${e.id}_${l}`,
        nodeId: e.id,
        key: l,
        scopeId: n.id,
        value: f,
        valueWordCount: k(f),
        hash: u,
        localizedValues: d,
        localizedValueStatuses: be(f, o, d, u),
        managedByPluginButUserEditable: !1,
      });
    };
    (bt(a, hn(n)) && s("allItemsLabel"),
      a.type === "boolean" && (s("booleanTrueLabel"), s("booleanFalseLabel")));
    return;
  }
}
function dd(t) {
  return Ne(t) && "type" in t;
}
function Vi(t, e, n, o, r, i, a, s, l) {
  if (i.type === "array" && Array.isArray(i.value)) {
    if (a.type !== "array") return;
    for (let u of i.value)
      !dd(u) || !Ne(u) || !L(u.id) || Vi(t, e, n, o, r, u, a.control, s, Me(l, u.id));
    return;
  }
  if (i.type === "object" && Ne(i.value)) {
    if (a.type !== "object") return;
    for (let [u, m] of Object.entries(i.value)) {
      if (!dd(m)) continue;
      let h = a.controls[u];
      h && Vi(t, e, n, o, r, m, h, s, le(l, u));
    }
    return;
  }
  let c = xt(e.id, r, l),
    f = jt("valueLocalized" in i ? i.valueLocalized : void 0, o),
    d = {
      keyPath: c,
      controlPropKey: r,
      controlPropName: a.title || Mt(r),
      ancestorControlKeyPath: l,
      nodeId: e.id,
      scopeId: n,
      localizedValues: f,
      managedByPluginButUserEditable: !1,
    };
  switch (i.type) {
    case "string": {
      if (
        (a.type === "string" && (a.preventLocalization || (a.optional && i.value === null))) ||
        (st(i.value) && pr(t, e, n, i.value, le(e.id, r), r, o), !L(i.value))
      )
        return;
      let u = R(i.value);
      t.push({
        ...d,
        type: "string",
        value: i.value,
        valueWordCount: k(i.value),
        hash: u,
        localizedValueStatuses: be(i.value, o, f, u),
      });
      return;
    }
    case "link": {
      if (
        !O.isOn("localizedLinks") ||
        (st(i.value) && pr(t, e, n, i.value, le(e.id, r), r, o), !Et(i.value))
      )
        return;
      let u = i.value,
        m = R(u.url);
      t.push({
        ...d,
        type: "link",
        value: u.url,
        valueWordCount: u.url ? 1 : 0,
        hash: m,
        localizedValueStatuses: be(u.url, o, f, m, void 0, (h) => (f[h.id] ? null : "done")),
        aiTranslationDisabled: !0,
      });
      return;
    }
    case "richtext": {
      if ((a.type === "richtext" && a.preventLocalization) || !Se(i.value)) return;
      let u = Ut(i.value),
        m = jp(i.value, s);
      t.push({
        ...d,
        type: "richtext",
        richTextValue: i.value,
        value: u,
        valueWordCount: k(u, !0),
        hash: m,
        localizedValueStatuses: be(u, o, f, m),
        contentType: void 0,
        isCollectionControlProp: !1,
      });
      return;
    }
    case "image": {
      if ((a.type !== "image" && a.type !== "responsiveimage") || !L(i.value)) return;
      let u = a.type === "responsiveimage",
        m = i.alt ?? Bt,
        h = R(m),
        g = R(i.value),
        y = u ? jt(i.altLocalized, o) : Ln,
        v = f;
      t.push({
        ...d,
        type: "image",
        value: m,
        valueWordCount: k(m),
        hash: h,
        localizedValues: y,
        localizedValueStatuses: be(m, o, y, h, void 0, (C) => {
          let S = v[C.id];
          return S ? (S.hash !== g || S.needsReview ? "needsReview" : "done") : null;
        }),
        supportsAlt: u,
        image: i.value,
        imageHash: g,
        imageLocalizedValues: v,
        supportsFocalPoint: u,
        positionX: u ? i.positionX : void 0,
        positionY: u ? i.positionY : void 0,
      });
      return;
    }
  }
}
function Xp(t, e, n, o, r, i, a) {
  let s = n.__class === "WebPageNode" ? Yp(t, n) : n.get("codeComponentIdentifier");
  if (!s) return;
  let l = i.componentForIdentifier(s);
  if ((Tn(s, l !== null, () => [...n.keys()].filter(Vn).map((u) => n.get(u)?.type)), !l)) return;
  let c = {};
  for (let u of n.keys()) Vn(u) && (c[Ea(u)] = n.get(u));
  let f = l.properties,
    d = At(f, c);
  for (let [u, m] of Object.entries(d)) {
    if (!m || !n.isKeySetAndNotInherited(`$control__${u}`)) continue;
    a?.addControlProp(m);
    let h = f[u];
    h && Vi(e, n, o, r, u, m, h, i);
  }
}
function Yp(t, e) {
  let n = e.get("layoutTemplateIdentifierOverride");
  if (n !== null) return n ?? t.root.layoutTemplateIdentifier;
}
function Zp(t, e, n, o, r, i) {
  let a = o.get("webMetadata"),
    s = o.get("dataIdentifier"),
    l = e.dataForIdentifier(s);
  for (let c of ["title", "description"]) {
    let f = Ht(o, r, `${c}Localized`);
    if (!a?.[c] && f === Ln) continue;
    let d = a?.[c] ?? t.root.webMetadata?.[c] ?? "",
      u = R(d),
      m = {};
    for (let h of r) {
      let g = f[h.id];
      if (!g || !L(g.value) || !g.value) continue;
      let y = Mo({ collectionData: l, componentLoader: e, type: "id", value: g.value });
      y && (m[h.id] = y);
    }
    i.push({
      type: "WebPageNode",
      keyPath: `${o.id}_${c}`,
      nodeId: o.id,
      scopeId: n,
      value: d,
      valueWordCount: k(d),
      hash: u,
      localizedValues: f,
      localizedValueStatuses: be(d, r, f, u, m),
      managedByPluginButUserEditable: !1,
      key: c,
      dataIdentifier: s,
      warnings: m,
    });
  }
}
function zi(t, e, n, o, r, i, a, s, l) {
  if ((s?.(), o.__class === "CodeComponentNode")) {
    let c = o.get("codeComponentIdentifier");
    l?.addIdentifier(c);
  }
  if (o.get("customCursorType") === "component") {
    let c = o.get("customCursorSmartComponentId");
    l?.addSmartComponentId(c);
  }
  (o.__class === "WebPageNode" && Zp(t, e, n.id, o, r, i),
    o.__class === "RichTextNode" && Jp(i, t, o, n.id, r, a),
    o.__class === "FrameNode" && (Kp(i, o, n.id, r), cd(i, o, n.id, r)),
    (o.__class === "SVGNode" || o.__class === "ShapeContainerNode") && cd(i, o, n.id, r),
    o.__class === "FormPlainTextInputNode" && qp(i, o, n.id, r),
    o.__class === "FormSelectNode" && Qp(i, o, n, r),
    (o.__class === "CodeComponentNode" || o.__class === "WebPageNode") &&
      Xp(t, i, o, n.id, r, e, l));
}
function Pi(t, e) {
  let n = qa(e).getRouteSegmentIdToRawPathMap(e).get(t.id);
  return n ? Ot(n) : "Unknown";
}
function em(t, e, n) {
  let o = e.root.homePageNodeId,
    r = o && (t.id === o || t.abTestingParentId === o),
    i;
  if (r) i = "Home";
  else {
    let a = Xe(e, t);
    if (!a) return "Unknown";
    let s = t.dataIdentifier;
    if (!s) i = Ot(a);
    else {
      let l = n.dataForIdentifier(s);
      i = l ? Ot($s(l, a)) : Ot(a);
    }
  }
  if (t.abTestingParentId) {
    let a = t.resolveValue("name") || $e(n, t);
    return `${i} \xB7 ${a}`;
  }
  return t.hasAbTestingChildren(e) ? `${i} \xB7 Control` : i;
}
function tm(t, e) {
  let n = e.getParent(t.id);
  return W(n) ? ($o(n, t) ?? "Unknown") : "Unknown";
}
function Sx(t, e, n) {
  if (ge(t)) return "Site";
  if (Je(t)) return Pi(t, e);
  if (W(t)) return t.resolveValue("name") || "Unknown";
  if (qe(t)) return tm(t, e) || "Unknown";
  if (x(t)) return em(t, e, n);
  if (ae(t)) return t.resolveValue("name") || "Unknown";
  if (K(t)) return t.resolveValue("name") || "Unknown";
  Z(t, "Invalid localization scope");
}
function qt(t, e) {
  return t === void 0 || !je(t, e) ? e : t;
}
function Mi(t, e) {
  return !t || t.includes(e);
}
function nm(t, e) {
  let n = new Set();
  for (let o of e.values()) Mi(o.includedLocaleIds, t) && n.add(o.collectionNodeId);
  return n;
}
function om(t, e, n, o, r) {
  if (t.abTestingParentId) return r(t.abTestingParentId);
  if (!t.collectionIdentifier) return Mi(t.includedLocaleIds, e);
  let i = n.get(t.collectionIdentifier);
  return i ? o.has(i) : !0;
}
function rm(t, e, n, o) {
  let r = new Map(),
    i = new Set(),
    a = (l) => {
      let c = r.get(l);
      if (c !== void 0) return c;
      if (i.has(l)) return !0;
      let f = e.get(l);
      if (!f) return !0;
      i.add(l);
      let d = om(f, t, n, o, a);
      return (i.delete(l), r.set(l, d), d);
    },
    s = new Set();
  for (let l of e.keys()) a(l) && s.add(l);
  return s;
}
function im(t, e, n) {
  return t ? (e.has(t) ? n.has(t) : !0) : !1;
}
function am(t, e) {
  for (let n of t) if (e(n)) return !0;
  return !1;
}
function sm(t, e, n) {
  let o = new Map();
  for (let [l, c] of t) {
    if (!c.parentRouteId) continue;
    let f = o.get(c.parentRouteId) ?? [];
    (f.push(l), o.set(c.parentRouteId, f));
  }
  let r = new Set(),
    i = new Map(),
    a = new Set(),
    s = (l) => {
      let c = i.get(l);
      if (c !== void 0) return c;
      if (a.has(l)) return !0;
      let f = t.get(l);
      if (!f) return !0;
      a.add(l);
      let d = im(f.webPageId, e, n),
        u = o.get(l) ?? [],
        m = am(u, s);
      a.delete(l);
      let h = d || m;
      return (i.set(l, h), h && r.add(l), h);
    };
  for (let l of t.keys()) s(l);
  return r;
}
function lm(t, e, n, o) {
  let r = o.get(t)?.get(e);
  return r || (n.get(t) ?? []);
}
function cm(t, e, n, o, r) {
  let i = new Set(),
    a = new Set(),
    s = Array.from(e);
  for (let l of r.values())
    if (Mi(l.includedLocaleIds, t))
      for (let c of l.componentIdsByLocaleId.get(t) ?? []) (a.add(c), s.push(c));
  for (; s.length > 0;) {
    let l = s.pop();
    if (!l || i.has(l)) continue;
    i.add(l);
    let c = lm(l, t, n, o);
    for (let f of c) (a.add(f), !i.has(f) && s.push(f));
  }
  return a;
}
function dm(t, e, n, o) {
  if (!o || !(o.hasTitleSource || o.hasDescriptionSource)) return !0;
  for (let i of t) {
    let a = e.get(i);
    if (
      !a ||
      (o.hasTitleSource && !a.titleOverrideLocaleIds.has(n)) ||
      (o.hasDescriptionSource && !a.descriptionOverrideLocaleIds.has(n))
    )
      return !0;
  }
  return !1;
}
function mr(t, e, n, o) {
  let r = qt(n, o);
  t.set(e, r);
}
function ud({
  localeIds: t,
  dependenciesByScopeId: e,
  localizedDependenciesByScopeId: n,
  pageFactsByPageId: o,
  collectionIdentifierById: r,
  itemFactsByItemId: i,
  routeFactsByRouteId: a,
  siteSettingsFact: s,
  previousResult: l,
}) {
  let c = new Map();
  for (let [N, D] of r) c.set(D, N);
  let f = new Map(),
    d = new Map(),
    u = new Map(),
    m = new Map(),
    h = new Map();
  for (let N of t) {
    let D = nm(N, i),
      V = rm(N, o, c, D),
      z = sm(a, o, V),
      P = cm(N, V, e, n, i);
    (mr(d, N, l?.visibleCollectionIdsByLocaleId.get(N), D),
      mr(f, N, l?.visiblePageIdsByLocaleId.get(N), V),
      mr(u, N, l?.visibleRouteIdsByLocaleId.get(N), z),
      mr(m, N, l?.reachableDependencyIdsByLocaleId.get(N), P));
    let M = dm(V, o, N, s);
    h.set(N, M);
  }
  let g = qt(l?.localeIds, t),
    y = qt(l?.visiblePageIdsByLocaleId, f),
    v = qt(l?.visibleCollectionIdsByLocaleId, d),
    C = qt(l?.visibleRouteIdsByLocaleId, u),
    S = qt(l?.reachableDependencyIdsByLocaleId, m),
    w = qt(l?.siteSettingsVisibleByLocaleId, h);
  return {
    localeIds: g,
    visiblePageIdsByLocaleId: y,
    visibleCollectionIdsByLocaleId: v,
    visibleRouteIdsByLocaleId: C,
    reachableDependencyIdsByLocaleId: S,
    siteSettingsVisibleByLocaleId: w,
  };
}
function um(t, e) {
  let n = t.get(e);
  if (n) return n;
  let o = new Set();
  return (t.set(e, o), o);
}
function md(t, e) {
  let n = t.get(e);
  if (n) return n;
  let o = new Map();
  return (t.set(e, o), o);
}
var ki = class {
  constructor(e, n) {
    this.context = e;
    this.checkpoint = n;
  }
  context;
  checkpoint;
  directSmartComponentIds = new Set();
  unlocalizedSmartComponentIds = new Set();
  richTextComponentIdsByLocaleId = new Map();
  componentIdsByRichTextValueByVariant = new Map();
  addIdentifier(e) {
    if (!e) return;
    let n = Sd(e);
    n && (this.directSmartComponentIds.add(n), this.unlocalizedSmartComponentIds.add(n));
  }
  addSmartComponentId(e) {
    L(e) && (this.directSmartComponentIds.add(e), this.unlocalizedSmartComponentIds.add(e));
  }
  addControlProp(e) {
    switch (e.type) {
      case "array":
        if (!so(e.value)) return;
        for (let n of e.value) this.addControlProp(n);
        return;
      case "object":
        if (!Ne(e.value)) return;
        for (let n of Object.values(e.value)) this.addControlProp(n);
        return;
      case "customcursor":
        if (!os(e.value)) return;
        this.addSmartComponentId(e.value.customCursorSmartComponentId);
        return;
      case "richtext":
        this.addRichTextControlProp(e);
    }
  }
  addRichTextControlProp(e) {
    for (let [n, o] of this.context.routerLocaleById) {
      let i = sn(o, e.valueLocalized)?.value,
        a = Se(i),
        s = a ? i : e.value,
        l = a ? "collectionControlPropLocalized" : "collectionControlProp",
        c = this.getRichTextComponentIds(s, l);
      if (!c) continue;
      let f = this.getComponentIdsForLocale(n);
      for (let d of c) (f.add(d), this.directSmartComponentIds.add(d));
    }
  }
  getComponentIdsForLocale(e) {
    let n = this.richTextComponentIdsByLocaleId.get(e);
    if (n) return n;
    let o = new Set();
    return (this.richTextComponentIdsByLocaleId.set(e, o), o);
  }
  getRichTextComponentIds(e, n) {
    if (!Se(e)) return;
    let o = md(this.componentIdsByRichTextValueByVariant, n),
      r = o.get(e);
    if (r) return r;
    this.checkpoint?.();
    let i = Cd(e, n);
    return (o.set(e, i), i);
  }
};
function Ox() {
  return {
    facts: {
      dependenciesByScopeId: new Map(),
      localizedDependenciesByScopeId: new Map(),
      cmsDependenciesByItemId: new Map(),
      pageFactsByPageId: new Map(),
      collectionIdentifierById: new Map(),
      routeFactsByRouteId: new Map(),
      siteSettingsFact: void 0,
    },
    allScopesCached: !0,
  };
}
function hd(t, e) {
  let n = Ue(t, "includeDrafts"),
    o = new Map();
  for (let r of n) o.set(r.id, r);
  return { collection: e, routerLocales: n, routerLocaleById: o };
}
function Ei(t, e) {
  if (t) return new ki(t, e);
}
function Bi(t) {
  if (t) return Array.from(t.directSmartComponentIds);
}
function Fi(t) {
  if (!t) return;
  let e = new Map();
  for (let n of t.context.routerLocaleById.keys()) {
    let o = new Set(t.unlocalizedSmartComponentIds),
      r = t.richTextComponentIdsByLocaleId.get(n);
    for (let i of r ?? []) o.add(i);
    e.set(n, Array.from(o));
  }
  return e;
}
function fd(t, e, n) {
  if (!x(e) || !vo(e)) return n;
  let o = qr(e.layoutTemplateIdentifier);
  if (!o) return n;
  let r = t.get(o);
  return !K(r) || n.includes(o) ? n : [...n, o];
}
function Qn(t, e, n, o, r, i = e, a) {
  let s = r?.collection;
  if (!s || !n) return;
  let l = s.facts,
    c = fd(t, e, n);
  if ((l.dependenciesByScopeId.set(e.id, c), o)) {
    let d = new Map();
    for (let [u, m] of o) d.set(u, fd(t, e, m));
    l.localizedDependenciesByScopeId.set(e.id, d);
  }
  if (!x(e)) return;
  let f = fm(t, e, i, r, a);
  return (l.pageFactsByPageId.set(e.id, f), f);
}
function fm(t, e, n, o, r) {
  if (r) return r;
  let i = un(t, n);
  return pm(e, i, o);
}
function pd(t, e, n) {
  let o = new Set();
  for (let [r, i] of n.routerLocaleById) {
    let s = sn(i, e)?.value != null;
    (!t && !s) || o.add(r);
  }
  return o;
}
function pm(t, e, n) {
  return {
    includedLocaleIds: e,
    abTestingParentId: t.abTestingParentId,
    collectionIdentifier: t.dataIdentifier,
    titleOverrideLocaleIds: pd(t.webMetadata?.title !== void 0, t.titleLocalized, n),
    descriptionOverrideLocaleIds: pd(
      t.webMetadata?.description !== void 0,
      t.descriptionLocalized,
      n
    ),
  };
}
function gd(t, e) {
  e && e.facts.collectionIdentifierById.set(t.id, t.instanceIdentifier);
}
function yd(t, e, n) {
  if (!n) return;
  let o = { parentRouteId: e, webPageId: t.webPageId };
  n.facts.routeFactsByRouteId.set(t.id, o);
}
function vd(t, e) {
  e &&
    (e.facts.siteSettingsFact = {
      hasTitleSource: !!(t.webMetadata?.title || t.titleLocalized),
      hasDescriptionSource: !!(t.webMetadata?.description || t.descriptionLocalized),
    });
}
function We(t, e) {
  return t === void 0 || !je(t, e) ? e : t;
}
function Sd(t) {
  return qr(t, "canvasComponent") ?? void 0;
}
function Cd(t, e) {
  let n = Sl(t, e),
    o = new Set(n.customCursorSmartComponentIds);
  for (let r of n.moduleIdentifiers) {
    let i = Sd(r);
    i && o.add(i);
  }
  return o;
}
function mm(t, e) {
  if (t?.type === "richtext" && Se(t.value)) return t.value;
  if (Se(e.initialValue)) return e.initialValue;
}
function hm(t, e, n, o, r) {
  function i(s, l) {
    if (l.type !== "richtext") return;
    let c = mm(s, l);
    if (c)
      for (let f of n) {
        let d = o.get(f.id),
          u = s?.type === "richtext" ? s.valueLocalized : void 0,
          h = sn(d, u)?.value,
          g = Se(h),
          y = g ? h : c,
          v = g ? "collectionControlPropLocalized" : "collectionControlProp";
        r(f.id, y, v);
      }
  }
  function a(s, l) {
    if (l.type === "array") {
      if (s?.type !== "array" || De(s.value)) return;
      for (let c of s.value)
        if (bo(c) && c.value) for (let f of l.itemVariables) Te(f) && a(c.value[f.id], f);
      return;
    }
    i(s, l);
  }
  for (let s of t.variables) Te(s) && a(e.getControlProp(s.id), s);
}
function gm(t, e, n, o) {
  let r = new Map(),
    i = new Map();
  hm(t, e, n, o.routerLocaleById, (s, l, c) => {
    let f = um(i, s),
      d = md(r, c),
      u = d.get(l),
      m = o.previousDependencies?.componentIdsByRichTextValueByVariant.get(c)?.get(l),
      h = u ?? m;
    h || o.checkpoint?.();
    let g = h ?? Cd(l, c);
    d.set(l, g);
    for (let y of g) f.add(y);
  });
  let a = new Map();
  for (let s of n) a.set(s.id, i.get(s.id) ?? new Set());
  return {
    componentIdsByRichTextValueByVariant: We(
      o.previousDependencies?.componentIdsByRichTextValueByVariant,
      r
    ),
    componentIdsByLocaleId: We(o.previousDependencies?.componentIdsByLocaleId, a),
  };
}
function Id(t, e, n, o, r) {
  let i = gm(t, e, n, r),
    a = r.previousDependencies;
  if (a) {
    let s = a.collectionNodeId === t.id,
      l = je(a.includedLocaleIds, o),
      c = a.componentIdsByRichTextValueByVariant === i.componentIdsByRichTextValueByVariant,
      f = a.componentIdsByLocaleId === i.componentIdsByLocaleId;
    if (s && l && c && f) return a;
  }
  return { includedLocaleIds: o, collectionNodeId: t.id, ...i };
}
function $x({ locales: t, facts: e, previousUsage: n }) {
  let o = We(
      n?.localeIds,
      t.map((m) => m.id)
    ),
    r = We(n?.dependenciesByScopeId, e.dependenciesByScopeId),
    i = We(n?.localizedDependenciesByScopeId, e.localizedDependenciesByScopeId),
    a = We(n?.cmsDependenciesByItemId, e.cmsDependenciesByItemId),
    s = We(n?.pageFactsByPageId, e.pageFactsByPageId),
    l = We(n?.collectionIdentifierById, e.collectionIdentifierById),
    c = We(n?.routeFactsByRouteId, e.routeFactsByRouteId),
    f = We(n?.siteSettingsFact, e.siteSettingsFact);
  if (
    n?.localeIds === o &&
    n.dependenciesByScopeId === r &&
    n.localizedDependenciesByScopeId === i &&
    n.cmsDependenciesByItemId === a &&
    n.pageFactsByPageId === s &&
    n.collectionIdentifierById === l &&
    n.routeFactsByRouteId === c &&
    n.siteSettingsFact === f
  )
    return n;
  let u = ud({
    localeIds: o,
    dependenciesByScopeId: r,
    localizedDependenciesByScopeId: i,
    pageFactsByPageId: s,
    collectionIdentifierById: l,
    itemFactsByItemId: a,
    routeFactsByRouteId: c,
    siteSettingsFact: f,
    previousResult: n?.visibility,
  });
  return {
    localeIds: o,
    dependenciesByScopeId: r,
    localizedDependenciesByScopeId: i,
    cmsDependenciesByItemId: a,
    pageFactsByPageId: s,
    collectionIdentifierById: l,
    routeFactsByRouteId: c,
    siteSettingsFact: f,
    visibility: u,
    reachableDependencyIdsByLocaleId: u.reachableDependencyIdsByLocaleId,
  };
}
function bd(t) {
  return t ? (t.abTestingParentId ? !0 : t.collectionIdentifier !== void 0) : !1;
}
function ym(t, e, n) {
  let o = n.visibility;
  switch (t.groupType) {
    case "component":
    case "template":
      return !o.reachableDependencyIdsByLocaleId.get(e)?.has(t.nodeId);
    case "collection":
      return !o.visibleCollectionIdsByLocaleId.get(e)?.has(t.nodeId);
    case "settings":
      return o.siteSettingsVisibleByLocaleId.get(e) === !1;
    case "page": {
      let r = n.pageFactsByPageId.get(t.nodeId);
      return bd(r)
        ? !o.visiblePageIdsByLocaleId.get(e)?.has(t.nodeId)
        : n.routeFactsByRouteId.has(t.nodeId)
          ? !o.visibleRouteIdsByLocaleId.get(e)?.has(t.nodeId)
          : !1;
    }
    case "collectionItem":
      return !1;
  }
}
function _x(t, e, n) {
  return t.map((o) => {
    let r = e.localeIds.filter((u) => ym(o, u, e)),
      i = n?.get(o.nodeId)?.derivedExcludedLocaleIds,
      a = We(i, r),
      s = a.length > 0 ? a : void 0,
      l = e.pageFactsByPageId.get(o.nodeId),
      c = o.groupType === "page" && bd(l),
      f = c ? !1 : o.supportsLocaleVisibility,
      d = c ? void 0 : o.includedLocaleIds;
    return o.derivedExcludedLocaleIds === s &&
      o.supportsLocaleVisibility === f &&
      o.includedLocaleIds === d
      ? o
      : { ...o, supportsLocaleVisibility: f, includedLocaleIds: d, derivedExcludedLocaleIds: s };
  });
}
function hr() {
  return O.isOn("fastLocalization") && el();
}
var fR = ["new", "needsReview", "done"],
  vm = "controlPropKey";
function pR(t) {
  return vm in t;
}
function mR(t) {
  return "groups" in t;
}
function Sm(t) {
  return t.cmsLocalizationDependencies !== void 0;
}
function Td(t, e) {
  return !(
    !t.directSmartComponentIds ||
    !t.localizedSmartComponentIdsByLocaleId ||
    (x(e) && !t.pageVisibilityFact)
  );
}
function Cm(t, e, n) {
  return n ? (qe(e) ? Sm(t) : Hn(e) || Je(e) ? Td(t, e) : W(e) || ge(e) ? !0 : Z(e)) : !0;
}
var Xn = Symbol("ignoreWebPageCollection"),
  Im = "collectionNodeId";
function hR(t) {
  return Im in t;
}
var bm = { string: !0, richtext: !0, image: !0, enum: !0, link: !0, slug: !0 };
function Tm(t) {
  return t in bm;
}
function xd(t) {
  return Te(t) && Tm(t.type);
}
var Lm = { string: !0, richtext: !0, image: !0, link: !0 };
function Dd(t) {
  return t in Lm;
}
function gR(t) {
  return t
    ? t.type === "richtext"
      ? !0
      : t.type === "RichTextNode"
        ? t.textType === "rich-text"
        : !1
    : !1;
}
function yR(t) {
  return t?.type === "richtext";
}
function vR(t) {
  return t.type === "richtext" || t.type === "RichTextNode"
    ? typeof t.richTextValue == "string"
    : !1;
}
function SR(t) {
  return t ? t.type === "image" || t.type === "FrameNode" : !1;
}
function CR(t) {
  return t ? t.type === "nodeLink" || t.type === "link" : !1;
}
function IR(t) {
  return t ? t.type === "RootNode" || t.type === "WebPageNode" : !1;
}
function bR(t) {
  return t?.type === "slug" || Nm(t);
}
function Nm(t) {
  return t?.type === "RouteSegmentNode";
}
function xm(t) {
  return lo(t) || t === "" || _t(t);
}
function TR(t, e) {
  if (!t) return !1;
  if (
    t.value.includes(`
`)
  )
    return !0;
  let n = t.localizedValues[e.id];
  return Co(n)
    ? !!zr(n, { preserveNewLines: !0 }).includes(`
`)
    : !1;
}
function Rd(t, e, n) {
  return e ? (e.hash !== n || e.needsReview ? "needsReview" : "done") : xm(t) ? "done" : "new";
}
function Y(t, e, n, o, r, i) {
  return Object.fromEntries(
    e.map((a) => {
      let s = n[a.id];
      if (r?.[a.id]) return [a.id, "warning"];
      if (i) {
        let l = i(a);
        if (l) return [a.id, l];
      }
      return [a.id, Rd(t, s, o)];
    })
  );
}
function $i(t) {
  return (e) => (t[e.id] ? null : "done");
}
function _i(t, e, n) {
  return (o) => {
    if (L(t) && e) {
      let r = e[o.id];
      if (r) return Rd(t, r, n);
    }
    return null;
  };
}
var G = {};
Object.freeze(G);
function Dm(t, e, n, o, r, i, a) {
  let s = (l, c, f) => {
    if (Ve(c) || !L(c) || (c.length === 0 && !f)) return;
    let d = R(c),
      u = f ?? G,
      m = Y(c, i, u, d);
    a.push({
      type: t.name,
      transformId: t.id,
      keyPath: le(e, l),
      nodeId: r.id,
      scopeId: o.id,
      property: l,
      value: c,
      controlPropKey: n,
      controlPropName: void 0,
      ancestorControlKeyPath: void 0,
      valueWordCount: k(c),
      hash: d,
      localizedValues: u,
      localizedValueStatuses: m,
      managedByPluginButUserEditable: !1,
    });
  };
  (s("truthy", t.truthy, t.truthyLocalized),
    s("falsy", t.falsy, t.falsyLocalized),
    t.fallback && s("fallback", t.fallback.value, t.fallback.valueLocalized));
}
function Rm(t, e, n, o, r, i, a, s) {
  if (e.outputType !== "string") return;
  for (let d of e.cases) {
    if (mo(t) && t.startValue.id === Do) break;
    if (Ve(d.to) || !L(d.to) || (d.to.length === 0 && !d.toLocalized)) continue;
    let u = R(d.to),
      m = d.toLocalized ?? G,
      h = Y(d.to, a, m, u),
      g = le(n, "cases");
    s.push({
      type: "convertFromEnum",
      transformId: e.id,
      keyPath: Me(g, d.id),
      nodeId: i.id,
      scopeId: r.id,
      caseId: d.id,
      isDefaultCase: !1,
      value: d.to,
      ancestorControlKeyPath: void 0,
      controlPropKey: o,
      controlPropName: void 0,
      valueWordCount: k(d.to),
      hash: u,
      localizedValues: m,
      localizedValueStatuses: h,
      managedByPluginButUserEditable: !1,
    });
  }
  if (!e.default || Ve(e.default) || !L(e.default)) return;
  let l = R(e.default),
    c = e.defaultLocalized ?? G,
    f = Y(e.default, a, c, l);
  s.push({
    type: "convertFromEnum",
    transformId: e.id,
    keyPath: le(n, "default"),
    nodeId: i.id,
    scopeId: r.id,
    caseId: null,
    isDefaultCase: !0,
    ancestorControlKeyPath: void 0,
    value: e.default,
    controlPropKey: o,
    controlPropName: void 0,
    valueWordCount: k(e.default),
    hash: l,
    localizedValues: c,
    localizedValueStatuses: f,
    managedByPluginButUserEditable: !1,
  });
}
function gr(t, e, n, o, r, i, a) {
  if (!(!po(n.outputType) || n.outputType !== "string")) {
    for (let s of n.transforms)
      if (fo(s))
        switch (s.name) {
          case "convertFromEnum": {
            Rm(n, s, o, r, t, e, i, a);
            break;
          }
          case "convertFromBoolean": {
            Dm(s, o, r, t, e, i, a);
            break;
          }
          case "prefix":
          case "suffix": {
            if (Ve(s.value) || (s.value.length === 0 && !s.valueLocalized)) return;
            let l = R(s.value),
              c = s.valueLocalized ?? G,
              f = Y(s.value, i, c, l);
            a.push({
              type: s.name,
              transformId: s.id,
              keyPath: Me(o, s.id),
              controlPropKey: r,
              controlPropName: void 0,
              ancestorControlKeyPath: void 0,
              nodeId: e.id,
              scopeId: t.id,
              value: s.value,
              valueWordCount: k(s.value),
              hash: l,
              localizedValues: c,
              localizedValueStatuses: f,
              managedByPluginButUserEditable: !1,
            });
            continue;
          }
          default:
        }
  }
}
function wm(t, e, n, o, r, i) {
  if (st(n.textContent)) {
    if (!Ie(t, n, "textContent")) return;
    gr(e, n, n.textContent, n.id, n.id, o, r);
    return;
  }
  if (De(n.htmlContent) || De(n.textContent)) return;
  let a = Ie(t, n, "html"),
    s = Ie(t, n, "htmlLocalized");
  if (!a && !s) return;
  let l = n.html,
    c = pn(l),
    f = Wo(l),
    d = i === "canvasHtmlAsTextContentIfPossible" ? f : "rich-text",
    u = lr(l),
    m = d === "rich-text" ? Ut(u) : c;
  if (d === "text" && !s && va(n)) {
    let C = t.getNode(n.originalid);
    if (C && pn(C.html) === c) return;
  }
  let g = mn(f === d ? m : c),
    y = n.htmlLocalized ?? G,
    v = Y(m, o, y, g);
  r.push({
    type: "RichTextNode",
    keyPath: qn(n.id),
    nodeId: n.id,
    scopeId: e.id,
    value: m,
    richTextValue: u,
    valueWordCount: k(m, !0),
    textType: d,
    hash: g,
    localizedValues: y,
    localizedValueStatuses: v,
    managedByPluginButUserEditable: !1,
    inherited: !s,
  });
}
function Ld(t) {
  if (ge(t)) return "RootNode";
  if (x(t)) return "WebPageNode";
  Z(t);
}
function Nd(t, e, n, o) {
  let r = {};
  for (let i of e) {
    let a = n[i.id];
    if (!a || !L(a.value) || !a.value) continue;
    let s = Mo({ collectionData: o, componentLoader: t, type: "id", value: a.value });
    s && (r[i.id] = s);
  }
  return r;
}
function wd(t, e, n, o, r, i) {
  let a = null;
  if (
    (x(o) && (a = e.dataForIdentifier(o.dataIdentifier)), o.webMetadata?.title || o.titleLocalized)
  ) {
    let s = o.webMetadata?.title ?? t.root.webMetadata?.title ?? "",
      l = R(s),
      c = o.titleLocalized ?? G,
      f = Nd(e, r, c, a),
      d = Y(s, r, c, l, f);
    i.push({
      type: Ld(o),
      keyPath: `${o.id}_title`,
      nodeId: o.id,
      scopeId: n.id,
      value: s,
      valueWordCount: k(s),
      hash: l,
      localizedValues: c,
      localizedValueStatuses: d,
      managedByPluginButUserEditable: !1,
      key: "title",
      dataIdentifier: Mr(o) ? o.dataIdentifier : void 0,
      warnings: f,
    });
  }
  if (o.webMetadata?.description || o.descriptionLocalized) {
    let s = o.webMetadata?.description ?? t.root.webMetadata?.description ?? "",
      l = R(s),
      c = o.descriptionLocalized ?? G,
      f = Nd(e, r, c, a),
      d = Y(s, r, c, l, f);
    i.push({
      type: Ld(o),
      keyPath: `${o.id}_description`,
      nodeId: o.id,
      scopeId: n.id,
      value: s,
      valueWordCount: k(s),
      hash: l,
      localizedValues: c,
      localizedValueStatuses: d,
      managedByPluginButUserEditable: !1,
      key: "description",
      dataIdentifier: Mr(o) ? o.dataIdentifier : void 0,
      warnings: f,
    });
  }
}
function Vm(t, e, n, o, r) {
  if (!Va(n) || !L(n.fillImage) || n.fillImage.length === 0) return;
  let i = Ie(t, n, "fillImage"),
    a = Ie(t, n, "fillImageLocalized"),
    s = Ie(t, n, "altAttribute"),
    l = Ie(t, n, "altAttributeLocalized");
  if (!i && !a && !s && !l) return;
  let c = n.altAttribute ?? Bt,
    f = R(c),
    d = n.altAttributeLocalized ?? G,
    u = n.fillImage,
    m = R(u),
    h = n.fillImageLocalized ?? G,
    g = Y(c, o, d, f, void 0, _i(u, h, m));
  r.push({
    type: "FrameNode",
    keyPath: Jn(n.id),
    nodeId: n.id,
    scopeId: e.id,
    value: c,
    valueWordCount: k(c),
    hash: f,
    localizedValues: d,
    localizedValueStatuses: g,
    managedByPluginButUserEditable: !1,
    inherited: !l,
    supportsAlt: !0,
    image: u,
    imageHash: m,
    imageLocalizedValues: h,
    imageInherited: !a,
    positionX: on(n.fillImagePositionX) ? n.fillImagePositionX : void 0,
    positionY: on(n.fillImagePositionY) ? n.fillImagePositionY : void 0,
    supportsFocalPoint: !0,
  });
}
function Vd(t, e, n, o) {
  if (!O.isOn("localizedLinks")) return;
  let r = e.getLink();
  if (!Et(r)) return;
  let i = r.url,
    a = R(i),
    s = e.linkLocalized ?? G,
    l = Y(i, n, s, a, void 0, $i(s));
  o.push({
    type: "nodeLink",
    keyPath: `${e.id}_linkLocalized`,
    nodeId: e.id,
    scopeId: t.id,
    value: i,
    valueWordCount: i ? 1 : 0,
    hash: a,
    localizedValues: s,
    localizedValueStatuses: l,
    managedByPluginButUserEditable: !1,
    aiTranslationDisabled: !0,
  });
}
function zm(t, e, n, o, r) {
  (Vm(t, e, n, o, r), Vd(e, n, o, r));
}
function yr(t) {
  return !!(Dd(t) || t === "array" || t === "object");
}
function Pm(t) {
  if (x(t) && vo(t)) return t.layoutTemplateIdentifier;
  if (ve(t)) return t.codeComponentIdentifier;
}
function zd(t, e) {
  let n = mn(t);
  if (typeof t != "string" || !t.includes("data-module-identifier")) return n;
  let o = fr(t, e);
  return o.length === 0 ? n : R(`${n}:${o}`);
}
function Mm(t, e, n, o, r, i, a) {
  let s = Pm(o);
  if (!s) return;
  let l = e.componentForIdentifier(s);
  if ((Tn(s, l !== null, () => Object.values(o.getRawControlProps()).map((d) => d?.type)), !l))
    return;
  let c = o.getControlProps(l.properties);
  function f(d, u, m, h) {
    let g = u.type;
    if (g === "array" && so(u.value)) {
      if (m.type !== "array") return;
      let y = m.control;
      for (let v of u.value) {
        if (!yr(v.type)) continue;
        let C = Me(h, v.id);
        f(d, v, y, C);
      }
    }
    if (g === "object" && Ne(u.value)) {
      if (m.type !== "object") return;
      for (let y in u.value) {
        let v = u.value[y];
        if (!v || !yr(v.type)) continue;
        let C = le(h, y),
          S = m.controls[y];
        S && f(d, v, S, C);
      }
    }
    if (Dd(g))
      switch (g) {
        case "string": {
          if (
            !Ie(t, o, rn(d)) ||
            (m.type === "string" && (m.preventLocalization || (m.optional && at(u.value)))) ||
            (st(u.value) && gr(n, o, u.value, le(o.id, d), d, r, i), !L(u.value))
          )
            break;
          let y = u.value,
            v = R(y),
            C = u.valueLocalized ?? G,
            S = Y(y, r, C, v);
          i.push({
            type: "string",
            keyPath: xt(o.id, d, h),
            controlPropKey: d,
            controlPropName: m.title || Mt(d),
            ancestorControlKeyPath: h,
            nodeId: o.id,
            scopeId: n.id,
            value: y,
            valueWordCount: k(y),
            hash: v,
            localizedValues: C,
            localizedValueStatuses: S,
            managedByPluginButUserEditable: !1,
          });
          break;
        }
        case "link": {
          if (
            !O.isOn("localizedLinks") ||
            !Ie(t, o, rn(d)) ||
            (st(u.value) && gr(n, o, u.value, le(o.id, d), d, r, i), !Et(u.value))
          )
            break;
          let y = u.value.url,
            v = R(y),
            C = u.valueLocalized ?? G,
            S = Y(y, r, C, v, void 0, $i(C));
          i.push({
            type: "link",
            keyPath: xt(o.id, d, h),
            controlPropKey: d,
            controlPropName: m.title || Mt(d),
            ancestorControlKeyPath: h,
            nodeId: o.id,
            scopeId: n.id,
            value: y,
            valueWordCount: y ? 1 : 0,
            hash: v,
            localizedValues: C,
            localizedValueStatuses: S,
            managedByPluginButUserEditable: !1,
            aiTranslationDisabled: !0,
          });
          break;
        }
        case "richtext": {
          if (
            g !== "richtext" ||
            !Se(u.value) ||
            (m.type === "richtext" && m.preventLocalization) ||
            !Ie(t, o, rn(d))
          )
            break;
          let y = u.value,
            v = Ut(y),
            C = zd(y, e),
            S = u.valueLocalized ?? G,
            w = Y(v, r, S, C);
          i.push({
            type: "richtext",
            keyPath: xt(o.id, d, h),
            controlPropKey: d,
            controlPropName: m.title || Mt(d),
            ancestorControlKeyPath: h,
            nodeId: o.id,
            scopeId: n.id,
            richTextValue: y,
            value: v,
            valueWordCount: k(v, !0),
            hash: C,
            localizedValues: S,
            localizedValueStatuses: w,
            managedByPluginButUserEditable: !1,
            contentType: void 0,
            isCollectionControlProp: !1,
          });
          break;
        }
        case "image": {
          if (
            !u ||
            u.type !== "image" ||
            !L(u.value) ||
            (m.type !== "responsiveimage" && m.type !== "image") ||
            !Ie(t, o, rn(d))
          )
            break;
          let y = m.type === "responsiveimage",
            v = u.alt ?? Bt,
            C = R(v),
            S = y ? (u.altLocalized ?? G) : G,
            w = u.value,
            N = R(w),
            D = u.valueLocalized ?? G,
            V = Y(v, r, S, C, void 0, _i(w, D, N)),
            z = m.type === "responsiveimage";
          i.push({
            type: "image",
            keyPath: xt(o.id, d, h),
            controlPropKey: d,
            controlPropName: m.title || Mt(d),
            ancestorControlKeyPath: h,
            nodeId: o.id,
            scopeId: n.id,
            value: v,
            valueWordCount: k(v),
            hash: C,
            localizedValues: S,
            localizedValueStatuses: V,
            managedByPluginButUserEditable: !1,
            supportsAlt: y,
            image: w,
            imageHash: N,
            imageLocalizedValues: D,
            supportsFocalPoint: z,
            positionX: z ? u.positionX : void 0,
            positionY: z ? u.positionY : void 0,
          });
          break;
        }
        default:
      }
  }
  for (let d in c) {
    let u = c[d];
    if (!u) continue;
    a && Ie(t, o, rn(d)) && a.addControlProp(u);
    let m = l.properties[d];
    m && f(d, u, m);
  }
}
function km(t, e, n, o) {
  switch (e.bindingType) {
    case "form": {
      let r = e.formSelectOptions;
      if (!r || r.length === 0) return;
      for (let i of r) {
        let a = i.title;
        if (!a) continue;
        let s = i.titleLocalized ?? G,
          l = R(a),
          c = Y(a, n, s, l);
        o.push({
          type: "FormSelectNode",
          keyPath: `${e.id}_${i.id}`,
          nodeId: e.id,
          optionId: i.id,
          scopeId: t.id,
          value: a,
          valueWordCount: k(a),
          hash: l,
          localizedValues: s,
          localizedValueStatuses: c,
          managedByPluginButUserEditable: !1,
        });
      }
      break;
    }
    case "variable": {
      if (!e.variableBinding || !ct(t) || !dn(t)) return;
      let r = t.getVariable(e.variableBinding.id);
      if (!r) return;
      (bt(r, hn(t)) &&
        o.push(Ai(e, t, n, "allItemsLabel", e.allItemsLabelWithDefault, e.allItemsLabelLocalized)),
        r.type === "boolean" &&
          (o.push(
            Ai(
              e,
              t,
              n,
              "booleanTrueLabel",
              e.booleanTrueLabelWithDefault,
              e.booleanTrueLabelLocalized
            )
          ),
          o.push(
            Ai(
              e,
              t,
              n,
              "booleanFalseLabel",
              e.booleanFalseLabelWithDefault,
              e.booleanFalseLabelLocalized
            )
          )));
      break;
    }
    default:
      Z(e.bindingType);
  }
}
function Ai(t, e, n, o, r, i) {
  let a = i ?? G,
    s = R(r),
    l = Y(r, n, a, s);
  return {
    type: "formSelectGenericOption",
    keyPath: `${t.id}_${o}`,
    nodeId: t.id,
    key: o,
    scopeId: e.id,
    value: r,
    valueWordCount: k(r),
    hash: s,
    localizedValues: a,
    localizedValueStatuses: l,
    managedByPluginButUserEditable: !1,
  };
}
function Em(t, e, n, o, r) {
  let i = `${n.id}_placeholder`;
  if (De(n.formInputPlaceholder)) {
    if (Ve(n.formInputPlaceholder) || !Ie(t, n, "formInputPlaceholder")) return;
    gr(e, n, n.formInputPlaceholder, i, n.id, o, r);
    return;
  }
  if (!n.formInputPlaceholder || !Ie(t, n, "formInputPlaceholder")) return;
  let a = R(n.formInputPlaceholder),
    s = n.formInputPlaceholderLocalized ?? G;
  r.push({
    keyPath: i,
    type: "FormPlainTextInputNode",
    nodeId: n.id,
    scopeId: e.id,
    value: n.formInputPlaceholder,
    valueWordCount: k(n.formInputPlaceholder),
    hash: R(n.formInputPlaceholder),
    localizedValues: s,
    localizedValueStatuses: Y(n.formInputPlaceholder, o, s, a),
    managedByPluginButUserEditable: !1,
  });
}
function Pd(t, e, n, o, r, i, a, s, l) {
  (s?.(),
    ve(o) && l?.addIdentifier(o.codeComponentIdentifier),
    rs(o) &&
      o.customCursorType === "component" &&
      l?.addSmartComponentId(o.customCursorSmartComponentId),
    x(o) && wd(t, e, n, o, r, i),
    ra(o) && wm(t, n, o, r, i, a),
    (ve(o) || x(o)) && Mm(t, e, n, o, r, i, l),
    tn(o) && zm(t, n, o, r, i),
    (ia(o) || aa(o)) && Vd(n, o, r, i),
    sa(o) && Em(t, n, o, r, i),
    la(o) && km(n, o, r, i));
}
function Md(t, e) {
  return e
    ? `r:${t.cache.localizationSourceRevision}-${e.cache.localizationSourceRevision}`
    : `r:${t.cache.localizationSourceRevision}`;
}
function kd(t, e) {
  return e
    ? `t:${t.cache.localizationTranslationRevision}-${e.cache.localizationTranslationRevision}`
    : `t:${t.cache.localizationTranslationRevision}`;
}
function Ed(t, e) {
  return e ? `u:${t.update}-${e.update}` : `u:${t.update}`;
}
function Bd(t) {
  return t !== "canvasHtmlAsHtml";
}
function Fd(t) {
  return hr() && !ge(t);
}
function Ad(t) {
  return O.isOn("localizationProgressSummaries") && t.isDataTree();
}
function vr(t) {
  return !O.isOn("localizationProgressSummaries") && t.isDataTree();
}
function Ud(t, e) {
  let n = t.get("originalid");
  if (!n || (e.add(n), na(t.id, n))) return;
  let o = t.get("replicaInfo")?.inheritsFrom;
  o && e.add(o);
}
function Od(t, e) {
  ho(t) && (e.add(t.originalid), ya(t) && e.add(t.replicaInfo.inheritsFrom));
}
function Bm(t, e, n) {
  if (!n) return [];
  let o = new Set();
  for (let r of n) {
    let i = t.getCanvasScopeNodeFor(t.get(r));
    i && i.id !== e.id && o.add(i.id);
  }
  return Array.from(o);
}
function $d(t, e) {
  let n = [];
  for (let o of e) {
    let r = t.get(o);
    if (!r) return;
    n.push(`${r.cache.localizationSourceRevision}:${r.cache.localizationTranslationRevision}`);
  }
  return n.join(",");
}
function Sr(t, e) {
  return Array.from(e, (n) => [n, t.getDefinitionHash(n)]);
}
var Fm = [];
function ut(t, e) {
  if (t) for (let [n, o] of e) t.set(n, o);
}
var Ge = oe("localization:cache");
function Am(t, e, n, o, r, i, a, s, l, c) {
  if (!Bd(c) || e.mutable || n?.mutable) return;
  let f = e.cache.localizationSourceGroup;
  if (!f) return;
  let d = Fd(e),
    u,
    m;
  if (
    (d
      ? ((u = f.sourceRevision !== Md(e, n) || f.translationRevision !== kd(e, n)),
        (m = f.parentSourceOwnRevision !== a))
      : ((u = f.sourceRevision !== Ed(e, n)), (m = f.parentVariables !== i)),
    u)
  ) {
    Ge.trace("Stale cache for", e.id, "->", "revision changed");
    return;
  }
  if (m) {
    Ge.trace("Stale cache for", e.id, "->", "parent revision / variables changed");
    return;
  }
  if (f.locales !== o) {
    Ge.trace("Stale cache for", e.id, "->", "Locales changed");
    return;
  }
  if (!Ct(f.subGroups, s)) {
    Ge.trace("Stale cache for", e.id, "->", "Subgroups changed");
    return;
  }
  if (!Ct(f.settings, l)) {
    Ge.trace("Stale cache for", e.id, "->", "Settings changed");
    return;
  }
  for (let [h, g] of f.loaderDependencies)
    if (r.getDefinitionHash(h) !== g) {
      Ge.trace("Stale cache for", e.id, "->", "Component dependency", h, "changed");
      return;
    }
  if (kn(t, e)) {
    if (f.includedLocaleIds !== e.includedLocales) {
      Ge.trace("Stale cache for", e.id, "->", "Included locale ids changed");
      return;
    }
    if (f.localeConfig !== e.localeConfig) {
      Ge.trace("Stale cache for", e.id, "->", "Locale config changed");
      return;
    }
  }
  if (Ad(t)) {
    let h = t.getResolvedSubtreeGeneration(e.id);
    if (h === void 0 || f.resolvedSubtreeGeneration !== h) {
      Ge.trace("Stale cache for", e.id, "->", "Resolved subtree generation changed or unavailable");
      return;
    }
    if (n) {
      let g = t.getResolvedSubtreeGeneration(n.id);
      if (g === void 0 || f.additionalDependencyResolvedSubtreeGeneration !== g) {
        Ge.trace(
          "Stale cache for",
          e.id,
          "->",
          "Additional dependency resolved subtree generation changed or unavailable"
        );
        return;
      }
    }
  }
  if (vr(t) && (f.masterScopeIds === void 0 || f.masterScopeRevision !== $d(t, f.masterScopeIds))) {
    Ge.trace("Stale cache for", e.id, "->", "Master scope revision changed or unavailable");
    return;
  }
  return f;
}
function Yn(t, e, n, o, r, i, a, s, l, c, f, d) {
  let u = Am(t, e, n, o, r, i, a, s, l, c);
  if (u) {
    let h = Cm(u, e, f),
      g = d === Xn || u.includesWebPage === d;
    if (h && g) return (td(u.unresolvedLocalizableCount), u);
  }
  let m = f?.collection;
  m && (m.allScopesCached = !1);
}
function Zn(t, e, n) {
  let {
    additionalDependencyNode: o,
    group: r,
    locales: i,
    loaderDependencies: a,
    unresolvedLocalizableCount: s,
    parentVariables: l,
    parentSourceOwnRevision: c,
    subGroups: f,
    settings: d,
    richTextBehavior: u,
    includesWebPage: m,
    directSmartComponentIds: h,
    localizedSmartComponentIdsByLocaleId: g,
    cmsLocalizationDependencies: y,
    pageVisibilityFact: v,
    replicaMasterNodeIds: C,
  } = n;
  if (!Bd(u) || e.mutable || o?.mutable) return;
  let S, w;
  if (vr(t) && ((S = Bm(t, e, C)), (w = $d(t, S)), w === void 0)) return;
  let N, D;
  if (
    Ad(t) &&
    ((N = t.getResolvedSubtreeGeneration(e.id)),
    N === void 0 || (o && ((D = t.getResolvedSubtreeGeneration(o.id)), D === void 0)))
  )
    return;
  let V = kn(t, e),
    z,
    P;
  (Fd(e) ? ((z = Md(e, o)), (P = kd(e, o))) : ((z = Ed(e, o)), (P = z)),
    (e.cache.localizationSourceGroup = {
      sourceRevision: z,
      translationRevision: P,
      resolvedSubtreeGeneration: N,
      additionalDependencyResolvedSubtreeGeneration: D,
      masterScopeIds: S,
      masterScopeRevision: w,
      locales: i,
      loaderDependencies: a,
      unresolvedLocalizableCount: s,
      parentVariables: l,
      parentSourceOwnRevision: c,
      group: r,
      includedLocaleIds: V ? e.includedLocales : void 0,
      localeConfig: V ? e.localeConfig : void 0,
      subGroups: f,
      settings: d,
      includesWebPage: m,
      directSmartComponentIds: h,
      localizedSmartComponentIdsByLocaleId: g,
      cmsLocalizationDependencies: y,
      pageVisibilityFact: v,
    }));
}
function Um(t) {
  let e = t.get(ja);
  return Rr(e) && e.parentid === t.root.id;
}
function Om(t) {
  return x(t) ? "page" : K(t) ? "template" : "component";
}
function $m(t, e, n, o) {
  let r = n.resolveValue("name") ?? "Unknown",
    i = x(n) ? En(t, e, n) : r;
  if (!o) return i;
  let a = n.resolveValue("name") || $e(e, n);
  return `${i} \xB7 ${a}`;
}
function _d(t, e, n, o, r, i, a, s) {
  var M = [];
  try {
    let { checkpoint: l, loaderDependenciesByIdentifier: c, dependencies: f } = s ?? {};
    l?.();
    let d = x(n) && n.abTestingParentId !== void 0;
    let u = d ? (t.getNodeWithTrait(n.abTestingParentId, x) ?? void 0) : void 0;
    let m = u ?? n;
    if (Zm(t, n) && !d) return;
    let h = Yn(t, n, u, o, e, pe, pe, void 0, void 0, i, f, Xn);
    if (h) {
      (ut(c, h.loaderDependencies),
        Qn(
          t,
          n,
          h.directSmartComponentIds,
          h.localizedSmartComponentIdsByLocaleId,
          f,
          m,
          h.pageVisibilityFact
        ),
        h.group && r.push(h.group));
      return;
    }
    let g = [];
    let y;
    let v = Ei(f, l);
    let C = vr(t);
    let S = new Set();
    let w = new Set();
    let N = xn(M, Kn());
    e.trackEntityReads(w, () => {
      if (a) for (let ue of n.fastWalk()) (C && Ud(ue, S), zi(t, e, n, ue, o, g, i, l, v));
      else for (let ue of n.walk()) (C && Od(ue, S), Pd(t, e, n, ue, o, g, i, l, v));
      let Q = g.length === 0,
        me = O.isOn("localizationUiUpdate") && x(n) && Um(t);
      if (Q && !me) return;
      let ce = !d && kn(t, n);
      if (Q && !(me && ce)) return;
      let ft = $m(t, e, n, d),
        pt = Om(n),
        mt = x(n) && Xe(t, n),
        de = !(xr(mt) && wr.has(mt)) && O.isOn("localizationModelUpdate");
      ((y = {
        type: "sourceGroup",
        name: ft,
        groupType: pt,
        nodeId: n.id,
        supportsLocaleVisibility: ce,
        supportsLocaleExclusion: de,
        includedLocaleIds: un(t, m),
        fallbackLocaleIds: zo(t, m),
        items: g,
      }),
        r.push(y));
    });
    let D = Sr(e, w);
    let V = Bi(v);
    let z = Fi(v);
    ut(c, D);
    let P = Qn(t, n, V, z, f, m);
    Zn(t, n, {
      additionalDependencyNode: u,
      group: y,
      locales: o,
      loaderDependencies: D,
      unresolvedLocalizableCount: N.lookups.unresolvedLocalizableCount,
      parentVariables: pe,
      parentSourceOwnRevision: pe,
      richTextBehavior: i,
      directSmartComponentIds: V,
      localizedSmartComponentIdsByLocaleId: z,
      pageVisibilityFact: P,
      replicaMasterNodeIds: S,
    });
  } catch (ne) {
    var re = ne,
      j = !0;
  } finally {
    Dn(M, re, j);
  }
}
function Jt(t, e, n) {
  return n ? `${t}.${e}.${n}` : `${t}.${e}`;
}
function LR(t, e = t.collectionItemId) {
  if (t.type === "collectionField") return Jt(e, t.variableId);
  if (t.type === "arrayItemField") {
    let n = Me(void 0, t.arrayItemId),
      o = le(n, t.variableId);
    return Jt(e, t.arrayVariableId, o);
  }
  return Z(t);
}
function _m(t) {
  return zr(t).length;
}
function Ui(t, e, n) {
  let o = n.required,
    r = n.type === "link" ? void 0 : n.options?.maxLength;
  if (!o && !A(r)) return;
  let i = {};
  for (let a of t) {
    let s = e[a.id];
    if (!Co(s)) continue;
    let l = _m(s);
    o && l === 0 ? (i[a.id] = Kl(n.name)) : A(r) && l > r && (i[a.id] = Ql(l, r));
  }
  return i;
}
function Wd(t, e, n, o) {
  let r = {};
  for (let i of t) {
    let a = e[i.id];
    if (!Co(a) || !L(a.value)) continue;
    let l = n[i.id]?.get(a.value);
    l && l > 1 && (r[i.id] = o);
  }
  return r;
}
function Wm(t, e, n, o, r, i, a, s, l, c) {
  let f = a.type;
  switch (f) {
    case "string": {
      if (cn(a)) return;
      let d;
      if (l) {
        if (l.type !== f || !L(l.value)) return;
        d = l.value;
      } else {
        if (Rs(a)) return;
        d = a.initialValue;
      }
      let u = R(d),
        m = l?.valueLocalized ?? G,
        h = Ui(r, m, a),
        g = Y(d, r, m, u, h);
      return {
        type: "string",
        keyPath: Jt(o.id, s, i),
        nodeId: o.id,
        scopeId: n,
        controlPropKey: s,
        controlPropName: a.name,
        ancestorControlKeyPath: i,
        value: d,
        valueWordCount: k(d),
        hash: u,
        localizedValues: m,
        localizedValueStatuses: g,
        managedByPluginButUserEditable: a.userEditable ?? !1,
        warnings: h,
      };
    }
    case "link": {
      if (!l || l.type !== f || !Et(l.value) || !O.isOn("localizedLinks")) return;
      let d = l.value.url,
        u = R(d),
        m = l?.valueLocalized ?? G,
        h = Ui(r, m, a),
        g = Y(d, r, m, u, h);
      return {
        type: "link",
        keyPath: Jt(o.id, s, i),
        nodeId: o.id,
        scopeId: n,
        controlPropKey: s,
        controlPropName: a.name,
        ancestorControlKeyPath: i,
        value: d,
        valueWordCount: d ? 1 : 0,
        hash: u,
        localizedValues: m,
        localizedValueStatuses: g,
        managedByPluginButUserEditable: a.userEditable ?? !1,
        warnings: h,
        aiTranslationDisabled: !0,
      };
    }
    case "slug": {
      if (t.root.translatePagePaths === "no" || !l) return;
      (b(l.type === "string", "expected slug controlType to be a string"),
        b(L(l.value), "expected slug value to be a string"));
      let d = l.value,
        u = R(d),
        m = l?.valueLocalized ?? G,
        h = Wd(r, m, c, "Each item in the Collection must have a unique slug"),
        g = Y(d, r, m, u, h, t.root.translatePagePaths === "legacy" ? $i(m) : void 0);
      return {
        type: "slug",
        keyPath: Jt(o.id, s, i),
        controlPropKey: s,
        controlPropName: a.name,
        ancestorControlKeyPath: i,
        nodeId: o.id,
        scopeId: n,
        value: d,
        valueWordCount: k(d),
        hash: u,
        localizedValues: m,
        localizedValueStatuses: g,
        managedByPluginButUserEditable: a.userEditable ?? !1,
        warnings: h,
        aiTranslationDisabled: !t.root.translatePagePathsWithAI,
        collectionItemIsDraft: o.isDraft ?? !1,
      };
    }
    case "richtext": {
      let d;
      if (cn(a)) return;
      if (l) {
        if (l.type !== f || !Se(l.value)) return;
        d = l.value;
      } else d = a.initialValue;
      let u = Ut(d),
        m = zd(d, e),
        h = l?.valueLocalized ?? G,
        g = Ui(r, h, a),
        y = Y(u, r, h, m, g);
      return {
        type: "richtext",
        keyPath: Jt(o.id, s, i),
        nodeId: o.id,
        scopeId: n,
        controlPropKey: s,
        controlPropName: a.name,
        ancestorControlKeyPath: i,
        richTextValue: d,
        value: u,
        valueWordCount: k(u, !0),
        hash: m,
        localizedValues: h,
        localizedValueStatuses: y,
        managedByPluginButUserEditable: a.userEditable ?? !1,
        warnings: g,
        contentType: a.type === "richtext" ? a.contentType : void 0,
        isCollectionControlProp: !0,
      };
    }
    case "image": {
      let d;
      if (l) {
        if (l.type !== f || !L(l.value)) return;
        d = l.value;
      } else {
        if (!L(a.initialValue)) return;
        d = a.initialValue;
      }
      let u = R(d),
        m = l?.valueLocalized ?? G,
        h = l?.alt ?? Bt,
        g = R(h),
        y = l?.altLocalized ?? G,
        v = Y(h, r, y, g, void 0, _i(d, m, u));
      return {
        type: "image",
        keyPath: Jt(o.id, s, i),
        nodeId: o.id,
        scopeId: n,
        controlPropKey: s,
        controlPropName: a.name,
        ancestorControlKeyPath: i,
        value: h,
        valueWordCount: k(h),
        hash: g,
        localizedValues: y,
        localizedValueStatuses: v,
        supportsAlt: !0,
        image: d,
        imageHash: u,
        imageLocalizedValues: m,
        managedByPluginButUserEditable: a.userEditable ?? !1,
        supportsFocalPoint: !0,
        positionX: l?.positionX,
        positionY: l?.positionY,
      };
    }
    case "enum":
      return;
    default:
  }
}
function Gm(t, e, n, o, r, i, a) {
  let s = o.id;
  function l(c, f, d, u) {
    if (f && f.type === "array") {
      if (d.type !== "array" || De(f.value)) return;
      for (let h of f.value) {
        if (!yr(h.type) || !bo(h) || !h.value) continue;
        let g = Me(u, h.id);
        for (let y of d.itemVariables) {
          if (!Te(y)) continue;
          let v = h.value[y.id];
          if (!v || !yr(v.type)) continue;
          let C = le(g, y.id);
          l(c, v, y, C);
        }
      }
    }
    if (!xd(d)) return;
    let m = Wm(t, e, s, o, r, u, d, c, f, a);
    m && i.push(m);
  }
  for (let c of n.variables) {
    if (!Te(c)) continue;
    let f = o.getControlProp(c.id);
    l(c.id, f, c);
  }
}
var Gd = Ha((t, e) => ({ translatePagePaths: t, translatePagePathsWithAI: e }));
function jd(t, e, n, o, r, i, a, s) {
  var D = [];
  try {
    let { checkpoint: l, loaderDependenciesByIdentifier: c, dependencies: f } = s ?? {};
    l?.();
    let d = Gd(t.root.translatePagePaths, t.root.translatePagePathsWithAI);
    let u = o.cache.localizationSourceGroup?.cmsLocalizationDependencies;
    let m = Yn(
      t,
      o,
      void 0,
      r,
      e,
      n.variables,
      n.cache.localizationSourceOwnRevision,
      void 0,
      d,
      void 0,
      f,
      Xn
    );
    if (m) {
      ut(c, m.loaderDependencies);
      let M = f?.collection;
      (M &&
        m.cmsLocalizationDependencies &&
        M.facts.cmsDependenciesByItemId.set(o.id, m.cmsLocalizationDependencies),
        m.group && a.push(m.group));
      return;
    }
    let h = [];
    let g = un(t, o);
    let y = zo(t, o);
    let v = new Set();
    let C = xn(D, Kn());
    e.trackEntityReads(v, () => {
      Gm(t, e, n, o, r, h, i);
    });
    let S;
    if (h.length > 0) {
      for (let M of h) M.insetLevel = 1;
      ((S = {
        type: "sourceGroup",
        groupType: "collectionItem",
        collectionName: n.resolveValue("name") ?? "Unknown",
        collectionNodeId: n.id,
        supportsLocaleVisibility: !0,
        supportsLocaleExclusion: !0,
        includedLocaleIds: g,
        fallbackLocaleIds: y,
        name: $o(n, o) ?? "Unknown",
        nodeId: o.id,
        items: h,
      }),
        a.push(S));
    }
    let w = Sr(e, v);
    let N;
    if (f) {
      let M = { routerLocaleById: f.routerLocaleById, checkpoint: l, previousDependencies: u };
      N = Id(n, o, r, g, M);
    }
    N && f?.collection && f.collection.facts.cmsDependenciesByItemId.set(o.id, N);
    ut(c, w);
    Zn(t, o, {
      group: S,
      locales: r,
      loaderDependencies: w,
      unresolvedLocalizableCount: C.lookups.unresolvedLocalizableCount,
      parentVariables: n.variables,
      parentSourceOwnRevision: n.cache.localizationSourceOwnRevision,
      settings: d,
      cmsLocalizationDependencies: N,
    });
  } catch (V) {
    var z = V,
      P = !0;
  } finally {
    Dn(D, z, P);
  }
}
function jm(t, e, n, o) {
  let r = n.name,
    i = R(r),
    a = n.nameLocalized ?? G,
    s = Y(r, o, a, i);
  return {
    type: "enum",
    keyPath: `${t.id}_${e.id}_${n.id}`,
    nodeId: t.id,
    scopeId: t.id,
    variableId: e.id,
    enumCaseId: n.id,
    value: r,
    valueWordCount: k(r),
    hash: i,
    localizedValues: a,
    localizedValueStatuses: s,
    managedByPluginButUserEditable: !1,
  };
}
function Hd(t, e, n, o, r, i) {
  let { checkpoint: a, loaderDependenciesByIdentifier: s, dependencies: l } = i ?? {};
  (a?.(), gd(n, l?.collection));
  let c = [],
    f = qd(o, n);
  for (let g of n.getUnsortedChildren()) jd(t, e, n, g, o, f, c, i);
  let d = Yn(t, n, void 0, o, e, pe, pe, c, void 0, void 0, l, Xn);
  if (d) {
    (ut(s, d.loaderDependencies), d.group && r.push(d.group));
    return;
  }
  let u = n.variables.filter(xd),
    m = [];
  for (let g of u) {
    let y = g.type;
    switch (y) {
      case "enum": {
        if (cn(g)) break;
        for (let v of g.cases) m.push(jm(n, g, v, o));
        break;
      }
      case "string":
      case "richtext":
      case "image":
      case "link":
      case "slug":
        break;
      default:
    }
  }
  let h =
    m.length > 0 || c.length > 0
      ? {
          type: "sourceGroup",
          groupType: "collection",
          name: n.resolveValue("name") ?? "Unknown",
          nodeId: n.id,
          supportsLocaleVisibility: !1,
          supportsLocaleExclusion: !1,
          includedLocaleIds: void 0,
          fallbackLocaleIds: void 0,
          items: m,
          groups: c,
        }
      : void 0;
  (Zn(t, n, {
    group: h,
    locales: o,
    loaderDependencies: Fm,
    unresolvedLocalizableCount: 0,
    parentVariables: pe,
    parentSourceOwnRevision: pe,
    subGroups: c,
  }),
    h && r.push(h));
}
function qd(t, e) {
  let n = {};
  for (let r of t) n[r.id] = new Map();
  let o = e.getSlugVariable();
  if (!o) return n;
  for (let r of e.getUnsortedChildren()) {
    let i = r.getControlProp(o.id);
    if (
      i &&
      (b(i.type === "string", "expect slug control type to be be of type string"),
      b(L(i.value), "expected slug control prop value to be string"),
      !!i.valueLocalized)
    )
      for (let a in i.valueLocalized) {
        let s = i.valueLocalized[a],
          l = n[a];
        if (!l || !L(s?.value) || !s.value) continue;
        let c = (l.get(s.value) ?? 0) + 1;
        l.set(s.value, c);
      }
  }
  return n;
}
function Hm(t, e, n) {
  let o = t.getParent(n.id)?.children ?? [],
    r = {};
  for (let i of e) i.id !== lt && (r[i.id] = new Map());
  for (let i of o) {
    if (!Je(i)) continue;
    let { segment: a, segmentLocalized: s } = i;
    for (let l of e) {
      if (l.id === lt) continue;
      let c = sn(l, s)?.value,
        d = (L(c) ? c : void 0) || a,
        u = r[l.id];
      if (!u) continue;
      let m = (u.get(d) ?? 0) + 1;
      u.set(d, m);
    }
  }
  return r;
}
function Jd(t, e, n, o, r, i) {
  var m = [];
  try {
    let a = i?.collection;
    vd(t.root, a);
    let s = Yn(t, t.root, void 0, n, e, pe, pe, void 0, void 0, void 0, i, Xn);
    if (s) {
      (ut(r, s.loaderDependencies), s.group && o.push(s.group));
      return;
    }
    let l = [];
    let c = new Set();
    let f = xn(m, Kn());
    e.trackEntityReads(c, () => {
      wd(t, e, t.root, t.root, n, l);
    });
    let d =
      l.length > 0
        ? {
            type: "sourceGroup",
            groupType: "settings",
            name: "Site Settings",
            nodeId: t.root.id,
            supportsLocaleVisibility: !1,
            supportsLocaleExclusion: !1,
            includedLocaleIds: void 0,
            fallbackLocaleIds: void 0,
            items: l,
          }
        : void 0;
    let u = Sr(e, c);
    ut(r, u);
    Zn(t, t.root, {
      group: d,
      locales: n,
      loaderDependencies: u,
      unresolvedLocalizableCount: f.lookups.unresolvedLocalizableCount,
      parentVariables: pe,
      parentSourceOwnRevision: pe,
    });
    d && o.push(d);
  } catch (h) {
    var g = h,
      y = !0;
  } finally {
    Dn(m, g, y);
  }
}
function qm(t, e, n, o, r, i, a) {
  for (let s of n.children) Hd(t, e, s, o, r, { checkpoint: i, dependencies: a });
}
function Jm(t, e, n, o, r, i) {
  if (!Ja(n)) return;
  let a = Ot(n.segment),
    s = R(n.segment),
    l = n.segmentLocalized ?? G,
    c = Wd(o, l, Hm(t, r, n), "Sibling pages must have unique slugs");
  i.push({
    keyPath: n.id,
    type: "RouteSegmentNode",
    nodeId: n.id,
    scopeId: e.id,
    value: a,
    valueWordCount: k(a),
    hash: R(n.segment),
    localizedValues: l,
    localizedValueStatuses: Y(n.segment, o, l, s, c, void 0),
    managedByPluginButUserEditable: !1,
    warnings: c,
    aiTranslationDisabled: !t.root.translatePagePathsWithAI,
  });
}
function Km(t, e, n, o, r, i, a, s) {
  let c = s?.dependencies?.routerLocales ?? Ue(t, "includeDrafts"),
    f = new Set();
  for (let d of n.walk()) Je(d) && Oi(t, e, d, o, c, r, i, f, a, s);
}
function Qm(t, e, n, o, r) {
  let i = _s(Pi(n, t)),
    a = x(o) ? En(t, e, o) : i,
    s = Xm(e, r, a);
  return n.segment === "" ? `${s} /` : s;
}
function Xm(t, e, n) {
  if (!x(e) || !e.abTestingParentId) return n;
  let o = e.resolveValue("name") || $e(t, e);
  return `${n} \xB7 ${o}`;
}
function Oi(t, e, n, o, r, i, a, s, l, c) {
  var ke = [];
  try {
    let { checkpoint: f, loaderDependenciesByIdentifier: d, dependencies: u } = c ?? {};
    f?.();
    let m = t.getParent(n.id);
    let h = Je(m) ? m.id : void 0;
    yd(n, h, u?.collection);
    let g = n.webPageId ? (t.get(n.webPageId) ?? void 0) : void 0;
    let y = x(g) && g.isLoaded() ? g : void 0;
    let v = Gd(t.root.translatePagePaths, t.root.translatePagePathsWithAI);
    let C = g && s.has(g.id);
    let S = y || n;
    let w = S === y ? n : void 0;
    let N = !!(g && !C);
    let D = Yn(t, S, w, o, e, pe, pe, void 0, v, a, u, N);
    if (D) {
      (ut(d, D.loaderDependencies),
        N &&
          x(S) &&
          Qn(
            t,
            S,
            D.directSmartComponentIds,
            D.localizedSmartComponentIdsByLocaleId,
            u,
            S,
            D.pageVisibilityFact
          ),
        D.group && i.push(D.group),
        N && g && s.add(g.id));
      return;
    }
    let V = [];
    let z = Ei(u, f);
    let P = vr(t);
    let M = new Set();
    let ne = new Set();
    let re = xn(ke, Kn());
    let j = e.trackEntityReads(ne, () => {
      if ((t.root.translatePagePaths === "yes" && Jm(t, S, n, o, r, V), g && !C)) {
        if (l) for (let Ee of g.fastWalk()) (P && Ud(Ee, M), zi(t, e, S, Ee, o, V, a, f, z));
        else for (let Ee of g.walk()) (P && Od(Ee, M), Pd(t, e, S, Ee, o, V, a, f, z));
        s.add(g.id);
      }
      return Qm(t, e, n, g, S);
    });
    let Q = kn(t, S);
    let me = O.isOn("localizationUiUpdate") && Q && !C;
    let ce = x(S) && Xe(t, S);
    let wt = xr(ce) && wr.has(ce);
    let ft = !wt && O.isOn("localizationModelUpdate");
    let pt;
    (V.length > 0 || me) &&
      ((pt = {
        type: "sourceGroup",
        name: j,
        groupType: "page",
        nodeId: S.id,
        supportsLocaleVisibility: Q,
        supportsLocaleExclusion: ft,
        includedLocaleIds: Q ? un(t, S) : void 0,
        fallbackLocaleIds: Q ? zo(t, S) : void 0,
        items: V,
      }),
      i.push(pt));
    let mt = Sr(e, ne);
    let H = Bi(z);
    let de = Fi(z);
    ut(d, mt);
    let ue = N && x(S) ? Qn(t, S, H, de, u) : void 0;
    Zn(t, S, {
      additionalDependencyNode: w,
      unresolvedLocalizableCount: re.lookups.unresolvedLocalizableCount,
      group: pt,
      locales: o,
      loaderDependencies: mt,
      parentVariables: pe,
      parentSourceOwnRevision: pe,
      settings: v,
      richTextBehavior: a,
      includesWebPage: N,
      directSmartComponentIds: H,
      localizedSmartComponentIdsByLocaleId: de,
      pageVisibilityFact: ue,
      replicaMasterNodeIds: M,
    });
  } catch (Vt) {
    var zt = Vt,
      rt = !0;
  } finally {
    Dn(ke, zt, rt);
  }
}
function NR(t, e, n, o, r, i) {
  let a = [],
    s = n.id,
    l = hr() && t.isDataTree(),
    c = new Map(),
    f = { checkpoint: i, loaderDependenciesByIdentifier: c },
    d = (u) => ({
      canonicalScopeId: u,
      groups: a,
      loaderDependencies: Array.from(c, ([m, h]) => [m, h]),
    });
  if (ge(n)) return (Jd(t, e, o, a, c), d(n.id));
  if (qe(n)) {
    let u = t.getNodeWithTrait(n.parentid, W);
    return (u && jd(t, e, u, n, o, qd(o, u), a, f), d(n.id));
  }
  if (W(n)) return (Hd(t, e, n, o, a, f), d(n.id));
  if (Je(n)) {
    Oi(t, e, n, o, Ue(t, "includeDrafts"), a, r, new Set(), l, f);
    let u = n.webPageId ? t.get(n.webPageId) : void 0;
    return d(x(u) && u.isLoaded() ? u.id : n.id);
  }
  if (Hn(n)) {
    let u = x(n) ? Ka(t, n.id) : void 0,
      m = x(n) && n.abTestingParentId !== void 0;
    return (
      u && n.id !== t.root.homePageNodeId && !m
        ? Oi(t, e, u, o, Ue(t, "includeDrafts"), a, r, new Set(), l, f)
        : _d(t, e, n, o, a, r, l, f),
      d(n.id)
    );
  }
  return d(s);
}
function xR(t, e, n, o, r, i = hr(), a) {
  let s = [];
  if (!n || n.length === 0) return s;
  let l = a ? hd(t, a) : void 0;
  Jd(t, e, n, s, void 0, l);
  let c = { checkpoint: r, dependencies: l };
  i &&= t.isDataTree();
  for (let f of t.root.children) Ym(t, e, f, n, o, i, s, c);
  return s;
}
function Ym(t, e, n, o, r, i, a, s) {
  let { checkpoint: l, dependencies: c } = s ?? {},
    f = !1;
  (x(n) && ((f = n.abTestingParentId !== void 0), !f && t.root.homePageNodeId !== n.id)) ||
    (Hn(n) && (b(n.isLoaded(), "Scope node must be fully loaded"), _d(t, e, n, o, a, r, i, s)),
    wn(n) && (b(n.isLoaded(), "Scope node must be fully loaded"), qm(t, e, n, o, a, l, c)),
    Rr(n) && Km(t, e, n, o, a, r, i, s));
}
function Zm(t, e) {
  return x(e) && e.id !== t.root.homePageNodeId;
}
function DR(t, e) {
  return je(t, e);
}
function RR(t, e) {
  return Ct(t, e);
}
function MR(t) {
  return new URL(`/multiplayer${t}`, bl().api).href;
}
function kR(t) {
  try {
    return new URL(t).pathname.startsWith("/multiplayer");
  } catch {
    return !1;
  }
}
async function AR(t, e, n) {
  let o = e && e !== Es ? { branchId: e } : void 0;
  return Xl.get(`/web/v1/sites/hostnames/${t}`, o, n);
}
var Kd = class {
  constructor(e) {
    this.scheduler = e;
  }
  scheduler;
};
var Rt = en(Ki(), 1);
var Qd = Symbol("uninitialized");
function eh(t) {
  return t + 1;
}
function WR(t, e, n, o) {
  let r = _a(),
    [, i] = (0, Rt.useReducer)(eh, 0),
    a = (0, Rt.useRef)(Qd);
  a.current === Qd && (a.current = e());
  let s = (0, Rt.useRef)(e);
  s.current = e;
  let l = (0, Rt.useRef)(n);
  if (!Ct(n, l.current)) {
    l.current = n;
    let f = a.current,
      d = s.current();
    (o ? !je(f, d) : !Ct(f, d)) && (a.current = d);
  }
  let c = Array.isArray(t) ? t : [t];
  return (
    (0, Rt.useEffect)(() => {
      let f = () => {
          let m = a.current,
            h = s.current();
          (o ? je(m, h) : Ct(m, h)) || ((a.current = h), i());
        },
        d = [...c, f],
        u = r.scheduler.changes.observe(...d);
      return () => r.scheduler.changes.removeObserver(u);
    }, c),
    a.current
  );
}
function th(t) {
  return ve(t) || co(t);
}
function Wi(t) {
  if (ve(t)) return t.codeComponentIdentifier;
  if (co(t)) return t.shaderModuleIdentifier;
}
function nh(t, e) {
  (ve(t) && t.set({ codeComponentIdentifier: e }), co(t) && t.set({ shaderModuleIdentifier: e }));
}
function oh(t, e) {
  return e.moduleId === t.moduleId && e.saveId !== t.saveId;
}
function Xd(t, e) {
  switch (e?.type) {
    case "canvasComponent":
    case "codeFile":
    case "shader": {
      let n = J(e.codeComponentIdentifier);
      return oh(t, n);
    }
    default:
      return !1;
  }
}
function rh(t, e) {
  let n = J(t),
    o = J(e);
  return (
    b(n.moduleId === o.moduleId, "Module id should be the same"),
    b(n.saveId !== o.saveId, "Module identifier should have a different save id than the instance"),
    Zi(n.moduleId, o.saveId, n.file, n.exportSpecifier).value
  );
}
function KR(t, e) {
  let n = t.tree.getNodeWithTrait(e, th);
  if (!n) return;
  b(!ho(n), "Module instance node should not be a replica or replica child");
  let o = Wi(n),
    r = J(o);
  b(r?.kind === "externalModuleExport", "Module identifier should be external");
  let i = t.tree.getNodeWithTrait(r.moduleId, uo);
  b(i, "Module node should exist");
  let a = rh(r.value, i.codeComponentIdentifier);
  nh(n, a);
}
function Yd(t, e, n, o, r) {
  let i = J(t);
  if (!Yi(i)) return;
  let a = Xi(i);
  if (a && !i.moduleId) return;
  let s = a ? n.getNodeWithTrait(i.moduleId, uo) : null,
    l = a ? (s?.type ?? "codeFile") : i.type,
    c = l === "shader" ? !!e.shaderForIdentifier(i.value) : !!e.componentForIdentifier(i.value);
  return a
    ? {
        type: l,
        hasDefinition: c,
        hasModuleEntry: o.hasModuleForIdentifier(i),
        hasLocalModuleError: !1,
        isModuleEvaluationPending: !1,
        outdatedInstance: Xd(i, s),
      }
    : {
        type: l,
        hasDefinition: c,
        hasModuleEntry: !!r.getModuleEntryByLocalId(i.localId),
        hasLocalModuleError: An(e.componentForIdentifier(ao(i))),
        isModuleEvaluationPending: r.isLocalModuleEvaluationPending(i.localId),
        outdatedInstance: !1,
      };
}
function ih(t, e, n, o, r) {
  let i = Wi(t);
  if (!i) return;
  let a = Yd(i, e, n, o, r);
  if (!a) return;
  let {
    hasDefinition: s,
    hasModuleEntry: l,
    hasLocalModuleError: c,
    isModuleEvaluationPending: f,
    type: d,
    outdatedInstance: u,
  } = a;
  if (!s || !l)
    switch (d) {
      case "codeFile":
        if (l) {
          if (s) break;
          return { kind: "code", codeError: 2 };
        } else return { kind: "code", codeError: 5 };
      case "shader":
        if (l) {
          if (s) break;
          return { kind: "code", codeError: 4 };
        } else return { kind: "code", codeError: 6 };
      case "canvasComponent":
        return l && f && !c ? void 0 : { kind: "code", codeError: 3 };
      default:
        return { kind: "code", codeError: 1 };
    }
  if (u) return { kind: "outdatedInstance", originalNodeId: t.getPrimaryId() };
}
function ah(t, e, n, o, r) {
  if (!es(t)) return;
  let i = Yd(t.codeOverrideIdentifier, e, n, o, r);
  if (!i) return;
  let { hasDefinition: a, hasModuleEntry: s } = i;
  if (!a) return s ? { kind: "code", codeError: 7 } : { kind: "code", codeError: 8 };
}
function sh(t, e) {
  let n = e[t.id];
  if (n)
    switch (n.type) {
      case "Form":
        return { kind: "form", reason: n.reason };
      case "CodeGeneration":
        return n.reason === "FileTooLarge" ? { kind: "code", codeError: 0 } : void 0;
      default:
        Z(n.type);
    }
}
function iw(t, e, n, o, r, i) {
  return ih(t, e, n, o, r) || ah(t, e, n, o, r) || (i && sh(t, i));
}
function lh(t) {
  switch (t.type) {
    case "Form":
      return !0;
    case "CodeGeneration":
      return t.reason === "FileTooLarge";
    default:
      return !1;
  }
}
function aw(t) {
  let e = {},
    n = t.get(Ws);
  return (
    n &&
      n.children?.forEach((o) => {
        o.nodeId &&
          (!o.type ||
            !lh(o) ||
            !o.reason ||
            o.nodeId in e ||
            (e[o.nodeId] = { type: o.type, reason: o.reason }));
      }),
    e
  );
}
var ch = "no_destinations";
function dh(t) {
  if (!L(t)) return { destinationType: void 0, reason: "" };
  let e = t.indexOf(":");
  if (e === -1) return { destinationType: void 0, reason: t.trim().toLowerCase() };
  let n = t.slice(0, e).trim(),
    o = t
      .slice(e + 1)
      .split("|")[0]
      .trim()
      .toLowerCase();
  return { destinationType: n, reason: o };
}
function cw(t) {
  return dh(t).reason === ch;
}
var uh = oe("exportToHTML");
function Zd(t, e, n, o = new Set([n]), r = new Set()) {
  for (let { node: i, skipChildren: a } of n.walkWithSkipChildren()) {
    if (r.has(i)) {
      a();
      continue;
    }
    r.add(i);
    for (let s of Us(i, e)) {
      let l = As(s);
      if (!l) continue;
      let c = t.getNode(l);
      if (c) {
        if (!t.isGroundNode(c)) {
          uh.reportError("Navigation target node must be ground node", {
            sourceId: i.id,
            targetId: c.id,
          });
          continue;
        }
        (o.add(c), Zd(t, e, c, o, r));
      }
    }
    ve(i) &&
      Object.values(tl(t, i, e))
        .flat()
        .forEach((s) => Zd(t, e, s.node, o, r));
  }
  return o;
}
function yw(t, e) {
  let n = e.getPrimaryVariant();
  kr(n) && (t.routes[e.id] = n.pageEffects);
}
function vw(t, e, n) {
  let o = e.getPrimaryVariant();
  if (kr(o)) {
    let r = {};
    for (let i in o.pageEffects) r[i] = n.dedupe("effect", o.pageEffects[i]);
    t.routes[e.id] = r;
  }
}
var eu = "utils";
function tu(t, e) {
  let n = J(e);
  if (!it(n) || n.kind !== "localModuleExport") return;
  let o = t.getPersistedModuleByLocalId(n.localId);
  if (o && o.exports.includes(eu))
    return { exportIdentifier: eu, moduleURL: o.moduleURL, collectionId: n.localIdName };
}
function Tw(t, e) {
  if (x(e) && e.dataIdentifier) return tu(t, e.dataIdentifier);
}
function Lw(t, e, n) {
  for (let o of e.variables) {
    if (!Ls(o)) continue;
    let r = tu(t, o.dataIdentifier);
    r && n(r);
  }
}
var Pw = ks({
  canvas: () => {
    let t = fh();
    return (Xr(t), Qr(t));
  },
  onPageCanvas: () => {
    let t = ph();
    Xr(t);
    let { url: e, isCrossOriginProtected: n } = Qr(t),
      o = hh(e);
    return { url: o, origin: new URL(o).origin, isCrossOriginProtected: n };
  },
});
function fh() {
  let t = yt.isDebugBuild ? "canvas-sandbox.debug.html" : "canvas-sandbox.html",
    e = Br(`./${t}`),
    n = yt.debugPerformance ? `?${Lr}` : "",
    o = JSON.stringify(Tr());
  return e + n + `#services=${encodeURIComponent(o)}`;
}
function ph() {
  let t = yt.isDebugBuild ? "canvas-sandbox-on-page.debug.html" : "canvas-sandbox-on-page.html",
    e = Br(`./${t}`),
    n = yt.debugPerformance ? `?${Lr}` : "",
    o = JSON.stringify(Tr());
  return e + n + `#services=${encodeURIComponent(o)}`;
}
var mh = 63;
function hh(t) {
  let e = new URL(t);
  if (
    e.hostname === "localhost" ||
    e.hostname === "127.0.0.1" ||
    e.hostname.split(".").length - 2 >= 2
  )
    return t;
  let o = new URLSearchParams(window.location.search).get("framerSiteId");
  b(o, "onPageCanvas must be loaded with a framerSiteId");
  let r = `site-${o}`.slice(0, mh);
  return ((e.hostname = `${r}.${e.hostname}`), e.toString());
}
export {
  Zl as a,
  ec as b,
  gn as c,
  ai as d,
  xu as e,
  On as f,
  te as g,
  tr as h,
  vn as i,
  Wt as j,
  Sn as k,
  Le as l,
  _y as m,
  Wc as n,
  $c as o,
  dt as p,
  fp as q,
  Hb as r,
  mp as s,
  qb as t,
  hp as u,
  nT as v,
  aT as w,
  tc as x,
  nc as y,
  oc as z,
  Vh as A,
  Zc as B,
  Hn as C,
  qL as D,
  le as E,
  Me as F,
  Ni as G,
  qn as H,
  Jn as I,
  xt as J,
  lr as K,
  rN as L,
  Kn as M,
  TN as N,
  LN as O,
  NN as P,
  Sx as Q,
  Ox as R,
  $x as S,
  _x as T,
  hr as U,
  fR as V,
  pR as W,
  mR as X,
  hR as Y,
  gR as Z,
  yR as _,
  vR as $,
  SR as aa,
  CR as ba,
  IR as ca,
  bR as da,
  Nm as ea,
  xm as fa,
  TR as ga,
  wd as ha,
  Pd as ia,
  Jt as ja,
  LR as ka,
  Wm as la,
  Gm as ma,
  jm as na,
  qd as oa,
  qm as pa,
  Jm as qa,
  NR as ra,
  xR as sa,
  DR as ta,
  RR as ua,
  MR as va,
  kR as wa,
  Zd as xa,
  yw as ya,
  vw as za,
  tu as Aa,
  Tw as Ba,
  Lw as Ca,
  BL as Da,
  FL as Ea,
  Ep as Fa,
  AL as Ga,
  Pw as Ha,
  mh as Ia,
  AR as Ja,
  Kd as Ka,
  WR as La,
  KR as Ma,
  iw as Na,
  aw as Oa,
  ch as Pa,
  dh as Qa,
  cw as Ra,
};
//# sourceMappingURL=chunk-JEF7BPYT.mjs.map
