import {
  $a as au,
  $d as Nu,
  $f as vi,
  $g as lr,
  $k as Fc,
  $m as Zo,
  $s as Da,
  Ad as jt,
  Af as Ou,
  Ai as nc,
  Ak as Ic,
  Am as Fi,
  An as da,
  As as $i,
  Bc as Co,
  Bd as Si,
  Bf as Hu,
  Bk as Oo,
  Bm as Ac,
  Bx as rt,
  Cc as To,
  Cj as Ao,
  Ck as Ii,
  Cm as Bi,
  Ct as Mf,
  Df as Gu,
  Di as oc,
  Dj as mc,
  Dk as wi,
  Dl as Dn,
  Dm as Nn,
  Dq as Jr,
  Dt as Ef,
  Ec as gi,
  Ed as wo,
  Ee as Es,
  Ej as hc,
  Eq as ff,
  Es as ka,
  Fc as Sr,
  Fg as Uu,
  Fi as ac,
  Fj as pc,
  Fn as qc,
  Fq as mf,
  Gc as In,
  Ge as Bo,
  Gf as br,
  Gj as gc,
  Gl as Go,
  Go as ha,
  Gq as Zs,
  Gw as Kf,
  Ha as dt,
  Hc as _o,
  Hh as Ze,
  Hm as Dr,
  Ho as ga,
  Hq as _a,
  Hs as qi,
  Ia as Ur,
  Ie as As,
  Ih as Ju,
  Ij as yc,
  Il as wr,
  Im as zc,
  Is as Ui,
  Ja as hi,
  Je as zs,
  Ji as Gs,
  Jj as Ks,
  Jm as Oc,
  Js as Pa,
  Kd as Su,
  Kl as $o,
  Kp as rf,
  Kq as hf,
  Ks as kf,
  Kw as Ba,
  La as eu,
  Ld as Rt,
  Le as Vo,
  Lh as Yr,
  Li as Zt,
  Lm as Nr,
  Lq as pf,
  Ma as re,
  Mc as wn,
  Md as xu,
  Mk as Pr,
  Ms as Ia,
  Na as bo,
  Nh as yt,
  Ni as $s,
  Nj as Pi,
  Nl as qo,
  Nn as Vi,
  Np as nf,
  Nq as gf,
  Oe as Ft,
  Oh as Zu,
  Oi as sc,
  Oo as ya,
  Op as of,
  Os as Pf,
  Pe as Ru,
  Pi as qs,
  Pk as Di,
  Pm as Xo,
  Pp as af,
  Q as Nd,
  Qa as nu,
  Qc as ze,
  Qd as xr,
  Qe as Fu,
  Qp as sf,
  Qs,
  Rc as pu,
  Re as Bu,
  Rf as sr,
  Rh as Os,
  Rq as yf,
  Rs as wa,
  S as ho,
  Sa as ou,
  Sc as Yt,
  Sm as Ko,
  Ss as If,
  Sv as Af,
  Td as Ms,
  Tg as et,
  Th as Ke,
  Ti,
  Tj as Ys,
  Tk as Wc,
  Tn as Li,
  Ts as wf,
  Tv as zf,
  Ub as Ls,
  Uc as ko,
  Ud as Do,
  Ug as Cr,
  Uh as Qu,
  Uk as Dc,
  Un as ua,
  V as Rd,
  Vd as Wu,
  Vg as Xu,
  Vh as ec,
  Vj as Sc,
  Vk as Nc,
  Vm as Yo,
  Vx as Yf,
  Wh as tc,
  Wi as _i,
  Wl as Ct,
  Wo as Sa,
  Xd as Du,
  Xg as bi,
  Xh as He,
  Xs as Wf,
  Xy as jf,
  Yd as No,
  Yk as Rc,
  Ym as Jo,
  Yn as Uc,
  Ys as Wa,
  Zf as qu,
  Zj as _r,
  Zl as Ri,
  Zq as Sf,
  _ as $d,
  _c as gu,
  _g as Ci,
  _k as Ni,
  ad as Nt,
  af as Mu,
  ah as Tr,
  ao as ca,
  ap as Ei,
  ar as xf,
  at as Df,
  bt as Nf,
  bw as Fa,
  ce as Ro,
  cf as Eu,
  cl as jr,
  cn as Qo,
  cr as Fr,
  cw as De,
  de as xi,
  dh as Ku,
  dl as Ir,
  dt as Rf,
  ec as fu,
  ek as Vt,
  eo as Xc,
  ep as xa,
  et as Ff,
  fe as Fo,
  ff as Au,
  fh as Yu,
  fj as lc,
  fm as Bc,
  fn as ta,
  gc as mu,
  gj as dc,
  gk as er,
  gl as Ho,
  gt as Xi,
  hf as Et,
  hp as va,
  ht as Na,
  ik as js,
  in as Hc,
  iq as lf,
  j as fo,
  jb as pi,
  jm as Vc,
  jt as Ki,
  k as Id,
  kj as Eo,
  kp as ba,
  kr as vf,
  kt as Ra,
  kw as Hf,
  ld as yi,
  lf as Lo,
  lj as Us,
  lk as Tc,
  lm as Lc,
  ln as ia,
  lr as dr,
  lt as Yi,
  lw as tl,
  ma as gr,
  mb as su,
  md as Po,
  mj as uc,
  mk as _c,
  mm as Uo,
  mo as Kc,
  mr as Rn,
  mt as Bf,
  mw as Gf,
  n as mo,
  nd as yu,
  nj as ki,
  nr as Br,
  nw as $f,
  oa as Jd,
  od as bt,
  oh as ju,
  oj as Xs,
  on as na,
  oq as df,
  ot as Vf,
  ow as qf,
  pa as fi,
  pb as lu,
  pf as Mo,
  pj as cc,
  pp as Ca,
  pt as el,
  pw as Uf,
  qa as Zd,
  qj as fc,
  qk as kc,
  qs as _f,
  qt as Lf,
  ra as yr,
  rn as oa,
  so as Yc,
  sw as Xf,
  to as Zc,
  tq as Ta,
  u as vn,
  ub as du,
  ud as fe,
  uk as kr,
  un as aa,
  uo as Qc,
  up as We,
  va as mi,
  vm as Wr,
  vo as ef,
  vp as tf,
  vq as uf,
  wd as Mt,
  wq as Rr,
  ws as tt,
  xc as hu,
  xd as Ne,
  xl as Js,
  xm as Mc,
  xn as la,
  xq as zi,
  y as wd,
  yd as de,
  yi as rc,
  yq as Oi,
  z as Wd,
  zd as Io,
  zi as ic,
  zk as Pc,
  zm as Ec,
  zo as ma,
  zq as cf,
  zs as Gi,
} from "chunk-LBKZPB25.mjs";
import {
  $b as It,
  $g as Md,
  $h as xo,
  $j as Xe,
  A as Or,
  Ab as $r,
  Ai as Yd,
  B as Hr,
  Bb as qr,
  C as Is,
  Ca as ws,
  Cb as xd,
  Cd as Te,
  Db as yn,
  Dd as Rs,
  Dj as vu,
  Dk as wu,
  Dl as xc,
  E as st,
  Eb as Sn,
  Fb as ri,
  Fj as bu,
  Gb as xn,
  Gh as yo,
  Gk as Jt,
  Hk as vr,
  I as Ql,
  Ii as vo,
  Il as vc,
  Im as Gc,
  Kd as oi,
  Kk as Wo,
  Lm as $c,
  Mk as ut,
  Mm as fa,
  Nd as Fd,
  Nl as bc,
  Oa as Ie,
  Om as jc,
  Pl as Cc,
  Pm as Jc,
  Rb as vd,
  Rd as le,
  Sb as or,
  Sc as Fe,
  Sk as Vu,
  So as Vr,
  T as ed,
  Tb as co,
  Tc as po,
  Tj as Cu,
  Tm as pa,
  U as td,
  Ub as Ns,
  Ui as jd,
  Uk as Lu,
  Vb as bd,
  Vi as qe,
  Vl as Wi,
  Wb as ii,
  Wl as wc,
  Xb as ni,
  Xi as Ue,
  Xj as Tu,
  Yd as Bd,
  Yh as qd,
  Yj as Kr,
  Yk as zu,
  Ym as Mi,
  Zb as Wt,
  Zd as bn,
  Zi as Qd,
  Zj as _u,
  Zl as jo,
  _a as ld,
  _b as Ut,
  _h as So,
  ac as Cd,
  ae as pr,
  ah as Ed,
  aj as ar,
  ak as Oe,
  bb as so,
  bh as Ad,
  bj as tu,
  bm as ea,
  c as $t,
  cb as dd,
  cc as Dt,
  cf as kn,
  cn as Ai,
  dc as Xt,
  de as Fs,
  di as ui,
  e as Ps,
  ee as ai,
  f as Qe,
  fb as gn,
  fe as Cn,
  fi as Vs,
  fm as ra,
  gb as Ws,
  gc as Td,
  ge as go,
  gj as ru,
  hb as ir,
  hc as _d,
  he as si,
  ia as rd,
  ib as Ds,
  ij as iu,
  jb as ud,
  jh as lt,
  ji as Kt,
  jk as we,
  jm as sa,
  ki as Ud,
  l as zr,
  lb as cd,
  ln as bf,
  m as mt,
  ma as id,
  mb as fd,
  me as Tn,
  mj as Xr,
  mn as Hi,
  nb as md,
  ni as ci,
  nl as Qt,
  nn as Cf,
  on as Tf,
  pb as hd,
  pe as Bs,
  qb as nr,
  qc as kd,
  ql as zo,
  rb as Gr,
  rc as Pd,
  rk as ku,
  sb as pd,
  si as Xd,
  t as jl,
  tb as gd,
  te as _n,
  th as zd,
  ua as no,
  ub as yd,
  uj as uu,
  uk as Pu,
  v as Jl,
  vb as Sd,
  vh as Je,
  vk as Iu,
  w as Zl,
  wb as lo,
  we as Vd,
  wi as Kd,
  wj as cu,
  yf as di,
  yh as Od,
  zb as uo,
  ze as li,
} from "chunk-KZ3J5BLF.mjs";
import { c as Pn, f as Hd, h as Gd } from "chunk-UYIYJ4FN.mjs";
import { u as Hs, w as Wn } from "chunk-KYPY4QS7.mjs";
import { a as Of } from "chunk-MYL546Y2.mjs";
import { f as ht } from "chunk-CPQVC4NV.mjs";
import { P as Kl, V as Pt, g as Xl, ka as $u } from "chunk-T6DXIA74.mjs";
import { d as Bt } from "chunk-4SNIJDUQ.mjs";
import { a as nd, b as od } from "chunk-WRBBN7SY.mjs";
import { a as $e } from "chunk-7BWPFUCH.mjs";
import { a as Ld } from "chunk-6TFWVVAP.mjs";
import { b as Dd } from "chunk-YSP5ZHDJ.mjs";
import { a as Yl } from "chunk-2FCXHKEL.mjs";
import { q as ao } from "chunk-6IRUXYG7.mjs";
import {
  b as pe,
  e as ie,
  f as oo,
  h as ad,
  m as J,
  n as qt,
  o as pn,
  p as sd,
} from "chunk-LA34HORX.mjs";
import { a as Hg } from "chunk-SWYZG2NI.mjs";
import { b as ee, c as Pe } from "chunk-4JY5UMT2.mjs";
import { b as Be, h as ti } from "chunk-KPMZENE5.mjs";
import { a as Ul } from "chunk-YRQ7G4QH.mjs";
import { c as _s, e as ks } from "chunk-WLHSDIGQ.mjs";
var om = _s(() => {
  "use strict";
});
var am = _s(() => {
  "use strict";
});
var lm = _s((sm, ji) => {
  "use strict";
  var Se = function (i, t) {
    i = i || om();
    var n = i.window,
      a = i.document,
      s = new (function () {
        var e = /^(statics|enumerable|beans|preserve)$/,
          r = [],
          o = r.slice,
          l = Object.create,
          d = Object.getOwnPropertyDescriptor,
          c = Object.defineProperty,
          h =
            r.forEach ||
            function (S, v) {
              for (var w = 0, _ = this.length; w < _; w++) S.call(v, this[w], w, this);
            },
          m = function (S, v) {
            for (var w in this) this.hasOwnProperty(w) && S.call(v, this[w], w, this);
          },
          p =
            Object.assign ||
            function (S) {
              for (var v = 1, w = arguments.length; v < w; v++) {
                var _ = arguments[v];
                for (var P in _) _.hasOwnProperty(P) && (S[P] = _[P]);
              }
              return S;
            },
          y = function (S, v, w) {
            if (S) {
              var _ = d(S, "length");
              (_ && typeof _.value == "number" ? h : m).call(S, v, (w = w || S));
            }
            return w;
          };
        function x(S, v, w, _, P) {
          var D = {};
          function I(B, V) {
            ((V = V || ((V = d(v, B)) && (V.get ? V : V.value))),
              typeof V == "string" && V[0] === "#" && (V = S[V.substring(1)] || V));
            var L = typeof V == "function",
              M = V,
              z = P || (L && !V.base) ? (V && V.get ? B in S : S[B]) : null,
              G;
            (!P || !z) &&
              (L && z && (V.base = z),
              L &&
                _ !== !1 &&
                (G = B.match(/^([gs]et|is)(([A-Z])(.*))$/)) &&
                (D[G[3].toLowerCase() + G[4]] = G[2]),
              (!M || L || !M.get || typeof M.get != "function" || !b.isPlainObject(M)) &&
                (M = { value: M, writable: !0 }),
              (d(S, B) || { configurable: !0 }).configurable &&
                ((M.configurable = !0), (M.enumerable = w ?? !G)),
              c(S, B, M));
          }
          if (v) {
            for (var C in v) v.hasOwnProperty(C) && !e.test(C) && I(C);
            for (var C in D) {
              var T = D[C],
                W = S["set" + T],
                N = S["get" + T] || (W && S["is" + T]);
              N && (_ === !0 || N.length === 0) && I(C, { get: N, set: W });
            }
          }
          return S;
        }
        function b() {
          for (var S = 0, v = arguments.length; S < v; S++) {
            var w = arguments[S];
            w && p(this, w);
          }
          return this;
        }
        return x(b, {
          inject: function (S) {
            if (S) {
              var v = S.statics === !0 ? S : S.statics,
                w = S.beans,
                _ = S.preserve;
              (v !== S && x(this.prototype, S, S.enumerable, w, _), x(this, v, null, w, _));
            }
            for (var P = 1, D = arguments.length; P < D; P++) this.inject(arguments[P]);
            return this;
          },
          extend: function () {
            for (var S = this, v, w, _ = 0, P, D = arguments.length; _ < D && !(v && w); _++)
              ((P = arguments[_]), (v = v || P.initialize), (w = w || P.prototype));
            return (
              (v =
                v ||
                function () {
                  S.apply(this, arguments);
                }),
              (w = v.prototype = w || l(this.prototype)),
              c(w, "constructor", { value: v, writable: !0, configurable: !0 }),
              x(v, this),
              arguments.length && this.inject.apply(v, arguments),
              (v.base = S),
              v
            );
          },
        }).inject({
          enumerable: !1,
          initialize: b,
          set: b,
          inject: function () {
            for (var S = 0, v = arguments.length; S < v; S++) {
              var w = arguments[S];
              w && x(this, w, w.enumerable, w.beans, w.preserve);
            }
            return this;
          },
          extend: function () {
            var S = l(this);
            return S.inject.apply(S, arguments);
          },
          each: function (S, v) {
            return y(this, S, v);
          },
          clone: function () {
            return new this.constructor(this);
          },
          statics: {
            set: p,
            each: y,
            create: l,
            define: c,
            describe: d,
            clone: function (S) {
              return p(new S.constructor(), S);
            },
            isPlainObject: function (S) {
              var v = S != null && S.constructor;
              return v && (v === Object || v === b || v.name === "Object");
            },
            pick: function (S, v) {
              return S !== t ? S : v;
            },
            slice: function (S, v, w) {
              return o.call(S, v, w);
            },
          },
        });
      })();
    (typeof ji < "u" && (ji.exports = s),
      s.inject(
        {
          enumerable: !1,
          toString: function () {
            return this._id != null
              ? (this._class || "Object") + (this._name ? " '" + this._name + "'" : " @" + this._id)
              : "{ " +
                  s
                    .each(
                      this,
                      function (e, r) {
                        if (!/^_/.test(r)) {
                          var o = typeof e;
                          this.push(
                            r +
                              ": " +
                              (o === "number"
                                ? k.instance.number(e)
                                : o === "string"
                                  ? "'" + e + "'"
                                  : e)
                          );
                        }
                      },
                      []
                    )
                    .join(", ") +
                  " }";
          },
          getClassName: function () {
            return this._class || "";
          },
          importJSON: function (e) {
            return s.importJSON(e, this);
          },
          exportJSON: function (e) {
            return s.exportJSON(this, e);
          },
          toJSON: function () {
            return s.serialize(this);
          },
          set: function (e, r) {
            return (e && s.filter(this, e, r, this._prioritize), this);
          },
        },
        {
          beans: !1,
          statics: {
            exports: {},
            extend: function e() {
              var r = e.base.apply(this, arguments),
                o = r.prototype._class;
              return (o && !s.exports[o] && (s.exports[o] = r), r);
            },
            equals: function (e, r) {
              if (e === r) return !0;
              if (e && e.equals) return e.equals(r);
              if (r && r.equals) return r.equals(e);
              if (e && r && typeof e == "object" && typeof r == "object") {
                if (Array.isArray(e) && Array.isArray(r)) {
                  var o = e.length;
                  if (o !== r.length) return !1;
                  for (; o--;) if (!s.equals(e[o], r[o])) return !1;
                } else {
                  var l = Object.keys(e),
                    o = l.length;
                  if (o !== Object.keys(r).length) return !1;
                  for (; o--;) {
                    var d = l[o];
                    if (!(r.hasOwnProperty(d) && s.equals(e[d], r[d]))) return !1;
                  }
                }
                return !0;
              }
              return !1;
            },
            read: function (e, r, o, l) {
              if (this === s) {
                var d = this.peek(e, r);
                return (e.__index++, d);
              }
              var c = this.prototype,
                h = c._readIndex,
                m = r || (h && e.__index) || 0,
                p = e.length,
                y = e[m];
              if (((l = l || p - m), y instanceof this || (o && o.readNull && y == null && l <= 1)))
                return (h && (e.__index = m + 1), y && o && o.clone ? y.clone() : y);
              if (
                ((y = s.create(c)),
                h && (y.__read = !0),
                (y = y.initialize.apply(y, m > 0 || m + l < p ? s.slice(e, m, m + l) : e) || y),
                h)
              ) {
                e.__index = m + y.__read;
                var x = y.__filtered;
                (x && ((e.__filtered = x), (y.__filtered = t)), (y.__read = t));
              }
              return y;
            },
            peek: function (e, r) {
              return e[(e.__index = r || e.__index || 0)];
            },
            remain: function (e) {
              return e.length - (e.__index || 0);
            },
            readList: function (e, r, o, l) {
              for (var d = [], c, h = r || 0, m = l ? h + l : e.length, p = h; p < m; p++)
                d.push(Array.isArray((c = e[p])) ? this.read(c, 0, o) : this.read(e, p, o, 1));
              return d;
            },
            readNamed: function (e, r, o, l, d) {
              var c = this.getNamed(e, r),
                h = c !== t;
              if (h) {
                var m = e.__filtered;
                (m || ((m = e.__filtered = s.create(e[0])), (m.__unfiltered = e[0])), (m[r] = t));
              }
              var p = h ? [c] : e,
                y = this.read(p, o, l, d);
              return y;
            },
            getNamed: function (e, r) {
              var o = e[0];
              if (
                (e._hasObject === t && (e._hasObject = e.length === 1 && s.isPlainObject(o)),
                e._hasObject)
              )
                return r ? o[r] : e.__filtered || o;
            },
            hasNamed: function (e, r) {
              return !!this.getNamed(e, r);
            },
            filter: function (e, r, o, l) {
              var d;
              function c(x) {
                if (!(o && x in o) && !(d && x in d)) {
                  var b = r[x];
                  b !== t && (e[x] = b);
                }
              }
              if (l) {
                for (var h = {}, m = 0, p, y = l.length; m < y; m++)
                  (p = l[m]) in r && (c(p), (h[p] = !0));
                d = h;
              }
              return (Object.keys(r.__unfiltered || r).forEach(c), e);
            },
            isPlainValue: function (e, r) {
              return s.isPlainObject(e) || Array.isArray(e) || (r && typeof e == "string");
            },
            serialize: function (e, r, o, l) {
              r = r || {};
              var d = !l,
                c;
              if (
                (d &&
                  ((r.formatter = new k(r.precision)),
                  (l = {
                    length: 0,
                    definitions: {},
                    references: {},
                    add: function (b, S) {
                      var v = "#" + b._id,
                        w = this.references[v];
                      if (!w) {
                        this.length++;
                        var _ = S.call(b),
                          P = b._class;
                        (P && _[0] !== P && _.unshift(P),
                          (this.definitions[v] = _),
                          (w = this.references[v] = [v]));
                      }
                      return w;
                    },
                  })),
                e && e._serialize)
              ) {
                c = e._serialize(r, l);
                var h = e._class;
                h && !e._compactSerialize && (d || !o) && c[0] !== h && c.unshift(h);
              } else if (Array.isArray(e)) {
                c = [];
                for (var m = 0, p = e.length; m < p; m++) c[m] = s.serialize(e[m], r, o, l);
              } else if (s.isPlainObject(e)) {
                c = {};
                for (var y = Object.keys(e), m = 0, p = y.length; m < p; m++) {
                  var x = y[m];
                  c[x] = s.serialize(e[x], r, o, l);
                }
              } else typeof e == "number" ? (c = r.formatter.number(e, r.precision)) : (c = e);
              return d && l.length > 0 ? [["dictionary", l.definitions], c] : c;
            },
            deserialize: function (e, r, o, l, d) {
              var c = e,
                h = !o,
                m = h && e && e.length && e[0][0] === "dictionary";
              if (((o = o || {}), Array.isArray(e))) {
                var p = e[0],
                  y = p === "dictionary";
                if (e.length == 1 && /^#/.test(p)) return o.dictionary[p];
                ((p = s.exports[p]), (c = []));
                for (var x = p ? 1 : 0, b = e.length; x < b; x++)
                  c.push(s.deserialize(e[x], r, o, y, m));
                if (p) {
                  var S = c;
                  r ? (c = r(p, S, h || d)) : ((c = s.create(p.prototype)), p.apply(c, S));
                }
              } else if (s.isPlainObject(e)) {
                ((c = {}), l && (o.dictionary = c));
                for (var v in e) c[v] = s.deserialize(e[v], r, o);
              }
              return m ? c[1] : c;
            },
            exportJSON: function (e, r) {
              var o = s.serialize(e, r);
              return r && r.asString == !1 ? o : JSON.stringify(o);
            },
            importJSON: function (e, r) {
              return s.deserialize(typeof e == "string" ? JSON.parse(e) : e, function (o, l, d) {
                var c = d && r && r.constructor === o,
                  h = c ? r : s.create(o.prototype);
                if (l.length === 1 && h instanceof ne && (c || !(h instanceof ft))) {
                  var m = l[0];
                  s.isPlainObject(m) && (m.insert = !1);
                }
                return ((c ? h.set : o).apply(h, l), c && (r = null), h);
              });
            },
            splice: function (e, r, o, l) {
              var d = r && r.length,
                c = o === t;
              ((o = c ? e.length : o), o > e.length && (o = e.length));
              for (var h = 0; h < d; h++) r[h]._index = o + h;
              if (c) return (e.push.apply(e, r), []);
              var m = [o, l];
              r && m.push.apply(m, r);
              for (var p = e.splice.apply(e, m), h = 0, y = p.length; h < y; h++) p[h]._index = t;
              for (var h = o + d, y = e.length; h < y; h++) e[h]._index = h;
              return p;
            },
            capitalize: function (e) {
              return e.replace(/\b[a-z]/g, function (r) {
                return r.toUpperCase();
              });
            },
            camelize: function (e) {
              return e.replace(/-(.)/g, function (r, o) {
                return o.toUpperCase();
              });
            },
            hyphenate: function (e) {
              return e.replace(/([a-z])([A-Z])/g, "$1-$2").toLowerCase();
            },
          },
        }
      ));
    var u = {
        on: function (e, r) {
          if (typeof e != "string")
            s.each(
              e,
              function (c, h) {
                this.on(h, c);
              },
              this
            );
          else {
            var o = this._eventTypes,
              l = o && o[e],
              d = (this._callbacks = this._callbacks || {});
            ((d = d[e] = d[e] || []),
              d.indexOf(r) === -1 &&
                (d.push(r), l && l.install && d.length === 1 && l.install.call(this, e)));
          }
          return this;
        },
        off: function (e, r) {
          if (typeof e != "string") {
            s.each(
              e,
              function (h, m) {
                this.off(m, h);
              },
              this
            );
            return;
          }
          var o = this._eventTypes,
            l = o && o[e],
            d = this._callbacks && this._callbacks[e],
            c;
          return (
            d &&
              (!r || ((c = d.indexOf(r)) !== -1 && d.length === 1)
                ? (l && l.uninstall && l.uninstall.call(this, e), delete this._callbacks[e])
                : c !== -1 && d.splice(c, 1)),
            this
          );
        },
        once: function (e, r) {
          return this.on(e, function () {
            (r.apply(this, arguments), this.off(e, r));
          });
        },
        emit: function (e, r) {
          var o = this._callbacks && this._callbacks[e];
          if (!o) return !1;
          var l = s.slice(arguments, 1),
            d = r && r.target && !r.currentTarget;
          ((o = o.slice()), d && (r.currentTarget = this));
          for (var c = 0, h = o.length; c < h; c++)
            if (o[c].apply(this, l) == !1) {
              r && r.stop && r.stop();
              break;
            }
          return (d && delete r.currentTarget, !0);
        },
        responds: function (e) {
          return !!(this._callbacks && this._callbacks[e]);
        },
        attach: "#on",
        detach: "#off",
        fire: "#emit",
        _installEvents: function (e) {
          var r = this._eventTypes,
            o = this._callbacks,
            l = e ? "install" : "uninstall";
          if (r) {
            for (var d in o)
              if (o[d].length > 0) {
                var c = r[d],
                  h = c && c[l];
                h && h.call(this, d);
              }
          }
        },
        statics: {
          inject: function e(r) {
            var o = r._events;
            if (o) {
              var l = {};
              (s.each(o, function (d, c) {
                var h = typeof d == "string",
                  m = h ? d : c,
                  p = s.capitalize(m),
                  y = m.substring(2).toLowerCase();
                ((l[y] = h ? {} : d),
                  (m = "_" + m),
                  (r["get" + p] = function () {
                    return this[m];
                  }),
                  (r["set" + p] = function (x) {
                    var b = this[m];
                    (b && this.off(y, b), x && this.on(y, x), (this[m] = x));
                  }));
              }),
                (r._eventTypes = l));
            }
            return e.base.apply(this, arguments);
          },
        },
      },
      f = s.extend({
        _class: "PaperScope",
        initialize: function e() {
          ((Se = this),
            (this.settings = new s({
              applyMatrix: !0,
              insertItems: !0,
              handleSize: 4,
              hitTolerance: 0,
            })),
            (this.project = null),
            (this.projects = []),
            (this.tools = []),
            (this._id = e._id++),
            (e._scopes[this._id] = this));
          var r = e.prototype;
          if (!this.support) {
            var o = je.getContext(1, 1) || {};
            ((r.support = {
              nativeDash: "setLineDash" in o || "mozDash" in o,
              nativeBlendModes: Ts.nativeModes,
            }),
              je.release(o));
          }
          if (!this.agent) {
            var l = i.navigator.userAgent.toLowerCase(),
              d = (/(darwin|win|mac|linux|freebsd|sunos)/.exec(l) || [])[0],
              c = d === "darwin" ? "mac" : d,
              h = (r.agent = r.browser = { platform: c });
            (c && (h[c] = !0),
              l.replace(
                /(opera|chrome|safari|webkit|firefox|msie|trident|atom|node)\/?\s*([.\d]+)(?:.*version\/([.\d]+))?(?:.*rv\:v?([.\d]+))?/g,
                function (m, p, y, x, b) {
                  if (!h.chrome) {
                    var S = p === "opera" ? x : /^(node|trident)$/.test(p) ? b : y;
                    ((h.version = S),
                      (h.versionNumber = parseFloat(S)),
                      (p = p === "trident" ? "msie" : p),
                      (h.name = p),
                      (h[p] = !0));
                  }
                }
              ),
              h.chrome && delete h.webkit,
              h.atom && delete h.chrome);
          }
        },
        version: "0.11.5-viewless",
        getView: function () {
          var e = this.project;
          return e && e._view;
        },
        getPaper: function () {
          return this;
        },
        execute: function (e, r) {
          (Se.PaperScript.execute(e, this, r), Le.updateFocus());
        },
        install: function (e) {
          var r = this;
          s.each(["project", "view", "tool"], function (l) {
            s.define(e, l, {
              configurable: !0,
              get: function () {
                return r[l];
              },
            });
          });
          for (var o in this) !/^_/.test(o) && this[o] && (e[o] = this[o]);
        },
        setup: function (e) {
          return ((Se = this), (this.project = new ct(e)), this);
        },
        createCanvas: function (e, r) {
          return je.getCanvas(e, r);
        },
        activate: function () {
          Se = this;
        },
        clear: function () {
          for (var e = this.projects, r = this.tools, o = e.length - 1; o >= 0; o--) e[o].remove();
          for (var o = r.length - 1; o >= 0; o--) r[o].remove();
        },
        remove: function () {
          (this.clear(), delete f._scopes[this._id]);
        },
        statics: new (function () {
          function e(r) {
            return (
              (r += "Attribute"),
              function (o, l) {
                return o[r](l) || o[r]("data-paper-" + l);
              }
            );
          }
          return {
            _scopes: {},
            _id: 0,
            get: function (r) {
              return this._scopes[r] || null;
            },
            getAttribute: e("get"),
            hasAttribute: e("has"),
          };
        })(),
      }),
      g = s.extend(u, {
        initialize: function (e) {
          ((this._scope = Se),
            (this._index = this._scope[this._list].push(this) - 1),
            (e || !this._scope[this._reference]) && this.activate());
        },
        activate: function () {
          if (!this._scope) return !1;
          var e = this._scope[this._reference];
          return (
            e && e !== this && e.emit("deactivate"),
            (this._scope[this._reference] = this),
            this.emit("activate", e),
            !0
          );
        },
        isActive: function () {
          return this._scope[this._reference] === this;
        },
        remove: function () {
          return this._index == null
            ? !1
            : (s.splice(this._scope[this._list], null, this._index, 1),
              this._scope[this._reference] == this && (this._scope[this._reference] = null),
              (this._scope = null),
              !0);
        },
        getView: function () {
          return this._scope.getView();
        },
      }),
      k = s.extend({
        initialize: function (e) {
          ((this.precision = s.pick(e, 5)), (this.multiplier = Math.pow(10, this.precision)));
        },
        number: function (e) {
          return this.precision < 16 ? Math.round(e * this.multiplier) / this.multiplier : e;
        },
        pair: function (e, r, o) {
          return this.number(e) + (o || ",") + this.number(r);
        },
        point: function (e, r) {
          return this.number(e.x) + (r || ",") + this.number(e.y);
        },
        size: function (e, r) {
          return this.number(e.width) + (r || ",") + this.number(e.height);
        },
        rectangle: function (e, r) {
          return this.point(e, r) + (r || ",") + this.size(e, r);
        },
      });
    k.instance = new k();
    var F = new (function () {
        var e = [
            [0.5773502691896257],
            [0, 0.7745966692414834],
            [0.33998104358485626, 0.8611363115940526],
            [0, 0.5384693101056831, 0.906179845938664],
            [0.2386191860831969, 0.6612093864662645, 0.932469514203152],
            [0, 0.4058451513773972, 0.7415311855993945, 0.9491079123427585],
            [0.1834346424956498, 0.525532409916329, 0.7966664774136267, 0.9602898564975363],
            [0, 0.3242534234038089, 0.6133714327005904, 0.8360311073266358, 0.9681602395076261],
            [
              0.14887433898163122, 0.4333953941292472, 0.6794095682990244, 0.8650633666889845,
              0.9739065285171717,
            ],
            [
              0, 0.26954315595234496, 0.5190961292068118, 0.7301520055740494, 0.8870625997680953,
              0.978228658146057,
            ],
            [
              0.1252334085114689, 0.3678314989981802, 0.5873179542866175, 0.7699026741943047,
              0.9041172563704749, 0.9815606342467192,
            ],
            [
              0, 0.2304583159551348, 0.44849275103644687, 0.6423493394403402, 0.8015780907333099,
              0.9175983992229779, 0.9841830547185881,
            ],
            [
              0.10805494870734367, 0.31911236892788974, 0.5152486363581541, 0.6872929048116855,
              0.827201315069765, 0.9284348836635735, 0.9862838086968123,
            ],
            [
              0, 0.20119409399743451, 0.3941513470775634, 0.5709721726085388, 0.7244177313601701,
              0.8482065834104272, 0.937273392400706, 0.9879925180204854,
            ],
            [
              0.09501250983763744, 0.2816035507792589, 0.45801677765722737, 0.6178762444026438,
              0.755404408355003, 0.8656312023878318, 0.9445750230732326, 0.9894009349916499,
            ],
          ],
          r = [
            [1],
            [0.8888888888888888, 0.5555555555555556],
            [0.6521451548625461, 0.34785484513745385],
            [0.5688888888888889, 0.47862867049936647, 0.23692688505618908],
            [0.46791393457269104, 0.3607615730481386, 0.17132449237917036],
            [0.4179591836734694, 0.3818300505051189, 0.27970539148927664, 0.1294849661688697],
            [0.362683783378362, 0.31370664587788727, 0.22238103445337448, 0.10122853629037626],
            [
              0.3302393550012598, 0.31234707704000286, 0.26061069640293544, 0.1806481606948574,
              0.08127438836157441,
            ],
            [
              0.29552422471475287, 0.26926671930999635, 0.21908636251598204, 0.1494513491505806,
              0.06667134430868814,
            ],
            [
              0.2729250867779006, 0.26280454451024665, 0.23319376459199048, 0.18629021092773426,
              0.1255803694649046, 0.05566856711617366,
            ],
            [
              0.24914704581340277, 0.2334925365383548, 0.20316742672306592, 0.16007832854334622,
              0.10693932599531843, 0.04717533638651183,
            ],
            [
              0.2325515532308739, 0.22628318026289723, 0.2078160475368885, 0.17814598076194574,
              0.13887351021978725, 0.09212149983772845, 0.04048400476531588,
            ],
            [
              0.2152638534631578, 0.2051984637212956, 0.18553839747793782, 0.15720316715819355,
              0.12151857068790319, 0.08015808715976021, 0.03511946033175186,
            ],
            [
              0.2025782419255613, 0.19843148532711158, 0.1861610000155622, 0.16626920581699392,
              0.13957067792615432, 0.10715922046717194, 0.07036604748810812, 0.03075324199611727,
            ],
            [
              0.1894506104550685, 0.18260341504492358, 0.16915651939500254, 0.14959598881657674,
              0.12462897125553388, 0.09515851168249279, 0.062253523938647894, 0.027152459411754096,
            ],
          ],
          o = Math.abs,
          l = Math.sqrt,
          d = Math.pow,
          c =
            Math.log2 ||
            function (b) {
              return Math.log(b) * Math.LOG2E;
            },
          h = 1e-12,
          m = 112e-18;
        function p(b, S, v) {
          return b < S ? S : b > v ? v : b;
        }
        function y(b, S, v) {
          function w(V) {
            var L = V * 134217729,
              M = V - L,
              z = M + L,
              G = V - z;
            return [z, G];
          }
          var _ = S * S - b * v,
            P = S * S + b * v;
          if (o(_) * 3 < P) {
            var D = w(b),
              I = w(S),
              C = w(v),
              T = S * S,
              W = I[0] * I[0] - T + 2 * I[0] * I[1] + I[1] * I[1],
              N = b * v,
              B = D[0] * C[0] - N + D[0] * C[1] + D[1] * C[0] + D[1] * C[1];
            _ = T - N + (W - B);
          }
          return _;
        }
        function x() {
          var b = Math.max.apply(Math, arguments);
          return b && (b < 1e-8 || b > 1e8) ? d(2, -Math.round(c(b))) : 0;
        }
        return {
          EPSILON: h,
          MACHINE_EPSILON: m,
          CURVETIME_EPSILON: 1e-8,
          GEOMETRIC_EPSILON: 1e-7,
          TRIGONOMETRIC_EPSILON: 1e-8,
          KAPPA: (4 * (l(2) - 1)) / 3,
          isZero: function (b) {
            return b >= -h && b <= h;
          },
          clamp: p,
          integrate: function (b, S, v, w) {
            for (
              var _ = e[w - 2],
                P = r[w - 2],
                D = (v - S) * 0.5,
                I = D + S,
                C = 0,
                T = (w + 1) >> 1,
                W = w & 1 ? P[C++] * b(I) : 0;
              C < T;
            ) {
              var N = D * _[C];
              W += P[C++] * (b(I + N) + b(I - N));
            }
            return D * W;
          },
          findRoot: function (b, S, v, w, _, P, D) {
            for (var I = 0; I < P; I++) {
              var C = b(v),
                T = C / S(v),
                W = v - T;
              if (o(T) < D) {
                v = W;
                break;
              }
              C > 0
                ? ((_ = v), (v = W <= w ? (w + _) * 0.5 : W))
                : ((w = v), (v = W >= _ ? (w + _) * 0.5 : W));
            }
            return p(v, w, _);
          },
          solveQuadratic: function (b, S, v, w, _, P) {
            var D,
              I = 1 / 0;
            if (o(b) < h) {
              if (o(S) < h) return o(v) < h ? -1 : 0;
              D = -v / S;
            } else {
              S *= -0.5;
              var C = y(b, S, v);
              if (C && o(C) < m) {
                var T = x(o(b), o(S), o(v));
                T && ((b *= T), (S *= T), (v *= T), (C = y(b, S, v)));
              }
              if (C >= -m) {
                var W = C < 0 ? 0 : l(C),
                  N = S + (S < 0 ? -W : W);
                N === 0 ? ((D = v / b), (I = -D)) : ((D = N / b), (I = v / N));
              }
            }
            var B = 0,
              V = _ == null,
              L = _ - h,
              M = P + h;
            return (
              isFinite(D) && (V || (D > L && D < M)) && (w[B++] = V ? D : p(D, _, P)),
              I !== D && isFinite(I) && (V || (I > L && I < M)) && (w[B++] = V ? I : p(I, _, P)),
              B
            );
          },
          solveCubic: function (b, S, v, w, _, P, D) {
            var I = x(o(b), o(S), o(v), o(w)),
              C,
              T,
              W,
              N,
              B;
            I && ((b *= I), (S *= I), (v *= I), (w *= I));
            function V(Y) {
              C = Y;
              var K = b * C;
              ((T = K + S), (W = T * C + v), (N = (K + T) * C + W), (B = W * C + w));
            }
            if (o(b) < h) ((b = S), (T = v), (W = w), (C = 1 / 0));
            else if (o(w) < h) ((T = S), (W = v), (C = 0));
            else {
              V(-(S / b) / 3);
              var L = B / b,
                M = d(o(L), 1 / 3),
                z = L < 0 ? -1 : 1,
                G = -N / b,
                O = G > 0 ? 1.324717957244746 * Math.max(M, l(G)) : M,
                E = C - z * O;
              if (E !== C) {
                do (V(E), (E = N === 0 ? C : C - B / N / (1 + m)));
                while (z * E > z * C);
                o(b) * C * C > o(w / C) && ((W = -w / C), (T = (W - v) / C));
              }
            }
            var H = F.solveQuadratic(b, T, W, _, P, D),
              U = P == null;
            return (
              isFinite(C) &&
                (H === 0 || (H > 0 && C !== _[0] && C !== _[1])) &&
                (U || (C > P - h && C < D + h)) &&
                (_[H++] = U ? C : p(C, P, D)),
              H
            );
          },
        };
      })(),
      A = {
        _id: 1,
        _pools: {},
        get: function (e) {
          if (e) {
            var r = this._pools[e];
            return (r || (r = this._pools[e] = { _id: 1 }), r._id++);
          } else return this._id++;
        },
      },
      R = s.extend(
        {
          _class: "Point",
          _readIndex: !0,
          initialize: function (r, o) {
            var l = typeof r,
              d = this.__read,
              c = 0;
            if (l === "number") {
              var h = typeof o == "number";
              (this._set(r, h ? o : r), d && (c = h ? 2 : 1));
            } else if (l === "undefined" || r === null)
              (this._set(0, 0), d && (c = r === null ? 1 : 0));
            else {
              var m = l === "string" ? r.split(/[\s,]+/) || [] : r;
              ((c = 1),
                Array.isArray(m)
                  ? this._set(+m[0], +(m.length > 1 ? m[1] : m[0]))
                  : "x" in m
                    ? this._set(m.x || 0, m.y || 0)
                    : "width" in m
                      ? this._set(m.width || 0, m.height || 0)
                      : "angle" in m
                        ? (this._set(m.length || 0, 0), this.setAngle(m.angle || 0))
                        : (this._set(0, 0), (c = 0)));
            }
            return (d && (this.__read = c), this);
          },
          set: "#initialize",
          _set: function (e, r) {
            return ((this.x = e), (this.y = r), this);
          },
          equals: function (e) {
            return (
              this === e ||
              (e &&
                ((this.x === e.x && this.y === e.y) ||
                  (Array.isArray(e) && this.x === e[0] && this.y === e[1]))) ||
              !1
            );
          },
          clone: function () {
            return new R(this.x, this.y);
          },
          toString: function () {
            var e = k.instance;
            return "{ x: " + e.number(this.x) + ", y: " + e.number(this.y) + " }";
          },
          _serialize: function (e) {
            var r = e.formatter;
            return [r.number(this.x), r.number(this.y)];
          },
          getLength: function () {
            return Math.sqrt(this.x * this.x + this.y * this.y);
          },
          setLength: function (e) {
            if (this.isZero()) {
              var r = this._angle || 0;
              this._set(Math.cos(r) * e, Math.sin(r) * e);
            } else {
              var o = e / this.getLength();
              (F.isZero(o) && this.getAngle(), this._set(this.x * o, this.y * o));
            }
          },
          getAngle: function () {
            return (this.getAngleInRadians.apply(this, arguments) * 180) / Math.PI;
          },
          setAngle: function (e) {
            this.setAngleInRadians.call(this, (e * Math.PI) / 180);
          },
          getAngleInDegrees: "#getAngle",
          setAngleInDegrees: "#setAngle",
          getAngleInRadians: function () {
            if (arguments.length) {
              var e = R.read(arguments),
                r = this.getLength() * e.getLength();
              if (F.isZero(r)) return NaN;
              var o = this.dot(e) / r;
              return Math.acos(o < -1 ? -1 : o > 1 ? 1 : o);
            } else
              return this.isZero() ? this._angle || 0 : (this._angle = Math.atan2(this.y, this.x));
          },
          setAngleInRadians: function (e) {
            if (((this._angle = e), !this.isZero())) {
              var r = this.getLength();
              this._set(Math.cos(e) * r, Math.sin(e) * r);
            }
          },
          getQuadrant: function () {
            return this.x >= 0 ? (this.y >= 0 ? 1 : 4) : this.y >= 0 ? 2 : 3;
          },
        },
        {
          beans: !1,
          getDirectedAngle: function () {
            var e = R.read(arguments);
            return (Math.atan2(this.cross(e), this.dot(e)) * 180) / Math.PI;
          },
          getDistance: function () {
            var e = R.read(arguments),
              r = e.x - this.x,
              o = e.y - this.y,
              l = r * r + o * o,
              d = s.read(arguments);
            return d ? l : Math.sqrt(l);
          },
          normalize: function (e) {
            e === t && (e = 1);
            var r = this.getLength(),
              o = r !== 0 ? e / r : 0,
              l = new R(this.x * o, this.y * o);
            return (o >= 0 && (l._angle = this._angle), l);
          },
          rotate: function (e, r) {
            if (e === 0) return this.clone();
            e = (e * Math.PI) / 180;
            var o = r ? this.subtract(r) : this,
              l = Math.sin(e),
              d = Math.cos(e);
            return ((o = new R(o.x * d - o.y * l, o.x * l + o.y * d)), r ? o.add(r) : o);
          },
          transform: function (e) {
            return e ? e._transformPoint(this) : this;
          },
          add: function () {
            var e = R.read(arguments);
            return new R(this.x + e.x, this.y + e.y);
          },
          subtract: function () {
            var e = R.read(arguments);
            return new R(this.x - e.x, this.y - e.y);
          },
          multiply: function () {
            var e = R.read(arguments);
            return new R(this.x * e.x, this.y * e.y);
          },
          divide: function () {
            var e = R.read(arguments);
            return new R(this.x / e.x, this.y / e.y);
          },
          modulo: function () {
            var e = R.read(arguments);
            return new R(this.x % e.x, this.y % e.y);
          },
          negate: function () {
            return new R(-this.x, -this.y);
          },
          isInside: function () {
            return X.read(arguments).contains(this);
          },
          isClose: function () {
            var e = R.read(arguments),
              r = s.read(arguments);
            return this.getDistance(e) <= r;
          },
          isCollinear: function () {
            var e = R.read(arguments);
            return R.isCollinear(this.x, this.y, e.x, e.y);
          },
          isColinear: "#isCollinear",
          isOrthogonal: function () {
            var e = R.read(arguments);
            return R.isOrthogonal(this.x, this.y, e.x, e.y);
          },
          isZero: function () {
            var e = F.isZero;
            return e(this.x) && e(this.y);
          },
          isNaN: function () {
            return isNaN(this.x) || isNaN(this.y);
          },
          isInQuadrant: function (e) {
            return this.x * (e > 1 && e < 4 ? -1 : 1) >= 0 && this.y * (e > 2 ? -1 : 1) >= 0;
          },
          dot: function () {
            var e = R.read(arguments);
            return this.x * e.x + this.y * e.y;
          },
          cross: function () {
            var e = R.read(arguments);
            return this.x * e.y - this.y * e.x;
          },
          project: function () {
            var e = R.read(arguments),
              r = e.isZero() ? 0 : this.dot(e) / e.dot(e);
            return new R(e.x * r, e.y * r);
          },
          statics: {
            min: function () {
              var e = R.read(arguments),
                r = R.read(arguments);
              return new R(Math.min(e.x, r.x), Math.min(e.y, r.y));
            },
            max: function () {
              var e = R.read(arguments),
                r = R.read(arguments);
              return new R(Math.max(e.x, r.x), Math.max(e.y, r.y));
            },
            random: function () {
              return new R(Math.random(), Math.random());
            },
            isCollinear: function (e, r, o, l) {
              return Math.abs(e * l - r * o) <= Math.sqrt((e * e + r * r) * (o * o + l * l)) * 1e-8;
            },
            isOrthogonal: function (e, r, o, l) {
              return Math.abs(e * o + r * l) <= Math.sqrt((e * e + r * r) * (o * o + l * l)) * 1e-8;
            },
          },
        },
        s.each(
          ["round", "ceil", "floor", "abs"],
          function (e) {
            var r = Math[e];
            this[e] = function () {
              return new R(r(this.x), r(this.y));
            };
          },
          {}
        )
      ),
      $ = R.extend({
        initialize: function (r, o, l, d) {
          ((this._x = r), (this._y = o), (this._owner = l), (this._setter = d));
        },
        _set: function (e, r, o) {
          return ((this._x = e), (this._y = r), o || this._owner[this._setter](this), this);
        },
        getX: function () {
          return this._x;
        },
        setX: function (e) {
          ((this._x = e), this._owner[this._setter](this));
        },
        getY: function () {
          return this._y;
        },
        setY: function (e) {
          ((this._y = e), this._owner[this._setter](this));
        },
        isSelected: function () {
          return !!(this._owner._selection & this._getSelection());
        },
        setSelected: function (e) {
          this._owner._changeSelection(this._getSelection(), e);
        },
        _getSelection: function () {
          return this._setter === "setPosition" ? 4 : 0;
        },
      }),
      q = s.extend(
        {
          _class: "Size",
          _readIndex: !0,
          initialize: function (r, o) {
            var l = typeof r,
              d = this.__read,
              c = 0;
            if (l === "number") {
              var h = typeof o == "number";
              (this._set(r, h ? o : r), d && (c = h ? 2 : 1));
            } else if (l === "undefined" || r === null)
              (this._set(0, 0), d && (c = r === null ? 1 : 0));
            else {
              var m = l === "string" ? r.split(/[\s,]+/) || [] : r;
              ((c = 1),
                Array.isArray(m)
                  ? this._set(+m[0], +(m.length > 1 ? m[1] : m[0]))
                  : "width" in m
                    ? this._set(m.width || 0, m.height || 0)
                    : "x" in m
                      ? this._set(m.x || 0, m.y || 0)
                      : (this._set(0, 0), (c = 0)));
            }
            return (d && (this.__read = c), this);
          },
          set: "#initialize",
          _set: function (e, r) {
            return ((this.width = e), (this.height = r), this);
          },
          equals: function (e) {
            return (
              e === this ||
              (e &&
                ((this.width === e.width && this.height === e.height) ||
                  (Array.isArray(e) && this.width === e[0] && this.height === e[1]))) ||
              !1
            );
          },
          clone: function () {
            return new q(this.width, this.height);
          },
          toString: function () {
            var e = k.instance;
            return "{ width: " + e.number(this.width) + ", height: " + e.number(this.height) + " }";
          },
          _serialize: function (e) {
            var r = e.formatter;
            return [r.number(this.width), r.number(this.height)];
          },
          add: function () {
            var e = q.read(arguments);
            return new q(this.width + e.width, this.height + e.height);
          },
          subtract: function () {
            var e = q.read(arguments);
            return new q(this.width - e.width, this.height - e.height);
          },
          multiply: function () {
            var e = q.read(arguments);
            return new q(this.width * e.width, this.height * e.height);
          },
          divide: function () {
            var e = q.read(arguments);
            return new q(this.width / e.width, this.height / e.height);
          },
          modulo: function () {
            var e = q.read(arguments);
            return new q(this.width % e.width, this.height % e.height);
          },
          negate: function () {
            return new q(-this.width, -this.height);
          },
          isZero: function () {
            var e = F.isZero;
            return e(this.width) && e(this.height);
          },
          isNaN: function () {
            return isNaN(this.width) || isNaN(this.height);
          },
          statics: {
            min: function (e, r) {
              return new q(Math.min(e.width, r.width), Math.min(e.height, r.height));
            },
            max: function (e, r) {
              return new q(Math.max(e.width, r.width), Math.max(e.height, r.height));
            },
            random: function () {
              return new q(Math.random(), Math.random());
            },
          },
        },
        s.each(
          ["round", "ceil", "floor", "abs"],
          function (e) {
            var r = Math[e];
            this[e] = function () {
              return new q(r(this.width), r(this.height));
            };
          },
          {}
        )
      ),
      Z = q.extend({
        initialize: function (r, o, l, d) {
          ((this._width = r), (this._height = o), (this._owner = l), (this._setter = d));
        },
        _set: function (e, r, o) {
          return (
            (this._width = e),
            (this._height = r),
            o || this._owner[this._setter](this),
            this
          );
        },
        getWidth: function () {
          return this._width;
        },
        setWidth: function (e) {
          ((this._width = e), this._owner[this._setter](this));
        },
        getHeight: function () {
          return this._height;
        },
        setHeight: function (e) {
          ((this._height = e), this._owner[this._setter](this));
        },
      }),
      X = s.extend(
        {
          _class: "Rectangle",
          _readIndex: !0,
          beans: !0,
          initialize: function (r, o, l, d) {
            var c = typeof r,
              h;
            if (
              (c === "number"
                ? (this._set(r, o, l, d), (h = 4))
                : c === "undefined" || r === null
                  ? (this._set(0, 0, 0, 0), (h = r === null ? 1 : 0))
                  : arguments.length === 1 &&
                    (Array.isArray(r)
                      ? (this._set.apply(this, r), (h = 1))
                      : r.x !== t || r.width !== t
                        ? (this._set(r.x || 0, r.y || 0, r.width || 0, r.height || 0), (h = 1))
                        : r.from === t &&
                          r.to === t &&
                          (this._set(0, 0, 0, 0), s.filter(this, r), (h = 1))),
              h === t)
            ) {
              var m = R.readNamed(arguments, "from"),
                p = s.peek(arguments),
                y = m.x,
                x = m.y,
                b,
                S;
              if ((p && p.x !== t) || s.hasNamed(arguments, "to")) {
                var v = R.readNamed(arguments, "to");
                ((b = v.x - y),
                  (S = v.y - x),
                  b < 0 && ((y = v.x), (b = -b)),
                  S < 0 && ((x = v.y), (S = -S)));
              } else {
                var w = q.read(arguments);
                ((b = w.width), (S = w.height));
              }
              (this._set(y, x, b, S), (h = arguments.__index));
              var _ = arguments.__filtered;
              _ && (this.__filtered = _);
            }
            return (this.__read && (this.__read = h), this);
          },
          set: "#initialize",
          _set: function (e, r, o, l) {
            return ((this.x = e), (this.y = r), (this.width = o), (this.height = l), this);
          },
          clone: function () {
            return new X(this.x, this.y, this.width, this.height);
          },
          equals: function (e) {
            var r = s.isPlainValue(e) ? X.read(arguments) : e;
            return (
              r === this ||
              (r &&
                this.x === r.x &&
                this.y === r.y &&
                this.width === r.width &&
                this.height === r.height) ||
              !1
            );
          },
          toString: function () {
            var e = k.instance;
            return (
              "{ x: " +
              e.number(this.x) +
              ", y: " +
              e.number(this.y) +
              ", width: " +
              e.number(this.width) +
              ", height: " +
              e.number(this.height) +
              " }"
            );
          },
          _serialize: function (e) {
            var r = e.formatter;
            return [
              r.number(this.x),
              r.number(this.y),
              r.number(this.width),
              r.number(this.height),
            ];
          },
          getPoint: function (e) {
            var r = e ? R : $;
            return new r(this.x, this.y, this, "setPoint");
          },
          setPoint: function () {
            var e = R.read(arguments);
            ((this.x = e.x), (this.y = e.y));
          },
          getSize: function (e) {
            var r = e ? q : Z;
            return new r(this.width, this.height, this, "setSize");
          },
          _fw: 1,
          _fh: 1,
          setSize: function () {
            var e = q.read(arguments),
              r = this._sx,
              o = this._sy,
              l = e.width,
              d = e.height;
            (r && (this.x += (this.width - l) * r),
              o && (this.y += (this.height - d) * o),
              (this.width = l),
              (this.height = d),
              (this._fw = this._fh = 1));
          },
          getLeft: function () {
            return this.x;
          },
          setLeft: function (e) {
            if (!this._fw) {
              var r = e - this.x;
              this.width -= this._sx === 0.5 ? r * 2 : r;
            }
            ((this.x = e), (this._sx = this._fw = 0));
          },
          getTop: function () {
            return this.y;
          },
          setTop: function (e) {
            if (!this._fh) {
              var r = e - this.y;
              this.height -= this._sy === 0.5 ? r * 2 : r;
            }
            ((this.y = e), (this._sy = this._fh = 0));
          },
          getRight: function () {
            return this.x + this.width;
          },
          setRight: function (e) {
            if (!this._fw) {
              var r = e - this.x;
              this.width = this._sx === 0.5 ? r * 2 : r;
            }
            ((this.x = e - this.width), (this._sx = 1), (this._fw = 0));
          },
          getBottom: function () {
            return this.y + this.height;
          },
          setBottom: function (e) {
            if (!this._fh) {
              var r = e - this.y;
              this.height = this._sy === 0.5 ? r * 2 : r;
            }
            ((this.y = e - this.height), (this._sy = 1), (this._fh = 0));
          },
          getCenterX: function () {
            return this.x + this.width / 2;
          },
          setCenterX: function (e) {
            (this._fw || this._sx === 0.5
              ? (this.x = e - this.width / 2)
              : (this._sx && (this.x += (e - this.x) * 2 * this._sx),
                (this.width = (e - this.x) * 2)),
              (this._sx = 0.5),
              (this._fw = 0));
          },
          getCenterY: function () {
            return this.y + this.height / 2;
          },
          setCenterY: function (e) {
            (this._fh || this._sy === 0.5
              ? (this.y = e - this.height / 2)
              : (this._sy && (this.y += (e - this.y) * 2 * this._sy),
                (this.height = (e - this.y) * 2)),
              (this._sy = 0.5),
              (this._fh = 0));
          },
          getCenter: function (e) {
            var r = e ? R : $;
            return new r(this.getCenterX(), this.getCenterY(), this, "setCenter");
          },
          setCenter: function () {
            var e = R.read(arguments);
            return (this.setCenterX(e.x), this.setCenterY(e.y), this);
          },
          getArea: function () {
            return this.width * this.height;
          },
          isEmpty: function () {
            return this.width === 0 || this.height === 0;
          },
          contains: function (e) {
            return (e && e.width !== t) || (Array.isArray(e) ? e : arguments).length === 4
              ? this._containsRectangle(X.read(arguments))
              : this._containsPoint(R.read(arguments));
          },
          _containsPoint: function (e) {
            var r = e.x,
              o = e.y;
            return (
              r >= this.x && o >= this.y && r <= this.x + this.width && o <= this.y + this.height
            );
          },
          _containsRectangle: function (e) {
            var r = e.x,
              o = e.y;
            return (
              r >= this.x &&
              o >= this.y &&
              r + e.width <= this.x + this.width &&
              o + e.height <= this.y + this.height
            );
          },
          intersects: function () {
            var e = X.read(arguments),
              r = s.read(arguments) || 0;
            return (
              e.x + e.width > this.x - r &&
              e.y + e.height > this.y - r &&
              e.x < this.x + this.width + r &&
              e.y < this.y + this.height + r
            );
          },
          intersect: function () {
            var e = X.read(arguments),
              r = Math.max(this.x, e.x),
              o = Math.max(this.y, e.y),
              l = Math.min(this.x + this.width, e.x + e.width),
              d = Math.min(this.y + this.height, e.y + e.height);
            return new X(r, o, l - r, d - o);
          },
          unite: function () {
            var e = X.read(arguments),
              r = Math.min(this.x, e.x),
              o = Math.min(this.y, e.y),
              l = Math.max(this.x + this.width, e.x + e.width),
              d = Math.max(this.y + this.height, e.y + e.height);
            return new X(r, o, l - r, d - o);
          },
          include: function () {
            var e = R.read(arguments),
              r = Math.min(this.x, e.x),
              o = Math.min(this.y, e.y),
              l = Math.max(this.x + this.width, e.x),
              d = Math.max(this.y + this.height, e.y);
            return new X(r, o, l - r, d - o);
          },
          expand: function () {
            var e = q.read(arguments),
              r = e.width,
              o = e.height;
            return new X(this.x - r / 2, this.y - o / 2, this.width + r, this.height + o);
          },
          scale: function (e, r) {
            return this.expand(
              this.width * e - this.width,
              this.height * (r === t ? e : r) - this.height
            );
          },
        },
        s.each(
          [
            ["Top", "Left"],
            ["Top", "Right"],
            ["Bottom", "Left"],
            ["Bottom", "Right"],
            ["Left", "Center"],
            ["Top", "Center"],
            ["Right", "Center"],
            ["Bottom", "Center"],
          ],
          function (e, r) {
            var o = e.join(""),
              l = /^[RL]/.test(o);
            r >= 4 && (e[1] += l ? "Y" : "X");
            var d = e[l ? 0 : 1],
              c = e[l ? 1 : 0],
              h = "get" + d,
              m = "get" + c,
              p = "set" + d,
              y = "set" + c,
              x = "get" + o,
              b = "set" + o;
            ((this[x] = function (S) {
              var v = S ? R : $;
              return new v(this[h](), this[m](), this, b);
            }),
              (this[b] = function () {
                var S = R.read(arguments);
                (this[p](S.x), this[y](S.y));
              }));
          },
          { beans: !0 }
        )
      ),
      be = X.extend(
        {
          initialize: function (r, o, l, d, c, h) {
            (this._set(r, o, l, d, !0), (this._owner = c), (this._setter = h));
          },
          _set: function (e, r, o, l, d) {
            return (
              (this._x = e),
              (this._y = r),
              (this._width = o),
              (this._height = l),
              d || this._owner[this._setter](this),
              this
            );
          },
        },
        new (function () {
          var e = X.prototype;
          return s.each(
            ["x", "y", "width", "height"],
            function (r) {
              var o = s.capitalize(r),
                l = "_" + r;
              ((this["get" + o] = function () {
                return this[l];
              }),
                (this["set" + o] = function (d) {
                  ((this[l] = d), this._dontNotify || this._owner[this._setter](this));
                }));
            },
            s.each(
              [
                "Point",
                "Size",
                "Center",
                "Left",
                "Top",
                "Right",
                "Bottom",
                "CenterX",
                "CenterY",
                "TopLeft",
                "TopRight",
                "BottomLeft",
                "BottomRight",
                "LeftCenter",
                "TopCenter",
                "RightCenter",
                "BottomCenter",
              ],
              function (r) {
                var o = "set" + r;
                this[o] = function () {
                  ((this._dontNotify = !0),
                    e[o].apply(this, arguments),
                    (this._dontNotify = !1),
                    this._owner[this._setter](this));
                };
              },
              {
                isSelected: function () {
                  return !!(this._owner._selection & 2);
                },
                setSelected: function (r) {
                  var o = this._owner;
                  o._changeSelection && o._changeSelection(2, r);
                },
              }
            )
          );
        })()
      ),
      Q = s.extend(
        {
          _class: "Matrix",
          initialize: function e(r, o) {
            var l = arguments.length,
              d = !0;
            if (
              (l >= 6
                ? this._set.apply(this, arguments)
                : l === 1 || l === 2
                  ? r instanceof e
                    ? this._set(r._a, r._b, r._c, r._d, r._tx, r._ty, o)
                    : Array.isArray(r)
                      ? this._set.apply(this, o ? r.concat([o]) : r)
                      : (d = !1)
                  : l
                    ? (d = !1)
                    : this.reset(),
              !d)
            )
              throw new Error("Unsupported matrix parameters");
            return this;
          },
          set: "#initialize",
          _set: function (e, r, o, l, d, c, h) {
            return (
              (this._a = e),
              (this._b = r),
              (this._c = o),
              (this._d = l),
              (this._tx = d),
              (this._ty = c),
              h || this._changed(),
              this
            );
          },
          _serialize: function (e, r) {
            return s.serialize(this.getValues(), e, !0, r);
          },
          _changed: function () {
            var e = this._owner;
            e && (e._applyMatrix ? e.transform(null, !0) : e._changed(9));
          },
          clone: function () {
            return new Q(this._a, this._b, this._c, this._d, this._tx, this._ty);
          },
          equals: function (e) {
            return (
              e === this ||
              (e &&
                this._a === e._a &&
                this._b === e._b &&
                this._c === e._c &&
                this._d === e._d &&
                this._tx === e._tx &&
                this._ty === e._ty)
            );
          },
          toString: function () {
            var e = k.instance;
            return (
              "[[" +
              [e.number(this._a), e.number(this._c), e.number(this._tx)].join(", ") +
              "], [" +
              [e.number(this._b), e.number(this._d), e.number(this._ty)].join(", ") +
              "]]"
            );
          },
          reset: function (e) {
            return (
              (this._a = this._d = 1),
              (this._b = this._c = this._tx = this._ty = 0),
              e || this._changed(),
              this
            );
          },
          apply: function (e, r) {
            var o = this._owner;
            return o ? (o.transform(null, !0, s.pick(e, !0), r), this.isIdentity()) : !1;
          },
          translate: function () {
            var e = R.read(arguments),
              r = e.x,
              o = e.y;
            return (
              (this._tx += r * this._a + o * this._c),
              (this._ty += r * this._b + o * this._d),
              this._changed(),
              this
            );
          },
          scale: function () {
            var e = R.read(arguments),
              r = R.read(arguments, 0, { readNull: !0 });
            return (
              r && this.translate(r),
              (this._a *= e.x),
              (this._b *= e.x),
              (this._c *= e.y),
              (this._d *= e.y),
              r && this.translate(r.negate()),
              this._changed(),
              this
            );
          },
          rotate: function (e) {
            e *= Math.PI / 180;
            var r = R.read(arguments, 1),
              o = r.x,
              l = r.y,
              d = Math.cos(e),
              c = Math.sin(e),
              h = o - o * d + l * c,
              m = l - o * c - l * d,
              p = this._a,
              y = this._b,
              x = this._c,
              b = this._d;
            return (
              (this._a = d * p + c * x),
              (this._b = d * y + c * b),
              (this._c = -c * p + d * x),
              (this._d = -c * y + d * b),
              (this._tx += h * p + m * x),
              (this._ty += h * y + m * b),
              this._changed(),
              this
            );
          },
          shear: function () {
            var e = R.read(arguments),
              r = R.read(arguments, 0, { readNull: !0 });
            r && this.translate(r);
            var o = this._a,
              l = this._b;
            return (
              (this._a += e.y * this._c),
              (this._b += e.y * this._d),
              (this._c += e.x * o),
              (this._d += e.x * l),
              r && this.translate(r.negate()),
              this._changed(),
              this
            );
          },
          skew: function () {
            var e = R.read(arguments),
              r = R.read(arguments, 0, { readNull: !0 }),
              o = Math.PI / 180,
              l = new R(Math.tan(e.x * o), Math.tan(e.y * o));
            return this.shear(l, r);
          },
          append: function (e, r) {
            if (e) {
              var o = this._a,
                l = this._b,
                d = this._c,
                c = this._d,
                h = e._a,
                m = e._c,
                p = e._b,
                y = e._d,
                x = e._tx,
                b = e._ty;
              ((this._a = h * o + p * d),
                (this._c = m * o + y * d),
                (this._b = h * l + p * c),
                (this._d = m * l + y * c),
                (this._tx += x * o + b * d),
                (this._ty += x * l + b * c),
                r || this._changed());
            }
            return this;
          },
          prepend: function (e, r) {
            if (e) {
              var o = this._a,
                l = this._b,
                d = this._c,
                c = this._d,
                h = this._tx,
                m = this._ty,
                p = e._a,
                y = e._c,
                x = e._b,
                b = e._d,
                S = e._tx,
                v = e._ty;
              ((this._a = p * o + y * l),
                (this._c = p * d + y * c),
                (this._b = x * o + b * l),
                (this._d = x * d + b * c),
                (this._tx = p * h + y * m + S),
                (this._ty = x * h + b * m + v),
                r || this._changed());
            }
            return this;
          },
          appended: function (e) {
            return this.clone().append(e);
          },
          prepended: function (e) {
            return this.clone().prepend(e);
          },
          invert: function () {
            var e = this._a,
              r = this._b,
              o = this._c,
              l = this._d,
              d = this._tx,
              c = this._ty,
              h = e * l - r * o,
              m = null;
            return (
              h &&
                !isNaN(h) &&
                isFinite(d) &&
                isFinite(c) &&
                ((this._a = l / h),
                (this._b = -r / h),
                (this._c = -o / h),
                (this._d = e / h),
                (this._tx = (o * c - l * d) / h),
                (this._ty = (r * d - e * c) / h),
                (m = this)),
              m
            );
          },
          inverted: function () {
            return this.clone().invert();
          },
          concatenate: "#append",
          preConcatenate: "#prepend",
          chain: "#appended",
          _shiftless: function () {
            return new Q(this._a, this._b, this._c, this._d, 0, 0);
          },
          _orNullIfIdentity: function () {
            return this.isIdentity() ? null : this;
          },
          isIdentity: function () {
            return (
              this._a === 1 &&
              this._b === 0 &&
              this._c === 0 &&
              this._d === 1 &&
              this._tx === 0 &&
              this._ty === 0
            );
          },
          isInvertible: function () {
            var e = this._a * this._d - this._c * this._b;
            return e && !isNaN(e) && isFinite(this._tx) && isFinite(this._ty);
          },
          isSingular: function () {
            return !this.isInvertible();
          },
          transform: function (e, r, o) {
            return arguments.length < 3
              ? this._transformPoint(R.read(arguments))
              : this._transformCoordinates(e, r, o);
          },
          _transformPoint: function (e, r, o) {
            var l = e.x,
              d = e.y;
            return (
              r || (r = new R()),
              r._set(l * this._a + d * this._c + this._tx, l * this._b + d * this._d + this._ty, o)
            );
          },
          _transformCoordinates: function (e, r, o) {
            for (var l = 0, d = 2 * o; l < d; l += 2) {
              var c = e[l],
                h = e[l + 1];
              ((r[l] = c * this._a + h * this._c + this._tx),
                (r[l + 1] = c * this._b + h * this._d + this._ty));
            }
            return r;
          },
          _transformCorners: function (e) {
            var r = e.x,
              o = e.y,
              l = r + e.width,
              d = o + e.height,
              c = [r, o, l, o, l, d, r, d];
            return this._transformCoordinates(c, c, 4);
          },
          _transformBounds: function (e, r, o) {
            for (
              var l = this._transformCorners(e), d = l.slice(0, 2), c = d.slice(), h = 2;
              h < 8;
              h++
            ) {
              var m = l[h],
                p = h & 1;
              m < d[p] ? (d[p] = m) : m > c[p] && (c[p] = m);
            }
            return (r || (r = new X()), r._set(d[0], d[1], c[0] - d[0], c[1] - d[1], o));
          },
          inverseTransform: function () {
            return this._inverseTransform(R.read(arguments));
          },
          _inverseTransform: function (e, r, o) {
            var l = this._a,
              d = this._b,
              c = this._c,
              h = this._d,
              m = this._tx,
              p = this._ty,
              y = l * h - d * c,
              x = null;
            if (y && !isNaN(y) && isFinite(m) && isFinite(p)) {
              var b = e.x - this._tx,
                S = e.y - this._ty;
              (r || (r = new R()), (x = r._set((b * h - S * c) / y, (S * l - b * d) / y, o)));
            }
            return x;
          },
          decompose: function () {
            var e = this._a,
              r = this._b,
              o = this._c,
              l = this._d,
              d = e * l - r * o,
              c = Math.sqrt,
              h = Math.atan2,
              m = 180 / Math.PI,
              p,
              y,
              x;
            if (e !== 0 || r !== 0) {
              var b = c(e * e + r * r);
              ((p = Math.acos(e / b) * (r > 0 ? 1 : -1)),
                (y = [b, d / b]),
                (x = [h(e * o + r * l, b * b), 0]));
            } else if (o !== 0 || l !== 0) {
              var S = c(o * o + l * l);
              ((p = Math.asin(o / S) * (l > 0 ? 1 : -1)),
                (y = [d / S, S]),
                (x = [0, h(e * o + r * l, S * S)]));
            } else ((p = 0), (x = y = [0, 0]));
            return {
              translation: this.getTranslation(),
              rotation: p * m,
              scaling: new R(y),
              skewing: new R(x[0] * m, x[1] * m),
            };
          },
          getValues: function () {
            return [this._a, this._b, this._c, this._d, this._tx, this._ty];
          },
          getTranslation: function () {
            return new R(this._tx, this._ty);
          },
          getScaling: function () {
            return (this.decompose() || {}).scaling;
          },
          getRotation: function () {
            return (this.decompose() || {}).rotation;
          },
          applyToContext: function (e) {
            this.isIdentity() ||
              e.transform(this._a, this._b, this._c, this._d, this._tx, this._ty);
          },
        },
        s.each(
          ["a", "b", "c", "d", "tx", "ty"],
          function (e) {
            var r = s.capitalize(e),
              o = "_" + e;
            ((this["get" + r] = function () {
              return this[o];
            }),
              (this["set" + r] = function (l) {
                ((this[o] = l), this._changed());
              }));
          },
          {}
        )
      ),
      oe = s.extend({
        _class: "Line",
        initialize: function (r, o, l, d, c) {
          var h = !1;
          (arguments.length >= 4
            ? ((this._px = r), (this._py = o), (this._vx = l), (this._vy = d), (h = c))
            : ((this._px = r.x), (this._py = r.y), (this._vx = o.x), (this._vy = o.y), (h = l)),
            h || ((this._vx -= this._px), (this._vy -= this._py)));
        },
        getPoint: function () {
          return new R(this._px, this._py);
        },
        getVector: function () {
          return new R(this._vx, this._vy);
        },
        getLength: function () {
          return this.getVector().getLength();
        },
        intersect: function (e, r) {
          return oe.intersect(
            this._px,
            this._py,
            this._vx,
            this._vy,
            e._px,
            e._py,
            e._vx,
            e._vy,
            !0,
            r
          );
        },
        getSide: function (e, r) {
          return oe.getSide(this._px, this._py, this._vx, this._vy, e.x, e.y, !0, r);
        },
        getDistance: function (e) {
          return Math.abs(this.getSignedDistance(e));
        },
        getSignedDistance: function (e) {
          return oe.getSignedDistance(this._px, this._py, this._vx, this._vy, e.x, e.y, !0);
        },
        isCollinear: function (e) {
          return R.isCollinear(this._vx, this._vy, e._vx, e._vy);
        },
        isOrthogonal: function (e) {
          return R.isOrthogonal(this._vx, this._vy, e._vx, e._vy);
        },
        statics: {
          intersect: function (e, r, o, l, d, c, h, m, p, y) {
            p || ((o -= e), (l -= r), (h -= d), (m -= c));
            var x = o * m - l * h;
            if (!F.isZero(x)) {
              var b = e - d,
                S = r - c,
                v = (h * S - m * b) / x,
                w = (o * S - l * b) / x,
                _ = 1e-12,
                P = -_,
                D = 1 + _;
              if (y || (P < v && v < D && P < w && w < D))
                return (y || (v = v <= 0 ? 0 : v >= 1 ? 1 : v), new R(e + v * o, r + v * l));
            }
          },
          getSide: function (e, r, o, l, d, c, h, m) {
            h || ((o -= e), (l -= r));
            var p = d - e,
              y = c - r,
              x = p * l - y * o;
            return (
              !m &&
                F.isZero(x) &&
                ((x = (p * o + p * o) / (o * o + l * l)), x >= 0 && x <= 1 && (x = 0)),
              x < 0 ? -1 : x > 0 ? 1 : 0
            );
          },
          getSignedDistance: function (e, r, o, l, d, c, h) {
            return (
              h || ((o -= e), (l -= r)),
              o === 0
                ? l > 0
                  ? d - e
                  : e - d
                : l === 0
                  ? o < 0
                    ? c - r
                    : r - c
                  : ((d - e) * l - (c - r) * o) / Math.sqrt(o * o + l * l)
            );
          },
          getDistance: function (e, r, o, l, d, c, h) {
            return Math.abs(oe.getSignedDistance(e, r, o, l, d, c, h));
          },
        },
      }),
      ct = g.extend({
        _class: "Project",
        _list: "projects",
        _reference: "project",
        _compactSerialize: !0,
        initialize: function (r) {
          (g.call(this, !0),
            (this._children = []),
            (this._namedChildren = {}),
            (this._activeLayer = null),
            (this._currentStyle = new Cs(null, null, this)),
            (this._view = Le.create(this, r || je.getCanvas(1, 1))),
            (this._selectionItems = {}),
            (this._selectionCount = 0),
            (this._updateVersion = 0));
        },
        _serialize: function (e, r) {
          return s.serialize(this._children, e, !0, r);
        },
        _changed: function (e, r) {
          if (e & 1) {
            var o = this._view;
            o && ((o._needsUpdate = !0), !o._requested && o._autoUpdate && o.requestUpdate());
          }
          var l = this._changes;
          if (l && r) {
            var d = this._changesById,
              c = r._id,
              h = d[c];
            h ? (h.flags |= e) : l.push((d[c] = { item: r, flags: e }));
          }
        },
        clear: function () {
          for (var e = this._children, r = e.length - 1; r >= 0; r--) e[r].remove();
        },
        isEmpty: function () {
          return !this._children.length;
        },
        remove: function e() {
          return e.base.call(this) ? (this._view && this._view.remove(), !0) : !1;
        },
        getView: function () {
          return this._view;
        },
        getCurrentStyle: function () {
          return this._currentStyle;
        },
        setCurrentStyle: function (e) {
          this._currentStyle.set(e);
        },
        getIndex: function () {
          return this._index;
        },
        getOptions: function () {
          return this._scope.settings;
        },
        getLayers: function () {
          return this._children;
        },
        getActiveLayer: function () {
          return this._activeLayer || new ft({ project: this, insert: !0 });
        },
        getSymbolDefinitions: function () {
          var e = [],
            r = {};
          return (
            this.getItems({
              class: xs,
              match: function (o) {
                var l = o._definition,
                  d = l._id;
                return (r[d] || ((r[d] = !0), e.push(l)), !1);
              },
            }),
            e
          );
        },
        getSymbols: "getSymbolDefinitions",
        getSelectedItems: function () {
          var e = this._selectionItems,
            r = [];
          for (var o in e) {
            var l = e[o],
              d = l._selection;
            d & 1 && l.isInserted() ? r.push(l) : d || this._updateSelection(l);
          }
          return r;
        },
        _updateSelection: function (e) {
          var r = e._id,
            o = this._selectionItems;
          e._selection
            ? o[r] !== e && (this._selectionCount++, (o[r] = e))
            : o[r] === e && (this._selectionCount--, delete o[r]);
        },
        selectAll: function () {
          for (var e = this._children, r = 0, o = e.length; r < o; r++) e[r].setFullySelected(!0);
        },
        deselectAll: function () {
          var e = this._selectionItems;
          for (var r in e) e[r].setFullySelected(!1);
        },
        addLayer: function (e) {
          return this.insertLayer(t, e);
        },
        insertLayer: function (e, r) {
          if (r instanceof ft) {
            (r._remove(!1, !0), s.splice(this._children, [r], e, 0), r._setProject(this, !0));
            var o = r._name;
            (o && r.setName(o),
              this._changes && r._changed(5),
              this._activeLayer || (this._activeLayer = r));
          } else r = null;
          return r;
        },
        _insertItem: function (e, r, o) {
          return (
            (r =
              this.insertLayer(e, r) ||
              (this._activeLayer || this._insertItem(t, new ft(ne.NO_INSERT), !0)).insertChild(
                e,
                r
              )),
            o && r.activate && r.activate(),
            r
          );
        },
        getItems: function (e) {
          return ne._getItems(this, e);
        },
        getItem: function (e) {
          return ne._getItems(this, e, null, null, !0)[0] || null;
        },
        importJSON: function (e) {
          this.activate();
          var r = this._activeLayer;
          return s.importJSON(e, r && r.isEmpty() && r);
        },
        removeOn: function (e) {
          var r = this._removeSets;
          if (r) {
            e === "mouseup" && (r.mousedrag = null);
            var o = r[e];
            if (o) {
              for (var l in o) {
                var d = o[l];
                for (var c in r) {
                  var h = r[c];
                  h && h != o && delete h[d._id];
                }
                d.remove();
              }
              r[e] = null;
            }
          }
        },
        draw: function (e, r, o) {
          (this._updateVersion++, e.save(), r.applyToContext(e));
          for (
            var l = this._children,
              d = new s({
                offset: new R(0, 0),
                pixelRatio: o,
                viewMatrix: r.isIdentity() ? null : r,
                matrices: [new Q()],
                updateMatrix: !0,
              }),
              c = 0,
              h = l.length;
            c < h;
            c++
          )
            l[c].draw(e, d);
          if ((e.restore(), this._selectionCount > 0)) {
            (e.save(), (e.strokeWidth = 1));
            var m = this._selectionItems,
              p = this._scope.settings.handleSize,
              y = this._updateVersion;
            for (var x in m) m[x]._drawSelection(e, r, p, m, y);
            e.restore();
          }
        },
      }),
      ne = s.extend(
        u,
        {
          statics: {
            extend: function e(r) {
              return (
                r._serializeFields &&
                  (r._serializeFields = s.set(
                    {},
                    this.prototype._serializeFields,
                    r._serializeFields
                  )),
                e.base.apply(this, arguments)
              );
            },
            NO_INSERT: { insert: !1 },
          },
          _class: "Item",
          _name: null,
          _applyMatrix: !0,
          _canApplyMatrix: !0,
          _canScaleStroke: !1,
          _pivot: null,
          _visible: !0,
          _blendMode: "normal",
          _opacity: 1,
          _locked: !1,
          _guide: !1,
          _clipMask: !1,
          _selection: 0,
          _selectBounds: !0,
          _selectChildren: !1,
          _serializeFields: {
            name: null,
            applyMatrix: null,
            matrix: new Q(),
            pivot: null,
            visible: !0,
            blendMode: "normal",
            opacity: 1,
            locked: !1,
            guide: !1,
            clipMask: !1,
            selected: !1,
            data: {},
          },
          _prioritize: ["applyMatrix"],
        },
        new (function () {
          var e = [
            "onMouseDown",
            "onMouseUp",
            "onMouseDrag",
            "onClick",
            "onDoubleClick",
            "onMouseMove",
            "onMouseEnter",
            "onMouseLeave",
          ];
          return s.each(
            e,
            function (r) {
              this._events[r] = {
                install: function (o) {
                  this.getView()._countItemEvent(o, 1);
                },
                uninstall: function (o) {
                  this.getView()._countItemEvent(o, -1);
                },
              };
            },
            {
              _events: {
                onFrame: {
                  install: function () {
                    this.getView()._animateItem(this, !0);
                  },
                  uninstall: function () {
                    this.getView()._animateItem(this, !1);
                  },
                },
                onLoad: {},
                onError: {},
              },
              statics: { _itemHandlers: e },
            }
          );
        })(),
        {
          initialize: function () {},
          _initialize: function (e, r) {
            var o = e && s.isPlainObject(e),
              l = o && e.internal === !0,
              d = (this._matrix = new Q()),
              c = (o && e.project) || Se.project,
              h = Se.settings;
            return (
              (this._id = l ? null : A.get()),
              (this._parent = this._index = null),
              (this._applyMatrix = this._canApplyMatrix && h.applyMatrix),
              r && d.translate(r),
              (d._owner = this),
              (this._style = new Cs(c._currentStyle, this, c)),
              l || (o && e.insert == !1) || (!h.insertItems && !(o && e.insert === !0))
                ? this._setProject(c)
                : ((o && e.parent) || c)._insertItem(t, this, !0),
              o &&
                e !== ne.NO_INSERT &&
                this.set(e, { internal: !0, insert: !0, project: !0, parent: !0 }),
              o
            );
          },
          _serialize: function (e, r) {
            var o = {},
              l = this;
            function d(c) {
              for (var h in c) {
                var m = l[h];
                s.equals(m, h === "leading" ? c.fontSize * 1.2 : c[h]) ||
                  (o[h] = s.serialize(m, e, h !== "data", r));
              }
            }
            return (
              d(this._serializeFields),
              this instanceof it || d(this._style._defaults),
              [this._class, o]
            );
          },
          _changed: function (e) {
            var r = this._symbol,
              o = this._parent || r,
              l = this._project;
            (e & 8 && (this._bounds = this._position = this._decomposed = this._globalMatrix = t),
              o && e & 40 && ne._clearBoundsCache(o),
              e & 2 && ne._clearBoundsCache(this),
              l && l._changed(e, this),
              r && r._changed(e));
          },
          getId: function () {
            return this._id;
          },
          getName: function () {
            return this._name;
          },
          setName: function (e) {
            if ((this._name && this._removeNamed(), e === +e + ""))
              throw new Error("Names consisting only of numbers are not supported.");
            var r = this._getOwner();
            if (e && r) {
              var o = r._children,
                l = r._namedChildren;
              ((l[e] = l[e] || []).push(this), e in o || (o[e] = this));
            }
            ((this._name = e || t), this._changed(128));
          },
          getStyle: function () {
            return this._style;
          },
          setStyle: function (e) {
            this.getStyle().set(e);
          },
        },
        s.each(
          ["locked", "visible", "blendMode", "opacity", "guide"],
          function (e) {
            var r = s.capitalize(e),
              o = "_" + e,
              l = { locked: 128, visible: 137 };
            ((this["get" + r] = function () {
              return this[o];
            }),
              (this["set" + r] = function (d) {
                d != this[o] && ((this[o] = d), this._changed(l[e] || 129));
              }));
          },
          {}
        ),
        {
          beans: !0,
          getSelection: function () {
            return this._selection;
          },
          setSelection: function (e) {
            if (e !== this._selection) {
              this._selection = e;
              var r = this._project;
              r && (r._updateSelection(this), this._changed(129));
            }
          },
          _changeSelection: function (e, r) {
            var o = this._selection;
            this.setSelection(r ? o | e : o & ~e);
          },
          isSelected: function () {
            if (this._selectChildren) {
              for (var e = this._children, r = 0, o = e.length; r < o; r++)
                if (e[r].isSelected()) return !0;
            }
            return !!(this._selection & 1);
          },
          setSelected: function (e) {
            if (this._selectChildren)
              for (var r = this._children, o = 0, l = r.length; o < l; o++) r[o].setSelected(e);
            this._changeSelection(1, e);
          },
          isFullySelected: function () {
            var e = this._children,
              r = !!(this._selection & 1);
            if (e && r) {
              for (var o = 0, l = e.length; o < l; o++) if (!e[o].isFullySelected()) return !1;
              return !0;
            }
            return r;
          },
          setFullySelected: function (e) {
            var r = this._children;
            if (r) for (var o = 0, l = r.length; o < l; o++) r[o].setFullySelected(e);
            this._changeSelection(1, e);
          },
          isClipMask: function () {
            return this._clipMask;
          },
          setClipMask: function (e) {
            this._clipMask != (e = !!e) &&
              ((this._clipMask = e),
              e && (this.setFillColor(null), this.setStrokeColor(null)),
              this._changed(129),
              this._parent && this._parent._changed(1024));
          },
          getData: function () {
            return (this._data || (this._data = {}), this._data);
          },
          setData: function (e) {
            this._data = e;
          },
          getPosition: function (e) {
            var r = this._position,
              o = e ? R : $;
            if (!r) {
              var l = this._pivot;
              r = this._position = l
                ? this._matrix._transformPoint(l)
                : this.getBounds().getCenter(!0);
            }
            return new o(r.x, r.y, this, "setPosition");
          },
          setPosition: function () {
            this.translate(R.read(arguments).subtract(this.getPosition(!0)));
          },
          getPivot: function () {
            var e = this._pivot;
            return e ? new $(e.x, e.y, this, "setPivot") : null;
          },
          setPivot: function () {
            ((this._pivot = R.read(arguments, 0, { clone: !0, readNull: !0 })),
              (this._position = t));
          },
        },
        s.each(
          {
            getStrokeBounds: { stroke: !0 },
            getHandleBounds: { handle: !0 },
            getInternalBounds: { internal: !0 },
          },
          function (e, r) {
            this[r] = function (o) {
              return this.getBounds(o, e);
            };
          },
          {
            beans: !0,
            getBounds: function (e, r) {
              var o = r || e instanceof Q,
                l = s.set({}, o ? r : e, this._boundsOptions);
              (!l.stroke || this.getStrokeScaling()) && (l.cacheItem = this);
              var d = this._getCachedBounds(o && e, l).rect;
              return arguments.length ? d : new be(d.x, d.y, d.width, d.height, this, "setBounds");
            },
            setBounds: function () {
              var e = X.read(arguments),
                r = this.getBounds(),
                o = this._matrix,
                l = new Q(),
                d = e.getCenter();
              (l.translate(d),
                (e.width != r.width || e.height != r.height) &&
                  (o.isInvertible() ||
                    (o.set(o._backup || new Q().translate(o.getTranslation())),
                    (r = this.getBounds())),
                  l.scale(
                    r.width !== 0 ? e.width / r.width : 0,
                    r.height !== 0 ? e.height / r.height : 0
                  )),
                (d = r.getCenter()),
                l.translate(-d.x, -d.y),
                this.transform(l));
            },
            _getBounds: function (e, r) {
              var o = this._children;
              return !o || !o.length
                ? new X()
                : (ne._updateBoundsCache(this, r.cacheItem), ne._getBounds(o, e, r));
            },
            _getBoundsCacheKey: function (e, r) {
              return [e.stroke ? 1 : 0, e.handle ? 1 : 0, r ? 1 : 0].join("");
            },
            _getCachedBounds: function (e, r, o) {
              e = e && e._orNullIfIdentity();
              var l = r.internal && !o,
                d = r.cacheItem,
                c = l ? null : this._matrix._orNullIfIdentity(),
                h = d && (!e || e.equals(c)) && this._getBoundsCacheKey(r, l),
                m = this._bounds;
              if ((ne._updateBoundsCache(this._parent || this._symbol, d), h && m && h in m)) {
                var p = m[h];
                return { rect: p.rect.clone(), nonscaling: p.nonscaling };
              }
              var y = this._getBounds(e || c, r),
                x = y.rect || y,
                b = this._style,
                S = y.nonscaling || (b.hasStroke() && !b.getStrokeScaling());
              if (h) {
                m || (this._bounds = m = {});
                var p = (m[h] = { rect: x.clone(), nonscaling: S, internal: l });
              }
              return { rect: x, nonscaling: S };
            },
            _getStrokeMatrix: function (e, r) {
              var o = this.getStrokeScaling()
                  ? null
                  : r && r.internal
                    ? this
                    : this._parent || (this._symbol && this._symbol._item),
                l = o ? o.getViewMatrix().invert() : e;
              return l && l._shiftless();
            },
            statics: {
              _updateBoundsCache: function (e, r) {
                if (e && r) {
                  var o = r._id,
                    l = (e._boundsCache = e._boundsCache || { ids: {}, list: [] });
                  l.ids[o] || (l.list.push(r), (l.ids[o] = r));
                }
              },
              _clearBoundsCache: function (e) {
                var r = e._boundsCache;
                if (r) {
                  e._bounds = e._position = e._boundsCache = t;
                  for (var o = 0, l = r.list, d = l.length; o < d; o++) {
                    var c = l[o];
                    c !== e &&
                      ((c._bounds = c._position = t), c._boundsCache && ne._clearBoundsCache(c));
                  }
                }
              },
              _getBounds: function (e, r, o) {
                var l = 1 / 0,
                  d = -l,
                  c = l,
                  h = d,
                  m = !1;
                o = o || {};
                for (var p = 0, y = e.length; p < y; p++) {
                  var x = e[p];
                  if (x._visible && !x.isEmpty()) {
                    var b = x._getCachedBounds(r && r.appended(x._matrix), o, !0),
                      S = b.rect;
                    ((l = Math.min(S.x, l)),
                      (c = Math.min(S.y, c)),
                      (d = Math.max(S.x + S.width, d)),
                      (h = Math.max(S.y + S.height, h)),
                      b.nonscaling && (m = !0));
                  }
                }
                return { rect: isFinite(l) ? new X(l, c, d - l, h - c) : new X(), nonscaling: m };
              },
            },
          }
        ),
        {
          beans: !0,
          _decompose: function () {
            return this._applyMatrix
              ? null
              : this._decomposed || (this._decomposed = this._matrix.decompose());
          },
          getRotation: function () {
            var e = this._decompose();
            return e ? e.rotation : 0;
          },
          setRotation: function (e) {
            var r = this.getRotation();
            if (r != null && e != null) {
              var o = this._decomposed;
              (this.rotate(e - r), o && ((o.rotation = e), (this._decomposed = o)));
            }
          },
          getScaling: function () {
            var e = this._decompose(),
              r = e && e.scaling;
            return new $(r ? r.x : 1, r ? r.y : 1, this, "setScaling");
          },
          setScaling: function () {
            var e = this.getScaling(),
              r = R.read(arguments, 0, { clone: !0, readNull: !0 });
            if (e && r && !e.equals(r)) {
              var o = this.getRotation(),
                l = this._decomposed,
                d = new Q(),
                c = this.getPosition(!0);
              (d.translate(c),
                o && d.rotate(o),
                d.scale(r.x / e.x, r.y / e.y),
                o && d.rotate(-o),
                d.translate(c.negate()),
                this.transform(d),
                l && ((l.scaling = r), (this._decomposed = l)));
            }
          },
          getMatrix: function () {
            return this._matrix;
          },
          setMatrix: function () {
            var e = this._matrix;
            e.initialize.apply(e, arguments);
          },
          getGlobalMatrix: function (e) {
            var r = this._globalMatrix,
              o = this._project._updateVersion;
            if ((r && r._updateVersion !== o && (r = null), !r)) {
              r = this._globalMatrix = this._matrix.clone();
              var l = this._parent;
              (l && r.prepend(l.getGlobalMatrix(!0)), (r._updateVersion = o));
            }
            return e ? r : r.clone();
          },
          getViewMatrix: function () {
            return this.getGlobalMatrix().prepend(this.getView()._matrix);
          },
          getApplyMatrix: function () {
            return this._applyMatrix;
          },
          setApplyMatrix: function (e) {
            (this._applyMatrix = this._canApplyMatrix && !!e) && this.transform(null, !0);
          },
          getTransformContent: "#getApplyMatrix",
          setTransformContent: "#setApplyMatrix",
        },
        {
          getProject: function () {
            return this._project;
          },
          _setProject: function (e, r) {
            if (this._project !== e) {
              (this._project && this._installEvents(!1), (this._project = e));
              for (var o = this._children, l = 0, d = o && o.length; l < d; l++)
                o[l]._setProject(e);
              r = !0;
            }
            r && this._installEvents(!0);
          },
          getView: function () {
            return this._project._view;
          },
          _installEvents: function e(r) {
            e.base.call(this, r);
            for (var o = this._children, l = 0, d = o && o.length; l < d; l++)
              o[l]._installEvents(r);
          },
          getLayer: function () {
            for (var e = this; (e = e._parent);) if (e instanceof ft) return e;
            return null;
          },
          getParent: function () {
            return this._parent;
          },
          setParent: function (e) {
            return e.addChild(this);
          },
          _getOwner: "#getParent",
          getChildren: function () {
            return this._children;
          },
          setChildren: function (e) {
            (this.removeChildren(), this.addChildren(e));
          },
          getFirstChild: function () {
            return (this._children && this._children[0]) || null;
          },
          getLastChild: function () {
            return (this._children && this._children[this._children.length - 1]) || null;
          },
          getNextSibling: function () {
            var e = this._getOwner();
            return (e && e._children[this._index + 1]) || null;
          },
          getPreviousSibling: function () {
            var e = this._getOwner();
            return (e && e._children[this._index - 1]) || null;
          },
          getIndex: function () {
            return this._index;
          },
          equals: function (e) {
            return (
              e === this ||
              (e &&
                this._class === e._class &&
                this._style.equals(e._style) &&
                this._matrix.equals(e._matrix) &&
                this._locked === e._locked &&
                this._visible === e._visible &&
                this._blendMode === e._blendMode &&
                this._opacity === e._opacity &&
                this._clipMask === e._clipMask &&
                this._guide === e._guide &&
                this._equals(e)) ||
              !1
            );
          },
          _equals: function (e) {
            return s.equals(this._children, e._children);
          },
          clone: function (e) {
            var r = new this.constructor(ne.NO_INSERT),
              o = this._children,
              l = s.pick(e ? e.insert : t, e === t || e === !0),
              d = s.pick(e ? e.deep : t, !0);
            (o && r.copyAttributes(this),
              (!o || d) && r.copyContent(this),
              o || r.copyAttributes(this),
              l && r.insertAbove(this));
            var c = this._name,
              h = this._parent;
            if (c && h) {
              for (var o = h._children, m = c, p = 1; o[c];) c = m + " " + p++;
              c !== m && r.setName(c);
            }
            return r;
          },
          copyContent: function (e) {
            for (var r = e._children, o = 0, l = r && r.length; o < l; o++)
              this.addChild(r[o].clone(!1), !0);
          },
          copyAttributes: function (e, r) {
            this.setStyle(e._style);
            for (
              var o = ["_locked", "_visible", "_blendMode", "_opacity", "_clipMask", "_guide"],
                l = 0,
                d = o.length;
              l < d;
              l++
            ) {
              var c = o[l];
              e.hasOwnProperty(c) && (this[c] = e[c]);
            }
            (r || this._matrix.set(e._matrix, !0),
              this.setApplyMatrix(e._applyMatrix),
              this.setPivot(e._pivot),
              this.setSelection(e._selection));
            var h = e._data,
              m = e._name;
            ((this._data = h ? s.clone(h) : null), m && this.setName(m));
          },
          rasterize: function (e, r) {
            var o = this.getStrokeBounds(),
              l = (e || this.getView().getResolution()) / 72,
              d = o.getTopLeft().floor(),
              c = o.getBottomRight().ceil(),
              h = new q(c.subtract(d)),
              m = new tr(ne.NO_INSERT);
            if (!h.isZero()) {
              var p = je.getCanvas(h.multiply(l)),
                y = p.getContext("2d"),
                x = new Q().scale(l).translate(d.negate());
              (y.save(),
                x.applyToContext(y),
                this.draw(y, new s({ matrices: [x] })),
                y.restore(),
                m.setCanvas(p));
            }
            return (
              m.transform(new Q().translate(d.add(h.divide(2))).scale(1 / l)),
              (r === t || r) && m.insertAbove(this),
              m
            );
          },
          contains: function () {
            return !!this._contains(this._matrix._inverseTransform(R.read(arguments)));
          },
          _contains: function (e) {
            var r = this._children;
            if (r) {
              for (var o = r.length - 1; o >= 0; o--) if (r[o].contains(e)) return !0;
              return !1;
            }
            return e.isInside(this.getInternalBounds());
          },
          isInside: function () {
            return X.read(arguments).contains(this.getBounds());
          },
          _asPathItem: function () {
            return new _e.Rectangle({
              rectangle: this.getInternalBounds(),
              matrix: this._matrix,
              insert: !1,
            });
          },
          intersects: function (e, r) {
            return e instanceof ne
              ? this._asPathItem().getIntersections(e._asPathItem(), null, r, !0).length > 0
              : !1;
          },
        },
        new (function () {
          function e() {
            return this._hitTest(R.read(arguments), rr.getOptions(arguments));
          }
          function r() {
            var l = R.read(arguments),
              d = rr.getOptions(arguments),
              c = [];
            return (this._hitTest(l, s.set({ all: c }, d)), c);
          }
          function o(l, d, c, h) {
            var m = this._children;
            if (m)
              for (var p = m.length - 1; p >= 0; p--) {
                var y = m[p],
                  x = y !== h && y._hitTest(l, d, c);
                if (x && !d.all) return x;
              }
            return null;
          }
          return (
            ct.inject({ hitTest: e, hitTestAll: r, _hitTest: o }),
            { hitTest: e, hitTestAll: r, _hitTestChildren: o }
          );
        })(),
        {
          _hitTest: function (e, r, o) {
            if (this._locked || !this._visible || (this._guide && !r.guides) || this.isEmpty())
              return null;
            var l = this._matrix,
              d = o ? o.appended(l) : this.getGlobalMatrix().prepend(this.getView()._matrix),
              c = Math.max(r.tolerance, 1e-12),
              h = (r._tolerancePadding = new q(_e._getStrokePadding(c, l._shiftless().invert())));
            if (
              ((e = l._inverseTransform(e)),
              !e ||
                (!this._children &&
                  !this.getBounds({ internal: !0, stroke: !0, handle: !0 })
                    .expand(h.multiply(2))
                    ._containsPoint(e)))
            )
              return null;
            var m = !(
                (r.guides && !this._guide) ||
                (r.selected && !this.isSelected()) ||
                (r.type && r.type !== s.hyphenate(this._class)) ||
                (r.class && !(this instanceof r.class))
              ),
              p = r.match,
              y = this,
              x,
              b;
            function S(C) {
              return (C && p && !p(C) && (C = null), C && r.all && r.all.push(C), C);
            }
            function v(C, T) {
              var W = T ? x["get" + T]() : y.getPosition();
              if (e.subtract(W).divide(h).length <= 1)
                return new rr(C, y, { name: T ? s.hyphenate(T) : C, point: W });
            }
            var w = r.position,
              _ = r.center,
              P = r.bounds;
            if (m && this._parent && (w || _ || P)) {
              if (
                ((_ || P) && (x = this.getInternalBounds()),
                (b = (w && v("position")) || (_ && v("center", "Center"))),
                !b && P)
              )
                for (
                  var D = [
                      "TopLeft",
                      "TopRight",
                      "BottomLeft",
                      "BottomRight",
                      "LeftCenter",
                      "TopCenter",
                      "RightCenter",
                      "BottomCenter",
                    ],
                    I = 0;
                  I < 8 && !b;
                  I++
                )
                  b = v("bounds", D[I]);
              b = S(b);
            }
            return (
              b ||
                (b =
                  this._hitTestChildren(e, r, d) ||
                  (m &&
                    S(
                      this._hitTestSelf(
                        e,
                        r,
                        d,
                        this.getStrokeScaling() ? null : d._shiftless().invert()
                      )
                    )) ||
                  null),
              b && b.point && (b.point = l.transform(b.point)),
              b
            );
          },
          _hitTestSelf: function (e, r) {
            if (r.fill && this.hasFill() && this._contains(e)) return new rr("fill", this);
          },
          matches: function (e, r) {
            function o(h, m) {
              for (var p in h)
                if (h.hasOwnProperty(p)) {
                  var y = h[p],
                    x = m[p];
                  if (s.isPlainObject(y) && s.isPlainObject(x)) {
                    if (!o(y, x)) return !1;
                  } else if (!s.equals(y, x)) return !1;
                }
              return !0;
            }
            var l = typeof e;
            if (l === "object") {
              for (var d in e) if (e.hasOwnProperty(d) && !this.matches(d, e[d])) return !1;
              return !0;
            } else {
              if (l === "function") return e(this);
              if (e === "match") return r(this);
              var c = /^(empty|editable)$/.test(e)
                ? this["is" + s.capitalize(e)]()
                : e === "type"
                  ? s.hyphenate(this._class)
                  : this[e];
              if (e === "class") {
                if (typeof r == "function") return this instanceof r;
                c = this._class;
              }
              if (typeof r == "function") return !!r(c);
              if (r) {
                if (r.test) return r.test(c);
                if (s.isPlainObject(r)) return o(r, c);
              }
              return s.equals(c, r);
            }
          },
          getItems: function (e) {
            return ne._getItems(this, e, this._matrix);
          },
          getItem: function (e) {
            return ne._getItems(this, e, this._matrix, null, !0)[0] || null;
          },
          statics: {
            _getItems: function e(r, o, l, d, c) {
              if (!d) {
                var h = typeof o == "object" && o,
                  m = h && h.overlapping,
                  p = h && h.inside,
                  y = m || p,
                  S = y && X.read([y]);
                ((d = {
                  items: [],
                  recursive: h && h.recursive !== !1,
                  inside: !!p,
                  overlapping: !!m,
                  rect: S,
                  path: m && new _e.Rectangle({ rectangle: S, insert: !1 }),
                }),
                  h && (o = s.filter({}, o, { recursive: !0, inside: !0, overlapping: !0 })));
              }
              var x = r._children,
                b = d.items,
                S = d.rect;
              l = S && (l || new Q());
              for (var v = 0, w = x && x.length; v < w; v++) {
                var _ = x[v],
                  P = l && l.appended(_._matrix),
                  D = !0;
                if (S) {
                  var y = _.getBounds(P);
                  if (!S.intersects(y)) continue;
                  S.contains(y) ||
                    (d.overlapping && (y.contains(S) || d.path.intersects(_, P))) ||
                    (D = !1);
                }
                if (
                  (D && _.matches(o) && (b.push(_), c)) ||
                  (d.recursive !== !1 && e(_, o, P, d, c), c && b.length > 0)
                )
                  break;
              }
              return b;
            },
          },
        },
        {
          importJSON: function (e) {
            var r = s.importJSON(e, this);
            return r !== this ? this.addChild(r) : r;
          },
          addChild: function (e) {
            return this.insertChild(t, e);
          },
          insertChild: function (e, r) {
            var o = r ? this.insertChildren(e, [r]) : null;
            return o && o[0];
          },
          addChildren: function (e) {
            return this.insertChildren(this._children.length, e);
          },
          insertChildren: function (e, r) {
            var o = this._children;
            if (o && r && r.length > 0) {
              r = s.slice(r);
              for (var l = {}, d = r.length - 1; d >= 0; d--) {
                var c = r[d],
                  h = c && c._id;
                !c || l[h] ? r.splice(d, 1) : (c._remove(!1, !0), (l[h] = !0));
              }
              s.splice(o, r, e, 0);
              for (var m = this._project, p = m._changes, d = 0, y = r.length; d < y; d++) {
                var c = r[d],
                  x = c._name;
                ((c._parent = this), c._setProject(m, !0), x && c.setName(x), p && c._changed(5));
              }
              this._changed(11);
            } else r = null;
            return r;
          },
          _insertItem: "#insertChild",
          _insertAt: function (e, r) {
            var o = e && e._getOwner(),
              l = e !== this && o ? this : null;
            return (l && (l._remove(!1, !0), o._insertItem(e._index + r, l)), l);
          },
          insertAbove: function (e) {
            return this._insertAt(e, 1);
          },
          insertBelow: function (e) {
            return this._insertAt(e, 0);
          },
          sendToBack: function () {
            var e = this._getOwner();
            return e ? e._insertItem(0, this) : null;
          },
          bringToFront: function () {
            var e = this._getOwner();
            return e ? e._insertItem(t, this) : null;
          },
          appendTop: "#addChild",
          appendBottom: function (e) {
            return this.insertChild(0, e);
          },
          moveAbove: "#insertAbove",
          moveBelow: "#insertBelow",
          addTo: function (e) {
            return e._insertItem(t, this);
          },
          copyTo: function (e) {
            return this.clone(!1).addTo(e);
          },
          reduce: function (e) {
            var r = this._children;
            if (r && r.length === 1) {
              var o = r[0].reduce(e);
              return (this._parent ? (o.insertAbove(this), this.remove()) : o.remove(), o);
            }
            return this;
          },
          _removeNamed: function () {
            var e = this._getOwner();
            if (e) {
              var r = e._children,
                o = e._namedChildren,
                l = this._name,
                d = o[l],
                c = d ? d.indexOf(this) : -1;
              c !== -1 &&
                (r[l] == this && delete r[l],
                d.splice(c, 1),
                d.length ? (r[l] = d[0]) : delete o[l]);
            }
          },
          _remove: function (e, r) {
            var o = this._getOwner(),
              l = this._project,
              d = this._index;
            return o
              ? (this._name && this._removeNamed(),
                d != null &&
                  (l._activeLayer === this &&
                    (l._activeLayer = this.getNextSibling() || this.getPreviousSibling()),
                  s.splice(o._children, null, d, 1)),
                this._installEvents(!1),
                e && l._changes && this._changed(5),
                r && o._changed(11, this),
                (this._parent = null),
                !0)
              : !1;
          },
          remove: function () {
            return this._remove(!0, !0);
          },
          replaceWith: function (e) {
            var r = e && e.insertBelow(this);
            return (r && this.remove(), r);
          },
          removeChildren: function (e, r) {
            if (!this._children) return null;
            ((e = e || 0), (r = s.pick(r, this._children.length)));
            for (var o = s.splice(this._children, null, e, r - e), l = o.length - 1; l >= 0; l--)
              o[l]._remove(!0, !1);
            return (o.length > 0 && this._changed(11), o);
          },
          clear: "#removeChildren",
          reverseChildren: function () {
            if (this._children) {
              this._children.reverse();
              for (var e = 0, r = this._children.length; e < r; e++) this._children[e]._index = e;
              this._changed(11);
            }
          },
          isEmpty: function () {
            var e = this._children;
            return !e || !e.length;
          },
          isEditable: function () {
            for (var e = this; e;) {
              if (!e._visible || e._locked) return !1;
              e = e._parent;
            }
            return !0;
          },
          hasFill: function () {
            return this.getStyle().hasFill();
          },
          hasStroke: function () {
            return this.getStyle().hasStroke();
          },
          hasShadow: function () {
            return this.getStyle().hasShadow();
          },
          _getOrder: function (e) {
            function r(h) {
              var m = [];
              do m.unshift(h);
              while ((h = h._parent));
              return m;
            }
            for (var o = r(this), l = r(e), d = 0, c = Math.min(o.length, l.length); d < c; d++)
              if (o[d] != l[d]) return o[d]._index < l[d]._index ? 1 : -1;
            return 0;
          },
          hasChildren: function () {
            return this._children && this._children.length > 0;
          },
          isInserted: function () {
            return this._parent ? this._parent.isInserted() : !1;
          },
          isAbove: function (e) {
            return this._getOrder(e) === -1;
          },
          isBelow: function (e) {
            return this._getOrder(e) === 1;
          },
          isParent: function (e) {
            return this._parent === e;
          },
          isChild: function (e) {
            return e && e._parent === this;
          },
          isDescendant: function (e) {
            for (var r = this; (r = r._parent);) if (r === e) return !0;
            return !1;
          },
          isAncestor: function (e) {
            return e ? e.isDescendant(this) : !1;
          },
          isSibling: function (e) {
            return this._parent === e._parent;
          },
          isGroupedWith: function (e) {
            for (var r = this._parent; r;) {
              if (r._parent && /^(Group|Layer|CompoundPath)$/.test(r._class) && e.isDescendant(r))
                return !0;
              r = r._parent;
            }
            return !1;
          },
        },
        s.each(
          ["rotate", "scale", "shear", "skew"],
          function (e) {
            var r = e === "rotate";
            this[e] = function () {
              var o = (r ? s : R).read(arguments),
                l = R.read(arguments, 0, { readNull: !0 });
              return this.transform(new Q()[e](o, l || this.getPosition(!0)));
            };
          },
          {
            translate: function () {
              var e = new Q();
              return this.transform(e.translate.apply(e, arguments));
            },
            transform: function (e, r, o, l) {
              var d = this._matrix,
                c = e && !e.isIdentity(),
                h =
                  (r || this._applyMatrix) && (!d.isIdentity() || c || (r && o && this._children));
              if (!c && !h) return this;
              if (c) {
                (!e.isInvertible() && d.isInvertible() && (d._backup = d.getValues()),
                  d.prepend(e, !0));
                var m = this._style,
                  p = m.getFillColor(!0),
                  y = m.getStrokeColor(!0);
                (p && p.transform(e), y && y.transform(e));
              }
              if (h && (h = this._transformContent(d, o, l))) {
                var x = this._pivot;
                (x && d._transformPoint(x, x, !0),
                  d.reset(!0),
                  l && this._canApplyMatrix && (this._applyMatrix = !0));
              }
              var b = this._bounds,
                S = this._position;
              (c || h) && this._changed(9);
              var v = c && b && e.decompose();
              if (v && v.skewing.isZero() && v.rotation % 90 === 0) {
                for (var w in b) {
                  var _ = b[w];
                  if (_.nonscaling) delete b[w];
                  else if (h || !_.internal) {
                    var P = _.rect;
                    e._transformBounds(P, P);
                  }
                }
                this._bounds = b;
                var D = b[this._getBoundsCacheKey(this._boundsOptions || {})];
                D && (this._position = D.rect.getCenter(!0));
              } else c && S && this._pivot && (this._position = e._transformPoint(S, S));
              return this;
            },
            _transformContent: function (e, r, o) {
              var l = this._children;
              if (l) {
                for (var d = 0, c = l.length; d < c; d++) l[d].transform(e, !0, r, o);
                return !0;
              }
            },
            globalToLocal: function () {
              return this.getGlobalMatrix(!0)._inverseTransform(R.read(arguments));
            },
            localToGlobal: function () {
              return this.getGlobalMatrix(!0)._transformPoint(R.read(arguments));
            },
            parentToLocal: function () {
              return this._matrix._inverseTransform(R.read(arguments));
            },
            localToParent: function () {
              return this._matrix._transformPoint(R.read(arguments));
            },
            fitBounds: function (e, r) {
              e = X.read(arguments);
              var o = this.getBounds(),
                l = o.height / o.width,
                d = e.height / e.width,
                c = (r ? l > d : l < d) ? e.width / o.width : e.height / o.height,
                h = new X(new R(), new q(o.width * c, o.height * c));
              (h.setCenter(e.getCenter()), this.setBounds(h));
            },
          }
        ),
        {
          _setStyles: function (e, r, o) {
            var l = this._style,
              d = this._matrix;
            if (
              (l.hasFill() && (e.fillStyle = l.getFillColor().toCanvasStyle(e, d)), l.hasStroke())
            ) {
              ((e.strokeStyle = l.getStrokeColor().toCanvasStyle(e, d)),
                (e.lineWidth = l.getStrokeWidth()));
              var c = l.getStrokeJoin(),
                h = l.getStrokeCap(),
                m = l.getMiterLimit();
              if (
                (c && (e.lineJoin = c),
                h && (e.lineCap = h),
                m && (e.miterLimit = m),
                Se.support.nativeDash)
              ) {
                var p = l.getDashArray(),
                  y = l.getDashOffset();
                p &&
                  p.length &&
                  ("setLineDash" in e
                    ? (e.setLineDash(p), (e.lineDashOffset = y))
                    : ((e.mozDash = p), (e.mozDashOffset = y)));
              }
            }
            if (l.hasShadow()) {
              var x = r.pixelRatio || 1,
                b = o._shiftless().prepend(new Q().scale(x, x)),
                S = b.transform(new R(l.getShadowBlur(), 0)),
                v = b.transform(this.getShadowOffset());
              ((e.shadowColor = l.getShadowColor().toCanvasStyle(e)),
                (e.shadowBlur = S.getLength()),
                (e.shadowOffsetX = v.x),
                (e.shadowOffsetY = v.y));
            }
          },
          draw: function (e, r, o) {
            var l = (this._updateVersion = this._project._updateVersion);
            if (!(!this._visible || this._opacity === 0)) {
              var d = r.matrices,
                c = r.viewMatrix,
                h = this._matrix,
                m = d[d.length - 1].appended(h);
              if (m.isInvertible()) {
                ((c = c ? c.appended(m) : m),
                  d.push(m),
                  r.updateMatrix && ((m._updateVersion = l), (this._globalMatrix = m)));
                var p = this._blendMode,
                  y = this._opacity,
                  x = p === "normal",
                  b = Ts.nativeModes[p],
                  S =
                    (x && y === 1) ||
                    r.dontStart ||
                    r.clip ||
                    ((b || (x && y < 1)) && this._canComposite()),
                  v = r.pixelRatio || 1,
                  w,
                  _,
                  P;
                if (!S) {
                  var D = this.getStrokeBounds(c);
                  if (!D.width || !D.height) return;
                  ((P = r.offset),
                    (_ = r.offset = D.getTopLeft().floor()),
                    (w = e),
                    (e = je.getContext(D.getSize().ceil().add(1).multiply(v))),
                    v !== 1 && e.scale(v, v));
                }
                e.save();
                var I = o ? o.appended(h) : this._canScaleStroke && !this.getStrokeScaling(!0) && c,
                  C = !S && r.clipItem,
                  T = !I || C;
                if (
                  (S
                    ? ((e.globalAlpha = y), b && (e.globalCompositeOperation = p))
                    : T && e.translate(-_.x, -_.y),
                  T && (S ? h : c).applyToContext(e),
                  C && r.clipItem.draw(e, r.extend({ clip: !0 })),
                  I)
                ) {
                  e.setTransform(v, 0, 0, v, 0, 0);
                  var W = r.offset;
                  W && e.translate(-W.x, -W.y);
                }
                (this._draw(e, r, c, I),
                  e.restore(),
                  d.pop(),
                  r.clip && !r.dontFinish && e.clip(),
                  S ||
                    (Ts.process(p, e, w, y, _.subtract(P).multiply(v)),
                    je.release(e),
                    (r.offset = P)));
              }
            }
          },
          _isUpdated: function (e) {
            var r = this._parent;
            if (r instanceof Ot) return r._isUpdated(e);
            var o = this._updateVersion === e;
            return (
              !o && r && r._visible && r._isUpdated(e) && ((this._updateVersion = e), (o = !0)),
              o
            );
          },
          _drawSelection: function (e, r, o, l, d) {
            var c = this._selection,
              h = c & 1,
              m = c & 2 || (h && this._selectBounds),
              p = c & 4;
            if ((this._drawSelected || (h = !1), (h || m || p) && this._isUpdated(d))) {
              var y,
                x = this.getSelectedColor(!0) || ((y = this.getLayer()) && y.getSelectedColor(!0)),
                b = r.appended(this.getGlobalMatrix(!0)),
                S = o / 2;
              if (
                ((e.strokeStyle = e.fillStyle = x ? x.toCanvasStyle(e) : "#009dec"),
                h && this._drawSelected(e, b, l),
                p)
              ) {
                var v = this.getPosition(!0),
                  w = v.x,
                  _ = v.y;
                (e.beginPath(), e.arc(w, _, S, 0, Math.PI * 2, !0), e.stroke());
                for (
                  var P = [
                      [0, -1],
                      [1, 0],
                      [0, 1],
                      [-1, 0],
                    ],
                    D = S,
                    I = o + 1,
                    C = 0;
                  C < 4;
                  C++
                ) {
                  var T = P[C],
                    W = T[0],
                    N = T[1];
                  (e.moveTo(w + W * D, _ + N * D), e.lineTo(w + W * I, _ + N * I), e.stroke());
                }
              }
              if (m) {
                var B = b._transformCorners(this.getInternalBounds());
                e.beginPath();
                for (var C = 0; C < 8; C++) e[C ? "lineTo" : "moveTo"](B[C], B[++C]);
                (e.closePath(), e.stroke());
                for (var C = 0; C < 8; C++) e.fillRect(B[C] - S, B[++C] - S, o, o);
              }
            }
          },
          _canComposite: function () {
            return !1;
          },
        },
        s.each(
          ["down", "drag", "up", "move"],
          function (e) {
            this["removeOn" + s.capitalize(e)] = function () {
              var r = {};
              return ((r[e] = !0), this.removeOn(r));
            };
          },
          {
            removeOn: function (e) {
              for (var r in e)
                if (e[r]) {
                  var o = "mouse" + r,
                    l = this._project,
                    d = (l._removeSets = l._removeSets || {});
                  ((d[o] = d[o] || {}), (d[o][this._id] = this));
                }
              return this;
            },
          }
        )
      ),
      it = ne.extend({
        _class: "Group",
        _selectBounds: !1,
        _selectChildren: !0,
        _serializeFields: { children: [] },
        initialize: function (r) {
          ((this._children = []),
            (this._namedChildren = {}),
            this._initialize(r) || this.addChildren(Array.isArray(r) ? r : arguments));
        },
        _changed: function e(r) {
          (e.base.call(this, r), r & 1026 && (this._clipItem = t));
        },
        _getClipItem: function () {
          var e = this._clipItem;
          if (e === t) {
            e = null;
            for (var r = this._children, o = 0, l = r.length; o < l; o++)
              if (r[o]._clipMask) {
                e = r[o];
                break;
              }
            this._clipItem = e;
          }
          return e;
        },
        isClipped: function () {
          return !!this._getClipItem();
        },
        setClipped: function (e) {
          var r = this.getFirstChild();
          r && r.setClipMask(e);
        },
        _getBounds: function e(r, o) {
          var l = this._getClipItem();
          return l
            ? l._getCachedBounds(r && r.appended(l._matrix), s.set({}, o, { stroke: !1 }))
            : e.base.call(this, r, o);
        },
        _hitTestChildren: function e(r, o, l) {
          var d = this._getClipItem();
          return (!d || d.contains(r)) && e.base.call(this, r, o, l, d);
        },
        _draw: function (e, r) {
          var o = r.clip,
            l = !o && this._getClipItem();
          ((r = r.extend({ clipItem: l, clip: !1 })),
            o
              ? (e.beginPath(), (r.dontStart = r.dontFinish = !0))
              : l && l.draw(e, r.extend({ clip: !0 })));
          for (var d = this._children, c = 0, h = d.length; c < h; c++) {
            var m = d[c];
            m !== l && m.draw(e, r);
          }
        },
      }),
      ft = it.extend({
        _class: "Layer",
        initialize: function () {
          it.apply(this, arguments);
        },
        _getOwner: function () {
          return this._parent || (this._index != null && this._project);
        },
        isInserted: function e() {
          return this._parent ? e.base.call(this) : this._index != null;
        },
        activate: function () {
          this._project._activeLayer = this;
        },
        _hitTestSelf: function () {},
      }),
      nt = ne.extend(
        {
          _class: "Shape",
          _applyMatrix: !1,
          _canApplyMatrix: !1,
          _canScaleStroke: !0,
          _serializeFields: { type: null, size: null, radius: null },
          initialize: function (r, o) {
            this._initialize(r, o);
          },
          _equals: function (e) {
            return (
              this._type === e._type &&
              this._size.equals(e._size) &&
              s.equals(this._radius, e._radius)
            );
          },
          copyContent: function (e) {
            (this.setType(e._type), this.setSize(e._size), this.setRadius(e._radius));
          },
          getType: function () {
            return this._type;
          },
          setType: function (e) {
            this._type = e;
          },
          getShape: "#getType",
          setShape: "#setType",
          getSize: function () {
            var e = this._size;
            return new Z(e.width, e.height, this, "setSize");
          },
          setSize: function () {
            var e = q.read(arguments);
            if (!this._size) this._size = e.clone();
            else if (!this._size.equals(e)) {
              var r = this._type,
                o = e.width,
                l = e.height;
              (r === "rectangle"
                ? this._radius.set(q.min(this._radius, e.divide(2)))
                : r === "circle"
                  ? ((o = l = (o + l) / 2), (this._radius = o / 2))
                  : r === "ellipse" && this._radius._set(o / 2, l / 2),
                this._size._set(o, l),
                this._changed(9));
            }
          },
          getRadius: function () {
            var e = this._radius;
            return this._type === "circle" ? e : new Z(e.width, e.height, this, "setRadius");
          },
          setRadius: function (e) {
            var r = this._type;
            if (r === "circle") {
              if (e === this._radius) return;
              var o = e * 2;
              ((this._radius = e), this._size._set(o, o));
            } else if (((e = q.read(arguments)), !this._radius)) this._radius = e.clone();
            else {
              if (this._radius.equals(e)) return;
              if ((this._radius.set(e), r === "rectangle")) {
                var o = q.max(this._size, e.multiply(2));
                this._size.set(o);
              } else r === "ellipse" && this._size._set(e.width * 2, e.height * 2);
            }
            this._changed(9);
          },
          isEmpty: function () {
            return !1;
          },
          toPath: function (e) {
            var r = new _e[s.capitalize(this._type)]({
              center: new R(),
              size: this._size,
              radius: this._radius,
              insert: !1,
            });
            return (
              r.copyAttributes(this),
              Se.settings.applyMatrix && r.setApplyMatrix(!0),
              (e === t || e) && r.insertAbove(this),
              r
            );
          },
          toShape: "#clone",
          _asPathItem: function () {
            return this.toPath(!1);
          },
          _draw: function (e, r, o, l) {
            var d = this._style,
              c = d.hasFill(),
              h = d.hasStroke(),
              m = r.dontFinish || r.clip,
              p = !l;
            if (c || h || m) {
              var y = this._type,
                x = this._radius,
                b = y === "circle";
              if ((r.dontStart || e.beginPath(), p && b)) e.arc(0, 0, x, 0, Math.PI * 2, !0);
              else {
                var S = b ? x : x.width,
                  v = b ? x : x.height,
                  w = this._size,
                  _ = w.width,
                  P = w.height;
                if (p && y === "rectangle" && S === 0 && v === 0) e.rect(-_ / 2, -P / 2, _, P);
                else {
                  var D = _ / 2,
                    I = P / 2,
                    C = 1 - 0.5522847498307936,
                    T = S * C,
                    W = v * C,
                    N = [
                      -D,
                      -I + v,
                      -D,
                      -I + W,
                      -D + T,
                      -I,
                      -D + S,
                      -I,
                      D - S,
                      -I,
                      D - T,
                      -I,
                      D,
                      -I + W,
                      D,
                      -I + v,
                      D,
                      I - v,
                      D,
                      I - W,
                      D - T,
                      I,
                      D - S,
                      I,
                      -D + S,
                      I,
                      -D + T,
                      I,
                      -D,
                      I - W,
                      -D,
                      I - v,
                    ];
                  (l && l.transform(N, N, 32),
                    e.moveTo(N[0], N[1]),
                    e.bezierCurveTo(N[2], N[3], N[4], N[5], N[6], N[7]),
                    D !== S && e.lineTo(N[8], N[9]),
                    e.bezierCurveTo(N[10], N[11], N[12], N[13], N[14], N[15]),
                    I !== v && e.lineTo(N[16], N[17]),
                    e.bezierCurveTo(N[18], N[19], N[20], N[21], N[22], N[23]),
                    D !== S && e.lineTo(N[24], N[25]),
                    e.bezierCurveTo(N[26], N[27], N[28], N[29], N[30], N[31]));
                }
              }
              e.closePath();
            }
            !m &&
              (c || h) &&
              (this._setStyles(e, r, o),
              c && (e.fill(d.getFillRule()), (e.shadowColor = "rgba(0,0,0,0)")),
              h && e.stroke());
          },
          _canComposite: function () {
            return !(this.hasFill() && this.hasStroke());
          },
          _getBounds: function (e, r) {
            var o = new X(this._size).setCenter(0, 0),
              l = this._style,
              d = r.stroke && l.hasStroke() && l.getStrokeWidth();
            return (
              e && (o = e._transformBounds(o)),
              d ? o.expand(_e._getStrokePadding(d, this._getStrokeMatrix(e, r))) : o
            );
          },
        },
        new (function () {
          function e(o, l, d) {
            var c = o._radius;
            if (!c.isZero())
              for (var h = o._size.divide(2), m = 1; m <= 4; m++) {
                var p = new R(m > 1 && m < 4 ? -1 : 1, m > 2 ? -1 : 1),
                  y = p.multiply(h),
                  x = y.subtract(p.multiply(c)),
                  b = new X(d ? y.add(p.multiply(d)) : y, x);
                if (b.contains(l)) return { point: x, quadrant: m };
              }
          }
          function r(o, l, d, c) {
            var h = o.divide(l);
            return (
              (!c || h.isInQuadrant(c)) &&
              h.subtract(h.normalize()).multiply(l).divide(d).length <= 1
            );
          }
          return {
            _contains: function o(l) {
              if (this._type === "rectangle") {
                var d = e(this, l);
                return d
                  ? l.subtract(d.point).divide(this._radius).getLength() <= 1
                  : o.base.call(this, l);
              } else return l.divide(this.size).getLength() <= 0.5;
            },
            _hitTestSelf: function o(l, d, c, h) {
              var m = !1,
                p = this._style,
                y = d.stroke && p.hasStroke(),
                x = d.fill && p.hasFill();
              if (y || x) {
                var b = this._type,
                  S = this._radius,
                  v = y ? p.getStrokeWidth() / 2 : 0,
                  w = d._tolerancePadding.add(_e._getStrokePadding(v, !p.getStrokeScaling() && h));
                if (b === "rectangle") {
                  var _ = w.multiply(2),
                    P = e(this, l, _);
                  if (P) m = r(l.subtract(P.point), S, w, P.quadrant);
                  else {
                    var D = new X(this._size).setCenter(0, 0),
                      I = D.expand(_),
                      C = D.expand(_.negate());
                    m = I._containsPoint(l) && !C._containsPoint(l);
                  }
                } else m = r(l, S, w);
              }
              return m ? new rr(y ? "stroke" : "fill", this) : o.base.apply(this, arguments);
            },
          };
        })(),
        {
          statics: new (function () {
            function e(r, o, l, d, c) {
              var h = new nt(s.getNamed(c), o);
              return ((h._type = r), (h._size = l), (h._radius = d), h);
            }
            return {
              Circle: function () {
                var r = R.readNamed(arguments, "center"),
                  o = s.readNamed(arguments, "radius");
                return e("circle", r, new q(o * 2), o, arguments);
              },
              Rectangle: function () {
                var r = X.readNamed(arguments, "rectangle"),
                  o = q.min(q.readNamed(arguments, "radius"), r.getSize(!0).divide(2));
                return e("rectangle", r.getCenter(!0), r.getSize(!0), o, arguments);
              },
              Ellipse: function () {
                var r = nt._readEllipse(arguments),
                  o = r.radius;
                return e("ellipse", r.center, o.multiply(2), o, arguments);
              },
              _readEllipse: function (r) {
                var o, l;
                if (s.hasNamed(r, "radius"))
                  ((o = R.readNamed(r, "center")), (l = q.readNamed(r, "radius")));
                else {
                  var d = X.readNamed(r, "rectangle");
                  ((o = d.getCenter(!0)), (l = d.getSize(!0).divide(2)));
                }
                return { center: o, radius: l };
              },
            };
          })(),
        }
      ),
      tr = ne.extend(
        {
          _class: "Raster",
          _applyMatrix: !1,
          _canApplyMatrix: !1,
          _boundsOptions: { stroke: !1, handle: !1 },
          _serializeFields: { crossOrigin: null, source: null },
          _prioritize: ["crossOrigin"],
          initialize: function (r, o) {
            if (!this._initialize(r, o !== t && R.read(arguments, 1))) {
              var l = typeof r == "string" ? a.getElementById(r) : r;
              l ? this.setImage(l) : this.setSource(r);
            }
            this._size || ((this._size = new q()), (this._loaded = !1));
          },
          _equals: function (e) {
            return this.getSource() === e.getSource();
          },
          copyContent: function (e) {
            var r = e._image,
              o = e._canvas;
            if (r) this._setImage(r);
            else if (o) {
              var l = je.getCanvas(e._size);
              (l.getContext("2d").drawImage(o, 0, 0), this._setImage(l));
            }
            this._crossOrigin = e._crossOrigin;
          },
          getSize: function () {
            var e = this._size;
            return new Z(e ? e.width : 0, e ? e.height : 0, this, "setSize");
          },
          setSize: function () {
            var e = q.read(arguments);
            if (!e.equals(this._size))
              if (e.width > 0 && e.height > 0) {
                var r = this.getElement();
                (this._setImage(je.getCanvas(e)),
                  r && this.getContext(!0).drawImage(r, 0, 0, e.width, e.height));
              } else (this._canvas && je.release(this._canvas), (this._size = e.clone()));
          },
          getWidth: function () {
            return this._size ? this._size.width : 0;
          },
          setWidth: function (e) {
            this.setSize(e, this.getHeight());
          },
          getHeight: function () {
            return this._size ? this._size.height : 0;
          },
          setHeight: function (e) {
            this.setSize(this.getWidth(), e);
          },
          getLoaded: function () {
            return this._loaded;
          },
          isEmpty: function () {
            var e = this._size;
            return !e || (e.width === 0 && e.height === 0);
          },
          getResolution: function () {
            var e = this._matrix,
              r = new R(0, 0).transform(e),
              o = new R(1, 0).transform(e).subtract(r),
              l = new R(0, 1).transform(e).subtract(r);
            return new q(72 / o.getLength(), 72 / l.getLength());
          },
          getPpi: "#getResolution",
          getImage: function () {
            return this._image;
          },
          setImage: function (e) {
            var r = this;
            function o(l) {
              var d = r.getView(),
                c = (l && l.type) || "load";
              d && r.responds(c) && ((Se = d._scope), r.emit(c, new ro(l)));
            }
            (this._setImage(e),
              this._loaded
                ? setTimeout(o, 0)
                : e &&
                  Ye.add(e, {
                    load: function (l) {
                      (r._setImage(e), o(l));
                    },
                    error: o,
                  }));
          },
          _setImage: function (e) {
            (this._canvas && je.release(this._canvas),
              e && e.getContext
                ? ((this._image = null), (this._canvas = e), (this._loaded = !0))
                : ((this._image = e),
                  (this._canvas = null),
                  (this._loaded = !!(e && e.src && e.complete))),
              (this._size = new q(
                e ? e.naturalWidth || e.width : 0,
                e ? e.naturalHeight || e.height : 0
              )),
              (this._context = null),
              this._changed(521));
          },
          getCanvas: function () {
            if (!this._canvas) {
              var e = je.getContext(this._size);
              try {
                (this._image && e.drawImage(this._image, 0, 0), (this._canvas = e.canvas));
              } catch {
                je.release(e);
              }
            }
            return this._canvas;
          },
          setCanvas: "#setImage",
          getContext: function (e) {
            return (
              this._context || (this._context = this.getCanvas().getContext("2d")),
              e && ((this._image = null), this._changed(513)),
              this._context
            );
          },
          setContext: function (e) {
            this._context = e;
          },
          getSource: function () {
            var e = this._image;
            return (e && e.src) || this.toDataURL();
          },
          setSource: function (e) {
            var r = new i.Image(),
              o = this._crossOrigin;
            (o && (r.crossOrigin = o), (r.src = e), this.setImage(r));
          },
          getCrossOrigin: function () {
            var e = this._image;
            return (e && e.crossOrigin) || this._crossOrigin || "";
          },
          setCrossOrigin: function (e) {
            this._crossOrigin = e;
            var r = this._image;
            r && (r.crossOrigin = e);
          },
          getElement: function () {
            return this._canvas || (this._loaded && this._image);
          },
        },
        {
          beans: !1,
          getSubCanvas: function () {
            var e = X.read(arguments),
              r = je.getContext(e.getSize());
            return (
              r.drawImage(this.getCanvas(), e.x, e.y, e.width, e.height, 0, 0, e.width, e.height),
              r.canvas
            );
          },
          getSubRaster: function () {
            var e = X.read(arguments),
              r = new tr(ne.NO_INSERT);
            return (
              r._setImage(this.getSubCanvas(e)),
              r.translate(e.getCenter().subtract(this.getSize().divide(2))),
              r._matrix.prepend(this._matrix),
              r.insertAbove(this),
              r
            );
          },
          toDataURL: function () {
            var e = this._image,
              r = e && e.src;
            if (/^data:/.test(r)) return r;
            var o = this.getCanvas();
            return o ? o.toDataURL.apply(o, arguments) : null;
          },
          drawImage: function (e) {
            var r = R.read(arguments, 1);
            this.getContext(!0).drawImage(e, r.x, r.y);
          },
          getAverageColor: function (e) {
            var r, o;
            if (
              (e
                ? e instanceof ei
                  ? ((o = e), (r = e.getBounds()))
                  : typeof e == "object" &&
                    ("width" in e
                      ? (r = new X(e))
                      : "x" in e && (r = new X(e.x - 0.5, e.y - 0.5, 1, 1)))
                : (r = this.getBounds()),
              !r)
            )
              return null;
            var l = 32,
              d = Math.min(r.width, l),
              c = Math.min(r.height, l),
              h = tr._sampleContext;
            (h
              ? h.clearRect(0, 0, l + 1, l + 1)
              : (h = tr._sampleContext = je.getContext(new q(l))),
              h.save());
            var m = new Q().scale(d / r.width, c / r.height).translate(-r.x, -r.y);
            (m.applyToContext(h),
              o && o.draw(h, new s({ clip: !0, matrices: [m] })),
              this._matrix.applyToContext(h));
            var p = this.getElement(),
              y = this._size;
            (p && h.drawImage(p, -y.width / 2, -y.height / 2), h.restore());
            for (
              var x = h.getImageData(0.5, 0.5, Math.ceil(d), Math.ceil(c)).data,
                b = [0, 0, 0],
                S = 0,
                v = 0,
                w = x.length;
              v < w;
              v += 4
            ) {
              var _ = x[v + 3];
              ((S += _),
                (_ /= 255),
                (b[0] += x[v] * _),
                (b[1] += x[v + 1] * _),
                (b[2] += x[v + 2] * _));
            }
            for (var v = 0; v < 3; v++) b[v] /= S;
            return S ? kt.read(b) : null;
          },
          getPixel: function () {
            var e = R.read(arguments),
              r = this.getContext().getImageData(e.x, e.y, 1, 1).data;
            return new kt("rgb", [r[0] / 255, r[1] / 255, r[2] / 255], r[3] / 255);
          },
          setPixel: function () {
            var e = R.read(arguments),
              r = kt.read(arguments),
              o = r._convert("rgb"),
              l = r._alpha,
              d = this.getContext(!0),
              c = d.createImageData(1, 1),
              h = c.data;
            ((h[0] = o[0] * 255),
              (h[1] = o[1] * 255),
              (h[2] = o[2] * 255),
              (h[3] = l != null ? l * 255 : 255),
              d.putImageData(c, e.x, e.y));
          },
          createImageData: function () {
            var e = q.read(arguments);
            return this.getContext().createImageData(e.width, e.height);
          },
          getImageData: function () {
            var e = X.read(arguments);
            return (
              e.isEmpty() && (e = new X(this._size)),
              this.getContext().getImageData(e.x, e.y, e.width, e.height)
            );
          },
          setImageData: function (e) {
            var r = R.read(arguments, 1);
            this.getContext(!0).putImageData(e, r.x, r.y);
          },
          _getBounds: function (e, r) {
            var o = new X(this._size).setCenter(0, 0);
            return e ? e._transformBounds(o) : o;
          },
          _hitTestSelf: function (e) {
            if (this._contains(e)) {
              var r = this;
              return new rr("pixel", r, {
                offset: e.add(r._size.divide(2)).round(),
                color: {
                  get: function () {
                    return r.getPixel(this.offset);
                  },
                },
              });
            }
          },
          _draw: function (e) {
            var r = this.getElement();
            r &&
              ((e.globalAlpha = this._opacity),
              e.drawImage(r, -this._size.width / 2, -this._size.height / 2));
          },
          _canComposite: function () {
            return !0;
          },
        }
      ),
      xs = ne.extend({
        _class: "SymbolItem",
        _applyMatrix: !1,
        _canApplyMatrix: !1,
        _boundsOptions: { stroke: !0 },
        _serializeFields: { symbol: null },
        initialize: function (r, o) {
          this._initialize(r, o !== t && R.read(arguments, 1)) ||
            this.setDefinition(r instanceof fr ? r : new fr(r));
        },
        _equals: function (e) {
          return this._definition === e._definition;
        },
        copyContent: function (e) {
          this.setDefinition(e._definition);
        },
        getDefinition: function () {
          return this._definition;
        },
        setDefinition: function (e) {
          ((this._definition = e), this._changed(9));
        },
        getSymbol: "#getDefinition",
        setSymbol: "#setDefinition",
        isEmpty: function () {
          return this._definition._item.isEmpty();
        },
        _getBounds: function (e, r) {
          var o = this._definition._item;
          return o._getCachedBounds(o._matrix.prepended(e), r);
        },
        _hitTestSelf: function (e, r, o) {
          var l = this._definition._item._hitTest(e, r, o);
          return (l && (l.item = this), l);
        },
        _draw: function (e, r) {
          this._definition._item.draw(e, r);
        },
      }),
      fr = s.extend({
        _class: "SymbolDefinition",
        initialize: function (r, o) {
          ((this._id = A.get()), (this.project = Se.project), r && this.setItem(r, o));
        },
        _serialize: function (e, r) {
          return r.add(this, function () {
            return s.serialize([this._class, this._item], e, !1, r);
          });
        },
        _changed: function (e) {
          (e & 8 && ne._clearBoundsCache(this), e & 1 && this.project._changed(e));
        },
        getItem: function () {
          return this._item;
        },
        setItem: function (e, r) {
          (e._symbol && (e = e.clone()),
            this._item && (this._item._symbol = null),
            (this._item = e),
            e.remove(),
            e.setSelected(!1),
            r || e.setPosition(new R()),
            (e._symbol = this),
            this._changed(9));
        },
        getDefinition: "#getItem",
        setDefinition: "#setItem",
        place: function (e) {
          return new xs(this, e);
        },
        clone: function () {
          return new fr(this._item.clone(!1));
        },
        equals: function (e) {
          return e === this || (e && this._item.equals(e._item)) || !1;
        },
      }),
      rr = s.extend({
        _class: "HitResult",
        initialize: function (r, o, l) {
          ((this.type = r), (this.item = o), l && this.inject(l));
        },
        statics: {
          getOptions: function (e) {
            var r = e && s.read(e);
            return s.set(
              {
                type: null,
                tolerance: Se.settings.hitTolerance,
                fill: !r,
                stroke: !r,
                segments: !r,
                handles: !1,
                ends: !1,
                position: !1,
                center: !1,
                bounds: !1,
                guides: !1,
                selected: !1,
              },
              r
            );
          },
        },
      }),
      ae = s.extend({
        _class: "Segment",
        beans: !0,
        _selection: 0,
        initialize: function (r, o, l, d, c, h) {
          var m = arguments.length,
            p,
            y,
            x,
            b;
          (m > 0 &&
            (r == null || typeof r == "object"
              ? m === 1 && r && "point" in r
                ? ((p = r.point), (y = r.handleIn), (x = r.handleOut), (b = r.selection))
                : ((p = r), (y = o), (x = l), (b = d))
              : ((p = [r, o]), (y = l !== t ? [l, d] : null), (x = c !== t ? [c, h] : null))),
            new vs(p, this, "_point"),
            new vs(y, this, "_handleIn"),
            new vs(x, this, "_handleOut"),
            b && this.setSelection(b));
        },
        _serialize: function (e, r) {
          var o = this._point,
            l = this._selection,
            d = l || this.hasHandles() ? [o, this._handleIn, this._handleOut] : o;
          return (l && d.push(l), s.serialize(d, e, !0, r));
        },
        _changed: function (e) {
          var r = this._path;
          if (r) {
            var o = r._curves,
              l = this._index,
              d;
            (o &&
              ((!e || e === this._point || e === this._handleIn) &&
                (d = l > 0 ? o[l - 1] : r._closed ? o[o.length - 1] : null) &&
                d._changed(),
              (!e || e === this._point || e === this._handleOut) && (d = o[l]) && d._changed()),
              r._changed(25));
          }
        },
        getPoint: function () {
          return this._point;
        },
        setPoint: function () {
          this._point.set(R.read(arguments));
        },
        getHandleIn: function () {
          return this._handleIn;
        },
        setHandleIn: function () {
          this._handleIn.set(R.read(arguments));
        },
        getHandleOut: function () {
          return this._handleOut;
        },
        setHandleOut: function () {
          this._handleOut.set(R.read(arguments));
        },
        hasHandles: function () {
          return !this._handleIn.isZero() || !this._handleOut.isZero();
        },
        isSmooth: function () {
          var e = this._handleIn,
            r = this._handleOut;
          return !e.isZero() && !r.isZero() && e.isCollinear(r);
        },
        clearHandles: function () {
          (this._handleIn._set(0, 0), this._handleOut._set(0, 0));
        },
        getSelection: function () {
          return this._selection;
        },
        setSelection: function (e) {
          var r = this._selection,
            o = this._path;
          ((this._selection = e = e || 0),
            o && e !== r && (o._updateSelection(this, r, e), o._changed(129)));
        },
        _changeSelection: function (e, r) {
          var o = this._selection;
          this.setSelection(r ? o | e : o & ~e);
        },
        isSelected: function () {
          return !!(this._selection & 7);
        },
        setSelected: function (e) {
          this._changeSelection(7, e);
        },
        getIndex: function () {
          return this._index !== t ? this._index : null;
        },
        getPath: function () {
          return this._path || null;
        },
        getCurve: function () {
          var e = this._path,
            r = this._index;
          return e
            ? (r > 0 && !e._closed && r === e._segments.length - 1 && r--, e.getCurves()[r] || null)
            : null;
        },
        getLocation: function () {
          var e = this.getCurve();
          return e ? new zt(e, this === e._segment1 ? 0 : 1) : null;
        },
        getNext: function () {
          var e = this._path && this._path._segments;
          return (e && (e[this._index + 1] || (this._path._closed && e[0]))) || null;
        },
        smooth: function (e, r, o) {
          var l = e || {},
            d = l.type,
            c = l.factor,
            h = this.getPrevious(),
            m = this.getNext(),
            p = (h || this)._point,
            y = this._point,
            x = (m || this)._point,
            b = p.getDistance(y),
            S = y.getDistance(x);
          if (!d || d === "catmull-rom") {
            var v = c === t ? 0.5 : c,
              w = Math.pow(b, v),
              _ = w * w,
              P = Math.pow(S, v),
              D = P * P;
            if (!r && h) {
              var I = 2 * D + 3 * P * w + _,
                C = 3 * P * (P + w);
              this.setHandleIn(
                C !== 0
                  ? new R(
                      (D * p._x + I * y._x - _ * x._x) / C - y._x,
                      (D * p._y + I * y._y - _ * x._y) / C - y._y
                    )
                  : new R()
              );
            }
            if (!o && m) {
              var I = 2 * _ + 3 * w * P + D,
                C = 3 * w * (w + P);
              this.setHandleOut(
                C !== 0
                  ? new R(
                      (_ * x._x + I * y._x - D * p._x) / C - y._x,
                      (_ * x._y + I * y._y - D * p._y) / C - y._y
                    )
                  : new R()
              );
            }
          } else if (d === "geometric") {
            if (h && m) {
              var T = p.subtract(x),
                W = c === t ? 0.4 : c,
                N = (W * b) / (b + S);
              (r || this.setHandleIn(T.multiply(N)), o || this.setHandleOut(T.multiply(N - W)));
            }
          } else throw new Error("Smoothing method '" + d + "' not supported.");
        },
        getPrevious: function () {
          var e = this._path && this._path._segments;
          return (e && (e[this._index - 1] || (this._path._closed && e[e.length - 1]))) || null;
        },
        isFirst: function () {
          return !this._index;
        },
        isLast: function () {
          var e = this._path;
          return (e && this._index === e._segments.length - 1) || !1;
        },
        reverse: function () {
          var e = this._handleIn,
            r = this._handleOut,
            o = e.clone();
          (e.set(r), r.set(o));
        },
        reversed: function () {
          return new ae(this._point, this._handleOut, this._handleIn);
        },
        remove: function () {
          return this._path ? !!this._path.removeSegment(this._index) : !1;
        },
        clone: function () {
          return new ae(this._point, this._handleIn, this._handleOut);
        },
        equals: function (e) {
          return (
            e === this ||
            (e &&
              this._class === e._class &&
              this._point.equals(e._point) &&
              this._handleIn.equals(e._handleIn) &&
              this._handleOut.equals(e._handleOut)) ||
            !1
          );
        },
        toString: function () {
          var e = ["point: " + this._point];
          return (
            this._handleIn.isZero() || e.push("handleIn: " + this._handleIn),
            this._handleOut.isZero() || e.push("handleOut: " + this._handleOut),
            "{ " + e.join(", ") + " }"
          );
        },
        transform: function (e) {
          (this._transformCoordinates(e, new Array(6), !0), this._changed());
        },
        interpolate: function (e, r, o) {
          var l = 1 - o,
            d = o,
            c = e._point,
            h = r._point,
            m = e._handleIn,
            p = r._handleIn,
            y = r._handleOut,
            x = e._handleOut;
          (this._point._set(l * c._x + d * h._x, l * c._y + d * h._y, !0),
            this._handleIn._set(l * m._x + d * p._x, l * m._y + d * p._y, !0),
            this._handleOut._set(l * x._x + d * y._x, l * x._y + d * y._y, !0),
            this._changed());
        },
        _transformCoordinates: function (e, r, o) {
          var l = this._point,
            d = !o || !this._handleIn.isZero() ? this._handleIn : null,
            c = !o || !this._handleOut.isZero() ? this._handleOut : null,
            h = l._x,
            m = l._y,
            p = 2;
          return (
            (r[0] = h),
            (r[1] = m),
            d && ((r[p++] = d._x + h), (r[p++] = d._y + m)),
            c && ((r[p++] = c._x + h), (r[p++] = c._y + m)),
            e &&
              (e._transformCoordinates(r, r, p / 2),
              (h = r[0]),
              (m = r[1]),
              o
                ? ((l._x = h),
                  (l._y = m),
                  (p = 2),
                  d && ((d._x = r[p++] - h), (d._y = r[p++] - m)),
                  c && ((c._x = r[p++] - h), (c._y = r[p++] - m)))
                : (d || ((r[p++] = h), (r[p++] = m)), c || ((r[p++] = h), (r[p++] = m)))),
            r
          );
        },
      }),
      vs = R.extend({
        initialize: function (r, o, l) {
          var d, c, h;
          if (!r) d = c = 0;
          else if ((d = r[0]) !== t) c = r[1];
          else {
            var m = r;
            ((d = m.x) === t && ((m = R.read(arguments)), (d = m.x)), (c = m.y), (h = m.selected));
          }
          ((this._x = d),
            (this._y = c),
            (this._owner = o),
            (o[l] = this),
            h && this.setSelected(!0));
        },
        _set: function (e, r) {
          return ((this._x = e), (this._y = r), this._owner._changed(this), this);
        },
        getX: function () {
          return this._x;
        },
        setX: function (e) {
          ((this._x = e), this._owner._changed(this));
        },
        getY: function () {
          return this._y;
        },
        setY: function (e) {
          ((this._y = e), this._owner._changed(this));
        },
        isZero: function () {
          var e = F.isZero;
          return e(this._x) && e(this._y);
        },
        isSelected: function () {
          return !!(this._owner._selection & this._getSelection());
        },
        setSelected: function (e) {
          this._owner._changeSelection(this._getSelection(), e);
        },
        _getSelection: function () {
          var e = this._owner;
          return this === e._point ? 1 : this === e._handleIn ? 2 : this === e._handleOut ? 4 : 0;
        },
      }),
      j = s.extend(
        {
          _class: "Curve",
          beans: !0,
          initialize: function (r, o, l, d, c, h, m, p) {
            var y = arguments.length,
              x,
              b,
              S,
              v,
              w,
              _;
            (y === 3
              ? ((this._path = r), (x = o), (b = l))
              : y
                ? y === 1
                  ? "segment1" in r
                    ? ((x = new ae(r.segment1)), (b = new ae(r.segment2)))
                    : "point1" in r
                      ? ((S = r.point1), (w = r.handle1), (_ = r.handle2), (v = r.point2))
                      : Array.isArray(r) &&
                        ((S = [r[0], r[1]]),
                        (v = [r[6], r[7]]),
                        (w = [r[2] - r[0], r[3] - r[1]]),
                        (_ = [r[4] - r[6], r[5] - r[7]]))
                  : y === 2
                    ? ((x = new ae(r)), (b = new ae(o)))
                    : y === 4
                      ? ((S = r), (w = o), (_ = l), (v = d))
                      : y === 8 &&
                        ((S = [r, o]), (v = [m, p]), (w = [l - r, d - o]), (_ = [c - m, h - p]))
                : ((x = new ae()), (b = new ae())),
              (this._segment1 = x || new ae(S, null, w)),
              (this._segment2 = b || new ae(v, _, null)));
          },
          _serialize: function (e, r) {
            return s.serialize(
              this.hasHandles()
                ? [this.getPoint1(), this.getHandle1(), this.getHandle2(), this.getPoint2()]
                : [this.getPoint1(), this.getPoint2()],
              e,
              !0,
              r
            );
          },
          _changed: function () {
            this._length = this._bounds = t;
          },
          clone: function () {
            return new j(this._segment1, this._segment2);
          },
          toString: function () {
            var e = ["point1: " + this._segment1._point];
            return (
              this._segment1._handleOut.isZero() || e.push("handle1: " + this._segment1._handleOut),
              this._segment2._handleIn.isZero() || e.push("handle2: " + this._segment2._handleIn),
              e.push("point2: " + this._segment2._point),
              "{ " + e.join(", ") + " }"
            );
          },
          classify: function () {
            return j.classify(this.getValues());
          },
          remove: function () {
            var e = !1;
            if (this._path) {
              var r = this._segment2,
                o = r._handleOut;
              ((e = r.remove()), e && this._segment1._handleOut.set(o));
            }
            return e;
          },
          getPoint1: function () {
            return this._segment1._point;
          },
          setPoint1: function () {
            this._segment1._point.set(R.read(arguments));
          },
          getPoint2: function () {
            return this._segment2._point;
          },
          setPoint2: function () {
            this._segment2._point.set(R.read(arguments));
          },
          getHandle1: function () {
            return this._segment1._handleOut;
          },
          setHandle1: function () {
            this._segment1._handleOut.set(R.read(arguments));
          },
          getHandle2: function () {
            return this._segment2._handleIn;
          },
          setHandle2: function () {
            this._segment2._handleIn.set(R.read(arguments));
          },
          getSegment1: function () {
            return this._segment1;
          },
          getSegment2: function () {
            return this._segment2;
          },
          getPath: function () {
            return this._path;
          },
          getIndex: function () {
            return this._segment1._index;
          },
          getNext: function () {
            var e = this._path && this._path._curves;
            return (e && (e[this._segment1._index + 1] || (this._path._closed && e[0]))) || null;
          },
          getPrevious: function () {
            var e = this._path && this._path._curves;
            return (
              (e && (e[this._segment1._index - 1] || (this._path._closed && e[e.length - 1]))) ||
              null
            );
          },
          isFirst: function () {
            return !this._segment1._index;
          },
          isLast: function () {
            var e = this._path;
            return (e && this._segment1._index === e._curves.length - 1) || !1;
          },
          isSelected: function () {
            return (
              this.getPoint1().isSelected() &&
              this.getHandle1().isSelected() &&
              this.getHandle2().isSelected() &&
              this.getPoint2().isSelected()
            );
          },
          setSelected: function (e) {
            (this.getPoint1().setSelected(e),
              this.getHandle1().setSelected(e),
              this.getHandle2().setSelected(e),
              this.getPoint2().setSelected(e));
          },
          getValues: function (e) {
            return j.getValues(this._segment1, this._segment2, e);
          },
          getPoints: function () {
            for (var e = this.getValues(), r = [], o = 0; o < 8; o += 2)
              r.push(new R(e[o], e[o + 1]));
            return r;
          },
        },
        {
          getLength: function () {
            return (
              this._length == null && (this._length = j.getLength(this.getValues(), 0, 1)),
              this._length
            );
          },
          getArea: function () {
            return j.getArea(this.getValues());
          },
          getLine: function () {
            return new oe(this._segment1._point, this._segment2._point);
          },
          getPart: function (e, r) {
            return new j(j.getPart(this.getValues(), e, r));
          },
          getPartLength: function (e, r) {
            return j.getLength(this.getValues(), e, r);
          },
          divideAt: function (e) {
            return this.divideAtTime(e && e.curve === this ? e.time : this.getTimeAt(e));
          },
          divideAtTime: function (e, r) {
            var o = 1e-8,
              l = 1 - o,
              d = null;
            if (e >= o && e <= l) {
              var c = j.subdivide(this.getValues(), e),
                h = c[0],
                m = c[1],
                p = r || this.hasHandles(),
                y = this._segment1,
                x = this._segment2,
                b = this._path;
              p &&
                (y._handleOut._set(h[2] - h[0], h[3] - h[1]),
                x._handleIn._set(m[4] - m[6], m[5] - m[7]));
              var S = h[6],
                v = h[7],
                w = new ae(
                  new R(S, v),
                  p && new R(h[4] - S, h[5] - v),
                  p && new R(m[2] - S, m[3] - v)
                );
              b
                ? (b.insert(y._index + 1, w), (d = this.getNext()))
                : ((this._segment2 = w), this._changed(), (d = new j(w, x)));
            }
            return d;
          },
          splitAt: function (e) {
            var r = this._path;
            return r ? r.splitAt(e) : null;
          },
          splitAtTime: function (e) {
            return this.splitAt(this.getLocationAtTime(e));
          },
          divide: function (e, r) {
            return this.divideAtTime(e === t ? 0.5 : r ? e : this.getTimeAt(e));
          },
          split: function (e, r) {
            return this.splitAtTime(e === t ? 0.5 : r ? e : this.getTimeAt(e));
          },
          reversed: function () {
            return new j(this._segment2.reversed(), this._segment1.reversed());
          },
          clearHandles: function () {
            (this._segment1._handleOut._set(0, 0), this._segment2._handleIn._set(0, 0));
          },
          statics: {
            getValues: function (e, r, o, l) {
              var d = e._point,
                c = e._handleOut,
                h = r._handleIn,
                m = r._point,
                p = d.x,
                y = d.y,
                x = m.x,
                b = m.y,
                S = l
                  ? [p, y, p, y, x, b, x, b]
                  : [p, y, p + c._x, y + c._y, x + h._x, b + h._y, x, b];
              return (o && o._transformCoordinates(S, S, 4), S);
            },
            subdivide: function (e, r) {
              var o = e[0],
                l = e[1],
                d = e[2],
                c = e[3],
                h = e[4],
                m = e[5],
                p = e[6],
                y = e[7];
              r === t && (r = 0.5);
              var x = 1 - r,
                b = x * o + r * d,
                S = x * l + r * c,
                v = x * d + r * h,
                w = x * c + r * m,
                _ = x * h + r * p,
                P = x * m + r * y,
                D = x * b + r * v,
                I = x * S + r * w,
                C = x * v + r * _,
                T = x * w + r * P,
                W = x * D + r * C,
                N = x * I + r * T;
              return [
                [o, l, b, S, D, I, W, N],
                [W, N, C, T, _, P, p, y],
              ];
            },
            getMonoCurves: function (e, r) {
              var o = [],
                l = r ? 0 : 1,
                d = e[l + 0],
                c = e[l + 2],
                h = e[l + 4],
                m = e[l + 6];
              if ((d >= c == c >= h && c >= h == h >= m) || j.isStraight(e)) o.push(e);
              else {
                var p = 3 * (c - h) - d + m,
                  y = 2 * (d + h) - 4 * c,
                  x = c - d,
                  b = 1e-8,
                  S = 1 - b,
                  v = [],
                  w = F.solveQuadratic(p, y, x, v, b, S);
                if (!w) o.push(e);
                else {
                  v.sort();
                  var _ = v[0],
                    P = j.subdivide(e, _);
                  (o.push(P[0]),
                    w > 1 && ((_ = (v[1] - _) / (1 - _)), (P = j.subdivide(P[1], _)), o.push(P[0])),
                    o.push(P[1]));
                }
              }
              return o;
            },
            solveCubic: function (e, r, o, l, d, c) {
              var h = e[r],
                m = e[r + 2],
                p = e[r + 4],
                y = e[r + 6],
                x = 0;
              if (!((h < o && y < o && m < o && p < o) || (h > o && y > o && m > o && p > o))) {
                var b = 3 * (m - h),
                  S = 3 * (p - m) - b,
                  v = y - h - b - S;
                x = F.solveCubic(v, S, b, h - o, l, d, c);
              }
              return x;
            },
            getTimeOf: function (e, r) {
              var o = new R(e[0], e[1]),
                l = new R(e[6], e[7]),
                d = 1e-12,
                c = 1e-7,
                h = r.isClose(o, d) ? 0 : r.isClose(l, d) ? 1 : null;
              if (h === null)
                for (var m = [r.x, r.y], p = [], y = 0; y < 2; y++)
                  for (var x = j.solveCubic(e, y, m[y], p, 0, 1), b = 0; b < x; b++) {
                    var S = p[b];
                    if (r.isClose(j.getPoint(e, S), c)) return S;
                  }
              return r.isClose(o, c) ? 0 : r.isClose(l, c) ? 1 : null;
            },
            getNearestTime: function (e, r) {
              if (j.isStraight(e)) {
                var o = e[0],
                  l = e[1],
                  d = e[6],
                  c = e[7],
                  h = d - o,
                  m = c - l,
                  p = h * h + m * m;
                if (p === 0) return 0;
                var y = ((r.x - o) * h + (r.y - l) * m) / p;
                return y < 1e-12
                  ? 0
                  : y > 0.999999999999
                    ? 1
                    : j.getTimeOf(e, new R(o + y * h, l + y * m));
              }
              var x = 100,
                b = 1 / 0,
                S = 0;
              function v(P) {
                if (P >= 0 && P <= 1) {
                  var D = r.getDistance(j.getPoint(e, P), !0);
                  if (D < b) return ((b = D), (S = P), !0);
                }
              }
              for (var w = 0; w <= x; w++) v(w / x);
              for (var _ = 1 / (x * 2); _ > 1e-8;) !v(S - _) && !v(S + _) && (_ /= 2);
              return S;
            },
            getPart: function (e, r, o) {
              var l = r > o;
              if (l) {
                var d = r;
                ((r = o), (o = d));
              }
              return (
                r > 0 && (e = j.subdivide(e, r)[1]),
                o < 1 && (e = j.subdivide(e, (o - r) / (1 - r))[0]),
                l ? [e[6], e[7], e[4], e[5], e[2], e[3], e[0], e[1]] : e
              );
            },
            isFlatEnough: function (e, r) {
              var o = e[0],
                l = e[1],
                d = e[2],
                c = e[3],
                h = e[4],
                m = e[5],
                p = e[6],
                y = e[7],
                x = 3 * d - 2 * o - p,
                b = 3 * c - 2 * l - y,
                S = 3 * h - 2 * p - o,
                v = 3 * m - 2 * y - l;
              return Math.max(x * x, S * S) + Math.max(b * b, v * v) <= 16 * r * r;
            },
            getArea: function (e) {
              var r = e[0],
                o = e[1],
                l = e[2],
                d = e[3],
                c = e[4],
                h = e[5],
                m = e[6],
                p = e[7];
              return (
                (3 *
                  ((p - o) * (l + c) -
                    (m - r) * (d + h) +
                    d * (r - c) -
                    l * (o - h) +
                    p * (c + r / 3) -
                    m * (h + o / 3))) /
                20
              );
            },
            getBounds: function (e) {
              for (var r = e.slice(0, 2), o = r.slice(), l = [0, 0], d = 0; d < 2; d++)
                j._addBounds(e[d], e[d + 2], e[d + 4], e[d + 6], d, 0, r, o, l);
              return new X(r[0], r[1], o[0] - r[0], o[1] - r[1]);
            },
            _addBounds: function (e, r, o, l, d, c, h, m, p) {
              function y(W, N) {
                var B = W - N,
                  V = W + N;
                (B < h[d] && (h[d] = B), V > m[d] && (m[d] = V));
              }
              c /= 2;
              var x = h[d] - c,
                b = m[d] + c;
              if (e < x || r < x || o < x || l < x || e > b || r > b || o > b || l > b)
                if (r < e != r < l && o < e != o < l) (y(e, c), y(l, c));
                else {
                  var S = 3 * (r - o) - e + l,
                    v = 2 * (e + o) - 4 * r,
                    w = r - e,
                    _ = F.solveQuadratic(S, v, w, p),
                    P = 1e-8,
                    D = 1 - P;
                  y(l, 0);
                  for (var I = 0; I < _; I++) {
                    var C = p[I],
                      T = 1 - C;
                    P <= C &&
                      C <= D &&
                      y(T * T * T * e + 3 * T * T * C * r + 3 * T * C * C * o + C * C * C * l, c);
                  }
                }
            },
          },
        },
        s.each(
          ["getBounds", "getStrokeBounds", "getHandleBounds"],
          function (e) {
            this[e] = function () {
              this._bounds || (this._bounds = {});
              var r = this._bounds[e];
              return (
                r ||
                  (r = this._bounds[e] = _e[e]([this._segment1, this._segment2], !1, this._path)),
                r.clone()
              );
            };
          },
          {}
        ),
        s.each(
          {
            isStraight: function (e, r, o, l) {
              if (r.isZero() && o.isZero()) return !0;
              var d = l.subtract(e);
              if (d.isZero()) return !1;
              if (d.isCollinear(r) && d.isCollinear(o)) {
                var c = new oe(e, l),
                  h = 1e-7;
                if (c.getDistance(e.add(r)) < h && c.getDistance(l.add(o)) < h) {
                  var m = d.dot(d),
                    p = d.dot(r) / m,
                    y = d.dot(o) / m;
                  return p >= 0 && p <= 1 && y <= 0 && y >= -1;
                }
              }
              return !1;
            },
            isLinear: function (e, r, o, l) {
              var d = l.subtract(e).divide(3);
              return r.equals(d) && o.negate().equals(d);
            },
          },
          function (e, r) {
            ((this[r] = function (o) {
              var l = this._segment1,
                d = this._segment2;
              return e(l._point, l._handleOut, d._handleIn, d._point, o);
            }),
              (this.statics[r] = function (o, l) {
                var d = o[0],
                  c = o[1],
                  h = o[6],
                  m = o[7];
                return e(
                  new R(d, c),
                  new R(o[2] - d, o[3] - c),
                  new R(o[4] - h, o[5] - m),
                  new R(h, m),
                  l
                );
              }));
          },
          {
            statics: {},
            hasHandles: function () {
              return !this._segment1._handleOut.isZero() || !this._segment2._handleIn.isZero();
            },
            hasLength: function (e) {
              return (
                (!this.getPoint1().equals(this.getPoint2()) || this.hasHandles()) &&
                this.getLength() > (e || 0)
              );
            },
            isCollinear: function (e) {
              return (
                e && this.isStraight() && e.isStraight() && this.getLine().isCollinear(e.getLine())
              );
            },
            isHorizontal: function () {
              return this.isStraight() && Math.abs(this.getTangentAtTime(0.5).y) < 1e-8;
            },
            isVertical: function () {
              return this.isStraight() && Math.abs(this.getTangentAtTime(0.5).x) < 1e-8;
            },
          }
        ),
        {
          beans: !1,
          getLocationAt: function (e, r) {
            return this.getLocationAtTime(r ? e : this.getTimeAt(e));
          },
          getLocationAtTime: function (e) {
            return e != null && e >= 0 && e <= 1 ? new zt(this, e) : null;
          },
          getTimeAt: function (e, r) {
            return j.getTimeAt(this.getValues(), e, r);
          },
          getParameterAt: "#getTimeAt",
          getOffsetAtTime: function (e) {
            return this.getPartLength(0, e);
          },
          getLocationOf: function () {
            return this.getLocationAtTime(this.getTimeOf(R.read(arguments)));
          },
          getOffsetOf: function () {
            var e = this.getLocationOf.apply(this, arguments);
            return e ? e.getOffset() : null;
          },
          getTimeOf: function () {
            return j.getTimeOf(this.getValues(), R.read(arguments));
          },
          getParameterOf: "#getTimeOf",
          getNearestLocation: function () {
            var e = R.read(arguments),
              r = this.getValues(),
              o = j.getNearestTime(r, e),
              l = j.getPoint(r, o);
            return new zt(this, o, l, null, e.getDistance(l));
          },
          getNearestPoint: function () {
            var e = this.getNearestLocation.apply(this, arguments);
            return e && e.getPoint();
          },
        },
        new (function () {
          var e = [
            "getPoint",
            "getTangent",
            "getNormal",
            "getWeightedTangent",
            "getWeightedNormal",
            "getCurvature",
          ];
          return s.each(
            e,
            function (r) {
              ((this[r + "At"] = function (o, l) {
                var d = this.getValues();
                return j[r](d, l ? o : j.getTimeAt(d, o));
              }),
                (this[r + "AtTime"] = function (o) {
                  return j[r](this.getValues(), o);
                }));
            },
            { statics: { _evaluateMethods: e } }
          );
        })(),
        new (function () {
          function e(l) {
            var d = l[0],
              c = l[1],
              h = l[2],
              m = l[3],
              p = l[4],
              y = l[5],
              x = l[6],
              b = l[7],
              S = 9 * (h - p) + 3 * (x - d),
              v = 6 * (d + p) - 12 * h,
              w = 3 * (h - d),
              _ = 9 * (m - y) + 3 * (b - c),
              P = 6 * (c + y) - 12 * m,
              D = 3 * (m - c);
            return function (I) {
              var C = (S * I + v) * I + w,
                T = (_ * I + P) * I + D;
              return Math.sqrt(C * C + T * T);
            };
          }
          function r(l, d) {
            return Math.max(2, Math.min(16, Math.ceil(Math.abs(d - l) * 32)));
          }
          function o(l, d, c, h) {
            if (d == null || d < 0 || d > 1) return null;
            var m = l[0],
              p = l[1],
              y = l[2],
              x = l[3],
              b = l[4],
              S = l[5],
              v = l[6],
              w = l[7],
              _ = F.isZero;
            (_(y - m) && _(x - p) && ((y = m), (x = p)),
              _(b - v) && _(S - w) && ((b = v), (S = w)));
            var P = 3 * (y - m),
              D = 3 * (b - y) - P,
              I = v - m - P - D,
              C = 3 * (x - p),
              T = 3 * (S - x) - C,
              W = w - p - C - T,
              N,
              B;
            if (c === 0)
              ((N = d === 0 ? m : d === 1 ? v : ((I * d + D) * d + P) * d + m),
                (B = d === 0 ? p : d === 1 ? w : ((W * d + T) * d + C) * d + p));
            else {
              var V = 1e-8,
                L = 1 - V;
              if (
                (d < V
                  ? ((N = P), (B = C))
                  : d > L
                    ? ((N = 3 * (v - b)), (B = 3 * (w - S)))
                    : ((N = (3 * I * d + 2 * D) * d + P), (B = (3 * W * d + 2 * T) * d + C)),
                h)
              ) {
                N === 0 && B === 0 && (d < V || d > L) && ((N = b - y), (B = S - x));
                var M = Math.sqrt(N * N + B * B);
                M && ((N /= M), (B /= M));
              }
              if (c === 3) {
                var b = 6 * I * d + 2 * D,
                  S = 6 * W * d + 2 * T,
                  z = Math.pow(N * N + B * B, 3 / 2);
                ((N = z !== 0 ? (N * S - B * b) / z : 0), (B = 0));
              }
            }
            return c === 2 ? new R(B, -N) : new R(N, B);
          }
          return {
            statics: {
              classify: function (l) {
                var d = l[0],
                  c = l[1],
                  h = l[2],
                  m = l[3],
                  p = l[4],
                  y = l[5],
                  x = l[6],
                  b = l[7],
                  S = d * (b - y) + c * (p - x) + x * y - b * p,
                  v = h * (c - b) + m * (x - d) + d * b - c * x,
                  w = p * (m - c) + y * (d - h) + h * c - m * d,
                  _ = 3 * w,
                  P = _ - v,
                  D = P - v + S,
                  I = Math.sqrt(D * D + P * P + _ * _),
                  C = I !== 0 ? 1 / I : 0,
                  T = F.isZero,
                  W = "serpentine";
                ((D *= C), (P *= C), (_ *= C));
                function N(M, z, G) {
                  var O = z !== t,
                    E = O && z > 0 && z < 1,
                    H = O && G > 0 && G < 1;
                  return (
                    O && (!(E || H) || (M === "loop" && !(E && H))) && ((M = "arch"), (E = H = !1)),
                    {
                      type: M,
                      roots: E || H ? (E && H ? (z < G ? [z, G] : [G, z]) : [E ? z : G]) : null,
                    }
                  );
                }
                if (T(D)) return T(P) ? N(T(_) ? "line" : "quadratic") : N(W, _ / (3 * P));
                var B = 3 * P * P - 4 * D * _;
                if (T(B)) return N("cusp", P / (2 * D));
                var V = B > 0 ? Math.sqrt(B / 3) : Math.sqrt(-B),
                  L = 2 * D;
                return N(B > 0 ? W : "loop", (P + V) / L, (P - V) / L);
              },
              getLength: function (l, d, c, h) {
                if ((d === t && (d = 0), c === t && (c = 1), j.isStraight(l))) {
                  var m = l;
                  (c < 1 && ((m = j.subdivide(m, c)[0]), (d /= c)),
                    d > 0 && (m = j.subdivide(m, d)[1]));
                  var p = m[6] - m[0],
                    y = m[7] - m[1];
                  return Math.sqrt(p * p + y * y);
                }
                return F.integrate(h || e(l), d, c, r(d, c));
              },
              getTimeAt: function (l, d, c) {
                if ((c === t && (c = d < 0 ? 1 : 0), d === 0)) return c;
                var h = Math.abs,
                  m = 1e-12,
                  p = d > 0,
                  y = p ? c : 0,
                  x = p ? 1 : c,
                  b = e(l),
                  S = j.getLength(l, y, x, b),
                  v = h(d) - S;
                if (h(v) < m) return p ? x : y;
                if (v > m) return null;
                var w = d / S,
                  _ = 0;
                function P(D) {
                  return ((_ += F.integrate(b, c, D, r(c, D))), (c = D), _ - d);
                }
                return F.findRoot(P, b, c + w, y, x, 32, 1e-12);
              },
              getPoint: function (l, d) {
                return o(l, d, 0, !1);
              },
              getTangent: function (l, d) {
                return o(l, d, 1, !0);
              },
              getWeightedTangent: function (l, d) {
                return o(l, d, 1, !1);
              },
              getNormal: function (l, d) {
                return o(l, d, 2, !0);
              },
              getWeightedNormal: function (l, d) {
                return o(l, d, 2, !1);
              },
              getCurvature: function (l, d) {
                return o(l, d, 3, !1).x;
              },
              getPeaks: function (l) {
                var d = l[0],
                  c = l[1],
                  h = l[2],
                  m = l[3],
                  p = l[4],
                  y = l[5],
                  x = l[6],
                  b = l[7],
                  S = -d + 3 * h - 3 * p + x,
                  v = 3 * d - 6 * h + 3 * p,
                  w = -3 * d + 3 * h,
                  _ = -c + 3 * m - 3 * y + b,
                  P = 3 * c - 6 * m + 3 * y,
                  D = -3 * c + 3 * m,
                  I = 1e-8,
                  C = 1 - I,
                  T = [];
                return (
                  F.solveCubic(
                    9 * (S * S + _ * _),
                    9 * (S * v + P * _),
                    2 * (v * v + P * P) + 3 * (w * S + D * _),
                    w * v + P * D,
                    T,
                    I,
                    C
                  ),
                  T.sort()
                );
              },
            },
          };
        })(),
        new (function () {
          function e(S, v, w, _, P, D, I) {
            var C = !I && w.getPrevious() === P,
              T = !I && w !== P && w.getNext() === P,
              W = 1e-8,
              N = 1 - W;
            if (
              _ !== null &&
              _ >= (C ? W : 0) &&
              _ <= (T ? N : 1) &&
              D !== null &&
              D >= (T ? W : 0) &&
              D <= (C ? N : 1)
            ) {
              var B = new zt(w, _, null, I),
                V = new zt(P, D, null, I);
              ((B._intersection = V), (V._intersection = B), (!v || v(B)) && zt.insert(S, B, !0));
            }
          }
          function r(S, v, w, _, P, D, I, C, T, W, N, B, V) {
            if (++T >= 4096 || ++C >= 40) return T;
            var L = 1e-9,
              M = v[0],
              z = v[1],
              G = v[6],
              O = v[7],
              E = oe.getSignedDistance,
              H = E(M, z, G, O, v[2], v[3]),
              U = E(M, z, G, O, v[4], v[5]),
              Y = H * U > 0 ? 3 / 4 : 4 / 9,
              K = Y * Math.min(0, H, U),
              te = Y * Math.max(0, H, U),
              se = E(M, z, G, O, S[0], S[1]),
              ue = E(M, z, G, O, S[2], S[3]),
              me = E(M, z, G, O, S[4], S[5]),
              Ce = E(M, z, G, O, S[6], S[7]),
              ce = o(se, ue, me, Ce),
              xe = ce[0],
              ke = ce[1],
              ve,
              he;
            if (
              (H === 0 && U === 0 && se === 0 && ue === 0 && me === 0 && Ce === 0) ||
              (ve = l(xe, ke, K, te)) == null ||
              (he = l(xe.reverse(), ke.reverse(), K, te)) == null
            )
              return T;
            var ge = W + (N - W) * ve,
              Me = W + (N - W) * he;
            if (Math.max(V - B, Me - ge) < L) {
              var ot = (ge + Me) / 2,
                St = (B + V) / 2;
              e(P, D, I ? _ : w, I ? St : ot, I ? w : _, I ? ot : St);
            } else if (((S = j.getPart(S, ve, he)), he - ve > 0.8))
              if (Me - ge > V - B) {
                var ye = j.subdivide(S, 0.5),
                  ot = (ge + Me) / 2;
                ((T = r(v, ye[0], _, w, P, D, !I, C, T, B, V, ge, ot)),
                  (T = r(v, ye[1], _, w, P, D, !I, C, T, B, V, ot, Me)));
              } else {
                var ye = j.subdivide(v, 0.5),
                  St = (B + V) / 2;
                ((T = r(ye[0], S, _, w, P, D, !I, C, T, B, St, ge, Me)),
                  (T = r(ye[1], S, _, w, P, D, !I, C, T, St, V, ge, Me)));
              }
            else
              V - B >= L
                ? (T = r(v, S, _, w, P, D, !I, C, T, B, V, ge, Me))
                : (T = r(S, v, w, _, P, D, I, C, T, ge, Me, B, V));
            return T;
          }
          function o(S, v, w, _) {
            var P = [0, S],
              D = [1 / 3, v],
              I = [2 / 3, w],
              C = [1, _],
              T = v - (2 * S + _) / 3,
              W = w - (S + 2 * _) / 3,
              N;
            if (T * W < 0)
              N = [
                [P, D, C],
                [P, I, C],
              ];
            else {
              var B = T / W;
              N = [B >= 2 ? [P, D, C] : B <= 0.5 ? [P, I, C] : [P, D, I, C], [P, C]];
            }
            return (T || W) < 0 ? N.reverse() : N;
          }
          function l(S, v, w, _) {
            return S[0][1] < w ? d(S, !0, w) : v[0][1] > _ ? d(v, !1, _) : S[0][0];
          }
          function d(S, v, w) {
            for (var _ = S[0][0], P = S[0][1], D = 1, I = S.length; D < I; D++) {
              var C = S[D][0],
                T = S[D][1];
              if (v ? T >= w : T <= w) return T === w ? C : _ + ((w - P) * (C - _)) / (T - P);
              ((_ = C), (P = T));
            }
            return null;
          }
          function c(S, v, w, _, P) {
            var D = F.isZero;
            if (D(_) && D(P)) {
              var I = j.getTimeOf(S, new R(v, w));
              return I === null ? [] : [I];
            }
            for (
              var C = Math.atan2(-P, _), T = Math.sin(C), W = Math.cos(C), N = [], B = [], V = 0;
              V < 8;
              V += 2
            ) {
              var L = S[V] - v,
                M = S[V + 1] - w;
              N.push(L * W - M * T, L * T + M * W);
            }
            return (j.solveCubic(N, 1, 0, B, 0, 1), B);
          }
          function h(S, v, w, _, P, D, I) {
            for (
              var C = v[0],
                T = v[1],
                W = v[6],
                N = v[7],
                B = c(S, C, T, W - C, N - T),
                V = 0,
                L = B.length;
              V < L;
              V++
            ) {
              var M = B[V],
                z = j.getPoint(S, M),
                G = j.getTimeOf(v, z);
              G !== null && e(P, D, I ? _ : w, I ? G : M, I ? w : _, I ? M : G);
            }
          }
          function m(S, v, w, _, P, D) {
            var I = oe.intersect(S[0], S[1], S[6], S[7], v[0], v[1], v[6], v[7]);
            I && e(P, D, w, j.getTimeOf(S, I), _, j.getTimeOf(v, I));
          }
          function p(S, v, w, _, P, D) {
            var I = 1e-12,
              C = Math.min,
              T = Math.max;
            if (
              T(S[0], S[2], S[4], S[6]) + I > C(v[0], v[2], v[4], v[6]) &&
              C(S[0], S[2], S[4], S[6]) - I < T(v[0], v[2], v[4], v[6]) &&
              T(S[1], S[3], S[5], S[7]) + I > C(v[1], v[3], v[5], v[7]) &&
              C(S[1], S[3], S[5], S[7]) - I < T(v[1], v[3], v[5], v[7])
            ) {
              var W = b(S, v);
              if (W)
                for (var N = 0; N < 2; N++) {
                  var B = W[N];
                  e(P, D, w, B[0], _, B[1], !0);
                }
              else {
                var V = j.isStraight(S),
                  L = j.isStraight(v),
                  M = V && L,
                  z = V && !L,
                  G = P.length;
                if (
                  ((M ? m : V || L ? h : r)(
                    z ? v : S,
                    z ? S : v,
                    z ? _ : w,
                    z ? w : _,
                    P,
                    D,
                    z,
                    0,
                    0,
                    0,
                    1,
                    0,
                    1
                  ),
                  !M || P.length === G)
                )
                  for (var N = 0; N < 4; N++) {
                    var O = N >> 1,
                      E = N & 1,
                      H = O * 6,
                      U = E * 6,
                      Y = new R(S[H], S[H + 1]),
                      K = new R(v[U], v[U + 1]);
                    Y.isClose(K, I) && e(P, D, w, O, _, E);
                  }
              }
            }
            return P;
          }
          function y(S, v, w, _) {
            var P = j.classify(S);
            if (P.type === "loop") {
              var D = P.roots;
              e(w, _, v, D[0], v, D[1]);
            }
            return w;
          }
          function x(S, v, w, _, P, D) {
            var I = !v;
            I && (v = S);
            for (var C = S.length, T = v.length, W = [], N = [], B, V, L = 0; L < T; L++)
              W[L] = v[L].getValues(P);
            for (var L = 0; L < C; L++) {
              var M = S[L],
                z = I ? W[L] : M.getValues(_),
                G = M.getPath();
              (G !== V && ((V = G), (B = []), N.push(B)), I && y(z, M, B, w));
              for (var O = I ? L + 1 : 0; O < T; O++) {
                if (D && B.length) return B;
                p(z, W[O], M, v[O], B, w);
              }
            }
            B = [];
            for (var L = 0, E = N.length; L < E; L++) B.push.apply(B, N[L]);
            return B;
          }
          function b(S, v) {
            function w(Ce) {
              var ce = Ce[6] - Ce[0],
                xe = Ce[7] - Ce[1];
              return ce * ce + xe * xe;
            }
            var _ = Math.abs,
              P = oe.getDistance,
              D = 1e-8,
              I = 1e-7,
              C = j.isStraight(S),
              T = j.isStraight(v),
              W = C && T,
              N = w(S) < w(v),
              B = N ? v : S,
              V = N ? S : v,
              L = B[0],
              M = B[1],
              z = B[6] - L,
              G = B[7] - M;
            if (P(L, M, z, G, V[0], V[1], !0) < I && P(L, M, z, G, V[6], V[7], !0) < I)
              !W &&
                P(L, M, z, G, B[2], B[3], !0) < I &&
                P(L, M, z, G, B[4], B[5], !0) < I &&
                P(L, M, z, G, V[2], V[3], !0) < I &&
                P(L, M, z, G, V[4], V[5], !0) < I &&
                (C = T = W = !0);
            else if (W) return null;
            if (C ^ T) return null;
            for (var O = [S, v], E = [], H = 0; H < 4 && E.length < 2; H++) {
              var U = H & 1,
                Y = U ^ 1,
                K = H >> 1,
                te = j.getTimeOf(O[U], new R(O[Y][K ? 6 : 0], O[Y][K ? 7 : 1]));
              if (te != null) {
                var se = U ? [K, te] : [te, K];
                (!E.length || (_(se[0] - E[0][0]) > D && _(se[1] - E[0][1]) > D)) && E.push(se);
              }
              if (H > 2 && !E.length) break;
            }
            if (E.length !== 2) E = null;
            else if (!W) {
              var ue = j.getPart(S, E[0][0], E[1][0]),
                me = j.getPart(v, E[0][1], E[1][1]);
              (_(me[2] - ue[2]) > I ||
                _(me[3] - ue[3]) > I ||
                _(me[4] - ue[4]) > I ||
                _(me[5] - ue[5]) > I) &&
                (E = null);
            }
            return E;
          }
          return {
            getIntersections: function (S) {
              var v = this.getValues(),
                w = S && S !== this && S.getValues();
              return w ? p(v, w, this, S, []) : y(v, this, []);
            },
            statics: { getOverlaps: b, getIntersections: x, getCurveLineIntersections: c },
          };
        })()
      ),
      zt = s.extend(
        {
          _class: "CurveLocation",
          initialize: function (r, o, l, d, c) {
            if (o >= 0.99999999) {
              var h = r.getNext();
              h && ((o = 0), (r = h));
            }
            (this._setCurve(r),
              (this._time = o),
              (this._point = l || r.getPointAtTime(o)),
              (this._overlap = d),
              (this._distance = c),
              (this._intersection = this._next = this._previous = null));
          },
          _setCurve: function (e) {
            var r = e._path;
            ((this._path = r),
              (this._version = r ? r._version : 0),
              (this._curve = e),
              (this._segment = null),
              (this._segment1 = e._segment1),
              (this._segment2 = e._segment2));
          },
          _setSegment: function (e) {
            (this._setCurve(e.getCurve()),
              (this._segment = e),
              (this._time = e === this._segment1 ? 0 : 1),
              (this._point = e._point.clone()));
          },
          getSegment: function () {
            var e = this._segment;
            if (!e) {
              var r = this.getCurve(),
                o = this.getTime();
              (o === 0
                ? (e = r._segment1)
                : o === 1
                  ? (e = r._segment2)
                  : o != null &&
                    (e = r.getPartLength(0, o) < r.getPartLength(o, 1) ? r._segment1 : r._segment2),
                (this._segment = e));
            }
            return e;
          },
          getCurve: function () {
            var e = this._path,
              r = this;
            e &&
              e._version !== this._version &&
              (this._time = this._offset = this._curveOffset = this._curve = null);
            function o(l) {
              var d = l && l.getCurve();
              if (d && (r._time = d.getTimeOf(r._point)) != null) return (r._setCurve(d), d);
            }
            return (
              this._curve ||
              o(this._segment) ||
              o(this._segment1) ||
              o(this._segment2.getPrevious())
            );
          },
          getPath: function () {
            var e = this.getCurve();
            return e && e._path;
          },
          getIndex: function () {
            var e = this.getCurve();
            return e && e.getIndex();
          },
          getTime: function () {
            var e = this.getCurve(),
              r = this._time;
            return e && r == null ? (this._time = e.getTimeOf(this._point)) : r;
          },
          getParameter: "#getTime",
          getPoint: function () {
            return this._point;
          },
          getOffset: function () {
            var e = this._offset;
            if (e == null) {
              e = 0;
              var r = this.getPath(),
                o = this.getIndex();
              if (r && o != null)
                for (var l = r.getCurves(), d = 0; d < o; d++) e += l[d].getLength();
              this._offset = e += this.getCurveOffset();
            }
            return e;
          },
          getCurveOffset: function () {
            var e = this._curveOffset;
            if (e == null) {
              var r = this.getCurve(),
                o = this.getTime();
              this._curveOffset = e = o != null && r && r.getPartLength(0, o);
            }
            return e;
          },
          getIntersection: function () {
            return this._intersection;
          },
          getDistance: function () {
            return this._distance;
          },
          divide: function () {
            var e = this.getCurve(),
              r = e && e.divideAtTime(this.getTime());
            return (r && this._setSegment(r._segment1), r);
          },
          split: function () {
            var e = this.getCurve(),
              r = e._path,
              o = e && e.splitAtTime(this.getTime());
            return (o && this._setSegment(r.getLastSegment()), o);
          },
          equals: function (e, r) {
            var o = this === e;
            if (!o && e instanceof zt) {
              var l = this.getCurve(),
                d = e.getCurve(),
                c = l._path,
                h = d._path;
              if (c === h) {
                var m = Math.abs,
                  p = 1e-7,
                  y = m(this.getOffset() - e.getOffset()),
                  x = !r && this._intersection,
                  b = !r && e._intersection;
                o =
                  (y < p || (c && m(c.getLength() - y) < p)) &&
                  ((!x && !b) || (x && b && x.equals(b, !0)));
              }
            }
            return o;
          },
          toString: function () {
            var e = [],
              r = this.getPoint(),
              o = k.instance;
            r && e.push("point: " + r);
            var l = this.getIndex();
            l != null && e.push("index: " + l);
            var d = this.getTime();
            return (
              d != null && e.push("time: " + o.number(d)),
              this._distance != null && e.push("distance: " + o.number(this._distance)),
              "{ " + e.join(", ") + " }"
            );
          },
          isTouching: function () {
            var e = this._intersection;
            if (e && this.getTangent().isCollinear(e.getTangent())) {
              var r = this.getCurve(),
                o = e.getCurve();
              return !(r.isStraight() && o.isStraight() && r.getLine().intersect(o.getLine()));
            }
            return !1;
          },
          isCrossing: function () {
            var e = this._intersection;
            if (!e) return !1;
            var r = this.getTime(),
              o = e.getTime(),
              l = 1e-8,
              d = 1 - l,
              c = r >= l && r <= d,
              h = o >= l && o <= d;
            if (c && h) return !this.isTouching();
            var m = this.getCurve(),
              p = r < l ? m.getPrevious() : m,
              y = e.getCurve(),
              x = o < l ? y.getPrevious() : y;
            if ((r > d && (m = m.getNext()), o > d && (y = y.getNext()), !p || !m || !x || !y))
              return !1;
            var b = [];
            function S(V, L) {
              var M = V.getValues(),
                z = j.classify(M).roots || j.getPeaks(M),
                G = z.length,
                O = L && G > 1 ? z[G - 1] : G > 0 ? z[0] : 0.5;
              b.push(j.getLength(M, L ? O : 0, L ? 1 : O) / 2);
            }
            function v(V, L, M) {
              return L < M ? V > L && V < M : V > L || V < M;
            }
            (c || (S(p, !0), S(m, !1)), h || (S(x, !0), S(y, !1)));
            var w = this.getPoint(),
              _ = Math.min.apply(Math, b),
              P = c ? m.getTangentAtTime(r) : m.getPointAt(_).subtract(w),
              D = c ? P.negate() : p.getPointAt(-_).subtract(w),
              I = h ? y.getTangentAtTime(o) : y.getPointAt(_).subtract(w),
              C = h ? I.negate() : x.getPointAt(-_).subtract(w),
              T = D.getAngle(),
              W = P.getAngle(),
              N = C.getAngle(),
              B = I.getAngle();
            return !!(c
              ? v(T, N, B) ^ v(W, N, B) && v(T, B, N) ^ v(W, B, N)
              : v(N, T, W) ^ v(B, T, W) && v(N, W, T) ^ v(B, W, T));
          },
          hasOverlap: function () {
            return !!this._overlap;
          },
        },
        s.each(
          j._evaluateMethods,
          function (e) {
            var r = e + "At";
            this[e] = function () {
              var o = this.getCurve(),
                l = this.getTime();
              return l != null && o && o[r](l, !0);
            };
          },
          { preserve: !0 }
        ),
        new (function () {
          function e(r, o, l) {
            var d = r.length,
              c = 0,
              h = d - 1;
            function m(w, _) {
              for (var P = w + _; P >= -1 && P <= d; P += _) {
                var D = r[((P % d) + d) % d];
                if (!o.getPoint().isClose(D.getPoint(), 1e-7)) break;
                if (o.equals(D)) return D;
              }
              return null;
            }
            for (; c <= h;) {
              var p = (c + h) >>> 1,
                y = r[p],
                x;
              if (l && (x = o.equals(y) ? y : m(p, -1) || m(p, 1)))
                return (o._overlap && (x._overlap = x._intersection._overlap = !0), x);
              var b = o.getPath(),
                S = y.getPath(),
                v =
                  b !== S
                    ? b._id - S._id
                    : o.getIndex() + o.getTime() - (y.getIndex() + y.getTime());
              v < 0 ? (h = p - 1) : (c = p + 1);
            }
            return (r.splice(c, 0, o), o);
          }
          return {
            statics: {
              insert: e,
              expand: function (r) {
                for (var o = r.slice(), l = r.length - 1; l >= 0; l--) e(o, r[l]._intersection, !1);
                return o;
              },
            },
          };
        })()
      ),
      ei = ne.extend({
        _class: "PathItem",
        _selectBounds: !1,
        _canScaleStroke: !0,
        beans: !0,
        initialize: function () {},
        statics: {
          create: function (e) {
            var r, o, l;
            if (
              (s.isPlainObject(e)
                ? ((o = e.segments), (r = e.pathData))
                : Array.isArray(e)
                  ? (o = e)
                  : typeof e == "string" && (r = e),
              o)
            ) {
              var d = o[0];
              l = d && Array.isArray(d[0]);
            } else r && (l = (r.match(/m/gi) || []).length > 1 || /z\s*\S+/i.test(r));
            var c = l ? Ot : _e;
            return new c(e);
          },
        },
        _asPathItem: function () {
          return this;
        },
        isClockwise: function () {
          return this.getArea() >= 0;
        },
        setClockwise: function (e) {
          this.isClockwise() != (e = !!e) && this.reverse();
        },
        setPathData: function (e) {
          var r = e && e.match(/[mlhvcsqtaz][^mlhvcsqtaz]*/gi),
            o,
            l = !1,
            d,
            c,
            h = new R(),
            m = new R();
          function p(C, T) {
            var W = +o[C];
            return (l && (W += h[T]), W);
          }
          function y(C) {
            return new R(p(C, "x"), p(C + 1, "y"));
          }
          this.clear();
          for (var x = 0, b = r && r.length; x < b; x++) {
            var S = r[x],
              v = S[0],
              w = v.toLowerCase();
            o = S.match(/[+-]?(?:\d*\.\d+|\d+\.?)(?:[eE][+-]?\d+)?/g);
            var _ = o && o.length;
            switch (((l = v === w), d === "z" && !/[mz]/.test(w) && this.moveTo(h), w)) {
              case "m":
              case "l":
                for (var P = w === "m", D = 0; D < _; D += 2)
                  (this[P ? "moveTo" : "lineTo"]((h = y(D))), P && ((m = h), (P = !1)));
                c = h;
                break;
              case "h":
              case "v":
                var I = w === "h" ? "x" : "y";
                h = h.clone();
                for (var D = 0; D < _; D++) ((h[I] = p(D, I)), this.lineTo(h));
                c = h;
                break;
              case "c":
                for (var D = 0; D < _; D += 6)
                  this.cubicCurveTo(y(D), (c = y(D + 2)), (h = y(D + 4)));
                break;
              case "s":
                for (var D = 0; D < _; D += 4)
                  (this.cubicCurveTo(
                    /[cs]/.test(d) ? h.multiply(2).subtract(c) : h,
                    (c = y(D)),
                    (h = y(D + 2))
                  ),
                    (d = w));
                break;
              case "q":
                for (var D = 0; D < _; D += 4) this.quadraticCurveTo((c = y(D)), (h = y(D + 2)));
                break;
              case "t":
                for (var D = 0; D < _; D += 2)
                  (this.quadraticCurveTo(
                    (c = /[qt]/.test(d) ? h.multiply(2).subtract(c) : h),
                    (h = y(D))
                  ),
                    (d = w));
                break;
              case "a":
                for (var D = 0; D < _; D += 7)
                  this.arcTo(
                    (h = y(D + 5)),
                    new q(+o[D], +o[D + 1]),
                    +o[D + 2],
                    +o[D + 4],
                    +o[D + 3]
                  );
                break;
              case "z":
                (this.closePath(1e-12), (h = m));
                break;
            }
            d = w;
          }
        },
        _canComposite: function () {
          return !(this.hasFill() && this.hasStroke());
        },
        _contains: function (e) {
          var r = e.isInside(this.getBounds({ internal: !0, handle: !0 }))
            ? this._getWinding(e)
            : {};
          return (
            r.onPath ||
            !!(this.getFillRule() === "evenodd" ? r.windingL & 1 || r.windingR & 1 : r.winding)
          );
        },
        getIntersections: function (e, r, o, l) {
          var d = this === e || !e,
            c = this._matrix._orNullIfIdentity(),
            h = d ? c : (o || e._matrix)._orNullIfIdentity();
          return d || this.getBounds(c).intersects(e.getBounds(h), 1e-12)
            ? j.getIntersections(this.getCurves(), !d && e.getCurves(), r, c, h, l)
            : [];
        },
        getCrossings: function (e) {
          return this.getIntersections(e, function (r) {
            return r.hasOverlap() || r.isCrossing();
          });
        },
        getNearestLocation: function () {
          for (
            var e = R.read(arguments),
              r = this.getCurves(),
              o = 1 / 0,
              l = null,
              d = 0,
              c = r.length;
            d < c;
            d++
          ) {
            var h = r[d].getNearestLocation(e);
            h._distance < o && ((o = h._distance), (l = h));
          }
          return l;
        },
        getNearestPoint: function () {
          var e = this.getNearestLocation.apply(this, arguments);
          return e && e.getPoint();
        },
        interpolate: function (e, r, o) {
          var l = !this._children,
            d = l ? "_segments" : "_children",
            c = e[d],
            h = r[d],
            m = this[d];
          if (!c || !h || c.length !== h.length)
            throw new Error("Invalid operands in interpolate() call: " + e + ", " + r);
          var p = m.length,
            y = h.length;
          if (p < y) for (var x = l ? ae : _e, b = p; b < y; b++) this.add(new x());
          else p > y && this[l ? "removeSegments" : "removeChildren"](y, p);
          for (var b = 0; b < y; b++) m[b].interpolate(c[b], h[b], o);
          l && (this.setClosed(e._closed), this._changed(9));
        },
        compare: function (e) {
          var r = !1;
          if (e) {
            var o = this._children || [this],
              l = e._children ? e._children.slice() : [e],
              d = o.length,
              c = l.length,
              h = [],
              m = 0;
            r = !0;
            for (var p = d - 1; p >= 0 && r; p--) {
              var y = o[p];
              r = !1;
              for (var x = c - 1; x >= 0 && !r; x--)
                y.compare(l[x]) && (h[x] || ((h[x] = !0), m++), (r = !0));
            }
            r = r && m === c;
          }
          return r;
        },
      }),
      _e = ei.extend(
        {
          _class: "Path",
          _serializeFields: { segments: [], closed: !1 },
          initialize: function (r) {
            ((this._closed = !1), (this._segments = []), (this._version = 0));
            var o = Array.isArray(r)
              ? typeof r[0] == "object"
                ? r
                : arguments
              : r && r.size === t && (r.x !== t || r.point !== t)
                ? arguments
                : null;
            (o && o.length > 0
              ? this.setSegments(o)
              : ((this._curves = t),
                (this._segmentSelection = 0),
                !o && typeof r == "string" && (this.setPathData(r), (r = null))),
              this._initialize(!o && r));
          },
          _equals: function (e) {
            return this._closed === e._closed && s.equals(this._segments, e._segments);
          },
          copyContent: function (e) {
            (this.setSegments(e._segments), (this._closed = e._closed));
          },
          _changed: function e(r) {
            if ((e.base.call(this, r), r & 8)) {
              if (((this._length = this._area = t), r & 16)) this._version++;
              else if (this._curves)
                for (var o = 0, l = this._curves.length; o < l; o++) this._curves[o]._changed();
            } else r & 32 && (this._bounds = t);
          },
          getStyle: function () {
            var e = this._parent;
            return (e instanceof Ot ? e : this)._style;
          },
          getSegments: function () {
            return this._segments;
          },
          setSegments: function (e) {
            var r = this.isFullySelected(),
              o = e && e.length;
            if (
              ((this._segments.length = 0), (this._segmentSelection = 0), (this._curves = t), o)
            ) {
              var l = e[o - 1];
              (typeof l == "boolean" && (this.setClosed(l), o--),
                this._add(ae.readList(e, 0, {}, o)));
            }
            r && this.setFullySelected(!0);
          },
          getFirstSegment: function () {
            return this._segments[0];
          },
          getLastSegment: function () {
            return this._segments[this._segments.length - 1];
          },
          getCurves: function () {
            var e = this._curves,
              r = this._segments;
            if (!e) {
              var o = this._countCurves();
              e = this._curves = new Array(o);
              for (var l = 0; l < o; l++) e[l] = new j(this, r[l], r[l + 1] || r[0]);
            }
            return e;
          },
          getFirstCurve: function () {
            return this.getCurves()[0];
          },
          getLastCurve: function () {
            var e = this.getCurves();
            return e[e.length - 1];
          },
          isClosed: function () {
            return this._closed;
          },
          setClosed: function (e) {
            if (this._closed != (e = !!e)) {
              if (((this._closed = e), this._curves)) {
                var r = (this._curves.length = this._countCurves());
                e && (this._curves[r - 1] = new j(this, this._segments[r - 1], this._segments[0]));
              }
              this._changed(25);
            }
          },
        },
        {
          beans: !0,
          getPathData: function (e, r) {
            var o = this._segments,
              l = o.length,
              d = new k(r),
              c = new Array(6),
              h = !0,
              m,
              p,
              y,
              x,
              b,
              S,
              v,
              w,
              _ = [];
            function P(I, C) {
              if ((I._transformCoordinates(e, c), (m = c[0]), (p = c[1]), h))
                (_.push("M" + d.pair(m, p)), (h = !1));
              else if (((b = c[2]), (S = c[3]), b === m && S === p && v === y && w === x)) {
                if (!C) {
                  var T = m - y,
                    W = p - x;
                  _.push(
                    T === 0 ? "v" + d.number(W) : W === 0 ? "h" + d.number(T) : "l" + d.pair(T, W)
                  );
                }
              } else
                _.push(
                  "c" +
                    d.pair(v - y, w - x) +
                    " " +
                    d.pair(b - y, S - x) +
                    " " +
                    d.pair(m - y, p - x)
                );
              ((y = m), (x = p), (v = c[4]), (w = c[5]));
            }
            if (!l) return "";
            for (var D = 0; D < l; D++) P(o[D]);
            return (this._closed && l > 0 && (P(o[0], !0), _.push("z")), _.join(""));
          },
          isEmpty: function () {
            return !this._segments.length;
          },
          _transformContent: function (e) {
            for (var r = this._segments, o = new Array(6), l = 0, d = r.length; l < d; l++)
              r[l]._transformCoordinates(e, o, !0);
            return !0;
          },
          _add: function (e, h) {
            for (
              var o = this._segments,
                l = this._curves,
                d = e.length,
                c = h == null,
                h = c ? o.length : h,
                m = 0;
              m < d;
              m++
            ) {
              var p = e[m];
              (p._path && (p = e[m] = p.clone()),
                (p._path = this),
                (p._index = h + m),
                p._selection && this._updateSelection(p, 0, p._selection));
            }
            if (c) o.push.apply(o, e);
            else {
              o.splice.apply(o, [h, 0].concat(e));
              for (var m = h + d, y = o.length; m < y; m++) o[m]._index = m;
            }
            if (l) {
              var x = this._countCurves(),
                b = h > 0 && h + d - 1 === x ? h - 1 : h,
                S = b,
                v = Math.min(b + d, x);
              e._curves && (l.splice.apply(l, [b, 0].concat(e._curves)), (S += e._curves.length));
              for (var m = S; m < v; m++) l.splice(m, 0, new j(this, null, null));
              this._adjustCurves(b, v);
            }
            return (this._changed(25), e);
          },
          _adjustCurves: function (e, r) {
            for (var o = this._segments, l = this._curves, d, c = e; c < r; c++)
              ((d = l[c]),
                (d._path = this),
                (d._segment1 = o[c]),
                (d._segment2 = o[c + 1] || o[0]),
                d._changed());
            ((d = l[this._closed && !e ? o.length - 1 : e - 1]) &&
              ((d._segment2 = o[e] || o[0]), d._changed()),
              (d = l[r]) && ((d._segment1 = o[r]), d._changed()));
          },
          _countCurves: function () {
            var e = this._segments.length;
            return !this._closed && e > 0 ? e - 1 : e;
          },
          add: function (e) {
            return arguments.length > 1 && typeof e != "number"
              ? this._add(ae.readList(arguments))
              : this._add([ae.read(arguments)])[0];
          },
          insert: function (e, r) {
            return arguments.length > 2 && typeof r != "number"
              ? this._add(ae.readList(arguments, 1), e)
              : this._add([ae.read(arguments, 1)], e)[0];
          },
          addSegment: function () {
            return this._add([ae.read(arguments)])[0];
          },
          insertSegment: function (e) {
            return this._add([ae.read(arguments, 1)], e)[0];
          },
          addSegments: function (e) {
            return this._add(ae.readList(e));
          },
          insertSegments: function (e, r) {
            return this._add(ae.readList(r), e);
          },
          removeSegment: function (e) {
            return this.removeSegments(e, e + 1)[0] || null;
          },
          removeSegments: function (e, r, o) {
            ((e = e || 0), (r = s.pick(r, this._segments.length)));
            var l = this._segments,
              d = this._curves,
              c = l.length,
              h = l.splice(e, r - e),
              m = h.length;
            if (!m) return h;
            for (var p = 0; p < m; p++) {
              var y = h[p];
              (y._selection && this._updateSelection(y, y._selection, 0),
                (y._index = y._path = null));
            }
            for (var p = e, x = l.length; p < x; p++) l[p]._index = p;
            if (d) {
              for (
                var b = e > 0 && r === c + (this._closed ? 1 : 0) ? e - 1 : e,
                  d = d.splice(b, m),
                  p = d.length - 1;
                p >= 0;
                p--
              )
                d[p]._path = null;
              (o && (h._curves = d.slice(1)), this._adjustCurves(b, b));
            }
            return (this._changed(25), h);
          },
          clear: "#removeSegments",
          hasHandles: function () {
            for (var e = this._segments, r = 0, o = e.length; r < o; r++)
              if (e[r].hasHandles()) return !0;
            return !1;
          },
          clearHandles: function () {
            for (var e = this._segments, r = 0, o = e.length; r < o; r++) e[r].clearHandles();
          },
          getLength: function () {
            if (this._length == null) {
              for (var e = this.getCurves(), r = 0, o = 0, l = e.length; o < l; o++)
                r += e[o].getLength();
              this._length = r;
            }
            return this._length;
          },
          getArea: function () {
            var e = this._area;
            if (e == null) {
              var r = this._segments,
                o = this._closed;
              e = 0;
              for (var l = 0, d = r.length; l < d; l++) {
                var c = l + 1 === d;
                e += j.getArea(j.getValues(r[l], r[c ? 0 : l + 1], null, c && !o));
              }
              this._area = e;
            }
            return e;
          },
          isFullySelected: function () {
            var e = this._segments.length;
            return this.isSelected() && e > 0 && this._segmentSelection === e * 7;
          },
          setFullySelected: function (e) {
            (e && this._selectSegments(!0), this.setSelected(e));
          },
          setSelection: function e(r) {
            (r & 1 || this._selectSegments(!1), e.base.call(this, r));
          },
          _selectSegments: function (e) {
            var r = this._segments,
              o = r.length,
              l = e ? 7 : 0;
            this._segmentSelection = l * o;
            for (var d = 0; d < o; d++) r[d]._selection = l;
          },
          _updateSelection: function (e, r, o) {
            e._selection = o;
            var l = (this._segmentSelection += o - r);
            l > 0 && this.setSelected(!0);
          },
          divideAt: function (e) {
            var r = this.getLocationAt(e),
              o;
            return r && (o = r.getCurve().divideAt(r.getCurveOffset())) ? o._segment1 : null;
          },
          splitAt: function (e) {
            var r = this.getLocationAt(e),
              o = r && r.index,
              l = r && r.time,
              d = 1e-8,
              c = 1 - d;
            l > c && (o++, (l = 0));
            var h = this.getCurves();
            if (o >= 0 && o < h.length) {
              l >= d && h[o++].divideAtTime(l);
              var m = this.removeSegments(o, this._segments.length, !0),
                p;
              return (
                this._closed
                  ? (this.setClosed(!1), (p = this))
                  : ((p = new _e(ne.NO_INSERT)), p.insertAbove(this), p.copyAttributes(this)),
                p._add(m, 0),
                this.addSegment(m[0]),
                p
              );
            }
            return null;
          },
          split: function (e, r) {
            var o,
              l = r === t ? e : (o = this.getCurves()[e]) && o.getLocationAtTime(r);
            return l != null ? this.splitAt(l) : null;
          },
          join: function (e, r) {
            var o = r || 0;
            if (e && e !== this) {
              var l = e._segments,
                d = this.getLastSegment(),
                c = e.getLastSegment();
              if (!c) return this;
              d && d._point.isClose(c._point, o) && e.reverse();
              var h = e.getFirstSegment();
              if (d && d._point.isClose(h._point, o))
                (d.setHandleOut(h._handleOut), this._add(l.slice(1)));
              else {
                var m = this.getFirstSegment();
                (m && m._point.isClose(h._point, o) && e.reverse(),
                  (c = e.getLastSegment()),
                  m && m._point.isClose(c._point, o)
                    ? (m.setHandleIn(c._handleIn), this._add(l.slice(0, l.length - 1), 0))
                    : this._add(l.slice()));
              }
              (e._closed && this._add([l[0]]), e.remove());
            }
            var p = this.getFirstSegment(),
              y = this.getLastSegment();
            return (
              p !== y &&
                p._point.isClose(y._point, o) &&
                (p.setHandleIn(y._handleIn), y.remove(), this.setClosed(!0)),
              this
            );
          },
          reduce: function (e) {
            for (
              var r = this.getCurves(), o = e && e.simplify, l = o ? 1e-7 : 0, d = r.length - 1;
              d >= 0;
              d--
            ) {
              var c = r[d];
              !c.hasHandles() &&
                (!c.hasLength(l) || (o && c.isCollinear(c.getNext()))) &&
                c.remove();
            }
            return this;
          },
          reverse: function () {
            this._segments.reverse();
            for (var e = 0, r = this._segments.length; e < r; e++) {
              var o = this._segments[e],
                l = o._handleIn;
              ((o._handleIn = o._handleOut), (o._handleOut = l), (o._index = e));
            }
            ((this._curves = null), this._changed(9));
          },
          flatten: function (e) {
            for (
              var r = new Gl(this, e || 0.25, 256, !0), o = r.parts, l = o.length, d = [], c = 0;
              c < l;
              c++
            )
              d.push(new ae(o[c].curve.slice(0, 2)));
            (!this._closed && l > 0 && d.push(new ae(o[l - 1].curve.slice(6))),
              this.setSegments(d));
          },
          simplify: function (e) {
            var r = new Bg(this).fit(e || 2.5);
            return (r && this.setSegments(r), !!r);
          },
          smooth: function (e) {
            var r = this,
              o = e || {},
              l = o.type || "asymmetric",
              d = this._segments,
              c = d.length,
              h = this._closed;
            function m(ke, ve) {
              var he = ke && ke.index;
              if (he != null) {
                var ge = ke.path;
                if (ge && ge !== r)
                  throw new Error(ke._class + " " + he + " of " + ge + " is not part of " + r);
                ve && ke instanceof j && he++;
              } else he = typeof ke == "number" ? ke : ve;
              return Math.min(he < 0 && h ? he % c : he < 0 ? he + c : he, c - 1);
            }
            var p = h && o.from === t && o.to === t,
              y = m(o.from, 0),
              x = m(o.to, c - 1);
            if (y > x)
              if (h) y -= c;
              else {
                var b = y;
                ((y = x), (x = b));
              }
            if (/^(?:asymmetric|continuous)$/.test(l)) {
              var S = l === "asymmetric",
                v = Math.min,
                w = x - y + 1,
                _ = w - 1,
                P = p ? v(w, 4) : 1,
                D = P,
                I = P,
                C = [];
              if ((h || ((D = v(1, y)), (I = v(1, c - x - 1))), (_ += D + I), _ <= 1)) return;
              for (var T = 0, W = y - D; T <= _; T++, W++) C[T] = d[(W < 0 ? W + c : W) % c]._point;
              for (
                var N = C[0]._x + 2 * C[1]._x,
                  B = C[0]._y + 2 * C[1]._y,
                  V = 2,
                  L = _ - 1,
                  M = [N],
                  z = [B],
                  G = [V],
                  O = [],
                  E = [],
                  T = 1;
                T < _;
                T++
              ) {
                var H = T < L,
                  U = H || S ? 1 : 2,
                  Y = H ? 4 : S ? 2 : 7,
                  K = H ? 4 : S ? 3 : 8,
                  te = H ? 2 : S ? 0 : 1,
                  se = U / V;
                ((V = G[T] = Y - se),
                  (N = M[T] = K * C[T]._x + te * C[T + 1]._x - se * N),
                  (B = z[T] = K * C[T]._y + te * C[T + 1]._y - se * B));
              }
              ((O[L] = M[L] / G[L]), (E[L] = z[L] / G[L]));
              for (var T = _ - 2; T >= 0; T--)
                ((O[T] = (M[T] - O[T + 1]) / G[T]), (E[T] = (z[T] - E[T + 1]) / G[T]));
              ((O[_] = (3 * C[_]._x - O[L]) / 2), (E[_] = (3 * C[_]._y - E[L]) / 2));
              for (var T = D, ue = _ - I, W = y; T <= ue; T++, W++) {
                var me = d[W < 0 ? W + c : W],
                  Ce = me._point,
                  ce = O[T] - Ce._x,
                  xe = E[T] - Ce._y;
                ((p || T < ue) && me.setHandleOut(ce, xe),
                  (p || T > D) && me.setHandleIn(-ce, -xe));
              }
            } else
              for (var T = y; T <= x; T++)
                d[T < 0 ? T + c : T].smooth(o, !p && T === y, !p && T === x);
          },
          toShape: function (e) {
            if (!this._closed) return null;
            var r = this._segments,
              o,
              l,
              d,
              c;
            function h(S, v) {
              var w = r[S],
                _ = w.getNext(),
                P = r[v],
                D = P.getNext();
              return (
                w._handleOut.isZero() &&
                _._handleIn.isZero() &&
                P._handleOut.isZero() &&
                D._handleIn.isZero() &&
                _._point.subtract(w._point).isCollinear(D._point.subtract(P._point))
              );
            }
            function m(S) {
              var v = r[S],
                w = v.getPrevious(),
                _ = v.getNext();
              return (
                w._handleOut.isZero() &&
                v._handleIn.isZero() &&
                v._handleOut.isZero() &&
                _._handleIn.isZero() &&
                v._point.subtract(w._point).isOrthogonal(_._point.subtract(v._point))
              );
            }
            function p(S) {
              var v = r[S],
                w = v.getNext(),
                _ = v._handleOut,
                P = w._handleIn,
                D = 0.5522847498307936;
              if (_.isOrthogonal(P)) {
                var I = v._point,
                  C = w._point,
                  T = new oe(I, _, !0).intersect(new oe(C, P, !0), !0);
                return (
                  T &&
                  F.isZero(_.getLength() / T.subtract(I).getLength() - D) &&
                  F.isZero(P.getLength() / T.subtract(C).getLength() - D)
                );
              }
              return !1;
            }
            function y(S, v) {
              return r[S]._point.getDistance(r[v]._point);
            }
            if (
              (!this.hasHandles() && r.length === 4 && h(0, 2) && h(1, 3) && m(1)
                ? ((o = nt.Rectangle),
                  (l = new q(y(0, 3), y(0, 1))),
                  (c = r[1]._point.add(r[2]._point).divide(2)))
                : r.length === 8 && p(0) && p(2) && p(4) && p(6) && h(1, 5) && h(3, 7)
                  ? ((o = nt.Rectangle),
                    (l = new q(y(1, 6), y(0, 3))),
                    (d = l.subtract(new q(y(0, 7), y(1, 2))).divide(2)),
                    (c = r[3]._point.add(r[4]._point).divide(2)))
                  : r.length === 4 &&
                    p(0) &&
                    p(1) &&
                    p(2) &&
                    p(3) &&
                    (F.isZero(y(0, 2) - y(1, 3))
                      ? ((o = nt.Circle), (d = y(0, 2) / 2))
                      : ((o = nt.Ellipse), (d = new q(y(2, 0) / 2, y(3, 1) / 2))),
                    (c = r[1]._point)),
              o)
            ) {
              var x = this.getPosition(!0),
                b = new o({ center: x, size: l, radius: d, insert: !1 });
              return (
                b.copyAttributes(this, !0),
                b._matrix.prepend(this._matrix),
                b.rotate(c.subtract(x).getAngle() + 90),
                (e === t || e) && b.insertAbove(this),
                b
              );
            }
            return null;
          },
          toPath: "#clone",
          compare: function e(r) {
            if (!r || r instanceof Ot) return e.base.call(this, r);
            var o = this.getCurves(),
              l = r.getCurves(),
              d = o.length,
              c = l.length;
            if (!d || !c) return d == c;
            for (var h = o[0].getValues(), m = [], p = 0, y, x = 0, b, S = 0; S < c; S++) {
              var P = l[S].getValues();
              m.push(P);
              var v = j.getOverlaps(h, P);
              if (v) {
                ((y = !S && v[0][0] > 0 ? c - 1 : S), (b = v[0][1]));
                break;
              }
            }
            for (var w = Math.abs, _ = 1e-8, P = m[y], D; h && P;) {
              var v = j.getOverlaps(h, P);
              if (v) {
                var I = v[0][0];
                if (w(I - x) < _) {
                  ((x = v[1][0]), x === 1 && ((h = ++p < d ? o[p].getValues() : null), (x = 0)));
                  var C = v[0][1];
                  if (w(C - b) < _) {
                    if (
                      (D || (D = [y, C]),
                      (b = v[1][1]),
                      b === 1 && (++y >= c && (y = 0), (P = m[y] || l[y].getValues()), (b = 0)),
                      !h)
                    )
                      return D[0] === y && D[1] === b;
                    continue;
                  }
                }
              }
              break;
            }
            return !1;
          },
          _hitTestSelf: function (e, r, o, l) {
            var d = this,
              c = this.getStyle(),
              h = this._segments,
              m = h.length,
              p = this._closed,
              y = r._tolerancePadding,
              x = y,
              b,
              S,
              v,
              w,
              _,
              P,
              D = r.stroke && c.hasStroke(),
              I = r.fill && c.hasFill(),
              C = r.curves,
              T = D ? c.getStrokeWidth() / 2 : (I && r.tolerance > 0) || C ? 0 : null;
            T !== null &&
              (T > 0
                ? ((b = c.getStrokeJoin()),
                  (S = c.getStrokeCap()),
                  (v = c.getMiterLimit()),
                  (x = x.add(_e._getStrokePadding(T, l))))
                : (b = S = "round"));
            function W(O, E) {
              return e.subtract(O).divide(E).length <= 1;
            }
            function N(O, E, H) {
              if (!r.selected || E.isSelected()) {
                var U = O._point;
                if ((E !== U && (E = E.add(U)), W(E, x)))
                  return new rr(H, d, { segment: O, point: E });
              }
            }
            function B(O, E) {
              return (
                ((E || r.segments) && N(O, O._point, "segment")) ||
                (!E &&
                  r.handles &&
                  (N(O, O._handleIn, "handle-in") || N(O, O._handleOut, "handle-out")))
              );
            }
            function V(O) {
              w.add(O);
            }
            function L(O) {
              var E = p || (O._index > 0 && O._index < m - 1);
              if ((E ? b : S) === "round") return W(O._point, x);
              if (
                ((w = new _e({ internal: !0, closed: !0 })),
                E
                  ? O.isSmooth() || _e._addBevelJoin(O, b, T, v, null, l, V, !0)
                  : S === "square" && _e._addSquareCap(O, S, T, null, l, V, !0),
                !w.isEmpty())
              ) {
                var H;
                return w.contains(e) || ((H = w.getNearestLocation(e)) && W(H.getPoint(), y));
              }
            }
            if (r.ends && !r.segments && !p) {
              if ((P = B(h[0], !0) || B(h[m - 1], !0))) return P;
            } else if (r.segments || r.handles) {
              for (var M = 0; M < m; M++) if ((P = B(h[M]))) return P;
            }
            if (T !== null) {
              if (((_ = this.getNearestLocation(e)), _)) {
                var z = _.getTime();
                z === 0 || (z === 1 && m > 1)
                  ? L(_.getSegment()) || (_ = null)
                  : W(_.getPoint(), x) || (_ = null);
              }
              if (!_ && b === "miter" && m > 1)
                for (var M = 0; M < m; M++) {
                  var G = h[M];
                  if (e.getDistance(G._point) <= v * T && L(G)) {
                    _ = G.getLocation();
                    break;
                  }
                }
            }
            return (!_ && I && this._contains(e)) || (_ && !D && !C)
              ? new rr("fill", this)
              : _
                ? new rr(D ? "stroke" : "curve", this, { location: _, point: _.getPoint() })
                : null;
          },
        },
        s.each(
          j._evaluateMethods,
          function (e) {
            this[e + "At"] = function (r) {
              var o = this.getLocationAt(r);
              return o && o[e]();
            };
          },
          {
            beans: !1,
            getLocationOf: function () {
              for (
                var e = R.read(arguments), r = this.getCurves(), o = 0, l = r.length;
                o < l;
                o++
              ) {
                var d = r[o].getLocationOf(e);
                if (d) return d;
              }
              return null;
            },
            getOffsetOf: function () {
              var e = this.getLocationOf.apply(this, arguments);
              return e ? e.getOffset() : null;
            },
            getLocationAt: function (e) {
              if (typeof e == "number") {
                for (var r = this.getCurves(), o = 0, l = 0, d = r.length; l < d; l++) {
                  var c = o,
                    h = r[l];
                  if (((o += h.getLength()), o > e)) return h.getLocationAt(e - c);
                }
                if (r.length > 0 && e <= this.getLength()) return new zt(r[r.length - 1], 1);
              } else if (e && e.getPath && e.getPath() === this) return e;
              return null;
            },
          }
        ),
        new (function () {
          function e(o, l, d, c) {
            var h = c / 2,
              m = new Array(6),
              p,
              y;
            function x(P) {
              var D = m[P],
                I = m[P + 1];
              (p != D || y != I) &&
                (o.beginPath(),
                o.moveTo(p, y),
                o.lineTo(D, I),
                o.stroke(),
                o.beginPath(),
                o.arc(D, I, h, 0, Math.PI * 2, !0),
                o.fill());
            }
            for (var b = 0, S = l.length; b < S; b++) {
              var v = l[b],
                w = v._selection;
              if (
                (v._transformCoordinates(d, m),
                (p = m[0]),
                (y = m[1]),
                w & 2 && x(2),
                w & 4 && x(4),
                o.fillRect(p - h, y - h, c, c),
                !(w & 1))
              ) {
                var _ = o.fillStyle;
                ((o.fillStyle = "#ffffff"),
                  o.fillRect(p - h + 1, y - h + 1, c - 2, c - 2),
                  (o.fillStyle = _));
              }
            }
          }
          function r(o, l, d) {
            var c = l._segments,
              h = c.length,
              m = new Array(6),
              p = !0,
              y,
              x,
              b,
              S,
              v,
              w,
              _,
              P;
            function D(C) {
              if (d) (C._transformCoordinates(d, m), (y = m[0]), (x = m[1]));
              else {
                var T = C._point;
                ((y = T._x), (x = T._y));
              }
              if (p) (o.moveTo(y, x), (p = !1));
              else {
                if (d) ((v = m[2]), (w = m[3]));
                else {
                  var W = C._handleIn;
                  ((v = y + W._x), (w = x + W._y));
                }
                v === y && w === x && _ === b && P === S
                  ? o.lineTo(y, x)
                  : o.bezierCurveTo(_, P, v, w, y, x);
              }
              if (((b = y), (S = x), d)) ((_ = m[4]), (P = m[5]));
              else {
                var W = C._handleOut;
                ((_ = b + W._x), (P = S + W._y));
              }
            }
            for (var I = 0; I < h; I++) D(c[I]);
            l._closed && h > 0 && D(c[0]);
          }
          return {
            _draw: function (o, l, d, c) {
              var h = l.dontStart,
                m = l.dontFinish || l.clip,
                p = this.getStyle(),
                y = p.hasFill(),
                x = p.hasStroke(),
                b = p.getDashArray(),
                S = !Se.support.nativeDash && x && b && b.length;
              (h || o.beginPath(),
                (y || (x && !S) || m) && (r(o, this, c), this._closed && o.closePath()));
              function v(C) {
                return b[((C % S) + S) % S];
              }
              if (
                !m &&
                (y || x) &&
                (this._setStyles(o, l, d),
                y && (o.fill(p.getFillRule()), (o.shadowColor = "rgba(0,0,0,0)")),
                x)
              ) {
                if (S) {
                  h || o.beginPath();
                  var w = new Gl(this, 0.25, 32, !1, c),
                    _ = w.length,
                    P = -p.getDashOffset(),
                    D,
                    I = 0;
                  for (P = P % _; P > 0;) P -= v(I--) + v(I--);
                  for (; P < _;)
                    ((D = P + v(I++)),
                      (P > 0 || D > 0) && w.drawPart(o, Math.max(P, 0), Math.max(D, 0)),
                      (P = D + v(I++)));
                }
                o.stroke();
              }
            },
            _drawSelected: function (o, l) {
              (o.beginPath(),
                r(o, this, l),
                o.stroke(),
                e(o, this._segments, l, Se.settings.handleSize));
            },
          };
        })(),
        new (function () {
          function e(r) {
            var o = r._segments;
            if (!o.length) throw new Error("Use a moveTo() command first");
            return o[o.length - 1];
          }
          return {
            moveTo: function () {
              var r = this._segments;
              (r.length === 1 && this.removeSegment(0),
                r.length || this._add([new ae(R.read(arguments))]));
            },
            moveBy: function () {
              throw new Error("moveBy() is unsupported on Path items.");
            },
            lineTo: function () {
              this._add([new ae(R.read(arguments))]);
            },
            cubicCurveTo: function () {
              var r = R.read(arguments),
                o = R.read(arguments),
                l = R.read(arguments),
                d = e(this);
              (d.setHandleOut(r.subtract(d._point)), this._add([new ae(l, o.subtract(l))]));
            },
            quadraticCurveTo: function () {
              var r = R.read(arguments),
                o = R.read(arguments),
                l = e(this)._point;
              this.cubicCurveTo(
                r.add(l.subtract(r).multiply(1 / 3)),
                r.add(o.subtract(r).multiply(1 / 3)),
                o
              );
            },
            curveTo: function () {
              var r = R.read(arguments),
                o = R.read(arguments),
                l = s.pick(s.read(arguments), 0.5),
                d = 1 - l,
                c = e(this)._point,
                h = r
                  .subtract(c.multiply(d * d))
                  .subtract(o.multiply(l * l))
                  .divide(2 * l * d);
              if (h.isNaN())
                throw new Error("Cannot put a curve through points with parameter = " + l);
              this.quadraticCurveTo(h, o);
            },
            arcTo: function () {
              var r = Math.abs,
                o = Math.sqrt,
                l = e(this),
                d = l._point,
                c = R.read(arguments),
                h,
                m = s.peek(arguments),
                p = s.pick(m, !0),
                y,
                x,
                b,
                S;
              if (typeof p == "boolean")
                var v = d.add(c).divide(2),
                  h = v.add(v.subtract(d).rotate(p ? -90 : 90));
              else if (s.remain(arguments) <= 2) ((h = c), (c = R.read(arguments)));
              else {
                var w = q.read(arguments),
                  _ = F.isZero;
                if (_(w.width) || _(w.height)) return this.lineTo(c);
                var P = s.read(arguments),
                  p = !!s.read(arguments),
                  D = !!s.read(arguments),
                  v = d.add(c).divide(2),
                  I = d.subtract(v).rotate(-P),
                  C = I.x,
                  T = I.y,
                  W = r(w.width),
                  N = r(w.height),
                  B = W * W,
                  V = N * N,
                  L = C * C,
                  M = T * T,
                  z = o(L / B + M / V);
                if (
                  (z > 1 && ((W *= z), (N *= z), (B = W * W), (V = N * N)),
                  (z = (B * V - B * M - V * L) / (B * M + V * L)),
                  r(z) < 1e-12 && (z = 0),
                  z < 0)
                )
                  throw new Error("Cannot create an arc with the given arguments");
                ((y = new R((W * T) / N, (-N * C) / W)
                  .multiply((D === p ? -1 : 1) * o(z))
                  .rotate(P)
                  .add(v)),
                  (S = new Q().translate(y).rotate(P).scale(W, N)),
                  (b = S._inverseTransform(d)),
                  (x = b.getDirectedAngle(S._inverseTransform(c))),
                  !p && x > 0 ? (x -= 360) : p && x < 0 && (x += 360));
              }
              if (h) {
                var G = new oe(d.add(h).divide(2), h.subtract(d).rotate(90), !0),
                  O = new oe(h.add(c).divide(2), c.subtract(h).rotate(90), !0),
                  E = new oe(d, c),
                  H = E.getSide(h);
                if (((y = G.intersect(O, !0)), !y)) {
                  if (!H) return this.lineTo(c);
                  throw new Error("Cannot create an arc with the given arguments");
                }
                ((b = d.subtract(y)), (x = b.getDirectedAngle(c.subtract(y))));
                var U = E.getSide(y);
                U === 0 ? (x = H * r(x)) : H === U && (x += x < 0 ? 360 : -360);
              }
              for (
                var Y = 1e-7,
                  K = r(x),
                  te = K >= 360 ? 4 : Math.ceil((K - Y) / 90),
                  se = x / te,
                  ue = (se * Math.PI) / 360,
                  me = ((4 / 3) * Math.sin(ue)) / (1 + Math.cos(ue)),
                  Ce = [],
                  ce = 0;
                ce <= te;
                ce++
              ) {
                var I = c,
                  xe = null;
                if (
                  (ce < te &&
                    ((xe = b.rotate(90).multiply(me)),
                    S
                      ? ((I = S._transformPoint(b)),
                        (xe = S._transformPoint(b.add(xe)).subtract(I)))
                      : (I = y.add(b))),
                  !ce)
                )
                  l.setHandleOut(xe);
                else {
                  var ke = b.rotate(-90).multiply(me);
                  (S && (ke = S._transformPoint(b.add(ke)).subtract(I)),
                    Ce.push(new ae(I, ke, xe)));
                }
                b = b.rotate(se);
              }
              this._add(Ce);
            },
            lineBy: function () {
              var r = R.read(arguments),
                o = e(this)._point;
              this.lineTo(o.add(r));
            },
            curveBy: function () {
              var r = R.read(arguments),
                o = R.read(arguments),
                l = s.read(arguments),
                d = e(this)._point;
              this.curveTo(d.add(r), d.add(o), l);
            },
            cubicCurveBy: function () {
              var r = R.read(arguments),
                o = R.read(arguments),
                l = R.read(arguments),
                d = e(this)._point;
              this.cubicCurveTo(d.add(r), d.add(o), d.add(l));
            },
            quadraticCurveBy: function () {
              var r = R.read(arguments),
                o = R.read(arguments),
                l = e(this)._point;
              this.quadraticCurveTo(l.add(r), l.add(o));
            },
            arcBy: function () {
              var r = e(this)._point,
                o = r.add(R.read(arguments)),
                l = s.pick(s.peek(arguments), !0);
              typeof l == "boolean" ? this.arcTo(o, l) : this.arcTo(o, r.add(R.read(arguments)));
            },
            closePath: function (r) {
              (this.setClosed(!0), this.join(this, r));
            },
          };
        })(),
        {
          _getBounds: function (e, r) {
            var o = r.handle ? "getHandleBounds" : r.stroke ? "getStrokeBounds" : "getBounds";
            return _e[o](this._segments, this._closed, this, e, r);
          },
          statics: {
            getBounds: function (e, r, o, l, d, c) {
              var h = e[0];
              if (!h) return new X();
              var m = new Array(6),
                p = h._transformCoordinates(l, new Array(6)),
                y = p.slice(0, 2),
                x = y.slice(),
                b = new Array(2);
              function S(_) {
                _._transformCoordinates(l, m);
                for (var P = 0; P < 2; P++)
                  j._addBounds(p[P], p[P + 4], m[P + 2], m[P], P, c ? c[P] : 0, y, x, b);
                var D = p;
                ((p = m), (m = D));
              }
              for (var v = 1, w = e.length; v < w; v++) S(e[v]);
              return (r && S(h), new X(y[0], y[1], x[0] - y[0], x[1] - y[1]));
            },
            getStrokeBounds: function (e, r, o, l, d) {
              var c = o.getStyle(),
                h = c.hasStroke(),
                m = c.getStrokeWidth(),
                p = h && o._getStrokeMatrix(l, d),
                y = h && _e._getStrokePadding(m, p),
                x = _e.getBounds(e, r, o, l, d, y);
              if (!h) return x;
              var b = m / 2,
                S = c.getStrokeJoin(),
                v = c.getStrokeCap(),
                w = c.getMiterLimit(),
                _ = new X(new q(y));
              function P(N) {
                x = x.include(N);
              }
              function D(N) {
                x = x.unite(_.setCenter(N._point.transform(l)));
              }
              function I(N, B) {
                B === "round" || N.isSmooth() ? D(N) : _e._addBevelJoin(N, B, b, w, l, p, P);
              }
              function C(N, B) {
                B === "round" ? D(N) : _e._addSquareCap(N, B, b, l, p, P);
              }
              for (var T = e.length - (r ? 0 : 1), W = 1; W < T; W++) I(e[W], S);
              return (r ? I(e[0], S) : T > 0 && (C(e[0], v), C(e[e.length - 1], v)), x);
            },
            _getStrokePadding: function (e, r) {
              if (!r) return [e, e];
              var o = new R(e, 0).transform(r),
                l = new R(0, e).transform(r),
                d = o.getAngleInRadians(),
                c = o.getLength(),
                h = l.getLength(),
                m = Math.sin(d),
                p = Math.cos(d),
                y = Math.tan(d),
                x = Math.atan2(h * y, c),
                b = Math.atan2(h, y * c);
              return [
                Math.abs(c * Math.cos(x) * p + h * Math.sin(x) * m),
                Math.abs(h * Math.sin(b) * p + c * Math.cos(b) * m),
              ];
            },
            _addBevelJoin: function (e, r, o, l, d, c, h, m) {
              var p = e.getCurve(),
                y = p.getPrevious(),
                x = p.getPoint1().transform(d),
                b = y.getNormalAtTime(1).multiply(o).transform(c),
                S = p.getNormalAtTime(0).multiply(o).transform(c);
              if (
                (b.getDirectedAngle(S) < 0 && ((b = b.negate()), (S = S.negate())),
                m && h(x),
                h(x.add(b)),
                r === "miter")
              ) {
                var v = new oe(x.add(b), new R(-b.y, b.x), !0).intersect(
                  new oe(x.add(S), new R(-S.y, S.x), !0),
                  !0
                );
                v && x.getDistance(v) <= l * o && h(v);
              }
              h(x.add(S));
            },
            _addSquareCap: function (e, r, o, l, d, c, h) {
              var m = e._point.transform(l),
                p = e.getLocation(),
                y = p
                  .getNormal()
                  .multiply(p.getTime() === 0 ? o : -o)
                  .transform(d);
              (r === "square" && (h && (c(m.subtract(y)), c(m.add(y))), (m = m.add(y.rotate(-90)))),
                c(m.add(y)),
                c(m.subtract(y)));
            },
            getHandleBounds: function (e, r, o, l, d) {
              var c = o.getStyle(),
                h = d.stroke && c.hasStroke(),
                m,
                p;
              if (h) {
                var y = o._getStrokeMatrix(l, d),
                  x = c.getStrokeWidth() / 2,
                  b = x;
                (c.getStrokeJoin() === "miter" && (b = x * c.getMiterLimit()),
                  c.getStrokeCap() === "square" && (b = Math.max(b, x * Math.SQRT2)),
                  (m = _e._getStrokePadding(x, y)),
                  (p = _e._getStrokePadding(b, y)));
              }
              for (
                var S = new Array(6), v = 1 / 0, w = -v, _ = v, P = w, D = 0, I = e.length;
                D < I;
                D++
              ) {
                var C = e[D];
                C._transformCoordinates(l, S);
                for (var T = 0; T < 6; T += 2) {
                  var W = T ? m : p,
                    N = W ? W[0] : 0,
                    B = W ? W[1] : 0,
                    V = S[T],
                    L = S[T + 1],
                    M = V - N,
                    z = V + N,
                    G = L - B,
                    O = L + B;
                  (M < v && (v = M), z > w && (w = z), G < _ && (_ = G), O > P && (P = O));
                }
              }
              return new X(v, _, w - v, P - _);
            },
          },
        }
      );
    _e.inject({
      statics: new (function () {
        var e = 0.5522847498307936,
          r = [
            new ae([-1, 0], [0, e], [0, -e]),
            new ae([0, -1], [-e, 0], [e, 0]),
            new ae([1, 0], [0, -e], [0, e]),
            new ae([0, 1], [e, 0], [-e, 0]),
          ];
        function o(d, c, h) {
          var m = s.getNamed(h),
            p = new _e(m && m.insert == !1 && ne.NO_INSERT);
          return (p._add(d), (p._closed = c), p.set(m, { insert: !0 }));
        }
        function l(d, c, h) {
          for (var m = new Array(4), p = 0; p < 4; p++) {
            var y = r[p];
            m[p] = new ae(
              y._point.multiply(c).add(d),
              y._handleIn.multiply(c),
              y._handleOut.multiply(c)
            );
          }
          return o(m, !0, h);
        }
        return {
          Line: function () {
            return o(
              [new ae(R.readNamed(arguments, "from")), new ae(R.readNamed(arguments, "to"))],
              !1,
              arguments
            );
          },
          Circle: function () {
            var d = R.readNamed(arguments, "center"),
              c = s.readNamed(arguments, "radius");
            return l(d, new q(c), arguments);
          },
          Rectangle: function () {
            var d = X.readNamed(arguments, "rectangle"),
              c = q.readNamed(arguments, "radius", 0, { readNull: !0 }),
              h = d.getBottomLeft(!0),
              m = d.getTopLeft(!0),
              p = d.getTopRight(!0),
              y = d.getBottomRight(!0),
              x;
            if (!c || c.isZero()) x = [new ae(h), new ae(m), new ae(p), new ae(y)];
            else {
              c = q.min(c, d.getSize(!0).divide(2));
              var b = c.width,
                S = c.height,
                v = b * e,
                w = S * e;
              x = [
                new ae(h.add(b, 0), null, [-v, 0]),
                new ae(h.subtract(0, S), [0, w]),
                new ae(m.add(0, S), null, [0, -w]),
                new ae(m.add(b, 0), [-v, 0], null),
                new ae(p.subtract(b, 0), null, [v, 0]),
                new ae(p.add(0, S), [0, -w], null),
                new ae(y.subtract(0, S), null, [0, w]),
                new ae(y.subtract(b, 0), [v, 0]),
              ];
            }
            return o(x, !0, arguments);
          },
          RoundRectangle: "#Rectangle",
          Ellipse: function () {
            var d = nt._readEllipse(arguments);
            return l(d.center, d.radius, arguments);
          },
          Oval: "#Ellipse",
          Arc: function () {
            var d = R.readNamed(arguments, "from"),
              c = R.readNamed(arguments, "through"),
              h = R.readNamed(arguments, "to"),
              m = s.getNamed(arguments),
              p = new _e(m && m.insert == !1 && ne.NO_INSERT);
            return (p.moveTo(d), p.arcTo(c, h), p.set(m));
          },
          RegularPolygon: function () {
            for (
              var d = R.readNamed(arguments, "center"),
                c = s.readNamed(arguments, "sides"),
                h = s.readNamed(arguments, "radius"),
                m = 360 / c,
                p = c % 3 === 0,
                y = new R(0, p ? -h : h),
                x = p ? -1 : 0.5,
                b = new Array(c),
                S = 0;
              S < c;
              S++
            )
              b[S] = new ae(d.add(y.rotate((S + x) * m)));
            return o(b, !0, arguments);
          },
          Star: function () {
            for (
              var d = R.readNamed(arguments, "center"),
                c = s.readNamed(arguments, "points") * 2,
                h = s.readNamed(arguments, "radius1"),
                m = s.readNamed(arguments, "radius2"),
                p = 360 / c,
                y = new R(0, -1),
                x = new Array(c),
                b = 0;
              b < c;
              b++
            )
              x[b] = new ae(d.add(y.rotate(p * b).multiply(b % 2 ? m : h)));
            return o(x, !0, arguments);
          },
        };
      })(),
    });
    var Ot = ei.extend(
      {
        _class: "CompoundPath",
        _serializeFields: { children: [] },
        beans: !0,
        initialize: function (r) {
          ((this._children = []),
            (this._namedChildren = {}),
            this._initialize(r) ||
              (typeof r == "string"
                ? this.setPathData(r)
                : this.addChildren(Array.isArray(r) ? r : arguments)));
        },
        insertChildren: function e(r, o) {
          var l = o,
            d = l[0];
          d && typeof d[0] == "number" && (l = [l]);
          for (var c = o.length - 1; c >= 0; c--) {
            var h = l[c];
            (l === o && !(h instanceof _e) && (l = s.slice(l)),
              Array.isArray(h)
                ? (l[c] = new _e({ segments: h, insert: !1 }))
                : h instanceof Ot &&
                  (l.splice.apply(l, [c, 1].concat(h.removeChildren())), h.remove()));
          }
          return e.base.call(this, r, l);
        },
        reduce: function e(r) {
          for (var o = this._children, l = o.length - 1; l >= 0; l--) {
            var d = o[l].reduce(r);
            d.isEmpty() && d.remove();
          }
          if (!o.length) {
            var d = new _e(ne.NO_INSERT);
            return (d.copyAttributes(this), d.insertAbove(this), this.remove(), d);
          }
          return e.base.call(this);
        },
        isClosed: function () {
          for (var e = this._children, r = 0, o = e.length; r < o; r++)
            if (!e[r]._closed) return !1;
          return !0;
        },
        setClosed: function (e) {
          for (var r = this._children, o = 0, l = r.length; o < l; o++) r[o].setClosed(e);
        },
        getFirstSegment: function () {
          var e = this.getFirstChild();
          return e && e.getFirstSegment();
        },
        getLastSegment: function () {
          var e = this.getLastChild();
          return e && e.getLastSegment();
        },
        getCurves: function () {
          for (var e = this._children, r = [], o = 0, l = e.length; o < l; o++)
            r.push.apply(r, e[o].getCurves());
          return r;
        },
        getFirstCurve: function () {
          var e = this.getFirstChild();
          return e && e.getFirstCurve();
        },
        getLastCurve: function () {
          var e = this.getLastChild();
          return e && e.getLastCurve();
        },
        getArea: function () {
          for (var e = this._children, r = 0, o = 0, l = e.length; o < l; o++) r += e[o].getArea();
          return r;
        },
        getLength: function () {
          for (var e = this._children, r = 0, o = 0, l = e.length; o < l; o++)
            r += e[o].getLength();
          return r;
        },
        getPathData: function (e, r) {
          for (var o = this._children, l = [], d = 0, c = o.length; d < c; d++) {
            var h = o[d],
              m = h._matrix;
            l.push(h.getPathData(e && !m.isIdentity() ? e.appended(m) : e, r));
          }
          return l.join("");
        },
        _hitTestChildren: function e(r, o, l) {
          return e.base.call(
            this,
            r,
            o.class === _e || o.type === "path" ? o : s.set({}, o, { fill: !1 }),
            l
          );
        },
        _draw: function (e, r, o, l) {
          var d = this._children;
          if (d.length) {
            ((r = r.extend({ dontStart: !0, dontFinish: !0 })), e.beginPath());
            for (var c = 0, h = d.length; c < h; c++) d[c].draw(e, r, l);
            if (!r.clip) {
              this._setStyles(e, r, o);
              var m = this._style;
              (m.hasFill() && (e.fill(m.getFillRule()), (e.shadowColor = "rgba(0,0,0,0)")),
                m.hasStroke() && e.stroke());
            }
          }
        },
        _drawSelected: function (e, r, o) {
          for (var l = this._children, d = 0, c = l.length; d < c; d++) {
            var h = l[d],
              m = h._matrix;
            o[h._id] || h._drawSelected(e, m.isIdentity() ? r : r.appended(m));
          }
        },
      },
      new (function () {
        function e(r, o) {
          var l = r._children;
          if (o && !l.length) throw new Error("Use a moveTo() command first");
          return l[l.length - 1];
        }
        return s.each(
          [
            "lineTo",
            "cubicCurveTo",
            "quadraticCurveTo",
            "curveTo",
            "arcTo",
            "lineBy",
            "cubicCurveBy",
            "quadraticCurveBy",
            "curveBy",
            "arcBy",
          ],
          function (r) {
            this[r] = function () {
              var o = e(this, !0);
              o[r].apply(o, arguments);
            };
          },
          {
            moveTo: function () {
              var r = e(this),
                o = r && r.isEmpty() ? r : new _e(ne.NO_INSERT);
              (o !== r && this.addChild(o), o.moveTo.apply(o, arguments));
            },
            moveBy: function () {
              var r = e(this, !0),
                o = r && r.getLastSegment(),
                l = R.read(arguments);
              this.moveTo(o ? l.add(o._point) : l);
            },
            closePath: function (r) {
              e(this, !0).closePath(r);
            },
          }
        );
      })(),
      s.each(
        ["reverse", "flatten", "simplify", "smooth"],
        function (e) {
          this[e] = function (r) {
            for (var o = this._children, l, d = 0, c = o.length; d < c; d++) l = o[d][e](r) || l;
            return l;
          };
        },
        {}
      )
    );
    ei.inject(
      new (function () {
        var e = Math.min,
          r = Math.max,
          o = Math.abs,
          l = {
            unite: { 1: !0, 2: !0 },
            intersect: { 2: !0 },
            subtract: { 1: !0 },
            exclude: { 1: !0, "-1": !0 },
          };
        function d(_, P) {
          var D = _.clone(!1).reduce({ simplify: !0 }).transform(null, !0, !0);
          return P ? D.resolveCrossings().reorient(D.getFillRule() === "nonzero", !0) : D;
        }
        function c(_, P, D, I, C) {
          var T = new Ot(ne.NO_INSERT);
          return (
            T.addChildren(_, !0),
            (T = T.reduce({ simplify: P })),
            (C && C.insert == !1) ||
              T.insertAbove(I && D.isSibling(I) && D.getIndex() < I.getIndex() ? I : D),
            T.copyAttributes(D, !0),
            T
          );
        }
        function h(_, P, D, I) {
          if (I && (I.trace == !1 || I.stroke) && /^(subtract|intersect)$/.test(D))
            return m(_, P, D);
          var C = d(_, !0),
            T = P && _ !== P && d(P, !0),
            W = l[D];
          ((W[D] = !0),
            T && (W.subtract || W.exclude) ^ (T.isClockwise() ^ C.isClockwise()) && T.reverse());
          var N = b(zt.expand(C.getCrossings(T))),
            B = C._children || [C],
            V = T && (T._children || [T]),
            L = [],
            M = [],
            z;
          function G(Y) {
            for (var K = 0, te = Y.length; K < te; K++) {
              var se = Y[K];
              (L.push.apply(L, se._segments),
                M.push.apply(M, se.getCurves()),
                (se._overlapsOnly = !0));
            }
          }
          if (N.length) {
            (G(B), V && G(V));
            for (var O = 0, E = N.length; O < E; O++) v(N[O]._segment, C, T, M, W);
            for (var O = 0, E = L.length; O < E; O++) {
              var H = L[O],
                U = H._intersection;
              (H._winding || v(H, C, T, M, W), (U && U._overlap) || (H._path._overlapsOnly = !1));
            }
            z = w(L, W);
          } else
            z = x(V ? B.concat(V) : B.slice(), function (Y) {
              return !!W[Y];
            });
          return c(z, !0, _, P, I);
        }
        function m(_, P, D) {
          var I = d(_),
            C = d(P),
            T = I.getCrossings(C),
            W = D === "subtract",
            N = D === "divide",
            B = {},
            V = [];
          function L(G) {
            if (!B[G._id] && (N || C.contains(G.getPointAt(G.getLength() / 2)) ^ W))
              return (V.unshift(G), (B[G._id] = !0));
          }
          for (var M = T.length - 1; M >= 0; M--) {
            var z = T[M].split();
            z &&
              (L(z) && z.getFirstSegment().setHandleIn(0, 0),
              I.getLastSegment().setHandleOut(0, 0));
          }
          return (L(I), c(V, !1, _, P));
        }
        function p(_, P) {
          for (var D = _; D;) {
            if (D === P) return;
            D = D._previous;
          }
          for (; _._next && _._next !== P;) _ = _._next;
          if (!_._next) {
            for (; P._previous;) P = P._previous;
            ((_._next = P), (P._previous = _));
          }
        }
        function y(_) {
          for (var P = _.length - 1; P >= 0; P--) _[P].clearHandles();
        }
        function x(_, P, D) {
          var I = _ && _.length;
          if (I) {
            var C = s.each(
                _,
                function (H, U) {
                  this[H._id] = { container: null, winding: H.isClockwise() ? 1 : -1, index: U };
                },
                {}
              ),
              T = _.slice().sort(function (H, U) {
                return o(U.getArea()) - o(H.getArea());
              }),
              W = T[0];
            D == null && (D = W.isClockwise());
            for (var N = 0; N < I; N++) {
              for (
                var B = T[N], V = C[B._id], L = B.getInteriorPoint(), M = 0, z = N - 1;
                z >= 0;
                z--
              ) {
                var G = T[z];
                if (G.contains(L)) {
                  var O = C[G._id];
                  ((M = O.winding), (V.winding += M), (V.container = O.exclude ? O.container : G));
                  break;
                }
              }
              if (P(V.winding) === P(M)) ((V.exclude = !0), (_[V.index] = null));
              else {
                var E = V.container;
                B.setClockwise(E ? !E.isClockwise() : D);
              }
            }
          }
          return _;
        }
        function b(_, P, D) {
          var I = P && [],
            C = 1e-8,
            T = 1 - C,
            W = !1,
            N = D || [],
            B = D && {},
            V,
            L,
            M;
          function z(xe) {
            return xe._path._id + "." + xe._segment1._index;
          }
          for (var G = (D && D.length) - 1; G >= 0; G--) {
            var O = D[G];
            O._path && (B[z(O)] = !0);
          }
          for (var G = _.length - 1; G >= 0; G--) {
            var E = _[G],
              H = E._time,
              U = H,
              Y = P && !P(E),
              O = E._curve,
              K;
            if (
              (O &&
                (O !== L
                  ? ((W = !O.hasHandles() || (B && B[z(O)])), (V = []), (M = null), (L = O))
                  : M >= C && (H /= M)),
              Y)
            ) {
              V && V.push(E);
              continue;
            } else P && I.unshift(E);
            if (((M = U), H < C)) K = O._segment1;
            else if (H > T) K = O._segment2;
            else {
              var te = O.divideAtTime(H, !0);
              (W && N.push(O, te), (K = te._segment1));
              for (var se = V.length - 1; se >= 0; se--) {
                var ue = V[se];
                ue._time = (ue._time - H) / (1 - H);
              }
            }
            E._setSegment(K);
            var me = K._intersection,
              Ce = E._intersection;
            if (me) {
              p(me, Ce);
              for (var ce = me; ce;) (p(ce._intersection, me), (ce = ce._next));
            } else K._intersection = Ce;
          }
          return (D || y(N), I || _);
        }
        function S(_, P, D, I, C) {
          var T = D ? 1 : 0,
            W = T ^ 1,
            N = [_.x, _.y],
            B = N[T],
            V = N[W],
            L = 1e-9,
            M = 1e-6,
            z = B - L,
            G = B + L,
            O = 0,
            E = 0,
            H = 0,
            U = 0,
            Y = !1,
            K = !1,
            te = 1,
            se = [],
            ue,
            me;
          function Ce(ye) {
            var xt = ye[W + 0],
              at = ye[W + 6];
            if (!(V < e(xt, at) || V > r(xt, at))) {
              var vt = ye[T + 0],
                Ht = ye[T + 2],
                mr = ye[T + 4],
                hr = ye[T + 6];
              if (xt === at) {
                ((vt < G && hr > z) || (hr < G && vt > z)) && (Y = !0);
                return;
              }
              var Er =
                  V === xt
                    ? 0
                    : V === at || z > r(vt, Ht, mr, hr) || G < e(vt, Ht, mr, hr)
                      ? 1
                      : j.solveCubic(ye, W, V, se, 0, 1) > 0
                        ? se[0]
                        : 1,
                Lt = Er === 0 ? vt : Er === 1 ? hr : j.getPoint(ye, Er)[D ? "y" : "x"],
                Gt = xt > at ? 1 : -1,
                io = ue[W] > ue[W + 6] ? 1 : -1,
                Ar = ue[T + 6];
              return (
                V !== xt
                  ? (Lt < z ? (H += Gt) : Lt > G ? (U += Gt) : (Y = !0),
                    Lt > B - M && Lt < B + M && (te /= 2))
                  : (Gt !== io
                      ? vt < z
                        ? (H += Gt)
                        : vt > G && (U += Gt)
                      : vt != Ar &&
                        (Ar < G && Lt > G
                          ? ((U += Gt), (Y = !0))
                          : Ar > z && Lt < z && ((H += Gt), (Y = !0))),
                    (te = 0)),
                (ue = ye),
                !C &&
                  Lt > z &&
                  Lt < G &&
                  j.getTangent(ye, Er)[D ? "x" : "y"] === 0 &&
                  S(_, P, !D, I, !0)
              );
            }
          }
          function ce(ye) {
            var xt = ye[W + 0],
              at = ye[W + 2],
              vt = ye[W + 4],
              Ht = ye[W + 6];
            if (V <= r(xt, at, vt, Ht) && V >= e(xt, at, vt, Ht)) {
              for (
                var mr = ye[T + 0],
                  hr = ye[T + 2],
                  Er = ye[T + 4],
                  Lt = ye[T + 6],
                  Gt =
                    z > r(mr, hr, Er, Lt) || G < e(mr, hr, Er, Lt) ? [ye] : j.getMonoCurves(ye, D),
                  io,
                  Ar = 0,
                  Og = Gt.length;
                Ar < Og;
                Ar++
              )
                if ((io = Ce(Gt[Ar]))) return io;
            }
          }
          for (var xe = 0, ke = P.length; xe < ke; xe++) {
            var ve = P[xe],
              he = ve._path,
              ge = ve.getValues(),
              Me;
            if (
              (!xe || P[xe - 1]._path !== he) &&
              ((ue = null),
              he._closed ||
                ((me = j.getValues(he.getLastCurve().getSegment2(), ve.getSegment1(), null, !I)),
                me[W] !== me[W + 6] && (ue = me)),
              !ue)
            ) {
              ue = ge;
              for (var ot = he.getLastCurve(); ot && ot !== ve;) {
                var St = ot.getValues();
                if (St[W] !== St[W + 6]) {
                  ue = St;
                  break;
                }
                ot = ot.getPrevious();
              }
            }
            if ((Me = ce(ge))) return Me;
            if (xe + 1 === ke || P[xe + 1]._path !== he) {
              if (me && (Me = ce(me))) return Me;
              (Y && !H && !U && (H = U = he.isClockwise(I) ^ D ? 1 : -1),
                (O += H),
                (E += U),
                (H = U = 0),
                Y && ((K = !0), (Y = !1)),
                (me = null));
            }
          }
          return (
            (O = o(O)),
            (E = o(E)),
            { winding: r(O, E), windingL: O, windingR: E, quality: te, onPath: K }
          );
        }
        function v(_, P, D, I, C) {
          var T = [],
            W = _,
            N = 0,
            M;
          do {
            var B = _.getCurve(),
              V = B.getLength();
            (T.push({ segment: _, curve: B, length: V }), (N += V), (_ = _.getNext()));
          } while (_ && !_._intersection && _ !== W);
          for (
            var L = [0.5, 0.25, 0.75], M = { winding: 0, quality: -1 }, z = 1e-8, G = 1 - z, O = 0;
            O < L.length && M.quality < 0.5;
            O++
          )
            for (var V = N * L[O], E = 0, H = T.length; E < H; E++) {
              var U = T[E],
                Y = U.length;
              if (V <= Y) {
                var B = U.curve,
                  K = B._path,
                  te = K._parent,
                  se = te instanceof Ot ? te : K,
                  ue = F.clamp(B.getTimeAt(V), z, G),
                  me = B.getPointAtTime(ue),
                  Ce = o(B.getTangentAtTime(ue).y) < Math.SQRT1_2,
                  ce =
                    C.subtract &&
                    D &&
                    ((se === P && D._getWinding(me, Ce, !0).winding) ||
                      (se === D && !P._getWinding(me, Ce, !0).winding))
                      ? { winding: 0, quality: 1 }
                      : S(me, I, Ce, !0);
                ce.quality > M.quality && (M = ce);
                break;
              }
              V -= Y;
            }
          for (var E = T.length - 1; E >= 0; E--) T[E].segment._winding = M;
        }
        function w(_, P) {
          var D = [],
            I;
          function C(ve) {
            var he;
            return !!(
              ve &&
              !ve._visited &&
              (!P ||
                (P[(he = ve._winding || {}).winding] &&
                  !(P.unite && he.winding === 2 && he.windingL && he.windingR)))
            );
          }
          function T(ve) {
            if (ve) {
              for (var he = 0, ge = I.length; he < ge; he++) if (ve === I[he]) return !0;
            }
            return !1;
          }
          function W(ve) {
            for (var he = ve._segments, ge = 0, Me = he.length; ge < Me; ge++) he[ge]._visited = !0;
          }
          function N(ve, he) {
            var ge = ve._intersection,
              Me = ge,
              ot = [];
            he && (I = [ve]);
            function St(ye, xt) {
              for (; ye && ye !== xt;) {
                var at = ye._segment,
                  vt = at && at._path;
                if (vt) {
                  var Ht = at.getNext() || vt.getFirstSegment(),
                    mr = Ht._intersection;
                  (at !== ve &&
                    (T(at) || T(Ht) || (Ht && C(at) && (C(Ht) || (mr && C(mr._segment))))) &&
                    ot.push(at),
                    he && I.push(at));
                }
                ye = ye._next;
              }
            }
            if (ge) {
              for (St(ge); ge && ge._prev;) ge = ge._prev;
              St(ge, Me);
            }
            return ot;
          }
          _.sort(function (ve, he) {
            var ge = ve._intersection,
              Me = he._intersection,
              ot = !!(ge && ge._overlap),
              St = !!(Me && Me._overlap),
              ye = ve._path,
              xt = he._path;
            return ot ^ St
              ? ot
                ? 1
                : -1
              : !ge ^ !Me
                ? ge
                  ? 1
                  : -1
                : ye !== xt
                  ? ye._id - xt._id
                  : ve._index - he._index;
          });
          for (var B = 0, V = _.length; B < V; B++) {
            var L = _[B],
              M = C(L),
              z = null,
              G = !1,
              O = !0,
              E = [],
              H,
              U,
              Y;
            if (M && L._path._overlapsOnly) {
              var K = L._path,
                te = L._intersection._segment._path;
              K.compare(te) && (K.getArea() && D.push(K.clone(!1)), W(K), W(te), (M = !1));
            }
            for (; M;) {
              var se = !z,
                ue = N(L, se),
                me = ue.shift(),
                G = !se && (T(L) || T(me)),
                Ce = !G && me;
              if ((se && ((z = new _e(ne.NO_INSERT)), (H = null)), G)) {
                ((L.isFirst() || L.isLast()) && (O = L._path._closed), (L._visited = !0));
                break;
              }
              if (
                (Ce && H && (E.push(H), (H = null)),
                H ||
                  (Ce && ue.push(L),
                  (H = {
                    start: z._segments.length,
                    crossings: ue,
                    visited: (U = []),
                    handleIn: Y,
                  })),
                Ce && (L = me),
                !C(L))
              ) {
                z.removeSegments(H.start);
                for (var ce = 0, xe = U.length; ce < xe; ce++) U[ce]._visited = !1;
                U.length = 0;
                do
                  ((L = H && H.crossings.shift()),
                    (!L || !L._path) &&
                      ((L = null), (H = E.pop()), H && ((U = H.visited), (Y = H.handleIn))));
                while (H && !C(L));
                if (!L) break;
              }
              var ke = L.getNext();
              (z.add(new ae(L._point, Y, ke && L._handleOut)),
                (L._visited = !0),
                U.push(L),
                (L = ke || L._path.getFirstSegment()),
                (Y = ke && ke._handleIn));
            }
            G &&
              (O && (z.getFirstSegment().setHandleIn(Y), z.setClosed(O)),
              z.getArea() !== 0 && D.push(z));
          }
          return D;
        }
        return {
          _getWinding: function (_, P, D) {
            return S(_, this.getCurves(), P, D);
          },
          unite: function (_, P) {
            return h(this, _, "unite", P);
          },
          intersect: function (_, P) {
            return h(this, _, "intersect", P);
          },
          subtract: function (_, P) {
            return h(this, _, "subtract", P);
          },
          exclude: function (_, P) {
            return h(this, _, "exclude", P);
          },
          divide: function (_, P) {
            return P && (P.trace == !1 || P.stroke)
              ? m(this, _, "divide")
              : c([this.subtract(_, P), this.intersect(_, P)], !0, this, _, P);
          },
          resolveCrossings: function () {
            var _ = this._children,
              P = _ || [this];
            function D(H, U) {
              var Y = H && H._intersection;
              return Y && Y._overlap && Y._path === U;
            }
            var I = !1,
              C = !1,
              T = this.getIntersections(null, function (H) {
                return (H.hasOverlap() && (I = !0)) || (H.isCrossing() && (C = !0));
              }),
              W = I && C && [];
            if (((T = zt.expand(T)), I))
              for (
                var N = b(
                    T,
                    function (H) {
                      return H.hasOverlap();
                    },
                    W
                  ),
                  B = N.length - 1;
                B >= 0;
                B--
              ) {
                var V = N[B],
                  L = V._path,
                  M = V._segment,
                  z = M.getPrevious(),
                  G = M.getNext();
                D(z, L) &&
                  D(G, L) &&
                  (M.remove(),
                  z._handleOut._set(0, 0),
                  G._handleIn._set(0, 0),
                  z !== M &&
                    !z.getCurve().hasLength() &&
                    (G._handleIn.set(z._handleIn), z.remove()));
              }
            C &&
              (b(
                T,
                I &&
                  function (H) {
                    var U = H.getCurve(),
                      Y = H.getSegment(),
                      K = H._intersection,
                      te = K._curve,
                      se = K._segment;
                    if (U && te && U._path && te._path) return !0;
                    (Y && (Y._intersection = null), se && (se._intersection = null));
                  },
                W
              ),
              W && y(W),
              (P = w(
                s.each(
                  P,
                  function (H) {
                    this.push.apply(this, H._segments);
                  },
                  []
                )
              )));
            var O = P.length,
              E;
            return (
              O > 1 && _
                ? (P !== _ && this.setChildren(P), (E = this))
                : O === 1 &&
                  !_ &&
                  (P[0] !== this && this.setSegments(P[0].removeSegments()), (E = this)),
              E ||
                ((E = new Ot(ne.NO_INSERT)),
                E.addChildren(P),
                (E = E.reduce()),
                E.copyAttributes(this),
                this.replaceWith(E)),
              E
            );
          },
          reorient: function (_, P) {
            var D = this._children;
            return (
              D && D.length
                ? this.setChildren(
                    x(
                      this.removeChildren(),
                      function (I) {
                        return !!(_ ? I : I & 1);
                      },
                      P
                    )
                  )
                : P !== t && this.setClockwise(P),
              this
            );
          },
          getInteriorPoint: function () {
            var _ = this.getBounds(),
              P = _.getCenter(!0);
            if (!this.contains(P)) {
              for (
                var D = this.getCurves(), I = P.y, C = [], T = [], W = 0, N = D.length;
                W < N;
                W++
              ) {
                var B = D[W].getValues(),
                  V = B[1],
                  L = B[3],
                  M = B[5],
                  z = B[7];
                if (I >= e(V, L, M, z) && I <= r(V, L, M, z))
                  for (var G = j.getMonoCurves(B), O = 0, E = G.length; O < E; O++) {
                    var H = G[O],
                      U = H[1],
                      Y = H[7];
                    if (U !== Y && ((I >= U && I <= Y) || (I >= Y && I <= U))) {
                      var K =
                        I === U
                          ? H[0]
                          : I === Y
                            ? H[6]
                            : j.solveCubic(H, 1, I, T, 0, 1) === 1
                              ? j.getPoint(H, T[0]).x
                              : (H[0] + H[6]) / 2;
                      C.push(K);
                    }
                  }
              }
              C.length > 1 &&
                (C.sort(function (te, se) {
                  return te - se;
                }),
                (P.x = (C[0] + C[1]) / 2));
            }
            return P;
          },
        };
      })()
    );
    var Gl = s.extend(
        {
          _class: "PathFlattener",
          initialize: function (e, r, o, l, d) {
            var c = [],
              h = [],
              m = 0,
              p = 1 / (o || 32),
              y = e._segments,
              x = y[0],
              b;
            function S(P, D) {
              var I = j.getValues(P, D, d);
              (c.push(I), v(I, P._index, 0, 1));
            }
            function v(P, D, I, C) {
              if (C - I > p && !(l && j.isStraight(P)) && !j.isFlatEnough(P, r || 0.25)) {
                var T = j.subdivide(P, 0.5),
                  W = (I + C) / 2;
                (v(T[0], D, I, W), v(T[1], D, W, C));
              } else {
                var N = P[6] - P[0],
                  B = P[7] - P[1],
                  V = Math.sqrt(N * N + B * B);
                V > 0 && ((m += V), h.push({ offset: m, curve: P, index: D, time: C }));
              }
            }
            for (var w = 1, _ = y.length; w < _; w++) ((b = y[w]), S(x, b), (x = b));
            (e._closed && S(b, y[0]),
              (this.curves = c),
              (this.parts = h),
              (this.length = m),
              (this.index = 0));
          },
          _get: function (e) {
            for (
              var r = this.parts, o = r.length, l, d, c = this.index;
              (d = c), !(!c || r[--c].offset < e);
            );
            for (; d < o; d++) {
              var h = r[d];
              if (h.offset >= e) {
                this.index = d;
                var m = r[d - 1],
                  p = m && m.index === h.index ? m.time : 0,
                  y = m ? m.offset : 0;
                return { index: h.index, time: p + ((h.time - p) * (e - y)) / (h.offset - y) };
              }
            }
            return { index: r[o - 1].index, time: 1 };
          },
          drawPart: function (e, r, o) {
            for (var l = this._get(r), d = this._get(o), c = l.index, h = d.index; c <= h; c++) {
              var m = j.getPart(
                this.curves[c],
                c === l.index ? l.time : 0,
                c === d.index ? d.time : 1
              );
              (c === l.index && e.moveTo(m[0], m[1]), e.bezierCurveTo.apply(e, m.slice(2)));
            }
          },
        },
        s.each(
          j._evaluateMethods,
          function (e) {
            this[e + "At"] = function (r) {
              var o = this._get(r);
              return j[e](this.curves[o.index], o.time);
            };
          },
          {}
        )
      ),
      Bg = s.extend({
        initialize: function (e) {
          for (
            var r = (this.points = []), o = e._segments, l = e._closed, d = 0, c, h = o.length;
            d < h;
            d++
          ) {
            var m = o[d].point;
            (!c || !c.equals(m)) && r.push((c = m.clone()));
          }
          (l && (r.unshift(r[r.length - 1]), r.push(r[1])), (this.closed = l));
        },
        fit: function (e) {
          var r = this.points,
            o = r.length,
            l = null;
          return (
            o > 0 &&
              ((l = [new ae(r[0])]),
              o > 1 &&
                (this.fitCubic(l, e, 0, o - 1, r[1].subtract(r[0]), r[o - 2].subtract(r[o - 1])),
                this.closed && (l.shift(), l.pop()))),
            l
          );
        },
        fitCubic: function (e, r, o, l, d, c) {
          var h = this.points;
          if (l - o === 1) {
            var m = h[o],
              p = h[l],
              y = m.getDistance(p) / 3;
            this.addCurve(e, [m, m.add(d.normalize(y)), p.add(c.normalize(y)), p]);
            return;
          }
          for (
            var x = this.chordLengthParameterize(o, l), b = Math.max(r, r * r), S, v = !0, w = 0;
            w <= 4;
            w++
          ) {
            var _ = this.generateBezier(o, l, x, d, c),
              P = this.findMaxError(o, l, _, x);
            if (P.error < r && v) {
              this.addCurve(e, _);
              return;
            }
            if (((S = P.index), P.error >= b)) break;
            ((v = this.reparameterize(o, l, x, _)), (b = P.error));
          }
          var D = h[S - 1].subtract(h[S + 1]);
          (this.fitCubic(e, r, o, S, d, D), this.fitCubic(e, r, S, l, D.negate(), c));
        },
        addCurve: function (e, r) {
          var o = e[e.length - 1];
          (o.setHandleOut(r[1].subtract(r[0])), e.push(new ae(r[3], r[2].subtract(r[3]))));
        },
        generateBezier: function (e, r, o, l, d) {
          for (
            var c = 1e-12,
              h = Math.abs,
              m = this.points,
              p = m[e],
              y = m[r],
              x = [
                [0, 0],
                [0, 0],
              ],
              b = [0, 0],
              S = 0,
              v = r - e + 1;
            S < v;
            S++
          ) {
            var w = o[S],
              _ = 1 - w,
              P = 3 * w * _,
              D = _ * _ * _,
              I = P * _,
              C = P * w,
              T = w * w * w,
              W = l.normalize(I),
              N = d.normalize(C),
              B = m[e + S].subtract(p.multiply(D + I)).subtract(y.multiply(C + T));
            ((x[0][0] += W.dot(W)),
              (x[0][1] += W.dot(N)),
              (x[1][0] = x[0][1]),
              (x[1][1] += N.dot(N)),
              (b[0] += W.dot(B)),
              (b[1] += N.dot(B)));
          }
          var V = x[0][0] * x[1][1] - x[1][0] * x[0][1],
            L,
            M;
          if (h(V) > c) {
            var z = x[0][0] * b[1] - x[1][0] * b[0],
              G = b[0] * x[1][1] - b[1] * x[0][1];
            ((L = G / V), (M = z / V));
          } else {
            var O = x[0][0] + x[0][1],
              E = x[1][0] + x[1][1];
            L = M = h(O) > c ? b[0] / O : h(E) > c ? b[1] / E : 0;
          }
          var H = y.getDistance(p),
            U = c * H,
            Y,
            K;
          if (L < U || M < U) L = M = H / 3;
          else {
            var te = y.subtract(p);
            ((Y = l.normalize(L)),
              (K = d.normalize(M)),
              Y.dot(te) - K.dot(te) > H * H && ((L = M = H / 3), (Y = K = null)));
          }
          return [p, p.add(Y || l.normalize(L)), y.add(K || d.normalize(M)), y];
        },
        reparameterize: function (e, r, o, l) {
          for (var d = e; d <= r; d++) o[d - e] = this.findRoot(l, this.points[d], o[d - e]);
          for (var d = 1, c = o.length; d < c; d++) if (o[d] <= o[d - 1]) return !1;
          return !0;
        },
        findRoot: function (e, r, o) {
          for (var l = [], d = [], c = 0; c <= 2; c++) l[c] = e[c + 1].subtract(e[c]).multiply(3);
          for (var c = 0; c <= 1; c++) d[c] = l[c + 1].subtract(l[c]).multiply(2);
          var h = this.evaluate(3, e, o),
            m = this.evaluate(2, l, o),
            p = this.evaluate(1, d, o),
            y = h.subtract(r),
            x = m.dot(m) + y.dot(p);
          return F.isZero(x) ? o : o - y.dot(m) / x;
        },
        evaluate: function (e, r, o) {
          for (var l = r.slice(), d = 1; d <= e; d++)
            for (var c = 0; c <= e - d; c++) l[c] = l[c].multiply(1 - o).add(l[c + 1].multiply(o));
          return l[0];
        },
        chordLengthParameterize: function (e, r) {
          for (var o = [0], l = e + 1; l <= r; l++)
            o[l - e] = o[l - e - 1] + this.points[l].getDistance(this.points[l - 1]);
          for (var l = 1, d = r - e; l <= d; l++) o[l] /= o[d];
          return o;
        },
        findMaxError: function (e, r, o, l) {
          for (var d = Math.floor((r - e + 1) / 2), c = 0, h = e + 1; h < r; h++) {
            var m = this.evaluate(3, o, l[h - e]),
              p = m.subtract(this.points[h]),
              y = p.x * p.x + p.y * p.y;
            y >= c && ((c = y), (d = h));
          }
          return { error: c, index: d };
        },
      }),
      eo = ne.extend({
        _class: "TextItem",
        _applyMatrix: !1,
        _canApplyMatrix: !1,
        _serializeFields: { content: null },
        _boundsOptions: { stroke: !1, handle: !1 },
        initialize: function (r) {
          ((this._content = ""), (this._lines = []));
          var o = r && s.isPlainObject(r) && r.x === t && r.y === t;
          this._initialize(o && r, !o && R.read(arguments));
        },
        _equals: function (e) {
          return this._content === e._content;
        },
        copyContent: function (e) {
          this.setContent(e._content);
        },
        getContent: function () {
          return this._content;
        },
        setContent: function (e) {
          ((this._content = "" + e),
            (this._lines = this._content.split(/\r\n|\n|\r/gm)),
            this._changed(265));
        },
        isEmpty: function () {
          return !this._content;
        },
        getCharacterStyle: "#getStyle",
        setCharacterStyle: "#setStyle",
        getParagraphStyle: "#getStyle",
        setParagraphStyle: "#setStyle",
      }),
      Vg = eo.extend({
        _class: "PointText",
        initialize: function () {
          eo.apply(this, arguments);
        },
        getPoint: function () {
          var e = this._matrix.getTranslation();
          return new $(e.x, e.y, this, "setPoint");
        },
        setPoint: function () {
          var e = R.read(arguments);
          this.translate(e.subtract(this._matrix.getTranslation()));
        },
        _draw: function (e, r, o) {
          if (this._content) {
            this._setStyles(e, r, o);
            var l = this._lines,
              d = this._style,
              c = d.hasFill(),
              h = d.hasStroke(),
              m = d.getLeading(),
              p = e.shadowColor;
            ((e.font = d.getFontStyle()), (e.textAlign = d.getJustification()));
            for (var y = 0, x = l.length; y < x; y++) {
              e.shadowColor = p;
              var b = l[y];
              (c && (e.fillText(b, 0, 0), (e.shadowColor = "rgba(0,0,0,0)")),
                h && e.strokeText(b, 0, 0),
                e.translate(0, m));
            }
          }
        },
        _getBounds: function (e, r) {
          var o = this._style,
            l = this._lines,
            d = l.length,
            c = o.getJustification(),
            h = o.getLeading(),
            m = this.getView().getTextWidth(o.getFontStyle(), l),
            p = 0;
          c !== "left" && (p -= m / (c === "center" ? 2 : 1));
          var y = new X(p, d ? -0.75 * h : 0, m, d * h);
          return e ? e._transformBounds(y, y) : y;
        },
      }),
      kt = s.extend(
        new (function () {
          var e = {
              gray: ["gray"],
              rgb: ["red", "green", "blue"],
              hsb: ["hue", "saturation", "brightness"],
              hsl: ["hue", "saturation", "lightness"],
              gradient: ["gradient", "origin", "destination", "highlight"],
            },
            r = {},
            o = {},
            l;
          function d(m) {
            var p = m.match(/^#(\w{1,2})(\w{1,2})(\w{1,2})$/),
              y;
            if (p) {
              y = [0, 0, 0];
              for (var x = 0; x < 3; x++) {
                var b = p[x + 1];
                y[x] = parseInt(b.length == 1 ? b + b : b, 16) / 255;
              }
            } else if ((p = m.match(/^rgba?\((.*)\)$/))) {
              y = p[1].split(",");
              for (var x = 0, S = y.length; x < S; x++) {
                var b = +y[x];
                y[x] = x < 3 ? b / 255 : b;
              }
            } else if (n) {
              var v = o[m];
              if (!v) {
                (l || ((l = je.getContext(1, 1)), (l.globalCompositeOperation = "copy")),
                  (l.fillStyle = "black"),
                  l.clearRect(0, 0, 1, 1),
                  (l.fillStyle = m),
                  l.fillRect(0, 0, 1, 1));
                var w = l.getImageData(0, 0, 1, 1).data;
                ((v = o[m] = [w[0] / 255, w[1] / 255, w[2] / 255]),
                  w[3] < 255 && v.push(w[3] / 255));
              }
              y = v.slice();
            } else y = [0, 0, 0];
            return y;
          }
          var c = [
              [0, 3, 1],
              [2, 0, 1],
              [1, 0, 3],
              [1, 2, 0],
              [3, 1, 0],
              [0, 1, 2],
            ],
            h = {
              "rgb-hsb": function (m, p, y) {
                var x = Math.max(m, p, y),
                  b = Math.min(m, p, y),
                  S = x - b,
                  v =
                    S === 0
                      ? 0
                      : (x == m
                          ? (p - y) / S + (p < y ? 6 : 0)
                          : x == p
                            ? (y - m) / S + 2
                            : (m - p) / S + 4) * 60;
                return [v, x === 0 ? 0 : S / x, x];
              },
              "hsb-rgb": function (m, p, y) {
                m = (((m / 60) % 6) + 6) % 6;
                var b = Math.floor(m),
                  x = m - b,
                  b = c[b],
                  S = [y, y * (1 - p), y * (1 - p * x), y * (1 - p * (1 - x))];
                return [S[b[0]], S[b[1]], S[b[2]]];
              },
              "rgb-hsl": function (m, p, y) {
                var x = Math.max(m, p, y),
                  b = Math.min(m, p, y),
                  S = x - b,
                  v = S === 0,
                  w = v
                    ? 0
                    : (x == m
                        ? (p - y) / S + (p < y ? 6 : 0)
                        : x == p
                          ? (y - m) / S + 2
                          : (m - p) / S + 4) * 60,
                  _ = (x + b) / 2,
                  P = v ? 0 : _ < 0.5 ? S / (x + b) : S / (2 - x - b);
                return [w, P, _];
              },
              "hsl-rgb": function (m, p, y) {
                if (((m = (((m / 360) % 1) + 1) % 1), p === 0)) return [y, y, y];
                for (
                  var x = [m + 1 / 3, m, m - 1 / 3],
                    b = y < 0.5 ? y * (1 + p) : y + p - y * p,
                    S = 2 * y - b,
                    v = [],
                    w = 0;
                  w < 3;
                  w++
                ) {
                  var _ = x[w];
                  (_ < 0 && (_ += 1),
                    _ > 1 && (_ -= 1),
                    (v[w] =
                      6 * _ < 1
                        ? S + (b - S) * 6 * _
                        : 2 * _ < 1
                          ? b
                          : 3 * _ < 2
                            ? S + (b - S) * (2 / 3 - _) * 6
                            : S));
                }
                return v;
              },
              "rgb-gray": function (m, p, y) {
                return [m * 0.2989 + p * 0.587 + y * 0.114];
              },
              "gray-rgb": function (m) {
                return [m, m, m];
              },
              "gray-hsb": function (m) {
                return [0, 0, m];
              },
              "gray-hsl": function (m) {
                return [0, 0, m];
              },
              "gradient-rgb": function () {
                return [];
              },
              "rgb-gradient": function () {
                return [];
              },
            };
          return s.each(
            e,
            function (m, p) {
              ((r[p] = []),
                s.each(
                  m,
                  function (y, x) {
                    var b = s.capitalize(y),
                      S = /^(hue|saturation)$/.test(y),
                      v = (r[p][x] =
                        y === "gradient"
                          ? function (w) {
                              var _ = this._components[0];
                              return (
                                (w = to.read(Array.isArray(w) ? w : arguments, 0, {
                                  readNull: !0,
                                })),
                                _ !== w && (_ && _._removeOwner(this), w && w._addOwner(this)),
                                w
                              );
                            }
                          : p === "gradient"
                            ? function () {
                                return R.read(arguments, 0, {
                                  readNull: y === "highlight",
                                  clone: !0,
                                });
                              }
                            : function (w) {
                                return w == null || isNaN(w) ? 0 : w;
                              });
                    ((this["get" + b] = function () {
                      return this._type === p || (S && /^hs[bl]$/.test(this._type))
                        ? this._components[x]
                        : this._convert(p)[x];
                    }),
                      (this["set" + b] = function (w) {
                        (this._type !== p &&
                          !(S && /^hs[bl]$/.test(this._type)) &&
                          ((this._components = this._convert(p)),
                          (this._properties = e[p]),
                          (this._type = p)),
                          (this._components[x] = v.call(this, w)),
                          this._changed());
                      }));
                  },
                  this
                ));
            },
            {
              _class: "Color",
              _readIndex: !0,
              initialize: function m(p) {
                var y = arguments,
                  x = this.__read,
                  b = 0,
                  S,
                  v,
                  w,
                  _;
                Array.isArray(p) && ((y = p), (p = y[0]));
                var P = p != null && typeof p;
                if (
                  (P === "string" &&
                    p in e &&
                    ((S = p),
                    (p = y[1]),
                    Array.isArray(p)
                      ? ((v = p), (w = y[2]))
                      : (x && (b = 1), (y = s.slice(y, 1)), (P = typeof p))),
                  !v)
                ) {
                  if (
                    ((_ = P === "number" ? y : P === "object" && p.length != null ? p : null), _)
                  ) {
                    S || (S = _.length >= 3 ? "rgb" : "gray");
                    var D = e[S].length;
                    ((w = _[D]),
                      x && (b += _ === arguments ? D + (w != null ? 1 : 0) : 1),
                      _.length > D && (_ = s.slice(_, 0, D)));
                  } else if (P === "string")
                    ((S = "rgb"), (v = d(p)), v.length === 4 && ((w = v[3]), v.length--));
                  else if (P === "object")
                    if (p.constructor === m) {
                      if (
                        ((S = p._type),
                        (v = p._components.slice()),
                        (w = p._alpha),
                        S === "gradient")
                      )
                        for (var I = 1, C = v.length; I < C; I++) {
                          var T = v[I];
                          T && (v[I] = T.clone());
                        }
                    } else if (p.constructor === to) ((S = "gradient"), (_ = y));
                    else {
                      S =
                        "hue" in p
                          ? "lightness" in p
                            ? "hsl"
                            : "hsb"
                          : "gradient" in p || "stops" in p || "radial" in p
                            ? "gradient"
                            : "gray" in p
                              ? "gray"
                              : "rgb";
                      var W = e[S],
                        N = r[S];
                      this._components = v = [];
                      for (var I = 0, C = W.length; I < C; I++) {
                        var B = p[W[I]];
                        (B == null &&
                          !I &&
                          S === "gradient" &&
                          "stops" in p &&
                          (B = { stops: p.stops, radial: p.radial }),
                          (B = N[I].call(this, B)),
                          B != null && (v[I] = B));
                      }
                      w = p.alpha;
                    }
                  x && S && (b = 1);
                }
                if (((this._type = S || "rgb"), !v)) {
                  this._components = v = [];
                  for (var N = r[this._type], I = 0, C = N.length; I < C; I++) {
                    var B = N[I].call(this, _ && _[I]);
                    B != null && (v[I] = B);
                  }
                }
                return (
                  (this._components = v),
                  (this._properties = e[this._type]),
                  (this._alpha = w),
                  x && (this.__read = b),
                  this
                );
              },
              set: "#initialize",
              _serialize: function (m, p) {
                var y = this.getComponents();
                return s.serialize(
                  /^(gray|rgb)$/.test(this._type) ? y : [this._type].concat(y),
                  m,
                  !0,
                  p
                );
              },
              _changed: function () {
                ((this._canvasStyle = null), this._owner && this._owner._changed(65));
              },
              _convert: function (m) {
                var p;
                return this._type === m
                  ? this._components.slice()
                  : (p = h[this._type + "-" + m])
                    ? p.apply(this, this._components)
                    : h["rgb-" + m].apply(
                        this,
                        h[this._type + "-rgb"].apply(this, this._components)
                      );
              },
              convert: function (m) {
                return new kt(m, this._convert(m), this._alpha);
              },
              getType: function () {
                return this._type;
              },
              setType: function (m) {
                ((this._components = this._convert(m)),
                  (this._properties = e[m]),
                  (this._type = m));
              },
              getComponents: function () {
                var m = this._components.slice();
                return (this._alpha != null && m.push(this._alpha), m);
              },
              getAlpha: function () {
                return this._alpha != null ? this._alpha : 1;
              },
              setAlpha: function (m) {
                ((this._alpha = m == null ? null : Math.min(Math.max(m, 0), 1)), this._changed());
              },
              hasAlpha: function () {
                return this._alpha != null;
              },
              equals: function (m) {
                var p = s.isPlainValue(m, !0) ? kt.read(arguments) : m;
                return (
                  p === this ||
                  (p &&
                    this._class === p._class &&
                    this._type === p._type &&
                    this.getAlpha() === p.getAlpha() &&
                    s.equals(this._components, p._components)) ||
                  !1
                );
              },
              toString: function () {
                for (
                  var m = this._properties,
                    p = [],
                    y = this._type === "gradient",
                    x = k.instance,
                    b = 0,
                    S = m.length;
                  b < S;
                  b++
                ) {
                  var v = this._components[b];
                  v != null && p.push(m[b] + ": " + (y ? v : x.number(v)));
                }
                return (
                  this._alpha != null && p.push("alpha: " + x.number(this._alpha)),
                  "{ " + p.join(", ") + " }"
                );
              },
              toCSS: function (m) {
                var p = this._convert("rgb"),
                  y = m || this._alpha == null ? 1 : this._alpha;
                function x(b) {
                  return Math.round((b < 0 ? 0 : b > 1 ? 1 : b) * 255);
                }
                return (
                  (p = [x(p[0]), x(p[1]), x(p[2])]),
                  y < 1 && p.push(y < 0 ? 0 : y),
                  m
                    ? "#" + ((1 << 24) + (p[0] << 16) + (p[1] << 8) + p[2]).toString(16).slice(1)
                    : (p.length == 4 ? "rgba(" : "rgb(") + p.join(",") + ")"
                );
              },
              toCanvasStyle: function (m, p) {
                if (this._canvasStyle) return this._canvasStyle;
                if (this._type !== "gradient") return (this._canvasStyle = this.toCSS());
                var y = this._components,
                  x = y[0],
                  b = x._stops,
                  S = y[1],
                  v = y[2],
                  w = y[3],
                  _ = p && p.inverted(),
                  P;
                if (
                  (_ &&
                    ((S = _._transformPoint(S)),
                    (v = _._transformPoint(v)),
                    w && (w = _._transformPoint(w))),
                  x._radial)
                ) {
                  var D = v.getDistance(S);
                  if (w) {
                    var I = w.subtract(S);
                    I.getLength() > D && (w = S.add(I.normalize(D - 0.1)));
                  }
                  var C = w || S;
                  P = m.createRadialGradient(C.x, C.y, 0, S.x, S.y, D);
                } else P = m.createLinearGradient(S.x, S.y, v.x, v.y);
                for (var T = 0, W = b.length; T < W; T++) {
                  var N = b[T],
                    B = N._offset;
                  P.addColorStop(B ?? T / (W - 1), N._color.toCanvasStyle());
                }
                return (this._canvasStyle = P);
              },
              transform: function (m) {
                if (this._type === "gradient") {
                  for (var p = this._components, y = 1, x = p.length; y < x; y++) {
                    var b = p[y];
                    m._transformPoint(b, b, !0);
                  }
                  this._changed();
                }
              },
              statics: {
                _types: e,
                random: function () {
                  var m = Math.random;
                  return new kt(m(), m(), m());
                },
              },
            }
          );
        })(),
        new (function () {
          var e = {
            add: function (r, o) {
              return r + o;
            },
            subtract: function (r, o) {
              return r - o;
            },
            multiply: function (r, o) {
              return r * o;
            },
            divide: function (r, o) {
              return r / o;
            },
          };
          return s.each(
            e,
            function (r, o) {
              this[o] = function (l) {
                l = kt.read(arguments);
                for (
                  var d = this._type, c = this._components, h = l._convert(d), m = 0, p = c.length;
                  m < p;
                  m++
                )
                  h[m] = r(c[m], h[m]);
                return new kt(d, h, this._alpha != null ? r(this._alpha, l.getAlpha()) : null);
              };
            },
            {}
          );
        })()
      ),
      to = s.extend({
        _class: "Gradient",
        initialize: function (r, o) {
          ((this._id = A.get()),
            r && s.isPlainObject(r) && (this.set(r), (r = o = null)),
            this._stops == null && this.setStops(r || ["white", "black"]),
            this._radial == null &&
              this.setRadial((typeof o == "string" && o === "radial") || o || !1));
        },
        _serialize: function (e, r) {
          return r.add(this, function () {
            return s.serialize([this._stops, this._radial], e, !0, r);
          });
        },
        _changed: function () {
          for (var e = 0, r = this._owners && this._owners.length; e < r; e++)
            this._owners[e]._changed();
        },
        _addOwner: function (e) {
          (this._owners || (this._owners = []), this._owners.push(e));
        },
        _removeOwner: function (e) {
          var r = this._owners ? this._owners.indexOf(e) : -1;
          r != -1 && (this._owners.splice(r, 1), this._owners.length || (this._owners = t));
        },
        clone: function () {
          for (var e = [], r = 0, o = this._stops.length; r < o; r++) e[r] = this._stops[r].clone();
          return new to(e, this._radial);
        },
        getStops: function () {
          return this._stops;
        },
        setStops: function (e) {
          if (e.length < 2)
            throw new Error("Gradient stop list needs to contain at least two stops.");
          var r = this._stops;
          if (r) for (var o = 0, l = r.length; o < l; o++) r[o]._owner = t;
          r = this._stops = bs.readList(e, 0, { clone: !0 });
          for (var o = 0, l = r.length; o < l; o++) r[o]._owner = this;
          this._changed();
        },
        getRadial: function () {
          return this._radial;
        },
        setRadial: function (e) {
          ((this._radial = e), this._changed());
        },
        equals: function (e) {
          if (e === this) return !0;
          if (e && this._class === e._class) {
            var r = this._stops,
              o = e._stops,
              l = r.length;
            if (l === o.length) {
              for (var d = 0; d < l; d++) if (!r[d].equals(o[d])) return !1;
              return !0;
            }
          }
          return !1;
        },
      }),
      bs = s.extend({
        _class: "GradientStop",
        initialize: function (r, o) {
          var l = r,
            d = o;
          (typeof r == "object" &&
            o === t &&
            (Array.isArray(r) && typeof r[0] != "number"
              ? ((l = r[0]), (d = r[1]))
              : ("color" in r || "offset" in r || "rampPoint" in r) &&
                ((l = r.color), (d = r.offset || r.rampPoint || 0))),
            this.setColor(l),
            this.setOffset(d));
        },
        clone: function () {
          return new bs(this._color.clone(), this._offset);
        },
        _serialize: function (e, r) {
          var o = this._color,
            l = this._offset;
          return s.serialize(l == null ? [o] : [o, l], e, !0, r);
        },
        _changed: function () {
          this._owner && this._owner._changed(65);
        },
        getOffset: function () {
          return this._offset;
        },
        setOffset: function (e) {
          ((this._offset = e), this._changed());
        },
        getRampPoint: "#getOffset",
        setRampPoint: "#setOffset",
        getColor: function () {
          return this._color;
        },
        setColor: function () {
          var e = kt.read(arguments, 0, { clone: !0 });
          (e && (e._owner = this), (this._color = e), this._changed());
        },
        equals: function (e) {
          return (
            e === this ||
            (e &&
              this._class === e._class &&
              this._color.equals(e._color) &&
              this._offset == e._offset) ||
            !1
          );
        },
      }),
      Cs = s.extend(
        new (function () {
          var e = {
              fillColor: null,
              fillRule: "nonzero",
              strokeColor: null,
              strokeWidth: 1,
              strokeCap: "butt",
              strokeJoin: "miter",
              strokeScaling: !0,
              miterLimit: 10,
              dashOffset: 0,
              dashArray: [],
              shadowColor: null,
              shadowBlur: 0,
              shadowOffset: new R(),
              selectedColor: null,
            },
            r = s.set({}, e, {
              fontFamily: "sans-serif",
              fontWeight: "normal",
              fontSize: 12,
              leading: null,
              justification: "left",
            }),
            o = s.set({}, r, { fillColor: new kt() }),
            l = {
              strokeWidth: 97,
              strokeCap: 97,
              strokeJoin: 97,
              strokeScaling: 105,
              miterLimit: 97,
              fontFamily: 9,
              fontWeight: 9,
              fontSize: 9,
              font: 9,
              leading: 9,
              justification: 9,
            },
            d = { beans: !0 },
            c = {
              _class: "Style",
              beans: !0,
              initialize: function (m, p, y) {
                ((this._values = {}),
                  (this._owner = p),
                  (this._project = (p && p._project) || y || Se.project),
                  (this._defaults = !p || p instanceof it ? r : p instanceof eo ? o : e),
                  m && this.set(m));
              },
            };
          return (
            s.each(r, function (h, m) {
              var p = /Color$/.test(m),
                y = m === "shadowOffset",
                x = s.capitalize(m),
                b = l[m],
                S = "set" + x,
                v = "get" + x;
              ((c[S] = function (w) {
                var _ = this._owner,
                  P = _ && _._children;
                if (P && P.length > 0 && !(_ instanceof Ot))
                  for (var D = 0, I = P.length; D < I; D++) P[D]._style[S](w);
                else if (m in this._defaults) {
                  var C = this._values[m];
                  C !== w &&
                    (p &&
                      (C && C._owner !== t && (C._owner = t),
                      w && w.constructor === kt && (w._owner && (w = w.clone()), (w._owner = _))),
                    (this._values[m] = w),
                    _ && _._changed(b || 65));
                }
              }),
                (c[v] = function (w) {
                  var _ = this._owner,
                    P = _ && _._children,
                    D;
                  if (m in this._defaults && (!P || !P.length || w || _ instanceof Ot)) {
                    var D = this._values[m];
                    if (D === t) ((D = this._defaults[m]), D && D.clone && (D = D.clone()));
                    else {
                      var I = p ? kt : y ? R : null;
                      I &&
                        !(D && D.constructor === I) &&
                        ((this._values[m] = D = I.read([D], 0, { readNull: !0, clone: !0 })),
                        D && p && (D._owner = _));
                    }
                  } else if (P)
                    for (var C = 0, T = P.length; C < T; C++) {
                      var W = P[C]._style[v]();
                      if (!C) D = W;
                      else if (!s.equals(D, W)) return t;
                    }
                  return D;
                }),
                (d[v] = function (w) {
                  return this._style[v](w);
                }),
                (d[S] = function (w) {
                  this._style[S](w);
                }));
            }),
            s.each({ Font: "FontFamily", WindingRule: "FillRule" }, function (h, m) {
              var p = "get" + m,
                y = "set" + m;
              ((c[p] = d[p] = "#get" + h), (c[y] = d[y] = "#set" + h));
            }),
            ne.inject(d),
            c
          );
        })(),
        {
          set: function (e) {
            var r = e instanceof Cs,
              o = r ? e._values : e;
            if (o) {
              for (var l in o)
                if (l in this._defaults) {
                  var d = o[l];
                  this[l] = d && r && d.clone ? d.clone() : d;
                }
            }
          },
          equals: function (e) {
            function r(o, l, d) {
              var c = o._values,
                h = l._values,
                m = l._defaults;
              for (var p in c) {
                var y = c[p],
                  x = h[p];
                if (!(d && p in h) && !s.equals(y, x === t ? m[p] : x)) return !1;
              }
              return !0;
            }
            return (
              e === this || (e && this._class === e._class && r(this, e) && r(e, this, !0)) || !1
            );
          },
          hasFill: function () {
            var e = this.getFillColor();
            return !!e && e.alpha > 0;
          },
          hasStroke: function () {
            var e = this.getStrokeColor();
            return !!e && e.alpha > 0 && this.getStrokeWidth() > 0;
          },
          hasShadow: function () {
            var e = this.getShadowColor();
            return (
              !!e && e.alpha > 0 && (this.getShadowBlur() > 0 || !this.getShadowOffset().isZero())
            );
          },
          getView: function () {
            return this._project._view;
          },
          getFontStyle: function () {
            var e = this.getFontSize();
            return (
              this.getFontWeight() +
              " " +
              e +
              (/[a-z]/i.test(e + "") ? " " : "px ") +
              this.getFontFamily()
            );
          },
          getFont: "#getFontFamily",
          setFont: "#setFontFamily",
          getLeading: function e() {
            var r = e.base.call(this),
              o = this.getFontSize();
            return (/pt|em|%|px/.test(o) && (o = this.getView().getPixelSize(o)), r ?? o * 1.2);
          },
        }
      ),
      Ge = new (function () {
        function e(r, o, l, d) {
          for (
            var c = ["", "webkit", "moz", "Moz", "ms", "o"],
              h = o[0].toUpperCase() + o.substring(1),
              m = 0;
            m < 6;
            m++
          ) {
            var p = c[m],
              y = p ? p + h : o;
            if (y in r) {
              if (l) r[y] = d;
              else return r[y];
              break;
            }
          }
        }
        return {
          getStyles: function (r) {
            var o = r && r.nodeType !== 9 ? r.ownerDocument : r,
              l = o && o.defaultView;
            return l && l.getComputedStyle(r, "");
          },
          getBounds: function (r, o) {
            var l = r.ownerDocument,
              d = l.body,
              c = l.documentElement,
              h;
            try {
              h = r.getBoundingClientRect();
            } catch {
              h = { left: 0, top: 0, width: 0, height: 0 };
            }
            var m = h.left - (c.clientLeft || d.clientLeft || 0),
              p = h.top - (c.clientTop || d.clientTop || 0);
            if (!o) {
              var y = l.defaultView;
              ((m += y.pageXOffset || c.scrollLeft || d.scrollLeft),
                (p += y.pageYOffset || c.scrollTop || d.scrollTop));
            }
            return new X(m, p, h.width, h.height);
          },
          getViewportBounds: function (r) {
            var o = r.ownerDocument,
              l = o.defaultView,
              d = o.documentElement;
            return new X(0, 0, l.innerWidth || d.clientWidth, l.innerHeight || d.clientHeight);
          },
          getOffset: function (r, o) {
            return Ge.getBounds(r, o).getPoint();
          },
          getSize: function (r) {
            return Ge.getBounds(r, !0).getSize();
          },
          isInvisible: function (r) {
            return Ge.getSize(r).equals(new q(0, 0));
          },
          isInView: function (r) {
            return !Ge.isInvisible(r) && Ge.getViewportBounds(r).intersects(Ge.getBounds(r, !0));
          },
          isInserted: function (r) {
            return a.body.contains(r);
          },
          getPrefixed: function (r, o) {
            return r && e(r, o);
          },
          setPrefixed: function (r, o, l) {
            if (typeof o == "object") for (var d in o) e(r, d, !0, o[d]);
            else e(r, o, !0, l);
          },
        };
      })(),
      Ye = {
        add: function (e, r) {
          if (e)
            for (var o in r)
              for (var l = r[o], d = o.split(/[\s,]+/g), c = 0, h = d.length; c < h; c++)
                e.addEventListener(d[c], l, !1);
        },
        remove: function (e, r) {
          if (e)
            for (var o in r)
              for (var l = r[o], d = o.split(/[\s,]+/g), c = 0, h = d.length; c < h; c++)
                e.removeEventListener(d[c], l, !1);
        },
        getPoint: function (e) {
          var r = e.targetTouches
            ? e.targetTouches.length
              ? e.targetTouches[0]
              : e.changedTouches[0]
            : e;
          return new R(
            r.pageX || r.clientX + a.documentElement.scrollLeft,
            r.pageY || r.clientY + a.documentElement.scrollTop
          );
        },
        getTarget: function (e) {
          return e.target || e.srcElement;
        },
        getRelatedTarget: function (e) {
          return e.relatedTarget || e.toElement;
        },
        getOffset: function (e, r) {
          return Ye.getPoint(e).subtract(Ge.getOffset(r || Ye.getTarget(e)));
        },
      };
    Ye.requestAnimationFrame = new (function () {
      var e = Ge.getPrefixed(n, "requestAnimationFrame"),
        r = !1,
        o = [],
        l;
      function d() {
        var c = o;
        o = [];
        for (var h = 0, m = c.length; h < m; h++) c[h]();
        ((r = e && o.length), r && e(d));
      }
      return function (c) {
        (o.push(c), e ? r || (e(d), (r = !0)) : l || (l = setInterval(d, 16.666666666666668)));
      };
    })();
    var Le = s.extend(
        u,
        {
          _class: "View",
          initialize: function e(r, o) {
            function l(b) {
              return o[b] || parseInt(o.getAttribute(b), 10);
            }
            function d() {
              var b = Ge.getSize(o);
              return b.isNaN() || b.isZero() ? new q(l("width"), l("height")) : b;
            }
            var c;
            if (n && o) {
              ((this._id = o.getAttribute("id")),
                this._id == null && o.setAttribute("id", (this._id = "view-" + e._id++)),
                Ye.add(o, this._viewEvents));
              var h = "none";
              if (
                (Ge.setPrefixed(o.style, {
                  userDrag: h,
                  userSelect: h,
                  touchCallout: h,
                  contentZooming: h,
                  tapHighlightColor: "rgba(0,0,0,0)",
                }),
                f.hasAttribute(o, "resize"))
              ) {
                var m = this;
                Ye.add(
                  n,
                  (this._windowEvents = {
                    resize: function () {
                      m.setViewSize(d());
                    },
                  })
                );
              }
              if (((c = d()), f.hasAttribute(o, "stats") && typeof Stats < "u")) {
                this._stats = new Stats();
                var p = this._stats.domElement,
                  y = p.style,
                  x = Ge.getOffset(o);
                ((y.position = "absolute"),
                  (y.left = x.x + "px"),
                  (y.top = x.y + "px"),
                  a.body.appendChild(p));
              }
            } else ((c = new q(o)), (o = null));
            ((this._project = r),
              (this._scope = r._scope),
              (this._element = o),
              this._pixelRatio || (this._pixelRatio = (n && n.devicePixelRatio) || 1),
              this._setElementSize(c.width, c.height),
              (this._viewSize = c),
              e._views.push(this),
              (e._viewsById[this._id] = this),
              ((this._matrix = new Q())._owner = this),
              e._focused || (e._focused = this),
              (this._frameItems = {}),
              (this._frameItemCount = 0),
              (this._itemEvents = { native: {}, virtual: {} }),
              (this._autoUpdate = !Se.agent.node),
              (this._needsUpdate = !1));
          },
          remove: function () {
            if (!this._project) return !1;
            (Le._focused === this && (Le._focused = null),
              Le._views.splice(Le._views.indexOf(this), 1),
              delete Le._viewsById[this._id]);
            var e = this._project;
            return (
              e._view === this && (e._view = null),
              Ye.remove(this._element, this._viewEvents),
              Ye.remove(a, this._docEvents),
              Ye.remove(n, this._windowEvents),
              (this._element = this._project = null),
              this.off("frame"),
              (this._animate = !1),
              (this._frameItems = {}),
              !0
            );
          },
          _events: s.each(
            ne._itemHandlers.concat(["onResize", "onKeyDown", "onKeyUp"]),
            function (e) {
              this[e] = {};
            },
            {
              onFrame: {
                install: function () {
                  this.play();
                },
                uninstall: function () {
                  this.pause();
                },
              },
            }
          ),
          _animate: !1,
          _time: 0,
          _count: 0,
          getAutoUpdate: function () {
            return this._autoUpdate;
          },
          setAutoUpdate: function (e) {
            ((this._autoUpdate = e), e && this.requestUpdate());
          },
          update: function () {},
          draw: function () {
            this.update();
          },
          requestUpdate: function () {
            if (!this._requested) {
              var e = this;
              (Ye.requestAnimationFrame(function () {
                if (((e._requested = !1), e._animate)) {
                  e.requestUpdate();
                  var r = e._element;
                  (!Ge.getPrefixed(a, "hidden") || f.getAttribute(r, "keepalive") === "true") &&
                    Ge.isInView(r) &&
                    e._handleFrame();
                }
                e._autoUpdate && e.update();
              }),
                (this._requested = !0));
            }
          },
          play: function () {
            ((this._animate = !0), this.requestUpdate());
          },
          pause: function () {
            this._animate = !1;
          },
          _handleFrame: function () {
            Se = this._scope;
            var e = Date.now() / 1e3,
              r = this._last ? e - this._last : 0;
            ((this._last = e),
              this.emit(
                "frame",
                new s({ delta: r, time: (this._time += r), count: this._count++ })
              ),
              this._stats && this._stats.update());
          },
          _animateItem: function (e, r) {
            var o = this._frameItems;
            r
              ? ((o[e._id] = { item: e, time: 0, count: 0 }),
                ++this._frameItemCount === 1 && this.on("frame", this._handleFrameItems))
              : (delete o[e._id],
                --this._frameItemCount === 0 && this.off("frame", this._handleFrameItems));
          },
          _handleFrameItems: function (e) {
            for (var r in this._frameItems) {
              var o = this._frameItems[r];
              o.item.emit("frame", new s(e, { time: (o.time += e.delta), count: o.count++ }));
            }
          },
          _changed: function () {
            (this._project._changed(2049), (this._bounds = this._decomposed = t));
          },
          getElement: function () {
            return this._element;
          },
          getPixelRatio: function () {
            return this._pixelRatio;
          },
          getResolution: function () {
            return this._pixelRatio * 72;
          },
          getViewSize: function () {
            var e = this._viewSize;
            return new Z(e.width, e.height, this, "setViewSize");
          },
          setViewSize: function () {
            var e = q.read(arguments),
              r = e.subtract(this._viewSize);
            r.isZero() ||
              (this._setElementSize(e.width, e.height),
              this._viewSize.set(e),
              this._changed(),
              this.emit("resize", { size: e, delta: r }),
              this._autoUpdate && this.update());
          },
          _setElementSize: function (e, r) {
            var o = this._element;
            o && (o.width !== e && (o.width = e), o.height !== r && (o.height = r));
          },
          getBounds: function () {
            return (
              this._bounds ||
                (this._bounds = this._matrix
                  .inverted()
                  ._transformBounds(new X(new R(), this._viewSize))),
              this._bounds
            );
          },
          getSize: function () {
            return this.getBounds().getSize();
          },
          isVisible: function () {
            return Ge.isInView(this._element);
          },
          isInserted: function () {
            return Ge.isInserted(this._element);
          },
          getPixelSize: function (e) {
            var r = this._element,
              o;
            if (r) {
              var l = r.parentNode,
                d = a.createElement("div");
              ((d.style.fontSize = e),
                l.appendChild(d),
                (o = parseFloat(Ge.getStyles(d).fontSize)),
                l.removeChild(d));
            } else o = parseFloat(o);
            return o;
          },
          getTextWidth: function (e, r) {
            return 0;
          },
        },
        s.each(
          ["rotate", "scale", "shear", "skew"],
          function (e) {
            var r = e === "rotate";
            this[e] = function () {
              var o = (r ? s : R).read(arguments),
                l = R.read(arguments, 0, { readNull: !0 });
              return this.transform(new Q()[e](o, l || this.getCenter(!0)));
            };
          },
          {
            _decompose: function () {
              return this._decomposed || (this._decomposed = this._matrix.decompose());
            },
            translate: function () {
              var e = new Q();
              return this.transform(e.translate.apply(e, arguments));
            },
            getCenter: function () {
              return this.getBounds().getCenter();
            },
            setCenter: function () {
              var e = R.read(arguments);
              this.translate(this.getCenter().subtract(e));
            },
            getZoom: function () {
              var e = this._decompose(),
                r = e && e.scaling;
              return r ? (r.x + r.y) / 2 : 0;
            },
            setZoom: function (e) {
              this.transform(new Q().scale(e / this.getZoom(), this.getCenter()));
            },
            getRotation: function () {
              var e = this._decompose();
              return e && e.rotation;
            },
            setRotation: function (e) {
              var r = this.getRotation();
              r != null && e != null && this.rotate(e - r);
            },
            getScaling: function () {
              var e = this._decompose(),
                r = e && e.scaling;
              return r ? new $(r.x, r.y, this, "setScaling") : t;
            },
            setScaling: function () {
              var e = this.getScaling(),
                r = R.read(arguments, 0, { clone: !0, readNull: !0 });
              e && r && this.scale(r.x / e.x, r.y / e.y);
            },
            getMatrix: function () {
              return this._matrix;
            },
            setMatrix: function () {
              var e = this._matrix;
              e.initialize.apply(e, arguments);
            },
            transform: function (e) {
              this._matrix.append(e);
            },
            scrollBy: function () {
              this.translate(R.read(arguments).negate());
            },
          }
        ),
        {
          projectToView: function () {
            return this._matrix._transformPoint(R.read(arguments));
          },
          viewToProject: function () {
            return this._matrix._inverseTransform(R.read(arguments));
          },
          getEventPoint: function (e) {
            return this.viewToProject(Ye.getOffset(e, this._element));
          },
        },
        {
          statics: {
            _views: [],
            _viewsById: {},
            _id: 0,
            create: function (e, r) {
              a && typeof r == "string" && (r = a.getElementById(r));
              var o = n ? Lg : Le;
              return new o(e, r);
            },
          },
        },
        new (function () {
          if (!n) return;
          var e,
            r,
            o = !1,
            l = !1;
          function d(O) {
            var E = Ye.getTarget(O);
            return E.getAttribute && Le._viewsById[E.getAttribute("id")];
          }
          function c() {
            var O = Le._focused;
            if (!O || !O.isVisible()) {
              for (var E = 0, H = Le._views.length; E < H; E++)
                if ((O = Le._views[E]).isVisible()) {
                  Le._focused = r = O;
                  break;
                }
            }
          }
          function h(O, E, H) {
            O._handleMouseEvent("mousemove", E, H);
          }
          var m = n.navigator,
            p,
            y,
            x;
          m.pointerEnabled || m.msPointerEnabled
            ? ((p = "pointerdown MSPointerDown"),
              (y = "pointermove MSPointerMove"),
              (x = "pointerup pointercancel MSPointerUp MSPointerCancel"))
            : ((p = "touchstart"),
              (y = "touchmove"),
              (x = "touchend touchcancel"),
              ("ontouchstart" in n &&
                m.userAgent.match(/mobile|tablet|ip(ad|hone|od)|android|silk/i)) ||
                ((p += " mousedown"), (y += " mousemove"), (x += " mouseup")));
          var b = {},
            S = {
              mouseout: function (O) {
                var E = Le._focused,
                  H = Ye.getRelatedTarget(O);
                if (E && (!H || H.nodeName === "HTML")) {
                  var U = Ye.getOffset(O, E._element),
                    Y = U.x,
                    K = Math.abs,
                    te = K(Y),
                    se = 1 << 25,
                    ue = te - se;
                  ((U.x = K(ue) < te ? ue * (Y < 0 ? -1 : 1) : Y), h(E, O, E.viewToProject(U)));
                }
              },
              scroll: c,
            };
          ((b[p] = function (O) {
            var E = (Le._focused = d(O));
            o || ((o = !0), E._handleMouseEvent("mousedown", O));
          }),
            (S[y] = function (O) {
              var E = Le._focused;
              if (!l) {
                var H = d(O);
                H
                  ? E !== H && (E && h(E, O), e || (e = E), (E = Le._focused = r = H))
                  : r &&
                    r === E &&
                    (e && !e.isInserted() && (e = null), (E = Le._focused = e), (e = null), c());
              }
              E && h(E, O);
            }),
            (S[p] = function () {
              l = !0;
            }),
            (S[x] = function (O) {
              var E = Le._focused;
              (E && o && E._handleMouseEvent("mouseup", O), (l = o = !1));
            }),
            Ye.add(a, S),
            Ye.add(n, { load: c }));
          var v = !1,
            w = !1,
            _ = { doubleclick: "click", mousedrag: "mousemove" },
            P = !1,
            D,
            I,
            C,
            T,
            W,
            N,
            B,
            V,
            L;
          function M(O, E, H, U, Y, K, te) {
            var se = !1,
              ue;
            function me(Ce, ce) {
              if (Ce.responds(ce)) {
                if (
                  (ue || (ue = new Eg(ce, U, Y, E || Ce, K ? Y.subtract(K) : null)),
                  Ce.emit(ce, ue) && ((v = !0), ue.prevented && (w = !0), ue.stopped))
                )
                  return (se = !0);
              } else {
                var xe = _[ce];
                if (xe) return me(Ce, xe);
              }
            }
            for (; O && O !== te && !me(O, H);) O = O._parent;
            return se;
          }
          function z(O, E, H, U, Y, K) {
            return (
              O._project.removeOn(H),
              (w = v = !1),
              (N && M(N, null, H, U, Y, K)) ||
                (E && E !== N && !E.isDescendant(N) && M(E, null, H, U, Y, K, N)) ||
                M(O, N || E || O, H, U, Y, K)
            );
          }
          var G = {
            mousedown: { mousedown: 1, mousedrag: 1, click: 1, doubleclick: 1 },
            mouseup: { mouseup: 1, mousedrag: 1, click: 1, doubleclick: 1 },
            mousemove: { mousedrag: 1, mousemove: 1, mouseenter: 1, mouseleave: 1 },
          };
          return {
            _viewEvents: b,
            _docEvents: S,
            _handleMouseEvent: function (O, E, H) {
              var U = this._itemEvents,
                Y = U.native[O],
                K = O === "mousemove",
                te = this._scope.tool,
                se = this;
              function ue(he) {
                return U.virtual[he] || se.responds(he) || (te && te.responds(he));
              }
              (K && o && ue("mousedrag") && (O = "mousedrag"), H || (H = this.getEventPoint(E)));
              var me = this.getBounds().contains(H),
                Ce = Y && me && se._project.hitTest(H, { tolerance: 0, fill: !0, stroke: !0 }),
                ce = (Ce && Ce.item) || null,
                xe = !1,
                ke = {};
              if (
                ((ke[O.substr(5)] = !0),
                Y &&
                  ce !== W &&
                  (W && M(W, null, "mouseleave", E, H),
                  ce && M(ce, null, "mouseenter", E, H),
                  (W = ce)),
                P ^ me &&
                  (M(this, null, me ? "mouseenter" : "mouseleave", E, H),
                  (D = me ? this : null),
                  (xe = !0)),
                (me || ke.drag) &&
                  !H.equals(C) &&
                  (z(this, ce, K ? O : "mousemove", E, H, C), (xe = !0)),
                (P = me),
                (ke.down && me) || (ke.up && I))
              ) {
                if ((z(this, ce, O, E, H, I), ke.down)) {
                  if (((L = ce === B && Date.now() - V < 300), (T = B = ce), !w && ce)) {
                    for (var ve = ce; ve && !ve.responds("mousedrag");) ve = ve._parent;
                    ve && (N = ce);
                  }
                  I = H;
                } else
                  ke.up &&
                    (!w &&
                      ce === T &&
                      ((V = Date.now()),
                      z(this, ce, L ? "doubleclick" : "click", E, H, I),
                      (L = !1)),
                    (T = N = null));
                ((P = !1), (xe = !0));
              }
              ((C = H),
                xe && te && (v = te._handleMouseEvent(O, E, H, ke) || v),
                ((v && !ke.move) || (ke.down && ue("mouseup"))) && E.preventDefault());
            },
            _handleKeyEvent: function (O, E, H, U) {
              var Y = this._scope,
                K = Y.tool,
                te;
              function se(ue) {
                ue.responds(O) && ((Se = Y), ue.emit(O, (te = te || new Mg(O, E, H, U))));
              }
              this.isVisible() && (se(this), K && K.responds(O) && se(K));
            },
            _countItemEvent: function (O, E) {
              var H = this._itemEvents,
                U = H.native,
                Y = H.virtual;
              for (var K in G) U[K] = (U[K] || 0) + (G[K][O] || 0) * E;
              Y[O] = (Y[O] || 0) + E;
            },
            statics: { updateFocus: c },
          };
        })()
      ),
      Lg = Le.extend({
        _class: "CanvasView",
        initialize: function (r, o) {
          if (!(o instanceof n.HTMLCanvasElement)) {
            var l = q.read(arguments, 1);
            if (l.isZero())
              throw new Error(
                "Cannot create CanvasView with the provided argument: " + s.slice(arguments, 1)
              );
            o = je.getCanvas(l);
          }
          var d = (this._context = o.getContext("2d"));
          if ((d.save(), (this._pixelRatio = 1), !/^off|false$/.test(f.getAttribute(o, "hidpi")))) {
            var c = n.devicePixelRatio || 1,
              h = Ge.getPrefixed(d, "backingStorePixelRatio") || 1;
            this._pixelRatio = c / h;
          }
          (Le.call(this, r, o), (this._needsUpdate = !0));
        },
        remove: function e() {
          return (this._context.restore(), e.base.call(this));
        },
        _setElementSize: function e(r, o) {
          var l = this._pixelRatio;
          if ((e.base.call(this, r * l, o * l), l !== 1)) {
            var d = this._element,
              c = this._context;
            if (!f.hasAttribute(d, "resize")) {
              var h = d.style;
              ((h.width = r + "px"), (h.height = o + "px"));
            }
            (c.restore(), c.save(), c.scale(l, l));
          }
        },
        getPixelSize: function e(r) {
          var o = Se.agent,
            l;
          if (o && o.firefox) l = e.base.call(this, r);
          else {
            var d = this._context,
              c = d.font;
            ((d.font = r + " serif"), (l = parseFloat(d.font)), (d.font = c));
          }
          return l;
        },
        getTextWidth: function (e, r) {
          var o = this._context,
            l = o.font,
            d = 0;
          o.font = e;
          for (var c = 0, h = r.length; c < h; c++) d = Math.max(d, o.measureText(r[c]).width);
          return ((o.font = l), d);
        },
        update: function () {
          if (!this._needsUpdate) return !1;
          var e = this._project,
            r = this._context,
            o = this._viewSize;
          return (
            r.clearRect(0, 0, o.width + 1, o.height + 1),
            e && e.draw(r, this._matrix, this._pixelRatio),
            (this._needsUpdate = !1),
            !0
          );
        },
      }),
      ro = s.extend({
        _class: "Event",
        initialize: function (r) {
          ((this.event = r), (this.type = r && r.type));
        },
        prevented: !1,
        stopped: !1,
        preventDefault: function () {
          ((this.prevented = !0), this.event.preventDefault());
        },
        stopPropagation: function () {
          ((this.stopped = !0), this.event.stopPropagation());
        },
        stop: function () {
          (this.stopPropagation(), this.preventDefault());
        },
        getTimeStamp: function () {
          return this.event.timeStamp;
        },
        getModifiers: function () {
          return $l.modifiers;
        },
      }),
      Mg = ro.extend({
        _class: "KeyEvent",
        initialize: function (r, o, l, d) {
          ((this.type = r), (this.event = o), (this.key = l), (this.character = d));
        },
        toString: function () {
          return (
            "{ type: '" +
            this.type +
            "', key: '" +
            this.key +
            "', character: '" +
            this.character +
            "', modifiers: " +
            this.getModifiers() +
            " }"
          );
        },
      }),
      $l = new (function () {
        var e = {
            "	": "tab",
            " ": "space",
            "\b": "backspace",
            "\x7F": "delete",
            Spacebar: "space",
            Del: "delete",
            Win: "meta",
            Esc: "escape",
          },
          r = { tab: "	", space: " ", enter: "\r" },
          o = {},
          l = {},
          d,
          c,
          h = new s({ shift: !1, control: !1, alt: !1, meta: !1, capsLock: !1, space: !1 }).inject({
            option: {
              get: function () {
                return this.alt;
              },
            },
            command: {
              get: function () {
                var y = Se && Se.agent;
                return y && y.mac ? this.meta : this.control;
              },
            },
          });
        function m(y) {
          var x = y.key || y.keyIdentifier;
          return (
            (x = /^U\+/.test(x)
              ? String.fromCharCode(parseInt(x.substr(2), 16))
              : /^Arrow[A-Z]/.test(x)
                ? x.substr(5)
                : x === "Unidentified" || x === t
                  ? String.fromCharCode(y.keyCode)
                  : x),
            e[x] || (x.length > 1 ? s.hyphenate(x) : x.toLowerCase())
          );
        }
        function p(y, x, b, S) {
          var v = y ? "keydown" : "keyup",
            w = Le._focused,
            _;
          if (
            ((o[x] = y), y ? (l[x] = b) : delete l[x], x.length > 1 && (_ = s.camelize(x)) in h)
          ) {
            h[_] = y;
            var P = Se && Se.agent;
            if (_ === "meta" && P && P.mac)
              if (y) d = {};
              else {
                for (var D in d) D in l && p(!1, D, d[D], S);
                d = null;
              }
          } else y && d && (d[x] = b);
          w && w._handleKeyEvent(y ? "keydown" : "keyup", S, x, b);
        }
        return (
          Ye.add(a, {
            keydown: function (y) {
              var x = m(y),
                b = Se && Se.agent;
              x.length > 1 ||
              (b && b.chrome && (y.altKey || (b.mac && y.metaKey) || (!b.mac && y.ctrlKey)))
                ? p(!0, x, r[x] || (x.length > 1 ? "" : x), y)
                : (c = x);
            },
            keypress: function (y) {
              if (c) {
                var x = m(y),
                  b = y.charCode,
                  S = b >= 32 ? String.fromCharCode(b) : x.length > 1 ? "" : x;
                (x !== c && (x = S.toLowerCase()), p(!0, x, S, y), (c = null));
              }
            },
            keyup: function (y) {
              var x = m(y);
              x in l && p(!1, x, l[x], y);
            },
          }),
          Ye.add(n, {
            blur: function (y) {
              for (var x in l) p(!1, x, l[x], y);
            },
          }),
          {
            modifiers: h,
            isDown: function (y) {
              return !!o[y];
            },
          }
        );
      })(),
      Eg = ro.extend({
        _class: "MouseEvent",
        initialize: function (r, o, l, d, c) {
          ((this.type = r),
            (this.event = o),
            (this.point = l),
            (this.target = d),
            (this.delta = c));
        },
        toString: function () {
          return (
            "{ type: '" +
            this.type +
            "', point: " +
            this.point +
            ", target: " +
            this.target +
            (this.delta ? ", delta: " + this.delta : "") +
            ", modifiers: " +
            this.getModifiers() +
            " }"
          );
        },
      }),
      Ag = ro.extend({
        _class: "ToolEvent",
        _item: null,
        initialize: function (r, o, l) {
          ((this.tool = r), (this.type = o), (this.event = l));
        },
        _choosePoint: function (e, r) {
          return e || (r ? r.clone() : null);
        },
        getPoint: function () {
          return this._choosePoint(this._point, this.tool._point);
        },
        setPoint: function (e) {
          this._point = e;
        },
        getLastPoint: function () {
          return this._choosePoint(this._lastPoint, this.tool._lastPoint);
        },
        setLastPoint: function (e) {
          this._lastPoint = e;
        },
        getDownPoint: function () {
          return this._choosePoint(this._downPoint, this.tool._downPoint);
        },
        setDownPoint: function (e) {
          this._downPoint = e;
        },
        getMiddlePoint: function () {
          return !this._middlePoint && this.tool._lastPoint
            ? this.tool._point.add(this.tool._lastPoint).divide(2)
            : this._middlePoint;
        },
        setMiddlePoint: function (e) {
          this._middlePoint = e;
        },
        getDelta: function () {
          return !this._delta && this.tool._lastPoint
            ? this.tool._point.subtract(this.tool._lastPoint)
            : this._delta;
        },
        setDelta: function (e) {
          this._delta = e;
        },
        getCount: function () {
          return this.tool[/^mouse(down|up)$/.test(this.type) ? "_downCount" : "_moveCount"];
        },
        setCount: function (e) {
          this.tool[/^mouse(down|up)$/.test(this.type) ? "downCount" : "count"] = e;
        },
        getItem: function () {
          if (!this._item) {
            var e = this.tool._scope.project.hitTest(this.getPoint());
            if (e) {
              for (var r = e.item, o = r._parent; /^(Group|CompoundPath)$/.test(o._class);)
                ((r = o), (o = o._parent));
              this._item = r;
            }
          }
          return this._item;
        },
        setItem: function (e) {
          this._item = e;
        },
        toString: function () {
          return (
            "{ type: " +
            this.type +
            ", point: " +
            this.getPoint() +
            ", count: " +
            this.getCount() +
            ", modifiers: " +
            this.getModifiers() +
            " }"
          );
        },
      }),
      Xx = g.extend({
        _class: "Tool",
        _list: "tools",
        _reference: "tool",
        _events: [
          "onMouseDown",
          "onMouseUp",
          "onMouseDrag",
          "onMouseMove",
          "onActivate",
          "onDeactivate",
          "onEditOptions",
          "onKeyDown",
          "onKeyUp",
        ],
        initialize: function (r) {
          (g.call(this), (this._moveCount = -1), (this._downCount = -1), this.set(r));
        },
        getMinDistance: function () {
          return this._minDistance;
        },
        setMinDistance: function (e) {
          ((this._minDistance = e),
            e != null &&
              this._maxDistance != null &&
              e > this._maxDistance &&
              (this._maxDistance = e));
        },
        getMaxDistance: function () {
          return this._maxDistance;
        },
        setMaxDistance: function (e) {
          ((this._maxDistance = e),
            this._minDistance != null &&
              e != null &&
              e < this._minDistance &&
              (this._minDistance = e));
        },
        getFixedDistance: function () {
          return this._minDistance == this._maxDistance ? this._minDistance : null;
        },
        setFixedDistance: function (e) {
          this._minDistance = this._maxDistance = e;
        },
        _handleMouseEvent: function (e, r, o, l) {
          ((Se = this._scope), l.drag && !this.responds(e) && (e = "mousemove"));
          var d = l.move || l.drag,
            c = this.responds(e),
            h = this.minDistance,
            m = this.maxDistance,
            p = !1,
            y = this;
          function x(S, v) {
            var w = o,
              _ = d ? y._point : y._downPoint || w;
            if (d) {
              if (y._moveCount && w.equals(_)) return !1;
              if (_ && (S != null || v != null)) {
                var P = w.subtract(_),
                  D = P.getLength();
                if (D < (S || 0)) return !1;
                v && (w = _.add(P.normalize(Math.min(D, v))));
              }
              y._moveCount++;
            }
            return (
              (y._point = w),
              (y._lastPoint = _ || w),
              l.down && ((y._moveCount = -1), (y._downPoint = w), y._downCount++),
              !0
            );
          }
          function b() {
            c && (p = y.emit(e, new Ag(y, e, r)) || p);
          }
          if (l.down) (x(), b());
          else if (l.up) (x(null, m), b());
          else if (c) for (; x(h, m);) b();
          return p;
        },
      }),
      zg = {
        request: function (e) {
          var r = new i.XMLHttpRequest();
          return (
            r.open((e.method || "get").toUpperCase(), e.url, s.pick(e.async, !0)),
            e.mimeType && r.overrideMimeType(e.mimeType),
            (r.onload = function () {
              var o = r.status;
              o === 0 || o === 200 ? e.onLoad && e.onLoad.call(r, r.responseText) : r.onerror();
            }),
            (r.onerror = function () {
              var o = r.status,
                l = 'Could not load "' + e.url + '" (Status: ' + o + ")";
              if (e.onError) e.onError(l, o);
              else throw new Error(l);
            }),
            r.send(null)
          );
        },
      },
      je = {
        canvases: [],
        getCanvas: function (e, r) {
          if (!n) return null;
          var o,
            l = !0;
          (typeof e == "object" && ((r = e.height), (e = e.width)),
            this.canvases.length
              ? (o = this.canvases.pop())
              : ((o = a.createElement("canvas")), (l = !1)));
          var d = o.getContext("2d");
          if (!d) throw new Error("Canvas " + o + " is unable to provide a 2D context.");
          return (
            o.width === e && o.height === r
              ? l && d.clearRect(0, 0, e + 1, r + 1)
              : ((o.width = e), (o.height = r)),
            d.save(),
            o
          );
        },
        getContext: function (e, r) {
          var o = this.getCanvas(e, r);
          return o ? o.getContext("2d") : null;
        },
        release: function (e) {
          var r = e && e.canvas ? e.canvas : e;
          r && r.getContext && (r.getContext("2d").restore(), this.canvases.push(r));
        },
      },
      Ts = new (function () {
        var e = Math.min,
          r = Math.max,
          o = Math.abs,
          l,
          d,
          c,
          h,
          m,
          p,
          y,
          x,
          b,
          S,
          v;
        function w(W, N, B) {
          return 0.2989 * W + 0.587 * N + 0.114 * B;
        }
        function _(W, N, B, M) {
          var L = M - w(W, N, B);
          ((b = W + L), (S = N + L), (v = B + L));
          var M = w(b, S, v),
            z = e(b, S, v),
            G = r(b, S, v);
          if (z < 0) {
            var O = M - z;
            ((b = M + ((b - M) * M) / O), (S = M + ((S - M) * M) / O), (v = M + ((v - M) * M) / O));
          }
          if (G > 255) {
            var E = 255 - M,
              H = G - M;
            ((b = M + ((b - M) * E) / H), (S = M + ((S - M) * E) / H), (v = M + ((v - M) * E) / H));
          }
        }
        function P(W, N, B) {
          return r(W, N, B) - e(W, N, B);
        }
        function D(W, N, B, V) {
          var L = [W, N, B],
            M = r(W, N, B),
            z = e(W, N, B),
            G;
          ((z = z === W ? 0 : z === N ? 1 : 2),
            (M = M === W ? 0 : M === N ? 1 : 2),
            (G = e(z, M) === 0 ? (r(z, M) === 1 ? 2 : 1) : 0),
            L[M] > L[z]
              ? ((L[G] = ((L[G] - L[z]) * V) / (L[M] - L[z])), (L[M] = V))
              : (L[G] = L[M] = 0),
            (L[z] = 0),
            (b = L[0]),
            (S = L[1]),
            (v = L[2]));
        }
        var I = {
            multiply: function () {
              ((b = (m * l) / 255), (S = (p * d) / 255), (v = (y * c) / 255));
            },
            screen: function () {
              ((b = m + l - (m * l) / 255),
                (S = p + d - (p * d) / 255),
                (v = y + c - (y * c) / 255));
            },
            overlay: function () {
              ((b = m < 128 ? (2 * m * l) / 255 : 255 - (2 * (255 - m) * (255 - l)) / 255),
                (S = p < 128 ? (2 * p * d) / 255 : 255 - (2 * (255 - p) * (255 - d)) / 255),
                (v = y < 128 ? (2 * y * c) / 255 : 255 - (2 * (255 - y) * (255 - c)) / 255));
            },
            "soft-light": function () {
              var W = (l * m) / 255;
              ((b = W + (m * (255 - ((255 - m) * (255 - l)) / 255 - W)) / 255),
                (W = (d * p) / 255),
                (S = W + (p * (255 - ((255 - p) * (255 - d)) / 255 - W)) / 255),
                (W = (c * y) / 255),
                (v = W + (y * (255 - ((255 - y) * (255 - c)) / 255 - W)) / 255));
            },
            "hard-light": function () {
              ((b = l < 128 ? (2 * l * m) / 255 : 255 - (2 * (255 - l) * (255 - m)) / 255),
                (S = d < 128 ? (2 * d * p) / 255 : 255 - (2 * (255 - d) * (255 - p)) / 255),
                (v = c < 128 ? (2 * c * y) / 255 : 255 - (2 * (255 - c) * (255 - y)) / 255));
            },
            "color-dodge": function () {
              ((b = m === 0 ? 0 : l === 255 ? 255 : e(255, (255 * m) / (255 - l))),
                (S = p === 0 ? 0 : d === 255 ? 255 : e(255, (255 * p) / (255 - d))),
                (v = y === 0 ? 0 : c === 255 ? 255 : e(255, (255 * y) / (255 - c))));
            },
            "color-burn": function () {
              ((b = m === 255 ? 255 : l === 0 ? 0 : r(0, 255 - ((255 - m) * 255) / l)),
                (S = p === 255 ? 255 : d === 0 ? 0 : r(0, 255 - ((255 - p) * 255) / d)),
                (v = y === 255 ? 255 : c === 0 ? 0 : r(0, 255 - ((255 - y) * 255) / c)));
            },
            darken: function () {
              ((b = m < l ? m : l), (S = p < d ? p : d), (v = y < c ? y : c));
            },
            lighten: function () {
              ((b = m > l ? m : l), (S = p > d ? p : d), (v = y > c ? y : c));
            },
            difference: function () {
              ((b = m - l),
                b < 0 && (b = -b),
                (S = p - d),
                S < 0 && (S = -S),
                (v = y - c),
                v < 0 && (v = -v));
            },
            exclusion: function () {
              ((b = m + (l * (255 - m - m)) / 255),
                (S = p + (d * (255 - p - p)) / 255),
                (v = y + (c * (255 - y - y)) / 255));
            },
            hue: function () {
              (D(l, d, c, P(m, p, y)), _(b, S, v, w(m, p, y)));
            },
            saturation: function () {
              (D(m, p, y, P(l, d, c)), _(b, S, v, w(m, p, y)));
            },
            luminosity: function () {
              _(m, p, y, w(l, d, c));
            },
            color: function () {
              _(l, d, c, w(m, p, y));
            },
            add: function () {
              ((b = e(m + l, 255)), (S = e(p + d, 255)), (v = e(y + c, 255)));
            },
            subtract: function () {
              ((b = r(m - l, 0)), (S = r(p - d, 0)), (v = r(y - c, 0)));
            },
            average: function () {
              ((b = (m + l) / 2), (S = (p + d) / 2), (v = (y + c) / 2));
            },
            negation: function () {
              ((b = 255 - o(255 - l - m)), (S = 255 - o(255 - d - p)), (v = 255 - o(255 - c - y)));
            },
          },
          C = (this.nativeModes = s.each(
            [
              "source-over",
              "source-in",
              "source-out",
              "source-atop",
              "destination-over",
              "destination-in",
              "destination-out",
              "destination-atop",
              "lighter",
              "darker",
              "copy",
              "xor",
            ],
            function (W) {
              this[W] = !0;
            },
            {}
          )),
          T = je.getContext(1, 1);
        (T &&
          (s.each(I, function (W, N) {
            var B = N === "darken",
              V = !1;
            T.save();
            try {
              ((T.fillStyle = B ? "#300" : "#a00"),
                T.fillRect(0, 0, 1, 1),
                (T.globalCompositeOperation = N),
                T.globalCompositeOperation === N &&
                  ((T.fillStyle = B ? "#a00" : "#300"),
                  T.fillRect(0, 0, 1, 1),
                  (V = T.getImageData(0, 0, 1, 1).data[0] !== B ? 170 : 51)));
            } catch {}
            (T.restore(), (C[N] = V));
          }),
          je.release(T)),
          (this.process = function (W, N, B, V, L) {
            var M = N.canvas,
              z = W === "normal";
            if (z || C[W])
              (B.save(),
                B.setTransform(1, 0, 0, 1, 0, 0),
                (B.globalAlpha = V),
                z || (B.globalCompositeOperation = W),
                B.drawImage(M, L.x, L.y),
                B.restore());
            else {
              var G = I[W];
              if (!G) return;
              for (
                var O = B.getImageData(L.x, L.y, M.width, M.height),
                  E = O.data,
                  H = N.getImageData(0, 0, M.width, M.height).data,
                  U = 0,
                  Y = E.length;
                U < Y;
                U += 4
              ) {
                ((l = H[U]),
                  (m = E[U]),
                  (d = H[U + 1]),
                  (p = E[U + 1]),
                  (c = H[U + 2]),
                  (y = E[U + 2]),
                  (h = H[U + 3]),
                  (x = E[U + 3]),
                  G());
                var K = (h * V) / 255,
                  te = 1 - K;
                ((E[U] = K * b + te * m),
                  (E[U + 1] = K * S + te * p),
                  (E[U + 2] = K * v + te * y),
                  (E[U + 3] = h * V + te * x));
              }
              B.putImageData(O, L.x, L.y);
            }
          }));
      })(),
      Ae = new (function () {
        var e = "http://www.w3.org/2000/svg",
          r = "http://www.w3.org/2000/xmlns",
          o = "http://www.w3.org/1999/xlink",
          l = { href: o, xlink: r, xmlns: r + "/", "xmlns:xlink": r + "/" };
        function d(m, p, y) {
          return h(a.createElementNS(e, m), p, y);
        }
        function c(m, p) {
          var y = l[p],
            x = y ? m.getAttributeNS(y, p) : m.getAttribute(p);
          return x === "null" ? null : x;
        }
        function h(m, p, y) {
          for (var x in p) {
            var b = p[x],
              S = l[x];
            (typeof b == "number" && y && (b = y.number(b)),
              S ? m.setAttributeNS(S, x, b) : m.setAttribute(x, b));
          }
          return m;
        }
        return { svg: e, xmlns: r, xlink: o, create: d, get: c, set: h };
      })(),
      ql = s.each(
        {
          fillColor: ["fill", "color"],
          fillRule: ["fill-rule", "string"],
          strokeColor: ["stroke", "color"],
          strokeWidth: ["stroke-width", "number"],
          strokeCap: ["stroke-linecap", "string"],
          strokeJoin: ["stroke-linejoin", "string"],
          strokeScaling: [
            "vector-effect",
            "lookup",
            { true: "none", false: "non-scaling-stroke" },
            function (e, r) {
              return !r && (e instanceof ei || e instanceof nt || e instanceof eo);
            },
          ],
          miterLimit: ["stroke-miterlimit", "number"],
          dashArray: ["stroke-dasharray", "array"],
          dashOffset: ["stroke-dashoffset", "number"],
          fontFamily: ["font-family", "string"],
          fontWeight: ["font-weight", "string"],
          fontSize: ["font-size", "number"],
          justification: [
            "text-anchor",
            "lookup",
            { left: "start", center: "middle", right: "end" },
          ],
          opacity: ["opacity", "number"],
          blendMode: ["mix-blend-mode", "style"],
        },
        function (e, r) {
          var o = s.capitalize(r),
            l = e[2];
          this[r] = {
            type: e[1],
            property: r,
            attribute: e[0],
            toSVG: l,
            fromSVG:
              l &&
              s.each(
                l,
                function (d, c) {
                  this[d] = c;
                },
                {}
              ),
            exportFilter: e[3],
            get: "get" + o,
            set: "set" + o,
          };
        },
        {}
      );
    return (
      new (function () {
        var e;
        function r(I, C, T) {
          var W = new s(),
            N = I.getTranslation();
          if (C) {
            I = I._shiftless();
            var B = I._inverseTransform(N);
            ((W[T ? "cx" : "x"] = B.x), (W[T ? "cy" : "y"] = B.y), (N = null));
          }
          if (!I.isIdentity()) {
            var V = I.decompose();
            if (V) {
              var L = [],
                M = V.rotation,
                z = V.scaling,
                G = V.skewing;
              (N && !N.isZero() && L.push("translate(" + e.point(N) + ")"),
                M && L.push("rotate(" + e.number(M) + ")"),
                (!F.isZero(z.x - 1) || !F.isZero(z.y - 1)) && L.push("scale(" + e.point(z) + ")"),
                G.x && L.push("skewX(" + e.number(G.x) + ")"),
                G.y && L.push("skewY(" + e.number(G.y) + ")"),
                (W.transform = L.join(" ")));
            } else W.transform = "matrix(" + I.getValues().join(",") + ")";
          }
          return W;
        }
        function o(I, C) {
          for (
            var T = r(I._matrix), W = I._children, N = Ae.create("g", T, e), B = 0, V = W.length;
            B < V;
            B++
          ) {
            var L = W[B],
              M = P(L, C);
            if (M)
              if (L.isClipMask()) {
                var z = Ae.create("clipPath");
                (z.appendChild(M),
                  w(L, z, "clip"),
                  Ae.set(N, { "clip-path": "url(#" + z.id + ")" }));
              } else N.appendChild(M);
          }
          return N;
        }
        function l(I, C) {
          var T = r(I._matrix, !0),
            W = I.getSize(),
            N = I.getImage();
          return (
            (T.x -= W.width / 2),
            (T.y -= W.height / 2),
            (T.width = W.width),
            (T.height = W.height),
            (T.href = (C.embedImages == !1 && N && N.src) || I.toDataURL()),
            Ae.create("image", T, e)
          );
        }
        function d(I, C) {
          var T = C.matchShapes;
          if (T) {
            var W = I.toShape(!1);
            if (W) return c(W, C);
          }
          var N = I._segments,
            B = N.length,
            V,
            L = r(I._matrix);
          if (T && B >= 2 && !I.hasHandles())
            if (B > 2) {
              V = I._closed ? "polygon" : "polyline";
              for (var M = [], z = 0; z < B; z++) M.push(e.point(N[z]._point));
              L.points = M.join(" ");
            } else {
              V = "line";
              var G = N[0]._point,
                O = N[1]._point;
              L.set({ x1: G.x, y1: G.y, x2: O.x, y2: O.y });
            }
          else ((V = "path"), (L.d = I.getPathData(null, C.precision)));
          return Ae.create(V, L, e);
        }
        function c(I) {
          var C = I._type,
            T = I._radius,
            W = r(I._matrix, !0, C !== "rectangle");
          if (C === "rectangle") {
            C = "rect";
            var N = I._size,
              B = N.width,
              V = N.height;
            ((W.x -= B / 2),
              (W.y -= V / 2),
              (W.width = B),
              (W.height = V),
              T.isZero() && (T = null));
          }
          return (
            T && (C === "circle" ? (W.r = T) : ((W.rx = T.width), (W.ry = T.height))),
            Ae.create(C, W, e)
          );
        }
        function h(I, C) {
          var T = r(I._matrix),
            W = I.getPathData(null, C.precision);
          return (W && (T.d = W), Ae.create("path", T, e));
        }
        function m(I, C) {
          var T = r(I._matrix, !0),
            W = I._definition,
            N = v(W, "symbol"),
            B = W._item,
            V = B.getBounds();
          return (
            N ||
              ((N = Ae.create("symbol", { viewBox: e.rectangle(V) })),
              N.appendChild(P(B, C)),
              w(W, N, "symbol")),
            (T.href = "#" + N.id),
            (T.x += V.x),
            (T.y += V.y),
            (T.width = V.width),
            (T.height = V.height),
            (T.overflow = "visible"),
            Ae.create("use", T, e)
          );
        }
        function p(I) {
          var C = v(I, "color");
          if (!C) {
            var T = I.getGradient(),
              W = T._radial,
              N = I.getOrigin(),
              B = I.getDestination(),
              V;
            if (W) {
              V = { cx: N.x, cy: N.y, r: N.getDistance(B) };
              var L = I.getHighlight();
              L && ((V.fx = L.x), (V.fy = L.y));
            } else V = { x1: N.x, y1: N.y, x2: B.x, y2: B.y };
            ((V.gradientUnits = "userSpaceOnUse"),
              (C = Ae.create((W ? "radial" : "linear") + "Gradient", V, e)));
            for (var M = T._stops, z = 0, G = M.length; z < G; z++) {
              var O = M[z],
                E = O._color,
                H = E.getAlpha(),
                U = O._offset;
              ((V = { offset: U ?? z / (G - 1) }),
                E && (V["stop-color"] = E.toCSS(!0)),
                H < 1 && (V["stop-opacity"] = H),
                C.appendChild(Ae.create("stop", V, e)));
            }
            w(I, C, "color");
          }
          return "url(#" + C.id + ")";
        }
        function y(I) {
          var C = Ae.create("text", r(I._matrix, !0), e);
          return ((C.textContent = I._content), C);
        }
        var x = {
          Group: o,
          Layer: o,
          Raster: l,
          Path: d,
          Shape: c,
          CompoundPath: h,
          SymbolItem: m,
          PointText: y,
        };
        function b(I, C, T) {
          var W = {},
            N = !T && I.getParent(),
            B = [];
          return (
            I._name != null && (W.id = I._name),
            s.each(ql, function (V) {
              var L = V.get,
                M = V.type,
                z = I[L]();
              if (V.exportFilter ? V.exportFilter(I, z) : !N || !s.equals(N[L](), z)) {
                if (M === "color" && z != null) {
                  var G = z.getAlpha();
                  G < 1 && (W[V.attribute + "-opacity"] = G);
                }
                M === "style"
                  ? B.push(V.attribute + ": " + z)
                  : (W[V.attribute] =
                      z == null
                        ? "none"
                        : M === "color"
                          ? z.gradient
                            ? p(z, I)
                            : z.toCSS(!0)
                          : M === "array"
                            ? z.join(",")
                            : M === "lookup"
                              ? V.toSVG[z]
                              : z);
              }
            }),
            B.length && (W.style = B.join(";")),
            W.opacity === 1 && delete W.opacity,
            I._visible || (W.visibility = "hidden"),
            Ae.set(C, W, e)
          );
        }
        var S;
        function v(I, C) {
          return (
            S || (S = { ids: {}, svgs: {} }),
            I && S.svgs[C + "-" + (I._id || I.__id || (I.__id = A.get("svg")))]
          );
        }
        function w(I, C, T) {
          S || v();
          var W = (S.ids[T] = (S.ids[T] || 0) + 1);
          ((C.id = T + "-" + W), (S.svgs[T + "-" + (I._id || I.__id)] = C));
        }
        function _(I, C) {
          var T = I,
            W = null;
          if (S) {
            T = I.nodeName.toLowerCase() === "svg" && I;
            for (var N in S.svgs)
              (W ||
                (T || ((T = Ae.create("svg")), T.appendChild(I)),
                (W = T.insertBefore(Ae.create("defs"), T.firstChild))),
                W.appendChild(S.svgs[N]));
            S = null;
          }
          return C.asString ? new i.XMLSerializer().serializeToString(T) : T;
        }
        function P(I, C, T) {
          var W = x[I._class],
            N = W && W(I, C);
          if (N) {
            var B = C.onExport;
            B && (N = B(I, N, C) || N);
            var V = JSON.stringify(I._data);
            V && V !== "{}" && V !== "null" && N.setAttribute("data-paper-data", V);
          }
          return N && b(I, N, T);
        }
        function D(I) {
          return (I || (I = {}), (e = new k(I.precision)), I);
        }
        (ne.inject({
          exportSVG: function (I) {
            return ((I = D(I)), _(P(this, I, !0), I));
          },
        }),
          ct.inject({
            exportSVG: function (I) {
              I = D(I);
              var C = this._children,
                T = this.getView(),
                W = s.pick(I.bounds, "view"),
                N = I.matrix || (W === "view" && T._matrix),
                B = N && Q.read([N]),
                V =
                  W === "view"
                    ? new X([0, 0], T.getViewSize())
                    : W === "content"
                      ? ne._getBounds(C, B, { stroke: !0 }).rect
                      : X.read([W], 0, { readNull: !0 }),
                L = { version: "1.1", xmlns: Ae.svg, "xmlns:xlink": Ae.xlink };
              V &&
                ((L.width = V.width),
                (L.height = V.height),
                (V.x || V.y) && (L.viewBox = e.rectangle(V)));
              var M = Ae.create("svg", L, e),
                z = M;
              B && !B.isIdentity() && (z = M.appendChild(Ae.create("g", r(B), e)));
              for (var G = 0, O = C.length; G < O; G++) z.appendChild(P(C[G], I, !0));
              return _(M, I);
            },
          }));
      })(),
      new (function () {
        var e = {},
          r;
        function o(C, T, W, N, B) {
          var V = Ae.get(C, T),
            L = V == null ? (N ? null : W ? "" : 0) : W ? V : parseFloat(V);
          return /%\s*$/.test(V)
            ? (L / 100) * (B ? 1 : r[/x|^width/.test(T) ? "width" : "height"])
            : L;
        }
        function l(C, T, W, N, B) {
          return (
            (T = o(C, T || "x", !1, N, B)),
            (W = o(C, W || "y", !1, N, B)),
            N && (T == null || W == null) ? null : new R(T, W)
          );
        }
        function d(C, T, W, N, B) {
          return (
            (T = o(C, T || "width", !1, N, B)),
            (W = o(C, W || "height", !1, N, B)),
            N && (T == null || W == null) ? null : new q(T, W)
          );
        }
        function c(C, T, W) {
          return C === "none"
            ? null
            : T === "number"
              ? parseFloat(C)
              : T === "array"
                ? C
                  ? C.split(/[\s,]+/g).map(parseFloat)
                  : []
                : T === "color"
                  ? P(C) || C
                  : T === "lookup"
                    ? W[C]
                    : C;
        }
        function h(C, T, W, N) {
          var B = C.childNodes,
            V = T === "clippath",
            L = T === "defs",
            M = new it(),
            z = M._project,
            G = z._currentStyle,
            O = [];
          if ((!V && !L && ((M = _(M, C, N)), (z._currentStyle = M._style.clone())), N))
            for (var E = C.querySelectorAll("defs"), H = 0, U = E.length; H < U; H++)
              D(E[H], W, !1);
          for (var H = 0, U = B.length; H < U; H++) {
            var Y = B[H],
              K;
            Y.nodeType === 1 &&
              !/^defs$/i.test(Y.nodeName) &&
              (K = D(Y, W, !1)) &&
              !(K instanceof fr) &&
              O.push(K);
          }
          return (
            M.addChildren(O),
            V && (M = _(M.reduce(), C, N)),
            (z._currentStyle = G),
            (V || L) && (M.remove(), (M = null)),
            M
          );
        }
        function m(C, T) {
          for (
            var W = C.getAttribute("points").match(/[+-]?(?:\d*\.\d+|\d+\.?)(?:[eE][+-]?\d+)?/g),
              N = [],
              B = 0,
              V = W.length;
            B < V;
            B += 2
          )
            N.push(new R(parseFloat(W[B]), parseFloat(W[B + 1])));
          var L = new _e(N);
          return (T === "polygon" && L.closePath(), L);
        }
        function p(C) {
          return ei.create(C.getAttribute("d"));
        }
        function y(C, T) {
          var W = (o(C, "href", !0) || "").substring(1),
            N = T === "radialgradient",
            B;
          if (W) ((B = e[W].getGradient()), B._radial ^ N && ((B = B.clone()), (B._radial = N)));
          else {
            for (var V = C.childNodes, L = [], M = 0, z = V.length; M < z; M++) {
              var G = V[M];
              G.nodeType === 1 && L.push(_(new bs(), G));
            }
            B = new to(L, N);
          }
          var O,
            E,
            H,
            U = o(C, "gradientUnits", !0) !== "userSpaceOnUse";
          N
            ? ((O = l(C, "cx", "cy", !1, U)),
              (E = O.add(o(C, "r", !1, !1, U), 0)),
              (H = l(C, "fx", "fy", !0, U)))
            : ((O = l(C, "x1", "y1", !1, U)), (E = l(C, "x2", "y2", !1, U)));
          var Y = _(new kt(B, O, E, H), C);
          return ((Y._scaleToBounds = U), null);
        }
        var x = {
          "#document": function (C, T, W, N) {
            for (var B = C.childNodes, V = 0, L = B.length; V < L; V++) {
              var M = B[V];
              if (M.nodeType === 1) return D(M, W, N);
            }
          },
          g: h,
          svg: h,
          clippath: h,
          polygon: m,
          polyline: m,
          path: p,
          lineargradient: y,
          radialgradient: y,
          image: function (C) {
            var T = new tr(o(C, "href", !0));
            return (
              T.on("load", function () {
                var W = d(C);
                this.setSize(W);
                var N = this._matrix._transformPoint(l(C).add(W.divide(2)));
                this.translate(N);
              }),
              T
            );
          },
          symbol: function (C, T, W, N) {
            return new fr(h(C, T, W, N), !0);
          },
          defs: h,
          use: function (C) {
            var T = (o(C, "href", !0) || "").substring(1),
              W = e[T],
              N = l(C);
            return W ? (W instanceof fr ? W.place(N) : W.clone().translate(N)) : null;
          },
          circle: function (C) {
            return new nt.Circle(l(C, "cx", "cy"), o(C, "r"));
          },
          ellipse: function (C) {
            return new nt.Ellipse({ center: l(C, "cx", "cy"), radius: d(C, "rx", "ry") });
          },
          rect: function (C) {
            return new nt.Rectangle(new X(l(C), d(C)), d(C, "rx", "ry"));
          },
          line: function (C) {
            return new _e.Line(l(C, "x1", "y1"), l(C, "x2", "y2"));
          },
          text: function (C) {
            var T = new Vg(l(C).add(l(C, "dx", "dy")));
            return (T.setContent(C.textContent.trim() || ""), T);
          },
        };
        function b(C, T, W, N) {
          if (C.transform) {
            for (
              var B = (N.getAttribute(W) || "").split(/\)\s*/g), V = new Q(), L = 0, M = B.length;
              L < M;
              L++
            ) {
              var z = B[L];
              if (!z) break;
              for (
                var G = z.split(/\(\s*/), O = G[0], E = G[1].split(/[\s,]+/g), H = 0, U = E.length;
                H < U;
                H++
              )
                E[H] = parseFloat(E[H]);
              switch (O) {
                case "matrix":
                  V.append(new Q(E[0], E[1], E[2], E[3], E[4], E[5]));
                  break;
                case "rotate":
                  V.rotate(E[0], E[1], E[2]);
                  break;
                case "translate":
                  V.translate(E[0], E[1]);
                  break;
                case "scale":
                  V.scale(E);
                  break;
                case "skewX":
                  V.skew(E[0], 0);
                  break;
                case "skewY":
                  V.skew(0, E[0]);
                  break;
              }
            }
            C.transform(V);
          }
        }
        function S(C, T, W) {
          var N = W === "fill-opacity" ? "getFillColor" : "getStrokeColor",
            B = C[N] && C[N]();
          B && B.setAlpha(parseFloat(T));
        }
        var v = s.set(
          s.each(
            ql,
            function (C) {
              this[C.attribute] = function (T, W) {
                if (T[C.set] && (T[C.set](c(W, C.type, C.fromSVG)), C.type === "color")) {
                  var N = T[C.get]();
                  if (N && N._scaleToBounds) {
                    var B = T.getBounds();
                    N.transform(new Q().translate(B.getPoint()).scale(B.getSize()));
                  }
                }
              };
            },
            {}
          ),
          {
            id: function (C, T) {
              ((e[T] = C), C.setName && C.setName(T));
            },
            "clip-path": function (C, T) {
              var W = P(T);
              if (W)
                if (((W = W.clone()), W.setClipMask(!0), C instanceof it)) C.insertChild(0, W);
                else return new it(W, C);
            },
            gradientTransform: b,
            transform: b,
            "fill-opacity": S,
            "stroke-opacity": S,
            visibility: function (C, T) {
              C.setVisible && C.setVisible(T === "visible");
            },
            display: function (C, T) {
              C.setVisible && C.setVisible(T !== null);
            },
            "stop-color": function (C, T) {
              C.setColor && C.setColor(T);
            },
            "stop-opacity": function (C, T) {
              C._color && C._color.setAlpha(parseFloat(T));
            },
            offset: function (C, T) {
              if (C.setOffset) {
                var W = T.match(/(.*)%$/);
                C.setOffset(W ? W[1] / 100 : parseFloat(T));
              }
            },
            viewBox: function (C, T, W, N, B) {
              var V = new X(c(T, "array")),
                L = d(N, null, null, !0),
                M,
                z;
              if (C instanceof it) {
                var G = L ? L.divide(V.getSize()) : 1,
                  z = new Q().scale(G).translate(V.getPoint().negate());
                M = C;
              } else C instanceof fr && (L && V.setSize(L), (M = C._item));
              if (M) {
                if (w(N, "overflow", B) !== "visible") {
                  var O = new nt.Rectangle(V);
                  (O.setClipMask(!0), M.addChild(O));
                }
                z && M.transform(z);
              }
            },
          }
        );
        function w(C, T, W) {
          var N = C.attributes[T],
            B = N && N.value;
          if (!B) {
            var V = s.camelize(T);
            ((B = C.style[V]), !B && W.node[V] !== W.parent[V] && (B = W.node[V]));
          }
          return B ? (B === "none" ? null : B) : t;
        }
        function _(C, T, W) {
          if (T.style) {
            var N = T.parentNode,
              B = {
                node: Ge.getStyles(T) || {},
                parent: (!W && !/^defs$/i.test(N.tagName) && Ge.getStyles(N)) || {},
              };
            s.each(v, function (V, L) {
              var M = w(T, L, B);
              C = (M !== t && V(C, M, L, T, B)) || C;
            });
          }
          return C;
        }
        function P(C) {
          var T = C && C.match(/\((?:["'#]*)([^"')]+)/),
            W = T && T[1],
            N = W && e[n ? W.replace(n.location.href.split("#")[0] + "#", "") : W];
          return (N && N._scaleToBounds && ((N = N.clone()), (N._scaleToBounds = !0)), N);
        }
        function D(C, T, W) {
          var N = C.nodeName.toLowerCase(),
            B = N !== "#document",
            V = a.body,
            L,
            M,
            z,
            G = Se.getView();
          if (W && B) {
            if (G) r = G.getSize();
            else {
              var O = T.size || {};
              r = { width: O.width || 1, height: O.height || 1 };
            }
            ((r = d(C, null, null, !0) || r),
              (L = Ae.create("svg", { style: "stroke-width: 1px; stroke-miterlimit: 10" })),
              (M = C.parentNode),
              (z = C.nextSibling),
              L.appendChild(C),
              V.appendChild(L));
          }
          var E = Se.settings,
            H = E.applyMatrix,
            U = E.insertItems;
          ((E.applyMatrix = !1), (E.insertItems = !1));
          var Y = x[N],
            K = (Y && Y(C, N, T, W)) || null;
          if (((E.insertItems = U), (E.applyMatrix = H), K)) {
            B && !(K instanceof it) && (K = _(K, C, W));
            var te = T.onImport,
              se = B && C.getAttribute("data-paper-data");
            (te && (K = te(C, K, T) || K),
              T.expandShapes && K instanceof nt && (K.remove(), (K = K.toPath())),
              se && (K._data = JSON.parse(se)));
          }
          return (
            L && (V.removeChild(L), M && (z ? M.insertBefore(C, z) : M.appendChild(C))),
            W && ((e = {}), K && s.pick(T.applyMatrix, H) && K.matrix.apply(!0, !0)),
            K
          );
        }
        function I(C, T, W) {
          if (!C) return null;
          T = typeof T == "function" ? { onLoad: T } : T || {};
          var N = Se,
            B = null;
          function V(G) {
            try {
              var O =
                typeof G == "object" ? G : new i.DOMParser().parseFromString(G, "image/svg+xml");
              if (!O.nodeName) throw ((O = null), new Error("Unsupported SVG source: " + C));
              ((Se = N), (B = D(O, T, !0)), (!T || T.insert !== !1) && W._insertItem(t, B));
              var E = T.onLoad;
              E && E(B, G);
            } catch (H) {
              L(H);
            }
          }
          function L(G, O) {
            var E = T.onError;
            if (E) E(G, O);
            else throw new Error(G);
          }
          if (typeof C == "string" && !/^.*</.test(C)) {
            var M = a.getElementById(C);
            M ? V(M) : zg.request({ url: C, async: !0, onLoad: V, onError: L });
          } else if (typeof File < "u" && C instanceof File) {
            var z = new FileReader();
            return (
              (z.onload = function () {
                V(z.result);
              }),
              (z.onerror = function () {
                L(z.error);
              }),
              z.readAsText(C)
            );
          } else V(C);
          return B;
        }
        (ne.inject({
          importSVG: function (C, T) {
            return I(C, T, this);
          },
        }),
          ct.inject({
            importSVG: function (C, T) {
              return (this.activate(), I(C, T, this));
            },
          }));
      })(),
      (Se = new (f.inject(s.exports, {
        Base: s,
        Numerical: F,
        Key: $l,
        DomEvent: Ye,
        DomElement: Ge,
        document: a,
        window: n,
        Symbol: fr,
        PlacedSymbol: xs,
      }))()),
      Se.agent.node && am()(Se),
      typeof define == "function" && define.amd
        ? define("paper", Se)
        : typeof ji == "object" && ji && (ji.exports = Se),
      Se
    );
  }.call(sm, typeof self == "object" ? self : null);
});
function Va(i) {
  let { paddingPerSide: t, paddingTop: n, paddingBottom: a, paddingLeft: s, paddingRight: u } = i,
    f = i.resolveValue("padding") ?? 0;
  if (pe(f)) {
    let [g = "0px", k = "0px", F = "0px", A = "0px"] = Qd(f);
    return {
      perSide: f.includes(" "),
      top: qe(g)[0] || 0,
      right: qe(A)[0] || 0,
      bottom: qe(F)[0] || 0,
      left: qe(k)[0] || 0,
    };
  }
  return t !== !1 && (n !== void 0 || a !== void 0 || s !== void 0 || u !== void 0)
    ? {
        perSide: !0,
        top: n !== void 0 ? n : f,
        bottom: a !== void 0 ? a : f,
        left: s !== void 0 ? s : f,
        right: u !== void 0 ? u : f,
      }
    : { perSide: !1, top: f, bottom: f, left: f, right: f };
}
function Zf(i) {
  let t = i.resolveValue("stackDirection") ?? bn,
    n = i.resolveValue("stackDistribution"),
    a = i.resolveValue("stackAlignment"),
    s = {
      display: "flex",
      flexDirection: t ? Tn(t) : void 0,
      justifyContent: n ? Jf(n) : void 0,
      alignItems: a ? Jf(a) : void 0,
    },
    { top: u, left: f, right: g, bottom: k } = Va(i);
  return (
    u === f && u === g && u === k
      ? u && (s.padding = `${u}px`)
      : (s.padding = `${u}px ${g}px ${k}px ${f}px`),
    s
  );
}
function Jf(i) {
  switch (i) {
    case "start":
      return "flex-start";
    case "end":
      return "flex-end";
    default:
      return i;
  }
}
var em = {
    Phone: [
      {
        id: "iPhone_390_844",
        title: "iPhone 13",
        screenWidth: 390,
        screenHeight: 844,
        canRotate: !0,
      },
      {
        id: "iPhone_375_812_mini",
        title: "iPhone 13 Mini",
        screenWidth: 375,
        screenHeight: 812,
        canRotate: !0,
      },
      {
        id: "iPhone_390_844_pro",
        title: "iPhone 13 Pro",
        screenWidth: 390,
        screenHeight: 844,
        canRotate: !0,
      },
      {
        id: "iPhone_428_926_pro",
        title: "iPhone 13 Pro Max",
        screenWidth: 428,
        screenHeight: 926,
        canRotate: !0,
      },
      {
        id: "iPhone_414_896",
        title: "iPhone 11",
        screenWidth: 414,
        screenHeight: 896,
        canRotate: !0,
      },
      {
        id: "iPhone_375_812",
        title: "iPhone 11 Pro / X",
        screenWidth: 375,
        screenHeight: 812,
        canRotate: !0,
      },
      {
        id: "iPhone_414_896_pro",
        title: "iPhone 11 Pro Max",
        screenWidth: 414,
        screenHeight: 896,
        canRotate: !0,
      },
      {
        id: "iPhone_375_667",
        title: "iPhone 8",
        screenWidth: 375,
        screenHeight: 667,
        canRotate: !0,
      },
      {
        id: "iPhone_414_736",
        title: "iPhone 8 Plus",
        screenWidth: 414,
        screenHeight: 736,
        canRotate: !0,
      },
      {
        id: "iPhone_320_568",
        title: "iPhone SE",
        screenWidth: 320,
        screenHeight: 568,
        canRotate: !0,
      },
      {
        id: "Android_360_800",
        title: "Android",
        screenWidth: 360,
        screenHeight: 800,
        canRotate: !0,
      },
      {
        id: "GooglePixel_360_780",
        title: "Google Pixel 5",
        screenWidth: 360,
        screenHeight: 780,
        canRotate: !0,
      },
      {
        id: "GooglePixel_360_760",
        title: "Google Pixel 4",
        screenWidth: 360,
        screenHeight: 760,
        canRotate: !0,
      },
      {
        id: "GooglePixel_360_760_xl",
        title: "Google Pixel 4XL",
        screenWidth: 360,
        screenHeight: 760,
        canRotate: !0,
      },
    ],
    Desktop: [
      {
        id: "MacBook_1440_900",
        title: "MacBook Air",
        screenWidth: 1440,
        screenHeight: 900,
        canRotate: !1,
      },
      {
        id: "MacBook_1512_982",
        title: 'MacBook Pro 14"',
        screenWidth: 1512,
        screenHeight: 982,
        canRotate: !1,
      },
      {
        id: "MacBook_1728_1117",
        title: 'MacBook Pro 16"',
        screenWidth: 1728,
        screenHeight: 1117,
        canRotate: !1,
      },
      {
        id: "iMac_1280_720",
        title: 'iMac 24"',
        screenWidth: 1280,
        screenHeight: 720,
        canRotate: !1,
      },
      {
        id: "iMac_2560_1440",
        title: 'iMac 27"',
        screenWidth: 2560,
        screenHeight: 1440,
        canRotate: !1,
      },
      {
        id: "Pro_Display_XDR_3008_1692",
        title: "Pro Display XDR",
        screenWidth: 3008,
        screenHeight: 1692,
        canRotate: !1,
      },
      {
        id: "DellXPS_1920_1080",
        title: "Dell XPS",
        screenWidth: 1920,
        screenHeight: 1080,
        canRotate: !1,
      },
      {
        id: "SurfaceBook_1500_1000",
        title: "Microsoft Surface Book",
        screenWidth: 1500,
        screenHeight: 1e3,
        canRotate: !1,
      },
    ],
    Tablet: [
      { id: "iPad_810_1080", title: "iPad", screenWidth: 810, screenHeight: 1080, canRotate: !0 },
      {
        id: "iPad_768_1024",
        title: "iPad Mini",
        screenWidth: 768,
        screenHeight: 1024,
        canRotate: !0,
      },
      {
        id: "iPad_820_1180",
        title: "iPad Air",
        screenWidth: 820,
        screenHeight: 1180,
        canRotate: !0,
      },
      {
        id: "iPad_834_1194",
        title: 'iPad Pro 11"',
        screenWidth: 834,
        screenHeight: 1194,
        canRotate: !0,
      },
      {
        id: "iPad_1024_1366",
        title: 'iPad Pro 12.9"',
        screenWidth: 1024,
        screenHeight: 1366,
        canRotate: !0,
      },
      {
        id: "Surface_1440_960",
        title: "Surface 3",
        screenWidth: 1440,
        screenHeight: 960,
        canRotate: !0,
      },
      {
        id: "Surface_1368_912",
        title: "Surface Pro 4",
        screenWidth: 1368,
        screenHeight: 912,
        canRotate: !0,
      },
    ],
    Watch: [
      {
        id: "AppleWatch_44",
        title: "Apple Watch 44mm",
        screenWidth: 184,
        screenHeight: 224,
        canRotate: !1,
      },
      {
        id: "AppleWatch_42",
        title: "Apple Watch 42mm",
        screenWidth: 156,
        screenHeight: 195,
        canRotate: !1,
      },
      {
        id: "AppleWatch_40",
        title: "Apple Watch 40mm",
        screenWidth: 162,
        screenHeight: 197,
        canRotate: !1,
      },
      {
        id: "AppleWatch_38",
        title: "Apple Watch 38mm",
        screenWidth: 136,
        screenHeight: 170,
        canRotate: !1,
      },
    ],
    TV: [
      {
        id: "FullHD_TV_1920_1080",
        title: "Full HD",
        screenWidth: 1920,
        screenHeight: 1080,
        canRotate: !1,
      },
      { id: "4K_TV_3840_2160", title: "4K", screenWidth: 3840, screenHeight: 2160, canRotate: !1 },
    ],
    Package: [
      {
        id: "PackageArtwork_800_600",
        title: "Artwork",
        screenWidth: 800,
        screenHeight: 600,
        canRotate: !1,
      },
      { id: "PackageIcon_50_50", title: "Icon", screenWidth: 50, screenHeight: 50, canRotate: !1 },
    ],
  },
  Qf = em,
  Gg = [],
  rl = {},
  $g = [];
Object.entries(em).forEach(([i, t]) => {
  (Gg.push(i),
    t.forEach((n) => {
      ((rl[n.id] = n), $g.push(n), (n.category = i));
    }));
});
function qg(i) {
  return rl[i];
}
function tm(i) {
  return i ? (qg(i)?.title ?? null) : null;
}
function ev(i) {
  if (i)
    for (let t in Qf) {
      let n = Qf[t].find((a) => a.id === i);
      if (n) return n.category;
    }
}
function rm(i) {
  return typeof i == "string" && i in rl;
}
var im = { framePreset: null },
  Ug = "framePreset";
function Xg(i) {
  return Ug in i;
}
function iv(i, t) {
  if (!Xg(i)) return;
  let n = rm(i.framePreset) ? i.framePreset : null;
  t.framePresetsInSelection.push(n);
}
function Fn(i) {
  if (!Qe.isQuadrilateralPoints(i)) return !0;
  let [t, n, a, s] = i;
  if (Kg(i)) return !1;
  let u = zr(t, n),
    f = zr(n, a),
    g = zr.intersectionAngle(u, f);
  if (Math.round(Math.abs(g)) !== 90) return !0;
  let k = zr(s, t),
    F = zr(a, s),
    A = zr.intersectionAngle(F, k);
  return Math.round(Math.abs(A)) !== 90;
}
function Kg(i) {
  if (i.length !== 4) return !1;
  let [t, n, a, s] = i;
  return t.x === s.x && t.y === n.y && a.x === n.x && a.y === s.y
    ? !0
    : t.x === n.x && t.y === s.y && a.x === s.x && a.y === n.y;
}
var sv = { pathBoolean: 0 },
  Yg = "pathBoolean";
function lv(i) {
  return Yg in i;
}
function nm(i) {
  switch (i) {
    case 0:
      return "Unite";
    case 2:
      return "Intersect";
    case 3:
      return "Exclude";
    case 1:
      return "Subtract";
    case 4:
      return "Join";
  }
}
function jg(i) {
  (i.setup(""), i.project.view.remove());
}
var La;
function ur() {
  return (La || ((La = lm()), jg(La)), La);
}
var nl = { matrix: null };
function ol({ item: i, parentAbsolutePosition: t = Qe(0, 0), options: n = nl }) {
  let a = ur();
  if (i instanceof a.CompoundPath)
    return Zg({ compoundPath: i, parentAbsolutePosition: t, options: n });
  if (i instanceof a.Path) return [Jg({ path: i, parentAbsolutePosition: t, options: n })];
  throw new Error("Unexpected path type");
}
function Jg({ path: i, parentAbsolutePosition: t = Qe(0, 0), options: n = nl }) {
  return (
    n.matrix !== null &&
      i.segments.forEach((s) => {
        s.transform(n.matrix);
      }),
    {
      pathSegments: i.segments.map((s) => {
        let { point: u, handleIn: f, handleOut: g } = s;
        return new Je({
          x: u.x - t.x,
          y: u.y - t.y,
          handleInX: f.x,
          handleInY: f.y,
          handleOutX: g.x,
          handleOutY: g.y,
          handleMirroring: Qg(s),
        });
      }),
      pathClosed: i.closed,
    }
  );
}
function Zg({ compoundPath: i, parentAbsolutePosition: t = Qe(0, 0), options: n = nl }) {
  let a = [];
  return (
    i.children.forEach((s) => {
      let u = ur();
      s instanceof u.PathItem &&
        (a = a.concat(ol({ item: s, parentAbsolutePosition: t, options: n })));
    }),
    a
  );
}
function Qg(i) {
  let { handleIn: t, handleOut: n } = i;
  return t.x === 0 && t.y === 0 && n.x === 0 && n.y === 0
    ? "straight"
    : t.x === -n.x && t.y === -n.y
      ? "symmetric"
      : "disconnected";
}
function il(i) {
  let t = ur();
  if (i instanceof t.Color) return i.toCSS(!1);
  if (i) throw Error(`Paper.Color unexpectedly passed as a string: ${i}`);
  return "rgba(0,0,0,1)";
}
function ey(i, t, n) {
  let a = n.x - t.x,
    s = n.y - t.y,
    u = Math.hypot(a, s);
  if (u === 0) return { start: 0, end: 1 };
  let f = a / u,
    g = s / u,
    k = Math.abs(i.width * f) + Math.abs(i.height * g);
  if (k === 0) return { start: 0, end: 1 };
  let F = i.x + i.width / 2,
    A = i.y + i.height / 2,
    R = ($) => 0.5 + (($.x - F) * f + ($.y - A) * g) / k;
  return { start: R(t), end: R(n) };
}
function vv(i) {
  let t = i.fillColor,
    n = "color",
    a,
    s,
    u = t;
  if (u?.origin && u.destination) {
    let f = t.gradient;
    if (f) {
      let g = 180,
        k,
        F = 0,
        A = 1;
      if (f.radial) {
        let $ = Math.max(i.bounds.width, 1),
          q = Math.max(i.bounds.height, 1),
          Z = u.origin.x / $,
          X = u.origin.y / q,
          be = { x: Z, y: X },
          Q = { x: u.destination.x / $, y: u.destination.y / q },
          oe = Qe.distance(be, Q),
          ct = oe,
          ne = Math.abs(be.y - Q.y) > Math.abs(be.x - Q.x),
          it = ne ? ct : oe,
          ft = ne ? oe : ct;
        ((n = "radial-gradient"),
          (k = { centerAnchorX: Z, centerAnchorY: X, widthFactor: it, heightFactor: ft }));
      } else {
        ((n = "linear-gradient"), (g = (90 + u.destination.subtract(u.origin).angle) % 360));
        let $ = ey(i.bounds, u.origin, u.destination);
        ((F = $.start), (A = $.end));
      }
      let R = f.stops.map(($, q, Z) => {
        let X = Z.length - 1,
          be = X === 0 ? 0 : q / X,
          Q = $.rampPoint ?? be,
          oe = Q;
        return (f.radial || (oe = F + Q * (A - F)), new Bc({ value: il($.color), position: oe }));
      });
      ((a = new Vc({ stops: R, angle: g })), (s = new Lc({ stops: R, ...k })));
    }
  }
  return {
    strokeEnabled: i.hasStroke(),
    strokeAlignment: "center",
    strokeWidth: i.strokeWidth,
    strokeColor: il(i.strokeColor),
    strokeMiterLimit: i.miterLimit,
    strokeDashArray: i.dashArray.join(","),
    strokeDashOffset: i.dashOffset,
    lineJoin: i.strokeJoin,
    lineCap: i.strokeCap,
    fillEnabled: i.hasFill(),
    fillColor: il(i.fillColor),
    fillType: n,
    fillLinearGradient: a,
    fillRadialGradient: s,
    opacity: i.opacity,
  };
}
function al(i, t) {
  let n = i.map((a) => ty(a, t));
  if (n.length === 1) return n[0];
  {
    let a = ur();
    return new a.CompoundPath({ insert: !1, children: n });
  }
}
function ty({ pathSegments: i, pathClosed: t }, n) {
  let a = ur(),
    s = new a.Path({ insert: !1, closed: t || n });
  return (
    i.forEach((u) => {
      u &&
        s.add(
          new a.Segment({
            point: new a.Point(u.x, u.y),
            handleIn: new a.Point(u.handleInX, u.handleInY),
            handleOut: new a.Point(u.handleOutX, u.handleOutY),
          })
        );
    }),
    s
  );
}
var Ma = class {
  booleanJoin(t, n) {
    if (n === 4) return [].concat(...t);
    let a = t.map((u) => al(u, !0)),
      s = a.shift();
    return (
      a.forEach((u) => {
        if (s && u)
          switch (n) {
            case 0:
              s = s.unite(u);
              break;
            case 3:
              s = s.exclude(u);
              break;
            case 2:
              s = s.intersect(u);
              break;
            case 1:
              s = s.subtract(u);
              break;
          }
      }),
      s ? ol({ item: s }) : []
    );
  }
  containsPoint(t, n) {
    let a,
      s = t.cache;
    s.paperPath ? (a = s.paperPath) : ((a = al(t.calculatedPaths())), (s.paperPath = a));
    let u = ur();
    return a.contains(new u.Point(n));
  }
};
var sl;
function Bn() {
  return (sl || (sl = new Ma()), sl);
}
function Vn(i, t) {
  let n = i.originAdjustedMatrix(null, i);
  return t.map(({ pathSegments: a, pathClosed: s }) => ({
    pathSegments: a.map((u) => iy(i, u, n)),
    pathClosed: s,
  }));
}
function iy(i, t, n) {
  let a = n ?? i.originAdjustedMatrix(null, i),
    s = jr.convertPoint(a, t),
    u = jr.convertPoint(a, Je.calculatedHandleIn(t)),
    f = jr.convertPoint(a, Je.calculatedHandleOut(t)),
    g = Qe.subtract(u, s),
    k = Qe.subtract(f, s);
  return t.merge({ ...s, handleOutX: k.x, handleOutY: k.y, handleInX: g.x, handleInY: g.y });
}
function Bv(i, t, n) {
  return jr.convertPoint(i.originAdjustedMatrix(null, n || i), t);
}
var Ea;
((n) => {
  function i(a) {
    let s = [];
    for (let u of a) {
      if (!_r(u) || u.visible === !1) continue;
      let f = u.calculatedPaths(),
        g = Vn(u, f);
      s.push(g);
    }
    return s;
  }
  n.sourcePaths = i;
  function t(a, s) {
    return Bn().booleanJoin(a, s);
  }
  n.executePathBoolean = t;
})((Ea ||= {}));
function ll(i, t, n) {
  let a = new DOMMatrix().translateSelf(t.x || 0, t.y || 0).rotateSelf(0, 0, n || 0),
    s = new DOMMatrix()
      .translateSelf(0.5 * t.width, 0.5 * t.height)
      .multiplySelf(a)
      .translateSelf(-0.5 * t.width, -0.5 * t.height);
  return jr.convertPoint(s, i);
}
var dm = ti("CanvasTree:utils:shapes"),
  dl = 2;
function um(i) {
  let t = (i.resolveValue("strokeWidth") ?? 0) / 2;
  return ou(i) && i.fillEnabled ? t : Math.max(t, dl);
}
function cm(i, t) {
  let n = 1 / 0;
  for (let a = 0, s = i.length; a < s; a++) {
    let g = i[a].project(t).d;
    ie(g) && g < n && (n = g);
  }
  return n;
}
function fm(i, t) {
  return cm(i.localCalculatedPathBezierCurves(), t);
}
function mm(i, t, n, a = t.id) {
  let s = t.canvasCalculatedPathBezierCurves(i, a);
  return cm(s, n);
}
function eb(i, t, n) {
  let a = {};
  return (ny(i, t, n, a), a);
}
function ny(i, t, n, a) {
  let s;
  if (
    (n &&
      !pn(n.calculatedPaths) &&
      dm.debug(
        n.id,
        n.__class,
        n.getProps(t),
        n.calculatedPaths,
        typeof n,
        typeof n.calculatedPaths
      ),
    !(qr(n) || $r(n)))
  )
    ((a[n.id] = { x: n.x, y: n.y }), (s = n.calculatedPaths()));
  else {
    let u = Ks(n).map((R) => hm(i, t, R, a)),
      f;
    ($r(n) ? (f = n.pathBoolean) : (f = 4), (s = Ea.executePathBoolean(u, f)));
    let [g, k] = gm(n, Nn(n) && ie(n.rotation) ? n.rotation : 0, s);
    a[n.id] = g;
    let F = k.x,
      A = k.y;
    pm(n.children, a, F, A);
  }
  return Vn(n, s);
}
function hm(i, t, n, a) {
  let s;
  if (!(qr(n) || $r(n)))
    ((a[n.id] = { x: n.x, y: n.y }),
      n &&
        !pn(n.calculatedPaths) &&
        dm.debug(
          "Unexpected nodeType:",
          n.id,
          n.__class,
          n.getProps(t),
          n.calculatedPaths,
          typeof n,
          typeof n.calculatedPaths,
          n
        ),
      (s = n.calculatedPaths()));
  else {
    let u = Ks(n).map((R) => hm(i, t, R, a)),
      f;
    ($r(n) ? (f = n.pathBoolean) : (f = 4), (s = Ea.executePathBoolean(u, f)));
    let [g, k] = gm(n, Nn(n) && ie(n.rotation) ? n.rotation : 0, s);
    a[n.id] = g;
    let F = k.x,
      A = k.y;
    pm(n.children, a, F, A);
  }
  return Vn(n, s);
}
function pm(i, t, n, a) {
  for (let s of i) {
    let u = t[s.id];
    if (!u) continue;
    ((u.x -= n), (u.y -= a));
    let f = !1,
      g;
    for (g in u)
      if (u[g] !== s[g]) {
        f = !0;
        break;
      }
    f || delete t[s.id];
  }
}
function gm(i, t, n) {
  let a = Ct.boundingBox(n),
    s = a.x,
    u = a.y,
    f = { x: i.x + s, y: i.y + u, width: a.width, height: a.height },
    g = ll({ x: 0, y: 0 }, i, t),
    k = ll({ x: -s, y: -u }, f, t),
    F = Qe.subtract(g, k);
  return ((f.x += F.x), (f.y += F.y), [f, { x: s, y: u }]);
}
function ul(i, t) {
  i = i.draftOrCurrent();
  let n = [],
    a = { minX: t.x, maxX: t.x + t.width, minY: t.y, maxY: t.y + t.height };
  return (ym(i, a, n), n);
}
function cl(i, t) {
  i = i.draftOrCurrent();
  let n = [],
    a = { minX: t.x, maxX: t.x + t.width, minY: t.y, maxY: t.y + t.height };
  return (Sm(i, a, n), n);
}
function ym(i, t, n) {
  let a = i.cache;
  if (!a.visible || !xm(a, t)) return;
  ((i = a.draft ? a.draft : i), ar(i) && n.push(i));
  let s = i.children;
  if (s) for (let u of s) ym(u, t, n);
}
function Sm(i, t, n) {
  let a = i.cache;
  i = a.draft ? a.draft : i;
  let s = !0;
  for (let [f, g] of a.iterateRenderInstances())
    xm(g, t) && ((s = !1), ar(i) && n.push({ node: i, renderId: f }));
  if (s) return;
  let u = i.children;
  if (u) for (let f of u) Sm(f, t, n);
}
function xm(i, t) {
  return t.minX <= i.maxX && t.minY <= i.maxY && t.maxX >= i.minX && t.maxY >= i.minY;
}
function gb(i, t, n, a = !1) {
  let s = [],
    u = mt.cornerPoints(n);
  return (
    ul(t, n).forEach((f) => {
      if (!Mc(f) && f.id !== i.root.id) {
        let g = Dn(i, f),
          k = wr(i, f);
        if (a) {
          if (Fn(g) ? di.intersects(g, mt.cornerPoints(n)) : mt.intersects(k, n)) {
            let A = u.map((q) => $o(i, f, q, !0)),
              R = mt.boundingRectFromPoints(A),
              $ = mt.atOrigin(i.getRect(f));
            if (mt.intersects($, R)) {
              if (qo(i, u, f)) return;
              s.push(f);
            }
          }
        } else mt.containsRect(n, k) && s.push(f);
      }
    }),
    s
  );
}
function Aa(i, t, n, a, s, u = t.id) {
  return fm(t, n) <= s ? !0 : mm(i, t, a, u) <= dl;
}
function vm(i, t, n, a = !1, s = !1, u) {
  let f = [],
    g = { x: n.x, y: n.y, width: 0, height: 0 };
  s && ((g.x -= 2), (g.width += 4), (g.y -= 2), (g.height += 4));
  let k = mt.points(g);
  return (
    ul(t, g).forEach((F) => {
      if (!ar(F)) return;
      let A = Dn(i, F);
      if (Fn(A) && !di.containsPoint(A, n)) return;
      let R = bm(i, F, F.id, s);
      if (
        mt.containsPoint(R, n) &&
        !(qo(i, k, F) && (!a || !F.isSelected() || !u || !mt.containsRect(u, R)))
      ) {
        if (_r(F) && Fi(F)) {
          let $ = $o(i, F, n, !0),
            q = F.calculatedPaths(),
            Z = Bn().containsPoint(F, $),
            X = Ct.isClosed(q);
          if (F.strokeEnabled && !!F.strokeWidth && !F.fillEnabled) {
            let Q = F.strokeWidth ? (F.resolveValue("strokeWidth") ?? 0) : 0;
            if (X && F.strokeAlignment === "inside") {
              if (!Z || !Aa(i, F, $, n, Q)) return;
            } else if (!Aa(i, F, $, n, 0.5 * Q)) return;
          } else if (X && !Z) return;
        }
        f.push(F);
      }
    }),
    f
  );
}
function yb(i, t, n, a = !1, s = !1, u, f) {
  let g = { x: n.x, y: n.y, width: 0, height: 0 };
  s && ((g.x -= 2), (g.width += 4), (g.y -= 2), (g.height += 4));
  let k = mt.points(g);
  return cl(t, g).filter((F) => {
    if (f && !f(F)) return !1;
    let { node: A, renderId: R } = F;
    if (!ar(A)) return !1;
    let $ = Dn(i, A, void 0, void 0, R);
    if (Fn($) && !di.containsPoint($, n)) return !1;
    let q = bm(i, A, R, s);
    if (
      !mt.containsPoint(q, n) ||
      (qo(i, k, A, R) && (!(a && A.isSelected()) || !u || !mt.containsRect(u, q)))
    )
      return !1;
    if (_r(A) && Fi(A)) {
      let Z = $o(i, A, n, !0, R),
        X = A.calculatedPaths(),
        be = Bn().containsPoint(A, Z),
        Q = Ct.isClosed(X);
      if (A.strokeEnabled && A.strokeWidth && !A.fillEnabled) {
        let oe = A.resolveValue("strokeWidth") ?? 0;
        if (Q && A.strokeAlignment === "inside") {
          if (!be || !Aa(i, A, Z, n, oe, R)) return !1;
        } else if (!Aa(i, A, Z, n, 0.5 * oe, R)) return !1;
      } else if (Q && !be) return !1;
    }
    return !0;
  });
}
function Sb(i, t, n, a, s) {
  let u = [];
  return (
    cl(t, mt.boundingRectFromPoints(n)).forEach((f) => {
      if (s && !s(f)) return;
      let { node: g, renderId: k } = f;
      if (!ar(g)) return;
      let F = Dn(i, g, void 0, void 0, k);
      a && Fn(F)
        ? di.containsPoint(F, a) && u.push({ node: g, renderId: k })
        : di.contains(F, n) && u.push({ node: g, renderId: k });
    }),
    u
  );
}
function bm(i, t, n, a) {
  let s = { ...wr(i, t, void 0, n) };
  return (
    a && (s.width < 4 && ((s.x -= 2), (s.width = 4)), s.height < 4 && ((s.y -= 2), (s.height = 4))),
    !_r(t) || !Fi(t) || !t.strokeEnabled ? s : mt.inflate(s, um(t))
  );
}
var Cm = (i, t, n, a) => wr(i, t)[n] + a;
var km = { guidesX: [], guidesY: [] },
  oy = "guidesX";
function Pm(i) {
  return oy in i;
}
var ay = 3,
  wb = (i, t, n, a) => {
    if (!Pm(t)) return null;
    let s = Tm(t, n),
      u = sy(i, t, n);
    if (!u && !s) return null;
    let f = s;
    if (u) {
      let g = ly(i, n, u),
        k = Tm(u, g);
      s ? (k ? k && s && (f = k.distance > s.distance ? s : k) : (f = s)) : (f = k);
    }
    return f && f.distance * a < ay ? f.guide : null;
  },
  sy = (i, t, n) => vm(i, t, n).filter((s) => i.isGroundNode(s) && Im(s))[0] || null,
  ly = (i, t, n) => {
    let a = wr(i, n);
    return { x: t.x - a.x, y: t.y - a.y };
  },
  Tm = (i, t) => {
    let { guidesX: n, guidesY: a } = i,
      s = _m(n, t.x),
      u = _m(a, t.y);
    return s && (!u || s.distance < u.distance)
      ? { guide: { nodeId: i.id, axis: "x", offset: s.value }, distance: s.distance }
      : u
        ? { guide: { nodeId: i.id, axis: "y", offset: u.value }, distance: u.distance }
        : null;
  };
function _m(i, t) {
  let n = null,
    a = 1 / 0;
  return (
    i.forEach((s) => {
      let u = dy(s, t);
      u < a && ((a = u), (n = s));
    }),
    ie(n) ? { value: n, distance: a } : null
  );
}
var dy = (i, t) => Math.abs(i - t),
  Im = (i) => (Nn(i) && i.resolveValue("rotation") !== 0 ? !1 : Pm(i)),
  uy = (i, t) => {
    let n = i.getNode(t.nodeId);
    return n && Im(n) ? n : null;
  },
  cy = (i) => (i === "x" ? "guidesX" : "guidesY"),
  Wb = (i, t) => i[cy(t)],
  Db = (i, t) => (!i || !t ? !1 : $e(i, t)),
  Nb = (i, t) => {
    let n = uy(i, t);
    if (!n) return null;
    let { axis: a, offset: s } = t;
    return Cm(i, n, a, s);
  },
  Rb = (i, t, n, a) => {
    let s = wr(i, t);
    return a - s[n];
  },
  Fb = (i) => (i === "x" ? "y" : "x");
var za = { isTarget: !1 },
  fy = "isTarget";
function Vb(i) {
  return fy in i;
}
var my = "codeOverrides";
function Eb(i) {
  return i ? my in i : !1;
}
var Oa = { codeOverrides: void 0 };
function Ab(i) {
  let t = {},
    n = Object.keys(i.controls);
  for (let a = 0, s = n.length; a < s; a++) {
    let u = n[a],
      f = i.controls[u];
    $c(f.type, f.value) && (t[u] = f.value);
  }
  return t;
}
var wm = { dataSourceLoaderId: void 0 },
  hy = "dataSourceLoaderId";
function py(i) {
  return hy in i;
}
function Ob(i) {
  return !!i && py(i) && !!i.dataSourceLoaderId;
}
var Ha = { dataNosnippet: void 0 },
  Gb = { dataNosnippet: !0 },
  gy = "dataNosnippet";
function $b(i) {
  return gy in i;
}
function qb(i) {
  return i.dataNosnippet === !0;
}
var Zb = "moduleSourceRevisionHint",
  Qb = "moduleSourceRevisionCommittedHint",
  Ve = {
    moduleSourceRevision: void 0,
    moduleSourceRevisionHint: void 0,
    moduleSourceRevisionCommittedHint: void 0,
  },
  yy = "moduleSourceRevision";
function Sy(i) {
  return yy in i;
}
function xy(i, t) {
  return Kt(i) ? !1 : vy(i, t);
}
function vy(i, t) {
  return !(
    !Sy(i) ||
    (lo(i) && ao.isOn("cmsDatabase")) ||
    (so(i) && !fd(t.get(i.parentid))) ||
    (uo(i) && !ha(i))
  );
}
var by = "moduleSourceRevisionHint";
function eC(i, t) {
  return (
    i.moduleSourceRevisionHint !== i.moduleSourceRevisionCommittedHint && t.some((n) => by in n.to)
  );
}
var Cy = ti("ModuleSourceRevision");
function pt(i, t) {
  if (!t.inEditor || !t.processingLocalUserEdits || t.applyingDiffs || !xy(i, t)) return;
  let n = t.getNodeAtStart(i.id),
    s = Math.max(i.moduleSourceRevisionHint ?? 0, n?.moduleSourceRevisionHint ?? 0) + 1;
  (i.setProp("moduleSourceRevisionHint", s),
    Cy.trace("Incrementing moduleSourceRevisionHint for", i.id, ":", s));
}
var Wm = { overlayGrid: void 0 },
  Ty = "overlayGrid";
function nC(i) {
  return i ? Ty in i : !1;
}
function oC(i, t) {
  let n = i.type === "column" ? t.width : t.height;
  switch (i.distribution) {
    case "stretch":
      return _y(i, n);
    case "center":
      return ky(i, n);
    case "left":
    case "right":
      return Py(i, n);
    default:
      Pe(i.distribution);
  }
}
function _y(i, t) {
  let n = i.margin || 0,
    a = t - (n || 0) * 2,
    s = (i.count - 1) * i.gap,
    u = Math.max((a - s) / i.count, 1),
    f = (a - (u * i.count + s)) / 2;
  return fl({ grid: i, itemSize: u, freeSpace: f });
}
function ky(i, t) {
  let n = i.size || 0,
    a = (i.count - 1) * i.gap + i.count * n + (i.margin ?? 0) * 2,
    s = (t - a) / 2;
  return fl({ grid: i, itemSize: n, freeSpace: s });
}
function Py(i, t) {
  let n = i.size || 0,
    a = (i.count - 1) * i.gap + i.count * n + (i.margin ?? 0) * 2,
    s = i.distribution === "left" ? 0 : t - a;
  return fl({ grid: i, itemSize: n, freeSpace: s });
}
function fl({ grid: i, freeSpace: t, itemSize: n }) {
  let a = [],
    s = t;
  a.push(Math.round(t));
  let u = i.count + i.count - 1 + 2;
  for (let f = 0; f < u; f++) {
    let g = Iy(s, f, i.margin || 0, n, i.gap, f === u - 1);
    (a.push(Math.round(g)), (s = g));
  }
  return a;
}
function Iy(i, t, n, a, s, u) {
  return t === 0 || u ? i + n || 0 : t % 2 !== 0 ? i + a : i + s;
}
var wy = () => "rgba(0, 153, 255, 0.2)";
function aC(i, t) {
  let n = { identifier: t || Dd(), type: "column", size: 50, color: wy() };
  return i >= 1200
    ? { ...n, count: 12, margin: 15, size: 65, gap: 30, distribution: "center" }
    : i >= 992
      ? { ...n, count: 12, margin: 15, size: 50, gap: 30, distribution: "center" }
      : i >= 768
        ? { ...n, count: 6, margin: 15, size: 90, gap: 30, distribution: "center" }
        : i >= 576
          ? { ...n, count: 6, margin: 15, size: 60, gap: 30, distribution: "center" }
          : { ...n, count: 3, margin: 15, size: 65, gap: 30, distribution: "stretch" };
}
var dC = "*",
  Dm = { pageEffects: void 0 },
  Wy = "pageEffects";
function Dy(i) {
  return Wy in i;
}
function uC(i) {
  return !!(Dy(i) && i.pageEffects && !Mo(i.pageEffects));
}
var Nm = { dataRepeaterArray: void 0 },
  Ny = "dataRepeaterArray";
function Ry(i) {
  return Ny in i;
}
function fC(i) {
  return !!i && Ry(i) && !!i.dataRepeaterArray;
}
var Rm = { isScreen: void 0 },
  Fy = "isScreen";
function By(i) {
  return Fy in i;
}
function Fm(i) {
  return By(i) && i.isScreen === !0;
}
var Bm = { hideScrollbars: void 0 },
  Vy = "hideScrollbars";
function pC(i) {
  return Vy in i;
}
var Ga = { tapHighlightColor: void 0 },
  yC = { tapHighlightColor: "rgba(0, 0, 0, 0)" },
  Ly = "tapHighlightColor";
function SC(i) {
  return Ly in i;
}
var $a = { textSelection: void 0 },
  bC = { textSelection: { color: "#0099FF", backgroundColor: "rgba(0, 153, 255, 0.15)" } },
  My = "textSelection";
function CC(i) {
  return My in i;
}
function TC(i) {
  return !(!oo(i) || !pe(i.color) || !pe(i.backgroundColor));
}
var Vm = We(() => ({
  ..._c,
  ...Qo,
  ...Hc,
  ...Xo,
  ...Ko,
  ...ia,
  ...jo,
  ...ea,
  ...Zo,
  ...ta,
  ...na,
  ...Yo,
  ...aa,
  ...da,
  ...oa,
  ...sa,
  ...la,
  ...ra,
  ...Jo,
  ...yr,
  ...Wi,
  ...er,
  ...Pr,
  ..._i,
  ...Oa,
  ...qu,
  ...wm,
  ...qc,
  ...Vi,
  ...Li,
  ...Zc,
  ...Qc,
  ...Nm,
  ...uu,
  ...ua,
  ...ca,
  ...Ni,
  ...Nd,
  ...Xc,
  ...Id,
  ...mo,
  ...Dc,
  ...pi,
  ...km,
  ...Kc,
  ...fa,
  ...kc,
  ...Bd,
  ...ef,
  ...Uc,
  ...Wr,
  ...ma,
  ...Ri,
  ...Ve,
  ...Dr,
  ...Ir,
  ...zo,
  ...Wm,
  ...pa,
  ...yi,
  ...Dm,
  ...ga,
  ...fi,
  ...Mi,
  ...kr,
  ...Oc,
  ...bt,
  ...Si,
  ...Nt,
  ...mu,
  ...Bi,
  ...Rm,
  ...Bm,
  ...Lo,
  ...mi,
  ...gr,
  ...ya,
  ...Sa,
  ...Ga,
  ...za,
  ...ui,
  ...$a,
  ...Vd,
  ...Ho,
  ...xa,
  ...$d,
  ...Ai,
  ...Xd,
  ...du,
  ...va,
  ...Nr,
  ...ba,
  ...Ca,
  ...Ha,
  ...Di,
  ...im,
  ...gc,
  ...Ei,
  ...Pi,
}));
function Lr(i, t, n) {
  if (ie(i)) return i;
  if (!pe(i)) return n;
  let a = i.split(" "),
    s;
  for (let f = t; f >= 0; f -= 2) {
    let g = a[f];
    if (pe(g)) {
      s = g;
      break;
    }
  }
  s ??= a[0];
  let u = parseFloat(s);
  return ie(u) ? u : n;
}
function Ua(i) {
  let t = Lr(i, 0, 0);
  return Math.max(0, t);
}
function v_(i) {
  let t = Lr(i, 1, 0);
  return Math.max(0, t);
}
function qa(i) {
  let [t, n] = qe(i);
  return !(Number.isNaN(t) || n !== "px");
}
function b_(i) {
  let [t, n] = qe(i, "px");
  if (!Number.isNaN(t) && n === "px") return t;
}
function Ey(i) {
  if (!pe(i)) return !1;
  let t = i.split(" ");
  return t.length !== 2 ? qa(i) : t.every((n) => qa(n));
}
function C_(i) {
  if (!pe(i)) return !1;
  let t = i.split(" ");
  return t.length !== 1 && t.length !== 2 && t.length !== 4 ? !1 : t.every((n) => qa(n));
}
function T_(i) {
  return i.perSide
    ? {
        all: 0,
        perSide: !0,
        top: i.top ?? 0,
        right: i.right ?? 0,
        bottom: i.bottom ?? 0,
        left: i.left ?? 0,
      }
    : { all: i.all ?? 0, perSide: !1, top: 0, right: 0, bottom: 0, left: 0 };
}
function __(i) {
  if (i.perSide === !1) return `${i.all ?? 0}px`;
  let t = i.top ?? 0,
    n = i.right ?? 0,
    a = i.bottom ?? 0,
    s = i.left ?? 0;
  return t === n && t === a && t === s ? `${t}px` : `${t}px ${n}px ${a}px ${s}px`;
}
function k_(i) {
  return i.length === 1 ? `${i[0]}px` : `${i[0]}px ${i[1]}px`;
}
function P_(i) {
  return i.length === 1
    ? { all: i[0], perSide: !1, top: void 0, right: void 0, bottom: void 0, left: void 0 }
    : {
        all: void 0,
        perSide: !0,
        top: i[0],
        right: i[1],
        bottom: i[2] ?? i[0],
        left: i[3] ?? i[1],
      };
}
function I_(i) {
  if (qa(i)) {
    let [u] = qe(i);
    return { all: u, perSide: !1, top: void 0, right: void 0, bottom: void 0, left: void 0 };
  }
  if (Ey(i)) {
    let u = Lr(i, 0, 0),
      f = Lr(i, 1, 0);
    return u === f
      ? { all: u, perSide: !1, top: void 0, right: void 0, bottom: void 0, left: void 0 }
      : { all: void 0, perSide: !0, top: u, right: f, bottom: u, left: f };
  }
  let t = Lr(i, 0, 0),
    n = Lr(i, 1, 0),
    a = Lr(i, 2, 0),
    s = Lr(i, 3, 0);
  return t === n && t === a && t === s
    ? { all: t, perSide: !1, top: void 0, right: void 0, bottom: void 0, left: void 0 }
    : { all: void 0, perSide: !0, top: t, right: n, bottom: a, left: s };
}
function w_(i) {
  return i.perSide === !1
    ? (i.all ?? 0) === 0
    : (i.top ?? 0) === 0 && (i.right ?? 0) === 0 && (i.bottom ?? 0) === 0 && (i.left ?? 0) === 0;
}
var Lm = Ne((i, t) => {
  let n = si(i);
  if (((t.columnMasonryLayoutEnabled = n || void 0), !n)) return;
  let a = ie(i.gridColumnCount) ? Math.max(Math.floor(i.gridColumnCount), 1) : 1,
    s = le(i.gap) ? void 0 : Ua(i.gap),
    u = ir(i);
  (mc(i) && (t.itemsOrder = i.itemsOrder),
    (t.trackCount = a),
    (t.rowGap = s),
    (t.parentIsDataRepeater = u));
});
var Mm = Ne((i, t, n) => {
  fo(i) &&
    ((t.action = fe(n) && i.formSaveId ? `${Ay()}/forms/v1/forms/${i.formSaveId}/submit` : void 0),
    (t.redirectUrl = zy(i, n)),
    (t.nodeId = i.getPrimaryId()),
    (t.submitTrackingId = Oy(i, n)));
});
function Ay() {
  return Ul().api;
}
function zy(i, t) {
  return oi(i.formOnSuccessRedirectUrl)
    ? t?.valueTransforms?.createComputedValue(i.formOnSuccessRedirectUrl, i, void 0)
    : t?.links?.create(i.formOnSuccessRedirectUrl);
}
function Oy(i, t) {
  return t?.variables && Te(i.formSubmitTrackingId)
    ? t?.variables.create(i.formSubmitTrackingId)
    : i.resolveValue("formSubmitTrackingId", t?.variableOverrides);
}
var Em = Ne((i, t, n) => {
  if (!ar(i) || !zc(i) || vo(i)) return;
  if (!i.name || xf(i.name)) {
    t.name = t["data-framer-name"] = void 0;
    return;
  }
  t["data-framer-name"] = i.name;
  let a = n.scopeType === "CanvasPage",
    s = Gr(i) && !i.isVectorInstance;
  (a || s) && !t.name && (t.name = i.name);
});
var Am = de((i, t, n) => {
  if (pr(i)) {
    if (!Cn(i)) {
      J(t.display) && (t.display = void 0);
      return;
    }
    Object.assign(t, fe(n) ? Hy(i, n) : Gy(i, n));
  }
});
function Hy(i, t) {
  return {
    display: "flex",
    flexDirection: Tn(
      le(i.stackDirection)
        ? t.cssVariableDefinitions.create(i, "stackDirection")
        : (i.resolveValue("stackDirection") ?? bn)
    ),
    justifyContent: Bs(
      le(i.stackDistribution)
        ? t.cssVariableDefinitions.create(i, "stackDistribution")
        : i.resolveValue("stackDistribution")
    ),
    alignContent: _n(
      le(i.stackAlignment)
        ? t.cssVariableDefinitions.create(i, "stackAlignment")
        : i.resolveValue("stackAlignment")
    ),
    alignItems: _n(
      le(i.stackAlignment)
        ? t.cssVariableDefinitions.create(i, "stackAlignment")
        : i.resolveValue("stackAlignment")
    ),
    flexWrap: i.stackWrapEnabled ? "wrap" : "nowrap",
  };
}
function Gy(i, t) {
  return {
    display: "flex",
    flexDirection: Tn(i.resolveValue("stackDirection", t.variableOverrides) ?? bn),
    justifyContent: Bs(i.resolveValue("stackDistribution", t.variableOverrides)),
    alignContent: _n(i.resolveValue("stackAlignment", t.variableOverrides)),
    alignItems: _n(i.resolveValue("stackAlignment", t.variableOverrides)),
    flexWrap: i.stackWrapEnabled ? "wrap" : "nowrap",
  };
}
var zm = de((i, t, n) => {
  if (!pr(i)) return;
  if (Cn(i) && !Fs(i)) {
    t.gap = void 0;
    return;
  }
  if (le(i.gap) && fe(n)) {
    t.gap = n.cssVariableDefinitions.create(i, "gap");
    return;
  }
  let a = i.resolveValue("gap", n.variableOverrides);
  pe(a) ? (t.gap = a) : ie(a) ? (t.gap = `${Math.max(0, a)}px`) : (t.gap = void 0);
});
function mk(i, t) {
  if (!ai(i)) return;
  if (Cn(i) && !Fs(i)) return "0px";
  if (le(i.gap) && fe(t)) return t.cssVariableDefinitions.createNegativeColumnGap(i, i.gap);
  let n = i.resolveValue("gap"),
    a = Ua(n);
  return bo`-${a}px`;
}
function $y(i) {
  return jd(i) ? le(i.gridItemColumnSpan) || le(i.gridItemRowSpan) : !1;
}
function Om(i) {
  return ir(i) || !ie(i.gridColumnCount) ? !0 : (i.children?.some($y) ?? !1);
}
function Uy(i) {
  return ie(i.gridColumnCount) && i.gridColumnCount > 0
    ? String(Math.round(i.gridColumnCount))
    : "auto-fill";
}
function Xy(i) {
  return i.gridColumnWidthType === "fixed"
    ? `${i.gridColumnWidth}px`
    : `minmax(${i.gridColumnMinWidth ?? 0}px, 1fr)`;
}
function Hm(i) {
  switch (i.gridRowHeightType) {
    case "fixed":
      return `${i.gridRowHeight ?? 0}px`;
    case "fit":
      return "min-content";
    case "auto":
    case void 0:
      return "minmax(0, 1fr)";
    default:
      return (i.gridRowHeightType, "minmax(0, 1fr)");
  }
}
function Ky(i) {
  let t = {};
  return (
    (t.gridAutoRows = Hm(i)),
    ie(i.gridRowCount) && !Om(i) && (t.gridTemplateRows = `repeat(${i.gridRowCount}, ${Hm(i)})`),
    t
  );
}
var Gm = {
    alignItems: void 0,
    alignContent: void 0,
    display: void 0,
    gridTemplateColumns: void 0,
    justifyContent: void 0,
  },
  $m = de((i, t) => {
    if (!pr(i)) return;
    if (!go(i)) {
      J(t.display) && (t.display = void 0);
      return;
    }
    if (si(i)) {
      let a = { ...Gm, display: "grid" };
      Object.assign(t, a);
      return;
    }
    let n = {
      ...Gm,
      display: "grid",
      justifyContent: i.gridAlignment,
      gridTemplateColumns: `repeat(${Uy(i)}, ${Xy(i)})`,
      ...Ky(i),
    };
    Object.assign(t, n);
  });
var qm = de((i, t, n) => {
  if (!jc(i)) return;
  if (le(i.imageRendering) && fe(n)) {
    t.imageRendering = n.cssVariableDefinitions.create(i, "imageRendering");
    return;
  }
  let a = i.resolveValue("imageRendering", n.variableOverrides);
  t.imageRendering = Jc(a) ? a : void 0;
});
var Um = { lineEnabled: !0, lineColor: "#ddd", lineWidth: 2, lineRadius: "1px" },
  Yy = "lineEnabled";
function Ln(i) {
  return Yy in i;
}
var Mr = de((i, t, n) => {
  if (!yu(i)) return;
  let a = { padding: void 0 };
  (ai(i) || Rt(i) || Ut(i) || It(i)
    ? Jy(i, n, a)
    : n.renderAsListNodes?.has(i.id) && (a.padding = 0),
    Object.assign(t, jy(a, i)));
});
function jy(i, t) {
  return Rt(t) ? { "--framer-input-padding": i.padding } : i;
}
function Jy(i, t, n) {
  if (le(i.padding))
    n.padding = fe(t)
      ? t.cssVariableDefinitions.create(i, "padding")
      : i.resolveValue("padding", t.variableOverrides);
  else {
    let a = Va(i);
    (Ln(i) && i.lineEnabled && (a.left = a.left + i.lineWidth), (n.padding = kn(a)));
  }
}
var Zy = /\d+(?:px)?$/u;
function Xm(i, t) {
  return (
    typeof i == "string" && Zy.test(i) && (i = parseFloat(i)),
    typeof i == "number" && !Number.isNaN(i) ? i : t
  );
}
var Km = de((i, t, n) => {
  if (!li(i) || !pr(i) || !i.children?.length) return;
  let a = { 3: 0, 1: 0, 0: 0, 2: 0, 5: 0, 4: 0 },
    s = 0;
  for (let u of i.children)
    !Jd(u) ||
      !lu(u) ||
      !u.resolveValue("visible") ||
      (i.stackDirection === "vertical"
        ? (a[u.heightType] += u.height)
        : i.stackDirection === "horizontal" && (a[u.widthType] += u.width),
      s++);
  a[3] <= 0 ||
    ((t["--ticker-total-fr"] = a[3]),
    (t["--ticker-total-children"] = s),
    (t["--ticker-gap"] =
      fe(n) && le(i.gap)
        ? n.createDeduplicatedDynamicValueBinding(i.gap, i, "gap")
        : Ue(Xm(i.resolveValue("gap"), 0), "px")),
    (a[1] > 0 || a[0] > 0) &&
      (t["--ticker-total-not-fr"] = `calc(${Ue(a[1] * 100, "%")} + ${Ue(a[0], "px")})`));
});
function Ym(i) {
  let t;
  return () => (t || (t = { returnValue: i() }), t.returnValue);
}
var Mn = class extends Fa {
    constructor(n = Of({ cache: () => new hl() })) {
      super();
      this.nonRepeatedChildDelegate = n;
    }
    nonRepeatedChildDelegate;
    arraySourceCache;
    getArraySourceCache() {
      return (this.arraySourceCache ??= new Xf());
    }
    getCombinedVariableValueMap(n, a, s, u, f) {
      return this.nonRepeatedChildDelegate && Et(s, u, f)
        ? this.nonRepeatedChildDelegate.cache.getCombinedVariableValueMap(n, a, s, u, f)
        : super.getCombinedVariableValueMap(n, a, s, u, f);
    }
    setCombinedVariableValueMap(n, a, s, u, f, g) {
      if (this.nonRepeatedChildDelegate && Et(u, f, g)) {
        this.nonRepeatedChildDelegate.cache.setCombinedVariableValueMap(n, a, s, u, f, g);
        return;
      }
      super.setCombinedVariableValueMap(n, a, s, u, f, g);
    }
    getCombinedVariableControlMap(n, a, s, u, f) {
      return this.nonRepeatedChildDelegate && Et(s, u, f)
        ? this.nonRepeatedChildDelegate.cache.getCombinedVariableControlMap(n, a, s, u, f)
        : super.getCombinedVariableControlMap(n, a, s, u, f);
    }
    setCombinedVariableControlMap(n, a, s, u, f, g) {
      if (this.nonRepeatedChildDelegate && Et(u, f, g)) {
        this.nonRepeatedChildDelegate.cache.setCombinedVariableControlMap(n, a, s, u, f, g);
        return;
      }
      super.setCombinedVariableControlMap(n, a, s, u, f, g);
    }
    getDataSourceVariableMap(n, a, s, u, f) {
      return this.nonRepeatedChildDelegate && Et(n, a, s)
        ? this.nonRepeatedChildDelegate.cache.getDataSourceVariableMap(n, a, s, u, f)
        : super.getDataSourceVariableMap(n, a, s, u, f);
    }
    getDataSourceValueMap(n, a, s, u, f, g) {
      return this.nonRepeatedChildDelegate && Et(n, a, s)
        ? this.nonRepeatedChildDelegate.cache.getDataSourceValueMap(n, a, s, u, f, g)
        : super.getDataSourceValueMap(n, a, s, u, f, g);
    }
    getDataSourceControlMap(n, a, s, u, f) {
      return this.nonRepeatedChildDelegate && Et(n, a, s)
        ? this.nonRepeatedChildDelegate.cache.getDataSourceControlMap(n, a, s, u, f)
        : super.getDataSourceControlMap(n, a, s, u, f);
    }
    computeDataSourceVariableMap(n, a) {
      let { variableMap: s, controlMap: u } = super.computeDataSourceVariableMap(n, a);
      return ir(n) || cu(n)
        ? { variableMap: new $f(s), controlMap: new Uf(u) }
        : { variableMap: s, controlMap: u };
    }
    computeDataSourceValueMap(n, a, s, u, f) {
      let g = super.computeDataSourceValueMap(n, a, s, u, f);
      return ir(n) ? new qf(g, 0, f) : g;
    }
    retrieveArrayValues(n, a, s, u, f, g, k) {
      return this.getArraySourceCache().retrieveArrayValues(n, a, s, u, f, g, k);
    }
    getArraySourceVariableMap(n, a, s, u, f, g) {
      return this.nonRepeatedChildDelegate && Et(n, a, s)
        ? this.nonRepeatedChildDelegate.cache.getArraySourceVariableMap(n, a, s, u, f, g)
        : this.getArraySourceCache().getArraySourceVariableMap(n, a, s, u, f, g);
    }
    getArraySourceValueMap(n, a, s, u, f, g, k) {
      return this.nonRepeatedChildDelegate && Et(n, a, s)
        ? this.nonRepeatedChildDelegate.cache.getArraySourceValueMap(n, a, s, u, f, g, k)
        : this.getArraySourceCache().getArraySourceValueMap(n, a, s, u, f, g, k);
    }
    getArraySourceVariableControlMap(n, a, s, u, f, g) {
      return this.nonRepeatedChildDelegate && Et(n, a, s)
        ? this.nonRepeatedChildDelegate.cache.getArraySourceVariableControlMap(n, a, s, u, f, g)
        : this.getArraySourceCache().getArraySourceVariableControlMap(n, a, s, u, f, g);
    }
  },
  Xa = Ym(function () {
    return { variableMap: new Hf(Vf), controlMap: new Gf(Lf) };
  }),
  hl = class extends Mn {
    constructor() {
      super(!1);
    }
    valuesCache;
    getDataSourceVariableMap(t, n, a, s, u) {
      return Xa().variableMap;
    }
    getDataSourceValueMap(t, n, a, s, u, f) {
      let g = s.getData(t.id)?.length ?? 0;
      if (this.valuesCache?.cacheKey.itemCount === g) return this.valuesCache.valueMap;
      let k = new tl(el, g);
      return ((this.valuesCache = { valueMap: k, cacheKey: { itemCount: g } }), k);
    }
    getDataSourceControlMap(t, n, a, s, u) {
      return Xa().controlMap;
    }
    getArraySourceVariableMap(t, n, a, s, u, f) {
      return Xa().variableMap;
    }
    getArraySourceValueMap(t, n, a, s, u, f, g) {
      let k = this.retrieveArrayValues(t, n, a, s, u, f, g)?.length ?? 0;
      if (this.valuesCache?.cacheKey.itemCount === k) return this.valuesCache.valueMap;
      let F = new tl(el, k);
      return ((this.valuesCache = { valueMap: F, cacheKey: { itemCount: k } }), F);
    }
    getArraySourceVariableControlMap(t, n, a, s, u, f) {
      return Xa().controlMap;
    }
  };
var pl = class extends Ie("FrameNode", Vt(Ra(Yi(De)))) {
  constructor(t) {
    (super(t, new Mn()), we(this, Vm(), t));
  }
  updateForRect({
    rect: t,
    parentSizeInfo: n,
    constraintsLocked: a,
    shouldGuessPinToBottom: s,
    shouldGuessPinToRight: u,
    updateSize: f,
  }) {
    let g = Ii({
      rect: t,
      parentSizeInfo: n,
      currentConstraints: this.constraints(),
      constraintsLocked: a,
      pixelAlign: this.__unsafeIsGroundNode() || !this.usesDOMRectCached(),
      shouldGuessPinToBottom: s,
      shouldGuessPinToRight: u,
      updateSize: f,
      floatingPosition: ho(this) ? Rd(this) : null,
    });
    return (t.width !== this.width || t.height !== this.height) && this.framePreset !== null
      ? { ...g, framePreset: null }
      : g;
  }
  updateForSize(t, n) {
    let a = wi(t, n, this.constraintValues());
    return this.framePreset !== null ? { ...a, framePreset: null } : a;
  }
  getChildrenInVisualOrder() {
    if (J(this.layout)) return this.children.toArray();
    if (!ci(this)) return this.children.toArray();
    if (!hc(this)) return this.children.toArray();
    if (li(this)) return this.children.toArray();
    if (this.cache.childrenInVisualOrder && !this.mutable) return this.cache.childrenInVisualOrder;
    let t = new Map();
    for (let a of this.children) t.set(a.getPrimaryId(), a);
    let n = [];
    return (
      this.itemsOrder.forEach((a) => {
        let s = t.get(a);
        s && (n.push(s), t.delete(a));
      }),
      t.size > 0 &&
        t.forEach((a) => {
          n.push(a);
        }),
      (this.cache.childrenInVisualOrder = n),
      n
    );
  }
  static updateNodes;
  getProps(t, n, a, s) {
    let u = { display: "block" },
      f = {
        ...super.getProps(t),
        ...this.newConstraintProperties(),
        _border: Tc(this),
        visible: this.resolveValue("visible", n),
        style: u,
      };
    (this.hideScrollbars === !0 && (f["data-hide-scrollbars"] = !0),
      Fr(this, f),
      this.__unsafeIsGroundNode() &&
        Kt(this) &&
        f._constraints &&
        (ie(f._constraints.intrinsicWidth) &&
          !ie(f.width) &&
          (f.width = f._constraints.intrinsicWidth),
        ie(f._constraints.intrinsicHeight) &&
          !ie(f.height) &&
          (f.height = f._constraints.intrinsicHeight)),
      this.__unsafeIsSlotPropertyChildNode() && (f._constraints = void 0));
    let g = { componentLoader: t, variableOverrides: n, getResolvedFetchDataValue: s };
    return (
      Pc(this, f, !0, g, a),
      Em(this, f, g),
      Na(this, u, g),
      Gi(this, u, g),
      Ki(this, u, g),
      zi(this, u, g),
      Rr(this, u, g),
      Ui(this, u, g),
      $m(this, u, g),
      Mr(this, u, g),
      Am(this, u, g),
      zm(this, u, g),
      Lm(this, f, g),
      Oi(u, f),
      qm(this, u, g),
      $i(this, f),
      tf(this, f, g),
      Xi(this, u, g),
      Mm(this, f, g),
      Oo(this, f),
      uf(this, u, g),
      this.cache.isEffectPreview && qi(this, u),
      Pa(this, u),
      Ta(this, f, g),
      Sf(this, f, g),
      gf(this, u, g),
      Km(this, u, g),
      f
    );
  }
  getVariableSourceIdentifier() {
    if (this.dataIdentifier) return this.dataIdentifier;
    if (this.repeatArray)
      return this.getVariableSourceIdentifierFromArrayProvider(this.repeatArray);
    if (this.dataRepeaterArray)
      return this.getVariableSourceIdentifierFromArrayProvider(this.dataRepeaterArray);
  }
  getProvidedVariableMap(t, n, a, s) {
    if (this.repeatArray)
      return this.cache.getArraySourceVariableMap(this, t, n, a, this.repeatArray, s);
    if (vi(this)) return this.cache.getDataSourceVariableMap(this, t, n, a, s);
    if (this.dataRepeaterArray)
      return this.cache.getArraySourceVariableMap(this, t, n, a, this.dataRepeaterArray, s);
  }
  getProvidedValueMap(t, n, a, s, u) {
    if (this.repeatArray)
      return this.cache.getArraySourceValueMap(this, t, n, a, this.repeatArray, s, u);
    if (vi(this)) return this.cache.getDataSourceValueMap(this, t, n, s, a, u);
    if (this.dataRepeaterArray)
      return this.cache.getArraySourceValueMap(this, t, n, a, this.dataRepeaterArray, s, u);
  }
  getProvidedControlMap(t, n, a, s) {
    if (this.repeatArray)
      return this.cache.getArraySourceVariableControlMap(this, t, n, a, this.repeatArray, s);
    if (vi(this)) return this.cache.getDataSourceControlMap(this, t, n, a, s);
    if (this.dataRepeaterArray)
      return this.cache.getArraySourceVariableControlMap(this, t, n, a, this.dataRepeaterArray, s);
  }
  getVariableSourceIdentifierFromArrayProvider(t) {
    return Bf(this, t.providerId)?.getVariableSourceIdentifier();
  }
  getActions() {
    let t = {};
    for (let n of vn(this)) {
      let a = this[n];
      if (!a || a.length === 0) {
        t[n] = void 0;
        continue;
      }
      t[n] = Ti(a);
    }
    return t;
  }
  hasActions() {
    return vn(this).some((t) => {
      let n = this[t];
      if (n && n.length > 0) return !0;
    });
  }
  collectCSS(t) {
    if (!dd(this)) return;
    let n = Zf(this);
    Object.assign(t, n);
  }
  preFreeze(t) {
    (t.processingLocalUserEdits &&
      t.inEditor &&
      this.itemsOrder &&
      (ci(this)
        ? pc(this) || (this.resetToDefault("itemsOrder"), Ao.resetItemsOrder(t, this))
        : this.resetToDefault("itemsOrder")),
      this.keepLatestSizeForEmptyAutoSizedStack(),
      pt(this, t));
  }
  keepLatestSizeForEmptyAutoSizedStack() {
    if (
      !(this.widthType === 2 || this.heightType === 2) ||
      this.children.some((u) => u.isVisible())
    )
      return;
    let a = this.getDOMRect();
    if (!a) return;
    let s = { width: this.width, height: this.height };
    (this.widthType === 2 && (s.width = a.width),
      this.heightType === 2 && (s.height = a.height),
      this.set({ ...s }));
  }
  getLink() {
    return Lu(this.link);
  }
  supportsDynamicValues() {
    return !0;
  }
  supportsPluginData() {
    return !0;
  }
};
function Zi(i) {
  return ii(i) || Wt(i) || Ut(i) || It(i) || Xt(i) || Dt(i);
}
function gl(i) {
  return Zi(i) && Ud(i);
}
function m0(i) {
  return Zi(i) && Kt(i);
}
var Sl = (i, t, n = "Rectangle") => {
  if (Ds(t, !0)) return "Smart Component";
  if (cd(t, !0)) return "Web Page";
  if (md(t, !0)) return "Canvas Page";
  if (ud(t, !0)) return "Template";
  if (Kf(t, !0)) return "Scope";
  if (yd(t)) return t.originalFilename || "Icon";
  if (hd(t)) return t.text.replace(/\s+/gu, " ").trim();
  if (nr(t)) return t.getTextContent().replace(/\s+/gu, " ").trim();
  if (xd(t)) return "Path";
  if (fo(t)) return "Form";
  if (vd(t)) return "Input";
  if (or(t)) return t.formBooleanInputType === "radio" ? "Radio" : "Checkbox";
  if (co(t)) return "Select";
  if (kd(t)) return "Funnel";
  if (Pd(t)) return "Any";
  let a = t.tree();
  if (so(t)) {
    if (ho(t)) return "Overlay";
    if (Kd(t)) {
      if (vo(t)) return "Breakpoint";
      if (Yd(t) && a) {
        let u = a.getScopeNodeFor(t);
        if (Ds(u)) {
          let f = u.getTopLevelVariantForGesture(t);
          return f.resolveValue("name") || Sl(i, f);
        }
      }
      return "Variant";
    }
    if (li(t)) return "Ticker";
    let s = tm(t.framePreset);
    if (s && (!t.cache.treeLineage || (a && !a.getParent(t.id)))) return s;
    if (Yc(t)) return "Label";
    if (ai(t)) {
      if (ir(t)) {
        if (gn(t)) {
          if (!a) return "Collection List";
          let f = sr(a, t.dataIdentifier)?.resolveValue("name");
          return f ? zf(f) : "Collection List";
        }
        if (Ws(t)) return a ? jm(a, t, "Array") : "Array";
        Pe(t);
      }
      if (a && Au(a, t)) {
        let u = Eu(a, t);
        if (u) {
          if (gn(u)) {
            let g = sr(a, u.dataIdentifier)?.resolveValue("name");
            return g ? Af(g) : "Collection Item";
          }
          if (Ws(u)) return jm(a, u, "Array Item", (f) => `${f} Item`);
          Pe(u);
        }
      }
      return si(t) ? "Masonry" : go(t) ? "Grid" : "Stack";
    }
    return Fm(t) ? "Screen" : t.children.length === 0 && au(t) ? "Image" : "Frame";
  }
  if (uo(t)) return ha(t) ? "Vector" : "Viewbox";
  if ($r(t)) return nm(t.pathBoolean);
  if (qr(t)) return "Group";
  if (Sn(t)) return "Star";
  if (xn(t)) return "Polygon";
  if (ri(t)) return "Oval";
  if (yn(t)) return "Rectangle";
  if (pd(t)) return i.shaderForIdentifier(t.shaderModuleIdentifier)?.title || "Shader";
  if (Gr(t)) return i.componentForIdentifier(t.codeComponentIdentifier)?.name ?? "Component";
  if (Sd(t)) return "Collection";
  if (gd(t)) return "Overlay";
  if (Td(t)) return "Placeholder";
  if (_d(t)) {
    let s = po(t.slotPropertyKey);
    if (!a) return s;
    let u = a.getParent(t.id);
    if (!Gr(u)) return s;
    let f = i.reactComponentForIdentifier(u.codeComponentIdentifier);
    if (!f) return s;
    let g = f.properties[t.slotPropertyKey];
    return g?.type !== "slot" ? s : g.title || s;
  }
  return bd(t) ? "Presets List" : Zi(t) ? "Style Preset" : ld(t) ? "Root" : n;
};
function jm(i, t, n, a) {
  let s = i.getNode(t.dataRepeaterArray.providerId);
  if (!vi(s)) return n;
  let f = sr(i, s.dataIdentifier)?.variables.find((g) => g.id === t.dataRepeaterArray.id);
  return f ? (pn(a) ? a(f.name) : f.name) : n;
}
function Ee(i, t) {
  return Br(
    i,
    {
      text(n) {
        let a = t.text?.(n) ?? n;
        return $e(a, n) ? n : a;
      },
      textBlock(n) {
        let a = t.textBlock?.(n) ?? n;
        return $e(a, n) ? n : a;
      },
    },
    { normalize: !1 }
  );
}
function eh(i, t, n) {
  return Ee(i, {
    text(a) {
      return $e(a.inlineStyles?.[t], n) ? a : { ...a, inlineStyles: { ...a.inlineStyles, [t]: n } };
    },
    textBlock(a) {
      return a.childNodes.some((s) => s.type === "text")
        ? a
        : {
            ...a,
            childNodes: [
              ...a.childNodes,
              { id: Qt(), type: "text", text: "", inlineStyles: { [t]: n } },
            ],
          };
    },
  });
}
function th(i, t, n) {
  let a = !1;
  return (
    dr(i, {
      text(s) {
        s.text && "backgroundStyles" in s && s.backgroundStyles && (a = !0);
      },
    }),
    Ee(i, {
      text(s) {
        if (!s.text || s.link || s.inlineCode || (a && !s.backgroundStyles)) return s;
        let u = { ...s.backgroundStyles, [t]: n };
        return { ...s, backgroundStyles: Tr(u) ? void 0 : u };
      },
    })
  );
}
function rh(i, t, n, a) {
  return Ee(i, {
    text(s) {
      if (!s.text || s.link || s.inlineCode || s.backgroundStyles?.[t] !== n) return s;
      let u = { ...s.backgroundStyles, [t]: a };
      return { ...s, backgroundStyles: Tr(u) ? void 0 : u };
    },
  });
}
function Qy(i) {
  return Te(i) || pe(i) || ie(i) || Sr(i);
}
function Ka(i, t) {
  return Xe(i.variant, "styles")
    ? Br(
        i,
        {
          text(n) {
            return sS(n, t);
          },
        },
        { normalize: !1 }
      )
    : i;
}
function xl(i, t) {
  return Xe(i.variant, "variables")
    ? Ka(i, (n) => {
        if (!Te(n)) return;
        let a = t(n);
        if (!J(a))
          return (
            ee(
              Qy(a),
              () =>
                `mapRichTextDocumentVariableReferences: expected a style value, got ${JSON.stringify(a)}`
            ),
            a
          );
      })
    : i;
}
function ih(i, t) {
  return Xe(i.variant, "links")
    ? Br(
        i,
        {
          text(n) {
            return tS(n, t);
          },
        },
        { normalize: !1 }
      )
    : i;
}
function J0(i, t) {
  return !Xe(i.variant, "links") || !Xe(i.variant, "variables")
    ? i
    : Br(
        i,
        {
          text(n) {
            return rS(n, t);
          },
        },
        { normalize: !1 }
      );
}
function Z0(i) {
  return Br(
    i,
    {
      document: eS,
      textBlock: At,
      bulletedList: At,
      numberedList: At,
      listItem: At,
      blockquote: At,
      media: Jm,
      module: At,
      table: At,
      tableRow: At,
      tableCell: At,
      text: Jm,
      lineBreak: At,
    },
    { normalize: !1 }
  );
}
function nh(i, t) {
  return t.size === 0
    ? i
    : Ka(i, (n, a) => {
        if (a !== "textColor" || !ut(n)) return;
        let s = Wo(n);
        if (s) return t.get(s);
      });
}
function oh(i, t) {
  return t === 1 || !Xe(i.variant, "styles")
    ? i
    : Br(
        i,
        {
          textBlock(n) {
            return iS(n, t);
          },
          text(n) {
            return nS(n, t);
          },
        },
        { normalize: !1 }
      );
}
function ah(i, t) {
  return Xe(i.variant, "styles")
    ? Br(
        i,
        {
          text(n) {
            if (!Kr(n) || !n.link || !n.linkStylePreset) return n;
            let a = t(n.linkStylePreset);
            return !a || a === n.linkStylePreset ? n : { ...n, linkStylePreset: a };
          },
        },
        { normalize: !1 }
      )
    : i;
}
function Q0(i, t) {
  return Tu(i)
    ? !!Rn(i, {
        text(n) {
          return Kr(n) ? Qm(n.inlineStyles, t) || Qm(n.backgroundStyles, t) : !1;
        },
      })
    : !1;
}
function eS(i) {
  return { ...i, id: Qt() };
}
function At(i) {
  return { ...i, id: Qt() };
}
function Jm(i) {
  let t = At(i);
  return t.link?.id ? { ...t, link: { ...t.link, id: Qt() } } : t;
}
function tS(i, t) {
  if (!i.link) return i;
  let n = t(i.link.link);
  return !n || n === i.link.link ? i : { ...i, link: { ...i.link, link: n } };
}
function rS(i, t) {
  if (!i.link) return i;
  let n;
  for (let a of Cu) {
    let s = i.link[a];
    if (!Te(s)) continue;
    let u = t(s);
    J(u) || u === s || (n = { ...(n ?? i.link), [a]: u });
  }
  return n ? { ...i, link: n } : i;
}
function iS(i, t) {
  if (!_u(i)) return i;
  let n = i.blockStyles?.lineHeight;
  if (n?.[1] !== "px") return i;
  let a = [n[0] * t, "px"];
  return a[0] === n[0] ? i : { ...i, blockStyles: { ...i.blockStyles, lineHeight: a } };
}
function nS(i, t) {
  let n = Kr(i) ? i.inlineStyles : void 0,
    a = oS(n?.fontSize, t),
    s = aS(n?.letterSpacing, t),
    u = { ...n },
    f = !1;
  return (
    !J(a) && !Object.is(a, n?.fontSize) && ((u.fontSize = a), (f = !0)),
    !J(s) && !Object.is(s, n?.letterSpacing) && ((u.letterSpacing = s), (f = !0)),
    f ? { ...i, inlineStyles: u } : i
  );
}
function oS(i, t) {
  let n = i ?? qe(ze.fontSize);
  if (!Sr(n)) return i;
  let [a, s] = n;
  return [$t(a * t, To[s]), s];
}
function aS(i, t) {
  return i?.[1] !== "px" ? i : [$t(i[0] * t, 2), "px"];
}
function sS(i, t) {
  if (!Kr(i)) return i;
  let n = Zm(i.inlineStyles, t),
    a = Zm(i.backgroundStyles, t);
  return n === i.inlineStyles && a === i.backgroundStyles
    ? i
    : { ...i, inlineStyles: n, backgroundStyles: a };
}
function Zm(i, t) {
  if (!i) return i;
  let n,
    a = i;
  for (let s in a) {
    let u = a[s];
    if (J(u)) continue;
    let f = t(u, s);
    J(f) || Object.is(f, u) || (n = { ...(n ?? i), [s]: f });
  }
  return n ?? i;
}
function Qm(i, t) {
  if (!i) return !1;
  let n = i;
  for (let a in n) {
    let s = n[a];
    if (typeof s == "string" && s.includes(t)) return !0;
  }
  return !1;
}
function Ya(i, t = Ze) {
  let n = i.$from,
    a = n.parent,
    s = a.childAfter(n.parentOffset);
  if (!s.node) return i.from;
  let u = s.node.marks.find(t);
  if (u === void 0) return i.from;
  let f = n.start() + s.offset;
  for (let g = n.index() - 1; g >= 0; g--) {
    let k = a.child(g);
    if (!u.isInSet(k.marks)) break;
    f -= k.nodeSize;
  }
  return f;
}
function ja(i, t = Ze) {
  let n = i.$to,
    a = n.parent,
    s = a.childBefore(n.parentOffset);
  if (!s.node) return i.to;
  let u = s.node.marks.find(t);
  if (u === void 0) return i.to;
  let f = n.start() + s.offset + s.node.nodeSize;
  for (let g = n.indexAfter(); g < a.childCount; g++) {
    let k = a.child(g);
    if (!u.isInSet(k.marks)) break;
    f += k.nodeSize;
  }
  return f;
}
function sh(i, t, n) {
  let a = !1,
    s = t.tr;
  return (
    t.doc.descendants((u, f) => {
      if (!He(u)) return;
      let g = u.marks.find(Ze);
      if (J(g) || J(g.attrs.link)) return;
      let k = n(g.attrs.link);
      k && ((a = !0), s.addMark(f, f + u.nodeSize, i.marks.link.create({ ...g.attrs, link: k })));
    }),
    a ? s : null
  );
}
var lS = /<([a-z1-9]+)/gi;
function uI(i) {
  let t = new Set();
  return (dS(i, t), t);
}
function lh(i) {
  return typeof i == "string"
    ? i.includes("</blockquote>")
    : Rn(i, { blockquote: () => !0 }) !== void 0;
}
function dh(i) {
  return typeof i == "string"
    ? i.includes("</a>")
    : Rn(i, { text: (t) => t.link !== void 0 }) !== void 0;
}
function dS(i, t) {
  if (Oe(i)) return uS(i, t);
  let n = i.matchAll(lS);
  for (let [, a] of n) a && t.add(a);
  return t;
}
function uS(i, t) {
  return (
    dr(i, {
      document(n) {},
      textBlock(n) {
        t.add(n.tag);
      },
      bulletedList(n) {
        t.add("ul");
      },
      numberedList(n) {
        t.add("ol");
      },
      listItem(n) {
        t.add("li");
      },
      blockquote(n) {
        t.add("blockquote");
      },
      media(n) {
        switch (n.mediaType) {
          case "image":
            t.add("img");
            break;
          case "video":
            t.add("video");
            break;
          default:
            Pe(n.mediaType);
        }
      },
      module(n) {},
      table(n) {
        t.add("table");
      },
      tableRow(n) {
        t.add("tr");
      },
      tableCell(n) {
        t.add(n.cellType === "header" ? "th" : "td");
      },
      text(n) {
        (n.bold && t.add("strong"),
          n.italic && t.add("em"),
          n.inlineCode && t.add("code"),
          n.link && t.add("a"));
      },
      lineBreak(n) {
        t.add("br");
      },
    }),
    t
  );
}
var cr = {
    linkFont: void 0,
    linkTextColor: void 0,
    linkFontSize: void 0,
    linkTextTransform: void 0,
    linkTextDecoration: void 0,
    linkTextDecorationColor: void 0,
    linkTextDecorationThickness: void 0,
    linkTextDecorationStyle: void 0,
    linkTextDecorationSkipInk: void 0,
    linkTextDecorationOffset: void 0,
    linkTextBackgroundColor: void 0,
    linkTextBackgroundRadius: void 0,
    linkTextBackgroundCornerShape: void 0,
    linkTextBackgroundPadding: void 0,
    linkHoverFont: void 0,
    linkHoverTextColor: void 0,
    linkHoverFontSize: void 0,
    linkHoverTextTransform: void 0,
    linkHoverTextDecoration: void 0,
    linkHoverTextDecorationColor: void 0,
    linkHoverTextDecorationThickness: void 0,
    linkHoverTextDecorationStyle: void 0,
    linkHoverTextDecorationSkipInk: void 0,
    linkHoverTextDecorationOffset: void 0,
    linkHoverTextBackgroundColor: void 0,
    linkHoverTextBackgroundRadius: void 0,
    linkHoverTextBackgroundCornerShape: void 0,
    linkHoverTextBackgroundPadding: void 0,
    linkCurrentFont: void 0,
    linkCurrentTextColor: void 0,
    linkCurrentFontSize: void 0,
    linkCurrentTextTransform: void 0,
    linkCurrentTextDecoration: void 0,
    linkCurrentTextDecorationColor: void 0,
    linkCurrentTextDecorationThickness: void 0,
    linkCurrentTextDecorationStyle: void 0,
    linkCurrentTextDecorationSkipInk: void 0,
    linkCurrentTextDecorationOffset: void 0,
    linkCurrentTextBackgroundColor: void 0,
    linkCurrentTextBackgroundRadius: void 0,
    linkCurrentTextBackgroundCornerShape: void 0,
    linkCurrentTextBackgroundPadding: void 0,
  },
  cS = "linkFont";
function Qi(i) {
  return cS in i;
}
var vl = ["link", "linkHover", "linkCurrent"];
function Tt(i, t) {
  return `${i}${t}`;
}
var uh = {
    stylePresetHeading1: void 0,
    stylePresetHeading2: void 0,
    stylePresetHeading3: void 0,
    stylePresetHeading4: void 0,
    stylePresetHeading5: void 0,
    stylePresetHeading6: void 0,
    stylePresetParagraph: void 0,
  },
  pI = Fe(uh);
function An(i) {
  switch (i) {
    case "h1":
      return "stylePresetHeading1";
    case "h2":
      return "stylePresetHeading2";
    case "h3":
      return "stylePresetHeading3";
    case "h4":
      return "stylePresetHeading4";
    case "h5":
      return "stylePresetHeading5";
    case "h6":
      return "stylePresetHeading6";
    case "p":
      return "stylePresetParagraph";
    default:
      Pe(i);
  }
}
var ch = { stylePresetLink: void 0, stylePresetAnchorLink: void 0 },
  fS = { stylePresetBlockquote: void 0 },
  mS = { stylePresetTable: void 0 },
  gI = Fe(ch),
  fh = { stylePresetInlineCode: void 0 },
  hS = { stylePresetImage: void 0 },
  yI = Fe(fh),
  zn = { ...uh, ...ch, ...fS, ...mS, ...fh, ...hS },
  On = Fe(zn),
  pS = "stylePresetHeading1";
function mh(i) {
  return pS in i;
}
var gS = "textVerticalAlignment",
  hh = { textVerticalAlignment: "top" };
function yS(i) {
  return gS in i;
}
function CI(i) {
  return yS(i) ? (su(i) ? !0 : Zd(i) && i.heightType !== 2) : !1;
}
var ph = We(() => ({
  ...Qo,
  ...Xo,
  ...Ko,
  ...ia,
  ...jo,
  ...ea,
  ...Zo,
  ...ta,
  ...na,
  ...Yo,
  ...aa,
  ...da,
  ...oa,
  ...sa,
  ...la,
  ...ra,
  ...Jo,
  ...yr,
  ...Wi,
  ..._i,
  ...Vi,
  ...Li,
  ...ua,
  ...ca,
  ...Ni,
  ...mo,
  ...pi,
  ...fa,
  ...cr,
  ...Wr,
  ...ma,
  ...Ri,
  ...Dr,
  ...Ir,
  ...zo,
  ...pa,
  ...Wa,
  ...ga,
  ...fi,
  ...Mi,
  ...kr,
  ...lc,
  ...Bi,
  ...Lo,
  ...$a,
  ...Iu,
  ...mi,
  ...gr,
  ...ya,
  ...zn,
  ...Sa,
  ...Ga,
  ...za,
  ...df,
  ...nf,
  ...Jr,
  ...hh,
  ..._a,
  ...Ho,
  ...xa,
  ...Ai,
  ...va,
  ...Nr,
  ...ba,
  ...Ca,
  ...Ha,
  ...Di,
  ...Ei,
  ...Pi,
  ...cf,
}));
var en = { fontBold: void 0, fontBoldItalic: void 0, fontItalic: void 0 },
  SS = "fontBold";
function gh(i) {
  return SS in i;
}
var xS = ["p", "h1", "h2", "h3", "h4", "h5", "h6"];
function yh(i) {
  return xS.includes(i);
}
var vS = "tag";
function $w(i) {
  return vS in i;
}
var Sh = { tag: "p" };
function qw(i, t) {
  switch (i) {
    case "h1":
      return t ? "H1" : "Heading 1";
    case "h2":
      return t ? "H2" : "Heading 2";
    case "h3":
      return t ? "H3" : "Heading 3";
    case "h4":
      return t ? "H4" : "Heading 4";
    case "h5":
      return t ? "H5" : "Heading 5";
    case "h6":
      return t ? "H6" : "Heading 6";
    case "p":
      return t ? "p" : "Paragraph";
    default:
      Pe(i);
  }
}
var bl = {
  font: "Inter-Italic",
  fontBold: "Inter-Bold",
  fontBoldItalic: "Inter-BoldItalic",
  fontItalic: "Inter-Italic",
  textColor: "#999",
  fontSize: 16,
  letterSpacing: [0, "em"],
  lineHeight: [1.8, "em"],
  paragraphSpacing: 20,
  paddingPerSide: !0,
  radius: 0,
  paddingLeft: 20,
  fillEnabled: !1,
};
var Ja = { breakpointWidth: void 0 },
  bS = "breakpointWidth";
function CS(i) {
  return bS in i;
}
function Yw(i) {
  return CS(i) && ie(i.breakpointWidth);
}
var Cl = We(() => ({
  ...Si,
  ...bt,
  ...Nt,
  ...Po,
  ...er,
  ...ze,
  ...ui,
  ...Ja,
  ...en,
  ...Um,
  ...Wc,
  ...Wa,
  ...Ve,
  font: void 0,
  textColor: void 0,
  textTransform: void 0,
  textDecoration: void 0,
  textAlignment: void 0,
  textStyleEnabled: !0,
}));
var tn = de((i, t, n) => {
    if (!(!js(i) || !i.borderEnabled || _r(i)) && !fe(n)) {
      if (le(i.borderEnabled)) {
        let a = i.resolveValue("borderEnabled", n.variableOverrides);
        if (vc(a)) {
          if (a.borderPerSide)
            ((t.borderTopWidth = a.borderTop),
              (t.borderBottomWidth = a.borderBottom),
              (t.borderLeftWidth = a.borderLeft),
              (t.borderRightWidth = a.borderRight));
          else {
            let s = a.borderWidth;
            t.borderTopWidth = t.borderBottomWidth = t.borderLeftWidth = t.borderRightWidth = s;
          }
          ((t.borderStyle = a.borderStyle),
            (t.borderColor = ut(a.borderColor) ? Jt(a.borderColor) : i.borderColor));
        } else Cc(a) && Object.assign(t, a);
        return;
      }
      if (i.borderPerSide)
        ((t.borderTopWidth = i.borderTop),
          (t.borderBottomWidth = i.borderBottom),
          (t.borderLeftWidth = i.borderLeft),
          (t.borderRightWidth = i.borderRight));
      else {
        let a = i.borderWidth;
        t.borderTopWidth = t.borderBottomWidth = t.borderLeftWidth = t.borderRightWidth = a;
      }
      ((t.borderStyle = i.borderStyle),
        (t.borderColor = Mt(n)
          ? n.colors.resolveColor(i.borderColor)
          : ut(i.borderColor)
            ? Jt(i.borderColor)
            : i.borderColor));
    }
  }),
  TS = {
    borderColor: "--border-color",
    borderStyle: "--border-style",
    borderTopWidth: "--border-top-width",
    borderBottomWidth: "--border-bottom-width",
    borderLeftWidth: "--border-left-width",
    borderRightWidth: "--border-right-width",
  },
  _S = {
    borderColor: "--framer-input-border-color",
    borderStyle: "--framer-input-border-style",
    borderTopWidth: "--framer-input-border-top-width",
    borderBottomWidth: "--framer-input-border-bottom-width",
    borderLeftWidth: "--framer-input-border-left-width",
    borderRightWidth: "--framer-input-border-right-width",
  },
  xh = de((i, t, n) => {
    if (!js(i) || Gr(i)) return;
    let a = {},
      {
        borderStyle: s,
        borderColor: u,
        borderTop: f,
        borderPerSide: g,
        borderBottom: k,
        borderLeft: F,
        borderRight: A,
        borderWidth: R,
      } = bc(i),
      $ = Rt(i) ? _S : TS;
    if (le(i.borderEnabled)) {
      if (!fe(n)) return;
      let q = n.createDeduplicatedDynamicValueBinding(i.borderEnabled, i, "borderEnabled");
      ((a[$.borderColor] = n.cssVariableDefinitions.createCSSVariableFromBinding(
        i,
        "borderColor",
        re`${q}?.borderColor`
      )),
        (a[$.borderStyle] = n.cssVariableDefinitions.createCSSVariableFromBinding(
          i,
          "borderStyle",
          re`${q}?.borderStyle`
        )));
      let Z = re`(${re`${q}?.borderTopWidth ?? ${q}?.borderWidth`}) + "px"`;
      a[$.borderTopWidth] = n.cssVariableDefinitions.createCSSVariableFromBinding(
        i,
        "borderTopWidth",
        Z
      );
      let X = re`(${re`${q}?.borderBottomWidth ?? ${q}?.borderWidth`}) + "px"`;
      a[$.borderBottomWidth] = n.cssVariableDefinitions.createCSSVariableFromBinding(
        i,
        "borderBottomWidth",
        X
      );
      let be = re`(${re`${q}?.borderLeftWidth ?? ${q}?.borderWidth`}) + "px"`;
      a[$.borderLeftWidth] = n.cssVariableDefinitions.createCSSVariableFromBinding(
        i,
        "borderLeftWidth",
        be
      );
      let Q = re`(${re`${q}?.borderRightWidth ?? ${q}?.borderWidth`}) + "px"`;
      a[$.borderRightWidth] = n.cssVariableDefinitions.createCSSVariableFromBinding(
        i,
        "borderRightWidth",
        Q
      );
    } else
      i.borderEnabled
        ? g
          ? ((a[$.borderStyle] = s),
            (a[$.borderColor] = n.cssVariableDefinitions?.resolveColor(u) ?? u),
            (a[$.borderTopWidth] = f + "px"),
            (a[$.borderBottomWidth] = k + "px"),
            (a[$.borderLeftWidth] = F + "px"),
            (a[$.borderRightWidth] = A + "px"))
          : ((a[$.borderStyle] = s),
            (a[$.borderColor] = n.cssVariableDefinitions?.resolveColor(u) ?? u),
            (a[$.borderLeftWidth] =
              a[$.borderRightWidth] =
              a[$.borderTopWidth] =
              a[$.borderBottomWidth] =
                R + "px"))
        : (a[$.borderLeftWidth] =
            a[$.borderRightWidth] =
            a[$.borderTopWidth] =
            a[$.borderBottomWidth] =
              void 0);
    Object.assign(t, a);
  });
function rn(i, t, n, a) {
  if (!i) return;
  let { fontFamily: s, fontStyle: u, fontWeight: f, fontVariationAxes: g } = Ft(i);
  ((t[`${n}-font-family-${a}`] = s),
    (t[`${n}-font-style-${a}`] = u ?? "normal"),
    (t[`${n}-font-weight-${a}`] = f ?? 400),
    (t[`${n}-font-variation-axes-${a}`] = g));
}
var vh = (i) =>
    de((t, n) => {
      gh(t) &&
        (rn(t.fontBold, n, i, "bold"),
        rn(t.fontItalic, n, i, "italic"),
        rn(t.fontBoldItalic, n, i, "bold-italic"));
    }),
  bh = vh("--framer"),
  Ch = vh("--framer-blockquote");
function Th(i) {
  return (t, n) => {
    (rn(t?.fontBold, n, i, "bold"),
      rn(t?.fontItalic, n, i, "italic"),
      rn(t?.fontBoldItalic, n, i, "bold-italic"));
  };
}
var _h = Th("--framer"),
  kh = Th("--framer-blockquote");
var Ph = de((i, t, n) => {
    Ln(i) &&
      ((t.content = '" "'),
      (t.display = i.lineEnabled ? "block" : "none"),
      (t.position = "absolute"),
      (t.width = i.lineWidth),
      (t.height = "100%"),
      (t.top = 0),
      (t.left = 0),
      (t.backgroundColor = Mt(n) ? n.colors.resolveColor(i.lineColor) : i.lineColor),
      (t.borderRadius = i.lineRadius));
  }),
  Ih = de((i, t) => {
    !Ln(i) || !i.lineEnabled || (t.position = "relative");
  });
function wh(i, t, n) {
  let a = Ft(i);
  ((t[`${n}-font-family`] = a.fontFamily),
    (t[`${n}-font-style`] = a.fontStyle ?? "normal"),
    (t[`${n}-font-weight`] = a.fontWeight ?? 400),
    (t[`${n}-font-variation-axes`] = a.fontVariationAxes ?? "normal"));
}
var Wh = (i) => {
    let t = {
      font(n, a) {
        n.font !== void 0 && wh(n.font, a, i);
      },
      textColor(n, a, s) {
        if (n.textColor === void 0) return;
        if (Te(n.textColor) && s?.variables) {
          a[`${i}-text-color`] = s.variables.create(n.textColor);
          return;
        }
        let u = n.resolveValue("textColor") ?? ze.textColor;
        if (
          u !== void 0 &&
          ((a[`${i}-text-color`] = ut(u) ? Jt(u) : st.toFormatString(u)), Hr.isP3String(u))
        ) {
          let f = Or.fromString(u)?.toString("srgb");
          a[`${i}-text-color-rgb`] = f;
        }
      },
      textBackgroundColor(n, a, s) {
        if (n.textBackgroundColor === void 0) return;
        if (Te(n.textBackgroundColor) && s?.variables) {
          a[`${i}-text-background-color`] = s.variables.create(n.textBackgroundColor);
          return;
        }
        let u = n.resolveValue("textBackgroundColor");
        if (
          !(u === void 0 || u === "initial") &&
          ((a[`${i}-text-background-color`] = ut(u) ? Jt(u) : st.toFormatString(u)),
          Hr.isP3String(u))
        ) {
          let f = Or.fromString(u)?.toString("srgb");
          a[`${i}-text-background-color-rgb`] = f;
        }
      },
      textBackgroundRadius(n, a) {
        if (n.textBackgroundRadius === void 0) return;
        let s = `${i}-text-background-corner-shape-fallback`,
          { radius: u, radiusCornerShapeFallbackFactor: f } = As(
            n.textBackgroundRadius,
            n.textBackgroundCornerShape,
            `var(${s},1)`
          );
        ((a[`${i}-text-background-radius`] = u), (a[s] = f));
      },
      textBackgroundCornerShape(n, a) {
        n.textBackgroundCornerShape?.cornerShape !== void 0 &&
          (a[`${i}-text-background-corner-shape`] = zs(n.textBackgroundCornerShape.cornerShape));
      },
      textBackgroundPadding(n, a) {
        n.textBackgroundPadding !== void 0 &&
          (a[`${i}-text-background-padding`] = n.textBackgroundPadding.paddingPerSide
            ? kn({
                top: n.textBackgroundPadding.paddingTop ?? 0,
                right: n.textBackgroundPadding.paddingRight ?? 0,
                bottom: n.textBackgroundPadding.paddingBottom ?? 0,
                left: n.textBackgroundPadding.paddingLeft ?? 0,
              })
            : `${n.textBackgroundPadding.padding}px`);
      },
      fontSize(n, a, s) {
        if (Te(n.fontSize) && s?.variables) {
          let u = s.variables.create(n.fontSize);
          a[`${i}-font-size`] = bo`${u}px`;
        } else {
          let u = n.resolveValue("fontSize") ?? ze.fontSize;
          if (gi(u)) {
            let [f, g] = qe(u);
            a[`${i}-font-size`] = g === "rem" ? Bo(f) : u;
          } else a[`${i}-font-size`] = Ue(u, "px");
        }
      },
      letterSpacing(n, a) {
        a[`${i}-letter-spacing`] = wn(n.letterSpacing).join("");
      },
      textTransform(n, a) {
        a[`${i}-text-transform`] = n.textTransform;
      },
      textDecoration(n, a) {
        a[`${i}-text-decoration`] = n.textDecoration;
      },
      textDecorationColor(n, a) {
        Yt(n.textDecoration) && (a[`${i}-text-decoration-color`] = n.textDecorationColor);
      },
      textDecorationThickness(n, a) {
        Yt(n.textDecoration) && (a[`${i}-text-decoration-thickness`] = n.textDecorationThickness);
      },
      textDecorationStyle(n, a) {
        Yt(n.textDecoration) && (a[`${i}-text-decoration-style`] = n.textDecorationStyle);
      },
      textDecorationSkipInk(n, a) {
        Yt(n.textDecoration) && (a[`${i}-text-decoration-skip-ink`] = n.textDecorationSkipInk);
      },
      textDecorationOffset(n, a) {
        Yt(n.textDecoration) && (a[`${i}-text-decoration-offset`] = n.textDecorationOffset);
      },
      lineHeight(n, a) {
        a[`${i}-line-height`] = n.lineHeight.join("");
      },
      textAlignment(n, a) {
        a[`${i}-text-alignment`] = n.textAlignment;
      },
      textStrokeWidth(n, a) {
        a[`${i}-text-stroke-width`] = hu(n.textStrokeWidth);
      },
      textStrokeColor(n, a) {
        a[`${i}-text-stroke-color`] = n.textStrokeColor;
      },
      openTypeFontFeatures(n, a) {
        a["--framer-font-open-type-features"] = ko(n.openTypeFontFeatures);
      },
    };
    return de((n, a, s) => {
      if (pu(n)) for (let u in ze) t[u](n, a, s);
    });
  },
  Dh = Wh("--framer"),
  Nh = Wh("--framer-blockquote");
function Rh(i) {
  return (t, n) => wh(t, n, i);
}
var Fh = Rh("--framer"),
  Bh = Rh("--framer-blockquote"),
  Za = de((i, t) => {
    if (Qi(i)) for (let n of vl) for (let a in nn) nn[a](i, t, n);
  }),
  Vh = de((i, t) => {
    if (Qi(i)) for (let n of vl) for (let a in nn) nn[a](i, t, n, !0);
  }),
  nn = {
    linkFont(i, t, n, a) {
      let s = Tt(n, "Font"),
        u = gt(n);
      if (J(i[s])) {
        a &&
          ((t[`${u}-font-family`] = "inherit"),
          (t[`${u}-font-style`] = "inherit"),
          (t[`${u}-font-weight`] = "inherit"));
        return;
      }
      let f = Ft(i[s]);
      ((t[`${u}-font-family`] = f.fontFamily),
        (t[`${u}-font-style`] = f.fontStyle ?? "normal"),
        (t[`${u}-font-weight`] = f.fontWeight ?? 400));
    },
    linkTextColor(i, t, n, a) {
      let s = Tt(n, "TextColor");
      if (J(i[s]) && !a) return;
      let u = gt(n);
      if (J(i[s])) {
        ((t[`${u}-text-color`] = "inherit"), (t[`${u}-text-color-rgb`] = "inherit"));
        return;
      }
      if (
        ((t[`${u}-text-color`] = ut(i[s]) ? Jt(i[s]) : st.toFormatString(i[s])),
        Hr.isP3String(i[s]))
      ) {
        let f = Or.fromString(i[s])?.toString("srgb");
        t[`${u}-text-color-rgb`] = f;
      }
    },
    linkFontSize(i, t, n, a) {
      let s = Tt(n, "FontSize");
      if (J(i[s]) && !a) return;
      let u = gt(n);
      t[`${u}-font-size`] = J(i[s]) ? "inherit" : Ue(i[s], "px");
    },
    linkTextTransform(i, t, n, a) {
      let s = Tt(n, "TextTransform");
      if (J(i[s]) && !a) return;
      let u = gt(n);
      t[`${u}-text-transform`] = i[s] ?? "inherit";
    },
    linkTextDecoration(i, t, n, a) {
      let s = Tt(n, "TextDecoration");
      if (J(i[s]) && !a) return;
      let u = gt(n);
      t[`${u}-text-decoration`] = i[s] ?? "initial";
    },
    linkTextDecorationColor(i, t, n, a) {
      let s = Hn(i, n, "TextDecorationColor");
      if (J(s) && !a) return;
      let u = gt(n);
      t[`${u}-text-decoration-color`] = s ?? "initial";
    },
    linkTextDecorationThickness(i, t, n, a) {
      let s = Hn(i, n, "TextDecorationThickness");
      if (J(s) && !a) return;
      let u = gt(n);
      t[`${u}-text-decoration-thickness`] = s ?? "initial";
    },
    linkTextDecorationStyle(i, t, n, a) {
      let s = Hn(i, n, "TextDecorationStyle");
      if (J(s) && !a) return;
      let u = gt(n);
      t[`${u}-text-decoration-style`] = s ?? "initial";
    },
    linkTextDecorationSkipInk(i, t, n, a) {
      let s = Hn(i, n, "TextDecorationSkipInk");
      if (J(s) && !a) return;
      let u = gt(n);
      t[`${u}-text-decoration-skip-ink`] = s ?? "initial";
    },
    linkTextDecorationOffset(i, t, n, a) {
      let s = Hn(i, n, "TextDecorationOffset");
      if (J(s) && !a) return;
      let u = gt(n);
      t[`${u}-text-decoration-offset`] = s ?? "initial";
    },
    linkTextBackgroundColor(i, t, n, a) {
      let s = Tt(n, "TextBackgroundColor");
      if (J(i[s]) && !a) return;
      let u = gt(n);
      if (J(i[s])) {
        ((t[`${u}-text-background-color`] = "initial"),
          (t[`${u}-text-background-color-rgb`] = "initial"));
        return;
      }
      if (
        ((t[`${u}-text-background-color`] = ut(i[s]) ? Jt(i[s]) : st.toFormatString(i[s])),
        Hr.isP3String(i[s]))
      ) {
        let f = Or.fromString(i[s])?.toString("srgb");
        t[`${u}-text-background-color-rgb`] = f;
      }
    },
    linkTextBackgroundRadius(i, t, n, a) {
      let s = i[Tt(n, "TextBackgroundRadius")],
        u = i[Tt(n, "TextBackgroundCornerShape")],
        f = gt(n),
        g = `${f}-text-background-radius`;
      if (J(s) && J(u)) {
        a && (t[g] = "initial");
        return;
      }
      let k = `${f}-text-background-corner-shape-fallback`,
        { radius: F, radiusCornerShapeFallbackFactor: A } = As(
          s ?? i[Tt("link", "TextBackgroundRadius")],
          u,
          `var(${k},1)`
        );
      ((t[g] = F), (t[k] = A));
    },
    linkTextBackgroundCornerShape(i, t, n, a) {
      let s = i[Tt(n, "TextBackgroundCornerShape")];
      if (J(s) && !a) return;
      let u = gt(n);
      t[`${u}-text-background-corner-shape`] = J(s?.cornerShape) ? "initial" : zs(s.cornerShape);
    },
    linkTextBackgroundPadding(i, t, n, a) {
      let s = i[Tt(n, "TextBackgroundPadding")];
      if (J(s) && !a) return;
      let u = gt(n);
      t[`${u}-text-background-padding`] = s
        ? s.paddingPerSide
          ? kn({
              top: s.paddingTop ?? 0,
              right: s.paddingRight ?? 0,
              bottom: s.paddingBottom ?? 0,
              left: s.paddingLeft ?? 0,
            })
          : `${s.padding}px`
        : "initial";
    },
  };
function Hn(i, t, n) {
  let a = i[Tt(t, "TextDecoration")];
  if (Yt(a)) return i[Tt(t, n)];
}
function gt(i) {
  switch (i) {
    case "link":
      return "--framer-link";
    case "linkHover":
      return "--framer-link-hover";
    case "linkCurrent":
      return "--framer-link-current";
  }
}
var Lh = (i, t) => {
  let n = {},
    a = gt("link"),
    s = gt(kS(t));
  for (let u in nn) nn[u](i, n, "link", !0);
  for (let u in n)
    if (u.startsWith(a)) {
      let f = u,
        g = f.replace(a, s);
      if ((t !== "default" ? (n[f] = `var(${g}, ${n[f]})`) : delete n[f], t !== "current")) {
        let k = f.replace(a, "--framer-link-current");
        n[k] = "unset";
      }
    }
  return n;
};
function kS(i) {
  switch (i) {
    case "default":
      return "link";
    case "hover":
      return "linkHover";
    case "current":
      return "linkCurrent";
  }
}
function PS(i, t = 1) {
  return `${"[class]".repeat(t)}${i}`;
}
function Qa(i, t = 1) {
  return i.map((n) => PS(n, t));
}
var IS = ["blwf", "cv03", "cv04", "cv09", "cv11"],
  wS = "initial",
  Mh = {
    font: "Inter",
    fontBold: "Inter-Bold",
    fontBoldItalic: "Inter-BoldItalic",
    fontItalic: "Inter-Italic",
    textColor: "#666",
    fontSize: Ue(16, "px"),
    letterSpacing: [-0.02, "em"],
    lineHeight: [1.6, "em"],
    paragraphSpacing: 20,
    textStrokeColor: "initial",
    textStrokeWidth: wS,
    textWrapBalance: void 0,
    openTypeFontFeatures: IS,
  };
var on = ["blwf", "cv03", "cv04", "cv09", "cv11"],
  Tl = {
    h1: {
      font: "Inter-Bold",
      fontBold: "Inter-Black",
      fontBoldItalic: "Inter-BlackItalic",
      fontItalic: "Inter-BoldItalic",
      textColor: "#000",
      fontSize: Ue(40, "px"),
      letterSpacing: [-0.04, "em"],
      lineHeight: [1.1, "em"],
      paragraphSpacing: 0,
      textStrokeColor: "initial",
      textStrokeWidth: "initial",
      textWrapBalance: void 0,
      openTypeFontFeatures: [...on],
    },
    h2: {
      font: "Inter-Bold",
      fontBold: "Inter-Black",
      fontBoldItalic: "Inter-BlackItalic",
      fontItalic: "Inter-BoldItalic",
      textColor: "#000",
      fontSize: Ue(30, "px"),
      letterSpacing: [-0.04, "em"],
      lineHeight: [1.4, "em"],
      paragraphSpacing: 40,
      textStrokeColor: "initial",
      textStrokeWidth: "initial",
      textWrapBalance: void 0,
      openTypeFontFeatures: [...on],
    },
    h3: {
      font: "Inter-Bold",
      fontBold: "Inter-Black",
      fontBoldItalic: "Inter-BlackItalic",
      fontItalic: "Inter-BoldItalic",
      textColor: "#000",
      fontSize: Ue(26, "px"),
      letterSpacing: [-0.04, "em"],
      lineHeight: [1.4, "em"],
      paragraphSpacing: 40,
      textStrokeColor: "initial",
      textStrokeWidth: "initial",
      textWrapBalance: void 0,
      openTypeFontFeatures: [...on],
    },
    h4: {
      font: "Inter-Bold",
      fontBold: "Inter-Black",
      fontBoldItalic: "Inter-BlackItalic",
      fontItalic: "Inter-BoldItalic",
      textColor: "#000",
      fontSize: Ue(22, "px"),
      letterSpacing: [-0.04, "em"],
      lineHeight: [1.4, "em"],
      paragraphSpacing: 40,
      textStrokeColor: "initial",
      textStrokeWidth: "initial",
      textWrapBalance: void 0,
      openTypeFontFeatures: [...on],
    },
    h5: {
      font: "Inter-Bold",
      fontBold: "Inter-Black",
      fontBoldItalic: "Inter-BlackItalic",
      fontItalic: "Inter-BoldItalic",
      textColor: "#000",
      fontSize: Ue(18, "px"),
      letterSpacing: [-0.04, "em"],
      lineHeight: [1.4, "em"],
      paragraphSpacing: 40,
      textStrokeColor: "initial",
      textStrokeWidth: "initial",
      textWrapBalance: void 0,
      openTypeFontFeatures: [...on],
    },
    h6: {
      font: "Inter-SemiBold",
      fontBold: "Inter-Bold",
      fontBoldItalic: "Inter-BoldItalic",
      fontItalic: "Inter-SemiBoldItalic",
      textColor: "#000",
      fontSize: Ue(16, "px"),
      letterSpacing: [-0.02, "em"],
      lineHeight: [1.4, "em"],
      paragraphSpacing: 40,
      textStrokeColor: "initial",
      textStrokeWidth: "initial",
      textWrapBalance: void 0,
      openTypeFontFeatures: [...on],
    },
    p: Mh,
  };
function aD(i, t) {
  return Tl[i.tag][t] ?? ze[t];
}
var _l = We(() => ({ paragraphSpacing: void 0, ...Ve, ...Sh, ...Ja, ...ui, ...ze, ...en, ..._a }));
function es(i) {
  switch (i) {
    case "h1":
      return "Heading 1";
    case "h2":
      return "Heading 2";
    case "h3":
      return "Heading 3";
    case "h4":
      return "Heading 4";
    case "h5":
      return "Heading 5";
    case "h6":
      return "Heading 6";
    case "p":
      return "Body";
    default:
  }
  return po(i);
}
var Gn = class i extends Ie("TextStylePresetNode", De) {
  constructor(t) {
    super(t);
    let n = _l();
    we(this, n, t);
  }
  static createDefault(t, n) {
    let a = Tl[t];
    return new i({ ...a, ...n, tag: t });
  }
  supportsPluginData() {
    return !0;
  }
  getName() {
    return this.resolveValue("name") || es(this.tag);
  }
  equals(t) {
    let n = _l();
    for (let a of Fe(n)) if (!(a in Ve) && !$e(this[a], t[a])) return !1;
    return !0;
  }
  getFontsForCodeGeneration() {
    return [xi(this.font), this.fontBold, this.fontBoldItalic, this.fontItalic].filter(pe);
  }
  getCSS(t) {
    let n = et(1, this.getPrimaryId()),
      a = [`.${n}:not(.${lt})`, `.${n}.${lt} ${this.tag}`],
      s = {};
    (Dh(this, s, t), Da(this, s, t), hf(this, s, t));
    let u = this.cache.fontPreview;
    u
      ? (ee(Ba(u), "Expected a style preset font preview"),
        Fh(u.fontSelector, s),
        _h(u.variantSelectors, s))
      : bh(this, s, t);
    let f = tt(s);
    return [{ selectors: a, declarations: f }];
  }
  generateCSSForCanvas(t) {
    let n = this.getFontsForCodeGeneration();
    Pt.loadFonts(n).catch(Be);
    let a = et(2, this.id),
      s = [],
      u = { componentLoader: t };
    for (let { selectors: f, declarations: g } of this.getCSS(u))
      ((f = Qa(f, 2)),
        pe(this.originalid) && (f = f.map((k) => `.${a} ${k}`)),
        s.push(`${f.join(", ")} { ${g} }`));
    return s;
  }
  getTextStyle() {
    let t = {};
    for (let n of Fe(ze)) t[n] = this[n];
    return t;
  }
  set(t, n, a, s) {
    return (Kt(this) && kl(this, n), super.set(t, n, a, s));
  }
  preFreeze(t) {
    pt(this, t);
  }
};
function kl(i, t) {
  (t || i.tree())?.get(i.replicaInfo?.master)?.asDraft(t);
}
var $n = class i extends Ie("BlockquoteStylePresetNode", De) {
  constructor(t) {
    super(t);
    let n = Cl();
    we(this, n, t);
  }
  static createDefault(t) {
    return new i({ ...bl, ...t });
  }
  getName() {
    return this.resolveValue("name") ?? "Blockquote";
  }
  equals(t) {
    let n = Cl();
    for (let a of Fe(n)) if (!$e(this[a], t[a])) return !1;
    return !0;
  }
  getFontsForCodeGeneration() {
    return [this.font, this.fontBold, this.fontBoldItalic, this.fontItalic].filter(pe);
  }
  getCSS(t) {
    let n = et(1, this.getPrimaryId()),
      a = [`.${n}:not(.${lt})`, `.${n}.${lt} blockquote`],
      s = {};
    if ((tn(this, s, t), this.textStyleEnabled)) {
      (Nh(this, s, t), Df(this, s, t));
      let F = this.cache.fontPreview;
      F
        ? (ee(Ba(F), "Expected a style preset font preview"),
          Bh(F.fontSelector, s),
          kh(F.variantSelectors, s))
        : Ch(this, s, t);
    }
    (Mr(this, s, t), xr(this, s, t), Pf(this, s, t), Ih(this, s, t));
    let u = tt(s),
      f = [`.${n}:not(.${lt})::before`, `.${n}.${lt} blockquote::before`],
      g = {};
    Ph(this, g, t);
    let k = tt(g);
    return [
      { selectors: a, declarations: u },
      { selectors: f, declarations: k },
    ];
  }
  generateCSSForCanvas(t) {
    let n = this.getFontsForCodeGeneration();
    Pt.loadFonts(n).catch(Be);
    let a = et(2, this.id),
      s = [],
      u = { componentLoader: t };
    for (let { selectors: f, declarations: g } of this.getCSS(u))
      ((f = f.map((k) => `[class]${k}`)),
        typeof this.originalid == "string" && (f = f.map((k) => `.${a} ${k}`)),
        s.push(`${f.join(", ")} { ${g} }`));
    return s;
  }
  getBlockquoteStyle() {
    let t = {};
    for (let n of Fe(bl)) t[n] = this[n];
    return t;
  }
  set(t, n, a, s) {
    return (Kt(this) && kl(this, n), super.set(t, n, a, s));
  }
  preFreeze(t) {
    pt(this, t);
  }
};
var Eh = "presets",
  Re = class i extends Ie("PresetsListNode", De) {
    constructor(t) {
      let n = { ...t, id: Eh };
      (super(n), we(this, Ve, n));
    }
    getComponentPresets() {
      return this.children.filter(Ns);
    }
    getStylePresets() {
      return this.children.filter(Zi);
    }
    getTextStylePresets() {
      return this.children.filter(ni);
    }
    getLinkStylePresets() {
      return this.children.filter(Wt);
    }
    getBlockquoteStylePresets() {
      return this.children.filter(Cd);
    }
    getTableStylePresets() {
      return this.children.filter(It);
    }
    getInlineCodeStylePresets() {
      return this.children.filter(Xt);
    }
    getImageStylePresets() {
      return this.children.filter(Dt);
    }
    getCmsPresets() {
      return this.children.filter((t) => Dt(t) || It(t) || Ns(t));
    }
    preFreeze(t) {
      pt(this, t);
    }
    static get(t) {
      return t.get(Eh);
    }
    static ensure(t) {
      let n = i.get(t);
      return n || t.create(i, t.root.id);
    }
    static getNodes(t) {
      return i.get(t)?.children.toArray() ?? Ld();
    }
  };
var WS = /^0+/u,
  DS = /^\d+$/u;
function NS(i) {
  return DS.test(i);
}
function RS(i, t) {
  let n = i.split(t),
    a = n.length;
  if (a === 1) return null;
  let s = n[a - 1];
  if ((ee(pe(s), "Segments must be a string"), s.length === 0 || !NS(s))) return null;
  let u = Number(s);
  if (!ie(u)) return null;
  n.pop();
  let f = s.match(WS);
  return { value: n.join(t), suffixCount: u, suffixMinimumLength: f ? s.length : 0 };
}
function wt(i, t, { startIndex: n = 1, omitCountIfFirst: a = !0, withDash: s = !1 } = {}) {
  let u = typeof t == "function" ? t : ($) => t.has($),
    f = n,
    g = 0,
    k = s ? "-" : " ",
    F = RS(i, k);
  F && ((i = F.value), (f = F.suffixCount), (a = !1), (g = F.suffixMinimumLength));
  let A = f.toString().padStart(g, "0"),
    R = f === 1 && a ? i : `${i}${k}${A}`;
  for (; u(R);) (f++, (A = f.toString().padStart(g, "0")), (R = `${i}${k}${A}`));
  return R;
}
function xN(i, t, { isDivider: n }) {
  return n ? i.trim() : wt(i.replace(/[{}]/gu, "").trim(), t);
}
function Ah(i) {
  let t = Re.get(i);
  return t ? (t.children.find(Ut) ?? null) : null;
}
function ts(i, t) {
  let { treeStore: n } = i.stores;
  if (n.tree.has(t.id)) return;
  let a = Ah(n.tree),
    s = Re.ensure(n.tree);
  (n.tree.insertNode(t, s.id),
    Bt(i) && i.stores.codeGenerationStore.createModuleFromComponentSource(t).catch(Be));
  let u = "stylePresetBlockquote";
  if (!a)
    if (!n.getDataTree())
      i.scheduler.runWithFullyLoadedTree(
        () => {
          let f = n.getLoadedIndexes();
          for (let g of f.richTextNodes)
            g.htmlContent && rt(n.tree, g, u) && g.setNodeBlockquoteStylePreset(t);
        },
        { runInBackground: !0, name: "insertBlockquoteStylePreset" }
      );
    else {
      let f = n.tree.query().whereClassName("RichTextNode").iterate();
      for (let g of f) g.htmlContent && rt(n.tree, g, u) && g.setNodeBlockquoteStylePreset(t);
    }
}
function zh(i) {
  if (Ah(i.tree)) return;
  let n = $n.createDefault();
  ts(i, n);
}
function DN(i, { name: t = "Blockquote", blockquoteStyle: n, onCreate: a } = {}) {
  i.scheduler.processWhenReady(() => {
    let u = Re.get(i.tree)?.getBlockquoteStylePresets() ?? [],
      f = new Set();
    for (let k of u) {
      let F = k.getName();
      f.add(F);
    }
    let g = $n.createDefault({ ...n, name: wt(t, f) });
    (ts(i, g),
      ht("styles_preset_create", { tag: "blockquote", name: g.getName(), id: g.id }),
      a && a(g));
  });
}
var rs = We(() => ({ ...er, ...Pr, ...Ve, ...jt, ...bt, ...Nt }));
var qn = class extends Ie("ImageStylePresetNode", De) {
  constructor(t) {
    super(t);
    let n = rs();
    we(this, n, t);
  }
  getName() {
    return this.resolveValue("name") || "Image";
  }
  equals(t) {
    let n = rs();
    for (let a of Fe(n)) if (!(a in Ve) && !$e(this[a], t[a])) return !1;
    return !0;
  }
  getFontsForCodeGeneration() {
    return [];
  }
  getCSSSelectors() {
    return [`.${et(1, this.id)}`];
  }
  getCSSDeclarations(t) {
    let n = {};
    return (tn(this, n, t), xr(this, n, t), If(this, n, t), tt(n));
  }
  getCSS(t) {
    return [{ selectors: this.getCSSSelectors(), declarations: this.getCSSDeclarations(t) }];
  }
  generateCSSForCanvas(t) {
    let n = [],
      a = { componentLoader: t };
    for (let { selectors: s, declarations: u } of this.getCSS(a))
      n.push(`${s.join(", ")} { ${u} }`);
    return n;
  }
  getImageStyle() {
    let t = {},
      n = rs();
    for (let a of Fe(n)) t[a] = this[a];
    return t;
  }
  preFreeze(t) {
    pt(this, t);
  }
};
function Oh(i) {
  let t = Re.get(i);
  return t ? (t.children.find(Dt) ?? null) : null;
}
function is(i, t) {
  let { treeStore: n } = i.stores;
  if (n.tree.has(t.id)) return;
  let a = Oh(n.tree),
    s = Re.ensure(n.tree),
    u = s.children.findLastIndex((g) => Dt(g));
  (n.tree.insertNode(t, s.id, u),
    Bt(i) && i.stores.codeGenerationStore.createModuleFromComponentSource(t).catch(Be));
  let f = "stylePresetImage";
  if (!a)
    if (!n.getDataTree())
      i.scheduler.runWithFullyLoadedTree(
        () => {
          let g = n.getLoadedIndexes();
          for (let k of g.richTextNodes)
            k.htmlContent && rt(n.tree, k, f) && k.setNodeImageStylePreset(t);
        },
        { runInBackground: !0, name: "insertImageStylePreset" }
      );
    else {
      let g = n.tree.query().whereClassName("RichTextNode").iterate();
      for (let k of g) k.htmlContent && rt(n.tree, k, f) && k.setNodeImageStylePreset(t);
    }
}
function Hh(i) {
  if (Oh(i.tree)) return;
  let n = new qn();
  is(i, n);
}
function lR(i, { name: t = "Image", imageStyle: n, onCreate: a } = {}) {
  i.scheduler.processWhenReady(() => {
    let u = Re.get(i.tree)?.getImageStylePresets() ?? [],
      f = new Set();
    for (let k of u) f.add(k.getName());
    let g = new qn({ ...n, name: wt(t, f) });
    (is(i, g), ht("styles_preset_create", { tag: "img", name: g.getName(), id: g.id }), a && a(g));
  });
}
var ns = {
    ...er,
    ...jt,
    ...bt,
    ...Nt,
    radius: 6,
    inlineCodeFont: "GF;Fragment Mono-regular",
    inlineCodeTextColor: "#333",
    inlineCodeFontSizeScale: 1,
    inlineCodeFillColor: "rgba(0, 0, 0, 0.1)",
    inlineCodePaddingHorizontal: 0.2,
    inlineCodePaddingVertical: 0.1,
  },
  FS = "inlineCodeTextColor";
function Gh(i) {
  return FS in i;
}
var Pl = We(() => ({ ...ns, ...Ve }));
function $h(i, t) {
  let n = Ft(t);
  ((i["--framer-code-font-family"] = n.fontFamily),
    (i["--framer-code-font-style"] = n.fontStyle ?? "normal"),
    (i["--framer-code-font-weight"] = n.fontWeight ?? 400));
}
var qh = de((i, t) => {
    if (Gh(i)) {
      if (
        ($h(t, i.inlineCodeFont),
        (t["--framer-font-size-scale"] = i.inlineCodeFontSizeScale),
        (t["--framer-code-text-color"] = i.inlineCodeTextColor),
        Hr.isP3String(i.inlineCodeTextColor))
      ) {
        let n = Or.fromString(i.inlineCodeTextColor)?.toString("srgb");
        t["--framer-code-text-color-rgb"] = n;
      }
      ((t.backgroundColor = i.inlineCodeFillColor),
        (t.paddingLeft = `${i.inlineCodePaddingHorizontal}em`),
        (t.paddingRight = `${i.inlineCodePaddingHorizontal}em`),
        (t.paddingTop = `${i.inlineCodePaddingVertical}em`),
        (t.paddingBottom = `${i.inlineCodePaddingVertical}em`));
    }
  }),
  Uh = $h;
var Un = class extends Ie("InlineCodeStylePresetNode", De) {
  constructor(t) {
    super(t);
    let n = Pl();
    we(this, n, t);
  }
  getName() {
    return this.resolveValue("name") || "Inline Code";
  }
  equals(t) {
    let n = Pl();
    for (let a of Fe(n)) if (!(a in Ve) && !$e(this[a], t[a])) return !1;
    return !0;
  }
  getFontsForCodeGeneration() {
    return [this.inlineCodeFont];
  }
  getCSSSelectors() {
    let t = et(1, this.id);
    return [`.${t}:not(.${lt})`, `.${t}.${lt} code`];
  }
  getCSSDeclarations(t) {
    let n = {};
    (tn(this, n, t), xr(this, n, t), qh(this, n, t));
    let a = this.cache.fontPreview;
    return (a && Uh(n, a.fontSelector), tt(n));
  }
  getCSS(t) {
    return [{ selectors: this.getCSSSelectors(), declarations: this.getCSSDeclarations(t) }];
  }
  generateCSSForCanvas(t) {
    let n = this.getFontsForCodeGeneration();
    Pt.loadFonts(n).catch(Be);
    let a = [],
      s = { componentLoader: t };
    for (let { selectors: u, declarations: f } of this.getCSS(s))
      a.push(`${u.join(", ")} { ${f} }`);
    return a;
  }
  getInlineCodeStyle() {
    let t = {};
    for (let n of Fe(ns)) t[n] = this[n];
    return t;
  }
  preFreeze(t) {
    pt(this, t);
  }
};
function Xh(i) {
  let t = Re.get(i);
  return t ? (t.children.find(Xt) ?? null) : null;
}
function os(i, t) {
  let { treeStore: n } = i.stores;
  if (n.tree.has(t.id)) return;
  let a = Xh(n.tree),
    s = Re.ensure(n.tree);
  (n.tree.insertNode(t, s.id),
    Bt(i) && i.stores.codeGenerationStore.createModuleFromComponentSource(t).catch(Be));
  let u = "stylePresetInlineCode";
  if (!a)
    if (!n.getDataTree())
      i.scheduler.runWithFullyLoadedTree(
        () => {
          let f = n.getLoadedIndexes();
          for (let g of f.richTextNodes)
            g.htmlContent && rt(n.tree, g, u) && g.setNodeInlineCodeStylePreset(t);
        },
        { runInBackground: !0, name: "insertInlineCodeStylePreset" }
      );
    else {
      let f = n.tree.query().whereClassName("RichTextNode").iterate();
      for (let g of f) g.htmlContent && rt(n.tree, g, u) && g.setNodeInlineCodeStylePreset(t);
    }
}
function Kh(i) {
  if (Xh(i.stores.treeStore.tree)) return;
  let n = new Un();
  os(i, n);
}
function YR(i, { name: t = "Inline Code", inlineCodeStyle: n, onCreate: a } = {}) {
  i.scheduler.processWhenReady(() => {
    let u = Re.get(i.stores.treeStore.tree)?.getInlineCodeStylePresets() ?? [],
      f = new Set();
    for (let k of u) {
      let F = k.getName();
      f.add(F);
    }
    let g = new Un({ ...n, name: wt(t, f) });
    (os(i, g), ht("styles_preset_create", { tag: "code", name: g.getName(), id: g.id }), a && a(g));
  });
}
var Yh = ["default", "hover", "current"];
function jh(i) {
  return `${et(1, i.presetNodeId)}-${i.state}`;
}
var Jh = { linkTextColor: "#0099ff", linkTextDecoration: void 0 };
var Zh = { linkTransition: void 0 },
  BS = "linkTransition";
function Qh(i) {
  return BS in i;
}
function eF(i) {
  return i.type === "tween";
}
var Il = We(() => ({ ...cr, ...Zh, ...Ve }));
var ep = de((i, t) => {
  if (!Qh(i)) return;
  let n = i.linkTransition;
  if (!n) return;
  let a =
      Qi(i) && i.linkTextDecoration && i.linkTextDecoration !== "none"
        ? ",text-decoration-color,text-decoration-thickness,text-underline-offset"
        : "",
    s = Qi(i) && i.linkTextBackgroundColor ? ",background-color,border-radius,corner-shape" : "",
    u = n.ease.join(", ");
  ((t.transitionProperty = `color${a}${s}`),
    (t.transitionDuration = `${n.duration}s`),
    (t.transitionDelay = `${n.delay}s`),
    (t.transitionTimingFunction = `cubic-bezier(${u})`));
});
var Xn = class i extends Ie("LinkStylePresetNode", De) {
  constructor(t) {
    super(t);
    let n = Il();
    we(this, n, t);
  }
  static createDefault(t) {
    return new i({ ...Jh, ...t });
  }
  getName() {
    return this.resolveValue("name") || "Link";
  }
  equals(t) {
    let n = Il();
    for (let a of Fe(n)) if (!(a in Ve) && !$e(this[a], t[a])) return !1;
    return !0;
  }
  getFontsForCodeGeneration() {
    let t = [];
    return (
      this.linkFont && t.push(this.linkFont),
      this.linkHoverFont && t.push(this.linkHoverFont),
      this.linkCurrentFont && t.push(this.linkCurrentFont),
      t
    );
  }
  getCSSSelectors() {
    let t = et(1, this.id);
    return [`.${t}:not(.${lt})`, `.${t}.${lt} a`];
  }
  getCSSDeclarations(t, n) {
    let a = {};
    return (
      n === "canvas" ? Vh(this, a, t) : Za(this, a, t),
      n !== "canvas" && ep(this, a, t),
      tt(a)
    );
  }
  getCSS(t) {
    return [{ selectors: this.getCSSSelectors(), declarations: this.getCSSDeclarations(t) }];
  }
  getActiveLinkStylePresetCSS() {
    let t = [];
    for (let n of Yh) {
      let a = jh({ presetNodeId: this.id, state: n }),
        s = this.getCSSSelectors().map((f) => `.${a} ${f}`),
        u = Lh(this, n);
      t.push({ selectors: s, declarations: tt(u) });
    }
    return t;
  }
  generateCSSForCanvas(t) {
    let n = [],
      a = { componentLoader: t },
      s = this.getCSSSelectors(),
      u = this.getCSSDeclarations(a, "canvas");
    n.push(`${VS(s).join(", ")} { ${u} }`);
    for (let { selectors: f, declarations: g } of this.getActiveLinkStylePresetCSS())
      n.push(`${Qa(f, 2).join(", ")} { ${g} }`);
    return n;
  }
  getLinkStyle() {
    let t = {};
    for (let n of Fe(cr)) {
      let a = this[n];
      t[n] = a ?? cr[n];
    }
    return t;
  }
  preFreeze(t) {
    pt(this, t);
  }
};
function VS(i) {
  return i.map((t) => `[class][class]${t}`);
}
function tp(i) {
  let t = Re.get(i);
  return t ? (t.children.find(Wt) ?? null) : null;
}
function as(i, t) {
  let { treeStore: n } = i.stores;
  if (n.tree.has(t.id)) return;
  let a = tp(n.tree),
    s = Re.ensure(n.tree);
  (n.tree.insertNode(t, s.id),
    Bt(i) && i.stores.codeGenerationStore.createModuleFromComponentSource(t).catch(Be));
  let u = "stylePresetLink";
  if (!a)
    if (!n.getDataTree())
      i.scheduler.runWithFullyLoadedTree(
        () => {
          let f = n.getLoadedIndexes();
          for (let g of f.richTextNodes)
            g.htmlContent && rt(n.tree, g, u) && g.setNodeLinkStylePreset(t);
        },
        { runInBackground: !0, name: "insertLinkStylePreset" }
      );
    else {
      let f = n.tree.query().whereClassName("RichTextNode").iterate();
      for (let g of f) g.htmlContent && rt(n.tree, g, u) && g.setNodeLinkStylePreset(t);
    }
}
function rp(i) {
  if (tp(i.stores.treeStore.tree)) return;
  let n = Xn.createDefault();
  as(i, n);
}
function MF(i, { name: t = "Link", presetOverrides: n, onCreate: a } = {}) {
  i.scheduler.processWhenReady(() => {
    let u = Re.get(i.stores.treeStore.tree)?.getLinkStylePresets() ?? [],
      f = new Set();
    for (let k of u) {
      let F = k.getName();
      f.add(F);
    }
    let g = Xn.createDefault({ ...n, name: wt(t, f) });
    (as(i, g), ht("styles_preset_create", { tag: "a", name: g.getName(), id: g.id }), a && a(g));
  });
}
var wl = {
  padding: 10,
  paddingBottom: 10,
  paddingLeft: 10,
  paddingPerSide: !1,
  paddingRight: 10,
  paddingTop: 10,
  radius: 8,
};
var zF = {
    tableBorderColor: void 0,
    tableBorderInner: void 0,
    tableBorderOuter: void 0,
    tableBorderStyle: void 0,
    tableBorderWidth: void 0,
  },
  LS = "tableBorderColor";
function Kn(i) {
  return LS in i;
}
function MS(i) {
  return (
    i.tableBorderColor !== void 0 && i.tableBorderStyle !== void 0 && i.tableBorderWidth !== void 0
  );
}
function OF(i) {
  return Kn(i) && MS(i);
}
var ip = {
  tableBorderColor: "rgba(153, 153, 153, 0.25)",
  tableBorderInner: "all",
  tableBorderOuter: "all",
  tableBorderStyle: "solid",
  tableBorderWidth: 1,
};
var np = { tableFillColor: void 0, tableHeaderFillColor: "rgba(153, 153, 153, 0.1)" },
  ES = "tableFillColor",
  ss = (i) => ES in i;
var Wl = We(() => ({ ...Ve, ...Po, ...jt, ...bt, ...Nt, ...np, ...ip }));
var op = de((i, t, n) => {
    if (Kn(i)) {
      switch (i.tableBorderOuter) {
        case "all": {
          ((t.borderWidth = i.tableBorderWidth),
            (t.borderStyle = i.tableBorderStyle),
            (t.borderColor = Mt(n)
              ? n.colors.resolveColor(i.tableBorderColor)
              : i.tableBorderColor));
          break;
        }
        case void 0:
          break;
        default:
          Pe(i.tableBorderOuter);
      }
      wo(i) && qt(i.radius) && (t.overflow = "hidden");
    }
  }),
  ap = de((i, t, n) => {
    if (Kn(i))
      switch (i.tableBorderInner) {
        case "all":
        case "vertical": {
          ((t.borderLeftWidth = i.tableBorderWidth),
            (t.borderLeftStyle = i.tableBorderStyle),
            (t.borderLeftColor = Mt(n)
              ? n.colors.resolveColor(i.tableBorderColor)
              : i.tableBorderColor));
          break;
        }
        case "horizontal":
        case void 0:
          break;
        default:
          Pe(i.tableBorderInner);
      }
  }),
  sp = de((i, t, n) => {
    if (Kn(i))
      switch (i.tableBorderInner) {
        case "all":
        case "horizontal": {
          ((t.borderTopWidth = i.tableBorderWidth),
            (t.borderTopStyle = i.tableBorderStyle),
            (t.borderTopColor = Mt(n)
              ? n.colors.resolveColor(i.tableBorderColor)
              : i.tableBorderColor));
          break;
        }
        case "vertical":
        case void 0:
          break;
        default:
          Pe(i.tableBorderInner);
      }
  });
var lp = de((i, t, n) => {
    ss(i) && wo(i) && qt(i.radius) && (t.overflow = "hidden");
  }),
  dp = de((i, t, n) => {
    ss(i) &&
      (t.backgroundColor = Mt(n) ? n.colors.resolveColor(i.tableFillColor) : i.tableFillColor);
  }),
  up = de((i, t, n) => {
    ss(i) &&
      (t.backgroundColor = Mt(n)
        ? n.colors.resolveColor(i.tableHeaderFillColor)
        : i.tableHeaderFillColor);
  });
var Yn = class i extends Ie("TableStylePresetNode", De) {
  constructor(t) {
    super(t);
    let n = Wl();
    we(this, n, t);
  }
  static createDefault(t) {
    return new i({ ...wl, ...t });
  }
  getName() {
    return this.resolveValue("name") || "Table";
  }
  equals(t) {
    let n = Wl();
    for (let a of Fe(n)) if (!$e(this[a], t[a])) return !1;
    return !0;
  }
  getFontsForCodeGeneration() {
    return [];
  }
  getCSS(t) {
    let n = et(1, this.getPrimaryId()),
      a = {};
    (xr(this, a, t), lp(this, a, t), op(this, a, t));
    let s = [`.${n}:not(.${lt})`, `.${n}.${lt} table`],
      u = tt(a),
      f = s.map((oe) => `${oe} th`),
      g = {};
    up(this, g, t);
    let k = tt(g),
      F = s.map((oe) => `${oe} td`),
      A = {};
    (Mr(this, A, t), dp(this, A, t));
    let R = tt(A),
      $ = {};
    sp(this, $, t);
    let q = s.flatMap((oe) => ["td", "th"].map((ct) => `${oe} tr + tr ${ct}`)),
      Z = tt($),
      X = {};
    ap(this, X, t);
    let be = s.flatMap((oe) =>
        ["td + td", "th + th", "td + th", "th + td"].map((ct) => `${oe} ${ct}`)
      ),
      Q = tt(X);
    return [
      { selectors: s, declarations: u },
      { selectors: f.concat(F), declarations: R },
      { selectors: f, declarations: k },
      { selectors: q, declarations: Z },
      { selectors: be, declarations: Q },
    ];
  }
  generateCSSForCanvas(t) {
    let n = [],
      a = { componentLoader: t };
    for (let { selectors: s, declarations: u } of this.getCSS(a))
      n.push(`${s.join(", ")} { ${u} }`);
    return n;
  }
  getTableStyle() {
    let t = {};
    for (let n of Fe(wl)) t[n] = this[n];
    return t;
  }
  preFreeze(t) {
    pt(this, t);
  }
};
function cp(i) {
  let t = Re.get(i);
  return t ? (t.children.find(It) ?? null) : null;
}
function ls(i, t) {
  let { treeStore: n } = i.stores;
  if (n.tree.has(t.id)) return;
  let a = cp(n.tree),
    s = Re.ensure(n.tree);
  (n.tree.insertNode(t, s.id),
    Bt(i) && i.stores.codeGenerationStore.createModuleFromComponentSource(t).catch(Be));
  let u = "stylePresetTable";
  if (!a)
    if (!n.getDataTree())
      i.scheduler.runWithFullyLoadedTree(
        () => {
          let f = n.getLoadedIndexes();
          for (let g of f.richTextNodes)
            g.htmlContent && rt(n.tree, g, u) && g.setNodeTableStylePreset(t);
        },
        { runInBackground: !0, name: "insertTableStylePreset" }
      );
    else {
      let f = n.tree.query().whereClassName("RichTextNode").iterate();
      for (let g of f) g.htmlContent && rt(n.tree, g, u) && g.setNodeTableStylePreset(t);
    }
}
function fp(i) {
  if (cp(i.stores.treeStore.tree)) return;
  let n = Yn.createDefault();
  ls(i, n);
}
function LB(i, { name: t = "Table", tableStyle: n, onCreate: a } = {}) {
  i.scheduler.processWhenReady(() => {
    let u = Re.get(i.stores.treeStore.tree)?.getTableStylePresets() ?? [],
      f = new Set();
    for (let k of u) {
      let F = k.getName();
      f.add(F);
    }
    let g = Yn.createDefault({ ...n, name: wt(t, f) });
    (ls(i, g),
      ht("styles_preset_create", { tag: "table", name: g.getName(), id: g.id }),
      a && a(g));
  });
}
var mp = /^(Heading \d)([a-z]?)$/u,
  Dl = "abcdefghijklmnopqrstuvwxyz";
function hp(i, t) {
  return mp.test(i)
    ? i.replace(mp, (n, a, s) => {
        let u = s ? Dl.indexOf(s) : 1;
        if (!t.has(n)) return n;
        for (let f = u; f < Dl.length; f++) {
          let g = a + Dl[f];
          if (!t.has(g)) return g;
        }
        return a;
      })
    : wt(i, t);
}
function pp(i, t) {
  let n = Re.get(i);
  return n ? (n.getTextStylePresets().find((s) => s.tag === t) ?? null) : null;
}
function AS(i, t) {
  let n = t.getName(),
    a = i.getTextStylePresets(),
    s = a.findLast((g) => g.tag === t.tag && g.getName() <= n);
  if (s) return i.children.indexOf(s) + 1;
  let u = a.find((g) => g.tag === t.tag);
  if (u) return i.children.indexOf(u);
  let f = a.findLast((g) => g.tag < t.tag);
  return f ? i.children.indexOf(f) + 1 : 0;
}
function ds(i, t) {
  let { treeStore: n } = i.stores;
  if (n.tree.has(t.id)) return;
  let a = pp(n.tree, t.tag),
    s = Re.ensure(n.tree),
    u = AS(s, t);
  (n.tree.insertNode(t, s.id, u),
    Bt(i) && i.stores.codeGenerationStore.createModuleFromComponentSource(t).catch(Be));
  let f = An(t.tag);
  if (!a)
    if (!n.getDataTree())
      i.scheduler.runWithFullyLoadedTree(
        () => {
          let g = n.getLoadedIndexes();
          for (let k of g.richTextNodes)
            k.htmlContent && rt(n.tree, k, f) && k.setNodeTextStylePreset(t);
        },
        { runInBackground: !0, name: "insertTextStylePreset" }
      );
    else {
      let g = n.tree.query().whereClassName("RichTextNode").iterate();
      for (let k of g) k.htmlContent && rt(n.tree, k, f) && k.setNodeTextStylePreset(t);
    }
}
function gp(i, t) {
  if (pp(i.stores.treeStore.tree, t)) return;
  let a = Gn.createDefault(t);
  ds(i, a);
}
function eV(i, t, { name: n = es(t), textStyle: a, breakpointOverrides: s, onCreate: u } = {}) {
  i.scheduler.processWhenReady(() => {
    let g = Re.get(i.stores.treeStore.tree)?.getTextStylePresets() ?? [],
      k = new Set();
    for (let A of g) k.add(A.getName());
    let F = Gn.createDefault(t, { ...a, name: hp(n, k) });
    if (
      (ds(i, F),
      ht("styles_preset_create", { tag: F.tag, name: F.getName(), id: F.id }),
      s && s.length > 0)
    ) {
      let A = Re.ensure(i.tree);
      for (let R of s) {
        let $ = i.tree.current(F);
        if (!$) continue;
        (Vs($) || ($ = $.set({ isMaster: !0, replicaInfo: null, originalid: null })),
          ee(Vs($), "Expected preset to be a master node after setting isMaster"));
        let q = Ao.create(i.tree, $, { overrides: { [$.id]: R } });
        i.tree.insertNode(q, A.id);
      }
    }
    u && u(F);
  });
}
function tV(i, t, n) {
  let { selector: a, axesCSS: s } = Fo(t),
    u = i.getFontBySelector(a);
  if (!u) return { font: a };
  let f = No(u),
    g = f ? a : Nu(u),
    k = i.getFontBySelector(g);
  if (!n || !g || !k?.variationAxes) {
    let R = f ? Du(u, Do(s ?? "") ?? {}) : void 0,
      $ = R ? i.getFontBySelector(R) : void 0;
    return {
      font: R ?? a,
      fontBold: $?.selectorBold ?? u.selectorBold,
      fontBoldItalic: $?.selectorBoldItalic ?? u.selectorBoldItalic,
      fontItalic: $?.selectorItalic ?? u.selectorItalic,
    };
  }
  let F = {};
  for (let R of k.variationAxes) F[R.tag] = R.defaultValue;
  let A = Ro(g, F);
  return { font: A, fontBold: A, fontBoldItalic: A, fontItalic: A };
}
function pV(i, t) {
  let n;
  if (i.getDataTree()) n = i.tree.query().whereClassName("RichTextNode").iterate();
  else {
    if (i.tree.getService("loader")) return !1;
    n = i.getLoadedIndexes().richTextNodes;
  }
  for (let a of n) if (a.getStylePresetIdsForCodeGeneration().has(t.id)) return !1;
  return !0;
}
function gV(i, t) {
  for (let n of t) zS(i, n);
}
function zS(i, t) {
  switch (t) {
    case "a":
      return rp(i);
    case "blockquote":
      return zh(i);
    case "table":
      return fp(i);
    case "code":
      return Kh(i);
    case "img":
      return Hh(i);
  }
  if (yh(t)) return gp(i, t);
}
function OS(i) {
  switch (i) {
    case "stylePresetHeading1":
      return "h1";
    case "stylePresetHeading2":
      return "h2";
    case "stylePresetHeading3":
      return "h3";
    case "stylePresetHeading4":
      return "h4";
    case "stylePresetHeading5":
      return "h5";
    case "stylePresetHeading6":
      return "h6";
    case "stylePresetParagraph":
      return "p";
    case "stylePresetLink":
      return "a";
    case "stylePresetAnchorLink":
      return "anchor";
    case "stylePresetInlineCode":
      return "code";
    case "stylePresetImage":
      return "img";
    case "stylePresetBlockquote":
      return "blockquote";
    case "stylePresetTable":
      return "table";
    default:
      Pe(i);
  }
}
function HS(i, t) {
  if (ii(t)) return ds(i, t);
  if (Wt(t)) return as(i, t);
  if (Ut(t)) return ts(i, t);
  if (It(t)) return ls(i, t);
  if (Xt(t)) return os(i, t);
  if (Dt(t)) return is(i, t);
  Pe(t);
}
function yV(i, t) {
  for (let n of t) HS(i, n);
}
function yp(i) {
  let t = {};
  for (let n of On) {
    let a = i[n];
    if (!a) continue;
    let s = OS(n),
      u = et(1, a);
    t[s] = u;
  }
  return t;
}
var Sp = Ne((i, t) => {
  if (!dc(i) || !mh(i) || ((t.stylesPresetsClassNames = void 0), !i.htmlContent)) return;
  let n = yp(i);
  Object.keys(n).length > 0 && (t.stylesPresetsClassNames = n);
});
function GS(i) {
  return !(!nr(i) || mf(i) || i.getFills().some(Uo) || Fd(i.textContent));
}
var xp = Ne((i, t, n) => {
  of(i) &&
    (t.effect =
      sf(i) && GS(i)
        ? fe(n)
          ? qS(i, n)
          : {
              type: i.textEffectType ?? "appear",
              tokenization: i.textEffectTokenization ?? "character",
              trigger: "onMount",
              effect: {},
            }
        : void 0);
});
function $S(i, t) {
  return (n) =>
    Ur(i?.ref)
      ? dt.fn(
          t.declarations.file.dedupe(
            "getTextEffect",
            re`(ref) => (${{ ...n, target: { ref: re`ref`, offset: i.offset } }})`
          ),
          i.ref
        )
      : t.declarations.file.dedupe("textEffect", { ...n, target: i });
}
function qS(i, t) {
  let n = i.textEffectTrigger !== "onMount" ? i.textEffectThreshold : void 0,
    a = i.textEffectTrigger === "onScrollTarget" && i.textEffectScrollTarget,
    s = $S(
      a
        ? t.elementIds.scrollTargetInfo({
            target: i.textEffectScrollTarget,
            collectionItem: i.textEffectScrollTargetCollectionItem,
          })
        : void 0,
      t
    );
  if (i.textEffectType === "appear") {
    let u = t.declarations.file.dedupe("animation", {
        filter: i.textEffectBlur ? `blur(${i.textEffectBlur}px)` : void 0,
        opacity: ie(i.textEffectOpacity) ? Math.max(0.001, i.textEffectOpacity) : void 0,
        rotate: i.textEffectRotate,
        rotateX: i.textEffectRotate3d ? i.textEffectRotateX : void 0,
        rotateY: i.textEffectRotate3d ? i.textEffectRotateY : void 0,
        scale: i.textEffectScale,
        skewX: i.textEffectSkewX,
        skewY: i.textEffectSkewY,
        x: i.textEffectX,
        y: i.textEffectY,
      }),
      f = t.declarations.file.dedupe("transition", qd(i.textEffectTransition ?? af));
    return s({
      type: "appear",
      effect: u,
      transition: f,
      threshold: n,
      repeat: i.textEffectReplay,
      tokenization: i.textEffectTokenization,
      trigger: i.textEffectTrigger ?? "onMount",
      startDelay: i.textEffectDelay,
    });
  } else Pe(i.textEffectType);
}
var vp = Ne((i, t, n) => {
  if (!ff(i)) return;
  let a = i.textFitViewBoxSize ? Zs(i.textFitViewBoxSize) : void 0;
  ((t.viewBox = a),
    (t.viewBoxScale = i.textFitScale !== 1 ? i.textFitScale : void 0),
    fe(n) &&
      (t.viewBox = n.localizedValues.add(a, i.textFitViewBoxSizeLocalized, (s) => {
        let u = i.textFitViewBoxSizeLocalized?.[s];
        if (u) return Zs(u);
      })));
});
function bp(i, t) {
  let n = i.cache.fontPreview;
  if (!n) return;
  let a = Ft(n.fontSelector);
  (a.fontFamily && (t["--framer-font-family-preview"] = a.fontFamily),
    a.fontStyle && (t["--framer-font-style-preview"] = a.fontStyle),
    a.fontWeight !== void 0 && (t["--framer-font-weight-preview"] = a.fontWeight),
    a.fontVariationAxes && (t["--framer-font-variation-axes-preview"] = a.fontVariationAxes));
}
var US = "auto",
  Nl = US;
function Cp(i) {
  if (i === void 0 || i === "start" || i === "end") return "auto";
  if (i === "left" || i === "right") return "fixed";
}
function XV(i, t) {
  let n = Cp(t);
  return n === void 0 || od(i) ? i : ru(i) ? n : i === n ? i : nd;
}
function KV(i, t) {
  return (Cp(t) === void 0 && (Nl = i), Nl);
}
function YV() {
  return Nl;
}
function Rl(i, t) {
  if (i === void 0) return t === "auto" ? void 0 : "left";
  if (t === "auto") {
    if (i === "left") return "start";
    if (i === "right") return "end";
  } else {
    if (i === "start") return "left";
    if (i === "end") return "right";
  }
  return i;
}
function Tp(i, t) {
  if (
    ((t.blur = i.resolveValue("blur")),
    (t.brightness = i.resolveValue("brightness")),
    (t.contrast = i.resolveValue("contrast")),
    (t.grayscale = i.resolveValue("grayscale")),
    (t.hueRotate = i.resolveValue("hueRotate")),
    (t.invert = i.resolveValue("invert")),
    (t.saturate = i.resolveValue("saturate")),
    (t.sepia = i.resolveValue("sepia")),
    wa(i))
  )
    if (ku(i) && i.boxShadows) {
      if (le(i.boxShadows)) return;
      t.dropShadows = i.boxShadows.filter((n) => !n.inset).map((n) => bu(n, { blur: wf(n.blur) }));
    } else Pu(i) && i.shadows && (t.dropShadows = i.shadows);
}
function _p(i, t) {
  i.shadows && (t.shadows = i.shadows);
}
function kp(i) {
  let t = {};
  (wc(i) && i.blendingMode !== "normal" && (t.blendingMode = i.resolveValue("blendingMode")),
    Ql(i) && (t.opacity = i.opacity));
  let n = xu(i);
  return (
    n !== void 0 && (t.radius = n),
    Fc(i) &&
      (ie(i.blur) && (t.blur = i.blur),
      ie(i.backgroundBlur) && (t.backgroundBlur = i.backgroundBlur)),
    t
  );
}
function Ip(i, t, n, a) {
  return {
    type: "richText",
    id: n,
    variant: i,
    childNodes: t.children.map((s, u) => {
      let f = a?.[u];
      return us(i, s, f ? [f] : [n, u]);
    }),
  };
}
function XS(i) {
  let t;
  for (let n of i.children) {
    if (!He(n) || n.marks.some(Yr)) continue;
    let a = n.marks.find(Ci)?.attrs;
    if (!a) return;
    if (!t) {
      t = a;
      continue;
    }
    if (a.fill !== t.fill || a.stopIds !== t.stopIds) return;
  }
  return t;
}
function us(i, t, n) {
  let a = t.type.name;
  switch ((ee(Fu(a), () => `richTextFromProseMirror: ${a} is an unknown node type`), a)) {
    case "textBlock": {
      ee(Ke(t), () => `richTextFromProseMirror: incorrect node type ${t.type.name}`);
      let {
          textDirection: s,
          inlineStyles: u,
          blockStyles: f,
          textStylePreset: g,
          tag: k,
          ...F
        } = t.attrs,
        A = XS(t),
        R = {
          id: _t(n),
          type: "textBlock",
          tag: k,
          textDirection: s,
          childNodes: t.children.map(($, q) => KS(i, $, [...n, q])),
        };
      if (Xe(i, "styles")) {
        let $ = R;
        (f && ($.blockStyles = f), A && ($.backgroundMask = A));
      }
      return (
        g !== Cr &&
          Fl(i, "textStylePreset", g, ($) => {
            R.textStylePreset = $;
          }),
        JS(i, R, u, n),
        R
      );
    }
    case "image": {
      (ee(Ju(t), () => `richTextFromProseMirror: incorrect node type ${t.type.name}`),
        ee(Xe(i, "media"), () => `richTextFromProseMirror: variant ${i} does not support images`));
      let { src: s, alt: u, pixelWidth: f, pixelHeight: g, type: k, ...F } = t.attrs;
      return {
        id: _t(n),
        type: "media",
        src: s,
        alt: u,
        pixelWidth: f,
        pixelHeight: g,
        mediaType: k,
        ...jS(i, F, [...n, "link"]),
      };
    }
    case "bulletedList": {
      ee(Qu(t), () => `richTextFromProseMirror: incorrect node type ${t.type.name}`);
      let { textDirection: s, inlineStyles: u, blockStyles: f, textStylePreset: g, ...k } = t.attrs;
      return {
        id: _t(n),
        type: "bulletedList",
        textDirection: t.attrs.textDirection,
        childNodes: t.children.map((F, A) => Pp(i, F, [...n, A])),
      };
    }
    case "numberedList": {
      ee(ec(t), () => `richTextFromProseMirror: incorrect node type ${t.type.name}`);
      let {
        textDirection: s,
        inlineStyles: u,
        blockStyles: f,
        textStylePreset: g,
        listStyleType: k,
        start: F,
        ...A
      } = t.attrs;
      return {
        id: _t(n),
        type: "numberedList",
        textDirection: s,
        listStyleType: k,
        start: F,
        childNodes: t.children.map((R, $) => Pp(i, R, [...n, $])),
      };
    }
    case "blockquote":
      return (
        ee(Ku(t), () => `richTextFromProseMirror: incorrect node type ${t.type.name}`),
        ee(
          Xe(i, "blockquotes"),
          () => `richTextFromProseMirror: variant ${i} does not support blockquotes`
        ),
        t.attrs,
        { id: _t(n), type: "blockquote", childNodes: t.children.map((u, f) => us(i, u, [...n, f])) }
      );
    case "table":
      return (
        ee(rc(t), () => `richTextFromProseMirror: incorrect node type ${t.type.name}`),
        ee(Xe(i, "tables"), () => `richTextFromProseMirror: variant ${i} does not support tables`),
        t.attrs,
        {
          id: _t(n),
          type: "table",
          childNodes: t.children.map(
            (u, f) => (
              ee(
                ic(u),
                () =>
                  `richTextFromProseMirror: incorrect table structure: found a row of type ${u.type.name}`
              ),
              u.attrs,
              {
                id: _t([...n, f]),
                type: "tableRow",
                childNodes: u.children.map((g, k) => {
                  ee(
                    nc(g),
                    () =>
                      `richTextFromProseMirror: incorrect table structure: found a cell of type ${g.type.name}`
                  );
                  let F = g.type.name === "table_cell" ? "cell" : "header",
                    { rowspan: A, colspan: R, ...$ } = g.attrs,
                    q = {
                      id: _t([...n, f, k]),
                      type: "tableCell",
                      cellType: F,
                      childNodes: g.children.map((Z, X) => us(i, Z, [...n, f, k, X])),
                    };
                  return (
                    A && (A > 1 || A === 0) && (q.rowSpan = A),
                    R && R > 1 && (q.columnSpan = R),
                    q
                  );
                }),
              }
            )
          ),
        }
      );
    case "module": {
      (ee(Uu(t), () => `richTextFromProseMirror: incorrect node type ${t.type.name}`),
        ee(Xe(i, "embeds"), () => `richTextFromProseMirror: variant ${i} does not support embeds`));
      let { identifier: s, props: u, width: f, alignment: g, ...k } = t.attrs,
        F = { id: _t(n), type: "module", identifier: s, props: u };
      return (f && (F.width = f), g && (F.alignment = g), F);
    }
    default:
      Pe(a);
  }
}
function Pp(i, t, n) {
  ee(tc(t), () => `richTextFromProseMirror: expected list item, got ${t.type.name}`);
  let { inlineStyles: a, blockStyles: s, textStylePreset: u, ...f } = t.attrs;
  return { id: _t(n), type: "listItem", childNodes: t.children.map((g, k) => us(i, g, [...n, k])) };
}
function KS(i, t, n) {
  let a = t.type.name;
  switch ((ee(Bu(a), () => `richTextFromProseMirror: ${a} is an unknown node type`), a)) {
    case "text": {
      t.attrs;
      let s = { id: _t(n), type: "text", text: t.text ?? "" };
      for (let u of t.marks)
        switch (
          (ee(
            Ru(u.type.name),
            () => `richTextFromProseMirror: ${u.type.name} is not a supported mark name`
          ),
          u.type.name)
        ) {
          case "bold":
            (ee(Yu(u), () => `richTextFromProseMirror: incorrect mark found: ${u.type.name}`),
              u.attrs,
              (s.bold = !0));
            continue;
          case "italic":
            (ee(Zu(u), () => `richTextFromProseMirror: incorrect mark found: ${u.type.name}`),
              u.attrs,
              (s.italic = !0));
            continue;
          case "inlineCode": {
            ee(Yr(u), () => `richTextFromProseMirror: incorrect mark found: ${u.type.name}`);
            let { inlineCodeStylePreset: f, ...g } = u.attrs;
            ((s.inlineCode = !0),
              Fl(i, "inlineCodeStylePreset", f, (k) => {
                s.inlineCodeStylePreset = k;
              }));
            continue;
          }
          case "link": {
            (ee(Ze(u), () => `richTextFromProseMirror: incorrect mark found: ${u.type.name}`),
              u.attrs);
            let { linkStylePreset: f, ...g } = u.attrs;
            ((s.link = wp(i, g, [...n, "link"])),
              Fl(i, "linkStylePreset", f, (k) => {
                s.linkStylePreset = k;
              }));
            continue;
          }
          case "inlineStyles": {
            ee(yt(u), () => `richTextFromProseMirror: incorrect mark found: ${u.type.name}`);
            let { inlineStyles: f, ...g } = u.attrs;
            Xe(i, "styles") && (s.inlineStyles = f);
            continue;
          }
          case "backgroundStyles": {
            ee(lr(u), () => `richTextFromProseMirror: incorrect mark found: ${u.type.name}`);
            let { backgroundStyles: f, ...g } = u.attrs;
            Xe(i, "styles") && (s.backgroundStyles = f);
            continue;
          }
          case "backgroundMask":
            ee(Ci(u), () => `richTextFromProseMirror: incorrect mark found: ${u.type.name}`);
            continue;
          default:
            Pe(u.type.name, () => `richTextFromProseMirror: unknown mark ${u.type.name}`);
        }
      return (ee(t.children.length === 0, "richTextFromProseMirror: unexpected children"), s);
    }
    case "lineBreak":
      return (t.attrs, { id: _t(n), type: "lineBreak" });
    default:
      Pe(a);
  }
}
function YS(i, t) {
  return (
    Xe(i, "variables") ||
    (!Te(t.link) &&
      !Te(t.openInNewTab) &&
      !Te(t.smoothScroll) &&
      !Te(t.clickTrackingId) &&
      !Te(t.relValues) &&
      !Te(t.preserveParams))
  );
}
function wp(i, t, n) {
  (ee(Xe(i, "links"), () => `richTextFromProseMirror: variant ${i} does not support links`),
    ee(YS(i, t), () => `richTextFromProseMirror: variant ${i} does not support variables`));
  let {
      link: a,
      openInNewTab: s,
      smoothScroll: u,
      clickTrackingId: f,
      relValues: g,
      preserveParams: k,
      ...F
    } = t,
    A = { id: _t(n), link: a };
  return (
    s && (A.openInNewTab = s),
    u && (A.smoothScroll = u),
    f && (A.clickTrackingId = f),
    g && (A.relValues = g),
    k && (A.preserveParams = k),
    A
  );
}
function jS(i, t, n) {
  if (t.link) return { link: wp(i, { ...t, link: t.link }, n) };
}
function JS(i, t, n, a) {
  if (
    !n ||
    Object.keys(n).length === 0 ||
    !Xe(i, "styles") ||
    t.childNodes.find((f) => f.type === "text")
  )
    return;
  let s = t,
    u = { id: _t([...a, s.childNodes.length]), type: "text", text: "", inlineStyles: n };
  s.childNodes.push(u);
}
function _t(i) {
  return i.join("-");
}
function Fl(i, t, n, a) {
  if (n !== void 0) {
    if (!Xe(i, "styles")) {
      Be(new Error(`richTextFromProseMirror: variant ${i} does not support ${t}`));
      return;
    }
    a(n);
  }
}
function Wp(i) {
  let [t] = i.getLetterSpacings(!1);
  if (!t) return 0;
  let [n] = i.getFontSizes(!1).filter(In),
    [a, s] = t;
  if (s === "em") {
    if (In(n)) {
      let [f] = n;
      return f * a;
    }
    let [u] = qe(ze.fontSize);
    return u * a;
  }
  return a;
}
function jn(i, t) {
  if (!t.textFitViewBoxSize) return;
  let [n] = t.getFontSizes(!1).find(In) ?? Co.px,
    a = i.getRect(t).width,
    { width: s } = t.textFitViewBoxSize;
  return n * (a / s);
}
function ZS(i, t, n) {
  let [a] = i.getFontSizes(!1).find(In) ?? Co.px,
    s = a * (n / t);
  if (!(Math.abs(s - a) < 0.2)) return s;
}
function QS(i, t, n = !0) {
  let a = [];
  for (let s of i.tree.getNodesWithTrait(t, nr))
    if (!(n && !s.textFitViewBoxSize) && (a.push(s.id), !ci(s)))
      for (let { relatedNode: u } of Ls(i.tree, s)) (n && !u.textFitViewBoxSize) || a.push(u.id);
  return a;
}
function ex(i, { width: t, height: n }) {
  return { width: t - Wp(i), height: n };
}
function tx(i) {
  let t = { heightType: 2, ...zn };
  return (
    i.widthType === 2 &&
      (i.cache.parentDirected
        ? ((t.widthType = 3), (t.width = 1))
        : ((t.width = 1), (t.widthType = 1), (t.left = 0))),
    t
  );
}
async function tL(i, t) {
  let n = QS(i, t, !1);
  (i.scheduler.process(() => {
    for (let a of n) {
      let s = i.tree.get(a);
      if (!nr(s)) continue;
      let [u, ...f] = s.getFontSizes(!1);
      if (Te(u) || f.some(Te)) continue;
      if ((s.set(tx(s)), s.textFitViewBoxSize || ht("text_fit_enable", {}), !Sr(u))) return;
      let [g, k] = u;
      if (k === "rem") {
        let F = Es(i.stores.scopeStore.active, i.tree, i.stores.modulesStore, s.id),
          A = [_o(g, k, "px", F), "px"];
        s.setFontSize(A, !1);
      } else f.length > 0 && s.setFontSize(u, !1);
    }
  }),
    await Bl(i, n));
}
function rL(i, t, n, a) {
  let s,
    u = jn(i.tree, t),
    f = Es(i.stores.scopeStore.active, i.tree, i.stores.modulesStore, t.id);
  if (ie(u)) {
    let k = Math.trunc(u);
    s = [n ? _o(k, "px", n, f) : k, n ?? "px"];
  } else {
    let [g, k] = t.getFontSizes(!1).find(Sr) ?? Co[n ?? "px"];
    s = [n ? _o(g, k, n, f) : g, n ?? k];
  }
  (t.setFontSize(s, !1),
    ht("font_size_update", { unit: s[1] }),
    t.textFitViewBoxSize && ht("text_fit_disable", { source: a }),
    t.set(Jr));
}
async function Bl(i, t, n, a) {
  if (t.length === 0) return;
  await new Promise((f) => i.scheduler.runBeforeNextFrame(f));
  let s = await i.canvasSandbox?.sandbox.renderer.getFitTextViewBoxes(t, i.scheduler.update);
  if (!s) throw Error("No view boxes were returned from the sandbox.");
  await (a ?? ((f) => i.scheduler.processWhenReadyAsync(f)))(() => {
    let f = {};
    for (let { nodeId: g, localeId: k, width: F, height: A } of s) {
      let R = i.tree.getNode(g);
      if (!nr(R) || !k) continue;
      let $ = f[g] ?? { textFitViewBoxSize: R.textFitViewBoxSize, textFitViewBoxSizeLocalized: {} };
      f[g] = $;
      let q = ex(R, { width: F, height: A });
      k === zu ? n !== g && ($.textFitViewBoxSize = q) : ($.textFitViewBoxSizeLocalized[k] = q);
    }
    for (let g in f) {
      let k = f[g],
        F = i.tree.getNode(g);
      !nr(F) ||
        J(k) ||
        F.set({
          textFitViewBoxSize: k.textFitViewBoxSize,
          textFitViewBoxSizeLocalized: Mo(k.textFitViewBoxSizeLocalized)
            ? void 0
            : k.textFitViewBoxSizeLocalized,
        });
    }
  });
}
async function iL(i, t, n) {
  if (ci(t) && t.textFitViewBoxSize) {
    await Bl(i, [t.id], n);
    return;
  }
  let a = [];
  t.textFitViewBoxSize && a.push(t.id);
  for (let { relatedNode: s } of Ls(i.tree, t)) s.textFitViewBoxSize && a.push(s.id);
  await Bl(i, a, n);
}
function nL(i, t) {
  let n = jn(i, t);
  if (!n) return !1;
  t.setFontSize(n, !1);
  let { width: a, height: s } = i.getRect(t);
  return (t.set({ textFitViewBoxSize: { width: a, height: s } }), !0);
}
function oL(i, t, n, a) {
  let s = n.textFitViewBoxSize;
  if (!s) return i;
  let u = Wp(n),
    f = ZS(n, t.dom.clientWidth - u, s.width);
  if (!f) return i;
  a || (a = Gs(i, { assertSupportsStyles: !0 }));
  let g = Dp(a, i, f),
    k = t.state.apply(g);
  return (t.updateState(k), k);
}
function aL(i, t) {
  i.textFitViewBoxSize &&
    i.set({
      textFitViewBoxSize: { width: i.textFitViewBoxSize.width, height: t.dom.clientHeight },
    });
}
function sL(i, t, n) {
  let a = t.cache.editorView;
  if (!a || !t.textFitViewBoxSize) return;
  let { width: s } = i.getRect(t),
    u = jn(i, t);
  if (!u) return;
  n || (n = Gs(a.state, { assertSupportsStyles: !0 }));
  let f = a.state.apply(Dp(n, a.state, u));
  (a.updateState(f), t.set({ textFitViewBoxSize: { width: s, height: a.dom.clientHeight } }));
  let g = t.html;
  if (ao.isOn("jsonRichTextStorage") && Oe(g)) t.set({ html: Ip("richTextNode", f.doc, g.id) });
  else {
    let k = Zt(f.doc, n);
    t.html !== k && t.set({ html: k });
  }
  return f;
}
function Dp(i, t, n) {
  let a = new Wn(t.doc),
    s = t.tr;
  return (
    t.doc.nodesBetween(a.from, a.to, (u, f) => {
      let g = u.marks.find(yt),
        k = Math.max(f, a.from),
        F = Math.min(f + u.nodeSize, a.to),
        A = g?.attrs.inlineStyles ?? {},
        R = i.marks.inlineStyles.create({ inlineStyles: { ...A, fontSize: n } });
      s.addMark(k, F, R);
    }),
    s.setMeta("addToHistory", !1),
    s
  );
}
var cs = class extends Fa {
  editorView = null;
  resizeHeight = null;
  htmlContent = null;
  originalFont = null;
  resetContent() {
    this.htmlContent = null;
  }
  reset() {
    (this.resetContent(), super.reset());
  }
  resetForRendering() {
    (this.resetContent(), super.resetForRendering());
  }
};
function Vl(i, t) {
  let n = Ou(i),
    a = n;
  for (; a;) {
    let s;
    if ((a.firstChild instanceof HTMLElement && (s = a.firstChild), s === void 0)) {
      let f = t.split(`
`);
      a.textContent = "";
      for (let g = 0; g < f.length; g++) {
        let k = f[g];
        if (g > 0) {
          let A = document.createElement("br");
          a.appendChild(A);
        }
        let F = document.createTextNode(k);
        a.appendChild(F);
      }
    }
    let u = a.nextSibling;
    for (; u;) {
      let f = u.nextSibling;
      (u.parentNode?.removeChild(u), (u = f));
    }
    a = s;
  }
  return n.innerHTML;
}
function rx(i) {
  return { id: Qt(), type: "textBlock", tag: "p", textDirection: "auto", childNodes: [i] };
}
function Np(i) {
  return { id: Qt(), type: "text", text: i };
}
function Rp(i, t) {
  let n = Fp(i.childNodes, [i]);
  if (n === void 0) return { ...i, childNodes: [rx(Np(t))] };
  let a = n.pop();
  (ee(a?.type === "text", "updateRichTextDocumentText: internal logic failure"),
    n.push({ ...a, text: t }));
  let s = n.reduceRight(
    (u, f) => (
      ee("childNodes" in f, "updateRichTextDocumentText: internal logic failure"),
      { ...f, childNodes: [u] }
    )
  );
  return (ee(Oe(s), "updateRichTextDocumentText: internal logic failure"), s);
}
function Fp(i, t) {
  for (let n of i) {
    if (n.type === "textBlock" && !n.childNodes.some((a) => a.type === "text"))
      return [...t, n, Np("")];
    if (n.type === "text") return [...t, n];
    if ("childNodes" in n) {
      let a = Fp(n.childNodes, [...t, n]);
      if (a) return a;
    }
  }
}
var ix = Ms.encode(ze.font),
  Ll = /--font-selector:\s?(.+?)\s?;/gu;
function Bp(i) {
  if (!Kr(i)) return;
  let t = i.inlineStyles?.font;
  if (pe(t)) return xi(t);
}
var nx = new Map(),
  ox = new Map(),
  Vp = class i extends Ie("RichTextNode", Vt(Ra(Yi(De)))) {
    constructor(t) {
      super(t, new cs());
      let n = ph();
      we(this, n, t);
    }
    set(t, n, a, s) {
      return (
        ("html" in t || "textContent" in t || "htmlContent" in t) && this.cache.resetContent(),
        super.set(t, n, a, s)
      );
    }
    isEditing() {
      return this.cache.isEditable;
    }
    getComponentPreset(t) {
      return this[Xs(t)];
    }
    getComponentPresets() {
      let t = new Map(),
        n = this.entries();
      for (let [a, s] of n)
        if (cc(a) && Hd(s)) {
          let u = fc(a);
          t.set(u, s);
        }
      return t;
    }
    setComponentPreset(t, n) {
      this.set({ [Xs(t)]: n });
    }
    computeHTMLContent() {
      if (Te(this.htmlContent)) {
        let n = this.resolveValue("htmlContent");
        return pe(n) ? n : Oe(n) ? qs(n, this.getProseMirrorSchema()) : "";
      }
      let t;
      if (
        (Oe(this.html) ? (t = qs(this.html, this.getProseMirrorSchema())) : (t = this.html),
        Te(this.textContent) || oi(this.textContent))
      ) {
        let n = this.resolveValue("textContent");
        return pe(n) ? Vl(t, n || "Content") : t;
      }
      return t;
    }
    getHTMLContent() {
      if (pe(this.cache.htmlContent)) return this.cache.htmlContent;
      let t = this.computeHTMLContent();
      return ((this.cache.htmlContent = t), t);
    }
    getTextContent() {
      return Oe(this.html) && !this.htmlContent && !Te(this.textContent) && !oi(this.textContent)
        ? Gu(this.html) || ""
        : Hu(this.getHTMLContent()) || "";
    }
    static updateNodes;
    updateForHeight(t) {
      let n = this.getDOMRect(),
        a = 1;
      return (
        this.cache.resizeHeight ? (a = t / this.cache.resizeHeight) : n && (a = t / n.height),
        this.updateForScale(a)
      );
    }
    updateForScale(t) {
      let n = this.html,
        a = {};
      if (Oe(n)) a.html = oh(n, t);
      else {
        let s = this.computeHTMLContent(),
          u = this.createEditorStateMemoized(s),
          f = this.getProseMirrorSchema(),
          g = u.tr;
        u.doc.descendants((F, A) => {
          if (
            (Ke(F) &&
              F.attrs.blockStyles.lineHeight?.[1] === "px" &&
              g.setNodeMarkup(A, void 0, {
                ...F.attrs,
                blockStyles: {
                  ...F.attrs.blockStyles,
                  lineHeight: [F.attrs.blockStyles.lineHeight[0] * t, "px"],
                },
              }),
            !He(F))
          )
            return;
          let $ = F.marks.find(yt)?.attrs.inlineStyles,
            q = $?.fontSize ?? qe(ze.fontSize);
          if (Sr(q)) {
            let [be, Q] = q;
            q = [$t(be * t, To[Q]), Q];
          }
          let Z = $?.letterSpacing;
          Z?.[1] === "px" && (Z = [$t(Z[0] * t, 2), "px"]);
          let X = f.marks.inlineStyles.create({
            inlineStyles: { ...$, letterSpacing: Z, fontSize: q },
          });
          g.addMark(A, A + F.nodeSize, X);
        });
        let k = u.apply(g);
        a.html = Zt(k.doc, f);
      }
      return (
        ie(this.linkFontSize) && (a.linkFontSize = $t(this.linkFontSize * t, 0)),
        ie(this.linkHoverFontSize) && (a.linkHoverFontSize = $t(this.linkHoverFontSize * t, 0)),
        ie(this.linkCurrentFontSize) &&
          (a.linkCurrentFontSize = $t(this.linkCurrentFontSize * t, 0)),
        a
      );
    }
    updateForRect(t) {
      let n = { width: t.rect.width, height: t.rect.height },
        a = Ys(this);
      if (a) {
        let u = this.getDOMRect();
        if (u && ie(this.cache.resizeHeight) && u.height !== t.rect.height) {
          let f = this.updateForHeight(t.rect.height);
          Object.assign(n, f);
        }
      }
      let s = Ii({
        rect: t.rect,
        parentSizeInfo: t.parentSizeInfo,
        currentConstraints: this.constraints(),
        constraintsLocked: t.constraintsLocked,
        pixelAlign: this.__unsafeIsGroundNode() || !this.usesDOMRectCached(),
        shouldGuessPinToBottom: t.shouldGuessPinToBottom,
        shouldGuessPinToRight: t.shouldGuessPinToRight,
        updateSize: t.updateSize,
        floatingPosition: null,
      });
      return a ? (Object.assign(n, s), n) : s;
    }
    updateForSize(t, n) {
      let a = {};
      if (Ys(this)) {
        let u = this.getDOMRect();
        if (u && !J(t.height) && u.height !== t.height) {
          let f = this.updateForHeight(t.height);
          Object.assign(a, f);
        }
      } else {
        let u = this.constraintValues(),
          f = wi(t, n, u);
        Object.assign(a, f);
      }
      return a;
    }
    getProps(t, n, a, s) {
      let u = this.getFontsForLoading(),
        f = this.newConstraintProperties();
      Pt.loadFonts(u)
        .then(({ newlyLoadedFontCount: A }) => {
          A > 0 && i.updateNodes?.([this.id]);
        })
        .catch(Be);
      let g = {},
        k = { componentLoader: t, variableOverrides: n, getResolvedFetchDataValue: s };
      (Qs(this, g, k),
        Ui(this, g, k),
        pf(this, g, k),
        Rf(this, g, k),
        Nf(this, g, k),
        Za(this, g, k),
        Wf(this, g, k),
        Da(this, g, k),
        Ki(this, g, k),
        zi(this, g, k),
        Rr(this, g, k),
        Ff(this, g, k),
        Xi(this, g, k),
        Gi(this, g, k),
        kf(this, g, k));
      let F = {
        ...super.getProps(t),
        ...f,
        ...kp(this),
        children: void 0,
        fonts: u,
        style: g,
        rotation: this.resolveValue("rotation", n),
        visible: this.resolveValue("visible", n),
        opacity: this.isApplyingCustomizations ? 0.1 : this.resolveValue("opacity", n),
        verticalAlignment: this.textVerticalAlignment,
        anchorLinkOffsetY: this.anchorLinkOffsetY,
      };
      return (
        Oi(g, F),
        this.cache.isEffectPreview && qi(this, g),
        this.cache.fontPreview && bp(this, g),
        Pa(this, g),
        this.textContent && (F.text = this.resolveValue("textContent", n, s)),
        $i(this, F),
        Sp(this, F, k),
        Fr(this, F),
        Oo(this, F),
        Tp(this, F),
        Qs(this, g, k),
        _p(this, F),
        vp(this, F, k),
        xp(this, F, k),
        F
      );
    }
    removeTokenVariables(t) {
      let n = this.html;
      if (Oe(n)) return Ka(n, (g, k) => Mp(g, k, t));
      let [a] = this.getEditorState(!1),
        s = this.getProseMirrorSchema(),
        u = fs(a, s, (g, k) => Mp(g, k, t));
      if (!u.docChanged) return n;
      let f = a.apply(u);
      return Zt(f.doc, s);
    }
    replaceColorTokenVariables(t) {
      let n = this.html;
      if (Oe(n)) {
        let g = nh(n, t);
        g !== n && this.set({ html: g });
        return;
      }
      let [a] = this.getEditorState(!1),
        s = this.getProseMirrorSchema(),
        u = fs(a, s, (g, k) => {
          if (k === "textColor" && ut(g)) {
            let F = Wo(g),
              A = F ? t.get(F) : void 0;
            if (A) return A;
          }
        });
      if (!u.docChanged) return;
      let f = a.apply(u);
      this.set({ html: Zt(f.doc, s) });
    }
    getInlineStyleTokenVariables() {
      let [t] = this.getEditorState(!1),
        n = [];
      return (
        t.doc.descendants((a) => {
          for (let s of a.marks)
            if (yt(s)) {
              let u = s.attrs.inlineStyles;
              for (let f in u) {
                if (f !== "textColor") continue;
                let g = u[f];
                ut(g) && n.push(g);
              }
            }
        }),
        n
      );
    }
    getInlineVariableReferences() {
      let [t] = this.getEditorState(!1),
        n = [];
      return (
        t.doc.descendants((a) => {
          for (let s of a.marks)
            if (yt(s)) {
              let u = s.attrs.inlineStyles,
                f;
              for (f in u) {
                ee(!J(f), "Expected key to be defined");
                let g = u[f];
                Te(g) && n.push(g);
              }
            }
        }),
        n
      );
    }
    withUpdatedInlineVariableReferences(t) {
      let n = this.html;
      if (Oe(n)) return xl(n, t);
      let [a] = this.getEditorState(!1),
        s = this.getProseMirrorSchema();
      return sx(n, a, s, t);
    }
    getHTMLWithoutContentVariableReferences(t, n) {
      if (!Te(this.htmlContent)) return;
      let a = [];
      for (let A of On) {
        let R = this[A];
        R && a.push(R);
      }
      let s = t.getNodesWithTrait(a, gl),
        u = this.createEditorStateMemoized(n),
        f = this.getProseMirrorSchema(),
        g = new Wn(u.doc),
        k = u.tr;
      if (
        (s.forEach((A) => {
          ii(A)
            ? u.doc.nodesBetween(g.from, g.to, (R, $) => {
                Ke(R) &&
                  R.attrs.tag === A?.tag &&
                  (k.setNodeMarkup($, f.nodes.textBlock, {
                    ...R.attrs,
                    blockStyles: {},
                    inlineStyles: {},
                    textStylePreset: A?.id ?? Cr,
                    tag: A?.tag ?? R.attrs.tag,
                  }),
                  bi(R) || k.removeMark($, $ + R.nodeSize, f.marks.inlineStyles));
              })
            : Wt(A) &&
              u.doc.nodesBetween(g.from, g.to, (R, $) => {
                if (R.type !== f.nodes.text) return;
                let q = R.marks.find(Ze);
                q !== void 0 &&
                  k.addMark(
                    $,
                    $ + R.nodeSize,
                    f.marks.link.create({ ...q.attrs, linkStylePreset: A?.id })
                  );
              });
        }),
        !k.docChanged)
      )
        return n;
      let F = u.apply(k);
      return Zt(F.doc, f);
    }
    withResolvedInlineVariableReferences(t) {
      let n = this.html;
      if (Oe(n)) return xl(n, t);
      let [a] = this.getEditorState(!1),
        s = this.getProseMirrorSchema(),
        u = fs(a, s, (g) => {
          if (Te(g)) return t(g);
        });
      if (!u.docChanged) return n;
      let f = a.apply(u);
      return Zt(f.doc, s);
    }
    getSelectionText() {
      let [t, n] = this.getEditorState(!0),
        a = Ya(n),
        s = ja(n);
      return t.doc.textBetween(a, s);
    }
    setPlainText(t) {
      let n = this.draftOrCurrent(),
        a = Oe(n.html) ? Rp(n.html, t) : Vl(n.html, t);
      this.set({ html: a, textContent: void 0 });
    }
    getProseMirrorSchema() {
      return oc(ac);
    }
    createEditorStateMemoized(t) {
      return Oe(t)
        ? Is(100, ox, t, (n) => {
            let a = this.getProseMirrorSchema();
            return $s(sc(n, a), a);
          })
        : Is(100, nx, t, (n) => $s(n, this.getProseMirrorSchema()));
    }
    getEditorState(t) {
      let n = this.cache.editorView,
        a = n?.state;
      if (!a) {
        let u = this.draftOrCurrent(),
          f =
            Oe(u.html) && !u.htmlContent && !Te(u.textContent) && !oi(u.textContent)
              ? u.html
              : u.getHTMLContent();
        a = this.createEditorStateMemoized(f);
      }
      if (oo(t)) {
        let u = Hs.create(a.doc, t.from, t.to);
        return [a, u];
      }
      if (n && t) return [a, a.selection];
      let s = new Wn(a.doc);
      return [a, s];
    }
    updateStoredDocument(t) {
      let n = this.draftOrCurrent();
      if (this.cache.editorView || !Oe(n.html)) return !1;
      if (!n.htmlContent) {
        let a = t(n.html);
        a !== n.html && this.set({ html: a });
      }
      return !0;
    }
    applyTransaction(t) {
      if (this.cache.editorView) {
        this.cache.editorView.dispatch(t);
        return;
      }
      let n = this.draftOrCurrent();
      if (n.htmlContent || !t.docChanged) return;
      Gc(n.html, "RichTextNode.applyTransaction");
      let [a] = this.getEditorState(!1),
        s = this.getProseMirrorSchema(),
        u = a.apply(t);
      this.set({ html: Zt(u.doc, s) });
    }
    getTextStyle(t) {
      let n = { ...ze, ...en },
        [a] = this.getFonts(t),
        [s] = this.getVariationAxes(t);
      if (!J(a)) {
        let ne = Pt.getFontBySelector(a);
        if (No(ne) && s) {
          let ft = Ro(a, s);
          ((n.font = ft), (n.fontBold = ft), (n.fontBoldItalic = ft), (n.fontItalic = ft));
        } else {
          n.font = a;
          let { selectorBold: ft, selectorBoldItalic: nt, selectorItalic: tr } = ne ?? {};
          ((n.fontBold = ft), (n.fontBoldItalic = nt), (n.fontItalic = tr));
        }
      }
      let [u] = this.getTextColors(t);
      J(u) || (n.textColor = Te(u) ? this.cache.getVariableReferenceValue(u) : u);
      let [f] = this.getFontSizes(t);
      if (!J(f))
        if (Te(f)) {
          let ne = this.cache.getVariableReferenceValue(f, void 0);
          ie(ne) && (n.fontSize = Ue(ne, "px"));
        } else n.fontSize = Ue(...f);
      let g = this.tree();
      if (this.textFitViewBoxSize && g) {
        let ne = jn(g, this);
        ie(ne) && (n.fontSize = Ue($t(ne, 0), "px"));
      }
      let [k] = this.getLetterSpacings(t);
      J(k) || (n.letterSpacing = wn(k));
      let [F] = this.getTextTransforms(t);
      J(F) || (n.textTransform = F);
      let [A] = this.getTextDecorations(t);
      if ((J(A) || (n.textDecoration = A), Yt(A))) {
        let [ne] = this.getTextDecorationColors(t);
        n.textDecorationColor = ne;
        let [it] = this.getTextDecorationOffsets(t);
        n.textDecorationOffset = it;
        let [ft] = this.getTextDecorationThicknesses(t);
        n.textDecorationThickness = ft;
        let [nt] = this.getTextDecorationStyles(t);
        n.textDecorationStyle = nt;
        let [tr] = this.getTextDecorationSkipInks(t);
        n.textDecorationSkipInk = tr;
      }
      let [R] = this.getLineHeights(t);
      J(R) || (n.lineHeight = R);
      let [$] = this.getTextAlignments(t);
      J($) || (n.textAlignment = $);
      let [q] = this.getTextStrokeWidths(t);
      J(q) || (n.textStrokeWidth = q);
      let [Z] = this.getTextStrokeColors(t);
      J(Z) || (n.textStrokeColor = Z);
      let [X] = this.getTextBackgroundColors(t);
      J(X) || (n.textBackgroundColor = Te(X) ? this.cache.getVariableReferenceValue(X) : X);
      let [be] = this.getTextBackgroundRadiuses(t);
      J(be) || (n.textBackgroundRadius = be);
      let [Q] = this.getTextBackgroundCornerShapes(t);
      J(Q) || (n.textBackgroundCornerShape = Q);
      let [oe] = this.getTextBackgroundPaddings(t);
      J(oe) || (n.textBackgroundPadding = oe);
      let [ct] = this.getOpenTypeFontFeatures(t);
      return (J(ct) || (n.openTypeFontFeatures = ct), n);
    }
    getInlineStyles(t, n, a) {
      let s = [],
        [u, f] = this.getEditorState(n),
        g = (k = {}) => {
          (a && !a(k)) || s.push(k[t]);
        };
      if (f.empty) {
        let k = f.$head.parent;
        if (Ke(k) && k.textContent === "") g(k.attrs.inlineStyles);
        else {
          let F = u.storedMarks ?? f.$head.marks();
          g(F.find(yt)?.attrs.inlineStyles ?? {});
        }
      } else
        u.doc.nodesBetween(f.from, f.to, (k) => {
          He(k) && g(k.marks.find(yt)?.attrs.inlineStyles ?? {});
        });
      return (s.length === 0 && s.push(void 0), sn(s));
    }
    setInlineStyle(t, n, a) {
      if (this.updateStoredDocument((k) => eh(k, t, n))) return;
      let [s, u] = this.getEditorState(a),
        f = this.getProseMirrorSchema(),
        g = s.tr;
      if (u.empty) {
        let k = u.$head.parent;
        if (Ke(k) && k.textContent === "") {
          let F = u.$head.before(1);
          g.setNodeMarkup(F, void 0, {
            ...k.attrs,
            inlineStyles: { ...k.attrs.inlineStyles, [t]: n },
          });
        } else {
          let A = (s.storedMarks ?? u.$head.marks()).find(yt)?.attrs.inlineStyles ?? {},
            R = f.marks.inlineStyles.create({ inlineStyles: { ...A, [t]: n } });
          g.addStoredMark(R);
        }
      } else
        (s.doc.nodesBetween(u.from, u.to, (F, A) => {
          if (F.type !== f.nodes.text && t !== "fontSize") return;
          let R = F.marks.find(yt)?.attrs.inlineStyles ?? {},
            $ = Math.max(A, u.from),
            q = Math.min(A + F.nodeSize, u.to),
            Z = f.marks.inlineStyles.create({ inlineStyles: { ...R, [t]: n } });
          g.addMark($, q, Z);
        }),
          s.doc.nodesBetween(u.from, u.to, (F, A) => {
            !Ke(F) ||
              F.textContent !== "" ||
              g.setNodeMarkup(A, void 0, {
                ...F.attrs,
                inlineStyles: { ...F.attrs.inlineStyles, [t]: n },
              });
          }));
      this.applyTransaction(g);
    }
    replaceInlineStyle(t, n, a) {
      if (
        this.updateStoredDocument((k) =>
          Ee(k, {
            text(F) {
              return !F.text || F.inlineStyles?.[t] !== n
                ? F
                : { ...F, inlineStyles: { ...F.inlineStyles, [t]: a } };
            },
          })
        )
      )
        return;
      let [s, u] = this.getEditorState(!1),
        f = this.getProseMirrorSchema(),
        g = s.tr;
      (s.doc.nodesBetween(u.from, u.to, (k, F) => {
        if (k.type !== f.nodes.text) return;
        let A = k.marks.find(yt)?.attrs.inlineStyles ?? {};
        if (A[t] !== n) return;
        let R = Math.max(F, u.from),
          $ = Math.min(F + k.nodeSize, u.to),
          q = f.marks.inlineStyles.create({ inlineStyles: { ...A, [t]: a }, [t]: a });
        g.addMark(R, $, q);
      }),
        this.applyTransaction(g));
    }
    getBackgroundStyles(t, n, a) {
      let s = [],
        [u, f] = this.getEditorState(n),
        g = this.getProseMirrorSchema(),
        k = (F = {}) => {
          (a && !a(F)) || s.push(F[t]);
        };
      return (
        u.doc.nodesBetween(f.from, f.to, (F) => {
          if (F.type !== g.nodes.text || F.marks.find(Ze)) return;
          let A = F.marks.find(lr);
          (!A && !this.isEditing()) || k(A?.attrs.backgroundStyles);
        }),
        s.length === 0 && s.push(void 0),
        sn(s)
      );
    }
    setBackgroundStyle(t, n, a) {
      if (this.updateStoredDocument((A) => th(A, t, n))) return;
      let [s, u] = this.getEditorState(a),
        f = this.getProseMirrorSchema(),
        g = s.tr;
      if (u.empty) {
        this.selectTargetMarkAtCursor(s, u.$from, lr) && this.setBackgroundStyle(t, n, !0);
        return;
      }
      let k = !1;
      s.doc.nodesBetween(u.from, u.to, (A) => {
        k || A.type !== f.nodes.text || (k = !!A.marks.find(lr));
      });
      let F = this.isEditing() || !k;
      (s.doc.nodesBetween(u.from, u.to, (A, R) => {
        if (A.type !== f.nodes.text) return;
        let $ = A.marks.find(lr)?.attrs.backgroundStyles,
          q = Math.max(R, u.from),
          Z = Math.min(R + A.nodeSize, u.to),
          X = { ...$, [t]: n };
        if ($ && Tr(X)) g.removeMark(q, Z, f.marks.backgroundStyles);
        else if ($ || F) {
          let be = f.marks.backgroundStyles.create({ backgroundStyles: X });
          g.addMark(q, Z, be);
        }
      }),
        this.applyTransaction(g));
    }
    replaceBackgroundStyle(t, n, a) {
      if (this.updateStoredDocument((k) => rh(k, t, n, a))) return;
      let [s, u] = this.getEditorState(!1),
        f = this.getProseMirrorSchema(),
        g = s.tr;
      (s.doc.nodesBetween(u.from, u.to, (k, F) => {
        if (k.type !== f.nodes.text) return;
        let A = k.marks.find(lr)?.attrs.backgroundStyles ?? {};
        if (A[t] !== n) return;
        let R = Math.max(F, u.from),
          $ = Math.min(F + k.nodeSize, u.to),
          q = { ...A, [t]: a };
        if (A && Tr(q)) g.removeMark(R, $, f.marks.backgroundStyles);
        else {
          let Z = f.marks.backgroundStyles.create({ backgroundStyles: q });
          g.addMark(R, $, Z);
        }
      }),
        this.applyTransaction(g));
    }
    selectTargetMarkAtCursor(t, n, a) {
      let s = t.tr,
        u = n.parent.childAfter(n.parentOffset),
        f = u?.node;
      if (!f || !He(f)) return !1;
      let g = f.marks.find(a);
      if (!g) return !1;
      let { parent: k } = n,
        F = n.index(),
        A = n.start() + u.offset,
        R = F + 1,
        $ = A + f.nodeSize;
      for (; F > 0 && g.isInSet(k.child(F - 1).marks);) (F--, (A -= k.child(F).nodeSize));
      for (; R < k.childCount && g.isInSet(k.child(R).marks);) (($ += k.child(R).nodeSize), R++);
      return (s.setSelection(Hs.create(t.doc, A, $)), this.applyTransaction(s), !0);
    }
    hasFontsToLoad() {
      return Oe(this.html)
        ? this.hasFontsToLoadFromRichTextDocument(this.html)
        : this.hasFontsToLoadFromHTML(this.html);
    }
    getFontsForLoading() {
      return Oe(this.html)
        ? this.getFontsForLoadingFromRichTextDocument(this.html)
        : this.getFontsForLoadingFromHTML(this.html);
    }
    hasFontsToLoadFromRichTextDocument(t) {
      let n = !1;
      return (
        dr(t, {
          text(a) {
            Bp(a) && (n = !0);
          },
        }),
        n
      );
    }
    getFontsForLoadingFromRichTextDocument(t) {
      let n = new Set(),
        a = !1,
        s = !1;
      (dr(t, {
        text(f) {
          (f.bold && (a = !0), f.italic && (s = !0));
          let g = Bp(f);
          g && n.add(g);
        },
      }),
        n.size === 0 && ze.font && n.add(ze.font));
      let u = new Set();
      for (let f of n) {
        u.add(f);
        let {
          selectorBold: g,
          selectorBoldItalic: k,
          selectorItalic: F,
        } = Pt.getFontBySelector(f) ?? {};
        (a && g && u.add(g), a && s && k && u.add(k), s && F && u.add(F));
      }
      return (
        this.linkFont && u.add(this.linkFont),
        this.linkHoverFont && u.add(this.linkHoverFont),
        this.linkCurrentFont && u.add(this.linkCurrentFont),
        Array.from(u)
      );
    }
    hasFontsToLoadFromHTML(t) {
      let n = Ll.test(t);
      return ((Ll.lastIndex = 0), n);
    }
    getFontsForLoadingFromHTML(t) {
      let n = new Set(),
        a = [...t.matchAll(Ll)];
      a.length === 0 && (a = [["", ix]]);
      let s = t.includes("</strong>"),
        u = t.includes("</em>");
      for (let f of a) {
        let g = f[1];
        if (!g) continue;
        let k = Ms.decode(g),
          F = xi(k);
        n.add(F);
        let {
          selectorBold: A,
          selectorBoldItalic: R,
          selectorItalic: $,
        } = Pt.getFontBySelector(F) ?? {};
        (s && A && n.add(A), s && u && R && n.add(R), u && $ && n.add($));
      }
      return (
        this.linkFont && n.add(this.linkFont),
        this.linkHoverFont && n.add(this.linkHoverFont),
        this.linkCurrentFont && n.add(this.linkCurrentFont),
        Array.from(n)
      );
    }
    getFonts(t) {
      let n = this.getInlineStyles("font", t),
        a = [];
      return (
        n.forEach((s) => {
          s && a.push(xi(s));
        }),
        a
      );
    }
    getFontsWithVariationAxes(t) {
      return this.getInlineStyles("font", t).filter((n) => !J(n));
    }
    setFont(t, n) {
      this.setInlineStyle("font", t, n);
    }
    getVariationAxes(t) {
      let n = this.getInlineStyles("font", t),
        a = [];
      for (let s of n) {
        if (J(s)) continue;
        let { axesCSS: u } = Fo(s);
        if (J(u)) continue;
        let f = Do(u);
        a.push(f);
      }
      return a;
    }
    getTextColors(t) {
      return this.getInlineStyles("textColor", t);
    }
    setTextColor(t, n) {
      (pe(t) && !ut(t) && (t = st.toFormatString(t)), this.setInlineStyle("textColor", t, n));
    }
    replaceTextColor(t, n) {
      (pe(n) && !ut(n) && (n = st.toFormatString(n)), this.replaceInlineStyle("textColor", t, n));
    }
    getTextBackgroundColors(t) {
      return this.getBackgroundStyles("textBackgroundColor", t);
    }
    setTextBackgroundColor(t, n) {
      this.setBackgroundStyle("textBackgroundColor", Jn(t), n);
    }
    replaceTextBackgroundColor(t, n) {
      this.replaceBackgroundStyle("textBackgroundColor", t, Jn(n));
    }
    getTextBackgroundRadiuses(t) {
      return this.getBackgroundStyles("textBackgroundRadius", t);
    }
    setTextBackgroundRadius(t, n) {
      this.setBackgroundStyle("textBackgroundRadius", t, n);
    }
    getTextBackgroundCornerShapes(t) {
      return this.getBackgroundStyles("textBackgroundCornerShape", t);
    }
    setTextBackgroundCornerShape(t, n) {
      this.setBackgroundStyle("textBackgroundCornerShape", t, n);
    }
    getTextBackgroundPaddings(t) {
      return this.getBackgroundStyles("textBackgroundPadding", t);
    }
    setTextBackgroundPadding(t, n) {
      this.setBackgroundStyle("textBackgroundPadding", t, n);
    }
    getFontSizes(t) {
      let n = [],
        a = this.getInlineStyles("fontSize", t);
      for (let s of a) {
        if (J(s)) continue;
        let u = ie(s) ? [s, "px"] : s;
        n.push(u);
      }
      return n;
    }
    setFontSize(t, n) {
      this.setInlineStyle("fontSize", t, n);
    }
    getLetterSpacings(t) {
      return this.getInlineStyles("letterSpacing", t);
    }
    setLetterSpacing(t, n) {
      this.setInlineStyle("letterSpacing", t, n);
    }
    getTextTransforms(t) {
      return this.getInlineStyles("textTransform", t);
    }
    setTextTransform(t, n) {
      this.setInlineStyle("textTransform", t, n);
    }
    getTextDecorations(t) {
      return this.getInlineStyles("textDecoration", t);
    }
    setTextDecoration(t, n) {
      this.setInlineStyle("textDecoration", t, n);
    }
    getTextDecorationColors(t, n = !1) {
      return this.getInlineStyles(
        "textDecorationColor",
        t,
        n ? (a) => Yt(a.textDecoration) : void 0
      );
    }
    setTextDecorationColor(t, n) {
      this.setInlineStyle("textDecorationColor", t, n);
    }
    replaceTextDecorationColor(t, n) {
      this.replaceInlineStyle("textDecorationColor", t, n);
    }
    getTextDecorationThicknesses(t) {
      return this.getInlineStyles("textDecorationThickness", t);
    }
    setTextDecorationThickness(t, n) {
      this.setInlineStyle("textDecorationThickness", t, n);
    }
    getTextDecorationStyles(t) {
      return this.getInlineStyles("textDecorationStyle", t);
    }
    setTextDecorationStyle(t, n) {
      this.setInlineStyle("textDecorationStyle", t, n);
    }
    getTextDecorationSkipInks(t) {
      return this.getInlineStyles("textDecorationSkipInk", t);
    }
    setTextDecorationSkipInk(t, n) {
      this.setInlineStyle("textDecorationSkipInk", t, n);
    }
    getTextDecorationOffsets(t) {
      return this.getInlineStyles("textDecorationOffset", t);
    }
    setTextDecorationOffset(t, n) {
      this.setInlineStyle("textDecorationOffset", t, n);
    }
    getTextStrokeWidths(t) {
      return this.getInlineStyles("textStrokeWidth", t);
    }
    setTextStrokeWidth(t, n) {
      this.setInlineStyle("textStrokeWidth", t, n);
    }
    getTextStrokeColors(t) {
      return this.getInlineStyles("textStrokeColor", t);
    }
    setTextStrokeColor(t, n) {
      this.setInlineStyle("textStrokeColor", t, n);
    }
    getOpenTypeFontFeatures(t) {
      return this.getInlineStyles("openTypeFontFeatures", t);
    }
    setOpenTypeFontFeatures(t, n) {
      this.setInlineStyle("openTypeFontFeatures", t, n);
    }
    getBlockStyles(t, n) {
      let a = [],
        s = Vo.defaults[t],
        [u, f] = this.getEditorState(n);
      return (
        u.doc.nodesBetween(f.from, f.to, (g) => {
          if (Ke(g)) {
            let k = g.attrs.blockStyles[t];
            a.push(k ?? s);
          }
        }),
        a.length === 0 && a.push(s),
        sn(a)
      );
    }
    setBlockStyle(t, n, a) {
      if (
        this.updateStoredDocument((g) =>
          Ee(g, { textBlock: (k) => ({ ...k, blockStyles: { ...k.blockStyles, [t]: n } }) })
        )
      )
        return;
      let [s, u] = this.getEditorState(a),
        f = s.tr;
      (s.doc.nodesBetween(u.from, u.to, (g, k) => {
        Ke(g) &&
          f.setNodeMarkup(k, void 0, {
            ...g.attrs,
            blockStyles: { ...g.attrs.blockStyles, [t]: n },
          });
      }),
        this.applyTransaction(f));
    }
    getLineHeights(t) {
      return this.getBlockStyles("lineHeight", t);
    }
    setLineHeight(t, n) {
      this.setBlockStyle("lineHeight", t, n);
    }
    getTextAlignments(t) {
      return this.getBlockStyles("textAlignment", t);
    }
    setTextAlignment(t, n) {
      this.setBlockStyle("textAlignment", t, n);
    }
    setTextAlignmentDirection(t) {
      if (
        this.updateStoredDocument((s) =>
          Ee(s, {
            textBlock: (u) => {
              let f = u.textStylePreset
                  ? this.tree()?.getNodeWithTrait(u.textStylePreset, ni)
                  : void 0,
                g = u.blockStyles?.textAlignment ?? f?.textAlignment ?? Vo.defaults.textAlignment,
                k = Rl(g, t);
              return k === g ? u : { ...u, blockStyles: { ...u.blockStyles, textAlignment: k } };
            },
          })
        )
      )
        return;
      let [n] = this.getEditorState(!1),
        a = n.tr;
      (n.doc.descendants((s, u) => {
        if (!Ke(s)) return;
        let f = this.getEffectiveTextAlignment(s),
          g = Rl(f, t);
        g !== f &&
          a.setNodeMarkup(u, void 0, {
            ...s.attrs,
            blockStyles: { ...s.attrs.blockStyles, textAlignment: g },
          });
      }),
        a.docChanged && this.applyTransaction(a));
    }
    getEffectiveTextAlignment(t) {
      let n = t.attrs.blockStyles.textAlignment;
      if (n !== void 0) return n;
      if (bi(t)) {
        let a = this.tree()?.getNodeWithTrait(t.attrs.textStylePreset, ni);
        if (a?.textAlignment !== void 0) return a.textAlignment;
      }
      return Vo.defaults.textAlignment;
    }
    getStylePresetIdsForCodeGeneration() {
      let t = new Set();
      if (this.htmlContent)
        for (let n of On) {
          let a = this[n];
          a && t.add(a);
        }
      else {
        let n = this.html;
        if (typeof n == "string") {
          let a = /data-styles-preset="(.+?)"/gu,
            s = n.matchAll(a);
          for (let u of s) {
            let f = Xu(u[1]);
            pe(f) && t.add(f);
          }
        } else
          dr(n, {
            textBlock(a) {
              "textStylePreset" in a && pe(a.textStylePreset) && t.add(a.textStylePreset);
            },
            text(a) {
              ("inlineCodeStylePreset" in a &&
                pe(a.inlineCodeStylePreset) &&
                t.add(a.inlineCodeStylePreset),
                "linkStylePreset" in a && pe(a.linkStylePreset) && t.add(a.linkStylePreset));
            },
          });
      }
      return t;
    }
    getStylePresetsForCodeGeneration(t) {
      let n = this.getStylePresetIdsForCodeGeneration();
      return t.getNodesWithTrait(n, gl);
    }
    hasTextStylePresets(t) {
      return this.getTextStylePresets(t).some(pe);
    }
    getTextStylePresets(t) {
      let n = new Set(),
        [a, s] = this.getEditorState(t);
      return (
        a.doc.nodesBetween(s.from, s.to, (u) => {
          if (Ke(u)) {
            let f = u.attrs.textStylePreset;
            f === Cr ? n.add(void 0) : n.add(f);
          }
        }),
        n.size === 0 && n.add(void 0),
        Array.from(n)
      );
    }
    setTextStylePreset(t, n, a = !1) {
      if (a && !t) return;
      if (
        this.updateStoredDocument((k) =>
          Ee(k, {
            textBlock(F) {
              return a && F.tag !== t?.tag
                ? F
                : {
                    ...F,
                    tag: t?.tag ?? F.tag,
                    textStylePreset: t?.id,
                    backgroundMask: void 0,
                    blockStyles:
                      F.textStylePreset && F.blockStyles?.textAlignment
                        ? { textAlignment: F.blockStyles.textAlignment }
                        : {},
                    childNodes: F.childNodes.map((A) =>
                      A.type !== "text" || (F.textStylePreset && A.text)
                        ? A
                        : { ...A, inlineStyles: void 0, backgroundStyles: void 0 }
                    ),
                  };
            },
          })
        )
      ) {
        this.set(Jr);
        return;
      }
      let [s, u] = this.getEditorState(n),
        f = this.getProseMirrorSchema(),
        g = s.tr;
      (s.doc.nodesBetween(u.from, u.to, (k, F) => {
        if (Ke(k) && (!a || k.attrs.tag === t?.tag)) {
          let A = {
            ...k.attrs,
            blockStyles: {},
            inlineStyles: {},
            textStylePreset: t?.id ?? Cr,
            tag: t?.tag ?? k.attrs.tag,
          };
          (bi(k) &&
            k.attrs.blockStyles.textAlignment &&
            (A.blockStyles.textAlignment = k.attrs.blockStyles.textAlignment),
            g.setNodeMarkup(F, f.nodes.textBlock, A),
            bi(k) ||
              (g.removeMark(F, F + k.nodeSize, f.marks.inlineStyles),
              g.removeMark(F, F + k.nodeSize, f.marks.backgroundStyles)),
            g.removeMark(F, F + k.nodeSize, f.marks.backgroundMask));
        }
      }),
        this.applyTransaction(g),
        this.set(Jr));
    }
    getNodeTextStylePreset(t, n) {
      let a = An(n);
      return t.getNodeWithTrait(this[a], ni);
    }
    setNodeTextStylePreset(t) {
      let n = An(t.tag);
      this.set({ [n]: t.id });
    }
    detachTextStylePreset(t, n) {
      let a = {
        font: t.font,
        textColor: Jn(t.textColor),
        fontSize: gi(t.fontSize) ? qe(t.fontSize) : [t.fontSize, "px"],
        letterSpacing: wn(t.letterSpacing),
        textTransform: t.textTransform,
        textDecoration: t.textDecoration,
        textDecorationColor: t.textDecorationColor,
        textDecorationOffset: t.textDecorationOffset,
        textDecorationThickness: t.textDecorationThickness,
        textDecorationStyle: t.textDecorationStyle,
        textDecorationSkipInk: t.textDecorationSkipInk,
        openTypeFontFeatures: t.openTypeFontFeatures,
        textStrokeWidth: t.textStrokeWidth,
        textStrokeColor: t.textStrokeColor,
      };
      if (
        this.updateStoredDocument((F) =>
          Ee(F, {
            textBlock(A) {
              if (A.textStylePreset !== (t.originalid || t.id)) return A;
              let R = A.childNodes.map(($) => {
                if ($.type !== "text") return $;
                let q = {
                  textBackgroundColor:
                    $.backgroundStyles?.textBackgroundColor ?? Jn(t.textBackgroundColor),
                  textBackgroundRadius:
                    $.backgroundStyles?.textBackgroundRadius ?? t.textBackgroundRadius,
                  textBackgroundCornerShape:
                    $.backgroundStyles?.textBackgroundCornerShape ?? t.textBackgroundCornerShape,
                  textBackgroundPadding:
                    $.backgroundStyles?.textBackgroundPadding ?? t.textBackgroundPadding,
                };
                return {
                  ...$,
                  inlineStyles: $.text
                    ? { ...a, textColor: $.inlineStyles?.textColor ?? a.textColor }
                    : a,
                  backgroundStyles: !$.text || $.link || $.inlineCode || Tr(q) ? void 0 : q,
                };
              });
              return (
                R.some(($) => $.type === "text") ||
                  R.push({ id: Qt(), type: "text", text: "", inlineStyles: a }),
                {
                  ...A,
                  textStylePreset: void 0,
                  blockStyles: {
                    lineHeight: t.lineHeight,
                    textAlignment: t.textAlignment,
                    ...A.blockStyles,
                  },
                  childNodes: R,
                }
              );
            },
          })
        )
      )
        return;
      let [s, u] = this.getEditorState(n),
        f = this.getProseMirrorSchema(),
        g = s.tr,
        k = t.originalid || t.id;
      (s.doc.nodesBetween(u.from, u.to, (F, A) => {
        if (Ke(F) && F.attrs.textStylePreset === k)
          return (
            g.setNodeMarkup(A, f.nodes.textBlock, {
              ...F.attrs,
              blockStyles: {
                lineHeight: t.lineHeight,
                textAlignment: t.textAlignment,
                ...F.attrs.blockStyles,
              },
              inlineStyles: a,
              textStylePreset: Cr,
            }),
            F.forEach((R, $) => {
              let q = R.marks.find(yt),
                Z = A + $ + 1;
              g.addMark(
                Z,
                Z + R.nodeSize,
                f.marks.inlineStyles.create({
                  inlineStyles: { ...a, textColor: q?.attrs.inlineStyles.textColor ?? a.textColor },
                })
              );
              let X = R.marks.find(lr),
                be = {
                  ...X?.attrs.backgroundStyles,
                  textBackgroundColor:
                    X?.attrs.backgroundStyles.textBackgroundColor ?? Jn(t.textBackgroundColor),
                  textBackgroundRadius:
                    X?.attrs.backgroundStyles.textBackgroundRadius ?? t.textBackgroundRadius,
                  textBackgroundCornerShape:
                    X?.attrs.backgroundStyles.textBackgroundCornerShape ??
                    t.textBackgroundCornerShape,
                  textBackgroundPadding:
                    X?.attrs.backgroundStyles.textBackgroundPadding ?? t.textBackgroundPadding,
                };
              Tr(be) && X
                ? g.removeMark(Z, Z + R.nodeSize, f.marks.backgroundStyles)
                : g.addMark(
                    Z,
                    Z + R.nodeSize,
                    f.marks.backgroundStyles.create({ backgroundStyles: be })
                  );
            }),
            !1
          );
      }),
        this.applyTransaction(g));
    }
    removeTextStylePreset(t, n) {
      if (
        this.updateStoredDocument((g) =>
          Ee(g, {
            textBlock: (k) => (k.textStylePreset === t ? { ...k, textStylePreset: void 0 } : k),
          })
        )
      )
        return;
      let [a, s] = this.getEditorState(n),
        u = this.getProseMirrorSchema(),
        f = a.tr;
      (a.doc.nodesBetween(s.from, s.to, (g, k) => {
        Ke(g) &&
          g.attrs.textStylePreset === t &&
          f.setNodeMarkup(k, u.nodes.textBlock, { ...g.attrs, textStylePreset: Cr });
      }),
        this.applyTransaction(f));
    }
    remapTextStylePresets(t) {
      if (
        this.updateStoredDocument((u) =>
          Ee(u, {
            textBlock(f) {
              let g = f.textStylePreset ? t(f.textStylePreset, f.tag) : void 0;
              return g ? { ...f, textStylePreset: g.id, tag: g.tag } : f;
            },
          })
        )
      )
        return;
      let [n] = this.getEditorState(!1),
        a = this.getProseMirrorSchema(),
        s = n.tr;
      (n.doc.descendants((u, f) => {
        if (Ke(u) && bi(u)) {
          let g = t(u.attrs.textStylePreset, u.attrs.tag);
          g &&
            s.setNodeMarkup(f, a.nodes.textBlock, {
              ...u.attrs,
              textStylePreset: g.id,
              tag: g.tag,
            });
        }
      }),
        this.applyTransaction(s));
    }
    replaceTextStylePreset(t, n) {
      if (
        this.updateStoredDocument((f) =>
          Ee(f, {
            textBlock: (g) =>
              g.textStylePreset === t.id ? { ...g, textStylePreset: n.id, tag: n.tag } : g,
          })
        )
      )
        return;
      let [a] = this.getEditorState(!1),
        s = this.getProseMirrorSchema(),
        u = a.tr;
      (a.doc.descendants((f, g) => {
        Ke(f) &&
          f.attrs.textStylePreset === t.id &&
          u.setNodeMarkup(g, s.nodes.textBlock, { ...f.attrs, textStylePreset: n.id, tag: n.tag });
      }),
        this.applyTransaction(u));
    }
    getLinkStylePresets(t) {
      let n = new Set(),
        [a, s] = this.getEditorState(t);
      return (
        a.doc.nodesBetween(s.from, s.to, (u) => {
          if (!He(u)) return;
          let f = u.marks.find(Ze);
          f !== void 0 && n.add(f.attrs.linkStylePreset);
        }),
        n.size === 0 && n.add(this.stylePresetLink),
        n
      );
    }
    setLinkStylePreset(t, n) {
      if (
        this.updateStoredDocument((g) =>
          Ee(g, { text: (k) => (k.text && k.link ? { ...k, linkStylePreset: t?.id } : k) })
        )
      )
        return;
      let [a, s] = this.getEditorState(n),
        u = this.getProseMirrorSchema(),
        f = a.tr;
      (a.doc.nodesBetween(s.from, s.to, (g, k) => {
        if (!He(g)) return;
        let F = g.marks.find(Ze);
        F !== void 0 &&
          f.addMark(k, k + g.nodeSize, u.marks.link.create({ ...F.attrs, linkStylePreset: t?.id }));
      }),
        this.applyTransaction(f));
    }
    getNodeLinkStylePreset(t) {
      return t.getNodeWithTrait(this.stylePresetLink, Wt);
    }
    setNodeLinkStylePreset(t) {
      this.set({ stylePresetLink: t.id });
    }
    remapLinkStylePresets(t) {
      if (this.updateStoredDocument((u) => ah(u, (f) => t(f)?.id))) return;
      let [n] = this.getEditorState(!1),
        a = this.getProseMirrorSchema(),
        s = n.tr;
      (n.doc.descendants((u, f) => {
        if (!He(u)) return;
        let g = u.marks.find(Ze);
        if (g === void 0 || g.attrs.linkStylePreset === void 0) return;
        let k = t(g.attrs.linkStylePreset);
        k &&
          s.addMark(f, f + u.nodeSize, a.marks.link.create({ ...g.attrs, linkStylePreset: k.id }));
      }),
        this.applyTransaction(s));
    }
    removeLegacyLinkStyle() {
      let t = {};
      for (let n of Fe(cr)) t[n] = void 0;
      this.set(t);
    }
    applyDefaultTextLinkStylePreset(t, n) {
      if (
        this.updateStoredDocument((g) =>
          Ee(g, {
            text: (k) =>
              k.text && k.link && !k.linkStylePreset ? { ...k, linkStylePreset: t.id } : k,
          })
        )
      )
        return;
      let [a, s] = this.getEditorState(n),
        u = this.getProseMirrorSchema(),
        f = a.tr;
      (a.doc.nodesBetween(s.from, s.to, (g, k) => {
        if (!He(g)) return;
        let F = g.marks.find(Ze);
        if (F === void 0 || F.attrs.linkStylePreset) return;
        let A = u.marks.link.create({ ...F.attrs, linkStylePreset: t.id });
        f.addMark(k, k + g.nodeSize, A);
      }),
        this.applyTransaction(f));
    }
    getNodeAnchorLinkStylePreset(t) {
      return t.getNodeWithTrait(this.stylePresetAnchorLink, Wt);
    }
    setNodeAnchorLinkStylePreset(t) {
      this.set({ stylePresetAnchorLink: t.id });
    }
    getNodeBlockquoteStylePreset(t) {
      return t.getNodeWithTrait(this.stylePresetBlockquote, Ut);
    }
    setNodeBlockquoteStylePreset(t) {
      this.set({ stylePresetBlockquote: t.id });
    }
    getTableStylePreset(t) {
      return t.getNodeWithTrait(this.stylePresetTable, It);
    }
    setNodeTableStylePreset(t) {
      this.set({ stylePresetTable: t.id });
    }
    getInlineCodeStylePresets(t) {
      let n = new Set(),
        [a, s] = this.getEditorState(t);
      return (
        a.doc.nodesBetween(s.from, s.to, (u) => {
          if (!He(u)) return;
          let f = u.marks.find(Yr);
          f !== void 0 && n.add(f.attrs.inlineCodeStylePreset);
        }),
        n
      );
    }
    setInlineCodeStylePreset(t, n) {
      if (
        this.updateStoredDocument((g) =>
          Ee(g, {
            text: (k) => (k.text && k.inlineCode ? { ...k, inlineCodeStylePreset: t.id } : k),
          })
        )
      )
        return;
      let [a, s] = this.getEditorState(n),
        u = this.getProseMirrorSchema(),
        f = a.tr;
      (a.doc.nodesBetween(s.from, s.to, (g, k) => {
        if (!He(g)) return;
        let F = g.marks.find(Yr);
        F !== void 0 &&
          f.addMark(
            k,
            k + g.nodeSize,
            u.marks.inlineCode.create({ ...F.attrs, inlineCodeStylePreset: t.id })
          );
      }),
        this.applyTransaction(f));
    }
    remapInlineCodeStylePresets(t) {
      if (
        this.updateStoredDocument((u) =>
          Ee(u, {
            text(f) {
              let g =
                f.text && f.inlineCode && f.inlineCodeStylePreset !== void 0
                  ? t(f.inlineCodeStylePreset)
                  : void 0;
              return g ? { ...f, inlineCodeStylePreset: g.id } : f;
            },
          })
        )
      )
        return;
      let [n] = this.getEditorState(!1),
        a = this.getProseMirrorSchema(),
        s = n.tr;
      (n.doc.descendants((u, f) => {
        if (!He(u)) return;
        let g = u.marks.find(Yr);
        if (g === void 0 || g.attrs.inlineCodeStylePreset === void 0) return;
        let k = t(g.attrs.inlineCodeStylePreset);
        k &&
          s.addMark(
            f,
            f + u.nodeSize,
            a.marks.inlineCode.create({ ...g.attrs, inlineCodeStylePreset: k.id })
          );
      }),
        this.applyTransaction(s));
    }
    getNodeInlineCodeStylePreset(t) {
      return t.getNodeWithTrait(this.stylePresetInlineCode, Xt);
    }
    setNodeInlineCodeStylePreset(t) {
      this.set({ stylePresetInlineCode: t.id });
    }
    getNodeImageStylePreset(t) {
      return t.getNodeWithTrait(this.stylePresetImage, Dt);
    }
    setNodeImageStylePreset(t) {
      this.set({ stylePresetImage: t.id });
    }
    getNodeCount() {
      let [t] = this.getEditorState(!1);
      return t.doc.childCount;
    }
    getTextBlockTags(t) {
      let n = new Set(),
        [a, s] = this.getEditorState(t);
      return (
        a.doc.nodesBetween(s.from, s.to, (u) => {
          Ke(u) && n.add(fu(u.attrs.tag) ? u.attrs.tag : "span");
        }),
        Array.from(n)
      );
    }
    setTextBlockTag(t, n) {
      if (
        this.updateStoredDocument((g) =>
          Ee(g, { textBlock: (k) => ({ ...k, tag: Os(t) ? t : "div" }) })
        )
      )
        return;
      let [a, s] = this.getEditorState(n),
        u = this.getProseMirrorSchema(),
        f = a.tr;
      (a.doc.nodesBetween(s.from, s.to, (g, k) => {
        Ke(g) && f.setNodeMarkup(k, u.nodes.textBlock, { ...g.attrs, tag: Os(t) ? t : "div" });
      }),
        this.applyTransaction(f));
    }
    getLinks(t) {
      let n = this.getLinkAttributes(t, "link");
      return (n.length === 0 && n.push(void 0), n);
    }
    setLink(
      t,
      n,
      {
        linkOpenInNewTab: a,
        linkClickTrackingId: s,
        linkRelValues: u,
        linkPreserveParams: f,
        linkSmoothScroll: g,
      },
      k
    ) {
      if (
        this.updateStoredDocument((be) => {
          let Q;
          return (
            dr(be, {
              text(oe) {
                oe.text && oe.link && Q === void 0 && (Q = oe.link.smoothScroll ?? !1);
              },
            }),
            Ee(be, {
              text: (oe) =>
                oe.text
                  ? {
                      ...oe,
                      backgroundStyles: void 0,
                      linkStylePreset: n?.id,
                      link: {
                        id: oe.link?.id,
                        link: t,
                        openInNewTab: a ?? !1,
                        smoothScroll: g ?? Q ?? !1,
                        clickTrackingId: s,
                        relValues: u,
                        preserveParams: f,
                      },
                    }
                  : oe,
            })
          );
        })
      )
        return;
      let [F, A] = this.getEditorState(k),
        R = this.getProseMirrorSchema(),
        $ = F.tr,
        q = Ya(A),
        Z = ja(A),
        [X] = this.getLinkSmoothScroll(k);
      ($.addMark(
        q,
        Z,
        R.marks.link.create({
          link: t,
          openInNewTab: a ?? !1,
          smoothScroll: g ?? X ?? !1,
          clickTrackingId: s,
          linkStylePreset: n?.id,
          relValues: u,
          preserveParams: f,
        })
      ),
        this.applyTransaction($));
    }
    updateLinks(t, n, a) {
      (this.setLinkAttribute("link", t, a),
        qt(n.linkOpenInNewTab) && this.setLinkOpenInNewTab(n.linkOpenInNewTab, a),
        qt(n.linkClickTrackingId) && this.setLinkClickTrackingId(n.linkClickTrackingId, a),
        qt(n.linkRelValues) && this.setLinkRelValues(n.linkRelValues, a),
        qt(n.linkPreserveParams) && this.setLinkPreserveParams(n.linkPreserveParams, a),
        qt(n.linkSmoothScroll) && this.setLinkSmoothScroll(n.linkSmoothScroll, a));
    }
    removeLink(t) {
      if (
        this.updateStoredDocument((k) =>
          Ee(k, {
            text: (F) => (F.text && F.link ? { ...F, link: void 0, linkStylePreset: void 0 } : F),
          })
        )
      )
        return;
      let [n, a] = this.getEditorState(t),
        s = this.getProseMirrorSchema(),
        u = n.tr,
        f = Ya(a),
        g = ja(a);
      (u.removeMark(f, g, s.marks.link), this.applyTransaction(u));
    }
    getLinkAttributes(t, n) {
      let a = [],
        [s, u] = this.getEditorState(t);
      if (u.empty) {
        let g = u.$head.marks().find(Ze);
        g && a.push(g.attrs[n]);
      } else
        s.doc.nodesBetween(u.from, u.to, (f) => {
          if (!He(f)) return;
          let g = f.marks.find(Ze);
          g && a.push(g.attrs[n]);
        });
      return sn(a);
    }
    setLinkAttribute(t, n, a) {
      if (
        this.updateStoredDocument((k) =>
          Ee(k, { text: (F) => (F.text && F.link ? { ...F, link: { ...F.link, [t]: n } } : F) })
        )
      )
        return;
      let [s, u] = this.getEditorState(a),
        f = this.getProseMirrorSchema(),
        g = s.tr;
      (s.doc.nodesBetween(u.from, u.to, (k, F) => {
        if (!He(k)) return;
        let A = k.marks.find(Ze);
        A !== void 0 && g.addMark(F, F + k.nodeSize, f.marks.link.create({ ...A.attrs, [t]: n }));
      }),
        this.applyTransaction(g));
    }
    getUniformLinkAttrs() {
      let [t, n] = this.getEditorState(!1),
        a = [];
      t.doc.nodesBetween(n.from, n.to, (u) => {
        !He(u) || u.text === "" || a.push(u.marks.find(Ze)?.attrs);
      });
      let s = xc(a, $e);
      if (iu(s)) return s;
    }
    getLinkOpenInNewTabs(t) {
      return this.getLinkAttributes(t, "openInNewTab");
    }
    setLinkOpenInNewTab(t, n) {
      this.setLinkAttribute("openInNewTab", t, n);
    }
    getLinkSmoothScroll(t) {
      return this.getLinkAttributes(t, "smoothScroll");
    }
    setLinkSmoothScroll(t, n) {
      this.setLinkAttribute("smoothScroll", t, n);
    }
    getLinkClickTrackingId(t) {
      return this.getLinkAttributes(t, "clickTrackingId");
    }
    setLinkClickTrackingId(t, n) {
      this.setLinkAttribute("clickTrackingId", t, n);
    }
    getLinksByClickTrackingId(t) {
      let n = [],
        [a, s] = this.getEditorState(!1);
      return (
        a.doc.nodesBetween(s.from, s.to, (u) => {
          if (!He(u)) return;
          let f = u.marks.find(Ze);
          f?.attrs.clickTrackingId === t && n.push(f.attrs.link);
        }),
        sn(n)
      );
    }
    getLinkRelValues(t) {
      return this.getLinkAttributes(t, "relValues");
    }
    setLinkRelValues(t, n) {
      this.setLinkAttribute("relValues", t, n);
    }
    getLinkPreserveParams(t) {
      return this.getLinkAttributes(t, "preserveParams");
    }
    setLinkPreserveParams(t, n) {
      this.setLinkAttribute("preserveParams", t, n);
    }
    hasLinks() {
      return dh(this.html);
    }
    hasBlockquotes() {
      return lh(this.html);
    }
    getRouteIdsToPreload() {
      let t = new Set(),
        n = this.getLinks(!1);
      for (let a of n) Vu(a) && t.add(a.webPageId);
      if (t.size > 0) return Array.from(t);
    }
    remapLinks(t) {
      if (this.updateStoredDocument((u) => ih(u, t))) return;
      let [n] = this.getEditorState(!1),
        a = this.getProseMirrorSchema(),
        s = sh(a, n, t);
      s && this.applyTransaction(s);
    }
    removeInlineCode(t) {
      if (
        this.updateStoredDocument((f) =>
          Ee(f, {
            text: (g) =>
              g.text && g.inlineCode
                ? { ...g, inlineCode: void 0, inlineCodeStylePreset: void 0 }
                : g,
          })
        )
      )
        return;
      let [n, a] = this.getEditorState(t),
        s = this.getProseMirrorSchema(),
        u = n.tr;
      (n.doc.nodesBetween(a.from, a.to, (f, g) => {
        He(f) && u.removeMark(g, g + f.nodeSize, s.marks.inlineCode);
      }),
        this.applyTransaction(u));
    }
    getFills() {
      let t = [],
        [n, a] = this.getEditorState(!1);
      if (a.empty) {
        let u = a.$head.marks().find(Ci);
        u ? t.push(u.attrs) : t.push(void 0);
      } else
        n.doc.nodesBetween(a.from, a.to, (s) => {
          if (!He(s)) return;
          let u = s.marks.find(Ci);
          u ? t.push(u.attrs) : t.push(void 0);
        });
      return (t.length === 0 && t.push(void 0), sn(t).map((s) => s && ax(s)));
    }
    setFill(t) {
      if (
        this.updateStoredDocument((u) => {
          let f = t === void 0 ? void 0 : Lp(t);
          return Ee(u, {
            textBlock(g) {
              return (f &&
                !g.childNodes.some((k) => k.type === "text" && k.text && !k.inlineCode)) ||
                (!f && !g.backgroundMask)
                ? g
                : { ...g, backgroundMask: f };
            },
          });
        })
      )
        return;
      let [n] = this.getEditorState(!1),
        a = this.getProseMirrorSchema(),
        s = n.tr;
      if (typeof t > "u") s.removeMark(0, n.doc.content.size, a.marks.backgroundMask);
      else {
        let u = a.marks.backgroundMask.create(Lp(t));
        s.addMark(0, n.doc.content.size, u);
      }
      this.applyTransaction(s);
    }
    getActions() {
      let t = {};
      for (let n of vn(this)) {
        let a = this[n];
        if (!a || a.length === 0) {
          t[n] = void 0;
          continue;
        }
        t[n] = Ti(a);
      }
      return t;
    }
    getProsemirrorTextBlocks(t) {
      let n = [],
        [a, s] = this.getEditorState(t);
      return (
        a.doc.nodesBetween(s.from, s.to, (u) => {
          Ke(u) && n.push(u);
        }),
        n
      );
    }
    supportsDynamicValues() {
      return !0;
    }
    supportsPluginData() {
      return !0;
    }
  };
function ax(i) {
  let t = i.stopIds?.split(","),
    n = jf(i.fill);
  return (
    n && t && (n = n.updated({ stops: n.stops.map((a, s) => a.merge({ id: t?.[s] || Pn() })) })),
    n ?? i.fill
  );
}
function Lp(i) {
  if (Uo(i)) {
    let t = i.stops.map((n) => {
      let a = ut(n.value) ? Jt(n.value) : n.value;
      return n.merge({ value: a, id: n.id || Pn() });
    });
    return { fill: i.updated({ stops: t }).toCSS(), stopIds: t.map((n) => n.id).join(",") };
  }
  return { fill: i, stopIds: "" };
}
function Jn(i) {
  return pe(i) && !ut(i) && i !== "initial" ? st.toFormatString(i) : i;
}
function sn(i) {
  return i.filter((t, n) => i.findIndex((s) => $u(t, s)) === n);
}
function sx(i, t, n, a) {
  let s = fs(t, n, (f) => {
    if (Te(f)) return a(f);
  });
  if (!s.docChanged) return i;
  let u = t.apply(s);
  return Zt(u.doc, n);
}
function Mp(i, t, n) {
  let a = wu(i, n);
  if (pe(a)) return t === "textColor" ? st.toFormatString(a) : a;
}
function fs(i, t, n) {
  let a = i.tr;
  return (
    i.doc.descendants((s, u) => {
      for (let f of s.marks)
        if (yt(f)) {
          let g = !1,
            k = { ...f.attrs.inlineStyles };
          for (let F of Object.keys(k)) {
            let A = k[F],
              R = n(A, F);
            J(R) || ((g = !0), (k[F] = R));
          }
          if (g) {
            let F = t.marks.inlineStyles.create({ inlineStyles: k });
            a.addMark(u, u + s.nodeSize, F);
          }
        }
    }),
    a
  );
}
var ln = We(() => ({
  ...yr,
  ...Pr,
  ...Li,
  ...nu,
  ...Wr,
  ...Dr,
  ...Ir,
  ...rf,
  ...Sc,
  ...kr,
  ...Bi,
  ...Rc,
  ...Ec,
  ...gr,
  ...Nr,
  ...Di,
}));
var lx = ti("attributesFromProps");
function Ep(i) {
  let t = new hi();
  for (let n in i) {
    let a = i[n];
    J(a)
      ? (t[n] = re`undefined`)
      : vf(a)
        ? (t[n] = a)
        : lx.reportError(`Invalid attribute value for ${n}: ${a}`);
  }
  return t;
}
function Ap(i, t) {
  if (!i.boxShadows || le(i.boxShadows) || Ac(i)) return;
  wa(i) ? (t.shadows = i.boxShadows.filter((a) => a.inset)) : (t.shadows = i.boxShadows);
}
var zp = Ne((i, t, n) => {
  if (!Fi(i) || !yo(i)) return;
  let a = i.strokeEnabled === !0;
  if (((t.strokeEnabled = a), !a)) return;
  let s = i.calculatedPaths(),
    u = Ct.isClosed(s);
  if (((t.insideStroke = !1), ie(i.strokeWidth) || Te(i.strokeWidth))) {
    let f = i.resolveValue("strokeWidth", n.variableOverrides),
      g = u && i.strokeAlignment === "inside";
    if (!ie(f)) return;
    ((t.insideStroke = g), (t.strokeWidth = g ? f * 2 : f));
  }
});
function Op(i, t) {
  !yo(i) || qr(i) || (t.d = Od(i.calculatedPaths()));
}
function dn(i) {
  return class extends i {
    isEmptyGroup() {
      switch (this.__class) {
        case "BooleanShapeNode":
        case "ShapeGroupNode":
          if (!yc(this)) return !0;
          break;
        default:
          return !1;
      }
      let n = this.children;
      for (let a of n) if (!a.isEmptyGroup()) return !1;
      return !0;
    }
    getProps(n, a) {
      let s = { ...super.getProps(n, a), visible: this.resolveValue("visible", a) };
      if (!tu(this)) return s;
      let u = this.id,
        f = Gd(u),
        g = this.resolveValue("name") || Sl(n, this),
        k = this.calculatedPaths(),
        A =
          !!this.strokeEnabled && this.strokeWidth
            ? this.resolveValue("strokeColor")
            : "transparent",
        R = A ? st.getAlpha(A) : 0,
        $ = `${f}_clip`,
        q = this.resolveValue("opacity", a),
        Z = {
          opacity: q === 1 ? void 0 : q,
          calculatedPath: k,
          calculatedPathBoundingBox: Ct.boundingBox(k),
          strokeClipId: $,
          strokeAlpha: R,
          key: f,
          name: g,
          lineCap: this.lineCap,
          lineJoin: this.lineJoin,
          strokeColor: this.resolveValue("strokeColor", a),
          id: f,
          strokeDashArray: this.strokeDashArray,
          strokeDashOffset: this.strokeDashOffset,
          strokeMiterLimit: this.strokeMiterLimit,
          fillOpacity: this.resolveValue("fillOpacity", a),
        },
        X = { componentLoader: n, variableOverrides: a };
      return (Fr(this, Z), zp(this, Z, X), Ap(this, Z), Ic(this, Z, X), Object.assign(s, Z), s);
    }
    getAttributes(n, a) {
      let s = { ...a, style: {} },
        { calculatedPath: u, layoutIdKey: f, key: g, duplicatedFrom: k, ...F } = this.getProps(n);
      return (Op(this, s), Object.assign(s, F), Ep(s));
    }
    supportsDynamicValues() {
      return !0;
    }
  };
}
function Hp(i, t, n, a = 1, s = t.id) {
  return (Array.isArray(n) ? n : [n]).map(({ pathSegments: f, pathClosed: g }) => ({
    pathSegments: f.map((F) => dx(i, t, F, a, void 0, s)),
    pathClosed: g,
  }));
}
function dx(i, t, n, a = 1, s, u = t.id) {
  let f = Je.calculatedHandleIn(n),
    g = Je.calculatedHandleOut(n),
    k = Go(i, t, n, !0, s, u),
    F = Go(i, t, f, !0, s, u),
    A = Go(i, t, g, !0, s, u),
    R = Qe.multiply(k, a),
    $ = Qe.multiply(F, a),
    q = Qe.multiply(A, a),
    Z = Qe.subtract($, R),
    X = Qe.subtract(q, R);
  return n.merge({ ...R, handleOutX: X.x, handleOutY: X.y, handleInX: Z.x, handleInY: Z.y });
}
function un(i) {
  return class extends i {
    localCalculatedPathBezierCurves() {
      let n = this.mutable ? void 0 : this.cache,
        a = n?.localCalculatedPathBezierCurves;
      if (a) return a;
      let s = Ct.toBezierJS(this.calculatedPaths());
      return (n && (n.localCalculatedPathBezierCurves = s), s);
    }
    canvasCalculatedPathBezierCurves(n, a = this.id) {
      let s = !this.mutable && a === this.id ? this.cache : void 0,
        u = s?.canvasCalculatedPathBezierCurves;
      if (u) return u;
      let f = Hp(n, this, this.calculatedPaths(), 1, a),
        g = Ct.toBezierJS(f);
      return (s && (s.canvasCalculatedPathBezierCurves = g), g);
    }
    rect(n) {
      return { x: this.x, y: this.y, width: this.width, height: this.height };
    }
    matrix(n, a, s = this.id) {
      let u = { x: this.x, y: this.y };
      a && ((u.x = a.x), (u.y = a.y));
      let f = this.cache.getRenderInstance(s)?.variableOverrides,
        g = this.resolveValue("rotation", f);
      return new DOMMatrix().translateSelf(u.x || 0, u.y || 0).rotateSelf(0, 0, g);
    }
    originAdjustedMatrix(n, a, s = this.id) {
      if ((a || (a = this.rect(n)), !a))
        throw Error("Could not determine frame to build matrix from");
      return new DOMMatrix()
        .translateSelf(0.5 * a.width, 0.5 * a.height)
        .multiplySelf(this.matrix(n, a, s))
        .translateSelf(-0.5 * a.width, -0.5 * a.height);
    }
    isRotated() {
      return this.resolveValue("rotation") % 360 !== 0;
    }
    updateForRect({ rect: n }) {
      return { ...n };
    }
    updateForSize(n, a) {
      return n;
    }
    getProps(n, a) {
      let s = { componentLoader: n, variableOverrides: a },
        u = {
          ...super.getProps(n),
          x: this.x,
          y: this.y,
          rotation: this.resolveValue("rotation", a),
          left: Js(this) ? void 0 : this.x,
          top: Js(this) ? void 0 : this.y,
          width: Ps(this.width),
          height: Ps(this.height),
        },
        f = u.style ?? {};
      return (Rr(this, f, s), (u.style = f), u);
    }
  };
}
var ms = { polygonSides: 6 },
  ux = "polygonSides";
function cx(i) {
  return ux in i;
}
function SA(i, t) {
  if (!cx(i)) {
    t.onlyPolygonNodes = !1;
    return;
  }
  Xr("polygonSides", t, i);
}
var Ml = { spikeCount: 5, spikeDepth: 50 },
  fx = "spikeCount";
function mx(i) {
  return fx in i;
}
function bA(i, t) {
  if (!mx(i)) {
    t.onlyNodesWithSpikes = !1;
    return;
  }
  (Xr("spikeCount", t, i), Xr("spikeDepth", t, i));
}
var Gp = { minX: 1 / 0, minY: 1 / 0, maxX: -1 / 0, maxY: -1 / 0 },
  hx = 10;
function $p(i, t, n) {
  return {
    minX: Math.min(n.minX, i),
    maxX: Math.max(n.maxX, i),
    minY: Math.min(n.minY, t),
    maxY: Math.max(n.maxY, t),
  };
}
function hs(i, t, n) {
  let a = n - t,
    s = (i - a) / 2;
  return t - s;
}
function El(i, t, n = 0, a = !1, s = 0, u = 0, f = 0, g = 0, k) {
  let F = a ? s : n,
    A = a ? u : n,
    R = a ? f : n,
    $ = a ? g : n;
  return [
    {
      pathSegments: [
        new Je({ radius: F, radiusSmoothing: k }),
        new Je({ x: i, y: 0, radius: A, radiusSmoothing: k }),
        new Je({ x: i, y: t, radius: R, radiusSmoothing: k }),
        new Je({ x: 0, y: t, radius: $, radiusSmoothing: k }),
      ],
      pathClosed: !0,
    },
  ];
}
function PA(i, t, n = hx) {
  return El(i, t, n);
}
function qp(i, t) {
  let n = (2 * (Math.sqrt(2) - 1)) / 3;
  return [
    {
      pathSegments: [
        new Je({
          x: i / 2,
          y: 0,
          handleMirroring: "symmetric",
          handleOutX: n * i,
          handleOutY: 0,
          handleInX: -n * i,
          handleInY: 0,
        }),
        new Je({
          x: i,
          y: t / 2,
          handleMirroring: "symmetric",
          handleOutY: n * t,
          handleOutX: 0,
          handleInY: -n * t,
          handleInX: 0,
        }),
        new Je({
          x: i / 2,
          y: t,
          handleMirroring: "symmetric",
          handleOutX: -n * i,
          handleOutY: 0,
          handleInX: n * i,
          handleInY: 0,
        }),
        new Je({
          x: 0,
          y: t / 2,
          handleMirroring: "symmetric",
          handleOutY: -n * t,
          handleOutX: 0,
          handleInY: n * t,
          handleInX: 0,
        }),
      ],
      pathClosed: !0,
    },
  ];
}
function Up(i, t, n = Ml.spikeCount, a = Ml.spikeDepth, s = 0, u = !0, f) {
  let k = Math.max(0, Math.min(1, (100 - a) / 100)),
    F = [],
    A = Gp,
    R = (Math.PI / 2) * 3,
    $ = Math.PI / n;
  function q(Q) {
    let oe = Math.cos(R) * Q,
      ct = Math.sin(R) * Q,
      ne = ((oe + 1) * i) / 2,
      it = ((ct + 1) * t) / 2;
    (F.push({ x: ne, y: it }), (R += $), u && (A = $p(ne, it, A)));
  }
  for (let Q = 0; Q < n; Q++) (q(1), q(k));
  let Z = u ? hs(i, A.minX, A.maxX) : 0,
    X = u ? hs(t, A.minY, A.maxY) : 0;
  return [
    {
      pathSegments: F.map((Q) => new Je({ x: Q.x - Z, y: Q.y - X, radius: s, radiusSmoothing: f })),
      pathClosed: !0,
    },
  ];
}
function Xp(i, t, n = ms.polygonSides, a = 0, s = !0, u) {
  let g = [],
    k = Gp,
    F = (Math.PI / 2) * 3,
    A = (2 * Math.PI) / n;
  for (let Z = 0; Z < n; Z++) {
    let X = Math.cos(F) * 1,
      be = Math.sin(F) * 1,
      Q = ((X + 1) * i) / 2,
      oe = ((be + 1) * t) / 2;
    (g.push({ x: Q, y: oe }), (F += A), s && (k = $p(Q, oe, k)));
  }
  let R = s ? hs(i, k.minX, k.maxX) : 0,
    $ = s ? hs(t, k.minY, k.maxY) : 0;
  return [
    {
      pathSegments: g.map((Z) => new Je({ x: Z.x - R, y: Z.y - $, radius: a, radiusSmoothing: u })),
      pathClosed: !0,
    },
  ];
}
var Qr;
((s) => {
  function i(u) {
    return yn(u) || ri(u) || Sn(u) || xn(u);
  }
  s.isBasicShape = i;
  function t(u) {
    if (u.mutable) return n(u);
    let f = u.cache;
    return (f.calculatedPaths || (f.calculatedPaths = n(u)), f.calculatedPaths);
  }
  s.calculatedPath = t;
  function n(u) {
    let f = a(u);
    return (
      ee(f.length === 1, "createPath should create a single path"),
      ri(u) ? f : [Ct.applyRadius(f[0].pathSegments, f[0].pathClosed)]
    );
  }
  function a(u) {
    let { width: f, height: g } = u;
    if (ri(u)) return qp(f, g);
    if (Sn(u))
      return Up(
        f,
        g,
        u.spikeCount,
        u.spikeDepth,
        ie(u.radius) ? u.radius : 0,
        u.centered ?? !1,
        u.radiusSmoothing
      );
    if (xn(u))
      return Xp(
        f,
        g,
        u.polygonSides,
        ie(u.radius) ? u.radius : 0,
        u.centered ?? !1,
        u.radiusSmoothing
      );
    if (yn(u))
      return El(
        f,
        g,
        ie(u.radius) ? u.radius : 0,
        u.radiusPerCorner,
        u.radiusTopLeft,
        u.radiusTopRight,
        u.radiusBottomRight,
        u.radiusBottomLeft,
        u.radiusSmoothing
      );
    throw Error("can't make path, unknown basic shape");
  }
  s.createPath = a;
})((Qr ||= {}));
var Kp = class extends Ie("OvalShapeNode", dn(Vt(un(De)))) {
  constructor(t) {
    (super(t), we(this, ln(), t), delete this.children);
  }
  calculatedPaths() {
    return Qr.calculatedPath(this);
  }
};
var px = "radiusSmoothing";
function gx(i) {
  return px in i;
}
var ps = { radiusSmoothing: void 0 };
function qA(i, t) {
  gx(i) && Xr("radiusSmoothing", t, i, zd);
}
var Yp = We(() => ({ ...ln(), ...jt, ...ps, ...ms, centered: void 0 }));
var jp = class extends Ie("PolygonShapeNode", dn(Vt(un(De)))) {
  constructor(t) {
    (super(t), we(this, Yp(), t), delete this.children);
  }
  calculatedPaths() {
    return Qr.calculatedPath(this);
  }
};
var Jp = We(() => ({ ...ln(), ...jt, ...bt, ...ps }));
var Zp = class extends Ie("RectangleShapeNode", dn(Vt(un(De)))) {
  constructor(t) {
    (super(t), we(this, Jp(), t), delete this.children);
  }
  calculatedPaths() {
    return Qr.calculatedPath(this);
  }
};
var Qp = { formInputAutofillEnabled: void 0 },
  yx = "formInputAutofillEnabled";
function eg(i) {
  return yx in i;
}
var cn = { formInputIconImage: void 0, formInputIconColor: void 0 },
  Sx = "formInputIconImage";
function tg(i) {
  return Sx in i;
}
var kz = "rgba(153, 153, 153, 1)",
  xx = {
    search: Kl(
      '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="none"><path d="M7 2.5a5 5 0 0 1 4.13 7.82l2.03 2.03a.75.75 0 0 1-1.06 1.06l-2-2A5 5 0 1 1 7 2.5Zm-3.5 5a3.5 3.5 0 1 0 7 0 3.5 3.5 0 0 0-7 0Z" fill="#999"/></svg>'
    ),
  },
  Pz = xx.search;
var vx = "formInputPlaceholder";
function Zn(i) {
  return vx in i;
}
var rg = {
    formInputPlaceholder: void 0,
    formInputPlaceholderLocalized: void 0,
    formInputPlaceholderColor: void 0,
  },
  Nz = "rgba(153, 153, 153, 1)";
function Rz(i) {
  if (!Hi(i)) return !1;
  if (le(i.formTextInputType)) return !0;
  switch (i.formTextInputType) {
    case "text":
    case "email":
    case "number":
    case "tel":
    case "url":
    case "textarea":
    case void 0:
      return !0;
    case "date":
    case "time":
      return !1;
    default:
      Pe(i.formTextInputType);
  }
}
var bx = "formInputStep";
function Bz(i) {
  return bx in i;
}
var ig = { formInputStep: void 0 };
var Cx = "formInputFontFamily",
  Lz = "formInputFontSelector",
  Qn = (i) => Cx in i,
  gs = {
    formInputFontFamily: void 0,
    formInputFontSize: void 0,
    formInputFontColor: void 0,
    formInputFontWeight: void 0,
    formInputFontSelector: void 0,
    formInputFontStyle: void 0,
    formInputFontLineHeight: void 0,
    formInputFontLineHeightUnit: void 0,
    formInputFontTextAlignment: void 0,
    formInputFontLetterSpacing: void 0,
    formInputFontLetterSpacingUnit: void 0,
    formInputOpenTypeFontFeatures: void 0,
    formInputVariationAxes: void 0,
  };
var fn = We(() => ({
  ...yr,
  ...Wi,
  ...er,
  ...Pr,
  ..._i,
  ...Oa,
  ...Vi,
  ...Ni,
  ...Su,
  ...wd,
  ...Ef,
  ...pi,
  ...Nc,
  ...Wr,
  ...Ri,
  ...Dr,
  ...Ir,
  ...fi,
  ...Mi,
  ...kr,
  ...bt,
  ...Si,
  ...Nt,
  ...mi,
  ...gr,
  ...Nr,
  ...Ei,
  ...Pi,
}));
var ng = We(() => ({ ...fn(), ...Qp, ...cn, ...gs, ...rg, ...ig, ...Cf, ...yi, ...ki }));
var og = Ne((i, t, n) => {
  eg(i) &&
    (t.autofillEnabled =
      fe(n) && le(i.formInputAutofillEnabled)
        ? n.createDeduplicatedDynamicValueBinding(i.formInputAutofillEnabled, i, "autoFillEnabled")
        : i.resolveValue("formInputAutofillEnabled"));
});
var sg = Ne((i, t, n) => {
  if (!Rt(i) || !Hi(i)) return;
  if (i.formInputHidden === !0) {
    t.type = "hidden";
    return;
  }
  let a =
    fe(n) && le(i.formTextInputType)
      ? n.createDeduplicatedDynamicValueBinding(i.formTextInputType, i, "type")
      : (i.resolveValue("formTextInputType") ?? "text");
  if (((t.type = a), uc(i)))
    if (fe(n)) {
      t.value = re`${n.variables.create(i.variableBinding)} ?? ""`;
      let s = n.variables.createSetter(i.variableBinding.id),
        f = n.tree.getScopeNodeFor(i);
      if (!f || !Vr(f)) return;
      let g = f.getVariable(i.variableBinding.id);
      if (!g) return;
      g.type === "string"
        ? g.optional === !0
          ? ((t.onChange = re`(event) => ${dt.fn(s, re`event.target.value || undefined`)}`),
            (t.onClear = re`() => ${dt.fn(s, re`undefined`)}`))
          : ((t.onChange = re`(event) => ${dt.fn(s, re`event.target.value`)}`),
            (t.onClear = re`() => ${dt.fn(s, re`""`)}`))
        : g.type === "number"
          ? ((t.onChange = re`(event) => ${dt.fn(s, re`event.target.valueAsNumber`)}`),
            ie(g.options?.min) && (t.min = g.options.min),
            ie(g.options?.max) && (t.max = g.options.max),
            ie(g.options?.step) && (t.step = g.options.step))
          : g.type === "date" &&
            (t.onChange = re`(event) => ${dt.fn(s, re`event.target.value || undefined`)}`);
    } else
      t.defaultValue = i.cache.getVariableReferenceValue(i.variableBinding, n?.variableOverrides);
  else {
    let s =
      fe(n) && le(i.formInputHidden)
        ? n.createDeduplicatedDynamicValueBinding(i.formInputHidden, i, "inputHidden")
        : i.resolveValue("formInputHidden");
    t.type = Ur(s) ? re`${s} ? "hidden" : ${a}` : a;
    let u =
        fe(n) && le(i.formTextInputMinNumber)
          ? n.createDeduplicatedDynamicValueBinding(i.formTextInputMinNumber, i, "min")
          : i.resolveValue("formTextInputMinNumber"),
      f =
        fe(n) && le(i.formTextInputMaxNumber)
          ? n.createDeduplicatedDynamicValueBinding(i.formTextInputMaxNumber, i, "max")
          : i.resolveValue("formTextInputMaxNumber");
    ((t.min = ag(u, a)), (t.max = ag(f, a)));
    let g =
      fe(n) && le(i.formInputMaxLength)
        ? n.createDeduplicatedDynamicValueBinding(i.formInputMaxLength, i, "maxLength")
        : i.resolveValue("formInputMaxLength");
    t.maxLength = g;
  }
});
function ag(i, t) {
  if ((Ur(t) || t === "number") && !J(i))
    return t === "number" ? i : re`${t} === "number" ? ${i} : undefined`;
}
var K2 = Ne((i, t, n) => {
  fe(n) &&
    (!Rt(i) ||
      !Zn(i) ||
      i.formInputHidden === !0 ||
      (t.placeholder = le(i.formInputPlaceholder)
        ? n.createDeduplicatedDynamicValueBinding(i.formInputPlaceholder, i, "placeholder")
        : n.localizedValues.add(
            i.formInputPlaceholder,
            i.formInputPlaceholderLocalized,
            (a) => i.formInputPlaceholderLocalized?.[a]?.value
          )));
});
function lg(i, t, n) {
  if (!Rt(i) || !Zn(i) || i.formInputHidden === !0) return;
  let a = br(n, i.formInputPlaceholderLocalized);
  t.placeholder =
    le(i.formInputPlaceholder) || !a ? i.resolveValue("formInputPlaceholder") : a.value;
}
var dg = { formInputInvalidTextColor: void 0 },
  j2 = "rgba(153, 153, 153, 1)",
  Tx = "formInputInvalidTextColor";
function ug(i) {
  return Tx in i;
}
var _x = de((i, t, n) => {
  if (!Tf(i)) return;
  if (fe(n) && le(i.formTextAreaResizable)) {
    t["--framer-textarea-resize"] = n.cssVariableDefinitions.create(i, "formTextAreaResizable");
    return;
  }
  (i.resolveValue("formTextAreaResizable", n.variableOverrides) ?? !1) &&
    (t["--framer-textarea-resize"] = "vertical");
});
function Al(i, t) {
  if (!(J(i) || J(t))) return `${i}${t}`;
}
function kx(i) {
  if (!gi(i)) return Al(i, "px");
  let [t, n] = qe(i);
  return n === "rem" ? Bo(t) : i;
}
var Px = de((i, t, n) => {
    if (!Qn(i)) return;
    let a;
    if (i.formInputFontSelector) {
      let s = gu(i);
      (n.fonts?.addFontObjectBySelector(i.formInputFontSelector, s),
        (a = Ft(i.formInputFontSelector).fontStyle));
    }
    Object.assign(t, {
      "--framer-input-font-family": i.formInputFontFamily ? `"${i.formInputFontFamily}"` : void 0,
      "--framer-input-font-style": a === "normal" ? void 0 : a,
      "--framer-input-font-weight": i.formInputFontWeight,
      "--framer-input-font-size": kx(i.formInputFontSize),
      "--framer-input-font-letter-spacing": Al(
        i.formInputFontLetterSpacing,
        i.formInputFontLetterSpacingUnit
      ),
      "--framer-input-font-line-height": Al(
        i.formInputFontLineHeight,
        i.formInputFontLineHeightUnit
      ),
      "--framer-input-font-text-alignment": i.formInputFontTextAlignment,
      "--framer-input-font-open-type-features": ko(i.formInputOpenTypeFontFeatures, null),
      "--framer-input-font-variation-axes": i.formInputVariationAxes
        ? Wu(i.formInputVariationAxes)
        : void 0,
    });
  }),
  Ix = de((i, t) => {
    if (!i.cache.fontPreview?.fontSelector) return;
    let n = Ft(i.cache.fontPreview.fontSelector);
    (J(n.fontFamily) || (t["--framer-input-font-family"] = n.fontFamily),
      J(n.fontStyle) || (t["--framer-input-font-style"] = n.fontStyle),
      J(n.fontWeight) || (t["--framer-input-font-weight"] = n.fontWeight),
      J(n.fontVariationAxes) || (t["--framer-input-font-variation-axes"] = n.fontVariationAxes));
  }),
  wx = de((i, t, n) => {
    Qn(i) && (t["--framer-input-font-color"] = Ia(i.formInputFontColor, n));
  }),
  Wx = de((i, t, n) => {
    Zn(i) && (t["--framer-input-placeholder-color"] = Ia(i.formInputPlaceholderColor, n));
  }),
  Dx = de((i, t, n) => {
    ug(i) && (t["--framer-input-invalid-text-color"] = Ia(i.formInputInvalidTextColor, n));
  }),
  cg = Io(wx, Px, Ix, Dx, Wx, _x);
function ys(i) {
  return class fg extends i {
    static updateNodes;
    getFontsForLoading() {
      let n = [];
      return (this.formInputFontSelector && n.push(this.formInputFontSelector), n);
    }
    hasFontsToLoad() {
      return !!this.formInputFontSelector;
    }
    getProps(n, a) {
      let s = this.getFontsForLoading();
      Pt.loadFonts(s)
        .then(({ newlyLoadedFontCount: k }) => {
          k > 0 && fg.updateNodes?.([this.id]);
        })
        .catch(Be);
      let u = {},
        f = { ...super.getProps(n) };
      return (
        cg(this, u, { componentLoader: n, variableOverrides: a }),
        "style" in f && f.style ? Object.assign(f.style, u) : (f.style = u),
        f
      );
    }
  };
}
var Ss = ks(Hg(), 1);
var Ol = ks(Yl(), 1),
  zl = (i) => (t) => {
    let n = (0, Ss.useRef)(null),
      { center: a, style: s, inputName: u, groundNodeId: f, ...g } = t;
    id(t, n);
    let k = Zl(t),
      F = Jl(t),
      A = !F || k,
      R = {
        ...s,
        flex: s?.flex ?? "none",
        position: s?.position === "relative" ? "relative" : "absolute",
        ...Mx(t, F, k),
        "--framer-input-focused-transition": void 0,
      };
    return (
      Object.assign(R, yf(R.height)),
      (0, Ol.jsx)(i, {
        ref: n,
        ...g,
        inputName: f ? `${f}-${u}` : u,
        autofillEnabled: !1,
        transformTemplate: A ? rd(a) : void 0,
        style: R,
      })
    );
  },
  Nx = zl(Ad),
  BO = zl(Md),
  VO = zl(Ed);
function Rx(i) {
  let { node: t, tree: n } = i;
  if (t.bindingType !== "variable") return;
  ee(t.variableBinding, "Variable-bound FormSelectNode must have a variableBinding");
  let a = n.getScopeNodeFor(t);
  if (!a || !Vr(a)) return;
  let s = a.getVariable(t.variableBinding.id);
  if (s) {
    if (s.type === "boolean") return Fx(i);
    if (s.type === "collectionreference") return Bx(i, s);
    if (s.type === "controlReference" && s.expectedType === "enum") return Vx(i, s);
  }
}
function Fx({ activeLocale: i, combinedValueMap: t, node: n }) {
  ee(n.variableBinding, "Variable-bound FormSelectNode must have a variableBinding");
  let a = n.cache.getVariableReferenceValue(n.variableBinding, t),
    s;
  return (
    a === !0
      ? (s = br(i, n.booleanTrueLabelLocalized)?.value || n.booleanTrueLabelWithDefault)
      : a === !1
        ? (s = br(i, n.booleanFalseLabelLocalized)?.value || n.booleanFalseLabelWithDefault)
        : (s = br(i, n.allItemsLabelLocalized)?.value || n.allItemsLabelWithDefault),
    { defaultValue: s, selectOptions: [{ disabled: !1, title: s, type: "option", value: s }] }
  );
}
function Bx({ activeLocale: i, combinedValueMap: t, node: n, tree: a }, s) {
  ee(n.variableBinding, "Variable-bound FormSelectNode must have a variableBinding");
  let u = sr(a, s.dataIdentifier);
  if (!u) return;
  let f = n.cache.getVariableReferenceValue(n.variableBinding, t);
  if (!pe(f)) return s.optional === !0 ? mg(i, n) : void 0;
  let g = u.getTitleVariable()?.id,
    k = u.getSlugVariable()?.id;
  if (!k) return;
  let F = a.getNodeWithTrait(f, lo),
    A = g && F?.getControlProp(g)?.value,
    R = F?.getControlProp(k)?.value,
    $ = A || R;
  if (pe($)) return { defaultValue: $, selectOptions: [{ title: $, type: "option", value: $ }] };
}
function Vx({ activeLocale: i, combinedValueMap: t, node: n, tree: a }, s) {
  ee(n.variableBinding, "Variable-bound FormSelectNode must have a variableBinding");
  let u = n.cache.getVariableReferenceValue(n.variableBinding, t);
  if (!pe(u)) return s.optional === !0 ? mg(i, n) : void 0;
  let f = Lx(a, s, u, i);
  if (f) return { defaultValue: f, selectOptions: [{ title: f, type: "option", value: f }] };
}
function Lx(i, t, n, a) {
  if (!no(t.entityIdentifier)) return;
  let s = sr(i, t.entityIdentifier);
  if (!s) return;
  let u = s.variables.find((g) => g.type === "enum" && g.id === t.controlKey);
  if (!u) return;
  let f = u.cases.find((g) => g.id === n);
  if (f) return br(a, f.nameLocalized)?.value ?? f.name;
}
function mg(i, t) {
  let a = br(i, t.allItemsLabelLocalized)?.value || t.allItemsLabelWithDefault;
  return { defaultValue: a, selectOptions: [{ title: a, type: "option", value: a }] };
}
var LO = ({ activeLocale: i, combinedValueMap: t, componentLoader: n, node: a, tree: s, ...u }) => {
  let f = (0, Ss.useMemo)(
    () => Rx({ activeLocale: i, combinedValueMap: t, componentLoader: n, node: a, tree: s }),
    [i, t, n, a, s]
  );
  return (0, Ol.jsx)(Nx, { ...u, ...f });
};
function hg(i) {
  if (!(J(i) || sd(i)))
    switch (i) {
      case 6:
        return ed;
      case 7:
        return td;
      case 0:
      case 1:
      case 3:
      case 5:
      case 4:
      case 2:
        return;
      default:
        Pe(i);
    }
}
function Mx(i, t, n) {
  if (jl(i) && t && !n)
    return { bottom: void 0, height: t.height, left: t.x, right: void 0, top: t.y, width: t.width };
  let { width: a, height: s, left: u, top: f, bottom: g, right: k } = i;
  return { bottom: g, height: s, left: u, right: k, top: f, width: a };
}
var AO = () => ({
    id: Pn(),
    type: "option",
    value: void 0,
    title: void 0,
    titleLocalized: void 0,
    disabled: void 0,
  }),
  pg = { formSelectOptions: void 0 },
  Ex = "formSelectOptions";
function mn(i) {
  return Ex in i;
}
var gg = Ne((i, t, n) => {
  if (Rt(i)) {
    if (
      (i.formInputName
        ? (t.inputName =
            fe(n) && le(i.formInputName)
              ? n.createDeduplicatedDynamicValueBinding(i.formInputName, i, "inputName")
              : i.resolveValue("formInputName"))
        : (t.inputName = i.id),
      mn(i))
    )
      ((t.defaultValue = le(i.formInputValue)
        ? void 0
        : i.formSelectOptions?.find((a) => a.id === i.resolveValue("formInputValue"))?.value),
        Qn(i) && (t.textAlignment = i.formInputFontTextAlignment));
    else {
      let a = i.resolveValue("formInputValue"),
        s = a === "" ? void 0 : a;
      t.defaultValue =
        fe(n) && le(i.formInputValue)
          ? n.createDeduplicatedDynamicValueBinding(i.formInputValue, i, "defaultValue")
          : s;
    }
    if (i.formInputHidden === !0) {
      mn(i) && (t.hidden = !0);
      return;
    }
    ((t.autoFocus =
      fe(n) && le(i.formInputAutoFocus)
        ? n.createDeduplicatedDynamicValueBinding(i.formInputAutoFocus, i, "autoFocus")
        : i.resolveValue("formInputAutoFocus")),
      !(or(i) && i.formBooleanInputType === "radio") &&
        (t.required =
          fe(n) && le(i.formInputRequired)
            ? n.createDeduplicatedDynamicValueBinding(i.formInputRequired, i, "required")
            : i.resolveValue("formInputRequired")));
  }
});
var yg = de((i, t, n) => {
  if (!Mf(i)) return;
  let a = i.formInputFocusedBorderWidth !== void 0;
  Object.assign(t, {
    "--framer-input-focused-box-shadow": ka(
      i.formInputFocusedBoxShadow,
      "undefinedWhenEmpty",
      (s) => n.cssVariableDefinitions?.resolveColor?.(s)
    ),
    "--framer-input-focused-border-width": a ? i.formInputFocusedBorderWidth : void 0,
    "--framer-input-focused-border-style": a ? i.formInputFocusedBorderStyle : void 0,
    "--framer-input-focused-border-color": a ? vr(i.formInputFocusedBorderColor) : void 0,
    "--framer-input-focused-background": vr(i.formInputFocusedBackgroundColor),
    "--framer-input-focused-transition": So(i.formInputFocusedTransition)
      ? xo(i.formInputFocusedTransition)
      : void 0,
  });
});
var Sg = de((i, t, n) => {
  if (!tg(i)) return;
  let a = Ax(i.formInputIconImage, n),
    s = vr(i.formInputIconColor, st.toRgbString);
  ((t["--framer-input-icon-color"] = s),
    a
      ? ((t["--framer-input-icon-mask-image"] = s ? a : "none"),
        (t["--framer-input-icon-image"] = s ? void 0 : a))
      : ((t["--framer-input-icon-mask-image"] = s ? void 0 : "none"),
        (t["--framer-input-icon-image"] = void 0)),
    a && Hi(i) && bf(i.formTextInputType) && (t["--framer-input-icon-content"] = '""'));
});
function Ax(i, t) {
  if (!i) return;
  if (i.startsWith("data:image/svg+xml")) return `url('${i}')`;
  if (t.assets) {
    let a = t.assets.resolve(i)?.src;
    return a ? `url('${a}')` : void 0;
  }
  let n = ju(i);
  if (n) return `url('${Xl(n.identifier)}')`;
}
function xg(i) {
  return class extends Vt(Yi(i)) {
    updateForSize(n, a) {
      return wi(n, a, this.constraintValues());
    }
    updateForRect({
      rect: n,
      parentSizeInfo: a,
      constraintsLocked: s,
      shouldGuessPinToBottom: u,
      shouldGuessPinToRight: f,
      updateSize: g,
    }) {
      return Ii({
        rect: n,
        parentSizeInfo: a,
        currentConstraints: this.constraints(),
        constraintsLocked: s,
        pixelAlign: this.__unsafeIsGroundNode() || !this.usesDOMRectCached(),
        shouldGuessPinToBottom: u,
        shouldGuessPinToRight: f,
        updateSize: g,
        floatingPosition: null,
      });
    }
  };
}
function hn(i) {
  return class extends xg(i) {
    getActions() {
      let n = {};
      for (let a of Wd) {
        let s = this[a];
        if (!s || s.length === 0) {
          n[a] = void 0;
          continue;
        }
        n[a] = Ti(s);
      }
      return n;
    }
    getProps(n, a, s) {
      let u = {},
        f = {
          ...super.getProps(n),
          ...this.newConstraintProperties(),
          visible: this.resolveValue("visible", a),
          className: hg(this.cache.effectType),
          style: u,
        },
        g = { componentLoader: n, variableOverrides: a };
      return (
        $i(this, f),
        Fr(this, f),
        xh(this, u, g),
        Na(this, u, g),
        Mr(this, u, g),
        Ki(this, u, g),
        yg(this, u, g),
        zi(this, u, g),
        Rr(this, u, g),
        Ui(this, u, g),
        Oi(u, f),
        gg(this, f, g),
        Sg(this, u, g),
        Gi(this, u, g),
        Yf(this, u, g),
        Xi(this, u, g),
        Ta(this, f, g),
        f
      );
    }
  };
}
var vg = class extends Ie("FormPlainTextInputNode", ys(hn(De))) {
  constructor(t) {
    (super(), we(this, ng(), t), delete this.children);
  }
  getProps(t, n, a) {
    let s = {},
      u = { ...super.getProps(t) },
      f = { componentLoader: t, variableOverrides: n };
    return (
      og(this, u, f),
      sg(this, u, f),
      lg(this, u, a),
      "style" in u && u.style ? Object.assign(u.style, s) : (u.style = s),
      u
    );
  }
  supportsDynamicValues() {
    return !0;
  }
};
var bg = We(() => ({
  ...fn(),
  ...gs,
  ...cn,
  ...dg,
  ...pg,
  ...yi,
  ...Ai,
  ...ki,
  allItemsLabel: void 0,
  allItemsLabelLocalized: void 0,
  booleanTrueLabel: void 0,
  booleanTrueLabelLocalized: void 0,
  booleanFalseLabel: void 0,
  booleanFalseLabelLocalized: void 0,
}));
function l3(i) {
  return `${i}ToDisplayName`;
}
var Cg = "enumToDisplayNameFunctions";
function d3(i) {
  if (i?.type !== "enum" || !i.optionTitles) return re`(value) => value`;
  let t = i.optionTitles;
  return new dt(
    eu.lines(
      re`(value, _locale) => {`,
      re`switch (value) {`,
      ...i.options.map((n, a) => {
        let s = t[a];
        return re`case ${n}: return ${J(s) ? re`undefined` : s};`;
      }),
      re`default: return "";`,
      re`}`,
      re`}`
    )
  );
}
var Tg = Ne((i, t, n) => {
  if (!fe(n) || !co(i) || i.bindingType !== "variable") return;
  ee(i.variableBinding, "variable bindingType must have variableBinding set");
  let s = n.tree.getScopeNodeFor(i);
  if (!s || !Vr(s)) return;
  let u = s.getVariable(i.variableBinding.id);
  if (u)
    switch (u.type) {
      case "boolean":
        zx(i, t, n, u);
        break;
      case "collectionreference":
        Ox(i, t, n, u);
        break;
      case "controlReference":
        u.expectedType === "enum" && Hx(i, t, n, u);
        break;
    }
});
function zx(i, t, n, a) {
  ee(i.variableBinding, "variable bindingType must have variableBinding set");
  let s = n.imports.addImport("framer", { exportSpecifier: "useFormSelectVariableBinding" }),
    u = n.variables.create(i.variableBinding),
    f = n.variables.createSetter(i.variableBinding.id),
    g = a.optional === !0,
    k = new hi({ variableType: "boolean", isOptional: g, value: u, setValue: f });
  ((k.booleanTrueLabel = n.localizedValues.add(
    i.booleanTrueLabelWithDefault,
    i.booleanTrueLabelLocalized,
    (F) => i.booleanTrueLabelLocalized?.[F]?.value
  )),
    (k.booleanFalseLabel = n.localizedValues.add(
      i.booleanFalseLabelWithDefault,
      i.booleanFalseLabelLocalized,
      (F) => i.booleanFalseLabelLocalized?.[F]?.value
    )),
    Hl(i, t, n, k, g, s));
}
function Ox(i, t, n, a) {
  ee(i.variableBinding, "variable bindingType must have variableBinding set");
  let s = n.tree,
    u = sr(s, a.dataIdentifier);
  if (!u) return;
  let f = u.getTitleVariable()?.id,
    g = u.getSlugVariable()?.id;
  if (!g) return;
  let k = n.imports.addImport("framer", { exportSpecifier: "useFormSelectVariableBinding" }),
    F = ws(a.dataIdentifier),
    {
      importSpecifier: A,
      exportSpecifier: R,
      importBinding: $,
    } = _f(F, n.moduleType, n.componentLoader, n.modulesStore);
  if (!A) return;
  let q = n.imports.addImport(A, { exportSpecifier: R, importBinding: $ }),
    Z = n.variables.create(i.variableBinding),
    X = n.variables.createSetter(i.variableBinding.id),
    be = a.optional === !0,
    Q = new hi({
      collectionData: q,
      isOptional: be,
      slugId: g,
      titleId: f,
      value: Z,
      setValue: X,
      variableType: "collectionreference",
    });
  Hl(i, t, n, Q, be, k);
}
function Hx(i, t, n, a) {
  ee(i.variableBinding, "variable bindingType must have variableBinding set");
  let s = a.entityIdentifier,
    u = ws(s);
  if (!no(u)) return;
  let f = a.controlKey,
    g = n.imports.addModuleImport(u, { exportSpecifier: Cg });
  if (!g) return;
  let k = n.imports.addModuleImport(u, { importBinding: `${lf(f)}CollectionData` });
  if (!k) return;
  let F = n.imports.addImport("framer", { exportSpecifier: "useFormSelectVariableBinding" }),
    A = n.variables.create(i.variableBinding),
    R = n.variables.createSetter(i.variableBinding.id),
    $ = a.optional === !0,
    q = new hi({
      variableType: "enum",
      options: re`${k}.propertyControls?.[${f}]?.options`,
      getOptionTitle: re`${g}?.[${f}]`,
      isOptional: $,
      value: A,
      setValue: R,
    });
  Hl(i, t, n, q, $, F);
}
function Hl(i, t, n, a, s, u) {
  s &&
    (a.allItemsLabel = n.localizedValues.add(
      i.allItemsLabelWithDefault,
      i.allItemsLabelLocalized,
      (F) => i.allItemsLabelLocalized?.[F]?.value
    ));
  let [f, g, k] = n.declarations.component.dedupe(
    [`selectValue_${i.id}`, `selectOptions_${i.id}`, `selectOnChange_${i.id}`],
    dt.fn(u, a)
  );
  ((t.value = f), (t.selectOptions = g), (t.onChange = k));
}
var W3 = Ne((i, t, n) => {
    fe(n) &&
      mn(i) &&
      (!Us(i) ||
        i.bindingType !== "form" ||
        (t.selectOptions = i.formSelectOptions?.map((a) => {
          let { id: s, titleLocalized: u, title: f, ...g } = a;
          return { ...g, title: n.localizedValues.add(f, u, (k) => u?.[k]?.value) };
        })));
  }),
  _g = (i, t, n) => {
    if (!mn(i) || !Us(i) || i.bindingType !== "form") return;
    let a = i.formSelectOptions?.map((s) => {
      let { id: u, titleLocalized: f, title: g, ...k } = s;
      return { ...k, title: n ? (f?.[n.id]?.value ?? g) : g };
    });
    t.selectOptions = a;
  };
var kg = class extends Ie("FormSelectNode", ys(hn(De))) {
  get allItemsLabelWithDefault() {
    return this.allItemsLabel || Eo.allItemsLabel;
  }
  get booleanTrueLabelWithDefault() {
    return this.booleanTrueLabel || Eo.booleanTrueLabel;
  }
  get booleanFalseLabelWithDefault() {
    return this.booleanFalseLabel || Eo.booleanFalseLabel;
  }
  constructor(t) {
    (super(), we(this, bg(), t), delete this.children);
  }
  getProps(t, n, a) {
    let s = {},
      u = { ...super.getProps(t) };
    return (
      Tg(this, u, { componentLoader: t, variableOverrides: n }),
      _g(this, u, a),
      qi(this, s),
      "style" in u && u.style ? Object.assign(u.style, s) : (u.style = s),
      u
    );
  }
  supportsDynamicValues() {
    return !0;
  }
};
var H3 = { autoSize: !0 };
function G3(i) {
  return "autoSize" in i;
}
var U3 = "checkbox";
function X3() {
  return {
    type: "tween",
    delay: 0,
    duration: 0.3,
    ease: [0.44, 0, 0.56, 1],
    stiffness: 0,
    damping: 0,
    mass: 0,
  };
}
var Gx = "formBooleanInputType";
function K3(i) {
  return Gx in i;
}
var Pg = {
    formBooleanInputType: void 0,
    formBooleanInputValue: void 0,
    formBooleanInputCheckedFillColor: void 0,
    formBooleanInputCheckedBorderColor: void 0,
    formBooleanInputCheckedBorderWidth: void 0,
    formBooleanInputCheckedBorderStyle: void 0,
    formBooleanInputCheckedBoxShadow: void 0,
    formBooleanInputCheckedTransition: void 0,
  },
  $x = 1,
  Y3 = () => ({
    formBooleanInputCheckedFillColor: "rgb(255, 255, 255, 1)",
    formBooleanInputCheckedBorderWidth: `${$x}px`,
    formBooleanInputCheckedBorderColor: "rgba(0, 153, 255, 1)",
    formBooleanInputCheckedBorderStyle: "solid",
    formBooleanInputCheckedBoxShadow: [
      vu({
        type: "box",
        color: "rgb(0, 153, 255)",
        x: 0,
        y: 0,
        inset: !0,
        blur: 0,
        spread: 5,
        diffusion: 0.5,
        focus: 0.5,
      }),
    ],
  }),
  j3 = { formBooleanInputCheckedFillColor: "rgba(0, 153, 255, 1)" },
  J3 = { formInputIconColor: "rgba(255, 255, 255, 1)" };
var Ig = We(() => ({ ...fn(), ...Pg, ...cn, ...ki }));
var wg = re`(a, b) => {
	if (typeof a === "string" && typeof b === "string") {
		return a.toLowerCase().includes(b.toLowerCase());
	}

	if (Array.isArray(a) && typeof b === "string") {
		return a.includes(b);
	}

	return false;
};
`;
var Wg = re`(list, value) => {
	if (typeof value !== "string") return list;
	if (!Array.isArray(list) || !list.every(item => typeof item === "string")) return [value];

	const filtered = list.filter(item => item !== value);
	if (filtered.length === list.length) {
		return [...list, value];
	}
	return filtered;
};
`;
var Ng = Ne((i, t, n) => {
  if (!or(i)) return;
  let a =
    fe(n) && le(i.formInputHidden)
      ? n.createDeduplicatedDynamicValueBinding(i.formInputHidden, i, "inputHidden")
      : i.resolveValue("formInputHidden");
  if (a === !0) t.type = "hidden";
  else {
    let s = i.resolveValue("formBooleanInputType") ?? "checkbox";
    t.type = Ur(a) ? re`${a} ? "hidden" : ${s}` : s;
  }
  switch (i.bindingType) {
    case "form": {
      (ee(!i.variableBinding, "form bindingType should not have variableBinding set"),
        fe(n)
          ? (t.defaultChecked = le(i.formBooleanInputValue)
              ? n.createDeduplicatedDynamicValueBinding(i.formBooleanInputValue, i, "checked")
              : i.resolveValue("formBooleanInputValue"))
          : (t.defaultChecked = i.resolveValue("formBooleanInputValue")));
      break;
    }
    case "variable": {
      ee(i.variableBinding, "variable bindingType must have variableBinding set");
      let s = i.tree();
      ee(s, "Node must have a tree");
      let u = s.getScopeNodeFor(i);
      if (!u || !Vr(u)) return;
      let f = u.getVariable(i.variableBinding.id);
      if (!f) return;
      if (f.type === "multicollectionreference") {
        let g = Mu(s, i);
        if (
          !g?.inRepeatedChild ||
          !gn(g.repeater) ||
          g.repeater.dataIdentifier !== f.dataIdentifier
        )
          return;
        if (fe(n)) {
          let k = n.variables.create(i.variableBinding),
            F = n.variables.create(Rs("id", g.repeater.getPrimaryId())),
            A = n.variables.createSetter(i.variableBinding.id),
            R = n.declarations.file.dedupe("contains", wg),
            $ = n.declarations.file.dedupe("toggleInList", Wg);
          ((t.checked = re`${R}(${k}, ${F})`),
            (t.onChange = re`() => ${dt.fn(A, re`${$}(${k}, ${F})`)}`));
        } else {
          let k = i.cache.getVariableReferenceValue(i.variableBinding, n?.variableOverrides);
          if (!ad(k)) return;
          let F = Rs("id", g.repeater.getPrimaryId()),
            A = i.cache.getVariableReferenceValue(F, n?.variableOverrides);
          t.defaultChecked = k.includes(A);
        }
      } else if (f.type === "boolean")
        if (fe(n)) {
          t.checked = n.variables.create(i.variableBinding);
          let g = n.variables.createSetter(i.variableBinding.id);
          f.optional === !0
            ? (t.onChange = re`(event) => ${dt.fn(g, re`event.target.checked ? true : undefined`)}`)
            : (t.onChange = re`(event) => ${dt.fn(g, re`event.target.checked`)}`);
        } else
          t.defaultChecked = i.cache.getVariableReferenceValue(
            i.variableBinding,
            n?.variableOverrides
          );
      break;
    }
    default:
      Pe(i.bindingType);
  }
});
var qx = de((i, t, n) => {
    if (!or(i)) return;
    let a = i.formBooleanInputCheckedBorderWidth !== void 0;
    Object.assign(t, {
      "--framer-input-boolean-checked-border-width": a
        ? i.formBooleanInputCheckedBorderWidth
        : void 0,
      "--framer-input-boolean-checked-border-color": a
        ? vr(i.formBooleanInputCheckedBorderColor, st.toRgbString)
        : void 0,
      "--framer-input-boolean-checked-border-style": a
        ? i.formBooleanInputCheckedBorderStyle
        : void 0,
      "--framer-input-boolean-checked-box-shadow": ka(
        i.formBooleanInputCheckedBoxShadow,
        "undefinedWhenEmpty",
        (s) => n.cssVariableDefinitions?.resolveColor?.(s)
      ),
      "--framer-input-boolean-checked-background": vr(
        i.formBooleanInputCheckedFillColor,
        st.toRgbString
      ),
    });
  }),
  Ux = de((i, t) => {
    if (!or(i)) return;
    let n = i.formBooleanInputCheckedTransition;
    t["--framer-input-boolean-checked-transition"] = So(n) ? xo(n) : void 0;
  }),
  Rg = Io(qx, Ux);
var Fg = class extends Ie("FormBooleanInputNode", hn(De)) {
  constructor(t) {
    (super(t), we(this, Ig(), t), delete this.children);
  }
  getProps(t, n) {
    let a = {},
      s = { ...super.getProps(t) },
      u = { componentLoader: t, variableOverrides: n };
    return (
      Ng(this, s, u),
      Rg(this, a, u),
      "style" in s && s.style ? Object.assign(s.style, a) : (s.style = a),
      s
    );
  }
  supportsDynamicValues() {
    return !0;
  }
};
export {
  Va as a,
  Qf as b,
  Gg as c,
  $g as d,
  qg as e,
  ev as f,
  Xg as g,
  iv as h,
  Fn as i,
  Kg as j,
  sv as k,
  lv as l,
  ur as m,
  ol as n,
  vv as o,
  Bn as p,
  Vn as q,
  Bv as r,
  Ea as s,
  ll as t,
  um as u,
  eb as v,
  gb as w,
  vm as x,
  yb as y,
  Sb as z,
  Cm as A,
  km as B,
  Pm as C,
  wb as D,
  sy as E,
  Im as F,
  uy as G,
  cy as H,
  Wb as I,
  Db as J,
  Nb as K,
  Rb as L,
  Fb as M,
  za as N,
  Vb as O,
  Eb as P,
  Ab as Q,
  Ob as R,
  Gb as S,
  $b as T,
  qb as U,
  Zb as V,
  Qb as W,
  Ve as X,
  xy as Y,
  vy as Z,
  eC as _,
  pt as $,
  nC as aa,
  oC as ba,
  wy as ca,
  aC as da,
  dC as ea,
  Dy as fa,
  uC as ga,
  Ry as ha,
  fC as ia,
  Fm as ja,
  pC as ka,
  yC as la,
  SC as ma,
  bC as na,
  CC as oa,
  TC as pa,
  Ua as qa,
  v_ as ra,
  b_ as sa,
  C_ as ta,
  T_ as ua,
  __ as va,
  k_ as wa,
  P_ as xa,
  I_ as ya,
  w_ as za,
  Lm as Aa,
  Mm as Ba,
  Em as Ca,
  Am as Da,
  zm as Ea,
  mk as Fa,
  Om as Ga,
  $m as Ha,
  qm as Ia,
  Um as Ja,
  Ln as Ka,
  Mr as La,
  Km as Ma,
  Ym as Na,
  pl as Oa,
  en as Pa,
  CS as Qa,
  Yw as Ra,
  tn as Sa,
  xh as Ta,
  bh as Ua,
  cr as Va,
  Qi as Wa,
  Tt as Xa,
  Dh as Ya,
  Za,
  xS as _a,
  yh as $a,
  $w as ab,
  Sh as bb,
  qw as cb,
  Tl as db,
  aD as eb,
  es as fb,
  Gn as gb,
  kl as hb,
  $n as ib,
  Zi as jb,
  gl as kb,
  m0 as lb,
  Sl as mb,
  qn as nb,
  ns as ob,
  Gh as pb,
  Un as qb,
  xl as rb,
  ih as sb,
  J0 as tb,
  Z0 as ub,
  Q0 as vb,
  sh as wb,
  Vl as xb,
  Rp as yb,
  jh as zb,
  eF as Ab,
  Xn as Bb,
  Eh as Cb,
  Re as Db,
  Ya as Eb,
  ja as Fb,
  uI as Gb,
  dS as Hb,
  pI as Ib,
  An as Jb,
  gI as Kb,
  yI as Lb,
  On as Mb,
  mh as Nb,
  hh as Ob,
  yS as Pb,
  CI as Qb,
  wt as Rb,
  xN as Sb,
  Ah as Tb,
  ts as Ub,
  DN as Vb,
  Oh as Wb,
  is as Xb,
  lR as Yb,
  Xh as Zb,
  os as _b,
  YR as $b,
  tp as ac,
  as as bc,
  MF as cc,
  zF as dc,
  Kn as ec,
  MS as fc,
  OF as gc,
  ip as hc,
  np as ic,
  Yn as jc,
  cp as kc,
  ls as lc,
  LB as mc,
  hp as nc,
  pp as oc,
  ds as pc,
  eV as qc,
  tV as rc,
  pV as sc,
  gV as tc,
  OS as uc,
  yV as vc,
  yp as wc,
  Sp as xc,
  xp as yc,
  vp as zc,
  XV as Ac,
  KV as Bc,
  YV as Cc,
  Rl as Dc,
  Tp as Ec,
  _p as Fc,
  kp as Gc,
  Ip as Hc,
  QS as Ic,
  tL as Jc,
  rL as Kc,
  Bl as Lc,
  iL as Mc,
  nL as Nc,
  oL as Oc,
  aL as Pc,
  sL as Qc,
  Vp as Rc,
  ax as Sc,
  Lp as Tc,
  sx as Uc,
  H3 as Vc,
  G3 as Wc,
  Hp as Xc,
  dx as Yc,
  un as Zc,
  U3 as _c,
  X3 as $c,
  K3 as ad,
  $x as bd,
  Y3 as cd,
  j3 as dd,
  J3 as ed,
  tg as fd,
  kz as gd,
  Pz as hd,
  wg as id,
  Wg as jd,
  Ng as kd,
  qx as ld,
  Ux as md,
  BO as nd,
  VO as od,
  LO as pd,
  Lz as qd,
  Qn as rd,
  AO as sd,
  mn as td,
  gg as ud,
  yg as vd,
  Sg as wd,
  Fg as xd,
  eg as yd,
  Zn as zd,
  Nz as Ad,
  Rz as Bd,
  Bz as Cd,
  og as Dd,
  sg as Ed,
  K2 as Fd,
  j2 as Gd,
  ug as Hd,
  _x as Id,
  Px as Jd,
  wx as Kd,
  Wx as Ld,
  Dx as Md,
  vg as Nd,
  l3 as Od,
  Cg as Pd,
  d3 as Qd,
  Tg as Rd,
  W3 as Sd,
  kg as Td,
  Ep as Ud,
  dn as Vd,
  ln as Wd,
  cx as Xd,
  SA as Yd,
  Ml as Zd,
  mx as _d,
  bA as $d,
  hx as ae,
  El as be,
  PA as ce,
  qp as de,
  Up as ee,
  Xp as fe,
  Qr as ge,
  Kp as he,
  px as ie,
  gx as je,
  ps as ke,
  qA as le,
  jp as me,
  Zp as ne,
};
/*! Bundled license information:

paper/dist/paper-core.js:
  (*!
   * Paper.js v0.11.5-viewless - The Swiss Army Knife of Vector Graphics Scripting.
   * http://paperjs.org/
   *
   * Copyright (c) 2011 - 2016, Juerg Lehni & Jonathan Puckey
   * http://scratchdisk.com/ & http://jonathanpuckey.com/
   *
   * Distributed under the MIT license. See LICENSE file for details.
   *
   * All rights reserved.
   *
   * Date: Thu Dec 21 13:44:02 2017 +0100
   *
   ***
   *
   * Straps.js - Class inheritance library with support for bean-style accessors
   *
   * Copyright (c) 2006 - 2016 Juerg Lehni
   * http://scratchdisk.com/
   *
   * Distributed under the MIT license.
   *
   ***
   *
   * Acorn.js
   * http://marijnhaverbeke.nl/acorn/
   *
   * Acorn is a tiny, fast JavaScript parser written in JavaScript,
   * created by Marijn Haverbeke and released under an MIT license.
   *
   *)
*/
//# sourceMappingURL=chunk-K7LC3A2W.mjs.map
